// Plan 59.1 — arity 3 mono'd tuple.
module nova_tests.plan59

fn[T] triple(v T) -> (T, T, T) => (v, v, v)

test "triple int" {
    ro (a, b, c) = triple[int](5)
    assert(a == 5)
    assert(b == 5)
    assert(c == 5)
}
