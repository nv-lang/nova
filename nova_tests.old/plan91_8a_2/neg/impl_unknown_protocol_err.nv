// EXPECT_COMPILE_ERROR E_UNKNOWN_PROTOCOL

module neg.impl_unknown_protocol_err

// Plan 91.9: #impl(NonExistentProtocol) — protocol name не найдено.

#impl(NonExistentProtocol)
type Foo { x int }

test "marker" {
    assert(true)
}
