// Plan 42 D29 rev-3 + правило F: folder-module с test-only peer.
// `helpers_test.nv` имеет suffix `_test` → test-only.

module modules.folder_module_with_test

export fn double(x int) -> int => x * 2
