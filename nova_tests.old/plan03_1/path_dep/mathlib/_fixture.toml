# Маркер: каталог — не корпус тестов, а дерево-зависимость для
# межпакетного резолва (Plan 03.1 Ф.3). `walk_nv` пропускает каталог —
# его модули компилируются только при импорте из пакета-потребителя.
