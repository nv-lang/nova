// Plan 125 Ф.3: trailing — пользовательская fn -> never.

module nova_tests.plan125

fn diverge(msg str) -> never {
    panic(msg)
}

fn rank1(c bool) -> int {
    if c {
        diverge("bad")
    } else {
        42
    }
}

test "user-never trailing in then" {
    assert(rank1(false) == 42)
}
