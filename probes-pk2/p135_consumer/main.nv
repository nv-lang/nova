// pk2-probe №135 часть 2: манифест ЗАВИСИМОСТИ (p135_leaf) несёт голый
// path-деп на bignum (выходящий за границу git-репо потребителя). Проверка:
// видит ли `nova check` потребителя (p135_consumer) этот ТРАНЗИТИВНЫЙ
// path-деп чужого манифеста, или молчит (как утверждает реестр).
module p135_consumer

import p135_leaf.{nzp135_leaf_zero}

fn main() {
    ro nzp135_z = nzp135_leaf_zero()
    println("leaf zero ok")
}
