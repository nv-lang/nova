# PROGRESS — p-chan244 (план 244, Ф.0 + Ф.1)

Worktree: `d:/Sources/nv-lang/nova-chan244`, ветка `pchan244`, база — main
`2ced70f2e` (влитие «план(244): УТВЕРЖДЁН владельцем 2026-08-03»).

Машина: AMD Ryzen 7 5800H, 16 логических ядер, Windows 11, release-сборка
(`cargo build --release`), не изолированная (без cpupower/affinity — числа
дают только ОТНОСИТЕЛЬНОЕ сравнение Ф.0 vs Ф.3 на этой же машине, не
абсолютную истину — см. `docs/dev/bench-conventions.md` «Reproducibility»).

---

## Ф.0 — базовый замер (ЭТАЛОН, снят ДО первой строки Ф.1)

### Инфраструктурная находка (документирую, НЕ чиню — окно кодогенератор не трогает)

`nova bench run` **ICE на ЛЮБОМ файле**, включая нетронутые прекогденты
(`bench/micro/hello.nv`, `bench/m_n/handler_chain_pingpong.nv`), в этой
release-сборке (`cargo build --release`, коммит 2ced70f2e):

```
nova: internal error at D:\...\compiler-codegen\src\codegen\emit_c.rs:60405:
[P67-LEGACY] Index element type unknown for obj_ty="_NovaFixArr_4_9_nova_byte"
— checker must annotate (compiler-conventions.md §0);
expr.span=Span { start: 4420, end: 4426, file_id: 4 } expr.id=ExprId(358) src=<unknown>
This is a bug in nova. Please report it.
```

Воспроизведено `nova bench run bench/micro/hello.nv`, `nova bench run
bench/micro/arith.nv`, `nova bench run bench/m_n/handler_chain_pingpong.nv` —
все три падают ИДЕНТИЧНО (тот же `expr.id=ExprId(358)`, `file_id=4`,
`src=<unknown>` → похоже на внутренний bench-driver harness, не на
пользовательский файл). Значит `nova bench run` целиком недоступен ДО
отдельного фикса компилятора (вне границ этого окна — только `std/**`).
**Не пытался чинить** (компиляторное окно занято другой волной).

**Обходной путь для Ф.0** (не заменяет DSL-корпус, а даёт числа): написан
ручной харнесс `bench/m_n/chan244_manual_probe.nv` — `fn main()`,
`bench.now_ns()` работает и вне `measure`-блока, тайминги печатаются
`println`. Прогрев (2 повтора отброшены) + 7 повторов (латентность — 14) на
конфигурацию, разброс — ниже. DSL-корпус (`bench/m_n/chan244_*.nv`, тот же
API, что `handler_chain_pingpong.nv`) **тоже положен в репозиторий** — он
типа-корректен (`nova check` — ok), но исполнить его `nova bench run`
сегодня нельзя; Ф.3 использует его, как только ICE починят отдельно.

### Побочная находка риска 5 (см. «Риски» ниже) — важно для Ф.1/Ф.2

Тот же прогон вскрыл: канонический пример из `docs/guide/channels.md`
(«producer-consumer pipeline», `mut sum = 0` захвачен ДВУМЯ `spawn`) **не
компилируется** вне `bench`-блока — `E_CONCURRENT_MUT_CAPTURE` (Plan
173.3/D415 §2), а НЕ №289. Внутри `bench "..." { measure { ... } }` тот же
буквальный паттерн (`handler_chain_pingpong.nv`, `spawn_microbench.nv`)
проходит `nova check` без ошибок — расхождение между bench-контекстом и
обычным `test`/`fn`. Проверено тремя минимальными репро (`test`-блок,
`fn main()`, буквальная копия bench-тела в `test`-блоке — все три
отклонены с одинаковым `E_CONCURRENT_MUT_CAPTURE`). **Это не №289** (текст
ошибки ссылается на Plan 173.3, более старое правило) — похоже, давно
существующий разрыв между документированной идиомой и энфорсом, а не
регрессия сегодняшнего слияния. Не чиню (вне границ Ф.0/Ф.1: это либо
компилятор, либо доки — оба вне `std/**`), докладываю интегратору.
Ф.0-харнесс обошёл его штатно рекомендованным способом из текста самой
ошибки — `AtomicInt` вместо голого `mut int` для счётчиков/таймстампов,
разделяемых между `spawn`.

### Сценарий 1 — пропускная способность (producer/consumer, N=50 000 элементов/замер)

Идиома — 1:1 `handler_chain_pingpong.nv` (два `spawn` в `supervised`),
только с переменной ёмкостью. Сырые `elapsed_ns` — 7 повторов на
конфигурацию (2 прогревочных отброшены), `bench/m_n/chan244_manual_probe.nv`.

| cap | сырые elapsed_ns (7 повторов) | min | median | mean | elem/s (median) | elem/s (mean) |
|---|---|---|---|---|---|---|
| 1 | 483232700, 436636900, 548824300, 494540200, 449925000, 368804500, 403178600 | 368.80ms | 449.93ms | 455.02ms | **111 130/с** | 109 884/с |
| 16 | 77117200, 80884400, 85208600, 72845000, 78813100, 82633500, 84571100 | 72.85ms | 80.88ms | 80.30ms | **618 163/с** | 622 697/с |
| 1024 | 15670300, 15633500, 15641000, 19672700, 16712100, 15730300, 15442700 | 15.44ms | 15.67ms | 16.36ms | **3 190 782/с** | 3 057 690/с |

Разброс cap=1 заметный (368–549мс, ~33%) — машина не изолирована
(cpupower/affinity не применялись, см. шапку); cap=16/1024 — разброс
заметно меньше (≤17%). Тренд ожидаемый: больше буфер → меньше
парковок/будильников на элемент → выше throughput (cap=1024 почти в 29×
быстрее cap=1).

### Сценарий 2 — задержка на пустом канале (recv уже припаркован, cap=1)

`Time.sleep(2ms)` в fiber-отправителе ГАРАНТИРУЕТ, что receiver успел
запарковаться до `send` (в измеряемую дельту сам sleep не входит — тайминг
берётся `bench.now_ns()` СРАЗУ после sleep, перед `send`, и в момент
пробуждения получателя). 14 повторов (2 прогревочных отброшены), сырые
`delta_ns`:

```
22800, 28400, 23200, 26500, 25000, 23600, 25300, 25700,
15100, 64400, 29300, 29100, 36400, 23800
```

min = **15 100 нс**, max = 64 400 нс (один выброс — вероятно, планировщик
переключил поток; без него max ≈ 36 400 нс), median = **25 500 нс**, mean =
**28 471 нс**.

### Сценарий 3 — `select`, 2/4/8 ветвей, одна готова (N=200 000 итераций/замер)

Один fiber, без парковки: канал 0 доливается перед каждым `select`,
остальные N−1 каналов всегда пусты (чистая стоимость сканирования
Fisher-Yates + розыгрыша). 7 повторов на конфигурацию.

| ветвей | сырые elapsed_ns (7 повторов) | min | median | mean | итер/с (median) | итер/с (mean) |
|---|---|---|---|---|---|---|
| 2 | 22000100, 22103000, 22108900, 22094200, 22813000, 22677100, 22471100 | 22.00ms | 22.11ms | 22.32ms | **9 047 000/с** | 8 958 000/с |
| 4 | 40122400, 39370800, 50430900, 45471900, 45508200, 39614000, 41600200 | 39.37ms | 41.60ms | 43.16ms | **4 807 676/с** | 4 634 004/с |
| 8 | 68348300, 70398400, 78494600, 68934000, 67866800, 66940600, 74170700 | 66.94ms | 68.93ms | 70.74ms | **2 901 822/с** | 2 827 013/с |

Тренд ожидаемый — стоимость почти линейна по числу ветвей (2→4 ≈ ×1.9,
4→8 ≈ ×1.5 по median), согласуется с O(N) сканированием арм в
`nova_select_try_immediate`.

### Файлы Ф.0

- `bench/m_n/chan244_manual_probe.nv` — фактический источник чисел выше
  (обходной путь ICE, см. находку).
- `bench/m_n/chan244_throughput.nv`, `bench/m_n/chan244_latency.nv`,
  `bench/m_n/chan244_select.nv` — тот же API, что `handler_chain_pingpong.nv`
  (bench-DSL, `nova check` — ok); ждут фикса ICE `nova bench run`, чтобы
  Ф.3 могла прогнать буквально ЭТИ файлы через `bench diff`.

---

## Ф.1 — `Chan[T]` на Nova

См. отдельный раздел ниже по итогам реализации.
