p259-gate-speed — журнал замеров (чекпоинт-файл окна, не отчёт)
==============================================================
Бинарь: копия nova.exe из главной репы (коммит 8e4038cbe, 2026-08-09 13:20),
        лежит в .p259/nova.exe — чтобы пересборка гейтом не подменила его в
        середине замера.
Рантайм/GC: NOVA_CG_INCLUDE/NOVA_RT_DIR/NOVA_GC_* -> главная репа (.p259/env.sh).
НАГРУЗКА МАШИНЫ: в момент всех замеров идёт гейт (есть /tmp/gate_full.log без
        .done) + 4 параллельных окна; wmic loadpercentage 50% при 16 логич.
        ядрах. measure.sh ОТКАЗЫВАЕТСЯ мерить в таких условиях (ворота 1) —
        поэтому замеры сняты напрямую, и КАЖДАЯ цифра ниже помечена нагрузкой.

--------------------------------------------------------------------------
[1] СТРУКТУРА ГЕЙТОВОГО ШАГА "мега-CU"
    Команда: nova test --positive --compile-error spec_tests/conformance
    --list даёт 781 РАБОТУ (job), а не одну:
      * 1 job  = folder-module spec_tests.conformance -> ОДИН слитый CU из
                 1161 top-level .nv (все объявляют `module spec_tests.conformance`),
                 entry = алфавитно первый peer `a_q3_println_debug_record`;
      * 6 job  = top-level `*_slow.nv` (отдельные CU);
      * 774 job= подкаталоги spec_tests/conformance/*/ (каждый — свой CU).
    Время самого `--list` (только обход+чтение заголовков): 28.9 с (1 прогон,
    под нагрузкой).

[2] МЕГА-CU (job a_q3_println_debug_record), --jobs 1, NOVA_PERF_TIMER=1
    Прогон ОБОРВАН по 10-мин потолку команды -> сам job > 600 с.
    Маркеры __PERF__, успевшие напечататься (ns -> с):
       imports-resolve          99.066 с   <-- доминирует фронтенд
       field-cache               3.087 с
       embed-resolve             3.076 с
       type-check                2.507 с
       post-normalize-annotate   2.363 с
       verify                    2.248 с
       const-fn-rewrite          0.100 с
       lints                     0.063 с
       alpha-rename              0.055 с
       callnorm                  0.049 с
       annotate-maps             0.077 с
       auto-derive-inject        0.015 с
       desugar                   0.016 с
       effects-infer             0.002 с
       const-fn-mono             0.0002 с
       parse                     0.0005 с (только entry-файл; peers парсятся
                                          внутри imports-resolve)
       ИТОГО фронтенд до codegen  ~112.7 с
    Маркер `codegen` НЕ напечатан за оставшиеся ~480 с -> кодогенерация
    мега-CU идёт БОЛЬШЕ 8 минут. Файл .c на диск не лёг (проверено).

[3] ПРОФИЛЬ ПОСЛЕДНЕГО РЕАЛЬНОГО ГЕЙТА (главная репа, /tmp/gate_full.log,
    завершился 2026-08-09 ~14:17, RC=0, SEC=2309)
      1619 с  mega-CU              (старт +547)
       351 с  самотесты стражей    (старт +194)
       123 с  check std/src        (старт +2166)
        55 с  doc-conventions
        45 с  invariant-discipline
        34 с  doc-truth
        16 с  flagship aggregator
        11 с  doc-examples
         9 с  форма записей реестра
         8 с  doc-hygiene / 8 с ci-status
         6 с  single-mco-resume / 6 с marker-registry-sync
         3 с  test-fixture-coverage
         2 с  sync-guards / 2 с cargo build (инкрементально)
       <=1 с  остальные 8 шагов
    ВНИМАНИЕ: цифры doc-* здесь ВТРОЕ-ВДЕСЯТЕРО меньше, чем в брифе
    (114/121/87 с) — те снимались под другой нагрузкой. Разброс между
    прогонами гейта у этих шагов огромный.

[4] ГЛАВНОЕ: ЧТО ИМЕННО ЗАНИМАЕТ 1619 с ШАГА mega-CU
    Из лога /tmp/gate_mega_43292.log, секция "SLOWEST TESTS":
       1613.597 с  spec_tests/conformance/a_q3_println_debug_record  <-- СЛИТЫЙ CU
         63.027 с  spec_tests/conformance/neg/neg_override_str_to_lower
         61.360 с  ... и далее ~60 с у каждого из top-10 neg/*
    Итог прогона: PASS 716 / FAIL 0 / SKIP 65.
    => ОДНА работа (слитый folder-module CU) = 1613.6 из 1619 с шага (99.7%).
       Остальные 780 работ идут ПАРАЛЛЕЛЬНО с ней (16 воркеров) и целиком
       прячутся под её тенью.

[5] ПОДКОРПУСА (слитый CU из первых N файлов conformance + peer с объединением
    std-импортов; --jobs 1; агрегируется только первая работа)
    N     .c(МБ)  nova(fe+cg)  imports  codegen  compile_ms  cc+link  run
     20    2.93       5.1 с     1.9 с    0.8 с      6.4 с      1.3 с  0.43 с
    100    3.90       7.6 с     3.0 с    2.1 с      9.5 с      1.9 с  0.52 с
    200    5.92     132.0 с    49.8 с   76.9 с    234.9 с    102.9 с  1.33 с
    400    9.06     213.9 с    39.2 с  166.9 с    245.9 с     32.0 с  1.07 с
    (замеры 200/400 сняты ПОД ИДУЩИМ ГЕЙТОМ — см. расхождение imports/cc+link
     между ними; требуют повтора, см. ниже)
