---
name: save
description: "сохранить контекст сессии в ролевую записку — адаптер .claude/commands/save.md"
type: prompt
disableModelInvocation: true
---

**Это АДАПТЕР, а не вторая копия.** Источник правды — `.claude/commands/save.md`.
Прочитай его целиком и следуй ему, заменяя:

- Запись в записку → инструментами Edit/Write (команды `/save` в Kimi Code нет).
  Раздел СВЕРХУ под шапкой, метка `## 0-ГГГГ-ММ-ДД[-<часть суток>]` из `date`.
- Таблица адресатов сохраняется: `main` → `integrator-handoff.md`,
  `p274-novac` → `carina-handoff.md`, контролёр → `controller-handoff.md`,
  помощник → `assistant-handoff.md`. Помощнику визитка не нужна — адресат по роли.
- Проверка уникальности метки перед правкой → через `grep` по файлу, как в
  исходнике.
- Содержимое раздела — пять частей из исходника, взятых из дерева (`git log`,
  `git status`).
- Хук `show-local-time.py` → не приезжает; время в докладе — `date "+%H:%M"`
  непосредственно перед докладом.
- Правка записки скриптом с CRLF → в Kimi Code проще через Edit/Write; если
  пишешь скриптом — одна подстановка перевода строки, как в исходнике, и
  проверка `check-guard-honesty.py`.

$ARGUMENTS
