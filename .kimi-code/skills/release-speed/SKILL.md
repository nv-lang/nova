---
name: release-speed
description: "релиз Карины: скорость приближения, простои, тормоза и шаги ускорения — адаптер .claude/commands/release-speed.md"
type: prompt
disableModelInvocation: true
---

**Это АДАПТЕР, а не вторая копия.** Источник правды —
`.claude/commands/release-speed.md`. Прочитай его целиком и следуй ему.

Под Kimi Code CLI замеры git и стражей запускаются через Bash. `gate-bg.sh` —
через Bash (интегратору, в фоне, см. `/integrator`). Никаких дополнительных
подстановок.

$ARGUMENTS
