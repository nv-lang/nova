// [DETECT-U.1.3] probe: str-методы (parse_int/pad) без import std.runtime.string.
module detect172

test "parse_int + pad без import" {
    assert("42".parse_int() == 42)
    assert("7".pad_start(3, ' ') == "  7")
}
