// EXPECT_COMPILE_ERROR E7301
// U.5.2: category mismatch str -> int param (cat_compatible_rt → Bad → E7301).
module neg.n_catmismatch
fn take_int(x int) -> int => x
test "t" { ro s str = "hi"  assert(take_int(s) >= 0) }
