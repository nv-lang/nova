// [DETECT-U.1.3] probe: Vec ops (raw_mem транзитивно через prelude) без import.
module detect172

test "Vec push/len/index без import" {
    mut v = Vec[int].new()
    v.push(1)
    v.push(2)
    v.push(3)
    assert(v.len() == 3)
    assert(v[0] == 1)
    assert(v[2] == 3)
}
