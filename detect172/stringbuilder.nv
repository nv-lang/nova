// [DETECT-U.1.3] probe: StringBuilder без import (prelude re-export).
module detect172

test "StringBuilder append/into_str без import" {
    consume sb = StringBuilder.new()
    sb.append("hello")
    sb.append(" world")
    ro s = sb.into_str()
    assert(s == "hello world")
}
