---
description: "перепроверить работу по novac: архитектура, доноры, запрет оракула, плюс общая часть"
argument-hint: "[что перепроверять; по умолчанию — работа текущей сессии]"
allowed-tools: "Bash, Read, Grep, Glob"
---

Перепроверь выполненную работу по `novac`: $ARGUMENTS
Если аргумент пуст — работу этой сессии.

**СНАЧАЛА — общая часть: @docs/dev/recheck-common.md** (если её текст почему-то не
приехал — прочитай `docs/dev/recheck-common.md` сам, это не отменяется) — там как проверять (по факту,
а не по памяти), форма ответа и общие пункты: маршрутизация, статус в плане, гейт/слияния/
пуш, CI, «всё ли починено», конвенции и стражи, хвосты, закрытие как для прода,
рекомендации, строка-итог. Общая часть живёт одним файлом намеренно: две копии одного
правила расходятся на первой же правке.

Затем ответь по общим пунктам И по этим четырём, которые есть только у `novac`:

1. **Соответствие.** Сделанное отвечает архитектуре (`docs/dev/novac-architecture.md`),
   плану (статус-строки в `docs/plans/`) и конвенциям (`novac-compiler-conventions.md`,
   `gate-guard-conventions.md`)? Где отступил — назови место и причину.
2. **Доноры.** Сделал, как в языках-донорах (rustc / Go / Swift / Zig / Roslyn / clang —
   по области), или это велосипед? Велосипед — не приговор, но он обязан быть названным
   и обоснованным замером, а не тишиной.
3. **ОРАКУЛ ДОНОРОМ БЫТЬ НЕ МОЖЕТ — строгий запрет** (П25 / П27 п.2а конвенции novac).
   Нынешний компилятор (`nova-cli`, `compiler-codegen`, `emit_c.rs`) не образец ни
   устройства, ни имени, ни разбиения: ошибок в нём слишком много, и архитектурно он
   сделан неверно — переписывание затеяно именно поэтому. Оракул — неподвижная точка
   ПОВЕДЕНИЯ (тот же stdout, тот же код возврата), и только.
   Проверь: ни одно решение этой работы не оправдано словами «в оракуле так же» —
   у каждого либо законный донор, либо честное «донора нет, потому что...».
   **Что запретом НЕ является:** механическая стыковка, пока эмиссия novac
   вклеивается в оракульскую (`shell.tpl.c`, одно C-имя типа, один рантайм на линковке).
   Это интероп, а не подражание, — но он обязан быть назван интеропом и нести условие
   конца («уходит, когда novac эмитит прелюдию целиком»), иначе временная привязка
   тихо станет образцом.

4. **Инварианты — сколько их стало и держит ли их конструкция, а не дисциплина.**
   Норма: «как можно меньше, и ни одного на честном слове»
   (`docs/dev/conventions-governance.md`), механизм — `check-invariant-discipline.sh`
   в `gate.sh`, зонтик работ — план [253](../../docs/plans/253-invariant-reduction-umbrella.md);
   для `novac` это сквозной принцип декомпозиции (решение 18 плана 274,
   [274.1 §2б](../../docs/plans/274.1-novac-architecture.md) п.2 — счётчик инвариантов на
   раздел входит в приёмку). Ответь: какие инварианты добавила эта работа, у каждого — чем
   он держится (конструкция или чьё-то внимание), и можно ли было выбрать конструкцию,
   которой инвариант не нужен. Инвариант на честном слове — не «мелочь на потом», а
   находка: заводи механизм или снимай инвариант.

**Адрес маршрутизации для `novac`:** находки пишутся в ТЕКУЩИЙ план (тот, который
выполняется) либо отдельным подпланом, когда они образуют свою волну и в текущем плане
утонут; подплан наследует номер (`NNN.N`).

**Маркеры — сперва развилка план-связанный/плавающий, и она не косметика.** Маркер про
ИНСТРУМЕНТЫ этого плана (gate-novac, стражи, бюджеты, смоук) — план-связанный: строка в
самом плане, НЕ в `backlog-followups.md`. Плавающий маркер обязан получить № в реестре
221.1 (правило владельца №217), а номер для не-дефекта компилятора был бы неправдой —
замер 2026-08-24: маркер про PCH-вычитание ушёл в плавающие, `bug-number-sync` покраснел
на CI, переезд стоил отдельного коммита. Дефект ОРАКУЛА — наоборот, всегда в 221.1
(с `№TBD` — номер присваивает интегратор) плюс запись в `novac-divergences.md`.

## Перепроверка пройдена — не останавливайся

Если итог перепроверки «пройдена» и очередь не пуста, **продолжай по
@.claude/commands/next.md** (если её текст не приехал — прочитай
`.claude/commands/next.md` сам): определи следующий шаг по основному плану и начинай.
Разрешения на продолжение не спрашивай — «пройдена» и есть разрешение. Не пройдена —
сперва закрой названное незакрытым, и только потом следующий шаг.
