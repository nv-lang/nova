<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 230 — Примеры-приложения Polaris

**Статус:** ✅ ИСПОЛНЕН 2026-07-25 (nova-polaris master 05c180e): 10/10 примеров + run_smokes (10/10 агент 3× + приёмка интегратора) + README-пары 11×2 + кросс-ссылки docs↔examples. Отклонение от §2: примеры ведут accept-цикл через `handle_connection_router` (не `serve_router`) — канон не линкуется (`nova_fn_hook`, реестр 221.1 №107); в каждом примере `production_main()` с каноном. Follow-up-волны: «примеры на serve_router» (после №107) и «примеры на экстракторах» (§5, после 222.3).
**Репа-цель:** nova-polaris, каталог `examples/` (каждый пример — самостоятельный мини-пакет).
**Связи:** 229 (дока — сниппеты; примеры = следующий уровень: цельные программы), 222
(архитектура), флагман nova `examples/flagship/aggregator` (прецедент оформления и гейтов).

## 0. Отличие от доки (229)

Сниппеты доки показывают ОДИН приём и живут в `doc_samples_test.nv`. Примеры — **цельные
запускаемые приложения**: показывают, как приёмы складываются вместе (структура проекта,
композиция middleware, обработка ошибок end-to-end). Пользователь копирует пример как
стартовую точку.

## 1. Принципы (жёсткие)

1. **Каждый пример собирается `--strict-effects`** (конвенция examples 2026-07-13) и
   **запускается** (smoke: поднялся → ответил на 1-2 запроса → погасился; по образцу
   rt-смоуков polaris).
2. **Источник истины — код**: только фактический API (без roadmap-фантазий; после 222.3
   примеры обновляются отдельной волной — правило поддержания из 229 §4).
3. Каждый пример несёт **README.md + README.ru.md** (что показывает, как запустить, что
   покрутить) — коротко, полстраницы.
4. От простого к сложному, каждый следующий добавляет ОДНУ концепцию.
5. Общий smoke-раннер: `examples/run_smokes.(ps1|sh)` — собирает и прогоняет все примеры
   (гейт волны и будущий CI-крючок).
6. **«Красивые и технологичные» (требование владельца, 2026-07-26).** Пример — витрина
   языка/фреймворка для новых пользователей; обходы компилятор-багов в примерах — брак,
   не временная норма. Конкретно:
   - **Никаких compiler-workaround'ов в теле примера.** Если фичи не хватает — баг
     чинится в компиляторе (реестр 221.1), пример ждёт фикса или переносит функциональность
     на ту, что реально работает; ручная имитация того, что должен делать язык/serde/роутер
     (руками собранный JSON вместо `Serialize`, руками написанный dispatch вместо роутера
     и т.п.) — недопустима как явленный код примера, даже с комментарием-оговоркой.
     Прецедент-урок: `examples/03-json-api`'s `todo_json_line`/`todos_json` (ручная сборка
     JSON-массива в обход №111, `Vec[T Serialize]` ICE) — владелец: «неподготовленный
     пользователь может подумать, что так нужно формировать JSON»; исправлено окном №111
     (компилятор-фикс + пример переведён на `ServerResponse.json(items)` напрямую).
   - **Идиоматичный код**: каждый пример использует САМУЮ канон-форму на момент
     написания (цепочки методов, extractors, а не низкоуровневые формы — см. канон
     `Router.new().get(...)!!` из 2026-07-26 «так не лучше?», применённый ко всем 10).
   - **Гейт волны обязан включать самопроверку**: перед сдачей примера — явный вопрос
     «есть ли в этом коде что-то, что читатель мог бы принять за канон, но это обход
     бага?» — если да, это блокер приёмки, не пометка в README.
   - Существующий workaround-инвентарь (реестр №109/110/111 на момент этой правки) —
     закрывается компилятор-окнами, примеры мигрируют на идиоматичную форму тем же
     слиянием, что и фикс (не отдельной, отложенной волной).

## 2. Состав (по нарастающей)

| # | Пример | Показывает |
|---|---|---|
| 01 | `hello/` | минимальный сервер: Router.new + один get + serve; «10 строк до работающего HTTP» |
| 02 | `routing/` | шаблоны `{name}`/`{*rest}`, MethodRouter (`get(h).post(h2)`), nest, fallback-404, typed-конфликты путей |
| 03 | `json-api/` | REST CRUD (in-memory todo): `req.json[T]`, `ServerResponse.json`, `#impl(Serialize/Deserialize)`, StatusCode-константы, коды ошибок |
| 04 | `middleware/` | свой middleware (`middleware(fn(req,next))`), `@then`, порядок layers, log+ratelimit батарейки |
| 05 | `auth/` | защита маршрутов: basic + bearer/JWT, session-cookie; публичная/приватная зоны через nest+layer |
| 06 | `static-site/` | polaris.static + fallback на index; кеш-заголовки |
| 07 | `sse-stream/` | StreamBody + sse_event: живой тикер |
| 08 | `websocket-echo/` | ws-upgrade hook, WebSocket.with_limit, echo-цикл |
| 09 | `graceful/` | ServerPolicy (лимиты/admission), BackgroundTasks, recover-500, graceful shutdown |
| 10 | `mini-service/` | «всё вместе» (уровень флагмана, компактнее): json-api + auth + middleware-стек + static + graceful — референс-структура реального сервиса |

### 2а. Первоисточники (решение владельца 2026-07-25: «взять за основу примеры с др. фреймворков наиболее известные»)

Каждый пример строится по УЗНАВАЕМОМУ канону индустрии (структура/сценарий — оттуда,
код — идиоматичный Nova; в README примера — строка «по мотивам: <ссылка>»):

| Наш | Первоисточник-канон |
|---|---|
| 01 hello | axum `hello-world`, Express hello |
| 02 routing | axum `routes-and-handlers-close-together`, chi routing |
| 03 json-api | axum `todos` + FastAPI tutorial (items CRUD) — самый цитируемый REST-старт |
| 04 middleware | axum tower-middleware showcase, Express middleware chain |
| 05 auth | axum `jwt` + FastAPI OAuth2/security tutorial |
| 06 static-site | axum `static-file-server` |
| 07 sse-stream | axum `sse` |
| 08 websocket-echo | axum `websockets` (chat/echo) |
| 09 graceful | axum `graceful-shutdown` + `key-value-store` (state) |
| 10 mini-service | **RealWorld (Conduit)** — сокращённый профиль знаменитой спеки «Medium-clone API»: users/auth + articles CRUD + пагинация; узнаваемый бенчмарк зрелости фреймворка |

10-й как RealWorld-mini — сильный сигнал сообществу («Polaris проходит RealWorld-профиль»);
полный RealWorld-порт — отдельный после-релизный кандидат.

## 3. Гейты волны

- `run_smokes` зелёный: 10/10 собираются `--strict-effects`, smoke-запрос отвечает.
- `nova test` реп-уровня — δ0 (примеры не ломают пакет; примеры вне `src/`, но собираются
  против локального polaris path-dep как обычные потребители — это заодно тестирует
  developer-experience подключения пакета).
- README-пары EN↔RU на каждый пример (счёт 10×2).
- Кросс-ссылки: каждый пример указывает разделы доки 229, которые иллюстрирует, и наоборот
  (в доке — «полный пример: examples/NN»).

## 4. Исполнение и порядок

Одна sonnet-волна в nova-polaris (ветка `p-examples`), ПОСЛЕ влития док-волны 229 (та же
репа — не плодить параллельные ветки; кросс-ссылки требуют готовой доки). Фазы: Ф.1 01-04 +
smoke-раннер → Ф.2 05-08 → Ф.3 09-10 + кросс-ссылки. Чекпоинт-коммит на фазу. Приёмка
интегратора: run_smokes + выборочный запуск 2-3 примеров руками.

## 5. Поддержание

То же правило, что 229 §4: волна, меняющая публичный API polaris, обязана прогнать
run_smokes и поправить задетые примеры в том же слиянии. После 222.3 — отдельная волна
«примеры на экстракторах» (обновить 02/03/10, дописать пример-раздел).
