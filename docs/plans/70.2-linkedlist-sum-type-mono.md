# Plan 70.2: LinkedList sum-type monomorphization

> **Создан:** 2026-05-18. **Статус:** 🟡 partial 2026-05-19 (data
> construction works; method-dispatch deferred). **Приоритет:** P1.
> **Трудоёмкость:** ~3-5 dev-days (factual: ~1.5h для partial closure).
> **Родительский план:** [Plan 70](70-no-silent-nova-int-fallback.md)

## Partial closure 2026-05-19

**Fix (compiler-codegen/src/codegen/emit_c.rs):** добавлен
`self.drain_generic_type_worklist()?;` после emit_test loop. Без этого
generic-type instances enqueued через `try_infer_variant_mono_args`
(Cons-constructor type inference) никогда не processed → struct decl
`Nova_LinkedList____nova_int` отсутствует в C → CC-FAIL.

**Fixture:** [nova_tests/plan70_2/f1_linkedlist_int_pos.nv](../../nova_tests/plan70_2/f1_linkedlist_int_pos.nv)
PASS — LinkedList[int] Nil/Cons construction + free-fn pattern matching
(len/sum) работают.

**Deferred (extracted в separate scope, не закрыто в этом plan):**

1. **Unit-variant inference** (Plan 48 Ф.7.4 V2): `Nil` constructor
   falls back to erased `nova_make_LinkedList_Nil()` instead of mono'd
   variant. Mixing erased + mono'd → type mismatch в Cons argument.
2. **@method dispatch on mono'd sum-type:** `l.@len()` эмитит literal
   `l->@len()` в C — method-name resolution не desugarит `@` префикс,
   не routes к `Nova_LinkedList____nova_int_method_len`.
3. **Method-name collision со StringBuilder.append** — generic-method
   dispatch shares namespace с unrelated types.

Per Plan 70.2 estimate (3-5 dev-days): full method-dispatch на mono'd
sum-types требует significant mono pass refactor. Текущий fix unblocks
LinkedList data usage (без instance methods); workaround — free fns
с explicit type annotations.

---

## Зачем

`LinkedList[int]` — рекурсивный sum-type:

```nova
type LinkedList[T] =
    | Nil
    | Cons(T, LinkedList[T])
```

При mono'д codegen генерируется `LinkedList.nil()` вместо
`LinkedList____nova_int.nil()` — тип теряется при monomorphization
sum-type конструкторов. Последующие вызовы методов диспатчатся в
`Nova_StringBuilder_method_append` вместо правильного метода.

**Воспроизведение:** тест `nova_tests/modules/linkedlist.nv` удалён
из batch 7 (Plan 45.B) — CC-FAIL и runtime crash.

---

## Проблема

В `emit_c.rs` monomorphization sum-type:
1. Generic конструктор `LinkedList[T].nil()` при mono с `T=int` не
   подставляет параметр в имя конструктора
2. `Cons(h, t)` destructure — `t: void*` вместо `LinkedList____nova_int*`
3. Метод `@append` диспатчится неверно — попадает в `StringBuilder`
   из-за name collision

---

## Решение

Три части:

**Ф.1** — Static constructors: `TypeName[T].variant()` при mono →
`TypeName____C_T.variant()` в generated C.

**Ф.2** — Recursive field types: в `Cons(T, LinkedList[T])` второй
элемент — рекурсивная ссылка на mono'd тип. Резолвить через
current type substitution context.

**Ф.3** — Method dispatch: при вызове метода на mono'd sum-type
проверять полное манглированное имя, не базовое.

---

## Тесты

После фикса:
- Вернуть `nova_tests/modules/linkedlist.nv` с тестами:
  - `LinkedList[int]` — nil, cons, append, len, sum
  - `LinkedList[str]` — nil, cons, contains
  - Negative: `LinkedList[int].nil()` + метод несовместимого типа

---

## Связь

- **[Plan 70](70-no-silent-nova-int-fallback.md)** — родительский план
- **[Plan 48](48-mono-pass.md)** — monomorphization infrastructure
- **[Plan 45.B batch 7](45-nova-doc.md)** — откуда обнаружен баг
