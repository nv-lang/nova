<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 152.6 — Encoding interop (UTF-16 / UTF-32 / code points) (D255)

> **Родитель:** [Plan 152 (umbrella)](152-string-coordinate-model.md). **Фаза A** (now) —
> мал, нужен FFI/JSON/протоколам.
> **Создан:** 2026-06-13. **Статус:** ✅ **ЗАКРЫТ** (Ф.0-Ф.4, 2026-06-13, ветка `plan-152`), P1. **Эстимат:** ~1–1.5 dev-day.
> **Model:** Sonnet 4.6 + High.
> **Зависит от:** 152.1 (`as_chars`/линзы), существующие `from_bytes_*`/`to_bytes`.
> **Владеет D-блоком:** D255.

---

## Идея

Для FFI/JSON/протоколов нужны конверсии в/из UTF-16 (Windows API, JS-interop, JSON
`\uXXXX`) и доступ к сырым codepoint'ам. Дополняет байтовый слой (`from_bytes_*`/
`to_bytes`) и codepoint-линзу (`as_chars`).

## D255. UTF-16/32 interop

- **`@encode_utf16() -> []u16`** — UTF-16 code units (surrogate-пары для cp > 0xFFFF).
- **`str.from_utf16(units []u16) -> Result[str, Utf16Error]`** — decode UTF-16
  (валидирует surrogate-пары; lone surrogate → `Err`).
- **`@code_points() -> []int`** — raw int codepoints (без `char`-обёртки) — для
  низкоуровневых протоколов/таблиц.
- **surrogate-помощники:** `is_high_surrogate`/`is_low_surrogate`/`decode_surrogate_pair`
  (в `std/encoding/utf16`).

Размещение: `std/encoding/utf16.nv` (модуль `encoding.utf16`), методы `str` —
делегаты в него или прямые.

## Фазы

- **Ф.0 — Spec D255.**
- **Ф.1 — `encode_utf16` + surrogate-помощники** (`std/encoding/utf16.nv`).
- **Ф.2 — `from_utf16` (checked)** + `Utf16Error`.
- **Ф.3 — `code_points()`** (raw int).
- **Ф.4 — Фикстуры + close.**

## Тесты

- **POS:** roundtrip `s.encode_utf16()` → `from_utf16()` == `s` (ASCII, BMP,
  supplementary — эмодзи 😀 = surrogate-пара); `"😀".encode_utf16().len()==2`;
  `code_points("a😀")==[0x61, 0x1F600]`.
- **NEG:** lone high/low surrogate в `from_utf16` → `Err(Utf16Error)`;
  усечённая surrogate-пара → `Err`.

## Критерии приёмки

- **A1.** `encode_utf16`/`from_utf16` — корректный roundtrip на ASCII/BMP/
  supplementary (surrogate-пары).
- **A2.** `from_utf16` отвергает lone/усечённые surrogate → `Err(Utf16Error)`
  (инвариант R-UTF8 сохраняется на выходе).
- **A3.** `code_points()` даёт корректные int-codepoint'ы (== `as_chars()` значения
  как int).
- **A4.** Размещено в `std/encoding/utf16`; `str`-методы — тонкие делегаты.
- **A5.** Полный `nova test` без новых FAIL; plan152_6-фикстуры PASS.

## Связь

D255. Дополняет `from_bytes_*`/`to_bytes` (байты) и `as_chars` (codepoint).

## Прогресс ✅ ЗАКРЫТ Ф.0-Ф.4 2026-06-13 (ветка plan-152)

- **`std/encoding/utf16.nv`** (модуль `encoding.utf16`, импортируется явно — не prelude):
  `Utf16Error` (LoneHigh/LoneLow/TruncatedSurrogatePair) + surrogate-помощники
  (`is_high_surrogate`/`is_low_surrogate`/`decode_surrogate_pair` с контрактами Plan 140:
  `requires is_*_surrogate`, `ensures result∈[0x10000,0x10FFFF]`).
- **`str @encode_utf16() -> []u16`** — BMP→1 unit, supplementary→surrogate-пара
  (через `@as_chars()` + `cp as int`).
- **`str.from_utf16(units []u16) -> Result[str, Utf16Error]`** — checked. Двухпроходно:
  валидация в `[]int` (ранний `Err` безопасен — нет живого consume), затем построение
  через `consume sb = StringBuilder` + `str.from_codepoint` (known-valid path),
  consume один раз через `@as_str`. R-UTF8 на выходе.
- **`str @code_points() -> []int`** — сырые codepoint'ы.
- Spec **D255** ([03-syntax.md](../../spec/decisions/03-syntax.md)); guide-секция в
  [docs/strings.md](../strings.md). Фикстура
  [plan152_6/utf16_interop](../../nova_tests/plan152_6/utf16_interop.nv) — 11 тестов 1/0
  (roundtrip ASCII/BMP/😀 + 3 neg surrogate + code_points==as_chars).
- **Критерии A1-A5 ✅.** Дизайн-открытие: str-расширения из импортируемого std-модуля
  dot-резолвятся (`s.encode_utf16()` при `import std.encoding.utf16`) — прецедента в std
  не было, проверено пробой.
