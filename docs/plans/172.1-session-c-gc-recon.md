# Session C recon — container/generic channeling блокер (ultracode 2026-06-28)

> Источник: workflow `wf_ba30367c-daf` (4 ридера + синтез). Полный результат — в transcript output
> `tasks/wdf6frjkf.output`. Вердикт: **путь B (codegen-time mono-subst annotation), НЕ Plan 144 A**.

## Механизм segfault (причинная цепь, подтверждено по коду)
1. **Guard-site:** `types/mod.rs:9992-9996` — `resolve_instance_method_return` после subst+fully-concrete-gate
   BAILS (`return None`) на container-возвратах (`Array`/`FixedArray`/`Tuple`/`Named{generics non-empty}`).
2. **Регистрирующий side-effect:** если бы не бейлил — на consume канала codegen (`emit_c.rs:36327-36329`)
   зовёт `resolved_type_to_c` → `resolved_named_to_c`/`register_mono_tuple` материализуют **СВЕЖУЮ**
   мономорфизацию (push в worklist, emit нового struct typedef+тела). Guard-коммент дословно:
   «registers a FRESH monomorphization via resolved_type_to_c side-effects, perturbing the binary layout».
3. **Mono-set → layout:** свежий mono меняет набор/порядок C-struct, sizeof/align/offsets, **fiber-arena
   `slot_size`**, набор размещаемых объектов.
4. **Conservative root-finding → SEGV:** GC = Boehm МАКСИМАЛЬНО консервативный (`GC_set_no_dls(1)`
   alloc_boehm.c:79, `GC_set_all_interior_pointers(1)` :83). Карт указателей НЕТ. Fiber-arena root-range
   `GC_add_roots(base, base+new_high*slot_size)` (fiber_arena.c:490) — границы ПРЯМО зависят от
   `slot_size`(=layout). Перетряхивание mono-set сдвигает где живые указатели vs pointer-shaped мусор +
   границы root-range → layout-sensitive Heisenbug (false-retention / missed-root→premature-collect→UAF)
   детерминированно становится SEGV. Триггер: агрегированный `plan154` sibling `test_9` (inline-ripple →
   `[]T`/`Vec` spelling-flip → ранний Vec-mono). Класс тот же, что 83.11.

## КЛЮЧЕВОЕ сужение блокера (определяет выбор)
Guard бейлит по ДВУМ причинам: **(a)** mono-layout-crash [только КАНАЛ], **(b)** f3_check_member
false-positive + nested mis-subst [только INLINE]. **DECOUPLE** (`types/mod.rs:9717-9723`:
`infer_method_call_channel_type` отдельна, method-arm return-type-free) **УЖЕ изолировал (b) от канала.**
⇒ container-**channeling** упирается ТОЛЬКО в **(a) mono-subst-timing.**

## Вердикт: B (codegen-time mono-subst annotation), НЕ A (Plan 144 GC-hardening)
1. DECOUPLE сузил до (a); B бьёт ровно в (a). A решает (a) дорого «по построению», (b) не трогает.
2. **§0:** B продолжает канал `resolved_types` + единый `resolved_type_to_c`, у которого УЖЕ есть
   subst-резолв (`emit_c.rs:2120-2127`: `type_subst_overrides`+`current_type_subst`). A вводит
   ПАРАЛЛЕЛЬНЫЙ GC-носитель корней (Henderson shadow-stack) — ортогонально type-engine, §0 не приближает.
3. **§7:** B локализован в type→C lowering, гейт готов (detect172 + spec_tests/conformance). A = 6-12 мес
   (144.md:6), blast = КАЖДАЯ эмитируемая функция + M:N fiber-switch + STW; Plan 144 ЯВНО «не блокирует».
4. **Plan 144 не готов:** root-finding-адресат Ф.3 (144.3) 🔴 NOT STARTED → гейт Ф.2 → Ф.0.1 freeze-ABI
   🔴. Только 144.0(may-GC D273)+144.1(heap-bitmaps D277) DONE — оба emit-nothing analysis.
5. **Ф.3-урок подтверждает B:** value-decay нужен на robust EMIT-based сигнале (codegen-time, with-subst) —
   ровно линия B.

## План Session C (B) — careful multi-session (НЕ хвост-сессии)
1. **Инвариант B:** container-возвраты НЕ резолвить on-demand в чекере (subst пуст), а нести ResolvedType
   в канал и лоуэрить ПОЗЖЕ в mono-эмиссии (subst populated). Снять `types/mod.rs:9992-9996` bail
   **ПОЭТАПНО по арму** (Named-generics → Tuple → Array/FixedArray), каждый под отдельным regress-гейтом.
2. **Энумерировать ВСЕ mono-side subst-population gaps** (proba D доказала: gaps — МНОЖЕСТВО). Env-gated
   debug-лог (как `NOVA_U45_RLCHECK`) в `resolved_named_to_c` (`emit_c.rs:2120-2127`): `args.is_empty()`
   && не в subst && имя=generic-template → лог proxy-bare. Прогон на `modules/lru` + `plan154*` →
   ИСЧЕРПЫВАЮЩИЙ список ДО фиксов.
3. **Закрыть gaps:** populated subst во всех mono/erased consume-контекстах (mono-emit-сайты;
   `emit_monomorphized_method ~15173`; `try_emit_default_body_candidates ~6061` gap#1 залэндж `bab296cf`).
   Восстановить `modules/lru` (proba D regression: bare `Nova_Lru*` → `Nova_Lru____nova_str__nova_int*`).
   Per-fix verify = targeted fixture (НЕ полный регресс в loop).
4. **Верификация:** чистый бинарь + `plan154` sibling-набор (test_9-триггер) NO segfault + полный регресс
   + detect172. spec_tests/conformance.

## Что разблокирует
**172.1.2 P4b** (container/chain-Call channeling) И **172.4 Ф.3** (fluent value-ABI — общий
receiver-inference-depth корень). Без него P7 container-re-derive не закрывается.

## Риск (честно)
B НЕ тривиален: proba D (172.1.1:445-460) сломала `modules/lru` (subst не populated к consume SelfAccess).
gap#2+ из других mono-путей открыты → careful multi-session. A остаётся самостоятельным v1.0-блокером
(Windows fiber-stack race, moving GC) — но как разблокировщик критпути 172 over-kill и на порядок медленнее.
