// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 100.1 — Implementation Playbook (Sonnet 4.6 minimal-settings ready)

> **Назначение:** пошаговое руководство для implementation-агента на
> Sonnet 4.6 c минимальными настройками (effort=medium, thinking=OFF
> для большинства фаз). Каждый шаг — 5-30 минут, exact file paths,
> exact code patterns, exact verify commands. Никаких judgment calls
> в run-time — все design-решения уже locked в [100.1 plan](100.1-core-must-consume.md)
> и [D133 spec](../../spec/decisions/02-types.md#d133).
>
> **Парент:** [100.1 core](100.1-core-must-consume.md). Этот документ
> = ИСПОЛНЕНИЕ. Дизайн смотреть в parent + D133 / D156-D166.
>
> **Calendar estimate:** 5-7 dev-day agent-time (one fresh session per
> phase recommended).

---

## Quick reference

**Per-phase model/effort/thinking recommendations** (см. §«Model recommendations»):

| Фаза | Effort | Thinking | Calendar | Почему |
|---|---|---|---|---|
| Ф.0 | low | OFF | 30 min | doc-writing, no compiler code |
| Ф.1 | low | OFF | 0 min | **уже сделано** Ред. 2 (D133 spec landed) |
| Ф.2 | medium | OFF | 4-6 h | mechanical AST + parser extension |
| Ф.3 | medium | OFF | 3-4 h | LinearityRegistry + marker checks |
| **Ф.4** | **high** | **ON** | **8-12 h** | **CRITICAL: ConsumeCtx FieldPath migration + flow analysis** |
| Ф.5 | medium | OFF | 3 h | diagnostic formatting + pilot mocks |
| Ф.6 | low | OFF | 1 h | tests **уже существуют** (17 фикстур), нужна только верификация |
| Ф.7 | low | OFF | 1 h | docs + merge |

**Single-session rule:** Ф.2, Ф.3, Ф.5, Ф.6, Ф.7 — могут идти за одну
сессию каждая. **Ф.4 — обязательно несколько микро-сессий** (см. §Ф.4
breakdown), каждая закрывает один sub-step с verify.

---

## Pre-requisites (one-time, перед Ф.2)

### PRE-1. Создать постоянный worktree

```powershell
git worktree add d:/Sources/nv-lang/nova-p100-impl-foundation -b plan-100-impl-foundation main
```

Per memory `feedback-worktree-naming`: convention `nova-pNN-*` (нe `plan-NN`).
Per memory `feedback-isolated-worktree`: постоянный worktree, не switching branches в main.

**Если worktree уже создан** (e.g. предыдущий run остановился) — просто
зарегистрируйся в нём первой Bash командой через `cd`, не пересоздавай.

### PRE-2. Env vars для `nova test` в worktree

В worktree отсутствует `compiler-codegen/vcpkg_installed/`. Env vars
указываем на main repo (gc.lib / gc.h — статические артефакты, не
зависят от branch'а).

**Правильные пути (verified 2026-05-25):**

```powershell
$env:NOVA_GC_INCLUDE_DIR = "d:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/include"
$env:NOVA_GC_LIB_DIR     = "d:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/lib"
```

**Verify env vars применились:**

```powershell
echo $env:NOVA_GC_INCLUDE_DIR
ls $env:NOVA_GC_INCLUDE_DIR/gc.h     # должен показать файл
ls $env:NOVA_GC_LIB_DIR/gc.lib       # должен показать файл
```

Без этих env vars: positive фикстуры (которые компилируются в .exe)
дают `gc.h not found` → CC-FAIL. Negative фикстуры (`*_err_*`)
проходят т.к. стопаются на compile-error gate до C-codegen — это
маскирует setup-issue.

**Libuv submodule.** Detection logic
(`compiler-codegen/src/test_runner.rs:2476`) ищет
`compiler-codegen/nova_rt/libuv/include/uv.h` в самом worktree.

Verify:
```powershell
ls d:/Sources/nv-lang/nova-p100-impl-foundation/compiler-codegen/nova_rt/libuv/include/uv.h
```

Если ✅ — submodule инициализирован git-worktree автоматически,
ничего делать не надо. Если ❌:
```powershell
cd d:/Sources/nv-lang/nova-p100-impl-foundation
git submodule update --init compiler-codegen/nova_rt/libuv
```

`libuv.lib` build'ится lazy при первом `nova test` (~30 sec, кэш в
`<worktree>/target/libuv-cache/libuv.lib`, per-worktree).

### PRE-3. Baseline `nova test`

Запускать ИЗ worktree ПОСЛЕ PRE-2 (env vars + libuv check).

```powershell
cd d:/Sources/nv-lang/nova-p100-impl-foundation
cargo build --release 2>&1 | Tee-Object baseline-build.log
# nova.exe билдится в nova-cli/target/release/, НЕ в worktree-root/target/
.\nova-cli\target\release\nova.exe test --workspace 2>&1 | Tee-Object baseline-tests.log
```

Записать summary `XXXX PASS / YY FAIL / ZZ SKIP` из `baseline-tests.log`.
Это reference для regression check в Ф.6 — НЕ опираться на числа из
`project-creation.txt` (они frozen-in-time; главное — твой свежий
worktree baseline).

⚠ Если baseline FAIL >> 20 (e.g. 50+) — это знак что PRE-2 setup
не полностью завершён. Останавливайся, перепроверь env vars указывают
на существующий gc.h/gc.lib + libuv инициализирован.

### PRE-4. Проверить Plan 73 alias-class работает

```powershell
.\nova-cli\target\release\nova.exe test nova_tests/plan73
```

Должно быть **`PASS: 12  FAIL: 0`** (verified на main repo 2026-05-25).
Plan 73 — foundation для нашего расширения ConsumeCtx; если plan73
fixtures сломаны, останавливаемся и фиксим before continuing.

**Если 5 CC-FAIL для `consume_ok_*`** (positive фикстуры падают, negative
проходят) — это ЯВНЫЙ симптом неполного PRE-2. Negative тесты
маскируют setup-issue потому что стопаются до C-codegen на compile-
error gate. Verify env vars указывают на gc.h/gc.lib (см. PRE-2).

---

## Ф.0 — GATE probe document (~30 min, effort=low, thinking=OFF)

**Цель:** проверить D1-D10 + D5.1 + D5.2 self-consistent через 4 hand-
written примера.

### Ф.0.1 Создать `docs/plans/100.1-artifacts/gate-probe.md`

Содержимое — 4 секции, по 1 use-case'у:

1. **Mock Transaction commit/rollback** — основной счастливый путь
   (D1, D2 №1, D3, D9).
2. **Service с consume File + reopen** — D5 mut-method preserves invariant
   (D5, D5.1).
3. **Inner→Outer nested** — D5.2 deep field path.
4. **Option-field peek** — generic transitivity (D6) + кросс-ссылка на
   100.3 D4 (match view).

Каждая секция:
- 10-25 строк pseudo-Nova кода
- 3-5 строк объяснения «что трекает flow-analysis»
- Ссылка на конкретный D-block.

### Ф.0.2 Self-check checklist

В конце файла:
- [ ] Каждое consume в коде имеет соответствие в D1-D10
- [ ] Нет contradiction между секциями
- [ ] Ни одно правило не введено в probe без D-block parent'а

### Ф.0.3 Commit

```powershell
git add docs/plans/100.1-artifacts/gate-probe.md
git commit -m "docs(plan100.1-F0): GATE probe — 4 use-case'а self-check D1-D10"
```

**Acceptance Ф.0:** файл создан, self-check checklist — все галки.

---

## Ф.1 — Spec D133 (уже сделано в Ред. 2)

**Статус:** ✅ **CLOSED** в `spec/decisions/02-types.md` D133 (Ред. 2
landing commit `d7464176352` + D9 update `6071c42a927`).

Если читаешь playbook fresh — verify:

```powershell
git log --oneline -- spec/decisions/02-types.md | Select-Object -First 5
```

Должны быть видны commits с D133. Никаких действий не требуется.

---

## Ф.2 — Lexer + Parser + AST (~4-6 h, effort=medium, thinking=OFF)

**Цель:** парсить `type X consume {…}`, `consume field T`, `consume tx = …`.
Лексер `KwConsume` уже есть (Plan 73).

### Ф.2.1 AST extension (~20 min)

**Файл:** `compiler-codegen/src/ast/mod.rs`

**Изменения:**

1. В `pub struct TypeDecl` (line ~617) — добавить поле после `name`:
   ```rust
   /// Plan 100.1 (D133 / D1): `type X consume { ... }` — type-level
   /// must-be-consumed marker. Каждый instance такого типа обязан
   /// быть consumed до scope-exit (через consume-method, return,
   /// consume-param, record-field-move, или defer).
   pub consume: bool,
   ```

2. В `pub struct RecordField` (line ~689) — добавить после `mutable`:
   ```rust
   /// Plan 100.1 (D133 / D4): `consume field T` — field хранит
   /// consume-typed значение. Type-decl с таким полем ДОЛЖЕН быть
   /// сам объявлен `consume` (compile error D133-type-marker-missing
   /// иначе). Field-type должен сам быть consume-типом (W
   /// D133-marker-on-non-consume иначе).
   pub consume: bool,
   ```

3. В `pub struct LetDecl` (line ~835) — добавить после `mutable`:
   ```rust
   /// Plan 100.1 (D133 / D9): `consume tx = expr` — explicit binding
   /// для Live-linear ownership. Обязателен для consume-типов
   /// (compile error D133-consume-needs-keyword иначе). Без него
   /// `let tx = …` для consume-rvalue = parse-accepted, type-check
   /// rejects.
   pub consume: bool,
   ```

### Ф.2.2 Update existing constructors (~30 min)

Все места, где создаются `TypeDecl`, `RecordField`, `LetDecl`, нужно
добавить `consume: false` (default).

**Команда поиска:**

```powershell
.\sg run --lang rust --pattern 'TypeDecl { $$$ }' compiler-codegen/src/ 2>$null
# fallback:
Get-ChildItem -Recurse compiler-codegen/src/*.rs | Select-String "TypeDecl\s*\{"
Get-ChildItem -Recurse compiler-codegen/src/*.rs | Select-String "RecordField\s*\{"
Get-ChildItem -Recurse compiler-codegen/src/*.rs | Select-String "LetDecl\s*\{"
```

**Ожидаемые места** (по grep'у на 2026-05-25):
- `compiler-codegen/src/parser/mod.rs:2316,2895,5652` (parse_type_decl, parse_let_decl, snapshot_let)
- `compiler-codegen/src/desugar.rs:221,368,398` (`Stmt::Let(LetDecl {...})`)
- `compiler-codegen/src/callnorm.rs:502` (LetDecl)

**Strategy:** для **каждого** конструктора добавить `consume: false,`.
Конструкторы внутри `parse_*` (где консьюм может быть true) обновятся в
следующих под-шагах — пока ставим `false`.

**Verify после Ф.2.2:**

```powershell
cargo build --release 2>&1 | Select-String "error\["
```

Должно быть 0 errors. Если есть — найти missing constructor (`error[E0063]: missing field consume`) и добавить `consume: false`.

### Ф.2.3 Parser — type-decl `consume` keyword (~30 min)

**Файл:** `compiler-codegen/src/parser/mod.rs` функция `parse_type_decl` (line 2246).

**Место:** после `parse_ident()` (line 2249) и parsing of `generics` (line 2253-2257), но **до** проверки `is_external` (line 2263).

**Добавить:**

```rust
// Plan 100.1 (D133 / D1): `type X consume { ... }` — после имени и
// generics, перед body. Mutually exclusive с `external` (D126 opaque
// types — без body, нет point'а в consume marker).
let consume_marker = self.eat(&TokenKind::KwConsume).is_some();
if consume_marker && is_external {
    let span = self.peek().span;
    return Err(Diagnostic::new(
        "external type cannot be `consume`: external types are opaque, \
         must-consume requires field-aware flow analysis. See D126 / D133."
            .to_string(),
        span,
    ));
}
```

**И:** в `Ok(TypeDecl { ... })` конструкторе (`line 2316` для external и
ниже для regular type) — заменить `consume: false` на `consume: consume_marker`.

**Verify:**

```powershell
cargo build --release 2>&1 | Select-String "error"
# Если 0 errors:
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/consume_ok_commit.nv
```

Должно: build OK; `nova check` НЕ должен жаловаться на parse `type Transaction consume {…}` (если жалуется — fix parser).

Регрессия:

```powershell
.\nova-cli\target\release\nova.exe check nova_tests/plan73 --quiet
```

Должно: 0 parse errors. Существующие `type X { … }` без consume должны парситься как раньше.

### Ф.2.4 Parser — record field `consume` keyword (~30 min)

**Файл:** `compiler-codegen/src/parser/mod.rs` функция `parse_record_fields` (line 2491).

**Действие:** найти точку начала field-парсинга (где обычно ловится `mutable` / `readonly`), добавить parsing для `consume` keyword:

```rust
// Plan 100.1 (D133 / D4): `consume field T` — field-level marker для
// consume-typed полей. Mutually exclusive с `mut` (consume-fields
// меняются только через explicit replace pattern; см. D5.1).
let field_consume = self.eat(&TokenKind::KwConsume).is_some();
if field_consume && field_mutable {
    return Err(Diagnostic::new(
        "field cannot be both `mut` and `consume`: consume-fields use \
         explicit replace pattern (see D5.1 100.1). Remove `mut` if \
         consume semantics intended, or remove `consume` for shared mutation."
            .to_string(),
        self.peek().span,
    ));
}
```

В `RecordField { ... }` constructor добавить `consume: field_consume,`.

**Verify:** parse `nova_tests/plan100.1/consume_ok_record_field.nv` без error'а.

### Ф.2.5 Parser — `consume tx = expr` binding (~45 min)

**Файл:** `compiler-codegen/src/parser/mod.rs` функция `parse_let_decl` (line 2856).

**Проблема:** D9 говорит `consume tx = expr` — это **альтернатива** `let tx = expr`. Текущий `parse_let_decl` начинается с `expect(KwLet)`. Нужно либо:
- (a) Создать новую `parse_consume_decl` + dispatch в `parse_stmt_or_expr`.
- (b) Расширить `parse_let_decl` чтобы принимать `consume` keyword вместо `let`.

**Рекомендация — вариант (a)** (минимальный риск регрессии):

1. В `parse_stmt_or_expr` (line 6281) добавить branch для `KwConsume`:
   ```rust
   TokenKind::KwConsume if matches!(self.peek_at(1).kind, TokenKind::Ident(_) | TokenKind::KwMut) => {
       // Plan 100.1 (D133 / D9): `consume tx = expr` binding form.
       // Disambig vs `consume` в receiver-pos (parse_fn handles before
       // stmt-context). Disambig vs `consume` method-arg keyword
       // (always inside parens, handled by parse_call_args).
       let let_decl = self.parse_consume_let()?;
       return Ok(StmtOrExpr::Stmt(Stmt::Let(let_decl)));
   }
   ```

2. Добавить новую функцию `parse_consume_let` рядом с `parse_let_decl`:
   ```rust
   fn parse_consume_let(&mut self) -> Result<LetDecl, Diagnostic> {
       let start = self.peek().span;
       self.expect(&TokenKind::KwConsume)?;
       // `consume tx = …` — strict ownership binding. `mut` не сочетается
       // (consume = full transfer; mut доступен через mut-method на receiver'е).
       if matches!(self.peek().kind, TokenKind::KwMut) {
           return Err(Diagnostic::new(
               "`consume mut tx` is not valid: consume = ownership transfer, \
                mut-methods accessed via receiver's `mut` qualifier (D7 100.1)."
                   .to_string(),
               self.peek().span,
           ));
       }
       let pattern = self.parse_pattern()?;
       let ty = if !matches!(self.peek().kind, TokenKind::Eq) {
           Some(self.parse_type()?)
       } else { None };
       self.expect(&TokenKind::Eq)?;
       self.skip_newlines();
       let value = self.parse_expr()?;
       let span = start.merge(value.span);
       self.expect_newline_or_eof().ok();
       Ok(LetDecl {
           mutable: false,
           pattern, ty, value, span,
           is_ghost: false,
           consume: true,
       })
   }
   ```

3. В `Ok(LetDecl { … })` (line 2895) добавить `consume: false`.

**Verify:**

```powershell
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/consume_ok_commit.nv
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/consume_err_forget.nv
```

`consume_ok_commit.nv` должен парситься без error (на этой стадии flow-
check ещё не enforce — это OK). `consume_err_forget.nv` тоже парсится
(EXPECT_COMPILE_ERROR не сработает пока Ф.4 не сделан).

### Ф.2.6 Parser — disambiguation guards (~15 min)

**Уже в Plan 73** parser обрабатывает `consume` в receiver/param/method-arg
позициях (line 1863, 2105). Проверить что новый stmt-level `consume tx`
НЕ конфликтует:

```powershell
.\nova-cli\target\release\nova.exe check nova_tests/plan73 --quiet 2>&1 | Select-String "error|FAIL"
```

Должно: 0 errors / FAILs. Если есть конфликт — добавить lookahead guard
в `parse_stmt_or_expr` branch (см. Ф.2.5 шаг 1 — `peek_at(1).kind` check).

### Ф.2.7 Parser-error фикстуры (~30 min)

**Создать `nova_tests/plan100.1/parser/`** (6 файлов):

| # | File | Что ловит |
|---|---|---|
| 1 | `parser_ok_type_consume.nv` | `type T consume { x int }` — parses |
| 2 | `parser_ok_field_consume.nv` | `consume field T` — parses |
| 3 | `parser_ok_consume_binding.nv` | `consume tx = expr` — parses |
| 4 | `parser_err_consume_mut_param.nv` | `fn f(consume mut x T)` — parse error (D131) |
| 5 | `parser_err_consume_at_return.nv` | `fn T consume @m() -> @` — parse error (D8) |
| 6 | `parser_err_external_consume.nv` | `external type X consume` — parse error |

Используй existing fixtures как шаблон (e.g. `nova_tests/plan100.1/consume_ok_commit.nv`).

**Acceptance Ф.2:**

```powershell
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/parser/*.nv
```

Все 3 pos должны pass parsing; 3 neg должны emit parse error с правильным D-кодом.

**Commit:**

```powershell
git add compiler-codegen/src/ast/mod.rs compiler-codegen/src/parser/mod.rs `
        compiler-codegen/src/desugar.rs compiler-codegen/src/callnorm.rs `
        nova_tests/plan100.1/parser/
git commit -m "feat(plan100.1-F2): parser + AST для type/field/binding consume keyword"
```

---

## Ф.3 — LinearityRegistry + marker consistency (~3-4 h, effort=medium, thinking=OFF)

**Цель:** сборщик `LinearityRegistry` (которые типы — consume) + проверка консистентности D133-маркеров.

### Ф.3.1 Define LinearityRegistry (~20 min)

**Файл:** `compiler-codegen/src/types/mod.rs`

**Место:** добавить **перед** `struct ConsumeRegistry` (line ~6954):

```rust
/// Plan 100.1 (D133 / D6): registry консьюм-типов модуля. Заполняется
/// pre-pass'ом до `check_consume`. Используется `type_is_consume`
/// для рекурсивной классификации (record-field consume-types,
/// generic-wraps).
///
/// **НЕ путать с `ConsumeRegistry`** (Plan 73 D131 — registry consume-
/// методов и consume-параметров; flat-name based).
struct LinearityRegistry {
    /// Имена типов, объявленных `type X consume {...}`.
    consume_types: HashSet<String>,
}

impl LinearityRegistry {
    fn build(module: &Module) -> Self {
        let mut consume_types = HashSet::new();
        for item in &module.items {
            if let Item::Type(td) = item {
                if td.consume {
                    consume_types.insert(td.name.clone());
                }
            }
        }
        LinearityRegistry { consume_types }
    }

    /// Plan 100.1 (D133 / D6): `type_is_consume(TypeRef)` — рекурсивно
    /// определяет, является ли тип consume через wrap-transitivity.
    /// Bootstrap: generic-param без bound → false (silent-ignore;
    /// 100.2 закроет через `[T consume]`).
    fn type_is_consume(&self, t: &TypeRef, module: &Module) -> bool {
        match t {
            TypeRef::Named { path, args, .. } => {
                // Direct consume-type.
                let name = path.last().cloned().unwrap_or_default();
                if self.consume_types.contains(&name) {
                    return true;
                }
                // Generic wrap: Option[Transaction], Box[Tx], Wrapper[T].
                if args.iter().any(|a| self.type_is_consume(a, module)) {
                    return true;
                }
                // Record/sum lookup: own fields consume-typed?
                for item in &module.items {
                    if let Item::Type(td) = item {
                        if td.name == name {
                            return match &td.kind {
                                TypeDeclKind::Record(fields) =>
                                    fields.iter().any(|f|
                                        f.consume || self.type_is_consume(&f.ty, module)),
                                TypeDeclKind::Sum(variants) =>
                                    variants.iter().any(|v|
                                        v.payload.iter().any(|p|
                                            self.type_is_consume(p, module))),
                                _ => false,
                            };
                        }
                    }
                }
                false
            }
            TypeRef::Tuple(elems, _) =>
                elems.iter().any(|e| self.type_is_consume(e, module)),
            // Generic-param без bound — bootstrap silent-ignore.
            TypeRef::Param(_) => false,
            _ => false,
        }
    }
}
```

**Note:** точные `TypeRef` варианты могут отличаться — adapt по
актуальному `enum TypeRef` в `ast/mod.rs`. Проверь grep:
```powershell
Get-ChildItem compiler-codegen/src/ast/mod.rs | Select-String "pub enum TypeRef"
```

### Ф.3.2 Marker consistency check (~1 h)

**Файл:** `compiler-codegen/src/types/mod.rs`

**Добавить функцию** перед `fn check_consume` (line ~7310):

```rust
/// Plan 100.1 (D133 / D4): проверка согласованности consume-маркеров
/// на type-decl'ах и полях. Emit diagnostics:
/// - D133-field-marker-missing: field consume-типа без `consume`.
/// - D133-type-marker-missing: `consume field` в non-consume type.
/// - D133-empty-consume: `type X consume {}` без consume-полей и без
///   declared consume-methods.
/// - D133-marker-on-non-consume: `consume f int` (non-consume type).
fn check_linearity_markers(
    module: &Module,
    reg: &LinearityRegistry,
    errors: &mut Vec<Diagnostic>,
) {
    for item in &module.items {
        let Item::Type(td) = item else { continue; };

        let TypeDeclKind::Record(fields) = &td.kind else {
            // Sum-types — D4 covers fields-level only; skip body check
            // for now (sum-variants могут содержать consume-payload —
            // type_is_consume их подхватит, marker-check на them — future).
            continue;
        };

        // 1. D133-field-marker-missing: consume-field без `consume`-маркера.
        for f in fields {
            let field_is_consume_type = reg.type_is_consume(&f.ty, module);
            if field_is_consume_type && !f.consume {
                errors.push(Diagnostic::new(
                    format!(
                        "[D133-field-marker-missing] field `{}` имеет consume-тип \
                         но не помечен `consume`. Добавь `consume field {}` либо \
                         замени тип на non-consume.",
                        f.name, f.name),
                    f.span,
                ));
            }
            // 4. D133-marker-on-non-consume: `consume f int` где int не consume.
            if f.consume && !field_is_consume_type {
                errors.push(Diagnostic::new(
                    format!(
                        "[D133-marker-on-non-consume] поле `{}` помечено `consume` \
                         но его тип не consume — маркер не нужен. Удали `consume` \
                         перед `{}`.",
                        f.name, f.name),
                    f.span,
                ));
            }
        }

        // 2. D133-type-marker-missing: consume-field в non-consume type-decl.
        if !td.consume && fields.iter().any(|f| f.consume) {
            errors.push(Diagnostic::new(
                format!(
                    "[D133-type-marker-missing] type `{}` содержит consume-поле, \
                     но сам не помечен `consume`. Добавь `consume` после имени: \
                     `type {} consume {{ … }}`.",
                    td.name, td.name),
                td.span,
            ));
        }

        // 3. D133-empty-consume: `type X consume` без consume-полей.
        // Допускаем opaque consume-типы (StringBuilder pattern: consume-methods
        // через external-fn, без consume-fields). Heuristic: ищем хотя бы один
        // consume-method для этого типа.
        if td.consume && !fields.iter().any(|f| f.consume) {
            let has_consume_method = module.items.iter().any(|it| {
                if let Item::Fn(fd) = it {
                    fd.receiver.as_ref().map_or(false, |r|
                        r.type_name == td.name && r.consume)
                } else { false }
            });
            if !has_consume_method {
                errors.push(Diagnostic::new(
                    format!(
                        "[D133-empty-consume] type `{}` помечен `consume` но не \
                         имеет ни consume-полей, ни consume-методов — добавь \
                         хотя бы один consume-method (`fn {} consume @method() -> ()`) \
                         либо убери `consume` с type-decl.",
                        td.name, td.name),
                    td.span,
                ));
            }
        }
    }
}
```

### Ф.3.3 Wire LinearityRegistry в check_module (~15 min)

**Файл:** `compiler-codegen/src/types/mod.rs`, функция `check_consume` (line 7310).

**Изменение:**

```rust
fn check_consume(module: &Module, errors: &mut Vec<Diagnostic>) {
    let reg = ConsumeRegistry::build(module);
    // Plan 100.1 (D133): LinearityRegistry + marker consistency.
    let lin_reg = LinearityRegistry::build(module);
    check_linearity_markers(module, &lin_reg, errors);
    // ... existing logic ...
}
```

**Verify:**

```powershell
cargo build --release 2>&1 | Select-String "error\["
# Если 0 errors:
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/consume_err_field_marker_missing.nv
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/consume_err_type_marker_missing.nv
```

Должны emit правильные D133-* diagnostics.

### Ф.3.4 Regression

```powershell
.\nova-cli\target\release\nova.exe test nova_tests/plan73 nova_tests/basics 2>&1 | Select-String "PASS|FAIL"
```

Должно: 0 FAIL для plan73 (Plan 73 типы не имеют consume-маркеров → новые проверки no-op).

**Commit:**

```powershell
git add compiler-codegen/src/types/mod.rs
git commit -m "feat(plan100.1-F3): LinearityRegistry + D133 marker consistency checks (4 diagnostics)"
```

---

## Ф.4 — `check_consume` extension: FieldPath + flow analysis (~8-12 h, effort=HIGH, thinking=ON)

**КРИТИЧЕСКАЯ ФАЗА.** Обязательно несколько микро-сессий. Each sub-step
— verify immediately. Sonnet 4.6 — поднять effort=high и включить thinking=ON.

### Ф.4.1 Define FieldPath + new ConsumeCtx::states key (~30 min)

**Файл:** `compiler-codegen/src/types/mod.rs`

**Добавить** перед `struct ConsumeCtx<'a>` (line ~7085):

```rust
/// Plan 100.1 (D133 / D5.2): canonical path к consume-trackable
/// position. Form: `["@", "field1", "field2"]` для `@.field1.field2`.
/// Для локальных переменных — `["__local__", "varname"]` (синтетический
/// prefix чтобы не конфликтовать с named-fields).
/// Пустой Vec не используется.
type FieldPath = Vec<String>;

fn local_path(name: &str) -> FieldPath {
    vec!["__local__".to_string(), name.to_string()]
}

fn field_path_root() -> FieldPath {
    vec!["@".to_string()]
}

fn extend_path(base: &FieldPath, segment: &str) -> FieldPath {
    let mut p = base.clone();
    p.push(segment.to_string());
    p
}

/// Plan 100.1: extract FieldPath из ExprKind для D5/D5.2 tracking.
/// Возвращает None если выражение не является tracked path (literal,
/// call, arithmetic — flow-analysis их не tracks как linear-position).
fn try_extract_field_path(e: &Expr) -> Option<FieldPath> {
    match &e.kind {
        ExprKind::Ident(name) => Some(local_path(name)),
        ExprKind::SelfReceiver | ExprKind::At => Some(field_path_root()),
        ExprKind::FieldAccess { receiver, field } => {
            let base = try_extract_field_path(receiver)?;
            Some(extend_path(&base, field))
        }
        _ => None,
    }
}
```

**Note:** точные `ExprKind` варианты — adapt по `ast/mod.rs`. Если
`SelfReceiver` называется иначе (`This`, `At`, etc.) — заменить.

### Ф.4.2 Migrate ConsumeCtx::states (~1 h)

**Текущее** (line 7089): `states: HashMap<String, VarState>`.

**Новое:** `states: HashMap<FieldPath, VarState>`.

**Strategy:**
1. Заменить тип поля.
2. Обновить `mark_consumed` / `declare` / etc. — wrapping String args через `local_path(name)`.
3. Каждый `self.states.get(&canon)` → `self.states.get(&local_path(&canon))`.

**Сохранить старые методы как fasade'ы**, добавить новые для FieldPath:

```rust
impl<'a> ConsumeCtx<'a> {
    // ... existing methods ...

    /// Plan 100.1 (D5/D5.2): mark Consumed по полному пути.
    fn mark_path_consumed(&mut self, path: &FieldPath, span: Span) {
        if self.states.contains_key(path) {
            self.states.insert(path.clone(), VarState::Consumed(span));
        }
    }

    /// Plan 100.1: declare новый Live path (для field initialization
    /// на entry метода и для `consume tx = …` binding).
    fn declare_path(&mut self, path: FieldPath, ty: Option<String>) {
        self.states.insert(path.clone(), VarState::Live);
        if let Some(t) = ty {
            // var_types keyed только локальными именами — оставить
            // существующую логику для local_path формы.
            if path.len() == 2 && path[0] == "__local__" {
                self.var_types.insert(path[1].clone(), t);
            }
        }
    }
}
```

**Verify:**

```powershell
cargo build --release 2>&1 | Select-String "error\["
.\nova-cli\target\release\nova.exe test nova_tests/plan73 2>&1 | Select-String "PASS|FAIL"
```

Plan 73 должен: 0 FAIL. Это **ключевой regression gate** — старый
функционал должен работать как раньше.

### Ф.4.3 Field-entry initialization (~45 min)

**Цель:** при entry в method record'а — все consume-поля помечаются `Live` в `ConsumeCtx`.

**Найти** место analysis-entry метода (grep `walk_fn` / `analyze_function` / `check_consume_in_fn`):

```powershell
Get-ChildItem compiler-codegen/src/types/mod.rs | Select-String "fn walk_fn|fn analyze.*fn|consume.*fn.*decl"
```

**Добавить** в начале analyze-функции (псевдокод):

```rust
// Plan 100.1 (D5): инициализация tracking'а для consume-полей метода.
if let Some(recv) = &fd.receiver {
    if let Some(td) = find_type_decl(module, &recv.type_name) {
        if let TypeDeclKind::Record(fields) = &td.kind {
            for f in fields {
                if f.consume {
                    let path = extend_path(&field_path_root(), &f.name);
                    ctx.declare_path(path, Some(field_type_name(&f.ty)));
                }
            }
        }
    }
}
```

**Verify:** `consume_ok_record_field.nv` должен pass без regression.

### Ф.4.4 Field-access mark Consumed (~1 h)

**Цель:** `tx.commit()` или `@.field.commit()` — mark соответствующий FieldPath Consumed.

**Найти** место handling `MethodCall` в `walk_expr`/`walk_stmt`:

```powershell
Get-ChildItem compiler-codegen/src/types/mod.rs | Select-String "MethodCall|method_call"
```

**Logic:**
1. Если method — consume-method (per `reg.methods`), extract receiver-path через `try_extract_field_path`.
2. Если path найден — `ctx.mark_path_consumed(&path, span)`.
3. Fallback на старый flat-name path для backward-compat.

### Ф.4.5 Exit-point check для local consume-vars (~1 h)

**Найти** место function-end / return / panic / branch-join проверок
(`fn check_exit`, `fn final_check`, etc.).

**Добавить** для каждого exit'а:

```rust
// Plan 100.1 (D3): local consume-vars Live на exit → error D133-not-consumed.
for (path, state) in &ctx.states {
    if path.len() == 2 && path[0] == "__local__" {
        let varname = &path[1];
        // Только consume-typed vars (skip обычных Plan 73 affine).
        let Some(ty) = ctx.var_types.get(varname) else { continue; };
        if !lin_reg.consume_types.contains(ty) { continue; }
        match state {
            VarState::Live | VarState::MaybeConsumed(_) => {
                let methods = lin_reg.consume_methods_for(ty);
                let methods_hint = if methods.len() > 4 {
                    format!("see `nova doc {}`", ty)
                } else if methods.is_empty() {
                    "no consume-methods declared".to_string()
                } else {
                    methods.join(" / ")
                };
                errors.push(Diagnostic::new(
                    format!(
                        "[D133-not-consumed] переменная `{}` (тип `{}`) не consumed \
                         до scope-exit. Добавь вызов одного из: {}, либо `return {}`, \
                         либо передачу в consume-param, либо покрой через `defer`.",
                        varname, ty, methods_hint, varname),
                    *exit_span,
                ));
            }
            VarState::Consumed(_) => {}
        }
    }
}
```

**LinearityRegistry::consume_methods_for** — helper:

```rust
impl LinearityRegistry {
    fn consume_methods_for(&self, type_name: &str) -> Vec<String> {
        // Сканируем module за consume-methods этого типа.
        // (Можно cached'ить при build'е через HashMap<String, Vec<String>>.)
        unimplemented!("scan module per type_name")
    }
}
```

Реализация — добавь поле `pub consume_methods: HashMap<String, Vec<String>>` в `LinearityRegistry` и заполняй при `build`.

### Ф.4.6 Exit-point check для field-paths (D5) (~1 h)

**Logic:**

```rust
// Plan 100.1 (D5): exit-point check для consume-полей метода.
let method_kind = fd.receiver.as_ref().map(|r| (r.consume, r.mutable));
for (path, state) in &ctx.states {
    // Только paths начинающиеся с "@" (field-paths receiver'а).
    if path.first().map_or(false, |s| s == "@") && path.len() > 1 {
        let field_name = &path[1];
        // Get field-type, check is consume.
        // ...
        match (method_kind, state) {
            (Some((true, _)), VarState::Live | VarState::MaybeConsumed(_)) => {
                // consume-метод: поле должно быть Consumed.
                errors.push(Diagnostic::new(format!(
                    "[D133-field-not-consumed] consume-метод `{}` оставляет \
                     consume-field `@.{}` Live; consume-методы должны закрыть \
                     все consume-поля (record self-destructs).",
                    fd.name, field_name), *exit_span));
            }
            (Some((false, _)), VarState::Consumed(_)) => {
                // non-consume метод: поле должно быть Live.
                errors.push(Diagnostic::new(format!(
                    "[D133-field-not-restored] метод `{}` consume'нул consume-field \
                     `@.{}` но не восстановил его до exit'а (re-assignment expected). \
                     Используй D5 reopen pattern: close → consume new = …  → @field = new.",
                    fd.name, field_name), *exit_span));
            }
            _ => {}
        }
    }
}
```

### Ф.4.7 Assignment в Live consume-field (D5.1) (~45 min)

**Найти** handling `Stmt::Assign` / `ExprKind::Assign` (field-assign):

```powershell
Get-ChildItem compiler-codegen/src/types/mod.rs | Select-String "Assign|assign_field"
```

**Logic:**

```rust
// Plan 100.1 (D5.1): assign в Live consume-field → error.
if let Some(target_path) = try_extract_field_path(&assign.target) {
    if let Some(VarState::Live) = ctx.states.get(&target_path) {
        // Только если target-path — consume-typed.
        // (lookup field-type via type-decl).
        if is_consume_path(&target_path, module, lin_reg) {
            errors.push(Diagnostic::new(format!(
                "[D133-assign-live-field] присваивание в Live consume-field/local \
                 `{}` без предшествующего consume — silent leak. Сначала consume: \
                 `{}.<consume-method>()`, затем assign.",
                path_to_str(&target_path), path_to_str(&target_path)),
                assign.span));
        }
    }
    // После assign — path снова Live.
    ctx.declare_path(target_path, None);
}
```

### Ф.4.8 Nested field paths D5.2 invariant (~1 h)

**Logic:** на exit-point в non-consume методе — для каждого Live `@.<f>`
path'а, ВСЕ deeper consume-sub-fields должны быть Live (invariant
preserved).

```rust
// Plan 100.1 (D5.2): nested invariant check.
for (path, state) in &ctx.states {
    if path.first().map_or(false, |s| s == "@") && path.len() == 2 {
        let field_name = &path[1];
        if matches!(state, VarState::Live) {
            // Field Live → все deeper consume-sub-paths должны быть Live.
            let prefix = path.clone();
            for (deeper_path, deeper_state) in &ctx.states {
                if deeper_path.len() > prefix.len()
                    && deeper_path.starts_with(&prefix[..])
                    && matches!(deeper_state, VarState::Consumed(_) | VarState::MaybeConsumed(_))
                {
                    errors.push(Diagnostic::new(format!(
                        "[D133-nested-broken] field `@.{}` Live на exit'е, но \
                         deeper consume-path `{}` Consumed — invariant nested-record \
                         broken. Либо consume `@.{}` целиком, либо restore deeper \
                         path через replace-pattern (D5.2).",
                        field_name, path_to_str(deeper_path), field_name),
                        *exit_span));
                }
            }
        }
    }
}
```

### Ф.4.9 Передача в non-consume param (D2/D7) (~30 min)

**Найти** handling call-args в `walk_expr` для `Call`/`MethodCall`:

```powershell
Get-ChildItem compiler-codegen/src/types/mod.rs | Select-String "Call.*args|consume_idx"
```

**Logic:** для каждого arg, если arg — consume-typed expr И callee-param БЕЗ consume qualifier → error D133-move-to-non-consume.

```rust
for (i, arg) in call.args.iter().enumerate() {
    let arg_path = try_extract_field_path(arg);
    let arg_is_consume = arg_path.as_ref()
        .map(|p| is_consume_path(p, module, lin_reg))
        .unwrap_or(false);
    let param_is_consume = consume_idx.contains(&i);

    if arg_is_consume && !param_is_consume {
        errors.push(Diagnostic::new(format!(
            "[D133-move-to-non-consume] передача consume-value `{}` в non-consume \
             param `{}` функции `{}` — view-borrow не consume'нет, ресурс утечёт. \
             Добавь `consume` qualifier на param, либо привяжи через `consume name = …` \
             перед передачей.",
            path_to_str(arg_path.as_ref().unwrap()),
            callee_param_name, callee_name),
            arg.span));
    }
    if arg_is_consume && param_is_consume {
        if let Some(p) = arg_path {
            ctx.mark_path_consumed(&p, arg.span);
        }
    }
}
```

**D9 rvalue-special case** (separate из 100.3, но реализация рядом):
если `arg` — rvalue (call, literal) consume-typed && callee-param — non-
consume → emit `D133-consume-rvalue-in-view` instead.

### Ф.4.10 Match-pattern на consume value (~30 min)

**Найти** handling `Match` в walk_expr/walk_stmt.

**Logic:** if scrutinee — consume-typed:
- arm-binding'и (`Some(f)`) → declare `f` как Live consume.
- Если arm-body не consume'нет `f` → exit-check сработает (стандартный flow).

`match view` (peek-mode) — отдельно в 100.3, в 100.1 — destructive default.

### Ф.4.11 Branch join (~30 min)

**Существует** `consume_join2` (line 7342) для Plan 73 flat-name path'ов. Расширить
для FieldPath: union по ключам обеих веток.

```rust
fn join_consume_states(
    a: &HashMap<FieldPath, VarState>,
    b: &HashMap<FieldPath, VarState>,
) -> HashMap<FieldPath, VarState> {
    let mut out = HashMap::new();
    let all_keys: HashSet<&FieldPath> = a.keys().chain(b.keys()).collect();
    for k in all_keys {
        let sa = a.get(k).cloned().unwrap_or(VarState::Live);
        let sb = b.get(k).cloned().unwrap_or(VarState::Live);
        out.insert(k.clone(), consume_join2(&sa, &sb));
    }
    out
}
```

### Ф.4.12 Verify (per-sub-step, after each Ф.4.X)

После каждого sub-step:

```powershell
cargo build --release 2>&1 | Select-String "error\["
# затем targeted fixture:
.\nova-cli\target\release\nova.exe check nova_tests/plan100.1/<specific-fixture>.nv
```

После Ф.4.12 (full Ф.4 done):

```powershell
.\nova-cli\target\release\nova.exe test nova_tests/plan100.1 2>&1 | Tee-Object f4-results.log
.\nova-cli\target\release\nova.exe test nova_tests/plan73 nova_tests/basics 2>&1 | Select-String "PASS|FAIL"
```

**Acceptance Ф.4:**
- Все 17 фикстур `nova_tests/plan100.1/` дают expected results (8 pos PASS, 9 neg emit правильный D-код).
- Plan 73 fixtures: 0 regression.

**Commit (после Ф.4.12 completion):**

```powershell
git add compiler-codegen/src/types/mod.rs
git commit -m "feat(plan100.1-F4): check_consume extension — FieldPath + flow analysis (D3/D5/D5.1/D5.2/D7)"
```

---

## Ф.5 — AI-first диагностика + pilot mocks (~3 h, effort=medium, thinking=OFF)

**Цель:** diagnostic messages — действенные, с suggestion. Pilot mocks
для D5/D5.1/D5.2 уже в фикстурах.

### Ф.5.1 Diagnostic format polish (~1 h)

Просмотри все `D133-*` diagnostics из Ф.3-Ф.4, проверь:
- Каждое message начинается с `[D133-<sub-code>]`.
- Содержит **что произошло** (state + position) + **что делать** (suggestion с актуальным consume-method name).
- Для list >4 методов — truncate с «see `nova doc <type>`».

### Ф.5.2 Pilot mock fixtures (~1 h)

Проверь что эти fixtures уже существуют (из Ред. 2 batch'а):

```powershell
ls nova_tests/plan100.1/consume_ok_record_field.nv
ls nova_tests/plan100.1/consume_ok_reopen.nv
ls nova_tests/plan100.1/consume_ok_nested_consume.nv
```

Если нет — создать (используй `consume_ok_commit.nv` как шаблон + расширь).

### Ф.5.3 Runtime mental model (D10) — no-code

D10 — это **только** mental-model для docs. Никаких runtime изменений
в Ф.5 не требуется (codegen pointer-based consume будет в Plan 100.6+).

**Commit:**

```powershell
git add nova_tests/plan100.1/ compiler-codegen/src/types/mod.rs
git commit -m "docs+test(plan100.1-F5): D133 diagnostics polish + pilot mocks coverage"
```

---

## Ф.6 — Tests pos/neg verification (~1 h, effort=low, thinking=OFF)

**17 фикстур уже в `nova_tests/plan100.1/`** (Ред. 2 batch). Действие
— только верификация.

### Ф.6.1 Run full plan100.1 suite

```powershell
.\nova-cli\target\release\nova.exe test nova_tests/plan100.1 2>&1 | Tee-Object f6-results.log
```

**Ожидается:**
- 8 positive PASS
- 9 negative emit правильные `[D133-*]` диагностики

Если какой-то fixture FAIL — debug per memory `feedback-one-pass-debug-investigation`:
1. `.\nova-cli\target\release\nova.exe check <fixture>` — посмотреть actual diagnostic.
2. Сравнить с `// EXPECT_COMPILE_ERROR D133` маркером.
3. Fix либо diagnostic (Ф.4 logic), либо fixture (если EXPECT неверен).

### Ф.6.2 Full regression

```powershell
.\nova-cli\target\release\nova.exe test --workspace 2>&1 | Tee-Object f6-full.log
```

Сравнить с `baseline-tests.log` из PRE-3. Ожидается:
- **+8** PASS (новые plan100.1 pos)
- **0** регрессий vs baseline FAILs

Если регрессия — найти теста через diff baseline-tests.log vs f6-full.log, debug per per-fix verify rule (memory `feedback_targeted_test_per_fix`).

### Ф.6.3 Commit results

```powershell
git add f6-results.log f6-full.log  # либо .gitignore — не commit logs
git commit -m "verify(plan100.1-F6): 17 fixtures PASS as expected; 0 regressions"
```

(или просто отметить в discussion-log, logs не commit'ить.)

**Acceptance Ф.6:** 17 fixtures PASS as expected; 0 регрессий.

---

## Ф.7 — Docs + close (~1 h, effort=low, thinking=OFF)

### Ф.7.1 docs/idiom/consume-types.md

Уже создан в Ред. 2 batch'е. Verify:

```powershell
ls docs/idiom/consume-types.md
```

Если есть — actualize: добавить ссылку на implementation merge commit
hash + дату close'а.

### Ф.7.2 docs/plans/README.md

Обновить row Plan 100.1: status `📋 proposed` → `✅ ЗАКРЫТ <merge-hash>`.

```powershell
Select-String -Path docs/plans/README.md -Pattern "100\.1"
```

### Ф.7.3 docs/plans/100-linear-must-consume.md (umbrella)

Обновить status table: 100.1 ✅.

### Ф.7.4 Logs (per memory `feedback-update-logs`)

1. **docs/project-creation.txt** — append entry «Plan 100.1 foundation
   implementation closed — merge <hash>».
2. **docs/simplifications.md** — обновить `[M-100-impl-deferred]`:
   100.1 ✅, 100.2-100.8 ⏳.
3. **d:/Sources/nv-lang/nova-private/discussion-log.md** — append
   detailed session log.

### Ф.7.5 Merge в main

```powershell
cd d:/Sources/nv-lang/nova
git merge --no-ff plan-100-impl-foundation
git push github main
git push gitverse main
```

**Acceptance Ф.7:** все docs обновлены; ветка merge'нута; push'и done.

---

## Model recommendations

### Per phase

| Фаза | Model | Effort | Thinking | Calendar | Сложность |
|---|---|---|---|---|---|
| Ф.0 | Sonnet 4.6 | low | OFF | 30 min | trivial |
| Ф.1 | — | — | — | done | uже сделано |
| Ф.2 | Sonnet 4.6 | medium | OFF | 4-6 h | mechanical |
| Ф.3 | Sonnet 4.6 | medium | OFF | 3-4 h | mechanical |
| **Ф.4** | **Sonnet 4.6 либо Opus 4.7** | **high** | **ON** | **8-12 h** | **non-trivial flow + nested invariants** |
| Ф.5 | Sonnet 4.6 | medium | OFF | 3 h | mechanical |
| Ф.6 | Sonnet 4.6 | low | OFF | 1 h | verify only |
| Ф.7 | Sonnet 4.6 | low | OFF | 1 h | docs + merge |

### Почему Ф.4 — отдельно

Ф.4 — единственная фаза с **non-trivial design decisions** во время
implementation:
- FieldPath migration: как сохранить backward-compat с Plan 73 flat-name
  ConsumeCtx?
- Nested invariant: какие edge-case'ы exit'a (early return + restore?)
- `try_extract_field_path` heuristics для chain method calls?

При thinking=OFF и effort=medium Sonnet 4.6 может принять wrong design
calls здесь — особенно по migrate-step-у 4.2. Поднять effort + thinking.

**Альтернатива:** Opus 4.7 на Ф.4, остальное Sonnet 4.6. Если бюджет
позволяет — Opus + thinking ON для Ф.4.

### Session size

- Ф.0 / Ф.1: 1 session, no commits in middle.
- Ф.2: 1 session, 1 commit в конце.
- Ф.3: 1 session, 1 commit в конце.
- **Ф.4: 6-8 sessions** (по Ф.4.1-4.12, по 1-2 sub-step за session).
  Commit после каждого working sub-step.
- Ф.5: 1 session, 1 commit.
- Ф.6: 1 session, no code commit (только verify).
- Ф.7: 1 session, 1 merge + 1 push.

**Итого: 12-15 sessions calendar.**

### Risk mitigations

1. **Ф.4.2 migration breaks Plan 73 fixtures.** Verify PRE-4 + Ф.4.2
   verify обязательны. Если plan73 FAIL — rollback Ф.4.2 commit, искать
   missing facade method.
2. **Ф.4.X edge-case в diagnostic format.** Каждый sub-step — targeted
   fixture verify per memory `feedback_targeted_test_per_fix`. Не идти
   в next sub-step пока current не green.
3. **Ф.7 merge conflicts** на `project-creation.txt`/`simplifications.md`.
   Per memory `feedback-update-logs` другие агенты часто пишут в эти
   же файлы — resolve appending chronologically.

### What NOT to do

- 🔴 НЕ `git add -A` или `git add .` — per memory `feedback_git_add_specific`.
- 🔴 НЕ amend commits — per memory commits-per-task.
- 🔴 НЕ skip `Plan 73 regression` verify (Ф.4.2/Ф.4.12) — это safety net.
- 🔴 НЕ начинать Ф.4.3 пока Ф.4.2 verify не зелёный.
- 🔴 НЕ переходить к 100.2/100.3 пока 100.1 не merge'нут (они blocked).
- 🔴 НЕ выдумывать Nova синтаксис — per memory `feedback_nova_syntax`,
  всегда смотреть `spec/decisions/` + `examples/`.

---

## Final acceptance gate (перед merge)

- [ ] PRE-1..4 done (worktree, env, baseline).
- [ ] Ф.0 GATE probe doc + self-check.
- [ ] Ф.2 6 parser fixtures pass.
- [ ] Ф.3 LinearityRegistry + 4 marker checks emit правильно.
- [ ] Ф.4 ALL 12 sub-steps green; ConsumeCtx FieldPath migration без plan73 regression.
- [ ] Ф.5 diagnostics polished; mocks coverage.
- [ ] Ф.6 17 fixtures verify-PASS; full nova test 0 regression vs baseline.
- [ ] Ф.7 docs updated (README + umbrella + idiom + 3 logs); merge done.

**После merge:** unblock Plan 100.2 (generic propagation), 100.3 (borrow/view), 100.4 (defer family). Они могут идти параллельно после foundation closed.
