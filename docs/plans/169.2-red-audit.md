# Plan 169.2 — Red folder-module audit (2026-06-20)

> Полный прогон всех **184 folder-module** в `nova_tests/` (батчами, main d7543606+).
> **37 красных** (~20%). Сгруппированы по корневой причине. Цель — fix-sweep корректности.
> Статусы: ✅ FIXED (эта сессия) · ⏳ TODO · 🔒 GATED (внешний план).

## Сводка по классам

| Класс | Шт | Причина | Подход |
|---|--:|---|---|
| **A. DUP-NAME** | 10 | folder-module пиры переиспользуют top-level имена (Account и т.п.) | rename / `priv(file)` (метод-less) per folder, или not-a-folder-module |
| **B. REFUTABLE-LET** | 7 | чекер reject'ит refutable pattern в `let`/`for` | ОДИН root-cause: test-rot (`let Some`→`if let`/match) ИЛИ over-strict — решить |
| **C. METHOD-GONE** | 5 | метод переименован/удалён (E7320 / E_BOUND_METHOD_REMOVED / int.len / E_STR_NO) | test-rot: на текущее API |
| **D. STD-API/CC** | 7 | std-сигнатуры (too-few-args / member-ref / returning / undeclared) + 2 RUN-assert | test-rot на текущее std API |
| **E. CONSUME-D133** | 2 | D133-not-consumed enforcement | test-rot / consume-аннотации |
| **F. IMPORT** | 2 | import resolution / extension-method-needs-import | резолвер / import |
| **G. SINGLES** | 4 | constexpr / shadow-E7310 / unsafe-extern / GC-runtime | разное (часть gated) |

## A. DUP-NAME (10) — ⏳ TODO
folder-module, где несколько пир-файлов объявляют одно top-level имя:
`doc`, `concurrency`, `plan100_6`, `plan124_1`, `plan124_2`, `plan124_3`, `plan124_6`,
`plan48_mpm`, `plan59_1` (`dup`), `plan61`.
→ Это та же причина, по которой Level-2 консолидация дорогая (см. 169.1.2). Per-folder:
rename переиспользуемых типов (с методами) или `priv(file)` (метод-less).

## B. REFUTABLE-LET (7) — ✅ FIXED 2026-06-20 (commit 75b66763, в main)
`refutable pattern in let`: `plan123_1_2`, `plan123_3`, `plan123_7_5`, `plan123_7_7`,
`plan123_followups_2026_06_05`, `plan124_9`, `plan128_2`.
Корень: заглавные bare-имена переменных (`mut Cv = Cv.new()`) Nova парсит как
variant-pattern (D52/D59, как Rust). **Решение автора: test-rot** → lowercase биндинги
(Cv→cv, Av→av, …), type-ссылки (type/fn/.new/literals/return-types) сохранены. Все 7 PASS.

## C. ~~METHOD-GONE (5) — test-rot~~ → ПЕРЕКЛАССИФИЦИРОВАНО 2026-06-20: компиляторно-глубокий, НЕ test-rot
Расследовано: поверхностные method-rename'ы корректны, но обнажают глубокие баги:
- `plan70_2` — `.@len()`→`.len()` ✓ (test-rot), НО дальше `undefined symbol
  nova_make_LinkedList1____nova_int_Nil` — sum-variant constructor не мономорфизируется (codegen-баг). ⏳ codegen
- `plan91_8a` — `.equals`→`.equal` ✓ (test-rot), НО дальше codegen E7320 на default-body
  protocol-методе (стр.19). ⏳ codegen
- `plan83_12` — `addr.ip()`/`to_str()` возвращают `str` (std/net/addr.nv), но codegen
  типизирует результат как `int` → `int.len` E_PRIMITIVE. Это **nova_int-fallback**
  (return-type метода с эффектом `AddrNet` не резолвится). 🔒 GATED **172 U.4**.
- `plan108_4` — `consume @close()` E7320 (codegen method-resolution). ⏳ codegen
- `plan11_followup` — импортирует `std._experimental.path.path`, который использует
  **13+ `str.len()` + str-индексацию** (оба retired D249). Полная D249-миграция
  _experimental-файла. ⏳ std-migration (_experimental, unstable).

**Вывод:** только **cluster B** был чистый bulk test-rot. Остальное (C/D/E/A) —
per-dir компиляторные заходы (codegen-mono / nova_int-fallback-gated-172 / std-D249 /
structural-dup-name), НЕ быстрый sweep. Worklist = точная классификация, не «21 правка».

## D. STD-API / CC (7) — ⏳ TODO (test-rot на std API)
`plan91/sort_basic` (too-few-args), `plan91_fe4` (too-few-args, pos+neg), `plan91_13`
(returning NovaOpt), `plan91_fe1` (member-ref), `plan65` (undeclared id),
`plan91_8a_2` (RUN-assert display), `plan91_fe2/neg` (RUN-assert overflow variant).

## E. CONSUME-D133 (2) — ⏳ TODO
`plan103_9/guard_cross_scope_transfer`, `plan73/binding_consume_move`.

## F. IMPORT (2) — ⏳ TODO
`plan118_1_7` (import resolution в folder-module peer), `plan153_5_nested`
(E_EXTENSION_METHOD_NEEDS_IMPORT).

## G. SINGLES (4)
- `map_literals/positive_clone_merge` — E_CONST_NOT_CONSTEXPR. ⏳
- `plan123_3_1` — E7310 (Vec-shadow). 🔒 `[M-vec-shadow-leak-e7310]`.
- `plan118_1_unsafe_extern_fn` — unsafe-extern parse. ⏳
- `plan55/f1_closure_array_gc_stress` — RUN-FAIL. 🔒 `[M-169.2-vec-fn-empty-literal-nova-int]` (gate 172 U.4).

## Handoff Plan 172 агенту (все компиляторные баги — для 172)

Решение (2026-06-20): test-rot чиню я (cluster B done; C-surface done); **ВСЕ
компиляторные баги остаются 172**. Формальные маркеры новых compiler-багов:
- **[M-169.2-sum-variant-ctor-mono]** — `plan70_2/f1_linkedlist_int_pos`: generic-sum
  unit-variant constructor не мономорфизируется → linker `undefined symbol
  nova_make_LinkedList1____nova_int_Nil`. (test-rot `.@len`→`.len` уже применён.)
- **[M-169.2-default-body-protocol-dispatch]** — `plan91_8a/default_body_parsed_test`:
  codegen E7320 на вызове protocol-метода с default body (`i.default_method()`).
  (test-rot `.equals`→`.equal` уже применён.)
- **[M-169.2-consume-method-resolve]** — `plan108_4/pos_consume_close_match`: `h.close()`
  на типе с `consume @close()` → E7320 (codegen method-resolution для consume-receiver).
- **[M-169.2-effect-method-ret-fallback]** — `plan83_12/tcp_socket_addr_test`: `addr.ip()`
  возвращает `str` (std/net/addr.nv), но codegen типит результат `int` → `int.len`
  E_PRIMITIVE. Инстанс **[M-172-nova-int-fallback-audit]** (return-type метода с эффектом
  `AddrNet` не резолвится).
- **`plan11_followup`** → `std/_experimental/path/path.nv` использует 13+ retired `str.len()`+
  str-индексацию → полная **D249-миграция** _experimental-файла.

D/E/F кластеры (std-api / consume-D133 / import) — НЕ расследованы детально; dirs +
поверхностные ошибки в таблице выше, 172 расследует. A (dup-name 10) — test-structure
(rename vs not-folder-module), нужен design-call автора.

## Уже починено этой сессией (для контекста, НЕ в списке выше)
- EXPECT-в-root кластер: plan59, plan70_3, plan62, plan70_5, plan138_2 (compiler-fixes §20-23).
- Cluster B (refutable-let, 7): lowercase заглавных биндингов (75b66763).
- Cluster C test-rot surface: plan70_2 `.@len`→`.len`, plan91_8a `.equals`→`.equal` (71f519b3).
