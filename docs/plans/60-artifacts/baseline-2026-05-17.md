# Plan 60 baseline metrics (2026-05-17)

Зафиксировано в Ф.0 на branch `plan-60` (worktree `nova-lang-p60`), 
коммит `dd62ffce562` (main HEAD на момент создания ветки).

## `nova test` baseline

```
PASS: 635  FAIL: 5  SKIP: 44
```

**5 known-FAIL'ов (НЕ регрессии Plan 60):**

| Test | Категория | Причина |
|---|---|---|
| `contracts/duplicate_apply_w2402` | W2402 drift | expected warning pattern не нашёлся |
| `contracts/empty_effect_w2402` | W2402 drift | same |
| `contracts/trivial_double_negation_positive` | `#verify` regression | `#verify` failed для `dbl_neg` |
| `plan11_followup/f2_self_method_static_dispatch_positive` | Windows UAC | spawn exe os error 740 |
| `plan11_followup/f3_self_method_instance_dispatch_positive` | Windows UAC | same |

**Acceptance Plan 60:** после миграции PASS ≥ 635, FAIL ≤ 5 (только known list выше), плюс Plan 60-specific tests добавляются.

## Size-accessor field-style occurrences

Regex pattern: `\.<accessor>[^a-zA-Z_(]` (исключает `length`, `len_t`, `\.len(...)`).

| Accessor | std `.nv` | tests `.nv` | examples `.nv` | spec `.md` | docs `.md` | **TOTAL** |
|---|---:|---:|---:|---:|---:|---:|
| `.len` | 262 | 104 | 27 | 33 | 57 | **483** |
| `.cap` | 1 | 0 | 0 | 3 | 9 | **13** |
| `.is_empty` | 4 | 0 | 0 | 2 | 17 | **23** |
| `.byte_len` | 0 | 0 | 0 | 0 | 4 | **4** |
| **TOTAL field-style** | **267** | **104** | **27** | **38** | **87** | **523** |

`.byte_len` в .nv уже 0 (все usage method-style) — пропускаем миграцию для него, но diagnostic всё равно нужен.

`.cap` — 1 случай в `std/collections/hashmap.nv:145` `@_buckets.cap` → мигрирует в `@_buckets.capacity()` (rename + parens). Mass migration в .md (документация).

**API naming decision (2026-05-17):** `capacity()`, не `cap()`. Прецеденты Rust/C++/Swift; AI-friendly mental mapping. См. Plan 60 §«Цель в одной фразе» / Naming-decision block.

`.is_empty` — 4 случая в std/collections/queue.nv на user-type `[]T` fields. После Plan 60 = `@_inbox.is_empty()`.

Acceptance Ф.6: после миграции — **0 field-style hits** во всех `.nv` файлах (.md — separately, аккуратно — некоторые упоминания иллюстративные).

## Compiler version

```
release build of nova-cli @ dd62ffce562
clang/cl detection: Clang (default per Plan 9 Ф.5)
```
