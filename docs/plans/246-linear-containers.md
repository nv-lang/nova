# План 246 — Линейные контейнеры (`Vec[consume T]`): владение внутри коллекций

> **Статус:** 🔬 ИССЛЕДОВАНИЕ + ПРОЕКТ. Заказ владельца 2026-08-03:
> «оформить `Vec[consume T]` как целевую модель, написать план на реализацию
> и спеку и D». Реализация — ПОСЛЕ тега (см. §Сроки); решение по модели
> нужно раньше, потому что от него зависит приёмка плана
> [244](244-channel-in-nova.md).
>
> Дефект-источник — [№294](221.1-bug-sweep.md).

## 1. Задача, измеренная а не предполагаемая

`consume`-значение неограниченного обобщённого `T` **невозможно положить в
коллекцию**: ни один механизм разряда владения не применим. Следствие,
из-за которого вопрос и всплыл: канал на Nova не может выполнить решение
владельца по №144 (`send(consume v)`) — значение некуда деть.

Три пробы интегратора 2026-08-03:

| Форма | Результат |
|---|---|
| `fn f() -> consume T` | `error: expected type, got consume` — формы нет |
| `Vec[consume T]` в позиции типа | `error: expected type, got consume` — формы нет |
| `send(consume v)` на **обычном** `Vec[int]`, затем `v.push(2)` | отклоняется (`использование потреблённой переменной`, D131) |

Третья строка важна: **`consume`-параметр гасит привязку даже у
некопируемо-нелинейного типа.** Это НЕ побочный эффект, а ровно та защита,
которую владелец получил решением №144 (отправитель не может мутировать
отправленное). Значит «упростить, сделав `consume` пустым для нелинейных
типов» — недопустимо: это молча снимет защиту канала.

## 2. Что в языке УЖЕ есть

| Уровень | Механизм | Смысл |
|---|---|---|
| Параметр/ресивер | **D131** affine `consume` | этот вызов забирает владение; привязка после него мертва |
| Объявление типа | **D133** `type X consume` | инстанс ОБЯЗАН быть потреблён на каждом пути до выхода из области видимости |

То есть линейность как свойство типа у нас есть с D133. **Отсутствует
третье звено: контейнеры в этом не участвуют.**

```nova
export fn Vec[T] mut @push(v T) -> @        // обычный параметр: владение не забирается
```

Коллекция с точки зрения проверки просто копирует значение внутрь и ничего
не заявляет о владении — поэтому «положить в массив» не считается разрядом.

## 3. Три модели-кандидата

### Модель A — парные методы (режим на методе)

`push` копирует, `give(consume v T)` забирает; `pop` копирует,
`take() -> consume T` выносит.

- ➕ не трогает систему типов;
- ➖ удваивает поверхность коллекций (каждый забирающий метод — второй);
- ➖ решение принимается в КАЖДОМ месте вызова, легко ошибиться;
- ➖ правило «нельзя уронить коллекцию с необслуженными элементами» вешается
  отдельно и держится на дисциплине;
- ➖ если делать не именами, а перегрузкой по режиму — новая ось разрешения
  перегрузок. **Категорически против сейчас:** 2026-08-03 в этой машинерии
  найдены два P0 (№254 мисрезолв, №295 схлопывание бланкетов).

### Модель B — `Vec[consume T]` (модификатор в аргументе типа) — ЗАКАЗ ВЛАДЕЛЬЦА

Контейнер объявляет, что его слоты ВЛАДЕЮТ содержимым.

- ➕ одно решение на уровне типа вместо решения на каждом вызове;
- ➕ методы не удваиваются — поведение вытекает из инстанцирования;
- ➕ правило смерти коллекции появляется само: `Vec[consume T]` нельзя
  уронить непустым, это свойство типа;
- ➕ не трогает разрешение перегрузок;
- ➖ модификатор уровня типа: тащить через дженерики, вывод, мономорфизацию;
- ➖ порождает вопросы тождества типов (см. §4).

### Модель C — автопропагация D133 (без нового синтаксиса)

Линейность уже объявлена на типе элемента; контейнер её просто наследует:
`Vec[Transaction]`, где `type Transaction consume`, ведёт себя линейно
автоматически.

- ➕ **ноль нового синтаксиса** — информация уже в объявлении `T`;
- ➕ невозможно ошибиться: нельзя случайно положить линейное значение в
  «нелинейный» контейнер, такого контейнера просто нет;
- ➖ **не покрывает главный случай — канал**: там `T` неограничен и обычно
  НЕ линеен (`Vec[int]`, `str`), а владение забрать надо всё равно
  (защита №144). Модель C для канала бесполезна.

### Вердикт интегратора

**Целевая модель — B, но она НАДСТРАИВАЕТСЯ над C, а не заменяет её.**

C — обязательная часть в любом случае: если тип объявлен `consume` по D133,
контейнер обязан это уважать, иначе линейность дырява (сегодня `Vec[File]`
молча роняет незакрытые файлы — это дефект уже сейчас, независимо от канала).

B добавляет то, чего C не даёт: возможность забрать владение у **нелинейного
по объявлению** типа, потому что этого требует конкретный контейнер. Ровно
случай канала.

Модель A отклоняется: удвоение поверхности плюс риск новой оси разрешения.

## 4. Вопросы, требующие D-слова владельца

1. **Тождество типов.** `Vec[Box]` и `Vec[consume Box]` — разные типы или
   один с флагом? *Рекомендация:* разные. Иначе присваивание одного другому
   молча теряет или приобретает обязательство.
2. **Односторонняя совместимость.** Допустимо ли передать `Vec[consume Box]`
   туда, где ждут `Vec[Box]`? *Рекомендация:* нет, ни в какую сторону —
   любое послабление открывает дыру (владеющий контейнер, отданный как
   невладеющий, потеряет обязательство).
3. **Наследование в обобщённой функции.** `fn f[U](v Vec[U])`, вызванная с
   `Vec[consume Box]`, — что такое `U`? *Рекомендация:* `U` несёт
   линейность; функция, не разряжающая элементы, обязана вернуть контейнер
   или отклоняться.
4. **Смерть контейнера.** *Рекомендация:* уронить непустой `Vec[consume T]`
   — ошибка компиляции; для намеренного случая — явный слив (`drain`),
   отдающий элементы вызывающему по одному.
5. **Взаимодействие с `#share` и `ro`.** *Рекомендация:* `ro Vec[consume T]`
   разрешён (читать можно, вынести нельзя); `#share` — запрещён до
   отдельного разбора, разделяемая линейность требует своей модели.
6. **Другие контейнеры.** `HashMap[K, consume V]`, `Option[consume T]`,
   кортежи. *Рекомендация:* правило общее для всех контейнеров, а не для
   `Vec`; вводить сразу общим, иначе получим разнобой.

## 5. Проект D-блока (черновик для спеки)

> Размещение: `spec/decisions/02-types.md`, рядом с D133. Номер **D444**
> (свободен на 2026-08-03; проверить перед вливанием).

```markdown
## D444. `Vec[consume T]` — линейные контейнеры (владение внутри коллекций)

> Plan 246. Расширяет D131 (affine `consume` на параметре) и D133
> (`type X consume` — must-be-consumed) на третье звено: контейнеры.

### Что

Модификатор `consume` в позиции аргумента типа: `Vec[consume T]`,
`HashMap[K, consume V]`, `Option[consume T]`. Означает, что слоты
контейнера ВЛАДЕЮТ содержимым:

- помещение элемента ЗАБИРАЕТ владение (законный разряд D131);
- извлечение элемента ОТДАЁТ владение вызывающему (тот обязан его разрядить);
- уронить непустой контейнер — ошибка компиляции; намеренная форма — `drain`.

### Автопропагация D133

Если `type X consume` (D133), то `Vec[X]` линеен ВСЕГДА — писать
`Vec[consume X]` не требуется и не запрещено (форма избыточна, но законна).
Модификатор нужен для типов, НЕ объявленных линейными, когда владение
требуется по месту — канонический случай: буфер канала (`send(consume v)`,
D-решение №144).

### Тождество и совместимость

`Vec[T]` и `Vec[consume T]` — РАЗНЫЕ типы. Неявное преобразование в обе
стороны запрещено: невладеющий контейнер, принятый как владеющий, породил бы
обязательство из ниоткуда; владеющий, отданный как невладеющий, потерял бы
его.

### Обобщённые функции

Параметр-тип наследует линейность аргумента. Функция, получившая линейный
контейнер и не разрядившая элементы, обязана вернуть его наружу.

### Границы

`ro Vec[consume T]` — разрешён (чтение да, вынос нет). `#share` с линейными
элементами — ЗАПРЕЩЁН до отдельного решения.
```

## 6. Фазы реализации

- **Ф.1 — парсер и представление типа:** `consume` в позиции аргумента типа;
  тождество типов (вопрос 1); отказ от неявных преобразований (вопрос 2).
- **Ф.2 — автопропагация D133:** `Vec[X]` для `type X consume` линеен без
  модификатора. **Это самостоятельная ценность:** закрывает сегодняшнюю дыру
  «`Vec[File]` молча роняет незакрытые файлы».
- **Ф.3 — методы коллекций:** помещение = разряд, извлечение = обязательство,
  `drain`. Общее правило для `Vec`/`HashMap`/`Option`/кортежей (вопрос 6).
- **Ф.4 — смерть контейнера** (вопрос 4): ошибка на непустом.
- **Ф.5 — дженерики и мономорфизация** (вопрос 3): наследование линейности.
- **Ф.6 — фикстуры и спека:** neg на каждое правило, pos на законные формы;
  D444 тем же слиянием.

## 7. Сроки и связь с планом 244

**Реализация — ПОСЛЕ тега.** Довод: это изменение системы типов, а на
2026-08-03 в резолве/мономорфизации открыты два P0 (№254, №295); класть
поверх них новую ось — плохая ставка перед релизом.

**Но решение по модели нужно РАНЬШЕ**, потому что от него зависит приёмка
плана 244:

> Сегодняшний ВСТРОЕННЫЙ канал защиту №144 ДАЁТ (проверено пробой).
> Перевод канала на Nova без №294 эту защиту ПОТЕРЯЕТ.

Отсюда в приёмку 244 добавляется условие: **регресс гарантии №144
недопустим**. Возможный дешёвый обход, проверяемый в Ф.1 плана 244: буфер
канала — слоты фиксированного размера, запись идёт ПРИСВАИВАНИЕМ в
существующий слот, а не вызовом `push`. Если присваивание в слот считается
законным разрядом владения, канал закрывается без единого языкового
изменения, а этот план спокойно уезжает за тег как общее решение для
коллекций.
