# Plan 85.1 — Полное тестовое покрытие `StringBuilder`

> **Статус:** 📋 proposed 2026-05-22, не начат
> **Приоритет:** P1 | **Оценка:** ~1 день
> **Родительский план:** [Plan 85](85-builtin-types-test-coverage.md)
> **Зависимости:** нет (standalone)

## Текущее покрытие

| Файл | Что покрывает |
|---|---|
| `nova_tests/runtime/string_builder.nv` | smoke: `new`, `append(str)`, `into` |
| `nova_tests/runtime/builder_chaining.nv` | chaining: append→append→into |
| `nova_tests/plan11_followup/f11_stringbuilder_new_methods_positive.nv` | `starts_with`, `ends_with`, `is_empty`, `peek` |
| `nova_tests/plan11_followup/f15_stringbuilder_consumed_negative.nv` | use-after-consume → panic |

## Полный API (std/runtime/string_builder.nv)

| Метод | Статус |
|---|---|
| `StringBuilder.new()` | smoke ✅ |
| `StringBuilder.with_capacity(n int)` | ❌ не покрыт |
| `StringBuilder.from(s str)` | ❌ не покрыт |
| `StringBuilder.from(c char)` | ❌ не покрыт |
| `@append(s str) -> @` | smoke ✅ |
| `@append(c char) -> @` | ❌ не покрыт |
| `@plus(s str) -> Self` | ❌ не покрыт |
| `@plus(c char) -> Self` | ❌ не покрыт |
| `@len() -> int` | поверхностно |
| `@byte_len() -> int` | ❌ не покрыт |
| `@capacity() -> int` | ❌ не покрыт |
| `@clone() -> Self` | ❌ не покрыт |
| `consume @into() -> str` | smoke ✅ |
| `@starts_with(prefix str) -> bool` | f11 ✅ |
| `@ends_with(suffix str) -> bool` | f11 ✅ |
| `@is_empty() -> bool` | f11 ✅ |
| `@peek() -> str` | f11 ✅ |

## Файлы тестов (новый каталог `nova_tests/str_builder/`)

| Файл | Что покрывает |
|---|---|
| `constructors.nv` | `new`, `with_capacity(0/1/100)`, `from(str)`, `from(char)`, `from("")`, `from('A')`, `from('Я')` |
| `append_ops.nv` | `@append(str)`, `@append(char)` ASCII/Unicode/emoji, `@plus`, chaining, пустой append |
| `metrics.nv` | `@len` codepoints, `@byte_len` bytes, `@capacity` ≥ len, after grow |
| `clone.nv` | `@clone` — мутация клона не влияет на оригинал и наоборот |
| `inspection.nv` | `@starts_with`/`@ends_with` пустой prefix/suffix, Unicode, false-case; `@peek` перед/после append |
| `consume.nv` | `@into()` → результат; use-after-consume (EXPECT_RUNTIME_PANIC) |
| `unicode.nv` | Кириллица и emoji во всех методах: `byte_len` > `len`, `starts_with`/`ends_with` Unicode |

## Фазы

### Ф.1 — Constructors + append (0.5 д)
- [ ] `nova_tests/str_builder/constructors.nv`
- [ ] `nova_tests/str_builder/append_ops.nv`
- [ ] Если `from(char)` или `append(char)` некорректны → fix codegen

### Ф.2 — Metrics + clone + inspection (0.5 д)
- [ ] `nova_tests/str_builder/metrics.nv`
- [ ] `nova_tests/str_builder/clone.nv`
- [ ] `nova_tests/str_builder/inspection.nv`

### Ф.3 — Consume + unicode (0.5 д)
- [ ] `nova_tests/str_builder/consume.nv`
- [ ] `nova_tests/str_builder/unicode.nv`

### Ф.4 — Финал
- [ ] `nova test --filter str_builder/` → 0 FAIL
- [ ] Полный `nova test` → 0 новых FAIL

## Acceptance criteria

- [ ] Все 17 методов — ≥1 тест
- [ ] `clone` isolation — отдельный тест (мутация клона ≠ оригинал)
- [ ] `with_capacity` — capacity ≥ requested
- [ ] `append(char)` + `from(char)` с Unicode (кириллица, emoji)
- [ ] `byte_len` > `len` для многобайтных строк
- [ ] use-after-consume → EXPECT_RUNTIME_PANIC

## Non-scope

- StringBuilder как часть Display/fmt — Plan 85.4
- Бенчмарки / throughput — Plan 57
