# Plan 208 Ф.0 — прогресс (checkpoint для возобновления)

Worktree: `d:/Sources/nv-lang/nova-208`, branch `plan208-spec` (from `899ae7ab7`).
Модель: sonnet. Только спек-правки (spec/decisions/), без кода/без CU-гейта.

## Сделано

1. ✅ **D422 (keystone)** — `spec/decisions/02-types.md`, добавлен в конец файла (после D419).
   Коммит `a6ba42d3b`.
   - Секции: Write (минимальный, []u8, инфаллибельный) · Fmt (use Write embed, D145) ·
     Display/Debug REQUIRED-примитив + инвариант «нет цикла» · derived Display≠Debug форма ·
     буфер-примитивы (.nv) + fmt_f64_into (extern "C", D282 литеральное имя) · энумы
     Align/Sign/FmtKind/FloatKind (D406) · StringBuilder-аменд API · Статус реализации
     (Ф.0 done, Ф.1-4 pending) · Почему · Связь (cross-refs D419/D374/D237/D229/D179/D55/
     D145/D406/D282/D176/D73).
2. ✅ **D419** — amend-note (RETRACT/SUPERSEDED → D422) в начале блока `02-types.md`.
   Тот же коммит `a6ba42d3b`.
3. ✅ **D374** — amend-note ×2 (sink Write→Fmt; @write_str(str)→@write([]u8)). Тот же коммит.
4. ✅ **D237** — amend-note (сигнатуры @display/@debug → (mut f Fmt), имена без изменений).
   Тот же коммит.
5. ✅ **D229** — amend-note (диспетч ${expr:?} через @debug(f), радикс через f.kind()).
   Тот же коммит.

## Осталось

Всё сделано. Итог:

6. ✅ **D55-аменд** (`spec/decisions/02-types.md`, новая подсекция «Str-литерал → `[]u8`
   coercion (D55 amend, Plan 208 Ф.0, 2026-07-15)» перед «Record-coercion», ~строка 1264+).
   Коммит `e827d637b`.
7. ✅ **D179** — amend-note в `spec/decisions/08-runtime.md` (~строка 4400). Коммит `cb4a66f0a`.
8. ✅ **README.md строка** — `spec/decisions/README.md` (~строка 176, после D421). Коммит `4612d6b78`.
9. ✅ Самопроверка связности — все новые `#dXXX`-якоря вручную сверены с реальными
   GitHub-slug'ами существующих заголовков (D419/D374/D237/D229/D179/D55/D145/D406/D282/D176/
   D422-self); найдена и исправлена ОДНА опечатка (кириллица «аб» вместо латиницы «ab» в якоре
   D282, 2 места) — коммит `4612d6b78`. Конфликт-маркеров нет (грепано перед каждым коммитом).

**ВСЕ 5 коммитов на branch `plan208-spec`:** `a6ba42d3b` (D422+D419/D374/D237/D229),
`e827d637b` (D55), `cb4a66f0a` (D179), `4612d6b78` (README+typo-fix).

Ф.0 ЗАВЕРШЕНА. Ждёт owner sign-off (язык-меняющее, полный conformance не гонялся — спека не
компилируется, по заданию).

## Неопределённости / находки (для отчёта владельцу)

- **str.from_debug**: 174.2-notes (`docs/plans/wip/174.2-scalar-to-str-notes.md` секция «ВАЖНАЯ
  находка вне карты») явно оставили `str.from_debug(@)` (Debug-протокол default-body,
  `std/prelude/protocols.nv`) НЕТРОНУТЫМ и, похоже, мёртвым/нереализованным символом (нет
  dispatch-ветки в emit_c.rs). Существующий D73-баннер (08-runtime.md ~1768) утверждает
  parenthetically «ретрактировано Plan 174.2» — это НЕ совсем точно для from_debug конкретно.
  Я это отразил в D422 §Связь как честную формулировку («остаётся мёртвым ДО D422, Ф.2
  удаляет default-body полностью») — не переписывал сам D73-баннер (не было явно поручено
  трогать D73, только D419/D374/D237/D229/D179 + D55). Если владелец хочет — можно добавить
  отдельный мини-аменд в D73, но текущая формулировка в D422 §Связь уже покрывает факт.
- D145 — подтверждено, ЭТО правильный якорь для protocol-embed `use` (не путать с ошибочным
  предположением, что D145 только про `fn[T]`-префикс — секция «Protocol composition (Plan
  101.4)» внутри того же D145-блока, 02-types.md:6557-6647, документирует именно `use A, B`
  embed).
