// Приёмка 172.15 Ф.1 (критерий владельца): `str.bytes()` возвращается
// на стеке вызывающего — буфер дескриптора вида берётся из кадра
// вызывающего, аллокации в цепочке нет.
fn use_bytes(s str) -> int {
    ro b = s.bytes()
    b.len()
}
fn main() -> () {
    assert(use_bytes("hello") == 5)
}
