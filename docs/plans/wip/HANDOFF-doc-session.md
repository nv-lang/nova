# Передача доко-сессии от интегратора (2026-08-05)

Накопленное за сутки — всё в вашей зоне, у меня не делается.

1. **roadmap.md полариса — исключить из PUBLISHED.list** (вывод консультанта:
   жанр = docs/plans, попал в публикацию механически при первичной описи;
   буквы релиз-нейтральности не нарушает, духу противоречит). Моё
   полярис-окно по ошибке закоммитило это в своей ветке — коммит велено
   откатить, правку делаете вы.
2. **Жанровый фильтр в манифестах пакетных реп**: сейчас проверяется только
   парность языков — следующая дорожная карта или журнал изменений уедут на
   сайт так же незаметно.
3. **`docs/guide/typed-pointers.md:115`** — пример `*mut uninit u8` описывает
   БУДУЩЕЕ поведение (после фикса №358 write-cap); спека в D246 —
   сегодняшнее (`*uninit u8`). Пока №358 не влит, гайду нужна пометка
   «после фикса»; после влития — пометка снимается. №358 в работе.
   Общая проблема «гайд документирует замысел, а не поведение» — вводите
   маркер-конвенцию, у консультанта это названо системным.
4. **`spec/decisions/**` ВНЕ периметра стража примеров** (`check-doc-examples.sh`
   не рекурсивен и decisions не смотрит) — а D-блоки публикуются на сайте.
   Снятые формы в примерах решений уходят на сайт без проверки. Решить:
   расширять периметр стража или пометить decisions на сайте как архивный
   норматив.
5. **Перенос истории D-блоков — НЕ делаем** (консультация 2:
   PROGRESS-doc-consult-2.md): реально переносимо 0.9% файла, выигрыш
   ничтожен. За тегом — только устранение дублей (§V3.3/V3.4 задублирован
   4×).
6. Ваша сверка backlog принята: №286 / M-126-sum-*-rich / M-91.8c помечены
   устаревшими (коммит 6c25f84a0).
