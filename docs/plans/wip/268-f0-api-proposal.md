# Plan 268, Ф.0 — пооперационная проработка API (`Os`/`Fs`/`Net`)

Основание — [D456](../../spec/decisions/04-effects.md#d456-граница-эффекта--публичный-api-на-nova-а-не-таблица-системных-вызовов-план-268-владелец-2026-08-11)
и [план 268](../268-effect-boundary-reform.md), фаза Ф.0. Документ пишется ДО
кода: код не менялся, ничего не собиралось и не гонялось. Источники — сами
файлы `std/src/os/effect.nv`, `std/src/os/os.nv`, `std/src/os/mock.nv`,
`std/src/os/ffi.nv` и парные для `Fs`/`Net`, прочитаны целиком.

Правило для каждой строки: колонка «что нарушено» — конкретный пункт списка
D456 (п.1–8) или прямая цитата из плана 268 (для случаев вне восьми пунктов,
например «тип обещает возврат, которого не бывает»). Колонка «мок-автор» —
не форма, а последствие: что автору мока придётся знать/делать до и после.

Пометка **†** у сигнатуры — форма зависит от [вопроса 1](#вопросы-владельцу)
(проходит ли `Vec[T]` через vtable эффекта как Ok-payload). Пока это не
проверено пробой, таблица даёт сигнатуру-намерение и в тексте под таблицей —
запасной вариант на основе уже ДОКАЗАННОГО рабочим прецедентом (`Net.lookup`,
`(array_base, count)`).

---

## 1. `Os` — 17 операций

| # | было | стало | нарушено | что меняется у автора мока |
|---|---|---|---|---|
| 1 | `arg_count() -> int` | *(операция удаляется — слита в №2)* | п.3, п.6 | — |
| 2 | `arg_at(i int) -> str` | `args() -> []str` † | п.3 (обход по индексу: `arg_count`+`arg_at` — ровно пример из D456), п.6 (счётчик рядом с данными) | Сегодня мок обязан держать `arg_count()` синхронным с диапазоном `arg_at(i)` — это два метода, которые ОБЯЗАНЫ согласиться. Завтра — один метод, возвращающий список; рассинхронизации не существует физически. |
| 3 | `env_get(key []u8) -> Option[str]` | `env_get(key []u8) -> Option[[]u8]` | п.7 (шапка файла прямо признаёт: «string getters return raw bytes verbatim» — в `str` кладутся байты без гарантии UTF-8) | Мок сегодня отдаёт `str` через `to_str_unchecked` — обязан помнить, что это не настоящая строка. Завтра отдаёт `[]u8` напрямую, никакого "почти-строкового" типа, который врёт компилятору. |
| 4 | `env_has(key []u8) -> bool` | *(операция удаляется)* — `has_env()` в `os.nv` вычисляется через `env_get(key).is_some()` | не входит в 8 пунктов буквально, но тот же принцип, что п.3: два метода эффекта (`env_get`/`env_has`), которым мок обязан верить ОБА и одинаково | Сегодня мок пишет `mem_env_has` независимо от `mem_env_get` — ничто не мешает им разойтись (например, забыть обновить одно при правке другого). Завтра — одна операция, `has_env` не существует на границе вообще. |
| 5 | `env_set(key []u8, val []u8) -> int` | `env_set(key []u8, val []u8) -> Result[(), IoError]` | п.1 (0/-errno) | Мок сегодня обязан знать, что `0` = успех и отрицательное число = «POSIX errno» (`0 - 22` для EINVAL и т.п., см. `real_os`). Завтра — `Ok(())`/`Err(IoError...)`, без знания errno-номеров. |
| 6 | `env_remove(key []u8) -> int` | `env_remove(key []u8) -> Result[(), IoError]` | п.1 | То же самое, что №5. |
| 7 | `env_len() -> int` | *(операция удаляется — слита в №9)* | п.3, п.6 | — |
| 8 | `env_key_at(i int) -> str` | *(операция удаляется — слита в №9)* | п.3, п.7 | — |
| 9 | `env_val_at(i int) -> str` | `vars() -> []EnvEntry` †, где `EnvEntry` — байт-первый эффект-уровневый record (`key []u8, value []u8`; НЕ путать с публичным `EnvVar{key str, value str}` из `os.nv`, тот строится СНАРУЖИ границы) | п.3 (это буквально пример из D456: «`env_len`+`env_key_at`+`env_val_at`… мок отдаст пять ключей и четыре значения — и не покраснеет НИЧЕГО»), п.7 | Сегодня `MockOs` держит ТРИ параллельных массива (`env_keys`/`env_vals`/`env_live`) и обязан вручную поддерживать их согласованность построчно (`@nth_live`, тombstone-логика). Завтра — один метод, отдающий список пар; внутреннее хранение мока может остаться каким угодно, но ГРАНИЦА больше не требует трёхстороннего согласия. |
| 10 | `cwd() -> str` | `cwd() -> Result[[]u8, IoError]` | п.2 (пустая строка = ошибка — sentinel), п.7 (путь как `str`, тогда как `Fs` уже несёт пути как `[]u8`; несогласованность между `Os` и `Fs` для одной и той же сущности «путь») | Сегодня мок обязан возвращать `""`, чтобы просигналить ошибку — ничем не отличимо от «текущая директория — пустая строка» (гипотетически). Завтра — `Err(...)` явно, `Ok(bytes)` — данные без совмещённого смысла. |
| 11 | `set_cwd(path []u8) -> int` | `set_cwd(path []u8) -> Result[(), IoError]` | п.1 | Как №5. |
| 12 | `temp_dir() -> str` | `temp_dir() -> []u8` | п.7 (документировано как никогда не пустое — здесь **не** п.2, ошибки не бывает по контракту; но байты пути всё ещё должны быть `[]u8`, не `str`) | Мок отдаёт сырые байты пути напрямую, не обязан гарантировать их валидный UTF-8, чтобы удовлетворить сигнатуру. |
| 13 | `home_dir() -> str` | `home_dir() -> Option[[]u8]` | п.2 (пустая строка = «нет дома»), п.7 | Сегодня «нет домашней директории» кодируется пустой строкой — неотличимо от результата `home_dir()` НА платформе, где путь пуст (гипотетически, но тип это не запрещает). Завтра — `None` прямым текстом. |
| 14 | `exit(code int) -> int` | `exit(code int) -> ()` | вне восьми пунктов — прямая цитата плана: «тип обещает возврат, которого не бывает» (см. [вопрос 3](#вопросы-владельцу): выбор между `()` и гипотетическим bottom-типом) | Возврат сегодня ВСЕГДА игнорируется с обеих сторон (`real_os` физически не возвращается; `mock_os` возвращает `0`, вызывающая сторона делает `ro _ = Os.exit(code)`). Автору мока сегодня приходится выдумывать бессмысленное число для возврата; завтра — нечего выдумывать. |
| 15 | `pid() -> int` | без изменений | — (не нарушение: `int` здесь легитимные ЧИСЛОВЫЕ ДАННЫЕ, не хендл, нигде не передаётся туда, где ожидается что-то другое) | — |
| 16 | `hostname() -> str` | `hostname() -> Result[str, IoError]` | п.2 (пустая строка = ошибка) | Мок сегодня для ошибки обязан вернуть `""`; в остальном hostname — легитимно текстовые ASCII-данные (DNS-имя), поэтому `str`, а не `[]u8`, оправдан. Завтра — `Err(...)` явно. |
| 17 | `process_run(program []u8, argv []u8, argc int, env []u8, envc int, use_env bool, cwd []u8) -> (int, int)` | `process_run(req ProcessSpawnRequest) -> (int, int)`, где `ProcessSpawnRequest { program []u8, argv []u8, argc int, env Option[[]u8], envc int, cwd Option[[]u8] }` | п.8 (семь позиционных параметров + два счётчика + один флаг-переключатель — ровно эталонный пример D456, там же D453) | Сегодня мок обязан помнить порядок из семи параметров и смысл булева `use_env` (включает или выключает соседний параметр `env`) и пустых-байт-как-«наследовать cwd». Завтра — одна запись; `env`/`cwd` — `Option`, само наличие `Some`/`None` заменяет отдельный флаг и sentinel-пустоту разом. `argc`/`envc` остаются как счётчики NUL-блоба — см. [вопрос 1](#вопросы-владельцу): именованное исключение, пока не закрыт Q1. |

**Итог по `Os`:** было 17 операций, предлагается 14 (три слияния: 1+2→1,
4→удалена, 7+8+9→1).

---

## 2. `Fs` — 26 операций

Дескриптор везде заменён на именованный тип `Fd` (`value priv { raw int }`,
конструктор package-private) — см. [вопрос 2](#вопросы-владельцу) насчёт
формы; таблица предполагает вариант «полная непрозрачная обёртка» как
рабочую гипотезу, окончательная форма — решение владельца.

| # | было | стало | нарушено | что меняется у автора мока |
|---|---|---|---|---|
| 1 | `open(path []u8, flags int, mode int) -> int` | `open(path []u8, flags int, mode int) -> Result[Fd, IoError]` | п.1 (fd/-errno в одном int), п.5 (голая ручка `int`) | Мок сегодня возвращает и «успешный fd», и «код ошибки» одним и тем же типом `int` — вызывающая сторона отличает их по знаку. Завтра — `Ok(Fd)`/`Err(IoError)`, ветка ошибки не спутать с удачным дескриптором никаким числом. |
| 2 | `close(fd int) -> int` | `close(fd Fd) -> Result[(), IoError]` | п.1, п.5 | Как №1; плюс — `Fd` нельзя перепутать с номером строки/другим `int`-параметром при вызове мока изнутри теста. |
| 3 | `read(fd int, buf mut []u8) -> int` | `read(fd Fd, buf mut []u8) -> Result[int, IoError]` | п.1, п.5 (buffer-fill форма сама по себе НЕ нарушение — тот же приём, что `Io.read_in`/`Net.read`, уже принят как идиома) | Мок отличает «прочитано n байт (n==0 — EOF)» от «ошибка» по знаку `int`; завтра — по варианту `Result`. |
| 4 | `write(fd int, data []u8) -> int` | `write(fd Fd, data []u8) -> Result[int, IoError]` | п.1, п.5 | Как №3. |
| 5 | `read_at(fd int, buf mut []u8, offset int) -> int` | `read_at(fd Fd, buf mut []u8, offset int) -> Result[int, IoError]` | п.1, п.5 | Как №3. |
| 6 | `write_at(fd int, data []u8, offset int) -> int` | `write_at(fd Fd, data []u8, offset int) -> Result[int, IoError]` | п.1, п.5 | Как №3. |
| 7 | `seek(fd int, offset i64, whence int) -> i64` | **операция сегодня НЕ ВЫЗЫВАЕТСЯ ни одним потребителем** (проверено грепом: `Fs.seek(` не встречается нигде в `std`; `File @seek` в `fs.nv` считает позицию сам, через `fstat`, минуя эту операцию). Предлагается удалить как мёртвую; если позиционирование понадобится на границе позже — вводить заново с `SeekFrom` (уже импортирован в `fs.nv`) вместо магических `whence`-констант `SEEK_SET/CUR/END` | п.1 (i64/-errno), плюс магический int-параметр `whence` вместо суммы | Цена правки этой строки — НОЛЬ: мок сегодня не обязан её вообще реализовывать осмысленно (никто не проверяет). Пункт называю явно, а не молчу — по духу D456 «молчаливое исключение запрещено». |
| 8 | `sync_all(fd int) -> int` | `sync_all(fd Fd) -> Result[(), IoError]` | п.1, п.5 | Как №1. |
| 9 | `sync_data(fd int) -> int` | `sync_data(fd Fd) -> Result[(), IoError]` | п.1, п.5 | Как №1. |
| 10 | `stat(path []u8, img mut []u8) -> int` | `stat(path []u8) -> Result[Metadata, IoError]` | п.1, п.4 (`img mut []u8` — out-параметр, в который C пишет сырую структуру; ИМЕННО пример из плана 268: «пишет C-структуру в байтовый образ») | Сегодня мок обязан звать `fs_stat_build_into` — ЧУЖУЮ C-FFI функцию — чтобы собрать байтовый образ, который потом парсят обратно `fs_stat_size`/`fs_stat_mode`/… Это мок, вызывающий FFI, чтобы притвориться — обратный принцип мока. Завтра мок строит `Metadata{...}` обычным record-литералом на Nova, без единого вызова в C. |
| 11 | `lstat(path []u8, img mut []u8) -> int` | `lstat(path []u8) -> Result[Metadata, IoError]` | п.1, п.4 | Как №10. |
| 12 | `fstat(fd int, img mut []u8) -> int` | `fstat(fd Fd) -> Result[Metadata, IoError]` | п.1, п.4, п.5 | Как №10, плюс `Fd` вместо `int`. |
| 13 | `mkdir(path []u8, mode int) -> int` | `mkdir(path []u8, mode int) -> Result[(), IoError]` | п.1 | Как Os №5. |
| 14 | `remove_file(path []u8) -> int` | `remove_file(path []u8) -> Result[(), IoError]` | п.1 | Как №13. |
| 15 | `remove_dir(path []u8) -> int` | `remove_dir(path []u8) -> Result[(), IoError]` | п.1 | Как №13. |
| 16 | `rename(src []u8, dst []u8) -> int` | `rename(src []u8, dst []u8) -> Result[(), IoError]` | п.1 | Как №13. |
| 17 | `symlink(target []u8, link []u8) -> int` | `symlink(target []u8, link []u8) -> Result[(), IoError]` | п.1 | Как №13. |
| 18 | `chmod(path []u8, mode int) -> int` | `chmod(path []u8, mode int) -> Result[(), IoError]` | п.1 | Как №13. |
| 19 | `copy_file(src []u8, dst []u8) -> int` | `copy_file(src []u8, dst []u8) -> Result[int, IoError]` | п.1 (Ok-payload — осмысленный счётчик байт, не ошибка) | Как №13, но успешная ветка остаётся числом (байты скопированы) — это не нарушение, число здесь ДАННЫЕ, не код ошибки. |
| 20 | `fsync_dir(path []u8) -> int` | `fsync_dir(path []u8) -> Result[(), IoError]` | п.1 | Как №13. |
| 21 | `scandir_open(path []u8) -> int` | `read_dir(path []u8) -> Result[[]DirEntry, IoError]` † — см. ниже, слияние №21–25 | п.1, п.5, и по духу п.3: **сегодня это handle-based обход, тот же смысл, что index-based** — четыре операции обязаны разделять состояние одного потока | Сегодня мок (`MemFs`) держит инстанс-поля `scan_names`/`scan_kinds`/`scan_pos` — собственный маленький итератор, который обязан продвигаться синхронно с четырьмя вызовами `next`/`name`/`kind`/`close`. Единственный потребитель — `read_dir` в `fs.nv` — ВСЕГДА вычитывает поток целиком, ни разу не останавливаясь на середине (комментарий в `fs.nv` это подтверждает прямым текстом). Пять операций существуют ради потребителя, которому нужен один вызов. Завтра — мок строит список `DirEntry` и отдаёт его одним значением; никакого внутреннего курсора на границе. |
| 22 | `scandir_next(h int) -> int` | *(операция удаляется, поглощена №21)* | см. №21 | — |
| 23 | `scandir_name(h int) -> str` | *(операция удаляется, поглощена №21)* | см. №21, плюс п.7 (`str` из сырых байт имени файла) | — |
| 24 | `scandir_kind(h int) -> int` | *(операция удаляется, поглощена №21)* | см. №21 | — |
| 25 | `scandir_close(h int) -> ()` | *(операция удаляется, поглощена №21)* | см. №21 | — |
| 26 | `realpath(path []u8) -> (int, str)` | `realpath(path []u8) -> Result[[]u8, IoError]` | п.1 (int-код рядом с результатом), п.7 (`str` для пути — путь может быть не-UTF-8 на Unix, как и любой другой путь в этом же эффекте; несогласованность с тем, что остальные path-параметры уже `[]u8`) | Мок сегодня возвращает `(errno_int, path_str)` парой; завтра — `Result[[]u8, IoError]`, тот же приём, что и везде в этом эффекте. |

**Итог по `Fs`:** было 26 операций, предлагается 21 (пять `scandir_*`
слиты в одну `read_dir`; `seek` — кандидат на удаление как мёртвая).

Если Q1 (`Vec[T]` через vtable) даёт ОТРИЦАТЕЛЬНЫЙ ответ — запасной вариант
для строк 2 и 21: `vars()`/`read_dir` возвращают `(int base, int count)`
по прецеденту `Net.lookup`, а список строится СНАРУЖИ границы (`os.nv`/
`fs.nv`), как уже делает `dns.nv @build_addrs`. Форма вызова у автора мока
не меняется в худшую сторону — он всё равно пишет ОДНУ функцию вместо
согласованной тройки/пятёрки, разница только в том, из чего эту функцию
строит `fs.nv`/`os.nv` СНАРУЖИ.

---

## 3. `Net` — 25 операций

`Net` — «недоделанная миграция» по формулировке D456: большинство операций
уже соответствуют норме (`Result[T, NetError]`, значимые хендлы-record). Ниже
— все 25, включая те, что не меняются (это тоже ответ, не пропуск).

| # | было | стало | нарушено | что меняется у автора мока |
|---|---|---|---|---|
| 1 | `tcp_bind(addr SocketAddr) -> Result[TcpListener, NetError]` | без изменений | — | — |
| 2 | `accept(listener TcpListener) -> Result[TcpStream, NetError]` | без изменений | — | — |
| 3 | `connect(addr SocketAddr) -> Result[TcpStream, NetError]` | без изменений | — | — |
| 4 | `read(stream TcpStream, buf mut []u8) -> Result[int, NetError]` | без изменений | — (buffer-fill — принятая идиома, та же, что `Io.read_in`) | — |
| 5 | `write(stream TcpStream, data []u8) -> Result[int, NetError]` | без изменений | — | — |
| 6 | `shutdown(stream TcpStream) -> Result[(), NetError]` | без изменений | — | — |
| 7 | `close_stream(stream TcpStream) -> ()` | без изменений (добавить именованный комментарий у декларации: закрытие в libuv fire-and-forget, ошибку в принципе некуда возвращать) | формально п.1 не задет (не код ошибки, а истинный `void`), но D456 требует комментарий у КАЖДОГО названного исключения — сейчас его нет прямо на этой строке | Никаких — только читаемость декларации. |
| 8 | `close_listener(listener TcpListener) -> ()` | без изменений (тот же комментарий, что №7) | как №7 | — |
| 9 | `mark_split(stream TcpStream) -> ()` | без изменений | — (внутренняя бухгалтерия, не публичная операция ввода-вывода) | — |
| 10 | `listener_local_port(listener TcpListener) -> int` | без изменений | — (`int` — данные, порт, не хендл) | — |
| 11 | `listener_local_addr(listener TcpListener, out mut []u8) -> ()` | `listener_local_addr(listener TcpListener) -> SocketAddr` † | п.4 (out-параметр — ИМЕННО пример из D456 по форме, хоть D456 текстом называет `stream_peer_addr`) | Сегодня мок обязан вручную звать `net_addr_loopback_into(port, out.ptr())` — писать в чужой буфер через FFI. Завтра — строит `SocketAddr` любым публичным конструктором (`SocketAddr.loopback(port)`) и возвращает значением. |
| 12 | `stream_local_port(stream TcpStream) -> int` | без изменений | — | — |
| 13 | `stream_peer_port(stream TcpStream) -> int` | без изменений | — | — |
| 14 | `stream_local_addr(stream TcpStream, out mut []u8) -> ()` | `stream_local_addr(stream TcpStream) -> SocketAddr` † | п.4 | Как №11. |
| 15 | `stream_peer_addr(stream TcpStream, out mut []u8) -> ()` | `stream_peer_addr(stream TcpStream) -> SocketAddr` † | п.4 — буквально пример, названный в D456 текстом | Как №11. |
| 16 | `set_nodelay(stream TcpStream, on bool) -> ()` | без изменений | — (одиночный именованный bool-переключатель ОДНОЙ настройки — не «позиционная простыня» п.8, тот же приём, что Rust `set_nodelay`) | — |
| 17 | `set_keepalive(stream TcpStream, on bool) -> ()` | без изменений | — | — |
| 18 | `set_reuse_address(listener TcpListener, on bool) -> ()` | без изменений | — | — |
| 19 | `udp_bind(addr SocketAddr) -> Result[UdpSocket, NetError]` | без изменений | — | — |
| 20 | `send_to(sock UdpSocket, data []u8, addr SocketAddr) -> Result[int, NetError]` | без изменений | — | — |
| 21 | `recv_from(sock UdpSocket, buf mut []u8, sender mut []u8) -> Result[int, NetError]` | `recv_from(sock UdpSocket, buf mut []u8) -> Result[(int, SocketAddr), NetError]` † | п.4 (`sender mut []u8` — второй out-параметр рядом с уже законным buffer-fill; `buf` остаётся buffer-fill — это не нарушение, `sender` — нарушение) | Сегодня мок пишет адрес отправителя В ЧУЖОЙ БУФЕР вторым параметром, отдельно от основного возврата. Завтра — один `Result`, внутри которого и байты, и адрес. |
| 22 | `close_socket(sock UdpSocket) -> ()` | без изменений (комментарий, как №7) | как №7 | — |
| 23 | `socket_local_port(sock UdpSocket) -> int` | без изменений | — | — |
| 24 | `socket_local_addr(sock UdpSocket, out mut []u8) -> ()` | `socket_local_addr(sock UdpSocket) -> SocketAddr` † | п.4 | Как №11. |
| 25 | `lookup(host []u8, port int) -> Result[(int, int), NetError]` | без изменений по механике (это ДОКАЗАННО рабочий приём — единственный во всей кодовой базе способ вернуть коллекцию через vtable); добавить у декларации явную ссылку на D456 как на именованное исключение (обоснование уже есть текстом в комментарии, но не оформлено как ссылка на конкретный пункт) | формально п.5/п.6 (адрес массива как `int`, счётчик рядом) — но это ЕДИНСТВЕННЫЙ в кодовой базе случай, где сама возможность иной формы не доказана (см. Q1) | Никаких — мок уже сегодня отдаёт `(base, count)`-пару, `dns.nv` уже строит `[]SocketAddr` снаружи границы. Меняется только текст комментария у декларации. |

**Итог по `Net`:** было 25 операций, предлагается 25 (без слияний/удалений)
— пять сигнатур меняются (11, 14, 15, 21, 24 — все про адрес через
out-параметр), одна получает более явную документацию (25), две получают
пояснительный комментарий без смены сигнатуры (7/8, аналогично 22).

Если Q1 даёт ОТРИЦАТЕЛЬНЫЙ ответ конкретно для `SocketAddr` (маловероятно —
`SocketAddr` ближе по форме к `TcpStream`/`NetError`, уже доказанно
проходящим Ok-payload'ом, чем к «Vec из record'ов», которого касается
негативный прецедент `lookup`) — запасной вариант тот же, что у D456 уже
принят для `lookup`: `(int, int)`-пара база+длина для ОДНОГО адреса
избыточна (адрес — фиксированные 20 байт, не список), так что при провале
Q1 для этих пяти строк придётся вернуться к форме out-параметра, но
переименовать её честно и задокументировать как названное исключение —
не как «эффект стирает богатый возврат» (эта фраза уже опровергнута самим
`Net`).

---

*(Продолжение — раздел вопросов и раздел цены — в следующих коммитах той же
волны.)*
