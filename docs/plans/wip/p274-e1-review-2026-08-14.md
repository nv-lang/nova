<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Ревью Э1 novac — 2026-08-14 (интегратор; владелец: «скорректирую всё, что требуется»)

Разбор дерева `nova-p274` на коммите `41c2787d0`. Вердикт: работа сильная —
lossless-инвариант несётся честно, отказы образцовые (интерполяция как
K7-отказ, самоистекающий маркер №659), «одна дверь» соблюдается буквально,
догфудинг дал №658/№659 с первой сборки. Ниже — что скорректировать, по
убыванию веса. Пункт вычёркивается ВМЕСТЕ с механизмом (фикстура/юнит), где он
назван. Файл удаляется при закрытии волны Э1 (конвенция wip/).

## 1. Каскад на верхнем уровне парсера — против собственного правила

`parse.nv:47`: каждый посторонний токен верхнего уровня — отдельный Err-ветвь;
файл `1 2 3` даст ТРИ диагностика на одну причину. В `fn_decl` (строки 71-82)
тот же случай уже собран в один panic-бакет. Нужен такой же бакет в цикле
`parse()` + негативная фикстура «мусор на верхнем уровне» под
`check-novac-no-cascade.sh`.

## 2. `Cursor.take` не сдвигается на Eof — дверь для будущего зависания

`parse.nv:28-32`: на Eof возвращает лист, не двигая курсор. Сегодня все вызовы
ограждены `at_eof`; первый неогороженный цикл повиснет, а повторный take Eof
задвоит хвостовую тривию в ресериализации. Сдвиг с клампом либо ассерт на
повторный take.

## 3. `tree.@kind_of`: Leaf → `NodeKind.Err` — лист не ошибка

`tree.nv:61-66`: лист с добротным токеном классифицируется как Err; будущий
обход по kind'ам молча посчитает листья ошибками. Честный ответ: отдельный
kind для листа либо Option[NodeKind].

## 4. Незакрытая строка остаётся `StrLit` — правдоподобно-неверное принятие

`lex.nv:128-137`: скан дошёл до EOF без закрывающей кавычки — kind всё равно
StrLit. По духу вашего же K7-отказа это ErrorTok. Рядом: экранированное `\${`
тоже взводит `has_interp` (`lex.nv:131-132` — после пропуска backslash
проверяется УЖЕ экранированный байт). Пере-отказ — безопасная сторона, но
контракт неточен; фикстуры на оба случая.

## 5. `panic` в diag против инварианта 11

`diag.nv:63`: `?? panic(...)` — дверь краха в компиляторе, чьё правило «ноль
паник, даже редких». Случай практически недостижим (плоский DTO), но паттерн
нормализует панику, и no-panic-страж это место не поймает (не зависит от
входа).

**Как у эталонов (вопрос владельца 2026-08-14):** rustc — прямой `panic!`
запрещён внутренним линтом; двери `bug!`/`span_bug!` дают ICE-баннер с
привязкой к месту, а `span_delayed_bug` откладывает ICE и гасит его, если
компиляция уже выдала настоящую ошибку (убивает класс «ICE на сломанном
вводе»). Roslyn — внутренняя ошибка наружу выходит ДИАГНОСТИКОЙ «internal
compiler error», процесс цел; в IDE — спроектированные fault-tolerance слои
(урок для Э3: не наследовать catch_unwind-заплатку). Go — `base.Fatalf`,
всегда именованная дверь с позицией и стеком. LLVM/Clang — антиобразец для
цели «стабильность»: release без ассертов превращает `llvm_unreachable` в UB
(мискомпиляция вместо краха); забрать у Clang стоит другое — crash-репродьюсер
(родня §12.3 автоминимизации).

**Форма для novac:** (1) голый `panic(` в `novac/src` — под страж (греппится,
зеркало rustc-линта); (2) одна дверь `ice(...)`/`ice_at(span,...)` в diag —
ICE тоже диагностика схемы §7 (код `E_NOVAC_ICE`, machine-first, «это баг
компилятора»), ненулевой выход; `diag.nv:63` становится `?? ice(...)`;
(3) `delayed_ice` по образцу rustc — с Э2, когда чекер держит инварианты
поверх Err-узлов; (4) ассерты/контракты как есть — В NOVA ОНИ РАБОТАЮТ И В
РЕЛИЗЕ (поправка владельца 2026-08-14: отдельного debug-яруса нет, и это
позиция языка — анти-LLVM пункт встроен в сам язык, а не в профиль сборки);
попутно поправить комментарий `source.nv:30` — «checked by the debug
assert» неточен, assert живёт и в релизе.

**Уточнение (вопрос владельца: «контракты и assert всё равно паникуют в
stdlib»):** верно, и это не ломает доктрину — у rustc паника тоже законный
МЕХАНИЗМ (bounds в коде компилятора — обычный panic), незаконны голая строка
`panic!` в исходнике (линт) и сырой выход паники наружу (граница драйвера
рендерит всё ICE-баннером). Для novac два яруса (поправка владельца: в Nova
assert/контракты/границы работают И В РЕЛИЗЕ — «debug-яруса» не существует,
и это позиция языка, анти-LLVM пункт встроен в сам язык): (а) `ice()` —
достижимые инварианты, рендер в §7-JSON изнутри; (б) всегда-включённая
языковая защита — std-`assert`/контракты/границы; abort-класс, В ЯЗЫКЕ НЕ
ЛОВИТСЯ (не `fail`), значит её дисциплина — доказательство недостижимости
ОТ ВХОДА: no-panic-страж (код ≥128 = крах) + фаззинг мутациями; ассерт,
стреляющий от пользовательского входа, — баг novac по определению. Если
язык когда-то даст ловимую обёртку вокруг тела компиляции — граница main
сможет рендерить и ярус (б); до тех пор честно жить без этого.

## 6. Точка спеки — комментарий, а §1.1 обещает машинное число

`novac/nova.toml`: `spec-point: 2026-08-14` живёт в комментарии. До первого
принятого D-блока бесплатно; после — станет прозой. Настоящий ключ + счётчик
очереди (пусть пока читает только будущий страж).

## 7. Юнит-тесты неравномерны

Покрыты lex/parse/source; без своих `_test.nv` — `check`, `diag`, `tree`,
`pipeline`. Просятся: юнит «один Err-бакет — один диагностик» (свойство №1
этого списка) и голден JSON diag против схемы §7 архитектуры.

## 8. Остаток Э1 — виден, не забыть в приёмке

CLI пока check-only; приёмка Э1 требует `hello`/`arithmetic` ДО БИНАРЯ с
дифф-гейтом поведения — эмиттер C впереди. `novac/divergences.allow` не
существует (страж терпит, но пустой файл с шапкой-правилом задокументировал бы
контракт записи).

## Мелочь

`lex.nv:62-66` `keyword_kind` if-цепочкой — на двух словах законно; §4а
(«идентичность — не имя») попросит табличную дверь при росте. Пометка «до Э2»
в коде — и совесть чиста.
