# PROGRESS — окно p-build304: №283 (гейт может врать зелёным) + №304 (build/test разный код)

Worktree: `d:/Sources/nv-lang/nova-build304`, ветка `pbuild304`, база — main
на момент старта (`b2bccc77f`). Модель: Claude Sonnet 5. Периметр: `nova-cli/`
и раннер тестов; `compiler-codegen/src/types/mod.rs` НЕ трогается (параллельное
окно).

---

## ЧАСТЬ 1 — №283: ВРЁТ ЛИ ГЕЙТ ЗЕЛЁНЫМ

### Вердикт: ДА, ПОДТВЕРЖДЕНО. Не флейк — детерминированный структурный дефект.

Гейт (`scripts/gate.sh`, шаг «flagship aggregator --strict-effects», команда
дословно из скрипта: `nova build examples/flagship/aggregator/src/main.nv
--strict-effects`) даёт **разный, детерминированный** результат в зависимости
от НЕОТСЛЕЖИВАЕМОГО состояния файловой системы главного репо, которое не
переносится ни в один worktree и ни в один свежий clone.

### Механизм (найден прямым чтением + эмпирическим A/B, не гипотеза)

`examples/nova.toml` объявляет `tls` как git-зависимость с версионным
диапазоном:
```
[dependencies]
tls = { git = "https://github.com/nv-lang/nova-tls", version = "0.1" }
```
Зафиксированная версия лежит в **закоммиченном** `examples/nova.lock.toml`:
```
[[package]]
name = "tls"
pin = "version:>=0.1.0, <0.2.0"
version = "0.1.3"
commit = "910e14be86c3690f4b5ddd1d30d365437336f910"
```
Этот commit (тег `v0.1.3` nova-tls) **предшествует** переименованию метода
`read_to_vec` → `read_bytes` (nova-tls commit `3003b64...`, решение владельца
2026-08-01, тег `v0.1.5` — подтверждено `git ls-remote --tags origin` на
`d:/Sources/nv-lang/nova-tls`: `v0.1.3^{} = 910e14be...`, `v0.1.5^{} =
3003b64...`). То есть **на удалённом репозитории УЖЕ есть подходящий по
диапазону `>=0.1.0,<0.2.0` более новый тег (`v0.1.5`) с новым API**, но
`nova.lock.toml` его не подхватил (никто не гонял `nova update tls` после
переименования).

Тем временем `examples/flagship/aggregator/src/app/live.nv:95` и
`nova-polaris/src/net/servernet_tls.nv:114,126` **уже вызывают новое имя**
`stream.read_bytes(...)` — то есть закоммиченные исходники и закоммиченный
lock рассинхронизированы: честная сборка по локу обязана падать.

Главный репозиторий владельца (`d:/Sources/nv-lang/nova`, НЕ worktree) несёt
файл `examples/nova.override.toml` — **gitignored** (`.gitignore:52-53`),
никогда не коммитится, существует только как персистентный локальный файл на
диске главного чекаута:
```
[replace]
tls = { path = "../../nova-tls" }
http = { path = "../../nova-http" }
polaris = { path = "../../nova-polaris" }
compress = { path = "../../nova-compress" }
```
`[replace]` (D420, `imports.rs::lookup_dependency`) **обходит `nova.lock.toml`
целиком** и резолвит `tls` напрямую в живую рабочую копию
`d:/Sources/nv-lang/nova-tls` — которая у владельца всегда актуальна (сейчас
на `9fed100`, далеко за переименованием). Из-за этого сборка флагмана **с
главного репо всегда видит `read_bytes`**, даже когда закоммиченный лок его
не содержит.

Любое дерево БЕЗ этого persistent-локального файла — свежий `git worktree
add`, свежий `git clone`, CI-раннер — резолвит `tls` строго по
`nova.lock.toml`, получает commit `910e14be` (ДО переименования) и падает.

### Эмпирическое A/B (дословный вывод обеих сторон)

**Сторона A — та же команда, ЧИСТОЕ дерево (`nova-build304`, без
`nova.override.toml`, `cargo build --release` собран локально, `target/`
до этого не существовал):**
```
error: D:\Sources\nv-lang\nova-build304\examples\flagship\aggregator\src\app\live.nv:95:48:
  error: [E7320] no field or method `read_bytes` on type `TlsStream`
95 |                                 Ok(_) => match stream.read_bytes(8192) {
  |                                                ^^^^^^^^^^^^^^^^^
  note: `TlsStream` has fields: tcp, session, scratch
D:\Sources\nv-lang\nova-polaris\src\net\servernet_tls.nv:114:15: error: [E7320] ...
D:\Sources\nv-lang\nova-polaris\src\net\servernet_tls.nv:126:15: error: [E7320] ...
```
(exit ненулевой, нет строки `built:` — тот самый ассерт гейта `grep -q
"built:"` НЕ прошёл бы)

**Сторона B — БУКВАЛЬНО ТА ЖЕ команда, главный репозиторий
(`d:/Sources/nv-lang/nova`, `nova.override.toml` присутствует, свой
`nova.exe`, HEAD `9962d13e7`):**
```
[... warnings ...]
built: D:\Sources\nv-lang\nova\main.exe (102.44s)
```
Полный успех, гейт прошёл бы.

Обе стороны запущены той же версией собранного компилятора (различие
коммитов между worktree и main на момент прогона не затрагивает ни
`nova.toml`/`nova.lock.toml` дерева `examples/`, ни разбираемый код — разница
воспроизводится ИСКЛЮЧИТЕЛЬНО присутствием/отсутствием `nova.override.toml`).

### Что ОПРОВЕРГНУТО этим прогоном

- **НЕ `target/.nova-cache` (build_cache.rs, Plan 81 Ф.9).** Прочитан код:
  `compute_c_key` хэширует байты ВСЕХ `peer_files`, а `resolve_imports_inline`
  (`imports.rs` PASS 2, строка ~2027) регистрирует `PeerFile` для КАЖДОГО
  импортированного модуля независимо от того, из какого пакета (локального,
  git-зависимости или override) он физически резолвился — путь берётся уже
  ПОСЛЕ резолва зависимости, так что смена резолва (лок vs override) меняет
  сам набор путей/байт и корректно инвалидирует ключ. Прогон на главном репо
  не показал строки `build cache hit` — это была полная пересборка (102s),
  несмотря на 372 существующих записи в `target/.nova-cache`. Кэш сгенерированного
  `.c` тут ни при чём.
- **Гипотеза «lock-файл отличается» (S1a, 2026-08-02) — верна лишь частично.**
  Предыдущее окно проверяло только `git diff` (лок идентичен между деревьями,
  что верно — он закоммичен и одинаков), но не заметило, что расхождение
  создаёт НЕ сам лок, а СЕДЬМОЙ, невидимый git'у файл (`nova.override.toml`),
  который *обходит* этот идентичный лок только на главном репо.

### Условия, при которых зелёное — ложно (для перепроверки сегодняшних приёмок)

Гейт лжёт зелёным ТОЧНО когда одновременно выполняются:
1. Гейт запущен из дерева, где физически лежит `nova.override.toml` (или
   `nova.local.toml`, тот же механизм) с `[replace]` на пакет, чья текущая
   рабочая копия (sibling-репозиторий) РАСХОДИТСЯ с тем, что зафиксировано в
   `nova.lock.toml` этого дерева;
2. Дерево, на которое ссылается override, содержит фикс/фичу/переименование,
   ЕЩЁ НЕ отражённое обновлением `nova.lock.toml` (никто не прогнал `nova
   update <pkg>` после локального изменения в sibling-репо).

**Практическое следствие для сегодняшних пяти приёмок:** если хоть одна из
них трогала `nova-tls`/`nova-http`/`nova-polaris`/`nova-compress` (все четыре
перечислены в `examples/nova.override.toml`) и гейт гонялся с главного репо
(а не со свежего дерева) — «built:» мог быть правдой ТОЛЬКО для локальной
копии владельца, не для `nova.lock.toml`, который реально коммитится и
достаётся любому третьему потребителю пакета. Автоматически определить,
какие из пяти волн задели эти пакеты, не входило в периметр этого окна
(периметр — `nova-cli`/раннер); нужен точечный `git log` по нужным окнам.

**Способ воспроизведения (для перепроверки в любой момент):**
```sh
# в любом дереве без nova.override.toml (или временно переименовав его):
NOVA_GC_LIB_DIR=... NOVA_INCLUDE_DIR=... \
  ./nova-cli/target/release/nova.exe build \
  examples/flagship/aggregator/src/main.nv --strict-effects
# ожидаемо: E7320 no field or method `read_bytes` on type `TlsStream`
# (3 сайта: aggregator/live.nv + 2× nova-polaris/servernet_tls.nv)
```
Не флейк: причина — присутствие/отсутствие конкретного файла на диске,
воспроизводится 100% раз за разом, пока состояние файла не меняется.

### Что НЕ было сделано на этом шаге

`examples/nova.lock.toml` в момент установления факта НЕ обновлялся (откат
случайного touch подтверждён — `git diff` пуст после `git checkout --`).
`nova.override.toml` главного репо не тронут (это персистентный локальный
файл владельца, не входит в git вообще) — часть 1б его тоже не трогает.

**Владелец принял находку** (сообщение 2026-08-04): override строил флагман
против ЖИВЫХ соседних репозиториев, то есть сегодняшние пять приёмок не
аннулируются целиком — но репозиторий в текущем виде не собирается ни у
кого, кроме владельца (ни CI, ни клон). Дано указание чинить (часть 1б) и
закрыть класс (часть 1в).

---

## ЧАСТЬ 1б — ПОЧИНЕНО: `examples/nova.lock.toml` обновлён

Команда (внутри `examples/`, cwd = корень пакета `nova_examples`):
```
nova update
→ updated: git-пины пере-резолвлены (5 зависимост(и) в графе)
```
Диф (коммит `f69db7335`):
```
compress: 47b0a642... -> 75919d9c...
http:     5257f302... -> 1af06c18...
tls:      0.1.3 (910e14be..., ДО rename) -> 0.1.5 (3003b64..., ПОСЛЕ rename read_to_vec->read_bytes)
```
`polaris`/`http` как ПРЯМЫЕ path-зависимости `examples/nova.toml` пина не
имеют (обновлять нечего) — `compress`/`http` в диффе выше это их
ТРАНЗИТИВНЫЕ git-пины (через `http`/`polaris`'s собственные `[dependencies]`),
переразрешённые той же командой.

### Критерий готовности (дословно, ДВАЖДЫ)

Прогон 1 — полностью с нуля (`rm -rf target/.nova-cache` перед стартом,
честная перекомпиляция без переиспользования сгенерированного C):
```
built: D:\Sources\nv-lang\nova-build304\main.exe (31.81s)
```
Прогон 2 — тот же файл сразу следом (build-cache честно срабатывает на
байт-идентичном входе):
```
note: build cache hit — reusing generated C
built: D:\Sources\nv-lang\nova-build304\main.exe (10.72s)
```
exit=0 в обоих случаях. **0 ошибок E7320.** Дерево — `nova-build304`, БЕЗ
`nova.override.toml`/`nova.local.toml` (подтверждено их отсутствием), с
изначально пустым `target/` (libuv/gc/rt-archive собраны заново этим же
прогоном — «nova: libuv not built, building...», «libnova_rt archive
built...» — честная холодная сборка окружения worktree, не заимствование
у main).

### Побочная подготовка (не баг, инфраструктурная)

Свежий `git worktree add` не тянет субмодули — `libuv` пришлось
`git submodule update --init compiler-codegen/nova_rt/libuv` (первая
попытка сборки падала на `FATAL libuv submodule not initialized`, уже
ПОСЛЕ успешного type-check — то есть №283 к этому шагу отношения не имеет,
чистая механика worktree/submodule).

### Другие несовместимости от обновления лока — НЕ НАЙДЕНО

Обновление трёх пинов (compress/http/tls) не вскрыло новых ошибок компиляции
ни в аггрегаторе, ни в транзитивных путях (`nova-polaris/src/net/
servernet_tls.nv`, единственный другой сайт `read_bytes`, тоже прошёл —
он резолвится через path-зависимость `polaris`, не через лок, но его
успех подтверждает отсутствие иных API-расхождений в текущих HEAD этих
пакетов). Единственные warning'и после фикса — pre-existing стиль
(`new-then-cap`, `W_PARAM_TYPE_POS_MUT`, `unused-import`) и служебные
(`W_DEP_PATH_NO_RELEASE` на голых path-зависимостях `http`/`polaris`,
`W_REPLACE_IN_DEPENDENCY` — оба класса существовали до этого окна, не
новые несовместимости).

### Побочная находка (НЕ фикс этим коммитом, вне периметра — другой репозиторий)

`nova-http/nova.lock` (собственный лок ПАКЕТА nova-http, отдельный
репозиторий) пинит `tls` на v0.1.0 (`510acc25...`) и `compress` на v0.1.0
(`47b0a642...`) — устарело СИЛЬНЕЕ, чем было у `examples`. Тот же класс
риска для СОБСТВЕННОГО гейта nova-http, если у него тоже где-то есть
override поверх этого лока. Не проверялось, не чинилось — отдельный
репозиторий, отдельная приёмка, вне периметра `nova-cli`/раннера этого окна.

---

## ЧАСТЬ 1в — ЗАКРЫТ КЛАСС: гейт больше не молчит про override

### Выбор: ПРЕДУПРЕЖДЕНИЕ, не отказ

Owner прямо указал: «override — рабочий инструмент владельца и ломать его
удобство нельзя; речь о видимости, а не о запрете» — это и есть решающий
довод. Дополнительные:
- Override — документированная (D420), санкционированная фича для
  локальной разработки без re-tag/push на каждой итерации.
- Гейт с главного репо гоняется постоянно (сегодня — пять волн); жёсткий
  отказ держал бы его вечно красным на обычном рабочем дереве владельца.
- Реалистичный исход жёсткого отказа — не «лок обновляют чаще», а «override
  на время гейта прячут руками» (`mv nova.override.toml nova.override.toml.bak`
  и обратно) — та же слепота, только без автоматики, и с шансом забыть
  вернуть файл на место.
- Дефект был не в СУЩЕСТВОВАНИИ override, а в его МОЛЧАНИИ: `built:` и
  `GATE OK` выглядели одинаково что с override, что без него. Значит и
  чинить нужно именно видимость.

### Реализация (`scripts/gate.sh`, коммит `d389f9a4d`)

- Скан всего дерева (`find "$ROOT" ... -prune` на `target/`, `.git/`,
  `vcpkg_installed/`, `node_modules/`, `.claude/` — чтобы не тонуть в
  вендоре и не залезать в чужие `.claude/worktrees/agent-*`) на
  `nova.override.toml`/`nova.local.toml`, ОДИН РАЗ в начале прогона.
- Если найдено — громкий баннер (рамка `!!!`), список файлов + их
  непустое/некомментарное содержимое (перекрытые пакеты видны дословно),
  печатается СРАЗУ (первым делом, до arch-ratchet) И ЕЩЁ РАЗ рядом с
  финальным вердиктом (`GATE OK (final) [DEV-OVERRIDE ACTIVE — ...]`
  вместо голого `GATE OK (final)`) — чтобы не потерялось при `tail`.
- exit code НЕ меняется ни в каком случае — чистое предупреждение.

### Проверка (функционально, изолированно — не полным прогоном: мега-CU вне периметра окна)

`bash -n scripts/gate.sh` → синтаксис ОК. Извлечённый идентичный фрагмент
логики прогнан отдельно в двух деревьях:
```
=== главный репо (override есть) ===
!!! GATE WARNING: dev-override (nova.override.toml/nova.local.toml) активен.
...
!!!   файл: /d/Sources/nv-lang/nova/examples/nova.override.toml
!!!     [replace]
!!!     tls = { path = "../../nova-tls" }
!!!     http = { path = "../../nova-http" }
!!!     polaris = { path = "../../nova-polaris" }
!!!     compress = { path = "../../nova-compress" }

=== worktree nova-build304 (override нет) ===
no override files found
```
Ровно ожидаемое поведение с обеих сторон.

---

## Гейты окна (сводно, дословно)

| Гейт | Результат |
|---|---|
| `cargo build --release` (nova-cli) | чисто, только pre-existing warnings |
| `nova check std/src` | `PASS: 147  FAIL: 26  WARN: 60` (канон 147/26/60 — брифом ожидался 148/26/61, разошлось; см. примечание ниже) |
| флагман, чистое дерево (`nova-build304`, без override, обновлённый лок) | `built: main.exe (31.81s)` холодный / `(10.72s)` cache-hit, exit=0 обоих |
| флагман, главный репо, обычный путь (override, СТАРЫЙ лок, ДО фикса части 1б) | `built: main.exe (102.44s)` — тот самый ложный зелёный из части 1 |
| `arch-ratchet` | `lines=64504 <= 64504`, `infer=348 <= 348` (в пределах лимита 64542/348 из брифа) |
| `nova lint` | не запускался — этим окном не тронут ни один `.nv`-файл |
| мега-CU | НЕ гонялся (интегратор при приёмке, по прямому слову брифа) |

Примечание по канону `check std/src`: бриф ожидал «148/26/61», наблюдаемое
— «147/26/60» — совпадает с тем, что зафиксировано в самом `scripts/gate.sh`
(строка ассерта `FAIL: 26`) и в последних двух окнах (`p279`, `ps1a2`,
`integrator-handoff.md`) как текущий канон. Расхождение с числом из брифа
похоже на опечатку/устаревшую цифру в самом брифе, не на регресс этого
окна — PASS/WARN гейт не ассертит (растут от новых файлов законно),
ассертится только `FAIL: 26`, что выполнено.

---

*(части 2/3 — №304 и записка о слиянии конвейеров — по приоритету ниже,
после частей 1/1б/1в; см. указание владельца 2026-08-04)*
