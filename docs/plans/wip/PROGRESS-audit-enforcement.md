# Аудит «правило объявлено — энфорс есть?»

Окно p-audit-enforcement. Метод: минимальный файл с заведомо нарушенным правилом,
`nova check` (не `build`) на нём. Ничего не чинится — только инвентаризация.
Пробы лежат в `probes/pNN_*/main.nv` этого worktree (каждая — свой каталог/модуль/
уникальное имя типа). Бинарь: `nova-cli/target/release/nova.exe` из main-репы
(в этом worktree своей сборки нет). Для `nova build` — `NOVA_RT_DIR`/
`NOVA_CG_INCLUDE`, указывающие на main-репу (иначе FATAL: libuv submodule not
initialized; сам submodule — 468 МБ, копировать не стал).

Статус: пункт 1 периметра (бáунды дженериков) закрыт целиком. Пункты 2–5 — не начаты.

## Таблица: пункт 1 — бáунды дженериков

| механизм | где объявлен | проба | вердикт check | вердикт build | класс |
|---|---|---|---|---|---|
| `AsSlice[T]` (метод-бáунд `@append[S AsSlice[T]]`) | protocols.nv:477; mutate.nv:283 | `probes/p1_asslice`: `[]u8.new().append(NoSlice{junk:7})` | **ok** (ложный) | падает на этапе C (см. №381, уже зафиксировано ранее) | расхождение check/build |
| `Compare` (метод-бáунд, форма `@method[K Bound]`) | protocols.nv:103(Iter)/пример своей формы ниже | — (см. корень «method-level bound», ниже прямая проба p13) | не проверялось напрямую по Compare-методам, но корень тот же | — | входит в групповой корень |
| `Clone` (метод-бáунд `@combine[S Clone]`, синтетическая проба точно по форме `@append`) | probes/p13_methodbound | `Box13.combine(NoClone13{...})`, `NoClone13` без `@clone` | **ok** (ложный) | **built** (ложный) — рантайм: `other.clone()` скомпилировался в `return other;` (identity, никакого клона, тихая подмена семантики) | и check, и build молчат — дыра, не просто расхождение |
| `Equal` (default-body метод `@equal(other Self) -> bool => @compare(other) == 0`, протокол верхнего уровня, НЕ метод-бáунд) | protocols.nv:83-85 | `probes/p8_equal`: `fn[T Equal] use_equal(a T, b T) -> bool => a.equal(b)` на `NoEqual8` без `@equal`/`@compare` | **ok** (ложный) | **built** (ложный) — рантайм печатает `false`, НО генерируемый C: `a.equal(b)` → `Nova_HashMap_method_equal(a, (void*)(b))` — резолвер молча взял ЧУЖОЙ одноимённый метод (`HashMap.equal`) на несвязанном типе, `void*`-каст скрывает type confusion. UB, не просто «не то значение» | check и build молчат — дыра; отдельный от method-bound корень (default-body протокола) |
| `Hash` | protocols.nv:60-62 | `probes/p7_hash`: `fn[T Hash] use_hash(x T) -> u64` на `NoHash7` | **FAIL** (корректно) | — | энфорсится |
| `Clone` (top-level `fn[T Clone] name(...)`, НЕ метод-бáунд) | protocols.nv:135-137 | `probes/p3_clone` | **FAIL** (корректно) | — | энфорсится |
| `Display` | protocols.nv:314 | `probes/p4_display`: `"${v}"` на типе без `#impl(Display)` | **FAIL** (корректно, `E_INTERP_NO_DISPLAY`) | — | энфорсится (свой механизм — opt-in impl gate, не protocol-bound) |
| `Debug` | protocols.nv:342 | `probes/p5_debug`: `"${v:?}"` | **FAIL** (корректно, `E_DEBUG_PRINTABLE_NOT_IMPLEMENTED`) | — | энфорсится |
| `Write` (top-level `fn[T Write] name(...)`) | protocols.nv:165-167 | `probes/p6_write` | **FAIL** (корректно) | — | энфорсится |
| `Next[T]` (top-level) | collections.nv:87-89 | `probes/p9_next` | **FAIL** (корректно) | — | энфорсится |
| `Iter[I]` (top-level) | collections.nv:103-105 | `probes/p10_iter` | **FAIL** (корректно) | — | энфорсится |
| `Index[K,V]` (top-level) | protocols.nv:435-437 | `probes/p11_index` | **FAIL** (корректно) | — | энфорсится |
| `MutIndex[K,V]` (top-level) | protocols.nv:454-456 | `probes/p12_mutindex` | **FAIL** (корректно) | — | энфорсится |

## Групповые корни (пункт 1)

### Корень А — «method-level generic-parameter бáунд не проверяется вообще»
Форма: `fn ReceiverType[...] @method[S SomeBound](args) -> ...` — бáунд `SomeBound`
висит на типовом параметре, объявленном НА САМОМ МЕТОДЕ (`[S ...]` после `@method`),
а не на верхнеуровневой `fn[T Bound] freeFunc(...)`. Для этой формы `nova check`
**вообще не проверяет** удовлетворение бáунда на call-site — доказано дважды
независимо (AsSlice/№381 и синтетический Clone/p13, разные протоколы, разный
codegen-фоллбэк). Для формы `fn[T Bound] freeFunc(...)` (бáунд на функции/на
receiver-типе, не на методе) — проверка работает штатно (Hash/Clone/Write/Next/
Iter/Index/MutIndex все корректно словили нарушение).

Известные call-site'ы этой формы в `std/` (все — потенциальные дыры для
пользовательского кода, вызывающего эти методы с неподходящим типом):

- `std/src/collections/vec/access.nv:220` — `@binary_search_by_key[K Compare]`
- `std/src/collections/vec/mutate.nv:262` — `@extend[S Iter[T]]`
- `std/src/collections/vec/mutate.nv:283` — `@append[S AsSlice[T]]` (=№381)
- `std/src/collections/vec/sort.nv:232` — `@sort_by_key[K Compare]`
- `std/src/collections/vec/sort.nv:242` — `@sort_unstable_by_key[K Compare]`
- `std/src/collections/vec/sort.nv:287` — `@dedup_by_key[K Equal]`
- `std/src/encoding/serde/serde.nv:186,202,206,210,214,218,285,319,340` — `@serialize[S Serializer]` (протокольный метод + ×8 impl'ов)

Итого минимум **9 сигнатур** в std одной формы, плюс любой пользовательский код,
объявляющий метод с собственным `[S Bound]`. Один фикс в checker'е (научить его
проверять method-own type-param бáунды на call-site, не только function-level)
закрывает весь класс разом — это ОДИН дефект, не N.

**Кодоgen-поведение на непройденной проверке — само по себе разное и опасное:**
- `Clone` (p13): `other.clone()` без метода → `return other;` (тихий identity,
  никакой ошибки компиляции C).
- `AsSlice` (№381, ранее задокументировано): падает на этапе C (недостающий
  символ/несовместимость типов) — то есть здесь хотя бы РАСХОДИТСЯ check/build,
  а не тихо «работает».

Значит сам этот корень A ветвится на подпримеры с разным поведением на C-этапе —
не гарантированно «упадёт на build», иногда тихо скомпилируется в семантически
неверный код. Это делает корень A более опасным, чем формулировка №381
(«молчит в check, ловит build») — в общем случае может НЕ ловить и build.

### Корень Б — «протокол с default-body методом не проверяет ЗАВИСИМОСТИ default-тела»
`Equal.@equal` имеет default-тело `=> @compare(other) == 0`. Любой тип
структурно «удовлетворяет» `Equal` просто по наличию默认 метода в протоколе —
компилятор не проверяет, что `@compare` (от которого зависит default-тело)
реально существует у типа-аргумента. На call-site (`a.equal(b)`) это не падает
ни на check, ни на build — вместо этого codegen резолвит `@compare`/`@equal` в
**произвольный одноимённый метод где-то ещё в программе** (`Nova_HashMap_method_equal`
в пробе), с `void*`-кастом, глушащим типовое несоответствие. Это type confusion /
UB, не просто «неверный ответ». Затронут ровно один протокол в текущем
`protocols.nv` (`Equal` — единственный с default-body методом среди
Hash/Equal/Compare/Clone/Write/Fmt/Display/Debug/Index/MutIndex/AsSlice/Next/Iter),
но сам механизм («default-body метод протокола не type-checks на предмет
зависимости от других методов бáунда на call-site, а неразрешённый вызов метода
падает в произвольный одноимённый символ, а не в ошибку компиляции») —
самостоятельный, потенциально более широкий по будущим протоколам с
default-body (сейчас таких мало, но паттерн предупреждает: default-body в
протоколе — источник этого класса дыр всегда).

## Ранжирование (пункт 1, промежуточное — до финала по всем пунктам)

1. **Корень А (method-level bound не проверяется)** — закрывает ≥9 известных
   сигнатур std + весь пользовательский код такой формы одним фиксом в
   checker'е. Наивысший приоритет: самый широкий охват, уже дважды подтверждён
   независимыми пробами.
2. **Корень Б (default-body протокола не проверяет свои зависимости + резолвер
   падает в произвольный одноимённый символ вместо ошибки)** — уже сейчас
   type confusion/UB на реальном коде, а не просто "неверный результат".
   Меньше охват (1 протокол сегодня), но опаснее по последствиям —
   второй по приоритету, возможно первый по severity (не по охвату).

## Смежная работа — не дублировать

`docs/plans/231.1-enforce-audit-table.md` (Plan 231-А, sonnet, 2026-07-26) —
УЖЕ существующий похожий аудит, но другим методом: grep кодов `E_*`/`W_*` из
`spec/decisions/*.md` против компилятора (0 совпадений = подозрение), БЕЗ
живых проб. Нашёл 29 «генуинно беззубых» P1-норм (`E_POINTER_CROSS_FIBER`,
`E_COERCE_AMBIGUOUS`, `E_FIELD_NOT_MUT` и т.д.) — почти все в other zонах
(pointer-safety/coercion/const-fn), пересечений с настоящим проходом (бáунды/
атрибуты) не найдено, кроме одного смежного пункта: `E_BOUND_MISSING`
(02-types.md:13800) — «Known limitation: checker does not validate field
Debug bounds at `#impl(Debug)` synthesis time — missing bound produces
CC-FAIL, not E_BOUND_MISSING» — это ДРУГОЙ механизм (field-bound внутри
`#impl(Debug)` auto-derive, самоописанный, класс «падает на C» не «молчит
везде»), не тот же корень, что найденный ниже Корень А (method-level
type-param бáунд на call site, вообще без диагностики нигде).

Этот проход (живые пробы, не grep) даёт то, чего 231-А методологически не
мог: подтверждённые, воспроизводимые примеры **того, ЧТО именно компилируется
вместо ошибки** (сгенерированный C, рантайм-поведение) — не просто «код не
найден», а «код скомпилировался в другой, семантически неверный вызов».

## Пункт 2 — атрибуты типов

| атрибут | где объявлен | проба | вердикт check | вердикт build | класс |
|---|---|---|---|---|---|
| `#no_copy` (второе имя, D447) | 02-types.md:17836 (D447) | `probes/p15_no_copy`: `ro h2 = h` для `#no_copy`-типа | **FAIL** (корректно, `E_NO_COPY_SECOND_NAME`) | — | энфорсится (волна 2, свежая, 2026-08-05) |
| `#pub_to(Type)` / `priv(type)` (D224 §4) | 02-types.md:11833 (D224) | существующая фикстура `nova_tests/plan124/neg/plan124_6_neg_pub_to_not_listed.nv` (не изобретал — прогнал как есть) | **FAIL** (корректно, `E_PRIV_FIELD_READ`) | — | энфорсится |
| `consume` на type-decl, D133 «empty consume» | 02-types.md:5495 (D133) | `probes/p17_consume` (вариант А: `type Tx17 consume {...}` без единого consume-метода) | **FAIL** (корректно, `D133-empty-consume` + попутно `E_CONSUME_KEYWORD_MISSING`) | — | энфорсится |
| `consume`, D133 not-consumed | 02-types.md:5552 | `probes/p17_consume` (вариант Б: `consume t = Tx17{...}` без вызова `commit`) | **FAIL** (корректно, `D133-not-consumed`) | — | энфорсится |
| `#impl(Equal)` — verification at decl-site (`E_IMPL_MISSING_METHODS`, спека 02-types.md:8303) | 02-types.md:8300-8380 | `probes/p14_impl_equal`: `#impl(Equal) type BadImplEqual14 {...}` БЕЗ `@compare` — спека прямо обещает `verify_impl_protocols` проверит, что default-body's `@compare`-зависимость реально резолвится для T | **ok** (ложный — спека прямо противоречит наблюдаемому поведению) | **built** (ложный), рантайм печатает `false` без ошибки | check и build молчат вопреки прямому тексту спеки — та же природа, что Корень Б ниже (см. группировку) |
| `#from_fields` на пользовательском типе | 02-types.md:1701 (D55) | `probes/p16_from_fields`: `#from_fields type BadMap16[V]{junk int}` без `with_capacity`/`insert_new`, литерал `{a:1,b:2}` | **ok** | не проверял (не форсил) | НЕ дыра этого класса — задокументированное bootstrap-ограничение: map-coercion honored ТОЛЬКО для canonical `std/collections/hashmap.HashMap` (`types/mod.rs:45120-45124`, явный комментарий «User-локальные типы с attribute получают warning… bootstrap не даёт им map-coercion для безопасности»); атрибут на пользовательском типе — намеренный no-op, не ложный accept |
| `#share` (D415 §2, explicit vouch) | 06-concurrency.md:7102 | не пробовал — спека прямым текстом называет это АУДИТИРУЕМЫМ unchecked-esc-hatch («компилятор верит без проверки, потому "audited"», аналог Rust `unsafe impl Sync`) | н/п | н/п | НЕ дыра — by-design unchecked assertion, не норма «обязана отвергать»; авто-вывод (непомеченный default case) — отдельный, давно зрелый механизм (D415, много амендментов до 2026-08-04), не пробовал отдельно (за пределами бюджета окна) |
| `#zero_on_move` — фактическое зануление памяти при move/consume | 02-types.md:13699-13747 (M-124.8-zero-on-move) | не пробовал заново — спека САМА помечает V2 (auto-injection на call site) как ⚠️ ОТЛОЖЕННЫЙ followup (`[M-124.8-zero-on-move-auto-inject]`); V1 landed — только парсер/AST/checker-валидация видов + C-helper эмитится, но НИКОГДА не вызывается автоматически | н/п (задокументированный gap) | н/п | НЕ новая находка — уже самоописанная отложенность (Type-2 в терминах 231-А); стоит перепроверить отдельным окном ЧТО именно сейчас происходит при `consume` над `#zero_on_move`-типом (память реально не зануляется — тихая деградация заявленной security-гарантии), но статус уже честно маркирован в спеке |
| `#serde(...)` типовые ключи | 16445 и далее (D382/D435) | не успел — не хватило бюджета окна | — | — | не дошёл |

## Пункт 3 — модификаторы объявления (частично)

| механизм | проба | вердикт check | класс |
|---|---|---|---|
| `type X consume` (D133) | см. пункт 2 таблицу (две формы) | **FAIL** оба раза | энфорсится |
| `priv(type)`/`#pub_to` | см. пункт 2 таблицу | **FAIL** | энфорсится |
| простой `priv` (D220, без friend-списка) | не пробовал отдельно — покрывается тем же механизмом, что и `#pub_to`-негатив (`E_PRIV_FIELD_READ`, тот же checker-путь `priv_field_access_allowed`); дублировать пробу не стал | — | по коду — тот же путь, что уже подтверждён |
| `priv(file)` | не пробовал | — | не дошёл |
| `value` — семантика копии (независимость копий при присваивании) | не пробовал — это RUNTIME-семантика (не syntax-level check), требует build+run, а не просто `nova check`; вне метода этого окна («check не build», но здесь и check не откроет проблему, только выполнение) | — | не дошёл, отдельный метод нужен |

## Пункты 4–5 (D246 §V2.2 указатели; эффекты) — НЕ НАЧАТЫ

Честно: бюджет окна исчерпан на пунктах 1–3. Пункты 4 (оставшиеся строки
таблицы D246 §V2.2 — часть уже закрыта №349/№353/№358/№375, что осталось не
проверено) и 5 (эффекты: чистая→эффектная, `#thread_affine` в файбере,
`Fail[E]` без объявления) — не тронуты вообще. Отдельно к сведению: Plan
231.1 (см. «смежная работа» выше) УЖЕ нашёл в pointer-safety зоне 7 генуинно
беззубых P1-норм (`E_POINTER_CROSS_FIBER`, `E_POINTER_RO_MUT_METHOD`,
`E_PTR_ARITHMETIC_INVALID`, `E_PTR_NO_MEMBER`, `E_LIT_PTR_NO_COERCE`,
`E_ADDR_OF_MUT_REQUIRES_MUT_BINDING`, `E_CAST_RAW_FN_TO_CLOSURE`) —
методом grep, не живой пробой; следующее окно на пункт 4 должно СНАЧАЛА
свериться с этим списком (не задваивать), затем закрыть остаток таблицы
D246 §V2.2 живыми пробами (этот проход показал, что grep-метод и живая
проба находят частично разные вещи — Корень А/Б ниже НЕ фигурируют в
231.1, потому что там не было живых кодогена-проб).

---

## Итоговое ранжирование (по критерию: сколько форм закрывает один фикс)

1. **Корень А — method-level generic-parameter бáунд не проверяется НИГДЕ**
   (ни на check, ни надёжно на build). Форма: `fn Recv[...] @method[S Bound]
   (...)`. Подтверждено дважды независимо (AsSlice/№381 реальный
   production-код; синтетический Clone/probes/p13 — идентичный root cause,
   другой codegen-фоллбэк). Минимум **9 сигнатур** в `std/` уже сегодня
   (`@append`, `@extend`, `@binary_search_by_key`, `@sort_by_key`,
   `@sort_unstable_by_key`, `@dedup_by_key`, `@serialize`×~4), плюс весь
   пользовательский код такой формы. Один фикс в checker'е (научить его
   проверять method-own type-param бáунды на call-site — тот же путь, что уже
   работает для `fn[T Bound] freeFunc(...)`) закрывает всё сразу. **Наивысший
   приоритет по охвату.**
2. **Корень Б — протокол с default-body методом не проверяет свои
   внутренние зависимости; неразрешённый вызов резолвится в ПРОИЗВОЛЬНЫЙ
   одноимённый символ где-то в программе вместо ошибки компиляции.**
   Подтверждено на `Equal` (единственный protocol в `protocols.nv` с
   default-body) в ДВУХ независимых точках входа: (а) generic bound call-site
   (`fn[T Equal] use_equal`) и (б) декларативная `#impl(Equal)`
   verification, которая по спеке ОБЯЗАНА поймать это на этапе декларации
   (`E_IMPL_MISSING_METHODS`) — не ловит. Конкретное доказательство:
   `a.equal(b)` на типе без `@equal`/`@compare` компилируется в
   `Nova_HashMap_method_equal(a, (void*)(b))` — type confusion через
   `void*`-каст, UB, не просто «неверный ответ». Меньше охват сегодня (один
   протокол), но **опаснее по последствиям** — тихий memory-safety риск, не
   просто plausible-looking неверная семантика. **Второй по приоритету,
   возможно первый по severity.**
3. **`#from_fields`/`#share`/`#zero_on_move`** — НЕ находки этого класса
   (уже самоописаны как ограничения/отложенности в спеке или сознательный
   unchecked-escape-hatch), но `#zero_on_move` V2 (реальное зануление
   памяти) стоит перепроверить отдельным окном — заявленная security-гарантия
   сейчас, похоже, не выполняется рантаймом ни разу (только helper эмитится,
   auto-injection отложена), хотя это уже честно помечено ⚠️ в спеке, не
   новая дыра.
4. **Не начато (4–5) — оставшиеся строки D246 §V2.2 и эффекты** — следующее
   окно; сверить со списком Plan 231.1 сначала (pointer-safety P1×7 уже
   найдены их методом, не задваивать).
