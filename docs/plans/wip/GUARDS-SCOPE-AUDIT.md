<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Аудит соответствия шапки/тела стражей (реестр 221.1 №490)

Повод: `check-doc-hygiene.sh` заявлял в шапке «кириллица в user-facing строках
compiler-codegen/src/», а считал каждую строку файла с кириллицей (в основном
комментарии) — гейт ронялся на легальных русских комментариях. Задача: для
КАЖДОГО стража в `scripts/guards/*.sh` сверить заявленное в шапке с тем, что
реально считает выражение. Метод: чтение шапки → чтение grep/awk/цикла →
вердикт `совпадает`/`шире`/`уже`/`не установлено`. Правки НЕ вносились —
только инвентарь для интегратора. Прочитаны все 25 файлов `scripts/guards/*.sh`.

| Страж | Что заявлено в шапке | Что реально считает | Вердикт | Замечание |
|---|---|---|---|---|
| check-doc-hygiene.sh | 3 счётчика: кириллица в `///`/`//!` .nv (std/src+examples+пакетные репы), внутренние ссылки там же, кириллица в USER-FACING строках compiler-codegen/lints.rs | Ровно то и заявлено — уже ИСПРАВЛЕНО тем же днём 2026-08-09 (сам №490): `cyrillic_lint` теперь `grep -aoE '"[^"]*[Кир][^"]*"'` внутри строковых литералов, а не строка файла | совпадает | Носитель дефекта №490 сам себя починил в этом же дне («УТОЧНЕНО 2026-08-09» в теле файла); первые два счётчика (cyrillic_doc/internal_doc) с самого начала считали ровно по doc-comment строкам — они не были частью дефекта |
| arch-ratchet.sh | 2 метрики emit_c.rs: `lines`=`wc -l` всего файла, `infer`=число ВЫЗОВОВ `infer_expr_c_type` (не любых вхождений — комментарии отфильтрованы) | `m_lines` — `wc -l` всего файла (как заявлено); `m_infer` — grep по строкам минус строки после trim, начинающиеся с `//`/`*` | совпадает | Уже несёт встроенный анти-паттерн-фикс от 2026-08-07 (№401b) — превентивно закрыл ровно класс №490 |
| check-bug-number-sync.sh | Каждый НОВЫЙ `[M-...]`-маркер в backlog-followups.md обязан иметь № в 221.1-bug-sweep.md | Регэксп извлечения `[M-[a-z0-9_.-]+]` — ТОЛЬКО lowercase-класс. Замер на живом дереве: в `docs/plans/backlog-followups.md` 19 вхождений маркеров с ЗАГЛАВНЫМИ буквами в идентификаторе (напр. `[M-153.2-Z-closure-devirt]`) — regex их НЕ матчит вовсе, они выпадают из сверки МОЛЧА | уже | Проверено грепом на реальных данных (не гипотеза): `grep -oE '\[M-[a-zA-Z0-9._-]+\]' docs/plans/backlog-followups.md \| sed 's/^\[M-//' \| grep -cE '[A-Z]'` → 19. Такой маркер без номера пройдёт мимо стража, хотя обязан быть пойман — гейт «нулевой толерантности» (правило владельца №217) имеет дыру по регистру |
| check-ci-status.sh | Внешний гейт GitHub Actions: OK/RED/STALE/SKIP по прогонам на хеше origin/main; не блокирует без `--strict` | Реализация повторяет описанные исходы, пороги, SKIP-причины 1:1 | совпадает | Подробная шапка, код ей соответствует построчно |
| check-commit-hygiene.sh | 4 проверки коммита: конфликт-маркеры В ДОБАВЛЯЕМЫХ строках (`git diff --cached`), нет Co-Authored-By, email автора, name автора | `git diff --cached --unified=0 \| grep '^\+...'` — именно added-строки, как заявлено; остальные 3 проверки — прямое чтение git config/msg-файла | совпадает | Явно документирует свой предел («не ловит смысл сообщения») |
| check-doc-conventions.sh | Шапка: «ШЕСТЬ независимых проверок» (1 spec_en_header, 2 guide_pairing, 2b same-commit «...— КРАСНЫЙ», 3 plan_status, 4 dev_links, 5 code_block_identity) | Реально ≥11 отдельных проверок (в коде есть необъявленные в оглавлении 5b manifest_genre, 6 readme_pair, 7 mixed_language, 7b code_comment_ru, 8 translation_drift + не упомянутый в тексте п.4 `plans_links`-ratchet); главное — п.2b: реализация вызывает `info()`, НЕ `red()` — same-commit проверка НИКОГДА не роняет гейт | уже | Внутреннее противоречие в одном файле: шапка (строка ~33) обещает «красный» на 2b, а комментарий РЯДОМ С КОДОМ (строка 193) прямо говорит «гейт не роняет» — наблюдательная проверка выдана в оглавлении за блокирующую |
| check-doc-examples.sh | ```nova-блоки в docs/guide/spec/README (верхний уровень, БЕЗ рекурсии); ловит перечисленный список снятых форм; исключение блок-гранулярное по маркеру RETIRED/E_*; open-questions.md вне периметра | Код 1:1 реализует все regex-классы, блок-гранулярное исключение через awk `exempt`, явное исключение `spec/open-questions.md`, нерекурсивный glob `"$d"/$g` | совпадает | Самая подробная и самопроверяющая себя шапка — история находок (typed-pointers.md, open-questions.md) зафиксирована как обоснование границ |
| check-doc-truth.sh | ОСЬ1 (имена EXPECT_*) сканирует AGENTS.md+docs/dev/**+docs/guide/**; ОСЬ2 (исполнимость команд `nova ...`) сканирует ТОЛЬКО AGENTS.md+docs/dev/** (docs/guide/ явно исключена — nova-cli.md это usage-синтаксис, не примеры вызова) | scan_paths / cmd_scan_paths в коде — ровно этот асимметричный периметр | совпадает | Асимметрия периметра между осями явно объяснена и в шапке, и в коде — не расхождение, а задокументированная граница |
| check-expect-markers.sh | Неизвестный `EXPECT_*` в тестовом корпусе — красный гейт | `grep -rhoE` по `spec_tests std examples`, сверка с белым списком `KNOWN` | совпадает | Список директорий не назван явно в шапке текстом, но соответствует описанному «раннер молчит по построению» намерению |
| check-guard-wiring.sh | МЕТА-СТРАЖ: «для каждого файла `scripts/guards/check-*.sh`» — документирован/подключён/покрыт селфтестом; `scripts/README.md` называет его гарантом ТРЁХ свойств «нового guard» в `scripts/guards/` вообще | Глоб `scripts/guards/check-*.sh` — по факту проверяет ТОЛЬКО файлы с префиксом `check-`. Замерено на живом дереве: `hardcode-audit.sh`, `lint-no-silent-int-fallback.sh`, `strict_effects_smoke.sh`, `tsan_concurrency.sh`, `install-guards.sh` живут в `scripts/guards/` (по `scripts/README.md` это ровно директория «постоянных механизмов принуждения»), НЕ подключены к `scripts/gate.sh` (`grep -c <имя> scripts/gate.sh` = 0 для всех пяти) и НЕ имеют селфтеста в `scripts/guards/selftest/` — а мета-страж, чья работа «поймать неподключённого/непроверенного стража», их даже не смотрит из-за naming-фильтра | уже | Сами четыре файла (hardcode-audit/lint-no-silent-int-fallback/strict_effects_smoke/tsan_concurrency) ЧЕСТНО признаются в собственных шапках «НЕ вызывается из gate.sh, запускать вручную» — то есть авторы каждого файла в курсе, но НИ ОДИН централизованный механизм это не отслеживает и не превращает в видимый долг (ratchet/baseline), как того требует правило владельца 2026-07-27, процитированное в самой шапке check-guard-wiring.sh |
| check-invariant-discipline.sh | Ищет «инвариант на честном слове» по 7 группам сигнатурных фраз (RU+EN) в ДОБАВЛЯЕМЫХ строках .rs/.c/.h/.nv/.sh/.py, исключая docs/spec/scripts/libuv/minicoro.h; плюс отдельно проверяет, что каждый `[INV-GUARD: X]` указывает на существующего стража с селфтестом | Один проход `awk` по `git diff` реализует ровно эти группы sig..sig7, ровно эти исключения по пути/расширению; второй блок — `grep`-сверка `[INV-GUARD: ...]` с `scripts/guards/${g}` + `selftest/test-${base}.sh` | совпадает | Самый «зрелый» страж набора — учтены уроки трёх предыдущих самопоимок (перечислены в теле) |
| check-marker-registry-sync.sh | Каждый `[M-...]`-маркер из `.nv`-исходников обязан встречаться в одном из реестров/планов | `grep` по `std/src examples spec_tests --include=*.nv`, сверка через `grep -F -f` с объединением реестров+`docs/plans/*.md` | совпадает | Явный храповик, честно документирует однонаправленность (обратное — код без маркера в реестре — не проверяется, это норма) |
| check-merge-discipline.sh | Слияние в `main` законно только если вердикт гейта (`/tmp/gate_full.done`) существует, `RC=0`, и не старше HEAD | Ровно эти три условия, в этом порядке, с `NOVA_MERGE_ALLOW_RED=1` обходом | совпадает | — |
| check-no-accumulation.sh | Храповик «замёрзших» веток: не влита в main И коммиттер-дата старше порога (14 дн. по умолчанию) | `git for-each-ref --no-merged main` + фильтр по `committerdate:unix` | совпадает | Честно документирует свой предел (не отличает живую редкую ветку от брошенной) |
| check-no-manual-status-table.sh | ≥3 строк вида `\| число \| ссылка.md \| ... эмодзи-статус` в `docs/plans/README.md` = ручная статус-таблица, запрещена | Ровно этот regex и порог `THRESHOLD=3` | совпадает | — |
| check-no-path-deps.sh | D420: `path =` в коммитящихся `nova.toml` разрешён ТОЛЬКО под `[replace]`; `nova_tests/spec_tests/probes/scratch` исключены (легитимные резолвер-фикстуры); плюс проверка лок-файла на `source = "path"` | `awk`-автомат по секциям `[...]`/`[replace]` + `git ls-files` с тем же исключением путей; отдельный цикл по `*.nova.lock.toml` | совпадает | — |
| check-no-runtime-copy.sh | Каталог `compiler-codegen/nova_rt` в любой НЕ-главной репе/worktree — нарушение, КРОМЕ symlink/junction на настоящий | Проверка `is_main_repo` по `compiler-codegen/Cargo.toml`, затем `-L` (symlink) И `fsutil reparsepoint query` (junction) — оба легальных случая явно пропущены | совпадает | — |
| check-registry-entry-shape.sh | Каждая запись реестра `\| N \|...` обязана нести приоритет К1/К2, слово КЛАСС, оговорку «фикс носителя приёмкой не считается» | `awk`-паттерны на СТРОКУ записи ровно по этим трём признакам, храповик по числу неполных | совпадает | Честно заявляет: проверяет форму, не смысл |
| check-single-mco-resume.sh | Ровно ОДИН вызов `mco_resume(` во всём `nova_rt/**` — внутри `fibers.h::nova_resume_fiber`, минус 2 документированных allowlist (minicoro.h, `nova_fiber_run` одна строка) и test_*.c | Stateful-вычитание комментариев (не эвристика по первому символу строки), поиск тела функции по балансу `{}`, allowlist `nova_fiber_run` ограничен ОКНОМ 25 строк + точным текстом вызова | совпадает | — |
| check-test-fixture-coverage.sh | 3 проверки: rule5 (новый E_*/W_* без neg-фикстуры, diff-based), rule1 (закрытие ✅ без .nv-ссылки в той же строке, diff-based), rule3-бонус (расхождение реестра/backlog, WARN не FAIL) | Реализация 1:1: rule5/rule1 требуют валидный diff-base иначе пропуск с пометкой; rule3 явно вызывает `warn()`, не `red()`, не трогает `fail` | совпадает | Здесь (в отличие от check-doc-conventions.sh 2b) шапка и код СОГЛАСНЫ, что это WARN, а не FAIL — образцовая честность |
| hardcode-audit.sh | TRIPWIRE 7 категорий хардкода в `compiler-codegen/src`; сама шапка предупреждает, что паттерн B «широкий, ловит Result/Option», категория G «не реализована» | `count_cat_*` — те же 7 grep-паттернов из шапки один-в-один; `count_cat_g` буквально `echo 0` (как и написано) | совпадает | Шапка САМА признаёт грубость паттернов B/G заранее — это не скрытое расхождение, а объявленная погрешность. Отдельно шапка честно говорит «НЕ вызывается из gate.sh» |
| install-guards.sh | Установщик: core.hooksPath в 5 репах, chmod+x, проверка объявления guard-git/guard-memory в `.claude/settings.json`, финальная диагностика через check-guard-wiring.sh + все селфтесты | Все 4 шага реализованы ровно как описаны | совпадает | Косвенно наследует слепую зону check-guard-wiring.sh (см. выше): шаг [4/4] «всё работает» не увидит hardcode-audit.sh/lint-no-silent-int-fallback.sh/strict_effects_smoke.sh/tsan_concurrency.sh без селфтеста, потому что сам мета-страж их не смотрит |
| lint-no-silent-int-fallback.sh | 2 паттерна silent int-fallback (Cat A1/A2) в `compiler-codegen/src`, храповик по baseline-константам в самом файле | `count_a1`/`count_a2` — ровно эти 2 grep, ровно эти baseline | совпадает | Шапка честно говорит «NOT currently invoked by gate.sh — run manually» |
| strict_effects_smoke.sh | pos_*.nv обязаны PASS и с флагом, и без; neg_*.nv обязаны PASS без флага и FAIL с флагом + точный `[E_код]` в stderr | `check_pass`/`check_fail_with_code` реализуют ровно эту матрицу для двух named-фикстур + всех `pos_*.nv` | совпадает | Шапка честно говорит «NOT invoked by gate.sh — run by hand» |
| tsan_concurrency.sh | Linux-only TSAN-гейт: 0 report'ов `WARNING: ThreadSanitizer` = зелено | Проверка платформы в начале, `grep -q "WARNING: ThreadSanitizer"` по логу прогона | совпадает | Шапка честно говорит «НЕ вызывается ни из gate.sh (Windows-only), ни из workflows» |

## Сводка
- всего стражей просмотрено: 25
- совпадает: 22
- шире (ложные срабатывания): 0
- уже (пропуски): 3 — `check-bug-number-sync.sh`, `check-doc-conventions.sh`, `check-guard-wiring.sh`
- не установлено: 0

Класс №490 (страж грубее правила → ложные срабатывания, «шире») в остальных
24 стражах повторно НЕ обнаружен — носитель уже сам себя починил тем же днём.
Но нашлось ТРИ живых «уже» — по формулировке задания это ХУЖЕ: страж есть,
выглядит зелёным, а дыра открыта:
1. **check-guard-wiring.sh** — мета-страж «страж без wiring/селфтеста»
   систематически не видит СЕБЯ ПОДОБНЫХ: 5 файлов в `scripts/guards/`
   (`hardcode-audit.sh`, `lint-no-silent-int-fallback.sh`,
   `strict_effects_smoke.sh`, `tsan_concurrency.sh`, `install-guards.sh`) не
   подпадают под глоб `check-*.sh`. Четыре из них не подключены к `gate.sh` и
   не имеют селфтеста — сами честно пишут об этом в своих шапках, но
   централизованного видимого долга (ratchet) по этому факту НЕТ.
2. **check-doc-conventions.sh** — проверка 2b (same-commit pairing) в шапке
   названа «красный», а в теле явно (тем же комментарием) — «гейт не
   роняет»: `info()` вместо `red()`. Оглавление также недосчитывает число
   проверок (заявлено 6, реально ≥11).
3. **check-bug-number-sync.sh** — regex извлечения маркеров `[M-[a-z0-9_.-]+]`
   не матчит идентификаторы с заглавными буквами; на живом дереве найдено 19
   таких вхождений в `docs/plans/backlog-followups.md` (напр.
   `[M-153.2-Z-closure-devirt]`), которые страж молча не сверяет вовсе.

Решение по каждой строке — за интегратором; правки НЕ вносились.
