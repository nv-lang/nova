<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 196.5 §W1-i — верификация: задача УЖЕ ВЫПОЛНЕНА в main (сессия sonnet, доклад)

**Ветка:** `p196-w1i` (worktree `nova-196w1`), база `bc91c0c4a` (main, HEAD на момент
захода). Модель: sonnet. Суб-агенты не спавнились. **В main НЕ мёржено, коммитов кода
НЕТ** — эта заметка документирует находку разведки: заданный объём (W1-i.A extract+SHADOW,
W1-i.B flip+retire по 6 армам B05/B06a/B07/B07r/B11c/B11x) реализован и слит в main
**раньше**, в сессиях, не отражённых явным чекпоинт-файлом с именем `196.5-stage-d-w1i-notes.md`
(заметки распределены по `196.5-stage-d-notes.md`, `196.5-stage-d-final-notes.md` и голым
commit-сообщениям веток, не влитым в отдельный notes-файл с этим именем — отсюда gap,
из-за которого задание пришло повторно).

## Итог одной строкой

Все 6 армов адрессированы: **B05/B06a/B07/B07r** флипнуты на `resolve_instance_call_subst`
(единый POST-mono резолвер, §W1.5 вариант (ii)) под SHADOW-гейтом 0/0 mismatch; **B11c**
call-site снят как structurally-unreachable (тот же instance-space, что B07/B07r);
**B11x** снесён целиком волной-3 как дублирующая builtin-таблица (не generic-subst путь —
предусмотрено самим планом, §"Замечание о B11x"). Дополнительно позже найдена и починена
РЕАЛЬНАЯ регрессия в самом резолвере (turbofish-only method-generic путь, m176-класс) —
коммит `e16c0c8db`. Текущий `main` (`bc91c0c4a`) гейт зелёный: **conformance PASS 470 FAIL 0
SKIP 14** (release-бинарь, `--positive --compile-error --timeout 300 --jobs 4`, полный прогон
в корне репозитория, БЕЗ дополнительных изменений).

## Сверка по коду (не по премисе задания)

| Арм | Состояние в `emit_c.rs` (текущий main) | Коммит |
|---|---|---|
| B05 (`B05_array_ext_slice_sentinel_mono`, ~50275) | flip: зовёт `resolve_instance_call_subst(expr.id, &fn_decl, &rt, args, &turbofish_args)` | `345d6ff65` |
| B06a (`B06a_method_overload_sentinel_mono`, ~50310) | flip: тот же резолвер + `resolve_result_option_ret`/`value_aware_subst_to_ref` хвост | `c867592a0` |
| B07 (`B07_generic_type_instance_method`, ~50415) | flip: резолвер + `value_aware_subst_to_ref` + Self-return branch | `997dcd785` |
| B07r (`B07r_generic_instance_method_return`, ~50482) | flip; **side-effect `register_generic_instances_in_typeref` подтверждён ДО снятия** (не после) — §9-риск из задания (segfault на `.collect()`/`.next()`) закрыт | `997dcd785` |
| B11c (`infer_mono_method_ret_with_args`, call-site ~51248) | **снят** (site REMOVED, хелпер `infer_mono_method_ret_with_args` остаётся SHARED для другого вызывающего) — NO-HIT после флипа B07/B07r (тот же instance-space) | `ecf3f0e03` (флип) → детач в Stage-D терминале 3 (`ce79aa49b`, `196.5-stage-d-notes.md` §3.2) |
| B11x (`B11x_novaarray_methods`, builtin-таблица ~51626) | **снесён целиком** волной-3 как дубль (kill-switch A/B байт-идентичен на conformance+std/{time,runtime,collections,data}) — не generic-subst путь, план это явно допускал | `1a863acc9` (`196.5-stage-d-wave3-notes.md`) |

`resolve_instance_call_subst` (`emit_c.rs:20243`+) реально существует, консолидирует
carrier-bind (`generic_type_instance_info` + `infer_type_param_binding`) + registry-
enrichment + method-level bind (args/closure-return) + explicit turbofish seeding
(последнее — фикс регрессии `e16c0c8db`, обнаруженной ПОСЛЕ первичного флипа).

## Гейты (дословно из commit-сообщений/verification notes)

```
W1-i.A extract (d43a8a397, 01e438eb2):
  SHADOW-probe 5 бакетов — 0 mismatch (conformance + std/collections):
  B07 96/0, B07r 96/0, B11c 17/0, B05/B06a 1/0.
  Фикс хелпера: receiver=None (Mutex.with_lock) не бейлится.
  GATE: conformance 98/0.

W1-i.B flip B05 (345d6ff65):     SHADOW 0/0 (1/1 hit).  GATE-1: conformance 104/0, collections 13/0/6.
W1-i.B flip B06a (c867592a0):    SHADOW 0/0 (1/1 hit).  GATE-1: conformance 104/0, collections 13/0/6.
W1-i.B flip B07+B07r (997dcd785): SHADOW 0/0 (96/96 hit). GATE-1: conformance 104/0, collections 13/0/6.
W1-i.B flip B11c (ecf3f0e03):    SHADOW 0/0 (17/17 hit). GATE-1: conformance 104/0, collections 13/0/6.
B11x снос (1a863acc9, волна-3): kill-switch byte-identical, conformance 111/0+7skip.

Регрессия-фикс резолвера (e16c0c8db, stage-d-final): turbofish-blind spot починен
(m176-класс, `nova_tests/plan176_holes/m176_method_return_turbofish.nv` CC-FAIL → PASS).
conformance 113/0/7 (release), collections+data 17/0/9, encoding+net 21/0/7.

Эта сессия (сверка, bc91c0c4a, HEAD):
conformance (--positive --compile-error --timeout 300 --jobs 4): PASS 470 FAIL 0 SKIP 14.
```

Корпус вырос между сессиями (98→104→111→113→470 записей за счёт добавленных с тех пор
тестов других планов) — само число не сопоставимо 1:1 между сессиями, но каждая
зафиксирована как 0 FAIL на своей базе, и текущий HEAD зелёный на актуальном корпусе.

## Вывод

Новой работы по §W1-instance addendum (196.5-node-substs-channel.md, W1-i.A/B) **не
требуется** — объём полностью закрыт. W1-i.C (мемоизация) не запускалась ни разу
(п.4 задания: только по метрике горячей точки — таковой не зафиксировано ни в одной из
предыдущих волн).

Единственная находка ЭТОЙ сессии: сам факт, что задание пришло повторно, указывает на
**gap в состоянии плана** — верхний TL;DR-статус `196.5-node-substs-channel.md` (строка 11)
до сих пор гласит `🔄 ДИЗАЙН`, хотя §W1-instance addendum и последующий Stage-D давно
реализованы и слиты (счётчик коммитов `196.5 ...` в `git log --oneline main` — десятки,
последний относящийся к резолверу — `e16c0c8db`). Обновление верхнего статус-маркера —
решение владельца плана (не в периметре точечного задания W1-i), поэтому оставляю
как явную рекомендацию, а не самовольную правку.

## Хэши

- Разведка/сверка: без кодовых изменений. Единственный коммит сессии — этот notes-файл.
- Релевантные коммиты, уже находящиеся в `main` (bc91c0c4a — HEAD этой сессии):
  `d43a8a397`, `01e438eb2` (W1-i.A) · `345d6ff65`, `c867592a0`, `997dcd785`, `ecf3f0e03`
  (W1-i.B flips) · `1a863acc9` (B11x снос, волна-3) · `ce79aa49b` (B11c детач, терминал 3) ·
  `e16c0c8db` (регрессия-фикс резолвера, stage-d-final).
- Worktree `nova-196w1` (ветка `p196-w1i`) создан по инструкции задания; изменений кода
  не потребовалось — можно удалить после интеграции этой заметки (`git worktree remove`)
  либо оставить пустым для следующей волны.
