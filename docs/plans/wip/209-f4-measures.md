<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# План 209 Ф.4 — замеры part-size порога (`MULTI_TU_PART_THRESHOLD_BYTES`)

Рабочий чекпоинт-файл (не финальный отчёт — тот идёт в план 209 Ф.4-статус). Обновляется
по одной точке за раз, коммитится после каждой точки (владелец: сеть рвалась, CPU уже не
пустой — фиксировать промежуточные результаты).

## Константа

`compiler-codegen/src/codegen/emit_c.rs:2022` — `MULTI_TU_PART_THRESHOLD_BYTES: usize`.
**Это Rust-константа, НЕ env-override** (env есть только для `multi_tu_enabled` —
`NOVA_MULTI_TU=1`). Значит каждая точка порога требует **пересборки компилятора**
(`cargo build --release --manifest-path nova-cli/Cargo.toml`, ~3 мин/точка). Матрица по
инструкции сокращена до **{500КБ (baseline), 1.5МБ, 4МБ}** (не 5 точек).

Соседняя константа `MULTI_TU_SIZE_THRESHOLD_BYTES = 2МБ` (emit_c.rs:2019, гейт "включать ли
multi-TU вообще" — по размеру ИЛИ >200 top-level fn) — НЕ трогается, это Ф.4 не касается.

## Окружение замера

- Worktree `d:/Sources/nv-lang/nova-209f4` (branch `p209-f4-measure`), бинарь
  `nova-cli/target/release/nova.exe`.
- `NOVA_GC_LIB_DIR`/`NOVA_INCLUDE_DIR`/`NOVA_GC_INCLUDE_DIR` → main-репа vcpkg_installed
  (worktree без vcpkg).
- `NOVA_MULTI_TU=1` (opt-in флаг, дефолт off — не трогаем).
- `NOVA_CACHE=0` (иначе build-cache hit пропускает codegen ЦЕЛИКОМ — `EmitOutput::Single`
  даже выше порога; см. `nova-cli/src/main.rs:4858` коммент — сравнение было бы бессмысленным).
- `TEMP`/`TMP` → изолированная scratch-папка (папка `$TEMP` шэрится с другими параллельными
  агентами на машине — иначе артефакты/PID-папки перемешиваются).
- ⚠ **Шум**: владелец сообщил, что CPU перестал быть тихим (флот агентов работает
  параллельно) в процессе замера. Точки, где это могло повлиять, помечены «шумная» —
  для них медиана по ×3 прогонам вместо ×2.

## Цели (таргеты)

1. **aggregator** — `nova build examples/flagship/aggregator/src/main.nv --strict-effects`
   (dev mode, без `--keep-artifacts` кроме первого прогона на точку — для part-count/size).
   CU переходит порог multi-TU НЕ по байтам (общий вывод ~2МБ, чуть ПОД
   `MULTI_TU_SIZE_THRESHOLD_BYTES`), а по **>200 top-level fn** — годный, хоть и не крупный,
   тест-кейс (реальный multi-part split при 500КБ: 4 части).
2. **conformance mega-CU** — `nova test --filter app_effect_basic_t8_1 --jobs 1
   spec_tests/conformance`. **Важность находки**: `nova check spec_tests/conformance`
   (как было в задании) НЕ годится — `nova check` вообще не проходит через codegen/C-компиляцию
   (чистый параллельный per-file type-check, `cmd_check`/`check_one_file`), порог part-size
   там ни на что не влияет. Реальный gate одного мега-CU (13 МБ / 963 файла, folder-module
   `spec_tests.conformance` — все top-level `.nv` в каталоге суть co-equal peer-файлы ОДНОГО
   модуля) достигается запуском `nova test` с `--filter` на ЛЮБОЙ файл этого общего модуля —
   `app_effect_basic_t8_1` (857 байт исходника) выбран как representative-имя именно потому,
   что упомянут в тексте Ф.4; сам файл тривиален, тяжесть — в общем CU его co-equal peers.
   `--jobs 1` (не 4) — чтобы не мешать с другими файлами/каталогами (у них своя, отдельная,
   компиляция — `--filter` всё равно требует пройтись по дереву, но выполнится только 1 тест).
   Один прогон на порог (дорого — ~130+с), как проговорено в задании.
   Известный PRE-EXISTING RUN-FAIL (`[M-208-vec-chained-debug-display-red]`, Vec chained
   .debug/.display assert) — НЕ связан с 209/этим замером, время сборки не искажает.
3. **std/src/collections** — `nova test std/src/collections` (13 файлов с test-блоками, 6
   library-skip). Каждый файл — СВОЙ отдельный CU (folder ≠ единый модуль здесь — отдельные
   test-файлы). При 500КБ каждый CU уже чуть выше 2МБ-гейта (общий prelude-объём) → **ровно 1
   part на файл** (13 `_common.h` / 13 `_part0.c`, ПРОВЕРЕНО) — т.е. per-file CU здесь МЕЛКИЕ,
   part-size порог 500КБ→1.5МБ→4МБ **не должен менять part-count** (все и так ниже 500КБ на
   part), полезен как negative-контроль (ожидаем плоскую линию).

## Таблица замеров

| Порог | Цель | N прогонов | Время (медиана) | Прогоны (с) | TU-частей | Общий .c (байт) | Заметки |
|---|---|---|---|---|---|---|---|
| 500КБ (baseline, как в main) | aggregator | 3 | **33.25s** | 44.11 / 30.81 / 33.25 | 4 | 1 992 529 | rep1 включал `--keep-artifacts` (лишний I/O) |
| 500КБ | conformance (app_effect_basic_t8_1, jobs=1) | 1 | **134.14s** | 134.14 | 24 | 13 259 539 (11 975 018 parts + 1 284 521 common.h) | известный RUN-FAIL (пред-существующий, не наш) |
| 500КБ | std/src/collections | 3 | **78.04s** | 76.92 / 78.04 / 79.50 | 1/файл (13 CU × 1 part) | — (per-CU мелкие) | 13 PASS / 0 FAIL / 6 SKIP; part-count не варьируется порогом (ожидаемо) |

| 1.5МБ | aggregator | 3 | **23.22s** | 31.05 / 20.90 / 23.22 | 2 | (не замерян отдельно — common.h 368 689, part0+part1 не собраны в сумму) | rep1 `--keep-artifacts` |
| 1.5МБ | conformance (app_effect_basic_t8_1, jobs=1) | 1 | **108.52s** | 108.52 | 8 | 13 258 867 (11 974 346 parts + 1 284 521 common.h) | тот же известный RUN-FAIL |
| 1.5МБ | std/src/collections | 3 | **74.79s** (шумная — разброс 67-123s, флот уже активен) | 74.79 / 67.19 / 123.42 | 1/файл (13 CU × 1 part, без изменений) | — | подтверждён negative-контроль: part-count не изменился |

| 4МБ | aggregator | 3 | **47.22s** | 37.18 / 49.83 / 47.22 | 1 (весь CU уместился в 1 часть — split-механизм всё равно платит header/IO, параллелить нечего) | — | медленнее ОБЕИХ соседних точек |
| 4МБ | conformance (app_effect_basic_t8_1, jobs=1) | 1 | **266.32s** | 266.32 | 3 | 13 258 657 (11 974 136 parts + 1 284 521 common.h) | тот же известный RUN-FAIL; ЗАМЕТНО медленнее 500КБ И 1.5МБ — компилятор-суперлинейность на крупной TU перевешивает экономию заголовка |
| 4МБ | std/src/collections | 3 | **32.79s** (машина заметно тише в этом окне — см. вердикт про шум) | 31.82 / 32.79 / 38.06 | 1/файл (13 CU × 1 part, без изменений) | — | подтверждает: часть разброса на 1.5МБ-точке была внешним шумом, не сигналом от порога |

## Вердикт

**Рекомендация: поднять `MULTI_TU_PART_THRESHOLD_BYTES` 500КБ → 1.5МБ.** Применено
(`compiler-codegen/src/codegen/emit_c.rs:2022`, с комментарием-обоснованием). Дефолтное
поведение (`NOVA_MULTI_TU` не установлен) **не затронуто** — порог используется ТОЛЬКО
внутри multi-TU split-пути (опция всё ещё opt-in, off по умолчанию); sanity-билд
`examples/getting_started.nv` без флага — тот же результат, что и раньше.

### Почему 1.5МБ, а не 500КБ или 4МБ

Сводка (медианы, part-count):

| Порог | aggregator (parts / медиана) | conformance mega-CU (parts / время, 1 прогон) | collections (parts / медиана) |
|---|---|---|---|
| 500КБ | 4 / 33.25s | 24 / 134.14s | 13×1 / 78.04s |
| **1.5МБ** | **2 / 23.22s** | **8 / 108.52s** | 13×1 / 74.79s (шумная) |
| 4МБ | 1 / 47.22s | 3 / 266.32s | 13×1 / 32.79s |

- **conformance mega-CU** (единственная цель с достаточным размером/частями, чтобы реально
  показать эффект параллели, `--jobs 1` — чтобы не путать с test-level параллелизмом):
  1.5МБ **на 19.1% быстрее** 500КБ (108.52s vs 134.14s) — подтверждает гипотезу плана
  («26 частей — многовато, каждая тянет весь `_common.h` → N лишних перепарсов заголовка»).
  4МБ **на 145% МЕДЛЕННЕЕ** 1.5МБ (266.32s) — слишком мало/крупных частей: (а) C-компилятор
  суперлинеен по размеру ОДНОЙ TU (ровно та проблема, ради которой затевался весь План 209),
  (б) на 3 частях параллельность толком не задействуется. Итог по conformance — классическая
  U-образная кривая, минимум около 1.5МБ (~8 частей), ЧТО СОВПАДАЕТ с целевым диапазоном
  владельца («~7-9 частей» при поднятии до 1.5-2МБ).
- **aggregator** (маленький multi-part CU, 4→2→1 частей): та же форма — 1.5МБ быстрее ОБЕИХ
  соседних точек (-30.2% vs 500КБ, медленнее сравнения с 4МБ, где всего 1 часть = 0 параллели
  при полной split-накладной). Подтверждает, что эффект не специфичен для гигантского CU.
- **std/collections** (negative-контроль, per-file CU — уже < 500КБ на файл): part-count
  ПЛОСКИЙ на всех 3 точках (13 CU × 1 part), как и предсказано — порог тут физически не может
  сработать (нечего резать). Разброс времени (78→75→33s) — ЧИСТЫЙ внешний шум (флот агентов
  на машине то занят, то простаивает), НЕ сигнал от порога; полезен как подтверждение, что
  часть замеров на других точках тоже подвержена такому же шуму на ВРЕМЕНИ (но НЕ на part-count
  — тот детерминирован размером/порогом, шумом не объясняется).

### Оговорка по шуму (владелец предупредил в процессе замера)

CPU перестал быть «тихим» в процессе задания (параллельный флот агентов). Абсолютные
секунды между ТОЧКАМИ (500КБ/1.5МБ/4МБ) поэтому НЕ строго сравнимы один-к-одному — окна
замера растянуты во времени, внешняя нагрузка плавает (see collections 4МБ-точка: время
УПАЛО вдвое без изменения part-count — чистый эффект того, что машина в этот момент была
свободнее, а не порог помог). Однако part-count-эффект (24→8→3, 4→2→1) детерминирован кодом
(не шумом), а НАПРАВЛЕНИЕ времени на conformance (1.5МБ быстрее ОБЕИХ соседей) устойчиво:
4МБ-точка измерена в окне, когда машина была ЗАМЕТНО СВОБОДНЕЕ (судя по collections
32.79s vs 74-78s на предыдущих точках) — то есть если бы шум объяснял разницу, 4МБ должна
была быть БЫСТРЕЕ, а не в 2.4 раза медленнее. Регрессия на 4МБ — реальный эффект (компилятор-
суперлинейность), не шум-артефакт. Вердикт по 1.5МБ уверенный.

### Что применено
- `compiler-codegen/src/codegen/emit_c.rs:2022` — `MULTI_TU_PART_THRESHOLD_BYTES` 500КБ →
  1.5МБ (с обоснованием в doc-комментарии).
- `MULTI_TU_SIZE_THRESHOLD_BYTES` (2МБ, "включать ли multi-TU вообще") — НЕ тронут (вне
  периметра Ф.4).
- Дефолт (`NOVA_MULTI_TU` off) — байт-идентичен, флаг всё ещё opt-in (Ф.6 — отдельная фаза).
