# PROGRESS — план 241, Ф.0 (глоссарий spec/glossary.ru.md)

> Предыдущая задача в этом файле (перевод ///-комментариев std) закрыта
> полностью (см. git log PROGRESS.md, коммит 192b73d5d) — файл переиспользован
> под новую задачу этого worktree/ветки.

Ветка: `p241-glossary`. Исполнитель: sonnet. Файл: `spec/glossary.ru.md`.

## ГОТОВО (3 волны).

**Волна 1:** 11 разделов + Open questions, 80 строк-терминов.

**Волна 2 (добор по норме #language):** для каждой строки — настоящая
русская форма или явная пометка `[keep-en: код/аббревиатура/идиома/
термин]`; спорные калькирования («файбер») помечены `[CONTESTED]` и
вынесены в Open questions, не решены единолично.

**Волна 2.5 (между сессиями, коммиты `c557a0a0f`/`646b64ac2`, не мои):**
владелец разобрал Open questions 1-11 — «одна дверь»→"no second door",
скрутини→scrutinee/«сопоставляемое значение», paradigm.md сначала
актуализировать, `ro` используется как сокращение readonly, «файбер»
остаётся, «тип-сумма», «тип-обёртка», «структурированная конкурентность»,
`enforce-with-elision` — keep-en, «равноправные файлы». На тот момент
пункт 4 (счёт терминов) был решён «оставить 80, не разбивать».

**Волна 3 (эта сессия) — координатор передал 2 новых решения владельца,
отменяющие/уточняющие прежнее:**
1. **Лимит «50-80» СНЯТ** («если есть термин — он должен быть»). Разнёс
   ВСЕ строки, где в одной строке было два понятия, обратно на отдельные
   строки: AI-first design/killer use-case; requires/ensures/invariant/
   decreases (§2, было 1 строка → 4); позиционный/именованный кортеж;
   мономорфизация/dyn-диспетчеризация; generic bound через protocol/
   через type-set; fluent chain/receiver; pattern matching/guard; embed/
   delegation; direct/transitive effect; операторы `?`/`!!`; region/
   real-time zone; deadline/cancellation; folder-module/peer files;
   unsafe block/pointer-mutability model; proven/unproven contract;
   precondition/postcondition; widening/narrowing. Заодно добавлен один
   пропущенный термин, замеченный по ходу правки §11 — семейство
   `to_str()` (D410). Пункт 4 журнала решений переписан (упоминание
   лимита убрано, зафиксировано новое решение).
2. **§8 «равноправные файлы» переформулирована** по замечанию владельца
   (не было видно связи с модулем): теперь «файлы одного модуля
   (равноправные файлы)», в примечании явно — папка = ОДИН модуль, файлы
   внутри равноправны, общее пространство имён, друг друга по import не
   подключают. Пункт 11 журнала решений обновлён тем же уточнением.

Счёт строк: было 80 → **стало 100** (после разбора слитых строк + 1
новый термин).

## Источники (сверены)
- spec/overview.ru.md, spec/paradigm.ru.md (устарел, помечен предупреждением в
  шапке — термины trait/impl НЕ брать как актуальные, актуально
  protocol/effect), spec/syntax.ru.md, spec/effects.ru.md, spec/conversions.ru.md,
  spec/revolutionary.ru.md
- spec/decisions/README.md — тематические разделы (для структуры глоссария)
- docs/guide/language-tour.md, value-vs-reference.md, consume-types.md,
  contracts.md, parameters.md, channels.md, typed-pointers.md
- docs/dev/doc-conventions.md #language (норма «минимум английских слов»)

## Счёт строк по разделам (волна 3, финал)
§1: 8 · §2: 17 · §3: 14 · §4: 13 · §5: 9 · §6: 5 · §7: 8 · §8: 5 · §9: 5
· §10: 8 · §11: 8 · **Итого: 100**.

## Открытые вопросы владельцу — актуальный статус
Пункты 1-11 журнала решений (§ "Open questions for owner review") **все
закрыты** решениями владельца (коммиты c557a0a0f/646b64ac2 + пункт 4/11
переписаны этой волной). Новых открытых вопросов эта волна не завела —
только техническая правка (разбор строк + переформулировка §8).

## Заметка
paradigm.md помечен УСТАРЕВШИМ в шапке файла (описывает `trait`/`impl`,
которые заменены на `protocol`/эффект-через-kind-токен) — владелец решил:
сначала актуализировать норматив, потом переводить (отдельная
микро-задача, не в объёме Ф.0).

---

# План 241 Ф.2c — чистка трёх старых ru-пар guide (ветка `p241-guide`)

Задача: TASK-241c.md. Исполнитель: big-pickle. Три файла
(`channels.ru.md`, `contracts.ru.md`, `nova-cli.ru.md`) — старше конвенции:
переведённые комментарии внутри ```-блоков + англицизмы в прозе.
Действия по каждому файлу: вернуть код-блоки байт-в-байт из `X.md`,
вычистить англицизмы по spec/glossary.ru.md (норма `#language`),
добавить frontmatter (`source_rev`/`source_date`). Коммит + push после
каждого файла.

## ГОТОВО

- **`docs/guide/channels.ru.md`** — коммит `a18faa561` (push выполнен).
  Код-блоки: diff через `awk` по ```-блокам — ПУСТО (идентичны
  `channels.md` байт-в-байт). Frontmatter добавлен
  (`source_rev: 07df7d2c9`, `source_date: 2026-08-02`). Англицизмы
  заменены: «capability-split» → «разделение прав», «record» → «запись»,
  «writer/reader-capability» → «право отправки/получения», «arm» → «ветвь»
  (в прозе), «guard» → «охранное условие», «waiter» → «ожидающий»,
  «wildcard» → «подстановочный знак», «ref-count» → «счётчик ссылок»,
  «managed heap» → «управляемая куча», «destructure» → «деструктуризация»,
  «scope» → «область видимости», «flaky» → «нестабильные»,
  «panic» → «паника», «shuffle» → «перемешивание», «non-blocking» →
  «неблокирующий», «graceful shutdown» → «корректное завершение»,
  «timeout» → «таймаут», «edge cases» → «крайние случаи»,
  «performance» → «производительность», «syscall» → «системный вызов»,
  «allocate» → «выделение памяти», «writer» → «отправитель»,
  «lifetime» — в коде оставлено (код-блок). Заголовки, структуру и ссылки
  не трогал (кроме `spec` → «спецификация» в прозе вне кода).

- **`docs/guide/contracts.ru.md`** — коммит `48b1417a4` (push выполнен).
  Код-блоки: diff через `awk` по ```-блокам — ПУСТО (идентичны
  `contracts.md` байт-в-байт). Маркеров конфликтов нет. Frontmatter
  добавлен (`source_rev: 07df7d2c9`, `source_date: 2026-08-02`).
  Англицизмы заменены: «SMT-солвер» → «SMT-решатель», «runtime» → «рантайм»
  (в прозе), «opt-out» → «отключение проверок», «fail-fast» →
  «немедленное аварийное завершение», «per-kind» → «по видам», «scope» →
  «область видимости», «matching loop» → «цикл сопоставления», «mutation» →
  «мутация», «pre/postcondition» → «пред/постусловие», «receiver» →
  «получатель», «bounded quantifiers» → «ограниченные кванторы»,
  «loop invariants» → «инварианты циклов», «widening/narrowing» →
  «знаковое расширение/отбрасывание». Термины lemma/apply/calc/reveal/
  fuel и FFI/CI оставлены (keep-en). Заголовки, структуру и ссылки не
  трогал.

- **`docs/guide/nova-cli.ru.md`** — коммит `98b631dd0` (push выполнен,
  `871dda253..98b631dd0`). Код-блоки: diff через `awk` по ```-блокам —
  ПУСТО (идентичны `nova-cli.md` байт-в-байт). Маркеров конфликтов нет.
  Frontmatter добавлен (`source_rev: 07df7d2c9`, `source_date: 2026-08-02`).
  Исправлены 5 код-блоков с переведёнными комментариями (Quickstart,
  установка/сборка, `nova info`, `nova test`, `Time.after(<expr>)` в
  migrate_plan65). Англицизмы заменены (проза + таблицы флагов):
  «default» → «по умолчанию», «субкомандам» → «подкомандам», «Cap» →
  «Лимит», «crate» → «крейт», «Workspace» → «рабочее пространство»,
  «path-зависимость» → «зависимость по пути», «exit» → «код выхода»,
  «commit» → «коммит», «summary» → «сводку», «output» → «вывод»,
  «noise-floor» → «уровень шума», «SMT-backend» → «SMT-бэкенд»,
  «mutation testing» → «мутационное тестирование», «override» →
  «переопределяет», «usage error» → «ошибка использования»,
  «escape hatch» → «запасной вариант», «forensic» → «диагностика»,
  «mult-runner CI matrix» → «CI-матрица из нескольких раннеров»,
  «per-bench» → «бюджет на каждый bench» и др. Код-идентификаторы и
  keep-en (bench, consume, effect-surface, SMT, git-термины, CWD, env)
  оставлены. Заголовки, структуру и ссылки не трогал.

## В работе / впереди

- Все три файла готовы: channels (`a18faa561`), contracts (`48b1417a4`),
  nova-cli (`98b631dd0`). Осталось — итоговый отчёт по TASK-241c.md.

---

# План 241 Ф.2d — обратные ссылки + англицизмы в guide (ветка `p241-guide`)

Задача: TASK-241d.md. Исполнитель: sonnet (эскалация после двух неудачных
стартов дешёвого исполнителя).

## Шаг 1 — переключатель языка в английских X.md (готово, 26/26)

Во всех 26 файлах `docs/guide/X.md`, у которых есть пара `X.ru.md`, добавлена
обратная ссылка `**English** | [Русский](X.ru.md)` ровно в той позиции, где в
`X.ru.md` стоит её зеркало `[English](X.md) | **Русский**` (после H1 — в
большинстве файлов; до SPDX-комментария и H1 — в `cleanup-cookbook`,
`ffi-cookbook`, `language-tour`, `typed-pointers`, где так стоит и ru-сторона).

`runtime-tuning.ru.md` не имел переключателя вовсе — добавлен симметрично в
обе стороны (`runtime-tuning.md` + `runtime-tuning.ru.md`).

3 файла (`channels.md`, `contracts.md`, `nova-cli.md`) уже имели переключатель
из предыдущей волны (241c) — пропущены, формат сверен и совпадает.

Коммиты (по 5 файлов):
- `db1332952` — authoring-a-module, auto-derive-guide, cleanup-cookbook,
  consume-types, datetime
- `43d166aa4` — embed, ffi-cookbook, field-cache-optimization,
  field-visibility-guide, io-fs
- `319bad4a1` — language-tour, linux-build, parameters, quickstart,
  runtime-tuning (+ недостающий переключатель в runtime-tuning.ru.md)
- `52a6708d6` — size-of-align-of, strings, time, tutorial-cleanup,
  typed-pointers
- `0f09b929b` — value-vs-reference, vec-lazy, vec-owned

Итог: 23 файла получили новый переключатель + 1 ru-файл (runtime-tuning.ru.md)
получил недостающий обратный переключатель = 26/26 пар с переключателем на
обеих сторонах.

## Шаг 2 — англицизмы в русской прозе (готово)

Грепом `скоуп|ресивер|хендлер|роутер|консюм|капчур` по всем 26
`docs/guide/*.ru.md` найдены совпадения только в 4 файлах (хендлер/роутер/
консюм*/капчур* не встретились нигде):

| Файл | Замены |
|---|---|
| `field-visibility-guide.ru.md` | 9× «скоуп» → «область видимости» (в т.ч. «строгий type-only скоуп» → «строгая область видимости только для типа», «вне-скоуп заблокирован» → «доступ вне области видимости заблокирован», «строжайший скоуп» → «строжайшая область видимости») |
| `strings.ru.md` | 2× «скоуп» → «область видимости» |
| `time.ru.md` | 1× «handler лексически скоупнут» → «handler лексически ограничен областью видимости» |
| `vec-lazy.ru.md` | 3× «ресивер» → «получатель» + 1× «файл-скоупный статический синглтон» → «статический синглтон с областью видимости файла» |

Коммиты (по файлу): `0047ce086` (field-visibility-guide), `aa0d2c1f7`
(strings), `60c66449f` (time), `49ccb63c7` (vec-lazy).

## Верификация

- Код-блоки (```-fences): `awk`-diff по всем 26 парам — ПУСТО (идентичны
  байт-в-байт по содержимому код-блоков) как до, так и после шага 2.
- Конфликт-маркеры (`<<<<<<<`/`>>>>>>>`): грепом по всему `docs/guide/` —
  ноль совпадений.
- Push: все коммиты в `github p241-guide`, ветка синхронна с remote.
