export type Safe protocol {
    @dummy() -> ()
}

// private, F has NO bound declared at all
fn register[F](f F) -> () {
    f.dummy()
}

fn inc(x int) -> int => x + 1

export fn use_it() -> () {
    register(inc)
}

// honesty control
export fn honesty_control() -> int => undefined_identifier_zzz
