export type Signal effect {
    ping() -> ()
}

// public, effect NOT declared in signature — per D28, should be a compile error
export fn helper() -> () {
    Signal.ping()
}

// honesty control
export fn honesty_control() -> int => undefined_identifier_zzz
