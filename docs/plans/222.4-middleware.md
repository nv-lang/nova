<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 222.4 — модуль `polaris.middleware`: ядро композиции

**Статус:** ✅ ЗАКРЫТ 2026-07-25 — Middleware = голый newtype-над-fn (nova-http 70fb77a; блокер-семья №78/90/96/97 закрыта планом 228 канал-материализацией; ре-гейт 26/0/19, флагман built)
**Зависит:** 222.1 (✅ влит). **Блокирует:** [222.12](222.12-http-batteries.md) (все батарейки —
это middleware поверх этого ядра).
**Основа:** **Go/`chi`** — их `func(http.Handler) http.Handler` **один-в-один** наш
`fn(next Handler) -> Handler`. tower-`Service` (generic + `poll_ready` + backpressure) НЕ переносится.
**Родитель:** [222](222-http-framework.md) · [222.0](222.0-module-map.md).

## 1. Типы

**Пересмотрено 2026-07-23 (вопрос владельца «почему не `type Middleware fn(Handler) -> Handler`»).**
Обёртка-запись `value { priv f fn(Handler) -> Handler }` была написана до слияния `4c26fe2a0`
и держалась на двух доводах — **оба отпали**: (1) «голый fn-тип не парсится» — это был
`[M-newtype-over-fn-type-unsupported]`, закрыт тем коммитом; (2) «обёртка позволяет вешать
методы композиции» — **неверно, проверено пробой**: методы на голом fn-newtype работают
(`fn Middleware @label() -> str` компилируется и вызывается). Целевая форма:

```nova
/// ГОЛЫЙ newtype над fn-типом — единообразно с `Handler` (222.3 §5а).
export type Middleware fn(Handler) -> Handler

/// Применить к хендлеру — ПРЯМОЙ вызов, метод `@apply` не нужен:
///     ro wrapped = mw(next)

/// Композиция: `a.then(b)` — сначала a (внешний), потом b (внутренний).
export fn Middleware @then(inner Middleware) -> Middleware =>
    fn(next Handler) -> Handler => @(inner(next))
```
`Middleware.new` и `@apply` исчезают вместе с обёрткой: конструктор — сама fn (авто-подъём
D55), применение — вызов. Это ровно та же линия, что снос `handler_fn` в 222.3 §5а.

**ЗАБЛОКИРОВАНО** — см. §4б: обе ключевые формы упираются в незакрытый call-through.

## 2. Подключение к `Router`

```nova
/// Копит цепочку; применяется при РЕГИСТРАЦИИ маршрута (не при dispatch — дешевле).
export fn Router mut @layer(mw Middleware) -> @

/// Слой только на группу — работает через уже существующий `nest` (222.1):
/// сначала `sub.layer(mw)`, затем `parent.nest("/api", sub)`.
```

**Порядок применения (зафиксировать в доке, частый источник путаницы):** слой, добавленный
ПОЗЖЕ, оборачивает СНАРУЖИ (как chi/Express `use`). Т.е. `r.layer(a); r.layer(b)` → запрос идёт
`b → a → handler`. Пин-тест обязателен.

## 3. Взаимодействие с `nest`

Слои родителя применяются к вложенным маршрутам; слои саб-роутера — только к его маршрутам.
Реализация: при `nest` слои родителя навешиваются на уже собранные хендлеры саб-роутера.
**Ловушка:** двойное применение, если саб-роутер уже был вложен куда-то ещё — пин-тест.

## 4. Что НЕ делаем (и почему)

- **tower-`Service`-модель** (poll_ready/backpressure) — не переносится в нашу синхронную
  фибро-модель и не нужна: backpressure у нас на уровне accept-loop (222.7 admission bounded).
- **Middleware, меняющий тип запроса/ответа** (Axum умеет через generic `Service`) — усложнение
  без спроса; наш контракт строго `Handler -> Handler`.

## 4а. Плоская форма `middleware(req, next)` — канон (решение владельца 2026-07-23)

**Факт 2026-07-23:** `src/server/middleware.nv` УЖЕ РЕАЛИЗОВАН в двухуровневой форме
(`Middleware.new(f fn(Handler) -> Handler)`, `@apply`, `@then`, `Router.layer`). Ниже —
**аддитивная** добавка, не переделка.

**Проблема (унаследована от Go/chi):** двойная вложенность на каждое middleware —
`Middleware.new(fn(next) -> Handler.new(fn(req) -> …))`. У chi ровно та же многословность.
Против цели «минимум кода» (222 §0в).

**Канон — плоская форма, одно замыкание:**
```nova
export fn middleware(f fn(ServerRequest, Handler) -> ServerResponse) -> Middleware

ro logging = middleware(fn(req, next) -> ServerResponse {
    ro resp = next.handle(req)          // next — обычное значение Handler
    resp
})
```
Двухуровневая `Middleware.new(fn(next) -> Handler)` ОСТАЁТСЯ — но как **power-форма** для
middleware с разовой работой при композиции (скомпилировать шаблон, выделить пул): там внешнее
замыкание оправдано, оно выполняется один раз при регистрации.

**АКТУАЛЬНЫЙ ОБЪЁМ миграции на плоскую форму (инвентарь 2026-07-23):** двухуровневых
`Middleware.new(fn(next) …)` — **10 сайтов в 8 файлах**: `server/{middleware,auth}.nv` ·
`middleware/{cors,compress,log,ratelimit}.nv` · `server_router.nv` + пир-тест. Все они —
кандидаты на переписывание в `middleware(fn(req, next) …)`, КРОМЕ тех, что делают разовую
работу при композиции (проверить каждый: если внешнее замыкание только оборачивает — плоская
форма; если считает/выделяет — остаётся power-форма). Плоская форма аддитивна: вводится
первой, миграция сайтов — вторым шагом, обе формы валидны одновременно.

**Про `next` (вопрос владельца — «как в Express?»):** Express-колбэк `(req, res, next)` нам НЕ
нужен — он следствие callback-модели, а не удобство. Наша форма строго лучше по четырём осям:
код ПОСЛЕ `next.handle(req)` выполняется буквально (у Express с async — классическая ловушка);
двойной вызов невозможен по построению (ответ — ЗНАЧЕНИЕ, вернуть можно одно, а не «headers
already sent»); короткое замыкание = просто вернуть свой ответ, забыть нельзя (функция обязана
что-то вернуть); цепочка собирается ОДИН раз при регистрации, а не на каждый запрос.

## 4б. БЛОКЕР Ф.1 (факт 2026-07-23, проба интегратора)

Плоская форма §4а и вся двухуровневая форма опираются на `fn(next Handler) -> Handler`, т.е. на
fn-newtype **в позиции возврата**. После слияния `4c26fe2a0` (три куска fntypes) эта позиция
**сломана**: `fn make() -> Handler => base` компилируется, но вызов результата даёт либо ICE
(`[P67-LEGACY] Path call return type unknown`, `emit_c.rs:56229`), либо CC-FAIL с переставленными
местами C-типами аргумента и возврата. Позиции *параметра* и *локального биндинга* работают.
Проверено на форме именно этого плана (2026-07-23): `mw(h)(v)` → CC-FAIL
`called object type 'void *' is not a function`; вызов самого приёмника `@(v)` в теле метода
fn-newtype → тот же ICE `[P67-LEGACY]`. То есть и `Middleware`-как-newtype (§1), и `@then`
блокированы одним куском.

Маркер `[M-fn-newtype-return-position-broken]` (backlog-followups.md, **P1**) — **Ф.1 не стартует
до его закрытия**.

## 5. Фазы

- **Ф.1** `Middleware` + `@apply`/`@then` + `Router.layer` + пин-тесты порядка.
- **Ф.2** взаимодействие с `nest` (§3) + пин-тест на двойное применение.
- **Ф.3** демо-middleware в тестах (тривиальный «добавь заголовок») — доказать композицию;
  реальные батарейки — уже 222.12.

## 6. Гейты

Юниты модуля (композиция проверяется без сокетов — `Handler` это функция) +
`nova test src/server` δ0 + интеграционный гейт зонтика. **Модель:** sonnet.
