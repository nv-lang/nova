<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 275. Цена гейта: привести стражей к Г-правилам конвенции

**Статус:** 🟡 В РАБОТЕ (заведён 2026-08-21 интегратором по решению владельца).

**Норматив:** [docs/dev/gate-guard-conventions.md](../dev/gate-guard-conventions.md)
(Г-правила, без диапазона: число устаревает молча; авторство — окно 274, приняты как норма всего дерева
2026-08-21). Этот план — не пересказ конвенции, а её применение к
`scripts/gate.sh` и к 85 стражам главного дерева.

---

## 0. Замер, с которого всё начинается

Профиль снят 2026-08-21 по шагам (Г1: сначала замер, потом ускорение).
**67 шагов, 1401 секунда, 23.4 минуты.** Первые три шага — 57% всего гейта:

| секунд | доля | шаг |
|---|---|---|
| 365 | 26.1% | самотесты стражей (все из каталога, параллельно) |
| 224 | 16.0% | `conformance --full` (PASS 854 / FAIL 1 = известный №692) |
| 210 | 15.0% | мега-CU `--positive --compile-error` (PASS 828 / FAIL 0) |
| 63 | 4.5% | флагман на чистом дереве из HEAD (№565) |
| 60 | 4.3% | `nova test std/src` (PASS 75 / FAIL 0) |
| 59 | 4.2% | флагманы `--strict-effects`, 6 целей |
| 56 | 4.0% | crate-tests (nova-lsp + nova-cli, №723) |
| 53 | 3.8% | retired-names |
| 42 | 3.0% | doc-conventions |
| 39 | 2.8% | process-exit-under-pool (×200 запусков, №694) |
| 32 | 2.3% | doc-truth |
| 26 | 1.9% | no-machine-paths |
| 19 | 1.4% | флаги `NOVA_*` имеют вызывающего (№578) |
| 62 | 4.4% | остальные 44 шага, каждый 0–13 с |

### Куда именно уходит время (разложено, а не оценено)

`check-doc-truth` — 45.3 с целиком, из них **обход дерева 0.37 с**
(`find｜xargs｜awk`, 234 кандидата), `nova <sub> --help` × 19 подкоманд по
210 мс ≈ 4 с, **остальные ~41 с — процессы в теле цикла**: `sed`,
`printf｜grep`, `grep -qxF` по три-четыре на каждую строку. Это Г2 в чистом
виде.

**Контрпример, важнее правила:** `check-retired-names` — 56 с и **уже на
питоне**. Внутри 14 отдельных регексов на КАЖДУЮ строку живых зон. Язык не
спас; лечится одной альтернацией. Отсюда формулировка плана: убираем не
shell, а **процесс/регекс на элемент**.

**Почему самотесты стоят 365 с.** Это не 88 стартов, а N × цена самого
стража: `doc-conventions` зовёт свой страж 12 раз, `guard-wiring` — 10,
`doc-examples` — 5, `doc-truth` — 3. Чинится страж — самотест дешевеет сам.

**Механическая перекличка** (совпадение двух независимых замеров): текстовых
процессов в теле цикла — у 39 стражей из 85; первые в этом списке
(`doc-conventions` 17, `doc-truth` 14, `doc-examples` 11) те же, что первые
по времени.

---

## Ф.1. Снять процесс-на-элемент у пяти носителей

Пять, потому что замер называет ровно их; шестого добавляем только по замеру
(Г7: единообразие — не довод).

| носитель | сейчас | цель | приём |
|---|---|---|---|
| `check-retired-names` | 56 с | ≤ 8 с | одна альтернация вместо 14 проходов по строке |
| `check-doc-truth` | 45 с | ≤ 6 с | тело цикла в python; `--help` кэшируется как сейчас |
| `check-doc-conventions` | 42 с | ≤ 14 с | 17 процессов в 12 циклах — в один проход |
| `check-no-machine-paths` | 29 с | ≤ 6 с | то же |
| `check-guard-wiring` | 22 с | ≤ 6 с | то же |

**Приёмка каждого носителя — три пункта, и первый обязателен (Г6):**

1. **Вердикт сверен байт-в-байт** на ЖИВОМ дереве: вывод старой и новой
   редакции совпадает дословно (`diff`), а не «выглядит так же». За 25
   переписанных стражей у автора конвенции такая сверка поймала четыре
   расхождения, каждое выглядело безобидным.
2. **Самотест зелёный** и в нём не убавилось случаев.
3. **Время до и после — числом в коммите** (Г1). Обещание без замера не
   принимается.

**Ожидание по сумме:** 250–300 с из 1401, то есть 23.4 → ~19 минут. Это
оценка; закрывается замером, а не ею.

### Ф.1 ЗАКРЫТА 2026-08-22 — замер вместо ожидания

| носитель | было | цель | стало |
|---|---|---|---|
| `check-doc-truth` | 45 с | ≤ 6 | **1.2 с** |
| `check-guard-wiring` | 22 с | ≤ 6 | **0.4 с** |
| `check-no-machine-paths` | 29 с | ≤ 6 | **0.6 с** |
| `check-doc-conventions` | 42 с | ≤ 14 | **7.6 с** |
| `check-retired-names` | 56 с | ≤ 8 | **5.8 с** |

Вердикт каждого сверен байт-в-байт на ЖИВОМ дереве, самотесты зелёные, ни
одна база не тронута.

**Виновник во всех пяти оказался один, и это не язык:** форк процесса внутри
цикла по файлам (чаще всего `basename`) — на этой машине один форк стоит
50–300 мс. Самый медленный из пяти (`check-retired-names`) БЫЛ УЖЕ НА ПИТОНЕ,
что и опровергает «переписать на питон» как рычаг: рычаг — убрать
процесс/регекс на элемент.

**Отдельная находка, которая шире плана:** `check-retired-names` не дотягивал
до цели не логикой, а периметром — стражи обходили **540 сгенерированных `.c`
на 122.9 МБ** (git отслеживает 20), то есть вывод сборки `nova test`. Периметр
сужен до того, что git считает текстом проекта (`ls-files --cached --others
--exclude-standard`, ОДИН процесс), и это дало 56 → 33 с; файловый отсев
альтернацией до строчного цикла — 33 → 5.8 с. Оба числа замерены порознь,
чтобы было видно, какая идея заплатила.

---

## Ф.2. Ярусы и бюджет

Гейт делится по Г4: `loop` (только текст, цель ≤ 60 с) / `push` (поведение) /
`full` (мутации, полный корпус). Механизмы у окна 274 уже есть
(`run-guards.py`, `check-gate-budget.py` + `gate-budget.baseline`) — берём их
формой, а не переписываем.

**Приёмка:** `loop` укладывается в бюджет на эталонной машине; число бюджета
снято ОДНИМ калибровочным прогоном и записано (угаданное число писать
нельзя); шаги, гоняющие компилятор, в `loop` не попадают по построению.

### Ф.2 ЗАКРЫТА 2026-08-23 вместе с Ф.3 (слияния `4175c978f`, `dde60dbc2`)

Ярусы и бюджет неразделимы на практике: механизм бюджета уже стоял в гейте,
не хватало файла чисел, и страж над ним краснел.

* **Ярусы** — накопительные (loop ⊂ push ⊂ full), умолчание `full` не изменилось
  (доказано `diff`'ом состава сухого прогона: ни один шаг не убран).
* **Шаги с компилятором в `loop` не попадают по построению:** ярус объявляется
  ПЕРВЫМ аргументом `step`, и шаг без яруса роняет гейт сразу — умолчания нет
  намеренно (иначе новый шаг, написанный по образцу соседней строки, тихо
  попадает не в тот ярус).
* **Бюджет** — `scripts/guards/gate-budget.baseline` на три яруса, и файл ГОВОРИТ,
  какое число какого рода: `loop 240` — прямой замер, `push 1300` — замер на CI
  (1120с), `full 1900` — ВЫВЕДЕНО и так и помечено (ГТ14), ярус на CI не гонялся.
* **Цель ≤ 60с для `loop`:** на этой машине НЕ достигнута (197–224с), на
  раннере CI — **11с**. Разница целиком в цене запуска процессов на Windows.
  Цель остаётся открытой как вторая волна Ф.1 и НЕ блокирует приёмку:
  время размазано по 59 шагам (самый дорогой 15с), обвала от одной правки нет.

---

## Ф.3. Внешний вызывающий (Г8) — главная цель плана

**Замер:** стражей 81, под внешним судьёй **25**, без внешнего вызывающего
**56**. CI зовёт `gate-novac.sh` (гейт окна 274) и НЕ зовёт `scripts/gate.sh`.

Отсюда цена, измеренная на этой же неделе: гейт не гонялся с фикса №720, и в
нём накопились четыре красных, из которых три не мои (arch-ratchet +140 строк
`emit_c` шестью коммитами, накопление веток, `fixed-but-open` №381).

**Приёмка:** CI зовёт `loop`+`push` на каждый пуш; `guard-callers.baseline`
опускается с 56 и дальше только вниз.

### СДЕЛАНО 2026-08-23 (слияние `4175c978f`) — приёмка закрыта

**Главное число: стражей без внешнего вызывающего 56 → 0** (82 из 82 под
внешним судьёй). Из них 53 сняты тем, что CI теперь ЗАПУСКАЕТ
`scripts/gate.sh`; оставшиеся трое получили шаг в `loop`.

**Ярусы** (ГТ4, накопительные: loop ⊂ push ⊂ full; умолчание `full`
не изменилось — доказано `diff`'ом состава сухого прогона: ни один
шаг не убран, пять добавлено). В `nova-gate.yml` два job'а: `gate-loop`
(без тулчейна вовсе) и `gate-push`.

**Цена молчания предъявлена ПЕРВЫМ ЖЕ прогоном троих — тезис
плана подтвердился не доводом, а состоянием дерева:**

* `check-lsp-launch-path` — КРАСНЫЙ. Сравнивал **написание** пути,
  а не путь: `cd && pwd -P` нормализует только СУЩЕСТВУЮЩИЙ каталог,
  а на дереве без `target/` (каждый чекаут CI, каждый свежий worktree)
  стороны оставались `D:/.../nova-lsp/../target` против `/d/.../target`.
  Зелёным он был только на уже собранном диске — тот же класс, что
  CRLF-базы. Починен, четвёртый случай самопроверки (POS2) судит фикс.
* `check-repo-root-clean` — красный честно: `.claude/` стал ему виден, как
  только `settings.json` взяли под версию. Разрешён с причиной рядом.

**Бюджет.** Механизм в гейте был, файла чисел — нет, поэтому страж
над ним краснел. `gate-budget.baseline` заведён на три яруса и ГОВОРИТ,
какое число какого рода: `loop 240` — прямой замер (177с чистый,
151с и 194с до него) с третью запаса; `push 1000` и `full 1500` — **ВЫВЕДЕНЫ**
из пошагового профиля и так и помечены (ГТ14), до первого замера яруса
на железе CI.

**ЗАМЕР ЯРУСА loop:** `GATE OK (final)`, 177с при потолке 240с, 59 шагов,
нуль красных (на main после слияния — 193с при калибровке 2x).

**ЦЕЛЬ 60с НЕ ДОСТИГНУТА и остаётся открытой.** Почему не закрыта
здесь: время яруса размазано по 59 шагам — самый дорогой 15с, дальше
12, 11, 10, 8, 7, 7, 6, 5; ни одного носителя, снятие которого дало бы
обвал. Значит это не одна правка, а вторая волна Ф.1 по десятку
стражей — отдельная работа, и она НЕ блокирует приёмку Ф.3:
приёмка была про вызывающего, а не про секунды.

**Что замечено по дороге — СДЕЛАНО 2026-09-07 (`4179ae266`, окно 283).**
Было: предупреждение о dev-override печаталось и на ярусе `loop`, который пакеты
вообще не резолвит. Точнее, чем «шум»: баннер утверждает «прогон НЕ доказывает,
что дерево собирается на чистом checkout'е», а `loop` этого и не утверждает: ярус
текстовый и в CI идёт без тулчейна вовсе. Предупреждение опровергало утверждение,
которого никто не делал.

Стало: `loop` получает ОДНУ строку — факт и причина, почему override на его
вердикт не влияет; `push`/`full` — прежний баннер ДВАЖДЫ. Молчание было бы
ОБРАТНОЙ ложью: читатель вердикта не узнал бы, что override в дереве есть.
Приписка `[DEV-OVERRIDE ACTIVE]` у строки вердикта живёт по тому же правилу: на
`loop` это было то же ложное утверждение, только короче.

Проверено В ОБЕ СТОРОНЫ временным override-файлом, без компилятора: `loop` —
одна строка и вердикт без приписки; `push` — баннер в начале и у вердикта; без
override — тишина на обоих ярусах (греп даёт 0). ПОСТОЯННОГО САМОТЕСТА НЕТ,
и причина названа, а не пропущена: проверка требует создать gitignored-файл
В КОРНЕ репозитория во время возможного прогона гейта, а падение между созданием и
удалением оставит его следующему прогону и стражу `check-repo-root-clean`.

---

## Ф.4. Пакетные репы: судьи нет вовсе

**Замер 2026-08-21:** у `nova-http`, `nova-tls`, `nova-polaris`,
`nova-compress`, `nova-bignum`, `nova-socks` — по ОДНОМУ стражу
(`check-invariant-discipline.sh`), **ноль workflow, ноль гейта**. Их не судит
ничто.

**Приёмка:** минимальный workflow на репу (свой страж + `nova test src`),
зелёный на текущем HEAD каждой.

### Ф.4 ЗАКРЫТА 2026-08-23 — шесть гейтов, все зелёные с первого прогона

`pkg-gate.yml` в каждой из шести реп (`nova-http@93ef52a`, `nova-tls@8ceb341`,
`nova-polaris@8270f8f`, `nova-compress@1fa8a3a`, `nova-bignum@2bdc413`,
`nova-socks@1ae5ca1` — форма `репа@хеш` по doc-conventions §«Ссылки наружу»:
хеш соседней репы недостижим из нашего `main` и голым писаться не должен).
Job собирает компилятор из `main` nova, гоняет ВСЕ
`scripts/guards/check-*.sh` репы (глобом, а не списком — страж, добавленный
завтра, запустится завтра без правки), сверяет тег на HEAD с версией
манифеста и запускает `nova test src`. Если `nova.override.toml` вдруг доедет
до CI — job падает явно.

**Что Ф.4 вскрыл в первый же вечер (реестр №749):** с убранным override замок
`nova-http` давал 4 CODEGEN-FAIL, а замок `nova-polaris` — полный отказ: пин
`compress 0.1.1` НЕ КОМПИЛИРУЕТСЯ нынешним компилятором. Локально этого
не видно НИКОГДА: каждый прогон резолвит через override на соседние HEAD.
Тот же корень уронил флагман nova в этот же день. Оба замка обновлены,
polaris выпущен `v0.1.5` (тег v0.1.4 стоял над манифестом с другим номером).

**ЗАМЕР ПЕРЕД ПУШЕМ** (те же три шага локально, override убран):
http 4/0, tls 1/0, polaris 48/0, compress 1/0, bignum 7/0, socks 1/0; стражи зелёные
везде. На CI все шесть — `pkg-gate completed success`.

---

## Ф.5. Двойной прогон конформанса — вопрос, а не работа

Мега-CU (210 с, 828 тестов) и `conformance --full` (224 с, 854 теста) — 434 с,
31% гейта, и второй включает первый. №732 объясняет, почему там `--full`, а не
перечисление лейнов (перечень отстаёт от корпуса). Вопрос: можно ли оставить
ОДИН прогон `--full` и снять мега-CU, или лейны расходятся по флагам сборки.

**Это не правка, а разбор**: решение владельца, если ответ меняет то, что
считается авторитетным вердиктом.

### Ф.5 РАЗОБРАНА 2026-08-23: НЕ ДУБЛИРОВАНИЕ, снимать нечего

Ответ дал первый же прогон яруса `push` на CI, а не рассуждение: два прогона
ловят РАЗНОЕ и по отдельности не заменяют друг друга.

* **Мега-CU** (`mega-CU exit=0 :: PASS: 840 FAIL: 0 SKIP: 90`) проверяет то, чего
  не проверяет ничто другое: что весь корпус уживается В ОДНОЙ единице
  компиляции — столкновения имён, мономорфизация, общие символы.
* **`conformance --full`** (`PASS: 864 FAIL: 3 SKIP: 57`) умеет лейны, которые в общий
  CU не склеишь по природе: panic / exit / timeout, где программа ОБЯЗАНА упасть
  (в одной единице падение убило бы остальные 828).

Именно этот лейн нашёл №745 — две фикстуры, красные на Linux и зелёные на
Windows, и через них дефект рантайма, который мега-CU не увидел бы никогда.
Снять любой из двух значит потерять покрытие, а не сэкономить 434с.

**Фаза закрывается без работы.** Экономить надо не здесь: самотесты стражей
стоят 365с (26% гейта) и живут в ярусе `full`, который CI не зовёт вовсе.

---

## Что НЕ делаем и почему

* **Не переписываем shell ради единообразия** (Г7). Довод — замер; у 34 из 88
  самотестов и у большинства мелких стражей его нет.
* **Не режем корпус** ради секунд: Г1-случай автора конвенции ровно об этом —
  профиль показал, что корпус стоил секунды, а время уходило в процессы.
* **Не поднимаем базы храповиков** вместо работы. Каждая база в этом плане
  может двигаться только вниз.

## Ф.6. Вердикт называет дерево, на котором снят (окно 283, 2026-09-07)

**ПРИНЯТА 2026-09-07, машиной проверено всё, кроме одного места, названного ниже.**
Работа пришла не из плана, а из вечера с ДВУМЯ носителями одного класса, и дом ей
назначил интегратор — сюда, потому что область плана есть ПРИМЕНЕНИЕ конвенции к
`scripts/gate.sh` и стражам дерева, а не только его цена.

**Класс:** вердикт, снятый не с того дерева, читается точно так же, как снятый с
нужного. Носители: окно 283 прогнало `check-doc-conventions` и `arch-ratchet` из
`main` вместо своей ветки (рабочий каталог оболочки сбросился, пути были
относительные) — числа СОШЛИСЬ с верными, то есть дырявый метод сам себя спрятал;
`check-commit-refs` у интегратора дал 819 локально против 844 на CI, потому что у
него достижимы ветки соседей. Ни один случай не выглядел ошибкой: ни отказа, ни
диффа, только другое число.

**Сделано:**

* `a4a8a6078` — `scripts/gate.sh` печатает `tree=/head=/branch=` в шапке и в ОБЕИХ
  итоговых строках, зелёной и красной. `GATE OK (final)` оставлен ПРЕФИКСОМ: строку
  грепают люди, планы и окно-интегратора, идентичность едет хвостом рядом с
  `$CI_TAIL`/`$TIER_TAIL`;
* `cbc00a39b` — самотест на механизм: строка вывода — самая удаляемая вещь в скрипте;
* `e0b82d169` — то же в `scripts/gate-novac.sh`, по слову интегратора: файл-вердикт
  читает СКРИПТ, строку в терминале читает ЧЕЛОВЕК. Хвост получили ЧЕТЫРЕ
  завершающих выхода, включая `BLOCKED` — та строка сообщает, что бинарь оракула и
  заголовки рантайма из РАЗНЫХ деревьев (№693), и вердикт про разные деревья обязан
  называть дерево;
* `b244a6b16` — в самотесте снят последний счёт-снимок: критерий сформулирован
  СВОЙСТВОМ («строка без хвоста законна, только если принимает `$1`, то есть
  сообщает об одном пункте»), а не числом. Число покраснело бы на законном удалении
  вердикта и ничего не поймало бы при добавлении.

**Доказательства (способом, который завёл сам гейт).** Сухой ход существует как
проба того, что список шагов не поехал: `NOVA_GATE_DRYRUN=1` по ярусам loop и push,
до и после, с вырезанными отметками секунд — единственные различия суть новая строка
шапки и хвост в итоговой. `bash -n` чист. Красный путь: проба вынимает `gate_barrier`
ИЗ файла (не переписывает рукой) и гоняет под `set -u` — с хвостом `rc=1` и хвост в
строке, без переменной `TREE_TAIL: unbound variable`. Второй случай не украшение:
незаданный хвост убил бы гейт РОВНО на отказе, поэтому присваивание стоит на строке
123, первый рубеж на 977, и порядок проверяется ассертом.

**ЧТО НЕ ПРОВЕРЕНО МАШИНОЙ И ПОЧЕМУ — названо, а не скрыто:** живой вывод
novac-гейта. У него нет сухого хода, а живой прогон зовёт компилятор, который был
занят чужим гейтом; там структура плюс проба строкой из файла. Проверяется первым же
прогоном novac-яруса.

## Ф.7. Номер правила конвенции принадлежит одному правилу (окно 283, 2026-09-07)

**ПРИНЯТА 2026-09-07.** Находка интегратора при проверке вопроса окна 283; факты
сверены окном заново перед правкой.

`docs/dev/gate-guard-conventions.md` объявлял СЕМНАДЦАТЬ заголовков на шестнадцать
номеров: `## Г14` стоял дважды (строки 160 и 311) и прожил так две недели. Ссылка на
«Г14» означала РАЗНОЕ в зависимости от того, кто читал, и за один вечер три сессии
сослались на Г-номера. Дубль не виден ни одному читателю: каждый видит СВОЁ правило.

* `03696f8ab` — номер оставлен правилу про КЭШ, потому что так решили ССЫЛКИ, а не
  порядок в файле: снаружи под Г14 понимают его (`integrator-handoff.md`, фикстура
  `a4_value_sum_pos.nv`, три внутренние ссылки — пять мест), а на правило про среду
  ссылалось ОДНО место (`gate-budget.baseline:26`), и оно переехало на Г17 той же
  правкой. Заодно сняты три ссылки в пустоту (`ГТ1`/`ГТ10`/`ГТ14` — семьи «ГТ» в
  дереве нет вовсе) и убраны диапазоны: заголовок этого плана обещал «Г1–Г14»,
  вступление конвенции — «Г1..Г13», а правил шестнадцать. Форма «Г-правила» не
  устаревает, «Г1–Г16» устарело бы при следующем правиле;
* `cb66de6ec` — страж `check-convention-rule-numbers` + самотест (5/5), шаг яруса
  loop, строка на странице правил. Страж проверен на НАСТОЯЩЕМ дефекте: файл из
  коммита до правки даёт «номер Г14 занят 2 раз — строки 160, 311», код 1. Отдельный
  случай самотеста требует, чтобы страж КРАСНЕЛ, не найдя ни одного документа с
  правилами: проверка, которой нечего судить, молчит так же, как исправная;
* `996e5639a` — `.gitattributes` крепил окончания строк у `*.sh` и не крепил у
  `*.py`, а python-стражей 99. Замер: `core.autocrlf = true`, `check-attr` даёт для
  shell-стража `eol: lf`, для python-стража `unspecified`. Следующий чекаут покраснил
  бы `check-guard-honesty` разом на 99 файлах — по ОКРУЖЕНИЮ, а не по существу.

* `123e68186` — замер КЛАССА после починки носителя: под `scripts/` и `.claude/`
  незакреплённых было 48, и среди них `scripts/githooks/*` — шелл-скрипты БЕЗ
  расширения, исполняемые git'ом на каждом коммите (правило ключевалось по `.sh`).
  Закреплены хуки, `*.awk`, данные стражей `*.allow`/`*.cutover`/`*.exempt` и
  `.claude/*.list`; проза, `*.ps1`, `settings.json`, `*.gdb` и `*.c`-опыты оставлены
  намеренно;
* `079d7563b` — страж `check-script-eol-pinned` + самотест (6/6), ярус loop.
  Форма задана интегратором и НЕ храповиком: «закреплено всё, кроме перечисленных
  КЛАССОВ исключений с причиной». Число «не больше 36» он отверг моим же доводом
  дня — критерий формулируется свойством; и добавил довод сильнее: счёт пропустил бы
  ПОДМЕНУ (сняли крепление с хуков, добавили исключённый `.md`, сумма сошлась).
  Проверен на настоящем состоянии до правки: 136 незакреплённых, включая хуки
  среды. В нём же ассерт «git ответил про столько же путей, сколько спросили»: мой
  первый замер этого класса дал «591 из 591», потому что текстовый режим Python
  дописал `\r` к каждому пути — замер закреплений окончаний строк оказался испорчен
  окончаниями строк, и поймало это только РАСХОЖДЕНИЕ двух замеров.

**Чего механизм НЕ покрывает, названо в шапке стража:** ссылки на несуществующие
правила и диапазоны в прозе. Обе половины требуют способа отличить живую ссылку от
ИСТОРИЧЕСКОЙ цитаты, а его нет; молчаливый запрет цитировать историю хуже незакрытой
половины класса.

## Ф.8. Ярус `loop` за 60 секунд (волна ЗАПЛАНИРОВАНА 2026-09-07, не начата)

**Откуда волна.** Ф.3 закрыта с прямо названным долгом: «ЦЕЛЬ 60с НЕ ДОСТИГНУТА и
остаётся открытой», 177 с при потолке 240 с, и время размазано по шагам — самый
дорогой 15 с, дальше 12, 11, 10, 8. Ни одного носителя, снятие которого дало бы
обвал. Волна пишется ДО работы, потому что цель есть, а приёмки у неё нет:
«стало быстрее» — это ощущение, а не вердикт.

**Замер, с которого волна начнётся (Ф.8.0).** Профиль яруса ПО ШАГАМ на НЕЗАНЯТОЙ
машине, дважды, с печатью дерева и коммита в той же команде; результат кладётся
файлом в `docs/plans/repro/p275/loop-profile-<дата>.txt`. Два прогона нужны не для
красоты: разброс валл-клока на этой машине измерен и достигал двукратного
(147–294 с за один день, реестр №558/№475), поэтому один прогон профилем не
считается.

**Приёмка волны — машинная:**

1. `NOVA_GATE_TIER=loop bash scripts/gate.sh` печатает `GATE OK (final)` и укладывается
   в **60 с при калибровке 1x** — на незанятой машине, дважды подряд, оба числа в
   докладе;
2. `scripts/guards/gate-budget.baseline` опущен до нового потолка ТОЙ ЖЕ правкой, с
   летописью: какое число замерено, а какое выведено (правило базы);
3. состав шагов НЕ изменился: `NOVA_GATE_DRYRUN=1` до и после, `diff` с вырезанными
   отметками секунд — пусто. Шаг, ушедший из яруса, обязан быть назван отдельно и
   обоснован по Г4, а не спрятан в общий выигрыш;
4. самотесты стражей, которых волна касается, остаются зелёными, и у каждого
   ускоренного стража есть его собственный вердикт `ok:` в логе (иначе ускорен не
   страж, а его молчание).

**Не делаем (границы).**

* **Не снимаем шаги ради секунд.** Снятый шаг — это не ускорение, а неудержанный
  класс; если шаг лишний, он снимается отдельным доводом и отдельным коммитом, а не
  внутри волны про время.
* **Не переносим шаги в `push` ради зелёного `loop`.** Перенос — это перемещение
  цены, а не её снятие; законен, только если шаг ПО Г4 принадлежит `push` (нужен
  собранный компилятор или поведение), и тогда это тоже отдельный довод.
* **Не трогаем novac-гейт и его ярусы** — у него свой владелец и свой профиль.
* **Не оптимизируем то, чего не измерили.** Носитель берётся из профиля Ф.8.0, а не
  из впечатления «этот страж выглядит медленным» (Г1).
* **Не поднимаем потолок вместо работы.** База опускается или остаётся; рост базы в
  этой волне — не результат, а отказ от неё.

**Открытые вопросы.**

1. **Цель 60 с — всё ещё верное число?** Адресат: владелец. Умолчание, если ответа
   нет: держим 60 с, потому что число названо в плане и не отменялось. Срок: до
   начала Ф.8.1 (первой правки). Довод для пересмотра: с 2026-08-23 ярус вырос с 59
   шагов до 89, и часть роста — механизмы, заведённые сознательно (в том числе два
   моих), то есть цель ставилась к другому составу.
2. **Считать ли калибровку машины частью цели?** Адресат: интегратор. Умолчание:
   цель проверяется при калибровке 1x, а на калиброванной машине сравнивается
   `время / множитель` — так число сравнимо между машинами. Срок: тот же.

**Почему волна не начата сегодня:** замер требует НЕЗАНЯТОЙ машины, а она под
авторитетным гейтом интегратора. Запускать профиль параллельно значит мерить
окружение вместо предмета — ровно тот класс, который эта же волна и обслуживает.

## Ф.9. РЕВИЗИЯ СЛОЯ СТРАЖЕЙ (заказ владельца 2026-09-16, ревизия проведена, работа не начата)

**Вопрос владельца дословно:** «по стражам надо провести ревизию, зачем столько
нам, возможно там 5 стражей нужно оставить» — и, отдельно, «проверь их
необходимость ТЕПЕРЬ», когда оракул в релиз не выходит.

### Замер: 215 стражей, и раскладка не та, что ожидалась

| слой, который страж ЧИТАЕТ | стражей | судьба при отставке оракула |
|---|---|---|
| novac | 61 | **остаются** — это Карина |
| доки и планы | 46 | процесс, разбирать по одному |
| процесс (стражи, гейт, базы) | 40 | стражи о стражах |
| оракул | 26 | **см. довод ниже: оставить до самосборки** |
| рантайм | 15 | **остаются** — `nova_rt` уезжает с Кариной |
| тесты | 12 | остаются |
| std | 8 | **остаются** — `std` уезжает с Кариной |
| спека | 5 | остаются |

**Сирот нет: все 215 кто-то зовёт.** Первый замер дал 16 «не зовущихся
ниоткуда» — он не считал вызовы из самих стражей и из git-хуков; «сироты»
оказались сканерами, которых зовёт их же страж, и `check-tree-matches-push`,
живущим в pre-push. Дешёвого критерия «отсечь неподключённое» НЕТ.

### ГЛАВНАЯ РЕКОМЕНДАЦИЯ: сокращать не ПРАВИЛА, а ПРОХОДЫ

**Замер, ради которого стоило считать:**

| предмет | сколько стражей его читают |
|---|---|
| `docs/plans/*.md` | **85** |
| `novac/src` (93 файла) | **53** |
| реестр дефектов (4.5 МБ) | **33** |
| `spec_tests` | 15 |
| `compiler-codegen/src` | 15 |
| `std/src` | 14 |
| `nova_rt` | 13 |

Пятьдесят три независимых обхода одного каталога из 93 файлов и тридцать три
чтения одного файла на 4.5 МБ — вот где живёт цена яруса, а не в числе правил.
**Сократив 215 правил до 5, мы потеряем 210 проверок и почти не выиграем
времени; сведя 53 обхода в один проход с 53 правилами, выиграем время и не
потеряем ничего.**

### Почему массовое сокращение опасно: почти каждый несёт ИЗМЕРЕННЫЙ случай

Стражи заведены не по идее, а по происшествию. Выборка из прочитанных шапок:

* `check-sigsegv-altstack` — обработчик переполнения стека был единственным, до
  кого переполнение не могло достучаться; core dump на 140 ГБ забивал диск;
* `check-rt-sigpipe-ign` — без `SIG_IGN` сетевой процесс на Linux умирает
  сигналом 13 без следа;
* `check-single-mco-resume` — четыре дублирующих resume-сайта, два несли двойное
  освобождение через гонку;
* `check-tree-matches-push` — гейт зелёный, ушло 92 коммита, а правка чекера и
  целый страж остались незакоммиченными (он же остановил пуш 2026-09-15);
* `check-guard-empty-root` — **9 стражей из 10 печатали зелёный ноль на пустом
  каркасе**; инвентарь того же дня: 44 из 82 не проверяли мишень вовсе;
* `check-guard-external-caller` — **48 из 120 стражей не имели внешнего
  вызывающего**;
* `check-diag-fixture-coverage` — **210 диагностических кодов из 421 без единой
  негативной фикстуры**.

Последние три говорят главное: слой не «разросся», он **догоняет накопленный
долг**. Отсекать в нём — значит сознательно принимать возврат конкретного
случая, поэтому у каждого «снять» ниже написано, ЧТО ИМЕННО вернётся.

### Довод об оракуле: он не мёртв, он не ВЫПУСКАЕТСЯ

Это разные вещи. Пока Карина не собирает себя сама — сегодня её держат 263
пробела собственного подмножества в четырёх файлах чекера — **оракул остаётся
инструментом, которым её строят**. Снять стражей с его чекера и кодогена сейчас
значит убрать защиту с инструмента ровно в тот период, когда он нужен исправным
больше всего. Событие, после которого 26 «оракульных» стражей можно пересмотреть,
названо машинно: **самосборка novac зелёная**.

### Предмет не исчезает, а ПЕРЕЕЗЖАЕТ — категория, которой в ревизии не было

**Уточнение владельца 2026-09-16: «nova-cli, nova-lsp должен замениться novac в
целевой схеме».** Это меняет разбор: инструменты вокруг оракула не умирают
вместе с ним — их РОЛЬ переходит к Карине.

**Замер:** `nova-cli` упоминают **29** стражей, `nova-lsp` — **2**, оба — **7**,
всего затронуто **38**. Но подавляющее большинство зовут `nova-cli/target/
release/nova` как БИНАРЬ, которым запускают проверку (`check-novac-differential`,
`check-std-test-baseline`, `check-novac-lint`), а не судят его как предмет. Их
правка при замене — смена пути к инструменту, не отставка.

**Предметом эти двое являются лишь у немногих:** `check-cli-output-language`,
`check-lsp-launch-path`, `check-checker-entrypoints`, `check-crate-tests`,
`check-crate-test-coverage`.

**И вот в чём разница, ради которой заведена категория.** Страж, чей предмет
ИСЧЕЗ, снимается — проверять нечего. Страж, чей предмет ПЕРЕЕЗЖАЕТ, снимать
нельзя: требование остаётся в силе, меняется лишь адрес. Снятый такой страж
означает, что новый носитель приобретёт тот же дефект заново — и на этот раз
без свидетеля. Правило «кириллицы в поставляемом выводе нет» одинаково нужно
CLI оракула и CLI Карины; то же с путём запуска LSP и с тестами крейтов.

**Отсюда работа, а не отсечение:** у пяти названных стражей предмет должен быть
переписан с пути на СВОЙСТВО («поставляемый вывод», «бинарь, который ищет
редактор»), чтобы при замене инструмента страж переехал вместе с ролью, а не
остался судить мёртвый путь. Сегодня они прибиты к именам каталогов — и это
тот же класс, что «охват правила задан рукописным списком имён» (№1112).

### Рекомендации по решениям

**1. СВЕСТИ — главная работа, выигрыш измерим:**

* **семейство реестра** (`registry-rows-intact`, `registry-entry-shape`,
  `registry-routes`, `registry-row-closed`, `registry-single-verdict`,
  `registry-past-tense-status`, `registry-closure-kept`, `fixed-but-open`) —
  восемь стражей читают один файл 4.5 МБ по восемь раз. Свести в ОДИН проход с
  восемью правилами. Правила сохраняются все;
* **семейство `novac/src`** — 53 обхода 93 файлов. Свести в один обход с
  раздачей строк правилам. Это же снимает класс «страж слеп к переезду на
  python» (F119) — разбор общий;
* **семейство снятого** (`check-retired-names`, `check-spec-retired-names`,
  `check-retracted-param-form`, `check-retracted-try-semantics`) — четыре стража
  об одном предмете «снятое имя/форма всё ещё упоминается». Свести в один со
  списком снятого и зонами.

**2. СНЯТЬ — и что вернётся:**

* ~~`check-cli-output-language`~~ — **РЕКОМЕНДАЦИЯ ОТОЗВАНА 2026-09-16 после
  уточнения владельца** (см. раздел «Предмет переезжает» ниже): `nova-cli` в
  целевой схеме заменяется Кариной, то есть требование «в поставляемом выводе
  нет кириллицы» не исчезает, а ПЕРЕХОДИТ к её CLI. Снять сейчас значит дать
  её выводу приобрести тот же дефект заново;
* `check-oracle-nesting-depth` — заведён со словом «закрыть класс ДО ТЕГА»;
  тега не будет. **Вернётся:** `thread has overflowed its stack` без
  диагностики на глубоко вложенном входе — у разработчика, не у пользователя.

**Больше кандидатов на снятие я не нашёл, и это честный итог ревизии, а не
уклонение.** Остальные 213 либо судят то, что уезжает с Кариной, либо защищают
инструмент до самосборки, либо держат дисциплину процесса, чей отказ уже
случался и измерен.

**3. ОСТАВИТЬ БЕЗУСЛОВНО (96):** 61 novac + 15 рантайм + 8 std + 12 тестов. Это
код и проверки, которые станут продуктом Карины.

### Не делаем (границы)

* **не отсекаем по числу** — «оставить пятерых» сокращает правила, а не цену;
* **не трогаем 26 «оракульных» до самосборки** — событие названо, срок не в днях;
* **не сводим семейства РАНЬШЕ, чем у сведённого стража появится самотест,
  доказывающий каждое из его правил** — иначе восемь доказанных правил
  превратятся в одно недоказанное.

### Открытые вопросы

* ~~поставляется ли `nova-cli` наружу~~ — **ОТВЕЧЕНО владельцем 2026-09-16 в тот
  же час:** `nova-cli` и `nova-lsp` в целевой схеме заменяются Кариной. Значит
  требования к поставляемому выводу и к пути запуска НЕ отменяются, а переходят
  к ней; стражи остаются, но их предмет переписывается с пути на свойство.
* **кому:** интегратор. **Вопрос:** порядок сведения — начинать с реестра (8
  стражей, один файл) или с `novac/src` (53 стража, 93 файла). **Умолчание:**
  с реестра: он меньше и его сведение проверяется быстрее. **Срок:** до начала.

### Чем эта ревизия ограничена

Предмет каждого стража взят из его ШАПКИ (выемка пятью агентами, sonnet),
а не из чтения тела. Значит страж, чья шапка расходится с кодом, описан здесь
неверно — и такой случай сегодня уже встретился: `check-gate-timeout-word`
числился как `.sh`, а существует как `.py`. Цена ошибки в эту сторону невелика
(неверное описание, не неверное решение), но она названа, а не скрыта.

**И ещё одна дыра, найденная агентом, а не мной:** список первого куска я
скопировал из обрезанного вывода, и девять стражей не попали в раздачу вовсе.
Агент пересчитал имена в задании и сказал «36, а не 43»; иначе девять
остались бы неописанными молча. Досланы отдельным заданием.

## Следы

* Профиль: `docs/plans/repro/p275/` (таблица шагов и разложение носителей).
* Конвенция: [gate-guard-conventions.md](../dev/gate-guard-conventions.md).
* Реестр: №740 (страж без вызывающего — тот же класс, другая ось).
* Ф.6/Ф.7 (окно 283, 2026-09-07): `a4a8a6078`, `cbc00a39b`, `e0b82d169`,
  `b244a6b16`, `03696f8ab`, `cb66de6ec`, `996e5639a`, `123e68186`, `079d7563b`.
  Строки реестра НЕТ и она
  не нужна: незакрытого остатка не осталось, а запись, рождённая закрытой,
  потом цитируется как долг (решение интегратора 2026-09-07).

## Ф.10. Свежий профиль и единственный путь к десятикратному ускорению (2026-09-16)

**Заказ владельца:** «профилируй гейт, очень долго, надо убыстрить в 10 раз».

### Профиль снят с ЖИВОГО прогона, а не пересчитан

Прежний профиль раздела 0 снят 2026-08-21 на **67 шагах**; сейчас их **114**, и
число 23.4 минуты держалось только потому, что никто не пересчитывал. Новый снят
без единого дополнительного прогона: гейт сам печатает `[ NNNs] == gate: ИМЯ ==`
перед каждым шагом, значит длительность шага есть разность соседних меток. Источник
— зелёный прогон яруса `push` на `2edf7afc7`, 2026-09-16, **1371 с = 22.9 мин**.

| с | доля | шаг |
|---|---|---|
| 334 | 24.4% | conformance-full (лейны panic/exit/timeout) |
| 308 | 22.5% | mega-CU (тот же корпус, одним compile unit) |
| 77 | 5.6% | nova test std/src |
| 69 | 5.0% | флагман `--strict-effects` |
| 67 | 4.9% | examples anti-rot |
| 55 | 4.0% | crate-tests (nova-lsp, nova-cli) |
| 45 | 3.3% | страж «принятая в реестре работа влита» (№594) |
| 44 | 3.2% | process-exit-under-pool (×200 запусков, №694) |

Верхние 22 шага дают 88% времени. **Двадцать два шага из 113 занимают меньше
секунды каждый** — то есть «стражей много» само по себе ценой не является, и это
третий замер подряд, подтверждающий вывод Ф.9 «меньше ПРОХОДОВ, а не правил».

### Раскладка по слоям — она и решает вопрос

Ярус каждого шага взят из самого `gate.sh` (`step loop|push|full`), длительность —
из меток прогона:

| слой | шагов | время | доля |
|---|---|---|---|
| `loop` — только чтение текста | 91 | **330 с** | 24% |
| `push` — поведение | 20 | **1033 с** | 76% |

**Отсюда потолок:** сколько ни ускоряй стражей, больше четверти не выиграть.
Десятикратное ускорение требует убрать ПОВЕДЕНИЕ, а поведение — это и есть
проверка. Значит вопрос не «как быстрее прогнать», а «когда его вообще надо
гонять».

### Число, которое даёт ответ

Замер по последним 299 коммитам `main`: **83% не трогают НИ ОДНОГО дерева, от
которого зависит поведение** (`compiler-codegen/`, `nova-cli/`, `nova-lsp/`, `std/`,
`spec_tests/`, `novac/`, `examples/`, `nova_tests/`) — это доки, планы, реестр,
стражи, `.claude`. Трогают код 17%, и внутри них чаще всего `novac/` (87
файло-касаний), который судит ОТДЕЛЬНЫЙ гейт `gate-novac.sh`.

### Предложение: ярус `push` становится ДИФФО-ЗАВИСИМЫМ

Поведенческий шаг исполняется, когда диффа `origin/main..HEAD` касается дерева,
от которого он зависит, и пропускается ИНАЧЕ — **с печатью того, что пропущено и
почему**, чтобы пропуск не выглядел зелёным прогоном.

- на 83% пушей: 1371 с → 330 с, а со стражами в параллель (слой текстовый и
  независимый) — **порядка 50-60 с, то есть больше чем в десять раз**, и при этом
  НИ ОДНА проверка не снята;
- на 17% пушей, трогающих код: честный потолок **около двух раз** — снять повтор
  позитивного лейна (`conformance-full` идёт с `--full` и повторяет то, что уже
  прогнал мега-CU; цена повтора названа в самом шаге, ~150 с) и наложить друг на
  друга независимые поведенческие шаги (std, examples, crate-tests, флагман — 312 с
  суммарно, все независимы от корпуса). Больше — только ценой покрытия, и это
  решение владельца, а не оптимизация.

**ЧЕГО ПРЕДЛОЖЕНИЕ НЕ ДЕЛАЕТ, названо прямо:** ярус `full` остаётся полным и
безусловным, и ночной прогон по-прежнему гоняет всё. Диффо-зависимость касается
ТОЛЬКО яруса `push`.

**Опасность класса, названная до работы:** «дифф не мог повлиять на X» — это
СУЖДЕНИЕ, и неверное суждение пропускает дефект. Поэтому карта «дерево → шаги»
обязана быть механической и умолчание у неё — ГОНЯТЬ: путь, не попавший ни в одну
строку карты, запускает всё. Страж на саму карту: файл верхнего уровня, которого
нет ни в одной строке, краснит гейт.

**Приёмка Ф.10, машинная:** (а) на коммите, трогающем только `docs/`, ярус `push`
печатает список пропущенных поведенческих шагов и укладывается в 90 с; (б) на
коммите, трогающем `compiler-codegen/src`, исполняются ВСЕ поведенческие шаги —
проверяется пробой «подсунь правку в компилятор, убедись, что мега-CU пошёл»;
(в) новый каталог верхнего уровня, не названный в карте, краснит гейт.
