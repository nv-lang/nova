# Plan 169.1.2 — Консолидация тестов по темам

> **Под-план [Plan 169.1](169.1-test-suite-profiling-and-speedup.md)** (ось «скорость»).
> **Создан:** 2026-06-19. **Статус:** 🟡 В РАБОТЕ (метод доказан на plan103_2).
> **Приоритет:** P1 — лучший ROI по CU/час.
> **Объединяет** бывшие 169.1.2 (by-theme) + 169.1.3 (partial-D) + 169.1.4 (panics) —
> это всё одна работа «склеить тесты», дробить не нужно (решение 2026-06-19).

## Цель

Сократить число compile-units (CU) тест-набора, склеивая отдельные тест-файлы
в **folder-module по ТЕМЕ** (atomics, sync, generics, syntax, effects, …), а не
по номеру плана. Базовый потолок: ~1314 → ~900 CU.

## Доказанный метод (plan103_2, 2026-06-19)

1. **Форма модуля** (из `scripts/catb_convert.py`): folder-module = `module nova_tests.<тема>`;
   negative = `module neg.<stem>` в `neg/`.
2. **Валидация — ТОЛЬКО канонически:** `nova test . --filter <тема>` из корня репо.
   Standalone `nova test nova_tests/<папка>` folder-module НЕ обрабатывает (root
   модуля считается иначе) — не использовать для проверки.
3. **Связь «тест ↔ план» через имя файла** (договорённость): при слиянии планов в
   `module nova_tests.<тема>` файлы именуются с префиксом плана —
   `plan103_2_atomic_i64.nv`, `plan103_1_ordering.nv`. Происхождение видно по
   имени файла; раннер печатает путь при падении → навигация цела. Имена файлов
   произвольны, на module-форму не влияют.
4. **Коллизии top-level имён между планами** (граница Plan 170):
   - `fn`/`const`/тип-без-методов → `priv(file)` (ноль rename, читаемо);
   - тип-С-методами → ordinal-rename (`Counter`→`Counter1`), т.к. метод-символ
     `Nova_<T>_static_<m>` не file-дискриминирован;
   - `fn main()` не уживаются (один entry/модуль) → main-based тесты сначала
     `main`→`test "…"`, иначе не сливать.
5. **Чинить fallout по ходу** (ось 169.2): свежий compiler-enforcement ломает
   позитивы (`E_LOCAL_NOT_MUT`: счётчики/флаги без `mut` → добавить `mut`, не
   выхолащивая). Склейка и починка идут вместе.

## Фазы

### Ф.1 — Позитивы по темам (основная, −239+ CU)
Таксономия тем (какой план в какую группу) + слияние позитивных тестов одной
темы в `module nova_tests.<тема>` (файлы с префиксом плана). priv(file)/rename
коллизий. Починка fallout (mut и т.п.). Валидация `nova test . --filter`.
Категории A (pos-only) + смешанные: negative выносится в `neg/` (`module neg.<stem>`),
позитивный остаток сливается в тему.

### Ф.2 — `panics`-клаузула + panic-тесты (−78 CU, компиляторная фича, рантайм-риск)
Контекстное KW `panics` (инверсия PASS/FAIL): `test "…" panics "паттерн" { … }`.
`nv_panic` (`effects.h:566`) при `_nova_test_frame` делает `longjmp` (не abort) —
panic уже ловится setjmp (`emit_c.rs:17218-17253`); вводим ожидание паники →
114 runtime-panic тестов в folder-module.
- parser+AST: `TestDecl { …, panics: Option<String> }`.
- codegen: при `panics.is_some()` инвертировать setjmp-ветки + `strstr(msg, pattern)`.
- миграция: `fn main`+`// EXPECT_RUNTIME_PANIC <pat>` → `test "<stem>" panics "<pat>"`.
- **ГЛАВНЫЙ РИСК — GC/fail-frame consistency между паниками в одном процессе**
  (`longjmp` пропускает деструкторы): `_nova_fail_top`, `_nova_current_handler_iframe`
  (чинился Plan 169.2 §16), GC-arena, scheduler. Стратегия: сначала ОДНА простая
  группа (contract/assert-паники, без concurrency) → если 5-10 паник сосуществуют,
  катить группами, concurrency-паники последними; если рассыпается → нужен
  `nova_runtime_reset()` между тестами (отд. инфра, тогда отложить). `EXPECT_RUNTIME_PANIC`
  остаётся для legacy `fn main` + селектора `--panic` (169.1.1).

## Прогресс

**Гигиена/инфра:**
- ✅ `negative/`→`neg/` convention fix (6 папок: plan101_1..4, plan103_1/2; `b8641225`).
- ✅ Подпланы 169.1.2/.3/.4 схлопнуты в один (`a3dd291e`+`03232b33`).

**Темы консолидированы (все в main, −51 CU позитивов):**
| Тема | Файлов | Из планов | CU |
|------|-------:|-----------|---:|
| `nova_tests.atomics` | 16 | plan103_1/2 | −15 |
| `nova_tests.sync` | 23 | plan103_3/4/5 | −22 |
| `nova_tests.generics` | 23 | plan101_1/2/3/4 | −18 |
| `nova_tests.unicode` | 43 | plan152_0..8 | −8 |
| `nova_tests.strings` | 85 | plan131/137/139/139_1/139_2/91_18/str_builder | −6 |

**Компилятор-фикс попутно:** `[M-blanket-method-resolve]` — blanket `fn[T] T @m` на
str/user-типах (§19 в [plan-169.2-compiler-fixes.md](../plan-169.2-compiler-fixes.md), `ca85c001`).

**Метод отлажен:** move-с-префиксом плана → `priv(file)` (fn/const/тип-без-методов) /
rename (тип/метод-коллизии) → fix-fallout (`mut` и т.п.) → валидация **path-args**
(`nova test nova_tests/<тема>`) → commit-per-задача → merge в main.

## Остаток (2026-06-20) — Level-1 исчерпан; Level-2 ДОСТУПЕН (не выполнен)

**ПОПРАВКА (2026-06-20):** прежний вывод «дешёвая консолидация исчерпана» относился
только к **Level-1** (склейка разрозненных файлов внутри папки → один модуль; почти везде
уже сделано, `mod=1`). **Level-2** (слияние уже-склеенных СИБЛИНГ-папок в одну тему —
как atomics/sync/generics) скан не считал, а он широко доступен (~**−55 CU**):
- **plan123** — 22 папки, все `mod=1`, без main/pkg/cross-import; только 2 тип-дубля
  с методами (Counter4/Inner4 → rename), остальное `priv(file)` → **−21 CU (джекпот)**.
- **plan124** — 8 папок, 0 cross-folder тип-коллизий → чистый `priv(file)`, **−7 CU**.
- plan118 (~−11), plan114 (−5), plan108 (−4), plan99 (−2), plan70 (−4, есть xi), plan59 (−1).
- НЕ для Level-2: plan100* (Transaction-методы, гейт 170.1), plan103 stress-остаток,
  plan91 PKG-пакеты, plan83 concurrency-stress.

Level-2 **НЕ выполнен**: сессия 2026-06-20 ушла в **169.2 fix-sweep** (см. ниже) —
скан вскрыл кластер молча-красных folder-module, их починка приоритетнее новой склейки.

### 169.2 fix-sweep (выполнено 2026-06-20, в main d7543606)
6 красных folder-module (EXPECT-негативы в позитивном корне + реальные баги):
plan59/plan70_3 (структурный вынос в `neg/`), plan62 (test-rot equal/into/PRELUDE_VERSION),
plan70_5 (codegen `uint.MAX`), plan138_2 (shadow-rename + 2 compiler-фикса P7/block-inference)
— ЗЕЛЁНЫЕ; plan55 root-caused (`[]fn` nova_int fallback, гейт 172 U.4). 3 compiler-бага
починены, 2 задокументированы. Детали — [plan-169.2-compiler-fixes.md](../plan-169.2-compiler-fixes.md) §20-23.

### Остаток консолидации (cross-resolve / method / main-based — by design не Level-2)
- **cross-resolve папки** (`modules` 29, `cfg` 8, `plan162` 8, `plan163` 4) — module-system
  тесты, peer'ы импортят друг друга → folder-module by design НЕ применим. НЕ консолидировать.
- **method-тяжёлые кластеры** (`consume`/plan100*, `plan147`, `plan140`, `plan154`, `plan114_4_1`)
  — типы с методами (Transaction 37 методов и т.п.); `priv(file)` не дискриминирует методы
  (граница 170), нужен **extensive rename ИЛИ Plan 170.1** (priv(file)-на-методах, отложен).
  ROI низкий (часть уже folder-module). **Гейт: Plan 170.1.**
- **main-based** (`plan67` 14, `expected_runtime` 22, `plan159`, `plan150`, `plan60`) — `fn main`
  тесты; сначала `main`→`test "…"` конвертация. Отдельная задача.
- **mostly-negative** (`plan103_8`, `plan160`) — негативы и так раздельны (каждый свой CU).

**Известные pre-existing neg-фейлы** (всплыли при консолидации, НЕ от неё — устаревшие
коды/правила): `E_PREFIX_SHADOWS_NAMED_TYPE`, neg `rotate_left` (`too few arguments`),
`E_UNSAFE_CALL_REQUIRES_WRAP`. → ось 169.2 (fix-sweep корректности), отдельно.

## Рабочий процесс (после каждой атомарной задачи)

1. **Закоммитить** выполненную задачу. Если за раз сделано несколько задач —
   **разбить на несколько коммитов** (по одной логической задаче на коммит).
2. **Синканись с main** (`D:\Sources\nv-lang\nova`) — bidirectional: подтянуть main
   в ветку И влить ветку в main (main — destination по этому пути).
3. **Обновить бинари** при необходимости (пересобрать `nova-cli` release, если
   слияние принесло compiler-изменения).

## Инструмент

`scripts/catb_convert.py` уже умеет positives→folder-module + EXPECT_*→neg/ +
ordinal-rename. Расширить: (а) by-theme (несколько планов → одна тема, префикс
файла), (б) priv(file) вместо ordinal-rename где можно.

## Followup-маркеры

`[M-169.1.2-consolidate]` (по темам), `[M-169.1.2-panics-clause]` (Ф.2 фича+риск).
Бывшие `[M-169.1.3-consolidate-partial-d]`, `[M-169.1.4-panics-clause]` свёрнуты сюда.
