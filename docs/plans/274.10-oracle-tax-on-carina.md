<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 274.10 — чего Карине стоит оракул: ломает сегодня · платит обходом · сломает при правильной

**Статус:** ОТКРЫТ 2026-09-16. **Ведёт:** интегратор.

> **ОДИН ДОМ, ТРИ ПРИЗНАКА ДОПУСКА — И ЭТО СВЕДЕНИЕ ДВУХ ФАЙЛОВ.** Сюда влит
> `221.3` (дефекты оракула, блокирующие релиз Карины, заведён 2026-09-09).
> **Довод замером, а не вкусом:** в 221.3 было шестнадцать номеров, в 274.10
> восемнадцать, **пересечение ПУСТОЕ**. Два файла отвечали на один вопрос — во
> что Карине обходятся наши дефекты — и не знали друг о друге ни на одну строку;
> именно так 2026-09-15 обнаружилось, что в 221.3 нет ни одного из восьми
> дефектов, которые она обходит каждый день.
>
> **Разница между ними была не в предмете, а в ПРИЗНАКЕ ДОПУСКА**, и все три
> признака теперь названы и живут рядом:
>
> 1. **ЛОМАЕТ СЕГОДНЯ** — судится ПРОГОНОМ четырёх исполнимых признаков;
> 2. **ПЛАТИТ ОБХОДОМ** — судится НОСИТЕЛЕМ: обход в её исходнике;
> 3. **СЛОМАЕТ ПРИ ПРАВИЛЬНОЙ КАРИНЕ** — судится СОБЫТИЕМ-будильником.
>
> **ХОЗЯИН — ИНТЕГРАТОР, А НЕ ОКНО 274.** Это дефекты ОРАКУЛА, чинятся в
> `compiler-codegen`, и окно, пишущее `novac`, их чинить не может и не должно.
> Без этой строки подплан под 274 читается как работа окна 274, и обе стороны
> будут ждать друг друга.

---
# ЧАСТЬ I. ЛОМАЕТ СЕГОДНЯ — признак судится ПРОГОНОМ

## Что сюда входит, а что нет

Оракул остаётся **инструментом**, которым Карина собирается и против которого
судится. Значит его дефекты делятся надвое, и делятся проверяемо.

**СЮДА — если дефект ломает хоть одно из четырёх:**

1. `nova build novac/src/main.nv` — сборку самой Карины;
2. дифференциал `novac` против оракула (побайтовое сравнение C);
3. корпусную эмиссию (`novac emit` по корпусу) либо её приёмку;
4. ярус `gate-novac.sh` — включая его ратчеты и бюджеты, потому что ярус,
   не способный дать зелёный вердикт, закрывает Карине приёмку целиком.

**НЕ СЮДА** — всё прочее по оракулу: заводится в [221.1](221.1-bug-sweep.md) и
ждёт. Тег v0.1 оракула не цель, и «дефект серьёзный» сам по себе основанием не
является.

**Признак проверяется, а не ощущается.** Прежде чем завести строку сюда, ответь
одной фразой: чем именно этот дефект мешает Карине. Нет ответа — значит 221.1.

## Когда сюда пишут — В ТОТ ЖЕ ХОД, ЧТО ЗАВОДЯТ СТРОКУ В РЕЕСТР

**Раздел выше говорит ЧТО сюда входит; этот — КОГДА, и без него первое не
работает.** Запись сюда делается ТЕМ ЖЕ ходом, которым строка заводится или
закрывается в [221.1](221.1-bug-sweep.md), а не «потом, когда дойдут руки».
Отдельным движением это забывается: **замер 2026-09-15 — шесть строк ночи
14/15 (№1105–№1110) сюда не попали**, вердикт по Карине писался прозой внутри
строк реестра, и вид отстал от реестра на сутки. Это записано не как упрёк
себе, а как причина правила.

**Три момента, и других нет:**

1. **Строка заведена в 221.1** → сразу ответь её же текстом на признак допуска
   («чем именно мешает Карине», по четырём исполнимым признакам выше) и, если
   проходит, поставь сюда номер и одну строку. Не проходит — ничего не пиши:
   молчание здесь значит «проверено и не подошло», и этого достаточно.
2. **Строка закрыта в 221.1** → сюда в тот же ход отметка о закрытии. Строка,
   стоящая тут открытой при слитом фиксе, врёт о расстоянии — так уже было с
   четырьмя блокирующими строками реестра.
3. **Перемер признаков** (любой прогон четырёх) → числа и вердикт сюда, с датой
   и временем прогона. Без даты вердикт неотличим от старого.

**ЧЕГО ЭТО ПРАВИЛО НЕ ДАЁТ, и это названо, чтобы не сочли закрытым:** оно
держит поток НОВЫХ строк, но не разбирает НАКОПЛЕННОЕ. Сверка уже открытых
блокеров тега против четырёх признаков — работа отдельная, читательская, и
исполнимой меры у неё нет: прогон отвечает на «ломается ли сегодня», а не на
«сломает ли при правильной Карине». Путать эти два вопроса — подменять трудный
лёгким; подмена особенно опасна тем, что приходит с настоящим замером.

## Где живут сами строки

**Строки остаются в реестре [221.1](221.1-bug-sweep.md)** — он один, у него есть
стражи формы, уникальности номеров и целостности (`check-registry-entry-shape`,
`check-registry-rows-intact`, `check-registry-routes`), и второй реестр разошёлся бы
с ним на первой правке. Этот файл — **вид на реестр**, а не второй его дом:
здесь список номеров с одной строкой «чем мешает Карине».

Правило одного дома здесь не нарушено намеренно: дом строки — реестр, дом
ПРИЧИСЛЕНИЯ к блокерам Карины — этот файл.

## Строки первого признака

| № в 221.1 | чем мешает Карине | состояние |
|---|---|---|
| [1058](221.1-bug-sweep.md) | `novac_load_scale` брала минимум из трёх, то есть отфильтровывала загрузку, которую обязана мерить: поправка УМЕНЬШАЛАСЬ при росте нагрузки. Пока фаза цены решается жребием, `gate-novac.sh` не может дать зелёный вердикт, и «красно по цене» неотличимо от «красно по делу» — приёмка ранга CORE простояла тёмной с 18:02 2026-09-08 | направление починено `0c25a79b8` (медиана из девяти); **открыто:** остаточный разброс 20%, протухший вдвое бюджет `diff-corpus-ms 230000` от 2026-09-02 при честной стене 495032мс, и класс — судить ОТНОШЕНИЕ, а не абсолют с угаданным множителем |
| [1090](221.1-bug-sweep.md) | **ПРОШЁЛ ПО ПРИЗНАКУ 1 И ЗАКРЫТ.** Свободная функция, чьё имя объявлено в двух модулях одного CU, получала мангл в ОПРЕДЕЛЕНИИ и голое `nova_fn_<имя>` в МЕСТЕ ВЫЗОВА — символ, которого никто не эмитит; `nova check` при этом зелёный. **Карину задевало НЕ гипотетически:** в её собственном исходнике `ro_param` объявлена свободной функцией в ДВУХ модулях — `novac/src/resolve/resolve_test.nv:29` и `novac/src/sem/mangle_test.nv:46`, сигнатуры идентичны, то есть ровно та форма столкновения. Значит ломался признак 1 (`nova build novac/src/main.nv`) на её тестовом CU. | ✅ ЗАКРЫТ 2026-09-15: место вызова берёт имя из `resolved_callees` (канал 196), проба в обе стороны, страж свойством собран и на корпусе не сработал ни разу; CI `nova-gate (conformance + flagship examples): success` |
| [691](221.1-bug-sweep.md) | **КАНДИДАТ, не подтверждён.** Машинерия вокруг `setjmp` пре-декларирует локали до прыжка, присваивает внутри охраняемой области и читает их телом cleanup — форма, запрещённая C11 7.13.2.1p3 без `volatile`. Дефект release-only: на `-O0` компилятор держит такие локали в памяти, и всё «работает». **Почему это может касаться Карины:** её собственный исходник использует `consume` **28 раз** (`consume buf = StringBuilder.new(…)`, `consume e = Emitter { … }`), а приёмка №691 прямо называет consume-scope частью непроверенного класса — «ревизия ВСЕХ пре-декларируемых локалей вокруг `setjmp`, не только defer». Если consume-scope эмитит ту же форму, то release-сборка САМОЙ Карины стоит на неопределённом поведении | **что проверено:** операторов `defer` в `novac/src` **ноль** (совпадения только в прозе), то есть через defer Карина не задета; `consume` — 28 настоящих употреблений. **ПУСТАЯ КЛЕТКА ЗАПОЛНЕНА ЧТЕНИЕМ 2026-09-09, И ОТВЕТ УТВЕРДИТЕЛЬНЫЙ: `consume` ИДЁТ ЧЕРЕЗ ТОТ ЖЕ ХОЙСТИНГ.** Адреса: `emit_c.rs:30548` вставляет имя в `hoisted_let_vars` и печатает пре-декларацию с пометкой в самом коде — `/* Plan 217: hoisted for auto-cleanup FAIL/INTERRUPT run-site */`, то есть хойстинг включается **автоочисткой**, а не только `defer`. А `emit_c.rs:66535` говорит про `consume` дословно: голый `consume x = e` порождает ТОТ ЖЕ диспатч на `Nova_<T>_consume_cleanup` через «`enter_defer_scope`'s auto-cleanup prologue scan». Значит механизм, который №691 называет запрещённой C11 формой, на пути `consume` присутствует — а Карина использует `consume` 28 раз, в том числе в `emit_c.nv` и `diag.nv`. **ПРОБА ПРОГНАНА 2026-09-09, ФОРМА НАЙДЕНА В ЖИВОМ C КАРИНЫ — ВСЕ ТРИ УСЛОВИЯ C11 7.13.2.1p3 ВЫПОЛНЕНЫ ОДНОВРЕМЕННО.** Как снято: `nova build novac/src/main.nv --mode release --keep-artifacts`, эмиссия 5.0 МБ. Найдено **два** места, `volatile` — **ноль**. Оба в `nova_fn_3std2fs4read` (`std.fs.read`, втянута в бинарь Карины). Дословно: (1) **объявлена до `setjmp`** — `Nova_File* f = NULL;  /* Plan 217: hoisted for auto-cleanup FAIL/INTERRUPT run-site */`, а ниже `nova_fail_push(&_defer_1_ff); if (setjmp(_defer_1_ff.jmp) != 0) {`; (2) **присваивается внутри охраняемой области** (там же, где открывается файл); (3) **читается ПОСЛЕ прыжка** — внутри ветки `setjmp(...) != 0` стоит `Nova_File_consume_cleanup(f, _nv_tmp_6748);`. В том же кадре та же форма у `_defer_f_active`, `_defer_f_prevdl`, `_defer_f_ccount` — они тоже объявлены до `setjmp`, меняются внутри и читаются после. **ЭТОГО ДОСТАТОЧНО ДЛЯ СТРОКИ, и вот почему не нужен `-O3`** (довод окна 274, принят): стандарт требует `volatile` НЕЗАВИСИМО от того, воспользовался ли компилятор своим правом. Наличие запрещённой формы доказано текстом и детерминированно; проявление на `-O3` — второй, НЕОБЯЗАТЕЛЬНЫЙ пункт, и его отсутствие ничего не опровергнет (урок №679 о UB работает в обе стороны). **ЧТО ЗАМЕР СУЗИЛ ПРОТИВ МОЕГО ОЖИДАНИЯ, и это честнее, чем звучало:** `consume` в Карине 28 раз, а форма возникает **дважды** — только там, где у типа есть `consume_cleanup` (`Nova_File`); `StringBuilder` и `Emitter` её не порождают. То есть радиус внутри Карины два места, а не двадцать восемь. **И ЛОВУШКА, В КОТОРУЮ Я СНАЧАЛА ПОПАЛ:** первый греп я сделал по `novac/src/main.c` — файлу от **14 августа**, артефакту почти месячной давности, лежащему рядом с исходником. Числа совпали случайно. Настоящая эмиссия лежит во временном каталоге сборки; правило «свежесть артефакта проверять до чтения» стоило мне одного ложного захода даже там, где я его знаю. |

## Поправка 2026-09-15: реестр не вёлся, и вердикты мерили не тем

**Шесть строк, заведённых в ночь 14/15 (№1105–№1110), сюда не попали — интегратор
писал вердикт по Карине ПРОЗОЙ внутри строк 221.1 вместо этого файла.** Записано
здесь, а не исправлено молча, потому что причина повторяемая.

**И вердикты были не по здешнему критерию.** Четыре признака выше — про то, что
ломается СЕГОДНЯ: сборка Карины, дифференциал, корпусная эмиссия, ярус
`gate-novac.sh`. Мой вердикт «БЛОКИРУЕТ КАРИНУ» у №1106 и №1107 означал другое —
«сломается в тот день, когда Карина реализует проверку правильно»: сегодня-то
оракул эти формы принимает молча, и ничто не падает. Условие будущего, а не
сломанный признак.

**По определению этого файла из шести не проходит ни одна**, кроме №1090, который
прошёл по признаку 1 и уже закрыт. Это не значит, что №1106/№1107 не важны — они
важны и стоят в 221.1 с измеренным следом в std (2 и 38 мест). Это значит, что
слово «блокирует» у них ДРУГОЕ, и смешивать два смысла в одном реестре — способ
получить число, которым нельзя управлять работой.

**Вопрос «нужен ли ВТОРОЙ разряд» здесь стоял как вопрос владельца с умолчанием «разряда нет, строки остаются в 221.1». РЕШЁН ИНТЕГРАТОРОМ 2026-09-15 — разряд заведён (ниже), и умолчание тем самым отменено.** Говорю это прямо, а не подменяю умолчание молча: решение моё, принято по прямому поручению владельца «актуализируй блокеры Карины и приоритизируй их», и **владелец может его отменить одной строкой** — тогда разряд снимается, а три заселённые строки возвращаются в 221.1 без потерь. Довод, по которому я не стал ждать: срок вопроса был назначен «до первой волны по 274.9 K4», а №1108 оказалась ОГРАНИЧЕНИЕМ ПОРЯДКА для самой этой волны — то есть ждать ответа до K4 значит узнать ответ после того, как он перестанет помогать.

**ГЛАВНЫЙ ДОЛГ ЭТОГО ФАЙЛА СНЯТ 2026-09-15 — И СНЯТ НЕ ТЕМ СПОСОБОМ, КОТОРЫЙ ЗДЕСЬ БЫЛ ЗАПИСАН.** Здесь стояло: открытых строк в 221.1 — 809, из них с полем `БЛОКИРУЕТ ТЕГ: ДА` — 67, а сколько из них проходят по четырём признакам ЗДЕСЬ, «не измерено ни разу»; пока сверка не сделана, ответ на «все ли блокеры Карины видны» — честное «неизвестно». Долг стоял с 2026-09-09.

**ОТВЕТ: НИ ОДНА ИЗ 68 — и получен он ПРОГОНОМ, а не чтением 68 строк.** Способ, который здесь подразумевался (прочесть каждую строку и решить, задевает ли она Карину), судил бы НАПИСАНИЕ — ту самую ошибку, которую этот файл сам называет. Признаки 1–4 **исполнимы**, и в этом всё дело: один прогон судит ВСЕ строки разом, потому что ломается либо признак, либо нет, независимо от того, сколько строк реестра описывают его окрестности. Признак 1 зелён, признак 2 зелён (замер 17:15), признак 4 красен ШЕСТЬЮ отказами, каждый из которых принадлежит собственной дисциплине novac и ни один — оракулу. Значит **ни одна из 68 открытых строк-блокеров тега не ломает Карину сегодня**, и это утверждение сильнее построчного разбора, а не слабее его.

**ЗНАМЕНАТЕЛЬ ВЗЯТ У СТРАЖА, А НЕ ГРЕПОМ, и разница оказалась поучительной.** Открытых блокеров **68** — из `blockers_list` `registry-routes-scan.py`, который читает статус строки и ПЕРВОЕ поле `БЛОКИРУЕТ ТЕГ:`. Грубый греп по подстроке `БЛОКИРУЕТ ТЕГ: ДА` даёт **138** — вдвое больше, потому что ловит и закрытые строки, и прозу внутри строк («ранее считалось ДА»). Число 67 в прежней редакции этого абзаца добыто было таким же грепом (агент, sonnet), и ЧТО ОНО МЕРИЛО, названо не было. Отсюда правило для этого файла: знаменатель берётся у стража, у которого есть правило счёта, а не у грепа, у которого его нет.

**ЧЕГО ЭТОТ ОТВЕТ НЕ ПОКРЫВАЕТ, названо, чтобы полнота не выглядела больше своей.** Прогон судит то, что он ИСПОЛНЯЕТ. Строка, ломающая признак только на входах, до которых Карина ещё не доросла (48 файлов корпуса из 116, которые novac не берёт), сегодня молчит и завтра заговорит — не потому, что дефект появился, а потому, что его наконец достигли. Признак 3 измерен частично. И сам ответ имеет СРОК: он верен на дереве 2026-09-15 и перестаёт быть верным на следующем слиянии в оракул — перемер обязан повторяться, а не цитироваться.

## ПЕРЕМЕР 2026-09-15, 16:20–16:26: признаки ЗАПУЩЕНЫ, а не выведены

Владелец велел перемерить список. Мерить по текстам 67 строк значило бы снова
судить НАПИСАНИЕ; четыре признака этого файла **исполнимы**, и что упадёт — то и
блокер. Запущены.

| признак | вердикт | чем измерен |
|---|---|---|
| 1. `nova build novac/src/main.nv` | ✅ **ЗЕЛЁН** | `built: novac-probe.exe (22.41s)`, RC=0, на бинаре с сегодняшними правками (фикс №1106 и страж свойством №1090 внутри) |
| 2. дифференциал novac против оракула | ✅ **ЗЕЛЁН — ИЗМЕРЕН 2026-09-15, 16:59:43–17:15:27** | `check-novac-differential` целиком, без швов `NOVAC_CORPUS`/`NOVAC_SMOKE`, `DIFF_RC=0`. Исходы: 36 фикстур из 36 совпали с оракулом, в `divergences.allow` — 0. Поведение: 36 из 36 байт-в-байт. Корпус `examples/`: 116 файлов — совпали-приняли 60, совпали-отвергли 6, отставание 48; поведенчески совпали 58 из 60; самосборка — отвергнуто 92 из 93. Храповик корпуса: contract 66, behavior 58 — РОВНО база |
| 3. корпусная эмиссия | ⚠️ **ИЗМЕРЕН ЧАСТИЧНО** | `check-novac-emission-size ok` (файлов 4, объём совпал с базой), `check-novac-emitted-unique ok` (юнитов 36, имён 71, повторов 0); сам прогон эмиссии — в оборвавшемся шаге |
| 4. ярус `gate-novac.sh` | ❌ **КРАСЕН — но НИ ОДИН отказ не есть дефект оракула** | шесть отказов, все перечислены ниже |

**ШЕСТЬ ОТКАЗОВ ЯРУСА, И ЭТО ГЛАВНЫЙ РЕЗУЛЬТАТ ПЕРЕМЕРА:**

* `check-novac-no-crutch` — механизм novac назван костылём вместо правила (П34): **текст самого novac**;
* `check-novac-local-only-work` — ветка с невлитой работой и БЕЗ копии на origin, 1 при базе 0: это **67 непушенных коммитов окна 274**, и лечится их собственным пушем своей ветки, разрешения на который им не нужно;
* `check-novac-plan-liveline` — живая строка плана отстала от кода на 3 дня при пределе 2: **процесс**;
* `check-novac-time-ledger` — пять коммитов в `novac/**` (10–14 сентября) без строки в леджере: **процесс**;
* `check-novac-type-field-docs` — типы/поля/функции novac без документации (П13): **конвенция novac**;
* `nova lint` в `novac/src` — замечания в его собственном исходнике.

**ВЫВОД, КОТОРЫЙ ЭТИМ ИЗМЕРЕН:** по признакам 1 и 4 **дефект ОРАКУЛА не блокирует
Карину сегодня ни в одном месте**. Ярус красен — но красен собственной дисциплиной
novac: незапушенная ветка, леджер, доки, лексика, линт. Оракул в этом списке не
участвует.

**ПРОБЕЛ ЭТОГО ПЕРЕМЕРА ЗАКРЫТ 2026-09-15 В 17:15 — И ЗАКРЫТ ЗАМЕРОМ, А НЕ РАССУЖДЕНИЕМ.** Здесь стояло: признак 2 (дифференциал) остался неизмеренным, прогон оборвался на своём шаге, и пока он не измерен, ответ «блокеров нет» **неполон**, потому что дифференциал — единственный из четырёх, кто сравнивает ВЫВОД двух компиляторов и способен показать расхождение, невидимое остальным трём. Перезапущен точечно (`check-novac-differential`, а не весь ярус: предмет замера — именно он) и **зелён**, числа — в строке 2 таблицы выше.

**ПОЧЕМУ ОН «ОБОРВАЛСЯ» В ПРОШЛЫЙ РАЗ — ОТВЕТ ОКАЗАЛСЯ СКУЧНЫМ И ВАЖНЫМ: он не обрывался, он ДЛИННЫЙ.** Прогон занял 15 минут 44 секунды (16:59:43 → 17:15:27), из них стена дифф-раннера 598492мс. Шапка стража обещает «корпусная часть ~2–6 мин (замер 2026-08-14: 334с под нагрузкой)» — то есть прозаическая оценка отстала от факта почти вдвое, и именно она сделала долгий шаг похожим на мёртвый. Сам страж при этом свой бюджет проверяет и уложился (598492мс при 1909600мс), так что расхождение живёт РОВНО в прозе, которую никто не сверяет, — тот же корень, что у №1109 и №1114. **Вывод для следующего окна, и он дороже самого замера: «лог замер, процесса нет» — это два РАЗНЫХ наблюдения, и второе надо проверять по часам, а не по молчанию лога.** Прошлый вердикт «ПРЕКРАТИЛСЯ» был сделан по молчанию. **ДОПОЛНЕНО ВЕЧЕРОМ 2026-09-15, И ЗДЕСЬ Я ОШИБАЛСЯ ДВАЖДЫ, А НЕ ОДИН РАЗ.** Прогон яруса `gate-novac`, о котором шла речь (старт 16:21:41), не просто был длинным — он был ЖИВ ещё более двух часов и снят мною вручную в 18:35 (PID 25202 и шесть потомков, включая идущий фаззер). Исчез не процесс, а ФАЙЛ: `.gate-novac.log`, в который он писал, удалён с диска, и прогон продолжал писать в удалённый дескриптор — со стороны сессии это неотличимо от несуществующего. **То есть я проверил обе половины собственного правила и вторую («процесса нет») проверил НЕВЕРНО**, глядя на файл вместо `ps`. Нашло это чужое окно и НЕ рассуждением, а четырьмя уликами: `ps -ef` со стартом и родителем, `readlink /proc/<pid>/cwd`, греп по историям сессий и отсутствие файла лога. **ЦЕНА, КОТОРУЮ ЭТО ОБЪЯСНЯЕТ:** два моих яруса и ярус окна 274 (с 16:10:04, в его дереве) шли на одной машине одновременно, поэтому мой вечерний прогон `gate-novac` занял 6734с при бюджете 3000с — и это число НЕ замер, а след давки. **СЛЕДСТВИЕ ДЛЯ ПРАВИЛА «ПРИ ЧУЖОМ ГЕЙТЕ НОЛЬ ЗАПУСКОВ»:** соседние окна честно доложили «машина свободна», померив процессы `cargo`, — а гейт `cargo` не держит. Дефект в СПОСОБЕ проверки занятости, не в дисциплине окон, и закрывается он механизмом (перечень идущих ярусов с PID и деревом), а не внимательностью.

**ЧТО ЭТИМ ТЕПЕРЬ ИЗМЕРЕНО ПОЛНОСТЬЮ:** три признака из четырёх зелены (1, 2, и 3 в своей измеренной части), четвёртый красен собственной дисциплиной novac. **Ответ «дефект ОРАКУЛА не блокирует Карину сегодня» перестал быть неполным** — единственный признак, способный показать расхождение ВЫВОДА, показал совпадение везде, где обе стороны принимают программу.

**И ЧТО ПО-ПРЕЖНЕМУ НЕ ИЗМЕРЕНО, чтобы полнота не выглядела больше своей:** признак 3 (корпусная эмиссия) закрыт лишь частично — два стража размера и уникальности зелены, сам прогон эмиссии как шага яруса не гонялся ни тогда, ни сейчас. И отставание корпуса — 48 файлов из 116, которые novac ещё не берёт, — дифференциалом НЕ судится: он сравнивает там, где обе стороны высказались. Отставание это долг Карины по 274, а не молчаливое зелёное.

**И ОТДЕЛЬНО — ЧТО ОКАЗАЛОСЬ НЕ УЛИКОЙ.** Сверка 67 строк против исходника Карины
(агент, sonnet) дала 30 строк с «попаданиями» — и разбор показал, что совпали
**номера D-блоков и родовые имена узлов** (`D133`, `Ident`, `TyId`), то есть
токены, которые цитирует каждый файл чекера. Уликой это не является, и виноват
бриф: я велел искать `D[0-9]+`. Записано, чтобы следующий не принял тридцать за
меру.

## Замер 2026-09-15, 22:15–22:18: что мешает Карине СОБРАТЬСЯ

**Владелец сузил признак до проверяемого (2026-09-15):** задача интегратора —
определить, что мешает Карине собрать САМУ СЕБЯ; отсутствие M:N, например,
собраться ей не мешает. Значит вопрос решается попыткой самосборки, а не
рассуждением о правильности. Прогнано.

**КАК МЕРЕНО, чтобы число можно было пересчитать:** все 93 файла `novac/src`
поданы ОДНОЙ единицей — `NOVAC_SELF_PATH=novac/src novac/target/novac.exe
check <93 файла>`, бинарь от 18:15. Выдача разобрана как JSON, классы сведены
по тексту сообщения, а не по файлу.

**ОТВЕТ: дефектов ОРАКУЛА среди причин НЕТ НИ ОДНОГО.** Всего 264 диагностики
в **6 файлах из 93**; коды `E_NOVAC_SUBSET` — 263, `E_NOVAC_ICE` — 1. Все 263 —
собственный диагноз novac «вне подмножества, которое я пока компилирую», ICE
помечен им же как «novac bug, not yours». Оракул её собирает — признак 1 выше
зелён. **Мешает Карине не наш компилятор, а её собственная незавершённость.**

**ТРИНАДЦАТЬ КЛАССОВ, по убыванию** (число — сколько раз):

| N | класс отказа |
|---|---|
| 129 | `unknown name: nothing with this name is bound at this point` |
| 61 | `unknown field: this type has no field with this name` |
| 23 | тела модуля не компилируются в этот юнит (названный этап E2-b2, многофайловость) |
| 19 | `must carry a value` |
| 15 | `syntax error: this is not a form of the language` |
| 6 | вызов опускает параметр без умолчания |
| 5 | два аргумента-вызова, порядок эмиттер пока не фиксирует (F65) |
| 1+1+1+1+1 | срезы/кортежи/fn-типы (ждут генериков, E2-b); индексирование; `match` в позиции значения; оболочка без `Checker`; `X` в позиции значения |
| 1 | ICE: `check: method call reached type_of with no decision in the channel` |

**ВЕСЬ ДОЛГ СИДИТ В ЧЕТЫРЁХ ФАЙЛАХ ЧЕКЕРА:** `check/binds.nv` — 171,
`check/assign_rules.nv` — 37, `check/casts.nv` — 32, `check/calls.nv` — 21.
**Концентрация 190 из 264 в двух классах (`unknown name` + `unknown field`), из
них 171 в ОДНОМ файле, почти наверняка означает ОДИН корень** — невидимый
модуль или тип, чьи поля не разрешаются, — а не 190 отдельных пробелов. Это
гипотеза, а не замер, и первый шаг чинящего — её проверить.

**И ОТДЕЛЬНО — ЧИСЛО, КОТОРОЕ НЕЛЬЗЯ ЧИТАТЬ КАК РАССТОЯНИЕ (строка №1122).**
Строка вывода `самосборка: отвергнуто 92 из 93` завышает долг примерно в
пятнадцать раз и делает это МЕХАНИЧЕСКИ: при любом ICE раннер откатывается на
ПОФАЙЛОВЫЙ проход, а пофайловая проверка многофайловой программы падает по
построению. Различающий опыт: `sem/mangle.nv`, `diag/diag.nv`, `lex/lex.nv`
чисты в единице сборки и падают поодиночке (RC=1 у всех трёх). Планировать по
этому числу нельзя: оно говорит «почти ничего не работает» там, где не
проходят шесть файлов.

## Рассмотрено и НЕ включено

Отвергнутый кандидат записан наравне с принятым: иначе следующий пойдёт по тому
же кругу и потратит день.

* **№737** (ратчет просел на одиннадцать за девять дней, потому что его проверяет
  только необязательный локальный гейт) — **не сюда.** Дефект настоящий и
  открытый, но по четырём признакам не проходит ни по одному: он не ломает ни
  сборку Карины, ни дифференциал, ни корпусную эмиссию, ни ярус `gate-novac.sh`.
  Он делает НЕНАДЁЖНЫМ любой вердикт вообще — это шире Карины, и место ему в
  221.1, где он и стоит;
* **№1055** (`nova.toml` молча теряет многострочные массивы) — **не сюда.**
  Проверено: `novac` не читает `nova.toml` вовсе, у него две подкоманды
  (`check`, `emit`), ключей `[ffi]` в исходниках нет. Дефект бьёт по пакетам, не
  по Карине;
* **№750** (переполнение стека в `nova-lsp`) — **не сюда.** Ломает шаг
  `crate-tests` основного яруса, а не яруса Карины; вдобавок страж выставляет
  `RUST_MIN_STACK`, и на его прогоне тест зелёный (проверено: 1897 тестов, exit 0).

---

# ЧАСТЬ II. ПЛАТИТ ОБХОДОМ — признак судится НОСИТЕЛЕМ

## Что сюда входит

Дефект нынешнего компилятора, из-за которого **исходник Карины написан иначе,
чем был бы написан без него**: обход, промежуточный биндинг, литерал вместо
константы, вектор длины 0..1 вместо `Option`, отказ от собственной архитектуры.

**Признак проверяемый:** в тексте строки реестра назван НОСИТЕЛЬ ОБХОДА — файл
и место в `novac/src`, — либо описано ограничение, из-за которого она не может
писать задуманным способом.

**НЕ входит:** дефекты самой Карины (её очередь — 274 и его волны), дефекты,
упоминающие её пути как контекст или аналогию, и родня по классу без носителя.
Две строки отпали при сверке именно по этому признаку: **№685** (у Карины СВОЙ
дефект той же формы, заведён ими как F65 и чинится на их стороне) и **№716**
(`novac/src` не упоминается вовсе, названа роднёй класса О-1).

---

## Четырнадцать дефектов — по содержимому, а не по ссылке

**Почему здесь форма и репро, а не номер строки.** Номер ведёт в реестр плана
221, который закрывается; после закрытия он станет архивной ссылкой, и чинить по
нему будет нельзя. Поэтому каждый пункт несёт то, что живёт вне реестра: форму
дефекта, что оракул делает не так, воспроизведение и файлы-носители. Номер
оставлен сноской о происхождении.

**Носители сняты ГРЕПОМ по маркерам, а не переписаны из реестра** (2026-09-16,
`novac/src`, 8 маркеров, 33 места). Числа строк в реестре местами протухли:
№760 значился «6 площадок» — на деле 10 мест в 6 файлах; №761 «1 площадка
`callables.nv:50`» — на деле 2 места, строки 49 и 60; №675 указан `lex.nv:81` —
на деле `lex.nv:235`. **Поэтому ниже названы ФАЙЛЫ и МАРКЕРЫ, а номера строк
нет:** они устареют снова.

**Колонка «оракул» — состояние ПОЧИНКИ, а не снятия обходов.** Разделены
намеренно: обходы снимает окно Карины отдельной работой, и их наличие в её коде
ничего не говорит о том, жив ли дефект у нас. Путать эти два состояния — значит
второй раз чинить починенное либо снимать обход раньше времени.

| порядок | дефект | мест обхода | маркер в коде | оракул |
|---|---|---|---|---|
| 1 | №760 newtype под `ro` | **10** в 6 файлах | `LEGACY-#760-newtype-ro-launder` | ЗАКРЫТ 2026-09-17 |
| 2 | №676 метод на `vec[j]` | снят (был **8** в 8 файлах) | нет | ЗАКРЫТ 2026-09-18 |
| 3 | №895 статик протокола через параметр типа | архитектура | нет | открыт |
| 4 | №764 вложенный `if Some(..)` с `return` | снят (был **2** в 2 файлах) | нет | ЗАКРЫТ 2026-09-18 |
| 5 | №761 newtype в payload варианта | **2** в 1 файле | `LEGACY-#761-newtype-order-in-sum-arm` | ЗАКРЫТ 2026-09-18 |
| 6 | №701 экспорт `ro`-биндинга | **1** | `LEGACY-#701-export-ro-vector` | открыт |
| 7 | №675 char-литерал | снят (репоинт на №1158, см. ниже) | нет | ЗАКРЫТ 2026-09-18 |
| 8 | №762 `match` по `str` с константами | снят (диагностика `E_MATCH_CONST_PATTERN` в чекере — образец-константа отвергается формой, не доезжает до C) | нет | ЗАКРЫТ 2026-09-18 |
| 9 | №763 поле типа generic-суммы | 1, без маркера | нет | открыт |
| 10–14 | №699, №705, №879, №693, №684 | обхода нет | нет | открыты |
| 15 | №1158 форма инициализатора константы решает эмиссию одноимённых в других модулях | **1** (`novac/src/lex/lex.nv:237`, репоинт бывшего носителя №675) | `LEGACY-#1158` | открыт |

**Найдено стражем `check-novac-oracle-tax-link.py` при его первом прогоне
2026-09-25 (см. «Порядок работы» п.1):** таблица выше отстала от кода на три
строки. №676 и №764 — обходы физически сняты в novac/src (грепом ни одного
`LEGACY-#676`/`LEGACY-#764`, только их закрывающие статусы), таблица ещё
называла живой маркер. №675 сложнее: сам дефект закрыт 2026-09-18 (реестр
221.1, D481), но окно Карины 2026-09-20 обнаружило, что носитель держит ДРУГОЙ,
не названный в реестре дефект — репоинт документирован прямо в коде
(`novac/src/lex/lex.nv:237-256`, `[was: LEGACY-#675]` → `[LEGACY-#1158]`) и в
реестре (221.1 №1158, «МАРШРУТ: ЧИНИТСЯ: интегратор (налог оракула на Карину,
274.10)»). Добавлена строка 15.

---

### 1. №760 — возврат `ro`-связанного newtype отвергается на примитиве-носителе

**Форма:** `fn f(t TyId) -> TyId => t` при `type TyId int`.
**Что не так:** оракул даёт `E_READONLY_COERCE`, хотя носитель примитив,
возвращается копия и утечка записи невозможна по построению.
**Репро:** в строке реестра не названо — воспроизводится формой выше.
**Носители (были 10 мест, 6 файлов):** `check/assign_rules.nv`, `check/casts.nv`,
`check/type_of.nv` (дважды), `sem/mangle.nv` (трижды), `sem/typeref.nv`,
`types/types.nv` (дважды, дверь `same_ty`: тот же id пересобирается, чтобы отказ
не сработал).

**СНЯТО 2026-09-26 (интегратор):** во всех 10 отмеченных вызовах `same_ty(x)`
заменён на голое `x` (репро `fn pass_row(r TyId) -> TyId => r` при `type TyId int`
— `nova check` принимает без обёртки); удалены попутные неиспользуемые импорты
`same_ty` в `check/assign_rules.nv`, `check/casts.nv`, `sem/mangle.nv`,
`sem/typeref.nv`. `nova check novac/src` — 16/16 PASS, `nova lint novac/src` —
0 находок, `NOVAC_TIER=loop bash scripts/gate-novac.sh` — оба отказа прогона НЕ
про этот код (бюджет яруса и «ветка без копии на origin», оба предсуществовали).
Дверь `same_ty` в `types/types.nv:126` НЕ удалена — на неё есть ещё ~27
незамаркированных вызовов в других файлах (`check/calls.nv`, `check/handler.nv`,
`check/option_rules.nv`, `check/return_rules.nv`, `check/typing.nv`,
`sem/binding.nv` и другие места `check/type_of.nv`/`sem/mangle.nv`), не
перечисленных как носители этой строки, — снимать их значило бы расширять
задачу без заявленного критерия приёмки; если это тоже #760, для них нужна
отдельная строка носителей.

### 2. №676 — метод на `vec[j]` с переменным индексом берёт тип КОНТЕЙНЕРА

**Форма, громкая:** `type Cell { ro n int }` с методом `@twice()`;
`cells[i].twice()` → `codegen error: [E_RECV_METHOD_MISMATCH] .twice(...) на
ресивере типа Vec`. **Форма, тихая и потому худшая:** `ro xs = [[10, 20, 30]]`,
`ro i = 0`; `xs[i].len()` даёт **1** (длина внешнего вектора) вместо 3, при том
что `ro v = xs[i]; v.len()` даёт верные 3.
**Что не так — ИСПРАВЛЕНО 2026-09-18 ПО ЗАМЕРУ; прежняя запись была неверна.**
Здесь стояло: «вывод типа ресивера не спрашивает канал чекера… берётся тип
контейнера». **Канал чекера отвечает ПРАВИЛЬНО** — трассировка `NOVA_CH2_TRACE=1`
даёт для `xs[i]` тип `Named{Vec,[int]}`, то есть ЭЛЕМЕНТ, и ни одного промаха.
До кодогена не доезжает сама ИНДЕКСАЦИЯ: узел `xs[i]` имеет вид
`ExprKind::TurboFish`, а не `Index`, и в выданном C нет вызова `nova_idx_chk`
вовсе — стоит `Vec____Nova_Vec____nova_int_p_method_len(xs)`.

Причина лежит на фазу раньше — в РАЗБОРЕ. `base[X].method(...)` неоднозначно:
`Vec[int].new()` и `xs[i].len()` написаны одинаково, а различает их только то,
чем является база — типом или значением. По букве D38 побеждал turbofish.
**Прежняя гипотеза объясняла две клетки из пяти**, и это был её главный изъян:
почему литеральный индекс `xs[0]` работает, она не говорила ничего. Настоящая
причина объясняет все пять одним предложением — литерал именем типа быть не
может, у поля база `Member` (гейт `base_is_type_like` её отсекал), у биндинга
нет пост-`]` продолжения.

**СДЕЛАНО 2026-09-18 (интегратор):** правка в `parser/mod.rs` — имя,
объявленное в файле ЗНАЧЕНИЕМ, базой turbofish'а быть не может; амендмент к D38
в `spec/decisions/03-syntax.md` тем же слиянием (правило языка менялось, значит
одного кода мало). Фикстура `spec_tests/conformance/standalone/`
`p676_var_index_receiver.nv` — пять клеток, `lit=3 local=3 param=3 field=3`
`bound=3 ok`. Приёмка снята: `nova check std/src` даёт `PASS: 157 FAIL: 26`
`WARN: 67` (число в число с замером до правки); проба в обе стороны на ОДНОМ
дереве с дофиксным `parser/mod.rs` из `HEAD` как выключателем — без фикса
фикстура красная, с фиксом зелёная, а КОНТРОЛЬНЫЙ носитель законного turbofish
(`Vec[u8].new()`) зелёный в обеих половинах, то есть выключатель красит ровно
предмет, а не сборку.

**Носители в novac можно снимать** — девять `ro pj = vec[j]` ниже; это работа
окна Карины, и слово ему дано после зелёного корпуса.
**Репро (проверено — каталоги существуют):** `docs/dev/repro/o1/676_var_index_method`
(громкая форма), `docs/dev/repro/o1/676b_var_index_silent` (тихая, печатает 1
вместо 3).
**Носители — 8 мест в 8 файлах (пересчитано окном Карины 2026-09-18, по замеру):**
`check/params.nv`, `check/tail_rules.nv`, `check/type_of.nv`, `lower/lowering.nv`,
`sem/collect.nv`, `sem/harvest.nv`, `sem/slots.nv`, `sem/typeref.nv` — везде
промежуточный биндинг `ro pj = vec[j]`.

**ПРЕЖНИЙ СПИСОК ПРОТУХ ДВАЖДЫ, И ОБА РАЗА МОЛЧА.** Здесь стояло «9 мест в 9
файлах» с `emit_c/emit_match.nv` и `sem/sem.nv`. (1) Маркера в `emit_c/emit_match.nv`
нет: носитель уехал в `lower/lowering.nv` при выделении понижения из эмиттера
(274.8/M3) — перечисление ИМЁН ФАЙЛОВ стареет от любого переезда, ничего об этом не
сообщая. (2) `sem/sem.nv` был ложным носителем вовсе: под маркером стоит
`raw_row(row)` — свободная ФУНКЦИЯ, а не метод, — значит №676 там не мог
применяться никогда; маркер скопирован по образцу, а не замерен.

**ПРИЗНАК, ПО КОТОРОМУ СУДИТЬ (его и применяло окно):** метод ли это на
индексированном элементе. `is_ident`/`kind_of`/`ident_str` — методы (`fn Node @…`);
`raw_row`/`leaf_text`/`branch_children` — свободные функции, и на них дефект не
действует. **Отсюда следствие для всей таблицы выше: счёт по МАРКЕРАМ — не замер
налога, а верхняя оценка.** Налог целиком по факту на 2026-09-18: 32 носителя в 21
файле (было 33 в 22).

**СНЯТИЕ ОБХОДОВ ЖДАЛО ПУША, и дождалось — `741d25940`, 2026-09-18 13:10.** Запись остаётся, потому что в ней поправка к ЛОЖНОМУ ЗАГОЛОВКУ моего коммита слияния, а его история не переписывается (2026-09-18, 01:45–02:00).
лежит только на ЛОКАЛЬНОМ `main`:
`git merge-base --is-ancestor e5a94c0eb origin/main` отвечает НЕТ.
**Мой коммит слияния `9325ad812` озаглавлен «the fix that lets nine
workarounds go» — это неправда: в том, что слито, фикса нет.**
История не переписывается, поправка живёт здесь. Поймано не глазами:
сняты все девять маркеров, пересобран novac — и сборка упала на
`E_RECV_METHOD_MISMATCH` (`.ident_str()` на ресивере `Vec`) в `lower/lowering.nv`.
Проба на моём оракуле: `for i in 0..xs.len() { n += xs[i].len() }` на
`["abc"]` печатает `total=1` вместо `3` — в простейшей форме, без всякого
`match`. Дерево возвращено `git checkout --`, сборка зелёная.

### 3. №895 — статический метод протокола через параметр типа резолвится на носитель

**Форма:** `type Idx protocol { @idx() -> int; .from_idx(i int) -> Self }` (D143),
`type FnRow int` с обеими реализациями, `fn mk[I Idx](n int) -> I => I.from_idx(n)`,
вызов `mk[FnRow](41)`.
**Что не так:** `nova check` — ok; `nova build` — `codegen error:
[E_UNKNOWN_STATIC_METHOD] int.from_idx(...)`: при мономорфизации `I → FnRow`
кодоген подставляет представление newtype вместо самого newtype. Вторая форма
(`fn[T SignedInts] T.from_idx(...)`, вызов `i64.from_idx(3)`) даёт
`[INTERNAL-PANIC] [E_CODEGEN_TYPE_UNKNOWN]`.
**Репро (проверено):** `docs/plans/repro/895-static-via-type-param/`.
**Чем платит Карина:** это ограничивает `IndexVec`, объявленную её архитектурой
(`sem/sem.nv`, `channel.nv`); обход — строить индекс снаружи по `len()`, и в
самой строке реестра записано, что этот обход открывает вторую дверь.
**Отдельно, СНЯТО:** поле `Статус:` у строки №895 отсутствовало на момент
написания этого раздела — проставлено интегратором 2026-09-21 (`ОТКРЫТ`,
направление решено владельцем 2026-09-18, реализация не начата; чинится
после ступени 0.2, план 274 «После 0.2 — №895»). Работа НЕ начинается сейчас
по той же причине.

### 4. №764 — вложенный `if Some(..)` с `return` эмитит временную вне её области

**Форма — СВЕДЕНА К КОДУ 2026-09-18** (в строке реестра её не было): внешний
`if Some(row) = ...`, внутри него второй `if Some(why) = ...` **С `else`**,
внутренняя ветвь уходит `return`'ом, за внешним `if` следует ещё одно ветвление.
Симптом — `use of undeclared identifier '_nv_if_let_NNN'` в трёх местах.

**ОСЬ, КОТОРОЙ НЕ БЫЛО НИ В СТРОКЕ, НИ ЗДЕСЬ, И БЕЗ КОТОРОЙ ДЕФЕКТ НЕ ВИДЕН:**
`else` у ВНУТРЕННЕГО if-let. Реестр называл три границы класса (вложенность,
`return`, код после внешнего `if`) — проба, написанная строго по ним, **зелёная
и на сломанном компиляторе**. Воспроизвелось по КОММЕНТАРИЮ В НОСИТЕЛЕ
(`novac/src/check/calls.nv:77`): автор обхода описал форму точнее, чем реестр.

**Что не так — ИСПРАВЛЕНО ПО ЗАМЕРУ.** Здесь стояло «временная объявляется в
области у́же той, откуда её читают». Область ни при чём: у временной ПУСТОЙ ТИП,
и строка ` _nv_if_let_NNN;` — не объявление, а чтение необъявленного имени.
Замер трассировкой: у внешней временной `result_ty=""`, у внутренней
`result_ty="nova_unit"`. Настоящая причина — ДВЕ ДВЕРИ НА ОДИН ВОПРОС: вывод
типа спрашивал канал только по трейлингам ветвей, а у ветви с `return` трейлинга
нет вовсе; эмиссия того же узла в этом случае пишет `nova_unit`.

**СДЕЛАНО 2026-09-18 (интегратор):** правка ветки `IfLet` в `infer_expr_c_type`
(`emit_c.rs`) — рекурсивный запрос по трейлингу-if-let плюс ответ `nova_unit`
там, где значения нет по построению. Фикстура `p764_nested_if_let_return.nv` —
четыре клетки, две из них контрольные: форма без `else` (зелёная всегда) и
if-let СО значением (краснеет, если запасной ответ перестарается). Проба в обе
стороны на одном дереве с `emit_c.rs` из `HEAD` как выключателем.
**Спека не менялась:** правило языка то же, расходились две реализации одного
вопроса внутри кодогена.
**Два уплощения в novac можно снимать** — это работа окна Карины.
**Носители (2 места):** `check/calls.nv`, `check/methods.nv` — внешний тест
уплощён до `if !only.is_none()` вместо `if Some(only) = ...`.

### 5. №761 — newtype в payload варианта требует порядка эмиссии

**Форма — СВЕДЕНА К КОДУ 2026-09-18:** `type Target enum | ToRow(Row) | Nothing`,
и `type Row int` объявлен НИЖЕ суммы. Отказ: «typedef redefinition with different
types ('struct Nova_Row' vs 'nova_int')».
**Что не так:** forward-декларация структуры выдавалась payload-типу по СПИСКУ
ИМЁН (двадцать рантаймовых) вместо вопроса «а это вообще структура?». Порядок
объявления решал, соберётся ли программа: сумма выше newtype — отказ, ниже — успех.
**СДЕЛАНО 2026-09-18 (интегратор):** forward выдаётся только записи, сумме и
generic-инстансу. Фикстура `p761_newtype_in_variant_payload.nv` — три клетки по
порядку объявления, одна из них контрольная. Проба в обе стороны с `emit_c.rs` из
`HEAD`; регресс — `nova check std/src` и корпус одним CU.
**Три типа в novac можно вернуть на место** — работа окна Карины.
**Носители (2 места, 1 файл):** `sem/callables.nv` — `VariantRow` и `FieldRow`
объявлены не рядом с `VariantDef`, а в файле, эмитящемся раньше потребителей.

### 6. №701 — `export ro` с runtime-инициализацией не переживает границу модуля

**Форма:** `export ro universe []Bt = []Bt.of(...)` — компилируется у владельца
модуля, у импортёра даёт `undefined identifier 'universe'`. `export const ... .of(...)`
даёт `E_CONST_NOT_CONSTEXPR`; `export ro answer = compute()` падает так же.
**Что не так:** дефект шире имени строки — не только вектор, любой `export ro`
с runtime-инициализацией. Молчит при компиляции экспортёра, падает у импортёра
на месте использования.
**Репро:** не названо (интегратор подтверждал на 12 строках, путь не сохранён).
**Носитель (1 место):** `builtins/builtins.nv` — приватная модульная таблица плюс
экспортированная функция-читатель.

### 7. №675 — char-литерал: сырой управляющий байт принимается, `'\\'` отвергается

**Форма:** одиночный `'\\'` отвергается; сырой управляющий байт принимается молча.
**Что не так:** две несогласованности разом — лексер принимает то, чего спека не
разрешает, и отвергает законное.
**Репро:** не названо.
**Носитель (1 место):** `lex/lex.nv` — hex-константы `B_*` вместо char-литералов.

### 8. №762 — `match` по `str` с образцами-константами эмитит код суммы

**Форма:** `const MUT_W = "mut"` и `match w { MUT_W => 1, _ => 0 }` по `w str`.
**Что не так:** `check` проходит, сборка C падает трижды: «member reference type
'nova_str' is not a pointer», «no member named 'tag' in 'nova_str'», «use of
undeclared identifier 'NOVA_TAG_nova_str_MUT_W'».
**Репро:** не названо — воспроизводится формой выше.
**Носитель:** `lex/lex.nv` — написания ключевых слов остались литералами в
таблице «слово → токен». **Маркера в коде НЕТ** — обход есть, пометки нет.

**ЗАКРЫТО 2026-09-18 (интегратор), проверено вдвойне 2026-09-21 (интегратор и
помощник независимо):** образец-константа НЕ стала формой языка — владелец
решил отвергать её диагностикой `E_MATCH_CONST_PATTERN`, канон `w if w ==
MUT_W => 1`. Проба сегодня подтверждает дословно: тот же `MUT_W => 1` даёт
именно эту диагностику, до C дело не доходит. Подробности — реестр №762.

### 9. №763 — поле записи с типом generic-суммы эмитится неверным C-типом

**Форма:** `_nv_tmp.d = (nova_int)(intptr_t)nova_make_Option_None();` — литерал
записи кастует конструктор в скаляр; привязка `if Some(v) = r.d` даёт «assigning
to 'nova_str' from incompatible type 'nova_int'».
**Что не так:** теряется инстанциация generic-суммы, когда она стоит ТИПОМ ПОЛЯ;
`check` проходит.
**Репро:** не названо (в строке пять проб того же дня без путей).
**Носитель:** `sem/binding.nv`, `ParamDef.default_expr` — вектор длины 0..1 вместо
`Option[Node]`, дверь `slot_default_of`. **Маркера в коде НЕТ.**

**НЕ ВОСПРОИЗВОДИТСЯ СЕГОДНЯ, проверено дважды независимо (интегратор и
помощник, 2026-09-21):** минимальная форма (`type Row { d Option[int] }`,
`if Some(v) = r.d`) собирается и печатает верно (`0` для `None`). Каким именно
коммитом чинилось — не установлено (бисекция по десяткам правок `emit_c.rs`
дорога, не делалась), поэтому строка в реестре ОСТАЁТСЯ ОТКРЫТОЙ — «сегодня
зелено» не равно «объяснено», и следующая правка эмиттера может вернуть отказ
молча. Снятие обхода в `sem/binding.nv` можно пробовать — дерево прогонит его
молча, если правда чисто, но это не заменяет причину.

### 10. №699 и №705 — одна семья: имя типа коллидирует между модулями

**№699, форма:** `std/src/encoding/serde/tagging_test.nv` объявляет `type Node
enum`; посторонний потребитель зовёт `serde.Node.Leaf(1)` — `nova check` даёт
`PASS: 1 FAIL: 0`, сборка даёт `[INTERNAL-PANIC] [E_CODEGEN_TYPE_UNKNOWN] method
call .Leaf return type unknown`. Тестовый peer-файл std утекает в CU потребителя.
Места в оракуле: `compiler-codegen/src/imports.rs` — `peer_file_included(...)`
(`:2687`, `:3766`, `:3802`; `:1008` не трогать), `check_pipeline.rs:54-59`.

**№705, та же грань на стороне ЧЕКЕРА:** таблица типов `types: HashMap<String,
TypeDecl>` ключуется голым именем, last-write-wins. Проявилось на `match` по
`ErrorKind` из `std/src/io/error.nv`, потребовавшем варианты ЧУЖОГО `ErrorKind`.
**Репро (проверено):** `docs/plans/repro/p705/`; фикстуры
`conformance/crossmod_exhaustive_collision/`, `neg/d65_exhaustive_crossmod_collision/`.
**Чинить вместе:** один корень, две стороны.

### 11. №879 — индексация Vec-элемента эмитит каст к чужому типу

**Форма:** `println(d[0].message)` при `d: []Diagnostic` эмитит каст к
`Nova_novac_tree_Node*`; индексация верная (`sizeof` свой), тип результата чужой.
**Репро (процедура, не каталог):** в `novac/src/pipeline/subset_test.nv` вставить
`if d.len() > 0 { println(d[0].message) }` и гнать
`nova test novac/src/pipeline/subset_test.nv` → CC-FAIL.
**Носителя обхода нет** — временный debug-print снят.

### 12. №693 — бинарь оракула и заголовки рантайма из разных деревьев

**Форма:** кода нет; симптом — `use of undeclared identifier` на функциях
рантайма (`nova_throw_site_set_dominant`, `nova_fiber_report_error_kinded`).
**Что не так:** компилятор эмитит вызовы новых функций рантайма вместо отказа
собираться с сообщением о рассинхроне.
**Обход:** `nova.sh` пакетов (`NOVA_RT_DIR`/`NOVA_CG_INCLUDE`) — для сборки
`novac` из чужого worktree не применён.

### 13. №684 — LSP не знает формы `@имя`

**Форма:** `@emit_println(st)` в `novac/src/emit_c/emit_c.nv` — goto-definition
даёт «No definition found».
**Что не так:** `resolve_symbol_at_with_limit` (`nova-lsp/src/symbol.rs`) не
покрывает `ExprKind::SelfAccess` ни одним из пяти путей резолва.
**Репро:** открыть указанное место и запросить goto-definition.
**Мешает РАБОТАТЬ над Кариной, а не собирать её** — включён с этой оговоркой,
и это открытый вопрос владельцу ниже.

---

## Расхождение учёта, найденное при сборке этого списка

**Страж `check-novac-legacy-workarounds.py` считает налог по маркерам
`[LEGACY-#N]` в `novac/src` и даёт ВОСЕМЬ** (760, 676, 700, 764, 761, 708, 701,
675; 33 носителя в 22 файлах). **Здесь — четырнадцать.** Разница не случайна и
показывает обе стороны:

* **Описано, но не помечено:** №762, №763, №895 — обход в её исходнике назван
  словами в реестре, маркера в коде нет. Счётчик их не видит.
* **Помечено, но не про нашу сборку:** №700 — дефект САМОЙ Карины (`ice()` на
  ошибке пользователя), в налог оракула не входит; №708 маркер несёт, а текст
  строки про сборку не говорит — требует отдельного чтения.

**Отсюда первая работа плана — не починка, а СВЯЗЬ:** страж, требующий, чтобы
каждый номер из маркеров `[LEGACY-#N]` имел строку здесь, и чтобы каждая строка
здесь с названным носителем имела маркер в коде. Сегодня оба направления
держатся на внимании, и оба разошлись.

---

## Порядок работы

1. ~~**Связь маркеров и списка** (страж, обе стороны) — до починок, иначе список
   разойдётся с кодом снова.~~ **СДЕЛАНО 2026-09-25** — `check-novac-oracle-tax-link.py`,
   в гейте, самотест 15 случаев, `check-novac-guard-registry` зелёный. Первый
   же прогон поймал реальный дрейф — три строки таблицы исправлены (см. ниже).
2. ~~**№760**~~ — оракул починен, все 10 отмеченных `same_ty(x)` сняты
   (заменены на `x`), `nova check novac/src` 16/16 — пункт ЗАКРЫТ 2026-09-26
   (интегратор). Дверь `same_ty` сама осталась (~27 незамаркированных вызовов
   в других файлах) — см. backlog `[M-same-ty-door-unmarked-callers]`.
3. ~~№676~~ — оракул починен, обход СНЯТ (страж `check-novac-oracle-tax-link.py`
   нашёл ноль `LEGACY-#676` в novac/src 2026-09-25) — пункт ЗАКРЫТ, приёмка
   исполнена без отдельного слияния этого плана.
4. ~~**№895**~~ — поле `Статус:` проставлено 2026-09-21. **НЕ БРАТЬ СЕЙЧАС:**
   владелец 2026-09-18 решил направление, но чинится ПОСЛЕ ступени 0.2 (план
   274, «После 0.2 — №895») — 0.2 берётся с обходом. Пропущен в очереди до
   той ступени.
5. Дальше по числу мест: №701, №1158, №763. (№764 выбыл тем же способом, что
   №676 — обход снят; №675 выбыл репоинтом на №1158, строка 15 таблицы выше —
   новая работа идёт под этим номером, не под старым; **№761 и №762 выбыли из
   этого списка тем же 2026-09-18 — оба уже ЗАКРЫТЫ, таблица выше называла
   №762 «открыт» по ошибке до правки интегратора 2026-09-26, найдено чтением
   против собственного `Статус:` строки реестра, не стражем — маркера у
   №762 нет, `check-novac-oracle-tax-link.py` такие строки не видит.**)
6. **№699/№705** — одна семья, чинить вместе.
7. **№879**, **№693**, **№684** — по остаточному принципу.

**Приёмка каждого пункта — снятый обход, а не зелёный тест.** Починка считается
принятой, когда окно 274 УДАЛИЛО обход из своего исходника и её сборка осталась
зелёной. Пока обход стоит, дефект «починен» только у нас.

---

## Чем список второго признака ограничен — названо, а не обойдено

Он получен так: 1062 строки реестра → 184 упоминают Карину → 65 говорят про её
сборку или несут маркер → **42 открытых** → чтение каждой (две модели, форма с
цитатой-уликой) → **моя сверка всех четырнадцати построчно**.

**Значит дефект, который её задевает, но её путей НЕ НАЗЫВАЕТ, в этот список не
попал — таких я не искал вовсе.** Список полон по признаку «названо в тексте
строки», а не по признаку «мешает Карине». Второе исполнимой меры не имеет и
закрывается только чтением всего реестра.

---

# ЧАСТЬ III. СЛОМАЕТ ПРИ ПРАВИЛЬНОЙ КАРИНЕ — признак судится СОБЫТИЕМ

## Второй разряд: «сегодня не ломает, сломает при ПРАВИЛЬНОЙ Карине»

**Вопрос владельца, стоявший в открытых, решён: разряд НУЖЕН, и вот почему умолчание «остаются в 221.1» оказалось хуже.** Четыре признака меряют СЕГОДНЯШНЮЮ Карину — ту, что многого ещё не проверяет. Дефект оракула, который она сегодня не встречает ПОТОМУ, что не реализовала соответствующую проверку, при её правильной реализации станет блокером в тот же день. Оставленный в общей куче 221.1, он неотличим от дефекта, который Карины не касается вовсе, — и всплывёт не тогда, когда его искали, а тогда, когда он уронит волну.

**ПРИЗНАК ДОПУСКА ВО ВТОРОЙ РАЗРЯД — проверяемый, а не «кажется важным». Нужны ОБА условия:**

1. **сегодня не ломает** ни один из четырёх признаков — то есть в первый разряд не проходит (иначе он просто блокер, и разряд не нужен);
2. **названо СОБЫТИЕ, после которого сломает**, и названо так, что наступление события видно машинно: волна плана, D-блок, строка приёмки. «Когда Карина станет строже» событием не является; «когда 274.9 закроет волну K4» — является.

Нет второго условия — строка остаётся в 221.1 и ждёт, как и было умолчанием. Второй разряд **не является** складом важного: он список отложенных срабатываний с названными будильниками.

**ЗАСЕЛЕНО ТЕМ, ЧТО ИЗМЕРЕНО МНОЮ, А НЕ ГРЕПОМ:**

| № | событие-будильник | чем сломает |
|---|---|---|
| [1106](221.1-bug-sweep.md) | реализация в Карине типизации статического вызова по ОБЪЯВЛЕННОМУ возвратному типу вместо списка имён | сегодня оракул принимает молча, след в std измерен (2 места); Карина, сделавшая это правильно, начнёт отвергать то, что оракул принимает, — и дифференциал (признак 2) станет красным |
| [1107](221.1-bug-sweep.md) | 274.9, волна по D133/D476: rvalue consume-типа во view-параметре | сегодня ни отказа, ни легализации; как только Карина реализует одну из двух сторон — расхождение исходов с оракулом, то есть снова признак 2 |
| [1108](221.1-bug-sweep.md) | 274.9, волна K4 (авто-`@cleanup` на consume-ПАРАМЕТРЕ) | операция эффекта, освобождающая ресурс, не забирает владение; сегодня двойного закрытия нет ИМЕННО потому, что D432 не покрывает параметры. K4 это меняет — и превращает строку в ОГРАНИЧЕНИЕ ПОРЯДКА: 1108 обязана быть закрыта ДО K4, иначе K4 вносит двойное освобождение |

**КАНДИДАТЫ — БЫЛИ, ПРОЧИТАНЫ 2026-09-25, НИ ОДИН НЕ ДОПУЩЕН.** Механическая
выборка (агент, haiku) дала среди 68 строк четырнадцать, чей ТЕКСТ упоминает
`Карин`/`novac`: 676, 684, 685, 691, 699, 705, 893, 959, 962, 971, 986, и три
уже заселённые. **Упоминание уликой не является** — этот файл уже обжёгся
ровно на этом: сверка 67 строк против исходника Карины дала 30 «попаданий»,
оказавшихся номерами D-блоков и родовыми именами узлов. Одиннадцать были
названы КАНДИДАТАМИ на чтение, а не строками разряда — прочитаны интегратором
построчно (агент spec-reader, sonnet, форма с цитатой-уликой; две цитаты
сверены интегратором лично до записи сюда):

| № | допуск | причина отказа | адрес |
|---|---|---|---|
| 676 | нет | уже в первом разряде (Часть I, №2), ЗАКРЫТ 2026-09-18 | `:280,303-374,569` |
| 684 | нет | уже в первом разряде (Часть I, №13, LSP), ЗАКРЫТ 2026-09-21 | `:288,532-541,573` |
| 685 | нет | это дефект САМОЙ Карины (`novac/src/emit_c`, её F65), а не оракула — правило «не чиним дефекты самой Карины» | `221.1-bug-sweep.md:871` |
| 691 | нет | уже стоит в таблице «Строки первого признака» Части I как кандидат первого разряда — второй трек дублировал бы учёт | `:95` |
| 699 | нет | уже в первом разряде (Часть I, №10, семья с 705) | `:288,498-512,572` |
| 705 | нет | уже в первом разряде (Часть I, №10, семья с 699) | `:288,498-512,572` |
| 893 | нет | сегодняшний блокер ТЕГА (К2, «БЛОКИРУЕТ ТЕГ: ДА»), найден дифференциалом — не отложенное срабатывание, событие не названо | `221.1-bug-sweep.md:1282` |
| 959 | нет | вердикт интегратора 2026-09-20 «ПО ВЛИЯНИЮ НА РЕЛИЗ КАРИНЫ: НЕ ДЕРЖИТ» уже вынесен и измерен — вопрос закрыт, не отложен | `221.1-bug-sweep.md:1353` |
| 962 | нет | текст описывает УЖЕ случившееся: novac сама сняла аналогичную проблему в своём коде (волна В6/№964) — событие в прошлом, не в будущем | `221.1-bug-sweep.md:1351` |
| 971 | нет | параллельный дефект самой Карины заведён отдельным номером в её очереди (№970, окно 274) — правило «не чиним дефекты самой Карины»; для оракульной стороны событие не названо | `221.1-bug-sweep.md:1364` |
| 986 | нет | «novac» в тексте — случайная деталь замера (какие деревья сравнивались), не причинная связь; сам дефект — сбой рантайма при завершении процесса, к Карине текст его не привязывает | `221.1-bug-sweep.md:1375` |

Второй разряд остаётся заселён только тремя строками (1106/1107/1108) выше.
Новый кандидат сюда попадает так же — упоминанием не является, только
прочитанным событием-будильником.

---

# ОБЩЕЕ

## Не делаем (границы)

* **не заводим второй реестр** — строки живут в 221.1, здесь только вид;
* **не тащим сюда работы плана 221** (Ф.2 дистрибуция, Ф.3 дока, Ф.4 тег): они
  заморожены вместе с ним, и их разморозка — отдельное решение владельца;
* **не чиним оракул «заодно»**: дефект, не проходящий проверку четырьмя пунктами
  выше, отсюда исключается, даже если он рядом и виден.

* **Не чиним дефекты самой Карины** — они очередь окна 274, и путать эти две
  очереди значит отнимать у неё работу и делать её хуже.
* **Не переносим сюда содержимое строк реестра** — здесь план, дом строки там.
* **Не заводим меру «сколько обходов осталось» базой** до того, как появится
  связь маркеров со списком: база на рукописное число мерила бы список, а не
  предмет.
* **Не трогаем 263 пробела подмножества**, которыми Карина сегодня не
  компилирует саму себя: это её работа и её план.

---

## Открытые вопросы

* **кому:** владелец. **Вопрос БЫЛ:** нужен ли второй разряд — «не ломает
  сегодня, но сломается, когда Карина реализует проверку правильно»?
  **РЕШЕНО ИНТЕГРАТОРОМ 2026-09-15, умолчание отменено:** разряд заведён, у
  него признак допуска из двух условий (сегодня не ломает + названо
  МАШИННО ВИДИМОЕ событие-будильник), заселены №1106, №1107, №1108.
  **Что осталось владельцу:** отменить это решение, если оно не нужно, —
  тогда разряд снимается и три строки живут в 221.1 по прежнему умолчанию.
  **Почему не стал ждать:** срок вопроса стоял «до первой волны по 274.9
  K4», а №1108 оказалась ограничением ПОРЯДКА для этой самой волны —
  ответ, полученный к сроку, пришёл бы позже, чем нужен.

* **кому:** владелец. **Вопрос:** куда заводить дефекты САМОЙ Карины (не оракула).
  `/novac-recheck` велит находки novac писать в текущий план, а в реестр 221.1 —
  «дефект ОРАКУЛА, наоборот, всегда»; практика реестра шире (168 строк упоминают
  `novac`, включая дефекты стражей). Сейчас два одинаковых по классу дефекта лежат
  по разным адресам: №1057 в реестре, `println('z')` в плане 274.8.
  **Умолчание, пока молчит:** оба адреса остаются как есть, ничего не переносится.
  **Срок:** до первого переноса — то есть до момента, когда кто-то решит свести их
  вместе.

* **кому:** владелец. **Вопрос БЫЛ:** №684 — дефект LSP, мешающий РАБОТАТЬ над
  Кариной, а не собирать её. Оставлять в этом списке или вынести в инструменты?
  **СРОК НАСТУПИЛ 2026-09-21 — №684 ЗАКРЫТ** (goto-definition/hover/type-definition
  интегратором, rename/document-highlight помощником, слито в main). Умолчание
  сработало само: вопрос снят, отдельного слова владельца не требуется.
* **кому:** интегратор (сам). **Вопрос:** №708 несёт маркер обхода, но текст
  строки про сборку не говорит — настоящий ли это налог. **Умолчание:** в список
  не включён. **Срок:** до шага 1.
  **СРОК НАСТУПИЛ 2026-09-25 (шаг 1 — страж `check-novac-oracle-tax-link.py`) —
  №708 НЕ ВКЛЮЧЁН, умолчание сработало.** Прочитан текст 221.1 №708: это не баг
  оракула, а нереализованная возможность (`report()`/`cause()`, D463, ждут
  №680) — сам обход «рабочий и опирается на нормативный формат», «БЛОКИРУЕТ
  ТЕГ: НЕТ», чинится планом 173.4 Ф.3, не этим. Вписан в `ALLOWED_EXTRA` стража
  с этой причиной, вместе с №700 (дефект самой Карины, тот же класс исключения).

---

