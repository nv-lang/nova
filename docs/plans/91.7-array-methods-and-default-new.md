# Plan 91.7 — Array methods cleanup + canonical `.new()` (D180)

> **Подплан Plan 91** ([91-stdlib-mvp-for-0.1.md](91-stdlib-mvp-for-0.1.md)).
> Статус: 🔴 OPEN (не начат).

## Мотивация

Текущий API массивов `[]T`:
- mut-методы возвращают `()` — нельзя fluent chain (`arr.push(1).push(2)` не работает)
- `@slice(from, to)` дублирует синтаксис `arr[a..b]` (Plan 96)
- sort/min/max/binary_search определены **только для `[]int`** — implementation shortcut из Plan 91.3
- Нет канонического конструктора `[]T.new()` / `[]T.with_capacity(n)` (приходится `let mut a []T = []; a.reserve(n)`)

Дополнительно — отсутствует **конвенция default-конструкторов**. Сейчас:
- `StringBuilder.new()` — explicit
- `HashMap.new()` — explicit
- `int`, `bool`, `str`, `[]T`, `Option` — нет `.new()` вообще, юзер пишет `0`, `false`, `""`, `[]`, `None` напрямую

## Scope

### Часть A — `-> @` для mut методов массива

Все мутирующие методы `[]T` должны возвращать `@` (Plan 77/D131 fluent API),
чтобы поддерживать chaining:

| Метод | Было | Стало |
|---|---|---|
| `@push(v T)` | `-> ()` | `-> @` |
| `@reserve(extra int)` | `-> ()` | `-> @` |
| `@truncate(n int)` | `-> ()` | `-> @` |
| `@fill(v T)` | `-> ()` | `-> @` |
| `@copy_from(src readonly []T)` | `-> ()` | `-> @` |
| `@extend_from(src readonly []T)` | `-> ()` | `-> @` |
| `@insert_from(i int, src readonly []T)` | `-> ()` | `-> @` |
| `@copy_within(src_from, dst_from, len)` | `-> ()` | `-> @` |

`@pop()` остаётся `-> Option[T]` (возвращает значение, не self).
`@get(i)` остаётся `-> Option[T]`.

**Реализация:** C-функции в `array.h` остаются void; на Nova-стороне можно
обёртку, либо emit_c.rs учитывает `-> @` flag для прямых array builtin-вызовов.

### Часть B — удалить `@slice`

`@slice(from, to) -> []T` дублирует `arr[a..b]` (Plan 96, zero-copy view).
Один способ — лучше. Удаление: декларации в `vec.nv` / runtime_registry + всех
вызовов в stdlib.

### Часть C — generic sort/min/max/binary_search

Текущая реализация в `std/sort.nv` — только для `[]int` (Plan 91.3 shortcut).
Распространить на `[T Ord]`:

```nova
fn[T Ord] []T mut @sort() -> @
fn[T Ord] []T @binary_search(target T) -> Option[int]
fn[T Ord] []T @min() -> Option[T]
fn[T Ord] []T @max() -> Option[T]
fn[T] []T mut @sort_by(cmp fn(T, T) -> Ordering) -> @
```

Бэкенд: monomorphization per T через Plan 101 (`fn[T Ord]` префикс). `Ord` —
protocol с `@compare(other T) -> Ordering`.

**Совместимость:** новый generic `fn[T Ord] []T @sort()` НЕ ломает existing
concrete `fn []int @sort()` — overload resolution выбирает specific over generic.
После добавления generic версии — concrete `[]int` можно удалить (или оставить
как fast-path).

### Часть D — канонические `.new()` через Nova-код (D180)

Все определения **в Nova-коде** stdlib, **без compiler magic**:

```nova
// std/runtime/numeric.nv (или новый файл)
export fn int.new()  -> Self => 0
export fn u8.new()   -> Self => 0
export fn u16.new()  -> Self => 0
export fn u32.new()  -> Self => 0
export fn u64.new()  -> Self => 0
export fn i8.new()   -> Self => 0
export fn i16.new()  -> Self => 0
export fn i32.new()  -> Self => 0
export fn i64.new()  -> Self => 0
export fn f32.new()  -> Self => 0.0
export fn f64.new()  -> Self => 0.0
export fn bool.new() -> Self => false

// std/runtime/string.nv
export fn str.new() -> Self => ""

// std/prelude/core.nv
export fn[T] Option[T].new() -> Self => None

// std/collections/vec.nv (или std/prelude/collections.nv)
export fn[T] []T.new() -> Self => []
export fn[T] []T.with_capacity(n int) -> Self {
    let mut a []T = []
    a.reserve(n)
    a
}
```

**Зависимость:** парсер должен поддерживать `[]T` как receiver в static-call.
Сейчас работает для `Option[T]`, `str`, `StringBuilder` (named types). Для `[]T`
(`TypeRef::Array`) — нужно проверить парсер; вероятно single small feature.

**Не делаем auto-generation:**
- `char`, `Result[T, E]` — нет канонического default (skip)
- user records / sum types / consume types — explicit `.new()` (forced discipline)
- protocols / fn types / external — N/A

### Часть E — D180 convention в спеке

Новый D-block в `spec/decisions/01-foundations.md` или `02-types.md`:

```markdown
## D180. Convention: canonical `.new()` constructors

**Status:** convention (not enforcement).

stdlib предоставляет `.new()` для типов с **единственным очевидным** default:
- числовые (`int`, `u8..u64`, `i8..i64`, `f32/f64`) → `0` / `0.0`
- `bool` → `false`
- `str` → `""`
- `[]T` → `[]` (empty array; works for any T including consume)
- `Option[T]` → `None`

Также `[]T.with_capacity(n)` — с pre-allocated capacity.

**Для своих типов** разработчик пишет `.new()` явно. Компилятор НЕ автогенерирует
для user records / sum types / consume types. Это design discipline:

1. Явный конструктор виден в `nova doc` и IDE.
2. Имена кодируют намерение (`User.new(name, email)` vs `User.guest()`).
3. Валидация инвариантов в момент создания.
4. Эволюция типа: добавление поля заставляет обновить конструктор → good failure.

**НЕ имеют canonical `.new()`** (compile error при попытке):
- `char` (`'\0'` сомнителен)
- `Result[T, E]` (`Ok` или `Err`? ambiguous)
- user-defined types — по конвенции этого блока
- protocols, fn types, external/opaque
```

## Затронутые файлы (предварительно)

**Компилятор (nova-p91 worktree):**
- `compiler-codegen/src/codegen/runtime_registry.rs` — обновить return-type 8 array методов; удалить slice
- `compiler-codegen/src/codegen/emit_c.rs` — учёт `-> @` для array builtin-вызовов; emit semantic для возврата receiver pointer
- `compiler-codegen/src/parser/mod.rs` — поддержка `[]T` как receiver в static-call (если ещё нет) + поддержка primitive-типов (`int.new()` уже работает через path-resolve — проверить)
- `compiler-codegen/src/types/mod.rs` — diagnostic E_DEFAULT_NEW_UNAVAILABLE для user types при попытке резолва `.new()` без декларации (см. Q4/Q5)

**Runtime (nova-p91 worktree):**
- `compiler-codegen/nova_rt/array.h` — без изменений (C-функции остаются void; обёртка на Nova-стороне emit'ит pointer)

**Stdlib (nova-p91 worktree):**
- `std/runtime/numeric.nv` (новый или расширить existing) — 12 числовых `.new()`
- `std/runtime/string.nv` — `str.new()`
- `std/prelude/core.nv` — `Option[T].new()`
- `std/collections/vec.nv` — `[]T.new()`, `[]T.with_capacity(n)`, **удалить** `@slice` декларации
- `std/sort.nv` — generic версии sort/min/max/binary_search
- `std/prelude.nv` — bump version (v12), обновить export imports для новых `.new()` если нужно

**Поиск использований `@slice` для замены на `[a..b]`:**
- `grep -rn "\.slice(" std/ nova_tests/` — заменить все на slice-синтаксис

---

## Обновления спеки, D, Q (main nova repo)

**Работа в `D:/Sources/nv-lang/nova` (branch `main`), НЕ в worktree.**

### D-block: D180 (новый)

**Файл:** `spec/decisions/02-types.md` (либо `01-foundations.md` — выбрать по близости тематики; type defaulting — это про типы).

**Content (полный текст блока):**

```markdown
## D180. Canonical `.new()` constructors (convention)

**Статус:** convention (stdlib provides, compiler does NOT auto-generate).

stdlib предоставляет `.new()` для типов с **единственным очевидным** default-значением:

| Тип | `.new()` возвращает | Файл декларации |
|---|---|---|
| `int`, `u8`–`u64`, `i8`–`i64` | `0` | `std/runtime/numeric.nv` |
| `f32`, `f64` | `0.0` | `std/runtime/numeric.nv` |
| `bool` | `false` | `std/runtime/numeric.nv` |
| `str` | `""` | `std/runtime/string.nv` |
| `[]T` (для любого T) | `[]` (empty array) | `std/collections/vec.nv` |
| `Option[T]` | `None` | `std/prelude/core.nv` |

Также `[]T.with_capacity(n int) -> Self` — empty с pre-allocated capacity.

**Для своих типов** разработчик пишет `.new()` явно. Компилятор НЕ автогенерирует
для user records / sum types / consume types. Это design discipline:

1. Явный конструктор виден в `nova doc` и IDE.
2. Имена кодируют намерение (`User.new(name, email)` vs `User.guest()`).
3. Валидация инвариантов в момент создания.
4. Эволюция типа: добавление поля заставляет обновить конструктор — good failure.

**НЕ имеют canonical `.new()`** (compile error при попытке):
- `char` (`'\0'` сомнителен)
- `Result[T, E]` (`Ok` или `Err`? ambiguous)
- tuples (`(int, str)`, etc. — не делаем mvp)
- user-defined types — по конвенции этого блока
- protocols, fn types, external/opaque

**Diagnostic при отсутствии:**

```
error: type `MyRecord` has no `.new()` constructor
hint: define explicitly — fn MyRecord.new() -> Self => { field1: ..., ... }
note: see D180 convention — only canonical types (int/bool/str/[]T/Option) have auto `.new()`
```

### Связь
- [D26](#d26-...) — prelude auto-availability
- [D131](03-syntax.md#d131-...) — consume types / fluent API
- [Plan 91.7](../../docs/plans/91.7-array-methods-and-default-new.md) — implementation
```

### D-block: D181 (новый)

**Файл:** `spec/decisions/02-types.md`.

**Title:** «Array methods — `-> @` mut chain + canonical static constructors».

**Content:**
- 8 mut-методов `[]T` (push/reserve/truncate/fill/copy_from/extend_from/insert_from/copy_within) возвращают `@` для fluent chaining (D131).
- `@slice(from, to)` удалён — `arr[a..b]` (Plan 96 slice syntax) единственный путь.
- Generic `fn[T Ord] []T @sort()` / `@sort_by` / `@binary_search` / `@min` / `@max` — расширение Plan 91.3 concrete `[]int` на любой `Ord`-тип.
- Связь: D131 (fluent), D72 (bounds), D180 (canonical .new()).

### D-block: D182 (новый)

**Файл:** `spec/decisions/02-types.md`.

**Title:** «`[]T.new()` и `[]T.with_capacity(n)` — primitive type static-calls».

**Content:**
- `[]T.new() -> Self => []`
- `[]T.with_capacity(n int) -> Self` — empty + reserve(n)
- Parser support: `TypeRef::Array` принимается как receiver в static-call (как `Named` types).
- Связь: D180.

### Q-block обновления

**Файл:** `spec/open-questions.md`.

Найти и обновить:
- **Q-array-api** (если есть) — добавить note про D181/D182.
- Если Q-блока про array API нет — создать новый раздел «Q-array-api — array `[]T` canonical API» со статусом ✅ ЗАКРЫТО (2026-MM-DD) и ссылками на D180/D181/D182.

### spec/decisions/<file>.md index

В таблице D-blocks в начале файла добавить три строки для D180/D181/D182 со ссылками.

### spec commits

Spec изменения в **отдельный коммит** в main nova repo, **без push**.

---

## Тесты (плановые)

**Путь:** `nova_tests/plan91_7/` (новый каталог).

### Позитивные тесты

| Файл | Что тестирует |
|---|---|
| `array_mut_chain_test.nv` | `arr.push(1).push(2).reserve(10).truncate(2)` — `-> @` chain работает |
| `array_extend_chain_test.nv` | `dst.extend_from(src).extend_from(other)` |
| `array_copy_from_chain_test.nv` | `dst.copy_from(src).fill(0)` |
| `array_new_test.nv` | `[]int.new() == []`, `[]int.new().len() == 0`, для `[]str`, `[]u8`, etc. |
| `array_with_capacity_test.nv` | `[]int.with_capacity(16).capacity() >= 16 && .len() == 0` |
| `primitive_new_test.nv` | `int.new() == 0`, `f64.new() == 0.0`, `bool.new() == false`, для всех 12 numeric+bool |
| `str_new_test.nv` | `str.new() == ""`, `str.new().len() == 0` |
| `option_new_test.nv` | `Option[int].new()` → `None`; для разных T |
| `sort_generic_test.nv` | sort/min/max/binary_search на `[]int`, `[]str`, `[]u64`, `[]i32` |
| `sort_by_chain_test.nv` | `arr.sort_by(cmp).first()` — chain после `-> @` |

### Негативные тесты (compile errors)

| Файл | Что должно дать compile error |
|---|---|
| `user_record_new_err.nv` | `type T { x int }; let v = T.new()` → E_DEFAULT_NEW_UNAVAILABLE, hint про D180 |
| `user_sum_new_err.nv` | `type T \| A \| B; let v = T.new()` → compile error |
| `user_consume_new_err.nv` | `type T consume { x int }; let v = T.new()` → compile error |
| `char_new_err.nv` | `let c = char.new()` → compile error (нет canonical default) |
| `result_new_err.nv` | `let r = Result[int, str].new()` → compile error (ambiguous Ok/Err) |
| `slice_method_err.nv` | `arr.slice(0, 3)` → compile error «method @slice removed, use arr[0..3]» |

### Регрессии

- `nova test plan73 plan77 plan91 plan100_2 plan100_5 plan100_6 plan108 plan109` — все PASS
- `nova test syntax` — без новых failures
- `nova test nova_tests/sort` — sort_basic.nv (Plan 91.3) PASS (generic не ломает concrete)
- `nova test nova_tests/protocols` — display etc. PASS

---

## Документация (обновить после implementation)

**В nova-p91 worktree (`plan-91-stdmvp` branch):**

1. `docs/project-creation.txt` — секция «Plan 91.7 closed YYYY-MM-DD»:
   - что сделано (5 частей A–E)
   - D180/D181/D182 spec
   - тесты результат
   - коммит-хэш

2. `docs/simplifications.md` — секция «Plan 91.7 — array API + canonical .new()»:
   - почему `-> @` для mut (fluent chain pattern)
   - почему удалили `@slice` (один способ — `arr[a..b]`)
   - почему `.new()` через Nova-код, а не compiler-magic (всё видно в исходниках)
   - D180 convention rationale (discipline by design)

**В nova-private repo (`main`):**

3. `D:/Sources/nv-lang/nova-private/discussion-log.md` — секция:
   - дата + контекст обсуждения (array API discipline, design choices)
   - decision summary: D180/D181/D182, что включено, что не включено (char, Result, user types)
   - lessons learned

---

## Acceptance criteria (расширенные)

### Реализация

- [ ] **A:** 8 mut-методов `[]T` декларированы `-> @`; `arr.push(1).push(2).reserve(10)` компилируется
- [ ] **B:** `@slice` удалён из vec.nv + runtime_registry; все `.slice(a, b)` в stdlib заменены на `[a..b]`
- [ ] **C:** Generic `fn[T Ord] []T @sort()` / `@sort_by` / `@binary_search` / `@min` / `@max` работают для `[]int`, `[]str`, `[]u64`
- [ ] **D:** 14 канонических `.new()` declared в stdlib:
  - `int`/`u8`/`u16`/`u32`/`u64`/`i8`/`i16`/`i32`/`i64` (9 числовых)
  - `f32`/`f64` (2)
  - `bool` (1)
  - `str` (1)
  - `Option[T]`, `[]T` (2 generic)
- [ ] **D parser:** `[]T.new()` парсится корректно; primitive `int.new()` парсится (если ещё не работает — отдельный коммит)
- [ ] **D diagnostic:** E_DEFAULT_NEW_UNAVAILABLE для non-canonical типов; текст содержит hint «see D180»

### Spec / D / Q

- [ ] **D180** написан и закоммичен в `spec/decisions/02-types.md` (main nova repo)
- [ ] **D181** (array mut `-> @` + slice removal + generic sort) написан
- [ ] **D182** (`[]T.new()` parser support) написан
- [ ] Index в начале `spec/decisions/02-types.md` обновлён (3 строки)
- [ ] **Q-array-api** обновлён или создан со статусом ✅ ЗАКРЫТО
- [ ] `Plan 91.6` уже содержит ссылки на D179 — добавить аналогичные cross-refs для D180/D181/D182 в Plan 91.7

### Тесты

- [ ] 10+ позитивных тестов в `nova_tests/plan91_7/` — все PASS
- [ ] 6 негативных тестов — все дают expected compile error
- [ ] Регрессия: `nova test plan73 plan77 plan91 plan100_2 plan100_5 plan100_6 plan108 plan109 syntax` — без новых failures
- [ ] Full `nova test` regression — baseline или лучше (без новых FAIL)

### Документация

- [ ] `docs/project-creation.txt` обновлён (nova-p91 worktree)
- [ ] `docs/simplifications.md` обновлён (nova-p91 worktree)
- [ ] `nova-private/discussion-log.md` обновлён (nova-private repo, main branch)
- [ ] Parent `docs/plans/91-stdlib-mvp-for-0.1.md` — статус 91.7 переключён 🔴 OPEN → ✅ ЗАКРЫТ + дата
- [ ] **Финальный статус-блок в этом файле** — секция «## Статус закрытия» в конце 91.7-array-methods-and-default-new.md: дата, коммит-хэши, тестовая статистика (X/Y PASS), что вошло / что отложено, lessons learned (если есть)

### Коммиты и merge

- [ ] Все изменения закоммичены атомарно (компилятор / stdlib / тесты / docs — отдельные коммиты)
- [ ] **НЕ push** в origin (локально только)
- [ ] **НЕ merge** в локальный main без подтверждения пользователя
- [ ] Ветка `plan-91-stdmvp` остаётся ветвью; merge решается отдельно

---

## Открытые вопросы (требуют решения ДО старта реализации)

1. **Q1:** `[]T.with_capacity(n)` — реализация через `let mut a []T = []; a.reserve(n); a` (Nova-side), или нужен C-stub? **Default: Nova-side** (если работает после Part D parser support).
2. **Q2:** Generic `Ord` bound — protocol уже существует? Проверить `std/prelude/protocols.nv`. Если нет — создать (`@compare(other T) -> Ordering`) — это **расширяет scope 91.7**; вынести Part C в 91.7c если надо.
3. **Q3:** `char.new()` — точно NOT auto? **Default: NOT auto** (compile error, в духе D180).
4. **Q4:** Текст diagnostic E_DEFAULT_NEW_UNAVAILABLE — финальная формулировка (см. drafts выше в D180).
5. **Q5:** Где emit diagnostic — `types/mod.rs` `BoundCtx::check_call_argbind` при резолве `MyRecord.new()` без декларации; integrated с pre-existing «method not found» path.
6. **Q6 (новый):** Какой D-block номер брать? Текущий последний — D179. Брать D180/D181/D182 sequentially. Перед коммитом проверить `MEMORY.md` (project-spec-dblock-numbering) на коллизии.

---

## Статус закрытия

> **Шаблон.** Заполнить при закрытии работ.

**Дата закрытия:** YYYY-MM-DD

**Коммиты:**
- nova-p91 (`plan-91-stdmvp`): `<hash>` — compiler/stdlib impl
- nova-p91 (`plan-91-stdmvp`): `<hash>` — tests
- nova-p91 (`plan-91-stdmvp`): `<hash>` — docs (project-creation + simplifications + parent index update)
- nova (`main`): `<hash>` — D180/D181/D182 spec + Q-array-api
- nova-private (`main`): `<hash>` — discussion-log

**Тесты:**
- plan91_7 positive: X/Y PASS
- plan91_7 negative: X/Y expected compile errors fired
- Регрессия: full `nova test` — baseline или лучше

**Что вошло:**
- Часть A (mut `-> @`): ✅
- Часть B (delete @slice): ✅
- Часть C (generic sort/min/max): ✅ / 🟡 deferred to 91.7c
- Часть D (canonical .new()): ✅
- Часть E (D180/D181/D182): ✅

**Что отложено / followup:**
- `[список]`

**Lessons learned:**
- `[если есть нетривиальные наблюдения]`

**Acceptance criteria check:** все пункты выше отмечены [x].
