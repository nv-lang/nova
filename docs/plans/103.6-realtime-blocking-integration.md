// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 103.6 — `realtime { }` / `blocking { }` integration для sync primitives

> **Статус:** ✅ ЗАКРЫТ 2026-05-27 (Plan 103.6 Ф.0-Ф.6 complete)
> **Приоритет:** P1 (closure для D64 contract на sync primitives)
> **Оценка:** ~1.5 dev-day
> **Зависимости:** Plan 103.2-103.5 ✅ (нужны типы для enforcement); Plan 16 ✅ (D63/D64 capability enforcement infrastructure); Plan 83.3 ✅ (Blocking effect + nogc enforcement в blocking-body)
> **Master:** [Plan 103](103-sync-primitives-spec-formalization.md). Design M12 применимо.
>
> **Recommended model:** Sonnet 4.6 HIGH + Thinking ON. Если type-checker integration (Ф.2) или annotation sweep ambiguity (Ф.1) застрянут — interrupt, переключить только эти фазы на Opus 4.7.
>
> **Conventions (M14b/c/d из master):**
> - Memory ordering enum — **`MemOrdering`** (не `Ordering` — collision с prelude).
> - Stability marker — `#stable(since = "0.1")` (consistency с baseline sync.nv).
> - Diagnostic infra — `compiler-codegen/src/diag.rs` напрямую (`Diagnostic` + `Suggestion` + `Applicability::MachineApplicable`).
>
> **Nova edge note (M12):** type-checker `realtime { }` ban на park-ing
> sync methods — **unique vs Go/Rust/Java/Kotlin** (никто из них не
> enforce этого статически). Production-grade enforcement требует
> precision в annotation sweep + cross-function inference (или
> conservative defaults).

## Worktree setup (pre-flight)

**Convention:** постоянный worktree `nova-p103-rt-blocking` (descriptor `rt-blocking`).

```bash
git worktree add -b plan-103.6 ../nova-p103-rt-blocking main

/d/Sources/nv-lang/nova/.claude/hooks/worktree-register.sh set "d:/Sources/nv-lang/nova-p103-rt-blocking"

cp -r d:/Sources/nv-lang/nova/compiler-codegen/nova_rt/libuv \
     d:/Sources/nv-lang/nova-p103-rt-blocking/compiler-codegen/nova_rt/libuv
rm -rf d:/Sources/nv-lang/nova-p103-rt-blocking/compiler-codegen/nova_rt/libuv/.git

cp -r d:/Sources/nv-lang/nova/target/libuv-cache \
     d:/Sources/nv-lang/nova-p103-rt-blocking/target/libuv-cache 2>/dev/null || true

export NOVA_GC_LIB_DIR=d:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/lib
export NOVA_GC_INCLUDE_DIR=d:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/include
```

## Model / Thinking mode per phase

| Phase | Thinking | Why |
|---|---|---|
| Ф.0 baseline audit | OFF | reading Plan 16/83.3 modules |
| Ф.1 annotation sweep на sync.nv (~85 methods) | **ON** | classification decisions (`#parks` vs `#wakes` vs `#realtime` для edge cases) |
| Ф.2 type-checker enforcement (~6 error codes) | **ON** | integration с existing capability check, function-level propagation logic |
| Ф.3 lint warnings | OFF | mechanical pattern detection |
| Ф.4 tests (≥10 pos + ≥15 neg) | OFF | explicit list |
| Ф.5 D172 draft (full matrix realtime/blocking × method) | **ON** | spec writing — large table |
| Ф.6 close | OFF | trivial |

**Fallback:** stuck >1 retry на Ф.1/Ф.2/Ф.5 → escalate на Opus 4.7.

**Parallelism note:** 103.6 GATED на 103.2 ✅ + 103.3 + 103.4 + 103.5
(нужны все типы для annotation sweep). Ready-to-launch только после
закрытия всех четырёх.

## Зачем

D64 (`realtime { }`) гарантирует non-suspension execution в block —
для real-time embedded / GC-pause-free hot loops. D50 §4 (Plan 83.3)
`blocking { }` — leaf FFI/syscall offload. Sync primitives имеют
**mixed contract:**

| Метод | Может park? | Allowed в `realtime { }`? | Allowed в `blocking { }`? |
|---|---|---|---|
| **AtomicX.load/store/swap/CAS/fetch_*** | ❌ leaf | ✅ | ✅ |
| **fence(MemOrdering)** | ❌ leaf | ✅ | ✅ |
| **Mutex.try_lock / try_lock_for** | ❌ no-wait или timer | ✅ (try_lock); ⚠ try_lock_for использует libuv timer (NO в realtime) | ⚠ try_lock_for NO; try_lock — leaf-like, but mu.lock внутри ещё leaf |
| **Mutex.lock / unlock / with_lock** | ✅ park possible | ❌ | ⚠ unlock — leaf; lock — park possible → NO |
| **RwLock.read / write / with_read / with_write** | ✅ park | ❌ | ❌ |
| **ReentrantMutex.lock** | ✅ park | ❌ | ❌ |
| **Semaphore.acquire / with_permit** | ✅ park | ❌ | ❌ |
| **Semaphore.try_acquire / release** | ❌ leaf-like | ✅ try; ⚠ release wakes waiters → NO в strict realtime | ✅ try; release ⚠ |
| **Barrier.wait** | ✅ park | ❌ | ❌ |
| **CountDownLatch.await / count_down (last one wakes)** | ⚠ await parks; count_down only wakes если reaches 0 | await ❌; count_down ⚠ если может trigger wake | await ❌ |
| **Condvar.wait / wait_for / wait_until / notify_one / notify_all** | wait parks; notify wakes | wait ❌; notify ⚠ | wait ❌; notify ⚠ |
| **Once.call_once / run** | ✅ park (если другой fiber running) | ❌ | ❌ |
| **Once.done** | ❌ leaf wake | ⚠ wakes waiters | ⚠ |
| **OnceCell.get / set / take / is_initialized** | ❌ leaf | ✅ | ✅ |
| **OnceCell.get_or_init** | ✅ park (если init concurrent) | ❌ | ❌ |
| **Lazy.force** | ✅ park | ❌ | ❌ |

Без type-checker enforcement пользователь пишет `realtime { mutex.lock() }`
→ compiles, runtime может suspend → violates D64 guarantee. Plan 103.6
fixes через **compile-time errors**.

**M12 (master):** type-checker enforce; runtime-only check (Go/Rust/Java
не делают этого вообще) недостаточно.

## Scope

### In scope

- **Type-checker rules** в `compiler-codegen/src/types/check_realtime.rs`
  (или extend existing Plan 16 module):
  - При проверке body внутри `realtime { ... }` block, walk method
    calls; reject любые из **forbidden list** (table above) с
    proper error code.
  - Same для `blocking { ... }` body — narrower forbidden list (только
    park-ing, не leaf-wake).
- **Method annotations:** добавить marker в `std/runtime/sync.nv`
  для каждого external fn:
  - `#realtime` — leaf method, allowed в realtime/blocking.
  - `#parks` — может park, forbidden в realtime/blocking.
  - `#wakes` — leaf wake (no park self, но wakes others); contextual
    decision: realtime forbids (wake может interact с scheduler),
    blocking allows (wake — operation на shared state).
  - Default (no annotation) → conservative `#parks` assumed.
- **6 error codes** (`compiler-codegen/src/diag.rs` (`Diagnostic` + `Suggestion` + `Applicability`), machine-applicable suggestions):
  - `E_REALTIME_SYNC_PARK`: «`{method}` может приостановить fiber;
    запрещено в `realtime { }`. Suggest: use `try_*` variant»
  - `E_REALTIME_SYNC_WAKE`: «`{method}` может разбудить waiter (тоже
    запрещено в realtime); suggest: defer wake outside realtime block»
  - `E_BLOCKING_SYNC_PARK`: «`{method}` может park fiber; в `blocking { }`
    leaf-execution only — переместить вне блока»
  - `E_REALTIME_NESTED_SYNC_VIA_FN`: «функция `{name}` использует
    park-ing sync; нельзя вызвать из realtime — отметить `#parks` или
    refactor»
  - `W_REALTIME_TRY_LOCK_FOR_TIMER`: «`try_lock_for(Duration)` использует
    libuv timer (потенциально suspend); если duration = 0 — use
    `try_lock()` instead»
  - `W_BLOCKING_NOTIFY_RISK`: «`notify_*` в `blocking { }` может wake
    fiber на blocking thread (M:N safety risk); use AtomicBool flag +
    fence pattern»
- **Spec contract** in D172 (Plan 103.7): explicit matrix realtime/
  blocking × method.
- **Test suite:** 12+ negative tests (one per error code × variations).

### Out of scope

- **Refactoring sync примитивов** на realtime-friendly (lock-free)
  alternative — beyond scope; Plan 103.6 just enforces existing
  semantics correctly.
- **Runtime check fallback** (если type-checker missed something) —
  reasonable assumption: nogc/realtime enforcement в bootstrap
  достаточно strict для catch'а; runtime check добавлен в Plan 16,
  reused.
- **Cross-function effect analysis** (whole-program — функция X
  использует park-ing → caller в realtime forbidden) — partial в Plan
  16; full inference — отдельный large plan (not 103.6).

## API design — annotations

Добавить в `std/runtime/sync.nv` (примеры; полный sweep — Ф.1):

```nova
// AtomicI64
#realtime
#stable(since = "0.1")
export external fn AtomicI64 @load(ord MemOrdering) -> int64
// ... все atomic ops — #realtime

// Mutex
#parks
#stable(since = "0.1")
export external fn Mutex mut @lock()

#realtime
#stable(since = "0.1")
export external fn Mutex mut @try_lock() -> bool

#parks  // libuv timer = potential suspend
#stable(since = "0.1")
export external fn Mutex mut @try_lock_for(timeout Duration) -> bool

#wakes  // leaf wake (no park self)
#stable(since = "0.1")
export external fn Mutex mut @unlock()
```

Annotation parser: extend existing #-attr lexer (Plan 33.x уже
парсит `#opaque`/`#fuel(N)`/`#stable(since)` etc.).

## Industry comparison

| Аспект | Nova 103.6 | Go | Rust | Java | Kotlin |
|---|---|---|---|---|---|
| **Compile-time `realtime` ban на sync** | ✅ M12 (unique to Nova) | ❌ | ❌ | ❌ | ❌ |
| **Realtime semantics primitive** | ✅ D64 | ❌ | ❌ (real-time crates: third-party) | ❌ (real-time JVM: third-party) | ❌ |
| **Blocking-thread isolation primitive** | ✅ Plan 83.3 D50 §4 | ⚠ goroutines auto-handle | ⚠ `spawn_blocking` Tokio | ⚠ `Dispatchers.IO` | ⚠ same |
| **Method-level effect annotation** | ✅ `#parks/#wakes/#realtime` | ❌ | ❌ | ❌ | ❌ |

**Nova edge:** unique compile-time enforcement. Industry relies на
documentation / runtime crashes / accidental drops.

## Фазы

### Ф.0 — Baseline audit (~0.1d)

- Plan 16 D63/D64 enforcement existing scope (`check_capability.rs`?).
- Plan 83.3 `nogc` + `Blocking`-body enforcement scope (где hooks для
  call-site check?).
- Plan 33.x `#`-attr parser — existing or need extension?

### Ф.1 — Annotation sweep на sync.nv (~0.3d)

- Walk all external fn declarations в `std/runtime/sync.nv` + new ones
  added by 103.2-103.5.
- Annotate каждый: `#realtime` / `#parks` / `#wakes`.
- Default fallback: если annotation missing → conservative `#parks`
  (errs on safe side, type-checker rejects in realtime/blocking).

Acceptance: ≥50 declarations annotated (12 atomics × 13 ops + Mutex
9 + RwLock 12 + Semaphore 8 + Barrier 6 + CountDownLatch 5 + Condvar 7
+ Once 5 + OnceCell 6 + Lazy 4 = ~85 annotations).

### Ф.2 — Type-checker enforcement (~0.5d)

- Extend `check_realtime.rs` / `check_blocking.rs` (Plan 16/83.3 modules):
  - On entering `realtime { }` / `blocking { }` body, push capability
    flag.
  - On method call resolution: check if method has `#parks` / `#wakes`
    annotation.
  - Reject с appropriate error code if conflict.
  - Propagate through user-defined functions: if function calls
    `#parks` method, function inherits `#parks` (conservative); if
    function attempts use in realtime → `E_REALTIME_NESTED_SYNC_VIA_FN`.
- Error code registration (6 codes; `compiler-codegen/src/diag.rs` (`Diagnostic` + `Suggestion` + `Applicability`)).

Acceptance: type-checker correctly rejects all forbidden combinations
(test matrix Ф.4).

### Ф.3 — Lint warnings (~0.2d)

- `W_REALTIME_TRY_LOCK_FOR_TIMER`: detect `try_lock_for(Duration.zero())`
  pattern, suggest plain `try_lock()`.
- `W_BLOCKING_NOTIFY_RISK`: detect `notify_*` calls within `blocking { }`.

Acceptance: 2 lint tests fire correctly.

### Ф.4 — Tests (~0.3d)

См. Tests breakdown.

### Ф.5 — D172 draft (~0.1d)

D172 «Realtime / Blocking interaction matrix» — full table.

### Ф.6 — Close (~0.05d)

## Tests breakdown

### Positive (10 файлов в `nova_tests/plan103_6/`)

1. `realtime_atomic_load_ok.nv` — `realtime { a.load(...) }` compiles,
   runs (verify atomic leaf-safe).
2. `realtime_atomic_fetch_add_ok.nv` — same для fetch_add.
3. `realtime_fence_ok.nv` — `realtime { fence(SeqCst) }` OK.
4. `realtime_mutex_try_lock_ok.nv` — `realtime { m.try_lock() }` OK
   (no-wait variant).
5. `realtime_semaphore_try_acquire_ok.nv` — same.
6. `realtime_oncecell_get_ok.nv` — `OnceCell.get()` (read-only,
   no init) OK.
7. `blocking_atomic_fetch_add_ok.nv` — `blocking { a.fetch_add(...) }`.
8. `blocking_mutex_unlock_ok.nv` — `blocking { m.unlock() }` (leaf
   wake — debatable; D172 specifies, here positive case).
9. `realtime_lazy_is_forced_ok.nv` — read-only check OK.
10. `realtime_oncecell_set_first_call_ok.nv` — set() — first call —
    leaf-like (no waiters yet) — D172 decides; here positive case
    с pre-condition сmoke.

### Negative (15 файлов — comprehensive coverage of 6 error codes)

**E_REALTIME_SYNC_PARK:**
1. `realtime_mutex_lock_neg.nv` — `realtime { m.lock() }` → error +
   suggestion «use try_lock».
2. `realtime_mutex_try_lock_for_neg.nv` — `realtime { m.try_lock_for(50ms) }`
   → error (libuv timer suspend).
3. `realtime_rwlock_read_neg.nv` — `realtime { rw.read() }` → error.
4. `realtime_rwlock_with_write_neg.nv` — `realtime { rw.with_write { ... } }`
   → error.
5. `realtime_semaphore_acquire_neg.nv` — `realtime { s.acquire() }`.
6. `realtime_barrier_wait_neg.nv` — `realtime { b.wait() }`.
7. `realtime_countdown_await_neg.nv`.
8. `realtime_condvar_wait_neg.nv` — `realtime { cv.wait(m) }`.
9. `realtime_once_call_once_neg.nv` — `realtime { o.call_once(...) }`.
10. `realtime_oncecell_get_or_init_neg.nv` —
    `realtime { c.get_or_init(...) }`.
11. `realtime_lazy_force_neg.nv` — `realtime { lz.force() }`.

**E_BLOCKING_SYNC_PARK:**
12. `blocking_mutex_lock_neg.nv` — `blocking { m.lock() }` → error +
    suggestion «move outside blocking block».
13. `blocking_condvar_wait_neg.nv` — `blocking { cv.wait(m) }` → error.

**E_REALTIME_NESTED_SYNC_VIA_FN:**
14. `realtime_via_user_fn_neg.nv`:
    ```nova
    fn helper() {  // не annotated, inferred #parks
        m.lock()
        defer m.unlock()
    }
    fn main() {
        realtime { helper() }  // error — propagated
    }
    ```

**W_REALTIME_TRY_LOCK_FOR_TIMER:**
15. `realtime_try_lock_for_zero_warn.nv` — `try_lock_for(Duration.zero())`
    → warning, suggest `try_lock()`.

### Property (none — enforcement testing only)

### Stress (none)

## Acceptance criteria

- [ ] All ~85 sync.nv external fn declarations имеют `#parks` /
      `#wakes` / `#realtime` annotation; missing → conservative
      `#parks`.
- [ ] Type-checker enforcement в `realtime { }` block: 11 negative
      tests passes.
- [ ] Type-checker enforcement в `blocking { }` block: 2 negative
      tests passes.
- [ ] Function-level propagation (`E_REALTIME_NESTED_SYNC_VIA_FN`):
      1 negative test passes.
- [ ] 6 error codes registered в diagnostic registry (Plan 50 D102
      format).
- [ ] 2 lint warnings (`W_REALTIME_TRY_LOCK_FOR_TIMER`,
      `W_BLOCKING_NOTIFY_RISK`) fire correctly.
- [ ] D172 draft in spec (full matrix realtime/blocking × method).
- [ ] ≥10 positive + ≥15 negative tests; PASS.
- [ ] Cross-platform Windows + Linux clang.
- [ ] `nova test` full suite — 0 новых FAIL (existing sync tests без
      realtime/blocking wrapper не affected).

## Risks + mitigations

| Риск | Mitigation |
|---|---|
| Conservative annotation (`#parks` default) ломает legitimate use case (leaf-method without annotation) | Sweep Ф.1 explicit для всех methods; lint `W_MISSING_SYNC_ANNOTATION` помечает new declarations |
| Cross-function inference incomplete — false negatives | Plan 16 уже имеет partial; full inference — out of scope; document'ировано в D172 что explicit annotation recommended |
| User-fn `#parks` annotation manually added — tedious | LSP quick-fix: detect call к `#parks` method из non-annotated fn, suggest add `#parks`. Plan 50 D102 integration. Optional V1. |
| `notify_one` в `blocking { }` — wake on blocking-thread может interact с GC | Conservative: `W_BLOCKING_NOTIFY_RISK` warning; full prohibition — отдельный future plan if needed |
| Existing tests без realtime/blocking wrapper — should still PASS | Smoke test full suite after Ф.2; if any regression — annotation bug |

## Связи

- [Plan 103 master](103-sync-primitives-spec-formalization.md) — M12.
- [Plan 103.2-103.5](103-sync-primitives-spec-formalization.md) — типы для enforcement.
- [Plan 16](16-capability-enforcement.md) — D63 forbid + D64 realtime infrastructure.
- [Plan 83.3](83.3-blocking-libuv-threadpool.md) — Blocking effect + nogc body enforcement.
- `compiler-codegen/src/diag.rs` — Diagnostic / Suggestion / Applicability infrastructure.
- [Plan 33.x](33.9-opaque-reveal-fuel.md) — `#`-attr parser precedent.
- [Plan 103.7](103.7-spec-d-blocks.md) — D172 final.

## Reality note (added 2026-05-27 после merge Plan 103.4)

Чтобы избежать сюрпризов в Ф.2 — текущее состояние codebase:

1. **Нет отдельного `check_realtime.rs` / `check_blocking.rs`.** Realtime-ban
   на park-ing методы сейчас сидит **inline в `compiler-codegen/src/codegen/emit_c.rs`**
   (см. функцию вокруг `is_realtime_blocking`, после merge Plan 103.4 это
   ~line 16960-17025). Это hardcoded `(recv_ty, method)`-список, дописываемый
   каждым новым sync-планом. Plan 103.6 заменяет его на annotation-driven
   lookup из ExternalRegistry.

2. **Атрибуты `#realtime / #parks / #wakes` НЕ существуют.** Парсер
   аттрибутов уже работает для `#stable(since=…)`, `#opaque`, `#fuel(N)`,
   `#nooverflow`, `#cfg(…)` (Plan 33.x). Расширение — добавить три новых
   bare-аттрибута (без значений) в lexer/parser whitelist.

3. **`ExternalRegistry` (`compiler-codegen/src/codegen/external_registry.rs`)**
   уже содержит `type_decls` для sync.nv через Plan 103.5 pattern. Расширить
   `ExternalDecl` полем `sync_class: Option<SyncClass>` (Park | Wake | Safe),
   парсить из АТР'ов при загрузке. Это даёт O(1) lookup при emit_call.

4. **Plan 16 (D63/D64 capability enforcement)** — реальная инфраструктура
   живёт в `compiler-codegen/src/types/` (capability ctx) + emit_c.rs
   (call-site check). НЕ переписывать Plan 16 — только дополнить
   sync-specific логику. `in_realtime` флаг уже трекается в `CEmitter`.

5. **Function-level propagation (Ф.2 последний пункт) — V1-friendly fallback.**
   Если полная cross-function inference раздувается за 0.5d budget — V1
   ограничиться **call-site check** (annotation lookup при каждом method call
   внутри `realtime { }`/`blocking { }`). Function-level propagation
   (`E_REALTIME_NESTED_SYNC_VIA_FN`) пометить как V2 followup и закрыть
   1 negative test через **explicit `#parks` annotation на user-fn** (manual,
   не inferred). Аудит Plan 16 показывает что full whole-program inference
   — отдельный large plan, не Plan 103.6 V1.

6. **Acceptance criterion #3 (function-level propagation)** в этом плане
   ослабляется до: «1 negative test с **explicit `#parks` annotation** на
   user-fn → realtime call rejected». Inference оставить followup-маркером
   `[M-103.6-function-level-parks-inference]`.

## Appendix A — Ф.0 baseline audit (TODO)

Full forbidden-list matrix verified against D50/D64 contracts.
