<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# План 214 — `#coerce`: декларативные неявные конверсии (str→[]u8 как первый инстанс)

**Статус:** 📘 СПЕКА ЗАФИКСИРОВАНА 2026-07-18 — D429 в `spec/decisions/02-types.md` написан
СРАЗУ В ПРОД-ВИДЕ (решение владельца «без упрощений»: без std-only-гейта, обе полосы, конвенция
+ линт нормативно; прецедент spec-first — D422/208). **Ф.0 = DONE (интегратор).** Реализация
(Ф.1-Ф.5) НЕ начата, ждёт очереди; до Ф.2 переходно действует write-костыль (задокументировано
в D429 и в ретракт-баннере D55-подсекции). **Приоритет P2** (снимает §3-нарушение
`[M-d55-str-literal-coercion-name-gated]` общим механизмом + закрывает эргономику str→[]u8).

## 0. Мотив (одной фразой)

Nova уже прячет безопасные zero-cost конверсии (single-wrapper D55: newtype/sum, переменные
включительно, int→i64 widening «невидимо для автора») — но str→[]u8 требует `.bytes()`, а
компромисс-костыль (str-литерал в `write`) захардкожен по ИМЕНИ метода — нарушение
[compiler-conventions §3](../compiler-conventions.md). Нужен ОБЩИЙ декларативный механизм:
объявление в `.nv`, никакого хардкода пар в Rust.

## 1. Дизайн (converged, финал)

**ОДИН атрибут `#coerce`** на функции. Функция обязана быть УНАРНОЙ по входному типу;
объявляет неявную коэрсию `I → O`:

```nova
// форма-ИСТОЧНИК: метод без параметров; I = тип-приёмник, O = возврат
#coerce fn str @bytes() -> ro []u8 => ...              // объявляет str → []u8 (наш инстанс)

// форма-ПРИЁМНИК: статик .new с одним параметром; I = параметр, O = Self
#coerce fn Matrix.new(v []f64) -> ro Self => ...       // объявляет []f64 → Matrix
```

Направление записи — где автору удобнее (у источника примитив уже есть — атрибут без нового
кода; у приёмника — когда конверсию логично декларирует целевой тип). Семантика ОДНА.

### Правила (все — нормативные, в D429)

1. **Унарность.** Ровно один входной тип: метод-без-параметров ИЛИ статик-с-одним-параметром.
   Иначе — compile error `E_COERCE_NOT_UNARY`.
2. **Неявная вставка — только zero-cost, ДВЕ полосы** (скрытых аллокаций не бывает):
   - **view**: не-consume форма с `ro`-возвратом (zero-cost view) — `#coerce fn str @bytes()
     -> ro []u8`;
   - **finalize** (владелец 2026-07-17): `consume`-метод с владеющим возвратом (zero-cost
     MOVE — кража буфера, не копия) — `#coerce fn StringBuilder consume @into_str() -> str`,
     `#coerce fn WriteBuffer consume @into_bytes() -> []u8`. Позиции — те же, что у view
     (ВСЕ типизированные, вкл. call-arg: `log(sb)` → `log(sb.into_str())`, sb разряжен).
     Опасности рантайма нет ни в одной ветке: линейность отслеживается всегда — «нецелевое»
     использование (use-after) = немедленный compile error (решение владельца: громко-на-
     компиляции = норма; return-only-срез отвергнут как перестраховка). **Обязательное
     качество диагностики:** use-after-consume после неявной вставки ОБЯЗАН указывать точку
     вставки — «потреблён неявной #coerce-финализацией `into_str()` в вызове … (строка N);
     для чтения без потребления — явный view-метод».
   `#coerce` на форме вне этих двух полос (не-consume с владеющим возвратом = скрытая
   аллокация) — error `E_COERCE_NOT_ZERO_COST` (жёстче, чем «молча не вставлять»: атрибут
   без эффекта = ловушка).
3. **Один `#coerce` на пару (I, O) на программу.** Дубль (в т.ч. с двух сторон) — error
   `E_COERCE_DUPLICATE_PAIR` с указанием обеих деклараций.
4. **Один уровень.** Цепочка `I→O→P` не разворачивается (паритет single-wrapper D55
   «ровно один уровень»).
5. **Exact > coercion.** Перегрузка с точным типом всегда побеждает; коэрсия пробуется только
   когда позиция ждёт `O` и точного совпадения нет. При ≥2 применимых коэрсиях к одной
   позиции — error неоднозначности (паритет single-wrapper «однозначность обязательна»).
6. **Позиции** — та же таблица «явно ожидаемого типа» D55: call-arg на резолвленный параметр,
   `let`/`const` с аннотацией, return-позиция, element-позиция коллекции.
7. **Вставляется ИМЕНОВАННЫЙ вызов** (`s.bytes()` / `Matrix.new(v)`) — АСТ-переписывание в
   существующем expected-type-propagated проходе (`MapLitAnnotator::try_wrap_leaf`-семья);
   неявная форма байт-в-байт эквивалентна явной. Никакого нового C-лоуэринга.
8. **Extension-разрешён** (orphan-развязка): автор `MyId` может объявить
   `#coerce fn str @to_myid() -> ro MyId` extension'ом в своём модуле.
9. **Охват — все типы, включая пользовательские, с первого дня** (решение владельца
   2026-07-18 «без упрощений»; std-only-гейт снят). Orphan-развязка — extension-форма (п.8);
   R3 (one-per-pair) глобально предотвращает конфликт деклараций.
10. **Взаимодействие с mut/consume:** `ro O` не матчит `mut`/`consume`-позицию → там как
    раньше, явно (`.bytes().clone()`). Падает из правила 2 само, но проговорить в D429.

### Конвенция и линт (владелец 2026-07-18): неявная форма — КАНОН

- **nv-coding-style (амендмент, Ф.5):** в позиции, где пара `(I,O)` объявлена `#coerce` и
  ожидаемый тип явен, канон — ГОЛОЕ значение (`w.write(s)`, `return sb`), НЕ явный вызов
  (`w.write(s.bytes())`, `return sb.into_str()`). Явный вызов уместен ТОЛЬКО там, где
  коэрсия не сработает: нетипизированный биндинг (`ro b = s.bytes()` — ожидаемого типа нет),
  mut/clone-нужда (`.bytes().clone()`), не-объявленные пары.
- **Линт `W_COERCE_EXPLICIT_REDUNDANT` (Ф.3b):** вызов `#coerce`-зарегистрированного метода
  в позиции, где голое значение скоэрсилось бы в ТО ЖЕ САМОЕ (та же пара, тот же результат)
  → warning «уберите явный вызов — действует #coerce (D429)». Детект — тем же реестром пар
  (I,O), что и вставка (одно окно: линт и коэрсия читают ОДИН реестр — расхождение
  невозможно). Не флагается: позиции без явного ожидаемого типа; вызовы вне реестра.
- Следствие для std: существующие `.bytes()`/`.into_str()`-сайты в коэрсибельных позициях
  МИГРИРУЮТСЯ на голую форму (Ф.3 расширена) — std обязан быть линт-чистым.

### Что НЕ входит (отклонено в ревью)

- Универсальная вшитая str→[]u8 коэрсия (хардкод пары в Rust) — отвергнута владельцем.
- Безымянная call-форма `fn Vec[T](s str)` — новый синтаксис + реинкарнация ретрактированного
  `from` (§1а) + всё равно требует opt-in атрибута.
- Два раздельных атрибута `#view`/`#coerce` — схлопнуты в один (унарность снимает
  двусмысленность источника).

## 2. Первый инстанс и поглощение хардкода

- Ф.3 помечает СУЩЕСТВУЮЩИЙ `str @bytes() -> ro []u8` атрибутом — ноль нового кода.
- **D55-амендмент «Str-литерал → []u8» (2026-07-15) — РЕТРАКТ целиком (владелец 2026-07-17):**
  литерал — просто частный случай str-ЗНАЧЕНИЯ, отдельного правила не остаётся; действует
  ТОЛЬКО общее D429-правило, на литералы оно распространяется автоматически. Подсекция в
  `02-types.md` помечается `RETRACTED → D429` (текст-история остаётся, как у прочих ретрактов).
- Реализация-костыль name-gated `synthesize_write_str_lit_bytes_coercion` (метод буквально
  `write`) СНОСИТСЯ; поведение старых сайтов (`w.write("...")`) сохраняется НОВЫМ механизмом
  байт-в-байт (у str есть `#coerce`→`ro []u8`, позиция — `[]u8`-параметр) — Ф.4-фикстура
  пинует эквивалентность на существующем d55-тесте.
- Маркер `[M-d55-str-literal-coercion-name-gated]` закрывается этим планом (home = 214).

## 3. Фазы

- **Ф.0 Спека — ✅ DONE 2026-07-18 (интегратор):** D429 в `02-types.md` (R1-R9, обе формы,
  обе полосы, ретракт-баннер на D55-подсекции, отклонённые альтернативы, связи) + строка в
  индекс-таблице 02-types.md + строка в spec/decisions/README.md.
- **Ф.1 Чекер (sonnet):** реестр `#coerce`-деклараций (собирается из деклараций при
  sig-скане; ключ (I,O) + FnDecl) + валидации E_COERCE_NOT_UNARY / NOT_ZERO_COST / DUPLICATE_PAIR;
  вставка в accept-путь (там же, где single-wrapper `single_wrap_candidates`) + АСТ-rewrite
  в `try_wrap_leaf`-проходе. Позиции — п.6. Exact>coercion — до кандидатов коэрсии.
- **Ф.2 Codegen:** НОВОГО НЕТ (rewrite = обычный вызов метода). Снос
  `synthesize_write_str_lit_bytes_coercion` + его call-site в emit_call pre-pass.
- **Ф.3 std:** view-полоса: `#coerce` на `str @bytes()`; finalize-полоса: `#coerce` на
  `StringBuilder consume @into_str()` и `WriteBuffer consume @into_bytes()`. **+ Миграция
  std-сайтов на канон-голую форму** (конвенция выше): по выводу линта Ф.3b — `.bytes()`/
  `.into_str()` в коэрсибельных позициях снимаются; вне позиций (нетипизированный биндинг,
  clone-нужда) — остаются. std после Ф.3 линт-чист по W_COERCE_EXPLICIT_REDUNDANT.
- **Ф.3b Линт (sonnet, та же волна):** `W_COERCE_EXPLICIT_REDUNDANT` в `nova lint` — детект
  по ТОМУ ЖЕ реестру пар, что вставка (одно окно); текст с подсказкой голой формы.
- **Ф.4 Тесты (haiku по списку):** pos view: str-значение/литерал в `[]u8` call-arg / let /
  return / element; exact-wins (перегрузка f(str)). pos finalize: `sb` в str-позиции
  (return И call-arg) → финализация, значение корректно. neg: НЕ-унарная #coerce; не-consume
  с владеющим возвратом (NOT_ZERO_COST); дубль пары; mut-позиция не коэрсится; двухуровневая
  цепочка не коэрсится; **finalize use-after**: `log(sb); sb.write(...)` → compile error,
  текст пинует «потреблён неявной #coerce-финализацией … (строка N)» (качество диагностики —
  нормативное, §1 п.2). Конвенция §116, standalone-прогон, EXPECT_COMPILE_ERROR для neg. + линт-фикстуры Ф.3b:
  pos (варнинг на явном вызове в коэрсибельной позиции) / neg-отсутствие (нетипизированный
  биндинг и clone-сайт НЕ флагаются) — по образцу существующих W_-линт-тестов.
- **Ф.5 Закрытие:** маркер `[M-d55-str-literal-coercion-name-gated]` закрыт; лог в
  simplifications; D55-подсекция «Статус реализации» обновлена (реализация = D429-механизм);
  **nv-coding-style амендмент** «неявная форма — канон» (§Конвенция выше) — в том же слиянии.

## 4. Гейты

Таргетно: фикстуры Ф.4 standalone + `nova test std/src/checksums` (горячий str/bytes-путь) +
`nova test std/src/collections/vec`; байт-паритет `.c` на 2-3 не-затронутых фикстурах
(коэрсия не срабатывает там, где не должна). Полный: CI (nova-gate) после слияния
интегратором. Флагман собирается под `--strict-effects` (CI, 5 таргетов).

## 5. Риски

| Риск | Митигация |
|---|---|
| Расползание неявности (урок Scala implicit) | правила 2-5 + V1-охват std-only (п.9) |
| Перф sig-скана (реестр) | атрибут редкий; сбор при существующем скане деклараций, O(декл) |
| Конфликт с overload-резолвом (`env(str)`/`env_bytes([]u8)`) | exact>coercion (п.5) — `env("K")` берёт str-перегрузку, коэрсия не пробуется |
| АСТ-rewrite в горячем проходе | вставка только при типовом мисматче ro-позиции — холодная ветка |

## 6. Модель исполнения

Дизайн (этот файл) — готовая карта. Ф.0-Ф.3 — sonnet одним заходом (worktree, без мега-CU);
Ф.4 — haiku по списку фикстур; приёмка + слияние + CI — интегратор.
