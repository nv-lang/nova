# Plan 42.15: Per-peer / per-import isolation (NameResCtx refactor)

> **Создан 2026-05-14, ЗАКРЫТ 2026-05-14** — sub-plan Plan 42 (правило C).
> Выделен из Plan 42.14 Ф.4 + Ф.6. Закрывает [M10] Rule C Path-form +
> [M12] selective import visible-scope.
>
> **СТАТУС:** ✅ Реализовано. NameResCtx переведён на per-group visible
> scope. Imports per-peer (Rule C strict). `export import` re-export
> propagation. 330 PASS / 0 FAIL.

## Реализация (итог)

- **`PeerFile.imported_item_names`** — resolver собирает items
  притащенные **прямыми** imports каждого peer'а (после rename +
  selective filter).
- **`PeerFile.is_entry_module`** — различает peers компилируемого
  module vs imported folder-modules.
- **NameResCtx `group_decls`** — `HashMap<FileId, HashSet<String>>`:
  declarations module-group каждого peer'а (peers одного folder-module
  по общему parent dir). Peers группы делят namespace; между группами —
  нет.
- **`peer_imported_names`** — per-peer imported items (Rule C: imports
  НЕ shared).
- **`all_decls`** — union всех declarations, ТОЛЬКО для эвристики
  pattern-binding detection (не enforcement).
- **`is_known(name, file_id, scope)`** — проверяет `group_decls[file_id]`
  → `peer_imported_names[file_id]` → `peer_module_names[file_id]` → scope.
- **re-export**: `export import X` пробрасывает items X в visible scope
  caller'а (не только импортирующего peer'а).
- **Path-form** (`alias.func()`): unknown lowercase `alias` ловится
  через Ident-ветку NameResCtx (парсер эмитит `Member{obj:Ident}` для
  неизвестных heads) — Path-form check в walk_expr дополняет для
  known-Path случаев. False positives = 0 (регрессия чистая).

## Тесты

- `nova_tests/negative_capability/peer_path_leak.nv` — peer B использует
  alias peer A → undefined identifier (Rule C violation).
- `nova_tests/modules/peer_isolation_ok_use.nv` — peers делят
  declarations (Rule C shared decls) → PASS.

---

## Два gap'а — один корень

### [M10] Rule C Path-form enforcement

Plan 42 правило C: peers share **declarations**, но **не imports**.
Сейчас:
- Bare `Ident` проверяется per-peer (Plan 42.04 — `peer_module_names`).
- **Path-form** (`alias.func()`) — `ExprKind::Path` полностью
  пропускается name-check'ом.
- `module.items` flat → imported items одного peer видны codegen'у
  как top-level, "протекают" в namespace других peers.

### [M12] Selective import visible-scope

`import X.{A}` — selective list парсится, rename (`A as B`) работает,
но items НЕ в `{...}` всё равно merge'атся в visible scope. `import X.{A}`
не прячет `B`.

### Общий корень

Оба — следствие **flat `module.items`** + **NameResCtx без per-import
visible tracking**. Resolver inline-expansion сваливает всё в один
namespace; type-checker не различает «видно потому что я импортировал»
vs «видно потому что транзитивная зависимость».

---

## Архитектурное решение

**Разделить codegen-scope и visible-scope:**

1. **Codegen-scope** = всё (для completeness — `A`'s body ссылается на
   `B` из того же модуля X). `module.items` flat остаётся для codegen.

2. **Visible-scope** (per-peer, per-import) — что type-checker считает
   «доступным по имени» в данном peer:
   - declarations всех peers этого folder-module (Rule C: shared decls)
   - + items явно импортированные **этим** peer'ом
   - + selective list если `import X.{A}` — только `A`, не весь X
   - НЕ: imports других peers, НЕ: транзитивные не-selective items

**Реализация:**
- `PeerFile` получает `visible_names: HashSet<String>` — что видно
  в этом peer (вычисляется в resolver из его imports + selective lists).
- `NameResCtx`: `is_known(name, file_id, scope)` проверяет
  `peer_files[file_id].visible_names` вместо общего flat top_level.
- `ExprKind::Path` — head-segment тоже проверяется (lowercase alias →
  должен быть в peer's visible module names).
- Codegen — не трогается, читает flat `module.items`.

---

## Phases (placeholder)

- Ф.1: `PeerFile.visible_names` — resolver вычисляет per-peer
- Ф.2: NameResCtx использует visible_names (bare Ident)
- Ф.3: Path-form head-segment check
- Ф.4: selective list → visible_names exclusion
- Ф.5: tests (positive + negative для обоих [M10] + [M12])
- Ф.6: regression + docs

**Size estimate:** ~300-500 LOC (NameResCtx + resolver + PeerFile).

---

## Риски

- **False positives.** Path-form head может быть тип / effect /
  builtin namespace (`gc`, `fibers`). Нужен careful whitelist +
  Capitalized-skip (уже есть).
- **Транзитивные зависимости.** `import X.{A}` где `A` использует `B`
  — `B` в codegen-scope (ok), но если user пишет `B()` напрямую →
  должна быть ошибка. Edge: `B` может совпасть с builtin.
- **Backward compat.** Существующие тесты используют folder-modules
  без strict isolation — могут начать падать. Mitigation: аккуратная
  миграция, возможно warning-mode сначала.

---

## Что НЕ входит

- Codegen items refactor (separate `imported_items`) — НЕ нужен,
  flat codegen-scope корректен.
- Visibility modifiers (`pub(crate)` и т.п.) — отвергнуто D5.

---

## Связь

- [Plan 42.04](42.04-per-file-imports-scope.md) — заложил `peer_module_names`.
- [Plan 42.14](42.14-backlog-closure.md) Ф.4 + Ф.6 — re-defer сюда.
- [simplifications.md](../simplifications.md) [M10] + [M12].
- [Plan 42](42-folder-modules.md) правило C.
