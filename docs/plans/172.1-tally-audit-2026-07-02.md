# Workflow digest 2026-07-02: tally audit + clusters

## Confirmed problems (refuted=false)

## Refuted flags (аудитор ошибся, сайт чист)


## Cluster fix-specs

### GenericRecvStd — Member-поля builtin-типов на generic-ресиверах в std (Member:.data/.len/._buckets/.cap/.0/.1/.map, ~650) + method-Call return на generic-ресиверах (Call:.len/.append/.map/.of/.next/.write/.reserve, ~500+). Все span'ы — внутри std-шаблонов (vec/core.nv, hashmap.nv и т.д., file_id 171–204); один и тот же ExprId повторяется в трейсе N раз = один template-expr, опрошенный при каждой mono-эмиссии. [large]
ROOT: Архитектурно: канал resolved_types ключуется ГОЛЫМ ExprId, а std generic-тела эмитятся N раз (mono_worklist drain emit_c.rs:4491 → emit_monomorphized_method:15345 ставит current_type_subst:15354 на всё тело) по ОДНОМУ AST. Чекер проверяет шаблон один раз (T абстрактен) и все продюсеры СОЗНАТЕЛЬНО отказываются аннотировать тип с residual type-param, потому что консюмер-лоуэринг resolved_type_to_c МОЛЧА ВРЁТ на несубституированном параметре: Named{T} без subst-entry → `Nova_T*` (emit_c.rs:2288), unwrap_or("nova_int") в generic-instance арме (:2249), stub→nova_int erasure в resolved_array_to_c (:2043-2045). Конкретные bail-точки чекера: (1) f3_check_member Record-арм — primitive_gate/concrete_value_named отвергает субституированный тип с остаточным T (types/mod.rs:8882-8906) → .data (*mut T), ._buckets ([]Slot[K,V]), .map (HashMap[K,V]); (2) f3 same-name field/method early-return (types/mod.rs:8851-8857) — Vec имеет И поле len/cap, И методы @len()/@cap() (std/collections/vec/core.nv:234,237,261), поэтому field-read `@len` внутри тел Vec возвращается ДО блока материализации → даже mono-НЕЗАВИСИМЫЙ int-тип (.len 221, .cap 48) не аннотируется; (3) Call-арм f1_expr gs-gate `!typeref_mentions_any(&tr, gs)` (types/mod.rs:6743); (4) resolve_instance_method_return FULLY-CONCRETE bail (types/mod.rs:11115-11117) и resolve_prefix_generic_method_return guard `generics.is_empty()` (types/mod.rs:11179-11186) — комментарий :11174 прямо называет причину: «resolved_type_to_c silently collapses to nova_int». Итог: весь пласт падает в legacy, который держит дубликат-движок типов на C-name строках.
LEGACY: Legacy отвечает дубликат-движком на C-name строках, работающим ТОЛЬКО потому, что вызывается внутри mono-эмиссии: Member-арм (emit_c.rs:40429+) берёт recv_c_type_materialized → декодирует self-describing mono-имя (Nova_Vec____<elem>*, generic_type_instance_info) либо record-схему шаблона + current_type_subst + name-keyed side-tables (array_element_types — last-wins, отравляемы между peer-файлами); Call-арм (:37983+) — recv_and_method + fn_ret_* var_types-реестры + turbofish/subst ad-hoc. Т.е. legacy УЖЕ делает вариант (б), но рассеянно, по C-строкам и с fallback-ложью — фикс переносит ту же субституцию в единое окно resolved_type_to_c под §1-гейтом Err-вместо-лжи.
FIX: Вариант (б) — codegen-side subst через current_type_subst — самый дешёвый И §0-чистый, и это ЦЕЛЕВАЯ ФОРМА для канала, а не интерим: аннотация шаблона абстрактным структурным типом — это ПРАВДА о шаблоне, валидная для ВСЕХ инстансов; подстановка = лоуэринг-забота, а мономорфизация сегодня целиком codegen-домен. Инфраструктура уже наполовину есть: resolved_named_to_c УЖЕ смотрит current_type_subst для голого имени (emit_c.rs:2129-2135), generic-instance арм регистрирует mono worklist (:2246-2265), диспетчер Channel 2 УЖЕ падает сквозь на Err (`if let Ok` :37139) — fall-through gate готов. Вариант (а) — checker re-check mono-инстансов — требует пере-ключевания канала на (mono_key, ExprId), знания чекером инстансов, которые открываются ДИНАМИЧЕСКИ во время эмиссии (fixpoint :4468), и checker↔codegen итерации — это «typed-IR mono path» 172.1.2 целиком, на порядок дороже, и для типов полей/возвратов НЕ даёт ничего сверх (б): subst — ровно то, что делает mono. Вариант (в) = (б) + лишние данные. ПЛАН: [Шаг 1, keystone-гейт §1] ResolvedType::TypeParam(String) (types/mod.rs:37-99) — явный носитель residual-параметра (НЕ name-эвристика, §3-чисто); fold-хелпер mark_type_params(rt, params) по td.generics∪f.generics∪gs. Лоуэринг-арм в resolved_named_to_c/resolved_type_to_c: TypeParam(n) → current_type_subst.get(n) (+type_subst_overrides), при промахе — Err, НИКОГДА не Nova_T*/nova_int. Закрыть эрозии на канальном пути: :2043-2045 и :2249 — TypeParam-Err ПРОПАГИРУЕТСЯ (channel-miss → legacy), не стирается в nova_int. Erased-тела (emit_generic_method_erased :12361, __erased__ sentinel :15174-15178, пустой subst) автоматически дают Err → legacy-erased поведение сохранено, канал не врёт. Разобрать exhaustive-match fallout нового варианта (cat_compatible_rt, distinct_mono, peel_view, is_primitive_lowerable, resolved_to_typeref). [Шаг 2, Member] f3_check_member Record-арм: после subst_receiver_generics (:8882) снять гейт — аннотировать и param-содержащие/generic-instance типы через mark_type_params; зеркально NamedTuple-арм (:9037+). Same-name fix: материализовать тип ПОЛЯ и в ветке has_same_name_method (:8851), но ТОЛЬКО для не-call-позиции (протащить флаг in_call_func из Call-арма f1_expr :6708 в f1_expr(func)→f3; иначе аннотация поля на func-Member метод-вызова — ложь при fn-typed поле). Это одним махом закрывает .len 221 + .cap 48 (тип int, mono-независим). [Шаг 3, Call] f1_expr Call-арм :6741-6746 — заменить gs-bail на mark+annotate; resolve_instance_method_return :11115-1117 — вместо None возвращать субституированный тип с mark'нутыми carrier-параметрами; resolve_prefix_generic_method_return :11179-1186 — снять peeled_ok запрет generic-instance ресиверов (причина запрета устранена Шагом 1). ОСТАВИТЬ bail на unbound METHOD-level generic из аргументов (:11091-11093, `map[U]` — U из лямбды): это отдельный шаг arg-binding inference; bucket Call:.map 142 закроется частично. [Верификация, §7 — это GC-зона 172.1.2]: per-step NOVA_U45A_XCHECK debug-эквивалентность канал-vs-legacy; detect172 pos+neg + звучность (behavior-gate); ПОЛНЫЙ регресс vs clean-бинарь на том же kill-switch бинаре (172.1.2 ловилось только полным, не сэмплом); tally p67 re-trace до/после.
POINTS: types/mod.rs:37-99 — enum ResolvedType: новый вариант TypeParam(String) + fallout по exhaustive-match (cat_compatible_rt, distinct_mono ~11864, peel_view, is_primitive_lowerable, resolved_to_typeref); types/mod.rs:8851-8857 — f3_check_member same-name field/method early-return: материализация типа поля для не-call позиции (закрывает Member:.len/.cap — int, mono-независимы); types/mod.rs:8882-8906 — f3_check_member Record-арм: гейт primitive_gate/concrete_value_named → mark_type_params-аннотация param-содержащих типов (.data *mut T, ._buckets []Slot[K,V]); types/mod.rs:9037+ — зеркальный NamedTuple-арм (.0/.1); types/mod.rs:6741-6746 — f1_expr Call-арм: gs-gate → mark+annotate; types/mod.rs:11115-11117 — resolve_instance_method_return FULLY-CONCRETE bail → возврат mark'нутого типа (bail по unbound method-level generic :11091 оставить); types/mod.rs:11179-11186 — resolve_prefix_generic_method_return: снять запрет generic-instance ресиверов; emit_c.rs:2117-2135 — resolved_named_to_c: арм TypeParam → current_type_subst lookup, Err на промахе (не Nova_T*); emit_c.rs:2288 — убрать молчаливый Nova_{full}* для неизвестного голого имени на канальном пути (гейт §1); emit_c.rs:2043-2045 — resolved_array_to_c: stub→nova_int erasure не глотает TypeParam-Err; emit_c.rs:2249 — generic-instance арм: unwrap_or(nova_int) не глотает TypeParam-Err; emit_c.rs:37137-37179 — Channel 2 диспетчера: изменений НЕ требует (Err уже = fall-through); emit_c.rs:15345-15357 — emit_monomorphized_method: current_type_subst уже ставится на тело (потребитель готов); emit_c.rs:4491, 12361, 15174-15178, 16741 — worklist drain / erased-эмиссия: пустой subst → TypeParam-Err → legacy (проверить тестом)
RISK: 1) Fresh-mono side-effects: канальный лоуэринг Named-with-args регистрирует mono worklist (emit_c.rs:2048-2057, 2252-2261) в иные моменты, чем legacy → сдвиг порядка эмиссии/layout; по памяти проекта fresh mono GC-безопасна (провалы = compile-time C-type clash), но урок 172.1.2/plan154 требует ПОЛНЫЙ регресс vs clean, не сэмпл. 2) Поведенческие расхождения там, где legacy ВРАЛ (nova_int-collapse) — это желаемые фиксы, но гейт = detect172 pos+neg, byte-identity невозможна по определению. 3) subst-timing (gap #2, комментарий :37141): аннотация читается вне mono-контекста (fwd-decl scan, closure-тело с отдельным scope) → TypeParam-Err → legacy — деградация в статус-кво, не ложь; но частота промахов может оставить хвост tally. 4) Same-name call-позиция: без in_call_func-флага аннотация типа ПОЛЯ на func-Member вызова — потенциальная ложь для fn-typed полей; флаг обязателен. 5) Fallout нового enum-варианта: пропущенный match-арм в identity/compat-логике (distinct_mono) может незаметно ослабить E_ARG_ELEM_TYPE_MISMATCH — нужны негативные тесты. 6) Bucket Call:.map закроется лишь частично (U из аргумента — отдельный шаг arg-binding inference).

### VariantsMatchClosure: (1) desc=Ident:None — 116 hits / 63 uniq ExprId; (2) kind=Other Discriminant(28)=ExprKind::Match — 54 hits / 37 uniq; (3) kind=Other Discriminant(36)=ExprKind::ClosureLight — 140 hits / 29 uniq. Всего ~310 legacy-заходов трейса p67_v8.txt, три независимых под-атома, объединённые общим механизмом закрытия — expected-type (literal-coercion) канал чекера + widening существующих продюсеров. [medium]
ROOT: Общий корень — чекер уже ЗНАЕТ ответ, но не материализует его в resolved_types_buf, и codegen re-derive'ит с молчаливыми fallback'ами (§1-нарушения того же семейства, что удалённый Member-fallback fd3207d6). Детально: (1) None: infer_expr_type Ident-арм (types/mod.rs:10373-10387) резолвит bare unit-варианты ТОЛЬКО не-generic sum'ов (td.generics.is_empty()) — Option generic → нет аннотации; expected-тип (Option[T]) при этом ДОСТУПЕН чекеру во всех definite-позициях через literal-coercion канал (materialize_literal_coercion types/mod.rs:9961; вызовы: fn-return 6149/6159 + materialize_returns_in_block 10061, annotated-let f1_check_assign_let 7356, call-args 8259/8585), но арма для Ident("None") в нём нет. Legacy (emit_c.rs:37749-37768) отвечает по function-LEVEL контексту current_fn_return_ty независимо от фактической позиции выражения — канал «врёт по построению» для None в аргументах/let. (2) Match: продюсер infer_match_common_primitive (types/mod.rs:7961-8018) infer'ит арм-тела в НАРУЖНОМ scope (pattern-binding армы `Some(x) => x` бейлят), отвергает блоки со stmts и всё не-primitive; legacy Match-арм (emit_c.rs:40319-40428) несёт полный дубль-derive (pattern_binding_overrides + collect_pattern_inner_bindings + divergence expr_diverges_125 + unit-домминирование [M-91.13] + финальный nova_int-fallback). (3) ClosureLight: f1-арм чекера (types/mod.rs:7195) только walk'ает тело («полный bidirectional — фаза C6», types/mod.rs:13070-13077); legacy (emit_c.rs:40730-40768) гадает params=nova_int + infer тела → clos_struct_name (34518). Дополнительный §0-разлом: resolved_type_to_c(R::Func)="void*" (emit_c.rs:1985), а C-тип closure-ЗНАЧЕНИЯ = NovaClos_X* (load-bearing: clos_struct_ret_type на call-site) — двойная C-репрезентация fn-типов, из-за которой Func-аннотация через Channel 2 сегодня лоуэрилась бы ложно.
LEGACY: (1) Ident:None (emit_c.rs:37749-37768): unit-вариант через sum_schema_registry.find_variant_compat; для Option — если current_fn_return_ty начинается с \"NovaOpt_\" → вернуть его (function-level контекст, игнорирует фактическую позицию выражения), иначе безусловный \"NovaOpt_nova_int\"; продублировано дословно в Channel 6 диспетчера для формы Call (None как вызов) — bare-Ident форма осталась только в legacy. (2) Match (emit_c.rs:40319-40428): scr_ty=infer(scrutinee); per-арм infer с временными pattern_binding_overrides (collect_pattern_inner_bindings) + seed блок-let'ов; divergent армы скипаются (expr_diverges_125/block_trailing_diverges); если ЛЮБОЙ non-divergent арм = nova_unit → весь match nova_unit (зеркало emit_match [M-91.13]); иначе первый non-unit non-int тип, затем первый non-unit; все unit → nova_unit; нет non-divergent армов → nova_int (молчаливый fallback, §1). (3) ClosureLight (emit_c.rs:40730-40768): params все → nova_int (через closure_param_type_overrides на время infer тела), ret = infer_expr_c_type(body); ответ = clos_struct_name(params,ret)+\"*\" → NovaClos_vi/ii/ib/iii/vii для int/bool-сигнатур, иначе NovaClosBase*; совпадает с emit_lambda (34209) только потому, что emit без контекста использует те же nova_int-дефолты.
FIX: ЦЕЛЕВАЯ ФОРМА (§0): чекер материализует тип из expected-контекста / полной арм-инференции; codegen читает Channel 2 через единый resolved_type_to_c; три legacy-арма удаляются. — АТОМ 1 (None): в materialize_literal_coercion (types/mod.rs:9961) добавить арм `ExprKind::Ident(n) if n=="None"`: если expected (после strip ro/mut/unsafe — уже есть) = Named{path.last()=="Option", generics=[T]} → resolved_types_buf.insert(value.id, from_type_ref(expected)). Гейт против лжи (§1): аннотировать только когда from_type_ref(T) проходит primitive_gate || concrete_value_named (зеркало types/mod.rs:8890-8898) И не упоминает generic-параметры callee (typeref_mentions_any, types/mod.rs:11983) — не-concrete T НЕ аннотировать (упадёт в legacy, а после его удаления — panic укажет реальный пробел), НИКАКОГО дефолта NovaOpt_nova_int в канале. Consumer готов: Channel 2 → resolved_named_to_c Option-арм (emit_c.rs:2148-2171) даёт NovaOpt_<sanitized> + register_novaopt_decl (typedef side-effect уже в лоуэринге). Покрытие: return/tail (через materialize_returns_*), армы match/if в return-позиции (рекурсия 10025-10045), annotated let, call-args — это надмножество legacy-контекста current_fn_return_ty. — АТОМ 2 (Match): расширить infer_match_common_primitive (types/mod.rs:7961) по трём осям: (a) перед infer арм-тела расширять scope pattern-биндингами из scrut_ty (scrut_ty уже вычислен в f1 Match-арме :7035; payload-типы вариантов чекер уже умеет доставать — тот же источник, что check_priv_pattern_recursive; для generic-Option/Result подставлять args скрутини); (b) блоки со stmts: прокатывать локальные let'ы в scope (зеркало f1_block snapshot-паттерна :6173), бейл при сложных stmt'ах; (c) widening гейта: primitive_gate(rt) || concrete_value_named (тот же гейт, что f3_check_member :8898). СОХРАНИТЬ жёсткие гейты: все non-divergent армы обязаны дать РАВНЫЙ rt; ЛЮБОЙ Unit-арм → None (unit-домминирование остаётся в legacy/emit_match до отдельного атома); неразрешимый арм → None. Продюсер-сайт не меняется (f1 :7057-7062). — АТОМ 3 (ClosureLight): продюсер: (a) в materialize_literal_coercion добавить арм ClosureLight/ClosureFull vs expected TypeRef::Func → аннотировать R::Func{params: from_type_ref(каждый), ret} с гейтом «все params+ret concrete» (definite HOF/let/return контексты — тот же источник, что emit'овский context_param_tys, поэтому сигнатуры сойдутся); (b) в f1_expr ClosureLight-арм (:7195) truthful-подмножество без гадания: zero-param `|| body` → R::Func{params:[], ret: from_type_ref(infer_expr_type(body))} при primitive ret (совпадает с legacy NovaClos_vi байт-в-байт). Consumer: НЕ трогать общий R::Func→void* (emit_c.rs:1985) — вместо этого kind-гейтированный Channel 6d в диспетчере (emit_c.rs:~37320, перед legacy-фоллом): expr.kind∈{ClosureLight,ClosureFull} && resolved_types[id]=Func{params,ret} && все лоуэрятся → `format!("{}*", clos_struct_name(&params_c, &ret_c))` (34518). Документировать [M-closure-two-reprs]: closure-ЗНАЧЕНИЕ=NovaClos_X*, closure-ТИП в declared-позиции=void* — унификация C-репрезентации fn-типов = отдельный follow-up план, вне этого кластера. — ВЕРИФИКАЦИЯ: per-atom targeted fixtures + detect172 pos+neg; NOVA_P67_TRACE tally до/после (ожидание: ~116+~30-40+~100 из 310); nova_tests byte-baseline НЕ гейт (behavior-change класс «канал точнее legacy»).
POINTS: compiler-codegen/src/types/mod.rs:9961 — materialize_literal_coercion: новые армы Ident("None") и ClosureLight/ClosureFull vs TypeRef::Func; compiler-codegen/src/types/mod.rs:6149,6159,7356,8259,8585 — уже существующие definite-сайты вызова literal-coercion (return / annotated-let / call-args): менять не надо, покрытие получается бесплатно; compiler-codegen/src/types/mod.rs:7961-8018 — infer_match_common_primitive: pattern-binding scope-extension + stmt'd-блоки + гейт concrete_value_named; compiler-codegen/src/types/mod.rs:7035,7057-7062 — f1_expr Match-арм: scrut_ty уже вычислен, продюсер-вызов на месте; compiler-codegen/src/types/mod.rs:8890-8898 — образец гейта primitive_gate||concrete_value_named (f3_check_member Record-арм); compiler-codegen/src/types/mod.rs:7195 — f1_expr ClosureLight-арм: zero-param truthful-аннотация; compiler-codegen/src/types/mod.rs:11983 — typeref_mentions_any: guard от не-связанных generic-параметров в expected; compiler-codegen/src/types/mod.rs:10373-10387 — infer_expr_type Ident-арм (generic-sum пробел — причина, НЕ точка правки: None закрывается expected-типом, не изобретением инференции); compiler-codegen/src/codegen/emit_c.rs:37118-37356 — диспетчер infer_expr_c_type: Channel 2 потребляет None/Match автоматически; новый kind-гейтированный Channel 6d для ClosureLight+R::Func; compiler-codegen/src/codegen/emit_c.rs:2148-2171 — resolved_named_to_c Option-арм: готовый NovaOpt-лоуэринг + register_novaopt_decl side-effect; compiler-codegen/src/codegen/emit_c.rs:1985 — resolved_type_to_c R::Func → void*: НЕ менять в этом кластере, документировать [M-closure-two-reprs]; compiler-codegen/src/codegen/emit_c.rs:34518 — clos_struct_name: единый источник имени closure-структуры для Channel 6d; compiler-codegen/src/codegen/emit_c.rs:37749-37768 — legacy Ident unit-variant fallback (цель удаления); compiler-codegen/src/codegen/emit_c.rs:40319-40428 — legacy Match-арм (цель удаления); compiler-codegen/src/codegen/emit_c.rs:40730-40768 — legacy ClosureLight-арм (цель удаления); compiler-codegen/src/codegen/emit_c.rs:34209-34270 — emit_lambda: источник фактических param/ret C-типов (context_param_tys / nova_int-дефолт) — канал обязан давать ТЕ ЖЕ типы
RISK: (1) None: behavior-change класс «канал точнее legacy» — в не-return позициях (call-arg f(None) при param Option[str], annotated let) legacy давал NovaOpt_nova_int, канал даст NovaOpt_nova_str; если emit-сторона значения None имеет СВОЙ параллельный контекст-derive и разойдётся с каналом — это CC-FAIL (видимый, не silent miscompile), ловится targeted-фикстурами; гейт typeref_mentions_any обязателен, иначе Named{T} в args → non-concrete путь resolved_named_to_c молча схлопнет в NovaOpt_nova_int (легально-эквивалентно legacy, но лучше не аннотировать вовсе). (2) Match: главный риск — рассинхрон с собственным result_ty-проходом emit_match (unit-домминирование [M-91.13], divergence Plan 125): прецедент D275-регрессии уже был на If-канале (см. коммент в диспетчере :37260-37270); митигация = сохранить any-Unit-бейл и требование строгого равенства всех non-divergent армов; pattern-binding расширение scope должно давать ровно те же payload-типы, что collect_pattern_inner_bindings у legacy, иначе арм-тип разойдётся — при несовпадении бейл. (3) ClosureLight: NovaClos_X* имя load-bearing (clos_struct_ret_type на call-site) — расхождение канал-vs-emit по имени структуры даёт неверный ret на вызове; митигация = kind-гейт (не трогаем общий R::Func→void*, нулевой blast-radius на существующие Func-аннотации Ident'ов) + гейт «канал аннотирует только из того же источника, из которого emit берёт context_param_tys»; zero-param подмножество безрисково (нет гадания params). Общее: verify через detect172 pos+neg + звучность, полный nova test в конце фазы; флака ≠ регрессия.

### kind=Other desc=Other:Discriminant(24) = ExprKind::Binary, 559 заходов (все arith/bitwise/shift; сравнения/логика УЖЕ покрыты — Binary-arm чекера безусловно аннотирует Bool при e.id.is_set, types/mod.rs:6815-6818). Sample-мэппинг спанов (byte-offset проба по файлам CU): ~45% — тела замыканий (d260_lazy_boxiter.nv `x+1`/`x*2`/`x*x` 165 хитов; d264 `x*10` 72; d185 комп-раторы `b-a`, `a.len()-b.len()`); ~40% — generic-тела std (vec/core.nv `@cap*2`, `@len+additional`, `n*size_of[T]()` 115; hashmap.nv `(key.hash() as int)&mask`, `@_buckets.len()-1`; sort `hi-lo`,`p+1`,`size/2`; string/search `@ptr+i`,`sn-fn_len`; read_buffer `cp<<6`); ~10% — pattern-bound переменные match/деструктуризации (d19 `3.0*r*r`, d59 `a+b`, d221 `x+y`); остаток — цепочки через нетипизированные локалы (`ro mask = @_buckets.len()-1` → mask не в scope). [large]
ROOT: НЕ логика Binary-арма — ГОЛОДАНИЕ ОПЕРАНДОВ. Binary-arm f1_expr (types/mod.rs:6791-6877) уже материализует через shared promote_arith_rt (number_exprs.rs:505), когда ОБА операнда резолвятся (infer_expr_type ИЛИ resolved_types_buf ребёнка); операнды не резолвятся по 4 независимым причинам: (1) f1_expr Lambda/ClosureLight/ClosureFull армы (types/mod.rs:7192-7201) рекурсируют в тело С ВНЕШНИМ scope — параметры замыкания НИКОГДА не биндятся, хотя ожидаемая сигнатура `fn(T)->U` с конкретным T=int известна на call-site (`BoxIter[int].map(|x| x*2)`); (2) f3_check_member (types/mod.rs:8851-8857) при same-name field/method (`Vec.len` поле + `@len()` метод, `.cap` аналогично) делает early-return ДО annotation-site (:8858-8907) — поля `@len`/`@cap` generic std-тел не аннотируются (это же даёт 221+48 заходов Member-кластера), а infer_expr_type Member-arm (:10542-10561) гейтится на generics.is_empty() → для Vec[T]-ресивера None; (3) pattern-биндинги match-армов не вносятся в scope (Match-arm :7032-7049 — scrut_ty вычислен на :7035, но тела армов ходятся с немодифицированным scope); (4) generic-вызовы с КОНКРЕТНЫМ объявленным return (`size_of[T]() -> int`, `.len() -> int` на generic-ресивере) не каналятся (infer_method_call_channel_type :11243 не покрывает TurboFish-func/generic-receiver формы) — 138 Call:<expr> в том же файле, каскадно морит Binary-родителей и локалы (`mask`, `size`). Канал НЕ врёт (D263-фикс уже убрал int-fallback :6867-6876) — он просто пуст.
LEGACY: emit_c.rs:37503-37560. Сравнения/логика/имликации → "nova_bool" (кластер их не содержит — уже в канале). Арифметика/битовые: рекурсивный infer обоих операндов С CODEGEN-ЛОКАЛЬНЫМ состоянием (closure_param_type_overrides, pattern_binding_overrides, var_types, схемы, current_type_subst — то, чего у чекера нет), затем: raw-ptr − raw-ptr → int64_t; raw-ptr + int → тип указателя; f64 побеждает; typed-int (u8..u32/i8..i32) побеждает nova_int; иначе тип ЛЕВОГО операнда. Для sampled-форм legacy отвечает ВЕРНО (int/f64/u32/str per-mono), потому что codegen к моменту запроса уже зарегистрировал параметры замыкания и mono-substitution — т.е. legacy здесь не врёт, а компенсирует пустоту канала поздним (emission-time) знанием; materialization должна дать тот же ответ раньше (check-time), где он mono-инвариантен, и оставить legacy-tally там, где ответ существует только per-mono.
FIX: ЦЕЛЕВАЯ ФОРМА (§0): Binary-arm не трогать — он консьюмер; кормить операндные каналы. 4 атома по убыванию yield: [A, keystone, ~40%+Member-каскад] f3_check_member: перенести материализацию field-type ВЫШЕ early-return has_same_name_method (types/mod.rs:8851-8857), НО только для VALUE-позиции member'а: протащить флаг is_callee из f1_expr Call-арма (:6707-6726, где f1_expr(func) ходит callee) vs Member-арма (:6930-6934). Языковой инвариант: bound-method values удалены (Plan 132, E_BOUND_METHOD_REMOVED :8738-8771) → bare `obj.field`/`@field` в value-позиции ВСЕГДА поле → аннотация subst_receiver_generics+primitive_gate (:8882-8906) не ложь; callee-позицию НЕ аннотировать (иначе повтор прецедента fd3207d6 — тип поля на выражении-методе). После A `@cap*2`, `@len-k`, `@len+additional` материализуются существующим Binary-armом автоматически. [B, ~45%] типизация параметров замыканий: в f1_check_call (:8290) / около infer_method_call_channel_type (:11243) — для arg-а Lambda/ClosureLight при резолвнутом callee с declared-параметром `fn(P..)->R`: субституировать receiver type-args, и если Pi конкретен (не упоминает gs), ходить тело f1_expr со scope+{param:Pi} (сейчас тела ходятся раньше, из Call-арма :6709-6712 — перенести walk closure-аргументов после резолва сигнатуры). [C, ~10%] Match-arm биндинги: в :7036-7048 деривнуть типы биндингов паттерна из scrut_ty (машинерия destructure есть — check_priv_pattern_recursive :7037) и ходить тело арма с child-scope; гейт: только конкретные (gs-free) типы биндингов. [D, каскад] канал return generic-вызовов с конкретным declared return: расширить infer_method_call_channel_type на generic-receiver/TurboFish формы, когда declared return НЕ упоминает type-params (`size_of[T]()->int`, `Vec[T].len()->int`) — закрывает и Call-кластер, и локалы-цепочки (`mask`, `size` попадут в scope через f1_block let-inference). ГЕЙТЫ ПРОТИВ ЛЖИ (§1): никаких default-типов — нерезолв → канал молчит → legacy-tally (D263-паттерн сохранён); все аннотации mono-инвариантны (ExprId делится между mono-инстансами: только конкретные примитивы/gs-free типы); в A аннотировать только субституированный конкретный тип поля (существующий primitive_gate/concrete_value_named). Верификация: detect172 pos+neg + звучность, NOVA_P67_TRACE re-tally на conformance CU (nova-cli, single CU), NOVA_MEMBER_INT_TRACE для A.
POINTS: compiler-codegen/src/types/mod.rs:6791-6877 — f1_expr Binary-arm (консьюмер операндных каналов; НЕ менять); compiler-codegen/src/types/mod.rs:8851-8857 — f3_check_member early-return has_same_name_method (атом A: аннотировать поле до return в value-позиции); compiler-codegen/src/types/mod.rs:8858-8907 — существующий annotation-site f3 (subst_receiver_generics + primitive_gate/concrete_value_named — переиспользовать); compiler-codegen/src/types/mod.rs:6707-6726 — f1_expr Call-arm (источник callee-позиции для A; точка переноса walk closure-аргументов для B); compiler-codegen/src/types/mod.rs:6930-6934 — f1_expr Member-arm → f3_check_member(e.id) (value-позиция); compiler-codegen/src/types/mod.rs:7192-7201 — Lambda/ClosureLight/ClosureFull армы f1_expr (атом B: биндинг параметров); compiler-codegen/src/types/mod.rs:8290 — f1_check_call (резолв сигнатуры callee для B); compiler-codegen/src/types/mod.rs:11243 — infer_method_call_channel_type (атом D: конкретный declared return generic-вызовов); compiler-codegen/src/types/mod.rs:7032-7063 — Match-arm (атом C: scrut_ty :7035 → биндинги паттерна в child-scope); compiler-codegen/src/types/mod.rs:10542-10589 — infer_expr_type Member-arm (generic-гейт) и Binary-arm; compiler-codegen/src/number_exprs.rs:505-520 — promote_arith_rt (единый промоушен, уже shared); compiler-codegen/src/codegen/emit_c.rs:37118-37341 — dispatcher infer_expr_c_type (Channel 2 потребит аннотации без изменений); compiler-codegen/src/codegen/emit_c.rs:37503-37560 — legacy Binary-arm (эталон поведения; умрёт с tally); compiler-codegen/src/codegen/emit_c.rs:18581,18616,36801,40813 — ключевые потребители (let-inference значения, return-тип тела замыкания)
RISK: Атом A: средний. Ложь при аннотации callee-позиции типом поля (точный повтор прецедента Member-fallback) — обязателен позиционный гейт; поведенческий сдвиг там, где legacy отвечал иначе, маловероятен (legacy для `@len`/`@cap` тоже даёт nova_int через схему) — но канал теперь перекрывает codegen-state, проверять detect172+CU. Атом B/C: средне-высокий — расширение scope включает f4_check_value/assignability на ранее Unknown-compat идентификаторах → возможны НОВЫЕ диагностики (false positives) на std/conformance; митигация: биндить только конкретные субституированные типы, прогон neg-suite. Сквозной риск: resolved_types keyed by ExprId и ДЕЛИТСЯ между mono-инстансами generic-тел — любая mono-вариантная аннотация (тип упоминает T) отравит все mono; все атомы обязаны гейтиться на gs-free/конкретность (существующие гейты переиспользуются). nova_tests — только byte-identical baseline, не гейт корректности.

### IfPathCallExpr [medium]
ROOT: Три независимых пробела чекер-канала (все in_resolved=false, один CU d102+prelude+conformance-peers):

(1) kind=If 126 — это НЕ unit-if (те закрывает Channel 5 emit_c.rs:37237: else_.is_none() → nova_unit) и НЕ statement-if как отдельный класс: ВСЕ 126 — if-С-else (span If-узла обрывается на `}` then-ветки, else в span не входит — потому в сэмплах выглядят как else-less). Падают потому, что checker preamble (types/mod.rs:6506-6524) аннотирует If через infer_expr_type, а его If-arm (types/mod.rs:10462-10513) возвращает None в трёх случаях: (a) АСИММЕТРИЯ: then-блок без trailing-expr → then_t=None (у else та же форма даёт Some(Unit), :10474!) — это весь класс statement-position цепочек `if c<0x80 { seq=1 } else if ...` (std/runtime/string/core.nv:110-113,187-189, parse.nv:42-53, defaults.nv:89-90, range.nv:190); (b) branch-trailing не берётся infer_expr_type'ом: variant-ctor `Ok(n)/Some(n)/None/Err(...)` (d135:14, d209:21, d282, sync.nv:1933, transform.nv:74/84, char.nv:22/33), container-method `v.push(1)`/`@buf.push(...)` (d275:31, string_builder.nv:105-110, resolve_instance_method_return гейтит контейнеры), Binary `@cap * 2` (vec/core.nv:312 — у infer_expr_type НЕТ Binary-arm), TupleLit с Binary-элементами (read_buffer.nv:361-365); причём preamble-аннотация PRE-ORDER — дети ещё не в resolved_types_buf, их канальные типы недоступны; (c) типы веток разошлись/не разрешились → умышленный None (D275-гейт от 2026-07-02, честный).

(2) desc=Path:__array.T 128+ — парсер (parser/mod.rs:8625-8633) превращает `[]T` в expr-позиции в синтетический Path(["__array","T"]) — это ИМЯ ТИПА, receiver статических ctor `[]u8.with_capacity(n)` (все сайты: std/runtime/string/*.nv, string_builder.nv:40, write_buffer.nv:46, read_buffer.nv:52/82, utf16.nv:53/78, errors.nv:228, d38/d180/d181/d145). Чекер корректно НЕ аннотирует его (исключён в infer_method_call_channel_type :11258-11261). Но codegen-пробы receiver'а — recv_c_type_materialized(obj) (emit_c.rs:36976, `_ =>` arm :37003 → infer_expr_c_type) и fn_field_call_sig (:37033) — вызываются в цепочке dispatch-блоков emit'а Call-Member (24255, 24358, 25764, 26074, 26275, 27252... — ДО D38 static-ctor арма :24290) по 4-8 раз на сайт, каждый раз проваливаясь в legacy Path-arm (:40820-40849) с ответом "" (пустая строка). Сам ЗАПРОС лишний — у type-name нет value-типа.

(3) desc=Call:<expr> 174 — это НЕ closure/fn-field calls (гипотеза не подтвердилась): desc считается по СЫРОМУ func.kind (:37357-37362), а func = TurboFish{base: Ident} → `_ => "Call:<expr>"`. Все сайты — вызовы generic free-fn с явными type-args: `size_of[T]()` (доминирует: vec/core.nv:292..., mutate.nv, restructure.nv — умножается mono-инстансами), `alloc_buf[T](n)` (core.nv:285), d16_first[int](xs), d216_dup[int](42), d310_twice[i64](10). Двe причины: (a) f1_check_call РАЗВОРАЧИВАЕТ TurboFish (:8306-8308) и находит callee, но annotation-гейт (:8531) режет return, упоминающий generics callee (`-> Option[T]`, `-> T`, `-> (T,U)`), НЕ подставив известные turbofish-аргументы; (b) size_of/align_of — интринсики D199, их нет в sig.fn_decls → `None => return` (:8369), аннотации нет вовсе, хотя return ВСЕГДА int (§0-факт). Channel 1 не спасает: fn_ret_by_span регистрируется ТОЛЬКО для конкретных (не-generic) callee (emit_c.rs:9947-9960 — generic'и выходят раньше), т.е. канал честно молчит, а не врёт.
LEGACY: Что отвечает legacy сейчас (для xcheck при флипе): (1) If — полный re-derive-mirror emit_if_expr: divergence-aware выбор ветки + D275 unit-домина (emit_c.rs:40273-40318), ответы nova_unit для statement-цепочек, NovaOpt_*/NovaRes_* для ctor-веток; (2) Path:__array.T — пустая строка "" (fallthrough legacy Path-arm :40820-40849: не variant/record/sum, u8∉var_types, __array_u8∉var_types); потребители деградируют к name-keyed dispatch, реальный лоуэринг делает D38-арм :24290 (nova_array_new_<suffix> → NovaArray_<suffix>*); (3) Call:<expr> — legacy unwrap_turbofish (:37992) → Ident-branch Plan 54 Ф.4 Source 1: type_ref_to_c(turbofish args) → subst в declared return (d16_first[int]→NovaOpt_nova_int, d216_pair→mono-tuple, d310_twice[i64]→int64_t); size_of/align_of → интринсик-special (:21335) → nova_int. Канальные аннотации по fix-spec байт-идентичны этим ответам на всех попавших в трейс сайтах.
FIX: ЦЕЛЕВАЯ ФОРМА (§0): чекер аннотирует join if-веток, подставленные turbofish-возвраты и интринсики; codegen-пробы НЕ спрашивают C-тип имени типа. Три атома:

АТОМ 1 — If (закрывает ~2/3 из 126):
1a. types/mod.rs:10468 — убрать асимметрию: then-блок без trailing и без divergence → then_t = Some(TypeRef::Unit(expr.span)) (точное зеркало else-стороны :10474). Дальше существующий D275-join (unit-доминирование :10492-10499, equality :10501-10505, разошлись→None :10511) сам решает. Покрывает все statement-form цепочки (string/core, parse, defaults, range — оба хвоста Unit → equality-join Unit). Гейт против лжи (§1): join уже зеркалит emit_if_expr (D275-mirror от 2026-07-02) — НИЧЕГО в join-правилах не менять.
1b. types/mod.rs:7004 (f1_expr match-arm ExprKind::If, ПОСЛЕ рекурсии в ветки) — POST-ORDER повторная аннотация, если preamble (:6506-6524) не записал: branch-тип := infer_expr_type(trailing) ИЛИ resolved_types_buf[trailing.id] (паттерн operand_rt из Binary-arm :6837-6841 — дети уже аннотированы). Это подхватывает Call-хвосты, аннотированные infer_method_call_channel_type/Some-ctor (:6742-6768), и Binary-хвосты (`@cap*2` через Binary-канал :6812). Тот же join (divergence через block_diverges/expr_diverges + D275 unit-домина + equality; extract join в helper, чтобы не дублировать :10462-10513). Писать ТОЛЬКО при полностью разрешённом join; иначе молчать → legacy (tally, не ложь).
1c. Остаток (честный residual до отдельных атомов): ветки `Ok(n)/Err(e)` (нужен контекст fn-return — контекстная типизация Result, отдельный атом) и контейнер-методы `v.push(1)` (resolve_instance_method_return container-гейт). Не трогать здесь.

АТОМ 2 — Path:__array (тривиальный, снимает все 128):
2a. Убрать ЗАПРОС: emit_c.rs:36977 (recv_c_type_materialized) — арм `ExprKind::Path(parts) if parts.len()==2 && parts[0]=="__array" => None` («ресивер — имя типа, side-channel неприменим»; ровно паттерн [M-172.1-d174] :36970-36975, None=отказ канала, не угаданный тип). Аналогично fn_field_call_sig :37033 → return None. Все .unwrap_or_default()-потребители получают "" — байт-идентично legacy-ответу.
2b. Добить прямые вызовы infer_expr_c_type(Path __array) (аудит 14829/21399/22523/34603): pre-legacy арм в dispatcher (после Channel 6c, ~:37338): Path(["__array", _]) → return String::new() — дословный подъём legacy-ответа (Path-arm :40820 falls-through → ""), по образцу Channel 6c; коммент «type-name receiver, значения нет; D38 static-ctor арм :24290 дальше отрабатывает по имени».
2c. НЕ аннотировать сам Call `[]T.with_capacity()` (19 hits) в чекере: честный ответ = NovaArray_<elem>* (legacy :39258-39282, emit :24290 лоуэрит в nova_array_new_*), а ResolvedType канонизирует []T→Named{Vec} → resolved_type_to_c → Nova_Vec____<elem>* (Vec-flip :2040-2059) — канал БЫ СОВРАЛ. Блокировано известной [M-array-vec-unify] (std/sort.nv:104-109 фиксирует legacy-lowering как codegen blocker). Задокументировать residual.

АТОМ 3 — Call:<expr> (снимает ~все 174):
3a. types/mod.rs:8528-8560 (f1_check_call, единственный-callee) — ПЕРЕД гейтом :8531: если func был TurboFish (base извлечён :8306; поднять type_args) и type_args.len()==callee.generics.len() → map {generic.name → type_arg}, ret_subst = crate::const_fn_trampoline::subst_type_ref_pub(ret_ty, &map) (уже импортирован, :8553 Self-паттерн), затем СУЩЕСТВУЮЩИЕ гейты mentions_any(callee_gs) + mentions_any(gs) на ПОДСТАВЛЕННОМ типе. `d16_first[int]`→Option[int], `d216_pair[int,str]`→(int,str), `d310_twice[i64]`→i64; `alloc_buf[T]` внутри generic-тела: type_arg=T ∈ gs → гейт молчит → legacy (корректно, mono-subst — codegen). То же зеркально в multi-overload ветке :8352-8366 (опционально, редко).
3b. size_of/align_of: types/mod.rs:6747 (f1_expr Call-arm, else-if рядом с Some-ctor каналом :6756) — `fname=="size_of"||"align_of"` && !scope.contains_key(fname) && self.sig.fn_decls.get(fname).is_none() (гейт от шэдоуинга, зеркало интринсик-условия emit_c.rs:15944/21335) → insert Scalar{width:64, signed:true, wide_default:true} (→ nova_int, байт-идентично legacy). §0: D199-факт «size_of[T]() -> int», не дублированная таблица.

ГЕЙТЫ ПРОТИВ ЛЖИ В КАНАЛЕ (§1) — сводно: (i) If-join пишет только полностью разрешённый join, правила = точное зеркало emit_if_expr (D275/Plan125), никакого «then-побеждает»; (ii) __array — канал отвечает «нет типа» (None/""), никогда выдуманный NovaArray/Vec; (iii) turbofish — аннотация только после подстановки И прохождения обоих mentions_any-гейтов; Channel 1 не может соврать (fn_ret_by_span только concrete — verified :9952-9956); (iv) size_of — только при отсутствии user-shadow. Никаких дефолтов-заглушек нигде.

ВЕРИФИКАЦИЯ: targeted fixtures cgfix_fluent_tail_if, cgfix_fluent_tail_chain (D275-регресс-прецедент!), d275_if_match_unit_coerce, d135, d85, d209, d282, d16, d216, d310, d38, d180, d181, d145; conformance single-CU прогон (project-conformance-single-cu-run) + пересчёт tally NOVA_P67_TRACE: ожидание If 126→~40 (residual Ok/Err+container), Path:__array 128→0, Call:<expr> 174→~0-10.
POINTS: compiler-codegen/src/types/mod.rs:10462-10515 — infer_expr_type If-arm: then-без-trailing None→Some(Unit) асимметрия (:10468 vs :10474); join-правила НЕ менять; compiler-codegen/src/types/mod.rs:7004-7021 — f1_expr ExprKind::If match-arm: post-order аннотация с fallback на resolved_types_buf детей (паттерн :6837-6841); compiler-codegen/src/types/mod.rs:6506-6524 — pre-order If/Block/Path preamble (оставить; post-order пишет только при отсутствии ключа); compiler-codegen/src/types/mod.rs:8306-8308, 8528-8560 — f1_check_call: turbofish type_args → subst_type_ref_pub в ret_ty ПЕРЕД mentions_any-гейтами; зеркально multi-overload :8352-8366; compiler-codegen/src/types/mod.rs:6747-6769 — f1_expr Call-arm: size_of/align_of интринсик-канал → Scalar int (гейт: не в fn_decls, не в scope); compiler-codegen/src/codegen/emit_c.rs:36976-37004 — recv_c_type_materialized: арм Path(["__array",_]) → None; compiler-codegen/src/codegen/emit_c.rs:37016-37034 — fn_field_call_sig: тот же skip; compiler-codegen/src/codegen/emit_c.rs:~37338 (после Channel 6c) — pre-legacy арм Path __array → "" (подъём legacy-ответа :40820-40849); compiler-codegen/src/codegen/emit_c.rs:24255/24290 — порядок fn-field-проба → D38 static-ctor dispatch (потребитель __array-пробы); compiler-codegen/src/codegen/emit_c.rs:37237-37249 — Channel 5 If-роутинг (не трогать; checker-аннотация берёт precedence через Channel 2); compiler-codegen/src/codegen/emit_c.rs:40273-40318 — legacy If-arm (зеркало emit_if_expr; кандидат на удаление при tally=0); compiler-codegen/src/codegen/emit_c.rs:9947-9960 — fn_ret_by_span: регистрация только concrete callee (гарантия, что Channel 1 не врёт для generic turbofish); compiler-codegen/src/codegen/emit_c.rs:2033-2061 — resolved_array_to_c Vec-flip (причина, почему Call []T.ctor() НЕЛЬЗЯ аннотировать до [M-array-vec-unify]); compiler-codegen/src/parser/mod.rs:8625-8633 — генерация Path(["__array",T]) (источник синтетики; не менять)
RISK: Главный риск — атом 1 (If): канальная аннотация через Channel 2 ПЕРЕКРЫВАЕТ emit-фоллбек; малейшее расхождение join-логики с emit_if_expr (divergence-приоритет Plan 125, unit-домина D275/[M-codegen-fluent-tail-if-unify]) даёт класс CC-FAIL `tmp(T*) = NOVA_UNIT` — свежий прецедент D275-регресса от 2026-07-02 (cgfix_fluent_tail_if/chain). Митигация: join извлечь в один helper, писать только полностью разрешённый join, гонять оба cgfix-фикстура + d275 первыми. Атом 3 риск средне-низкий: подстановка turbofish может дать тип с mono-side-effect'ами при lowering (register_mono_tuple для (T,U) — идемпотентен, прецедент Channel 2); опасность вранья закрыта двумя mentions_any-гейтами ПОСЛЕ подстановки; проверено, что fn_ret_by_span не содержит generic-спанов (иначе Channel 1 перехватил бы с erased-типом). Атом 2 риск ~нулевой: и None-skip, и pre-legacy "" байт-идентичны текущему legacy-ответу (Path-arm возвращает "" на __array); единственная теоретическая дырка — локал с именем элемента (`var_types.get(\"u8\")`) менял бы legacy-ответ, практически исключено. Общий гейт корректности: detect172 pos+neg + звучность (nova_tests только byte-identical baseline, не гейт корректности); полный conformance-CU в конце фазы.

### kind=Other desc=Other:Discriminant(16) = ExprKind::Index (variant 16 в ast/mod.rs:2155), 325 заходов. ВАЖНО: src_file=d102_named_args_default_params.nv в трейсе — стейл-лейбл (source_file_name не обновляется per-expr); реальный ключ file_id. Декомпозиция по file_id (span-байты сверены с содержимым файлов): (A) 88× fid=182 std/sort.nv — `@[i]`/`@[j+1]` SelfAccess-индексация в extension-методах на `[]int` (concrete: @sort/@sort_by/@min/@max/@sum/@product) и `[]T` (generic: @min_of/@sort_of/@reverse_of/…); (B) ~140× fid=168/170/174/175/179/180 std/collections (hashmap.nv `@_buckets[idx]`, vec/sort.nv `@data[a]`, vec/mutate.nv `@data[@len]`, vec/access.nv, vec/protocols.nv `other.data[i]`, vec/views.nv) — индексация raw-buffer-поля `data *mut T` внутри GENERIC-методов Vec[T]/HashMap[K,V]; (C) ~60× conformance-тесты (fid 25/41/57/72/85/93/104/110/154): d231 `buf[i]` (obj = typed pointer `*mut byte`), d262 `pair.0[0]` (tuple-member), d145/d251/d284/d60 — локалы `strs[0]`/`got[1]`/`c[0]` с inferred-binding (`ro x = expr` без аннотации, RHS чекеру не резолвится); (D) ~17× fid=200-204 std/runtime/string — `bytes[i]`/`buf[n]` (та же C-природа: `[]byte`/inferred locals). [medium]
ROOT: Index-проба f1_expr (types/mod.rs:6943-7003) извлекает элемент ТОЛЬКО из структурного TypeRef::Array/FixedArray (+ Readonly-обёртка) — строки 6982-6992 — с primitive_gate (7936→is_primitive_lowerable, types/mod.rs:344). Промахи по четырём причинам: (1) Receiver `@`: f1_check_fn (types/mod.rs:6089-6096) кладёт scope["@"] = TypeRef::Named{path:[recv.type_name]}, а для slice-ресивера type_name — СТРОКА-сплющивание "[]int"/"[]T" (parser/mod.rs:2867-2872); infer_expr_type(SelfAccess)=scope.get("@") (10603) возвращает псевдо-Named{"[]int"} — НЕ Array → elem=None → без аннотации. Структурная форма recv.receiver_ty (ast/mod.rs:692, Plan 153.5) в f1 НЕ используется. Побочно: SelfAccess-проба (6436-6443) уже сегодня пишет в канал псевдо-имя Named{"[]int"} — инертная ложь, нейтрализуемая только var_types-предпочтением потребителя (emit_c.rs:37140-37151). (2) Generic-контейнеры: элемент `@data[i]` = T; ExprId-канал ОДИН на AST-узел, а тело эмитится N раз с разными mono-подстановками — канал принципиально не может нести per-instantiation ответ; primitive_gate корректно отсекает (честный tally, не ложь). (3) TypedPtr: в extraction-match нет арма для `*mut T` (ptr[i] ≡ *(ptr+i)); legacy это умеет (emit_c.rs:37920-37933). (4) Inferred-локалы: `ro bytes = s.as_bytes()` — infer_expr_type(obj) → None (RHS-вызов чекеру в f1-scope не резолвится) → проба молчит. Нет также арма для номинального Named{"Vec",[elem]} (аннотация локала `v Vec[str]` даёт Named, не Array — D239-канонизация).
LEGACY: Legacy Index-арм (emit_c.rs:37811-37934), каскад: (1) obj_ty_pre = infer_expr_c_type(obj); для `@[i]` obj=SelfAccess → Channel 3 var_types["nova_self"] (emit_c.rs:37184-37191) — надёжный PER-MONO C-тип ресивера ("NovaArray_nova_int*" для `[]int`-ресивера, "Nova_Vec____X*" для Vec-mono). (2) Если имя self-describing (Nova_Vec____/NovaArray_, 37835-37857) — декодирует элемент ПРЯМО из mono-имени: strip "NovaArray_"→elem; для Nova_Vec____ — generic_type_instance_info реестр (первый type-arg) или strip префикса. Это АВТОРИТЕТНЫЙ per-instantiation ответ, комментарий 37824-37834 прямо говорит: name-keyed реестр обходится, т.к. mono-имя авторитетнее. (3) Иначе array_element_types реестр: по голому имени переменной (37858-37862) или по синтез-ключу "(nova_self->field)" для @field (37863-37871) — last-wins across folder-module peers, отравляем одноимённой переменной другого типа (задокументированный дефект). (4) compute_array_elem_type_for_obj — deep field-chain по record-схемам (37872-37878). (5) str→nova_char (37884-37887). (6) typed pointer: strip '*' → pointee (37920-37933). (7) Иначе panic [P67-LEGACY]. Спрашивают: все потребители infer_expr_c_type при эмите — тип let-binding `ro key = @[i]`, операнды сравнений `@[j] > key`, типизация аргументов `cmp(@[j], key)`, LHS/RHS assignment `@[j+1] = @[j]`, `Some(m)` NovaOpt-выбор (отсюда кратность до 3-6 на span).
FIX: Целевая форма (§0), три независимых атома. АТОМ 1 — структурный `@` в f1-scope (закрывает concrete-половину sub-bucket A, ~40-50 заходов): в f1_check_fn (types/mod.rs:6089-6096) при инъекции scope["@"] предпочесть recv.receiver_ty (ast/mod.rs:692) когда Some; иначе если type_name начинается с "[]" — построить TypeRef::Array(inner) по depth+elem (зеркало parser/mod.rs:2872), иначе текущий Named{type_name,generics}. НЕ трогать current_recv_type (6073, строковый — на нём висит метод-резолюция "[]"-типов). Тогда infer_expr_type(SelfAccess)→Array(int) и существующая Index-проба аннотирует Scalar-int. Заодно SelfAccess-проба (6436-6443) перестаёт писать псевдо-Named{"[]int"} в канал (устранение инертной лжи, §1). АТОМ 2 — расширение elem-extraction Index-пробы (types/mod.rs:6981-6999): (a) арм TypedPtr(_, inner) → elem=inner (зеркало legacy 37920-37933; закрывает d231 и часть D); (b) арм Named{name:"Vec", args:[elem]} — номинальный D239-карриер (аннотация локала `v Vec[str]`); (c) прозрачность Mut/Unsafe-обёрток (сейчас peel'ится только Readonly) — через общий helper. ГЕЙТЫ ПРОТИВ ЛЖИ (§1): primitive_gate ОСТАЁТСЯ на элементе (generic T/record/вложенный Vec → без аннотации → legacy, честный tally); НИКАКОГО fallback при None (зеркало D263-комментария 6867-6873 и удалённого Member-fallback fd3207d6); НИКОГДА не аннотировать элемент, упоминающий параметр T (ExprId делится между mono-инстансами — аннотация была бы ложью для всех инстансов кроме одного). АТОМ 3 — sub-bucket B (~140, generic-тела Vec/HashMap): целевая форма = mono-aware канал 172.1.2 (resolved_type_to_c резолвит Named{T} через активный current_type_subst в момент эмита); интерим, консистентный с прецедентом U.4.4b var_types-gate: поднять self-describing-декод (emit_c.rs:37835-37857) в диспетчер как явный Channel 7 «mono-receiver Index» перед legacy (~37338): для ExprKind::Index получить obj C-тип через каналы (SelfAccess→Channel 3), декодировать элемент из mono-имени + generic_type_instance_info — дословный лифт авторитетной логики, не новая ложь (тот же паттерн, что Channel 6c). Sub-bucket C inferred-локалы (`ro x = call()`) закрываются НЕ здесь, а прогрессом Call-канала (resolved_callees→fn_ret): когда RHS-тип попадает в scope через let-inference, Index-проба сработает сама — отдельный кластер. Верификация: targeted fixtures (spec_tests conformance single-CU прогон, project-conformance-single-cu-run) + detect172 pos+neg; tally-повтор NOVA_P67_TRACE: ожидание — Discriminant(16) с 325 → ~60 (остаются inferred-локалы C) после атомов 1-2, → ~0 после атома 3.
POINTS: compiler-codegen/src/types/mod.rs:6089-6096 — f1_check_fn инъекция scope["@"] (Named{type_name} → структурный recv.receiver_ty / Array); compiler-codegen/src/types/mod.rs:6943-7003 — f1_expr Index-арм; elem-extraction 6981-6999 (добавить TypedPtr / Named{Vec,[elem]} / Mut-peel); compiler-codegen/src/types/mod.rs:6436-6443 — SelfAccess-проба (сегодня пишет псевдо-Named{"[]int"} в канал — станет truthful после атома 1); compiler-codegen/src/types/mod.rs:10603 и 13916 — infer_expr_type SelfAccess = scope.get("@") (потребители формы @); compiler-codegen/src/types/mod.rs:7936 + :344 — primitive_gate / is_primitive_lowerable (гейт §1, НЕ ослаблять); compiler-codegen/src/ast/mod.rs:674-701 — Receiver.type_name (строка "[]T") vs receiver_ty:692 (структурный TypeRef, Plan 153.5); compiler-codegen/src/parser/mod.rs:2867-2872 — сплющивание receiver type_name в "[]…"-спеллинг; compiler-codegen/src/codegen/emit_c.rs:37811-37934 — legacy Index-арм (лифтуемая логика: self-describing декод 37835-37857, ptr-deref 37920-37933, array_element_types 37858-37871); compiler-codegen/src/codegen/emit_c.rs:37118-37341 — диспетчер infer_expr_c_type; точка вставки Channel 7 перед legacy (~37338); Channel 3 SelfAccess var_types["nova_self"] 37184-37191; compiler-codegen/src/types/mod.rs:1779 — env.resolved_types = resolved_types_buf.take() (канал накрывает и std-файлы CU — f1 ходит по merged module)
RISK: (1) Смена формы scope["@"] (Named{"[]int"}→Array) затрагивает ВСЕХ потребителей scope.get("@"): f3_check_member (@.field), check_target_readonly, typeref_is_ptr (types/mod.rs:11949), обе SelfAccess-ветки infer_expr_type — сегодня псевдо-имя "[]int" нигде не матчится (инертно), Array строго информативнее, но каждый потребитель требует аудита на армы, ведущие себя иначе на TypeRef::Array (например str-index диагностика E_STR_NO_INT_INDEX 6956 — не задеть). (2) КАНАЛ НЕ ДОЛЖЕН ВРАТЬ про сам `@`: mono C-тип ресивера `[]int` = NovaArray_nova_int* (legacy runtime-тип, [M-153.x-array-new-not-vec]), а канонический lowering Named{Vec,[int]} дал бы Nova_Vec____nova_int* — расхождение имён; защита существует (Channel 2 SelfAccess предпочитает var_types["nova_self"], 37140-37151) — НЕ снимать этот guard; аннотация ЭЛЕМЕНТА (nova_int) идентична в обоих мирах — безопасна. (3) Атом 3 (Channel 7) наследует зависимость от порядка регистрации generic_type_instance_info — при промахе реестра обязан падать в legacy, не в fallback-тип. (4) Изменения behavior-change (не byte-identical): гейт = detect172 pos+neg + звучность (feedback-nova-tests-not-correctness-gate), полный nova test не является гейтом. (5) Кратность вызовов infer_expr_c_type (3-6× на span) означает: одна неверная аннотация размножается по всем потребителям (binding/сравнение/аргумент) — усиливает цену лжи, primitive_gate обязателен.



## CONFIRMED problems (16, финальный результат)

### types/mod.rs:6431 [SUSPECT]
WHAT: Каждый ExprKind::Ident с установленным id, чей тип разрешил infer_expr_type: параметры fn (declared ty), let-локалы (аннотация или inferred RHS), а при промахе scope — round-trip через resolved_types_buf либо last-resort скан «голый unit-вариант sum-типа». Аннотируются ВСЕ иденты, включая типизированные голым generic-параметром (`x T` → Named{T}).
REASON: Три небезупречности. (1) Комментарий сайта (6413-6416) декларирует GATE «skip bare generic-params (T)», но в коде гейта НЕТ — более поздний комментарий 6423-6430 признаёт: «annotate ALL Idents… the consumer applies a var_types-gate». Generic-level Named{T} попадает в канал, а корректность делегирована парному guard'у недоверия в consumer'е (emit_c.rs 37152-37163: Ident в var_types → канал ИГНОРИРУЕТСЯ) — ровно паттерн прецедент-бага «канал пишет, codegen не верит». Для идента ВНЕ var_types Named{T} лоуэрится через current_type_subst: чужой одноимённый параметр в объемлющем generic-контексте → МОЛЧА неверный C-тип; пустой subst → фабрикация `Nova_T*` (resolved_named_to_c fallback emit_c.rs:2288 `format!("Nova_{}*", full)`). (2) Last-resort скан в infer_expr_type (10373-10387) угадывает тип по имени варианта, итерируя HashMap self.types БЕЗ проверки уникальности — при коллизии имён вариантов у двух sum-типов выбор недетерминирован; путь реально достижим: ClosureLight-параметры (7195-7197) и неаннотированные for-переменные (f1_for_body 7838-7847) НЕ добавляются в scope. (3) Смягчение: основные источники — факты (declared types; block-shadowing корректно snapshot/restore 6182-6211), поэтому не POISON.
EVIDENCE: Код 6419-6432 без гейта: `if let ExprKind::Ident(_) = &e.kind { if e.id.is_set() { if let Some(tr) = self.infer_expr_type(e, scope) { let rt = ResolvedType::from_type_ref(&tr); … insert(e.id, rt); } } }`. Last-resort guess (10373-10387): `// Last resort: if name = a unit-variant of a known sum type… for (type_name, td) in &self.types { if let TypeDeclKind::Sum(variants) = … if variants.iter().any(|v| &v.name == name) { return Some(TypeRef::Named { path: vec![type_name.clone()], … }) } }`. Парный guard недоверия (emit_c.rs 37152): `// Ident in var_types: prefer reliable per-local codegen state over channel.`
VERIFY: Read all cited sites plus data-flow sources. (1) VERIFIED: producer comment types/mod.rs:6413-6416 promises a bare-generic-param gate that is absent from the code (6419-6432 annotates every scope-resolved Ident; params seeded verbatim at 6085/12758, so `x T` → Named{T} in channel; from_type_ref:154-161 has no Any-mapping for bare type params). Correctness is delegated to the consumer var_types distrust-guard (emit_c.rs:37152-37163) — the exact precedent-bug pattern. For idents absent from var_types (Cat-B13, admitted in the consumer's own comment), Named{T} lowers via resolved_named_to_c: silent current_type_subst/type_subst_overrides substitution for empty-args names (emit_c.rs:2129-2135 — a same-named param of an enclosing mono context yields a silently wrong C type) or the `format!("Nova_{}*", full)` fabrication fallback (emit_c.rs:2288); both return Ok, so the Ok(ir_c) guard at 37139 does not reject and the value is returned at 37177. No invariant guarantees such idents are in var_types. (2) VERIFIED: last-resort scan (mod.rs:10373-10387) iterates self.types = HashMap<String,&TypeDecl> (mod.rs:3075, std RandomState → nondeterministic order), returns the first non-generic sum type with a matching unit-variant name, no cross-type uniqueness check. Reachability confirmed: ClosureLight params are not added to scope (mod.rs:7195-7198) and unannotated for-loop pattern vars are not either (mod.rs:7838-7848 `_ =>` arm), so first-visit use-sites miss scope and resolved_types_buf and hit the scan; a variant-name collision writes a wrong/nondeterministic type to the channel. Naming convention (PascalCase variants) reduces probability but is not a gate. (3) The softening (main sources are facts; block shadowing snapshot/restore) is the audit's own mitigation supporting SUSPECT rather than POISON. I found no gate or invariant the auditor missed; the SUSPECT verdict stands.

### types/mod.rs:6440 [SUSPECT]
WHAT: ExprKind::SelfAccess (`@`) в instance-методах; тип берётся из scope["@"], инжектированного f1_check_fn (6089-6096) как Named{recv.type_name, generics: recv.generics} — т.е. для метода generic-типа `fn Box[T] @get` аннотация = Box[T] на GENERIC-уровне (args=[Named{T}]).
REASON: Для non-generic ресиверов аннотация — факт (Named{Type} совпадает с декларацией). Для generic-ресиверов записывается generic-level тип, чья конкретизация целиком зависит от current_type_subst codegen'а В МОМЕНТ потребления: в mono-контексте args лоуэрятся верно, в generic-stub/при пустом subst — `Nova_Box____Nova_T_p`-класс мусора (resolved_named_to_c 2246-2265 лоуэрит args через resolved_type_to_c, где Named{T} без subst → `Nova_T*`). Consumer сам держит ЖИВОЙ парный guard недоверия: и в Channel 2 (37140-37151), и в Channel 3 (37184-37191) var_types["nova_self"] предпочитается каналу с явной пометкой про gap #2 — точный анти-паттерн прецедента (канал может врать, codegen страхуется). Сам факт guard'а доказывает, что записанное значение признано контекстно-ненадёжным.
EVIDENCE: Producer (6436-6442) без гейта: `if let ExprKind::SelfAccess = &e.kind { if e.id.is_set() { if let Some(tr) = self.infer_expr_type(e, scope) { … insert(e.id, rt); } } }`; scope-источник (6091-6095): `scope.insert("@".to_string(), TypeRef::Named { path: vec![recv.type_name.clone()], generics: recv.generics.clone(), … })`. Guard недоверия (emit_c.rs 37140-37143): `// SelfAccess: prefer var_types["nova_self"] when available (reliable per-scope codegen state; channel depends on current_type_subst timing — gap #2).`
VERIFY: Не опровергнуто — вердикт SUSPECT подтверждён с усилением. (1) Write-site (types/mod.rs:6436-6443) пишет generic-level Named{recv, args:[Named{T}]} из scope["@"] (инжект f1_check_fn:6089-6096) БЕЗ гейта на generic-ресиверы, при том что соседний Ident-arm (6413-6418) сам документирует этот класс как опасный («Nova_T* vs void*»). (2) Гейта в лоуэринге нет: resolved_named_to_c (emit_c.rs:2246-2265) лоуэрит args через unwrap_or("nova_int"), а голый Named{T} при пустом current_type_subst падает в `Nova_T*` (строка 2288) — всё возвращается Ok, значит consumer-guard `if let Ok(ir_c)` НЕ отводит generic-level значение в legacy. (3) Инвариант «generic-receiver тела эмитятся только per-mono с полным subst» ЛОЖЕН: emit_generic_method_erased (12361) эмитит erased-тела instance-методов generic-типов с пустым subst и var_types["nova_self"] (12498) — живой контекст потребления, где аннотация лоуэрится в мусор. (4) Парный guard недоверия существует в обоих каналах (Ch2 37140-37151, Ch3 37184-37191) с явным комментом про gap #2; хуже — guard стоит ВНУТРИ ветки Ok(resolved_type_to_c(rt)), т.е. лоуэринг с side-effect'ом выполняется ДО guard'а и в erased-контексте регистрирует мусорный mono-инстанс `Box____Nova_T_p` в generic_type_worklist/generic_type_instance_info даже когда guard затем отбрасывает значение. Единственное смягчение: для non-generic ресиверов аннотация — контекстно-независимый факт, но аудит это уже учёл, ограничив SUSPECT generic-ресиверами. Свежий tally-трейс не содержит SelfAccess-заходов в legacy — SelfAccess всегда покрыт каналами, т.е. корректность зависит именно от var_types-guard'а, а не от достоверности записанного значения.

### types/mod.rs:6453 [POISON]
WHAT: ExprKind::Index `obj[i]`: (a) obj типа []T/[N]T/ro []T → тип элемента; (b) range-индекс `obj[a..b]` → тип obj (слайс); (c) obj Named-типа → ДЕКЛАРИРОВАННЫЙ return-тип `@index`, найденный find_method_decl — клонируется ДОСЛОВНО, без подстановки generic-аргументов ресивера.
REASON: Ветки (a)/(b) — факты. Ветка (c) пишет заведомо возможно-неверный тип как факт: (1) НЕТ гейта generics.is_empty() на obj_tr (в отличие от Member-арма, где он есть) — для `v Vec[str]` (номинальная запись) или user `MyMap[K,V]` возвращается голый `T`/`V` из декларации `export fn Vec[T] @index(i int) -> T` (std/collections/vec/access.nv:31) БЕЗ подстановки → канал утверждает `v[i] : Named{T}` при истине `str`. (2) find_method_decl (9855-9866) возвращает `fns.first()` при итерации HashMap-таблицы методов — у Vec МИНИМУМ три @index-оверлоада (`-> T`, `@index(r Range) -> Self` slice.nv:34, `mut @index(i,val)`), выбор произволен: аннотация `Self` лоуэрится resolved_named_to_c (emit_c.rs 2193-2198) в ресивер ОБЪЕМЛЮЩЕГО метода на момент codegen — совершенно чужой тип. (3) Consumer Channel 2 для Index НЕ имеет guard'а (возвращает ir_c как есть, 37136-37178): Named{T} через current_type_subst → МОЛЧА неверный тип при одноимённом параметре объемлющего generic-контекста, иначе фабрикация `Nova_T*`. (4) Это ЕДИНСТВЕННЫЙ producer Index-аннотаций (grep всех insert-сайтов: другие пишут только Call/Member) — никто не перезаписывает исправленным значением.
EVIDENCE: infer_expr_type Index-арм (10903-10911), без generic-гейта и без подстановки: `TypeRef::Named { path, .. } => { // User-defined @index(key K) -> V: look up @index method on type. if let Some(type_name) = path.last() { if let Some(fd) = self.find_method_decl(type_name, "index") { if let Some(ret) = &fd.return_type { return Some(ret.clone()); } } } }`. find_method_decl (9859-9861): `for (k, fns) in methods.iter() { if k.trim_start_matches('@') == name { return fns.first().copied(); } }`. Декларация: std/collections/vec/access.nv:31 `export fn Vec[T] @index(i int) -> T`; slice.nv:34 `export fn Vec[T] @index(r Range) -> Self`.
VERIFY: Could not refute — every load-bearing claim of the audit is confirmed by direct code reading.

(1) NO generics gate, verbatim clone — CONFIRMED. The preamble probe (types/mod.rs:6449-6456) channels infer_expr_type's Index arm (mod.rs:10887-10916). Its Named branch (10903-10912) matches ANY TypeRef::Named without checking generics and returns `fd.return_type.clone()` (10908) with no substitution of receiver type-args. std/collections/vec/access.nv:31 is exactly `export fn Vec[T] @index(i int) -> T`, so nominal `v Vec[str]` → obj_tr Named{Vec,[str]} → annotation `Named{T}` when truth is `str`; user `fn MyMap[K,V] @index(k K) -> V` → bare `Named{V}`. Contrast: the RecordLit producer (6485-6495) gates on `td.generics.is_empty()`, the Member producer substitutes receiver generics (per the consumer comment at emit_c.rs:37164-37170) — the Index Named branch has neither.

(2) Arbitrary overload pick — CONFIRMED. find_method_decl (mod.rs:9855-9867) iterates a HashMap and returns `fns.first()`; its own doc says "overloads not typical" but @index has THREE std overloads under one key: `-> T` (access.nv:31), `@index(r Range) -> Self` (slice.nv:34), `mut @index(i,val) -> ()` (access.nv:56). The syntactic Range check at 10890 only intercepts literal `a..b`, not a bound `let r=2..5; v[r]`, and does not steer which overload first() yields for `v[i]`. A picked `Self` return lowers at resolved_named_to_c (emit_c.rs:2193-2194) to the ENCLOSING method's receiver via current_receiver_type — an unrelated type (Err only outside any receiver context).

(3) No consumer guard — CONFIRMED. Channel 2 (emit_c.rs:37136-37178) has expr-kind carve-outs only for SelfAccess/Ident (var_types preference) and a trace for Member; Index returns `ir_c` unconditionally. For `Named{T}` with empty args, resolved_named_to_c first consults type_subst_overrides/current_type_subst (2129-2136) — a same-named param in the CALLER's generic context substitutes silently wrong — otherwise falls to the final arm `format!("Nova_{}*", full)` (2288), fabricating `Nova_T*` as Ok, not Err, so it does NOT fall through to legacy.

(4) No correcting producer — CONFIRMED. Grep of all resolved_types_buf insert sites: the only other Index-id producer is the main-dispatch arm (mod.rs:6981-6999), gated to structural []T/[N]T with primitive elements; its own comment (6977-6979) states Named containers / custom @index are "not annotated → legacy (minimal, sound)" — an invariant the preamble probe at 6453 silently defeats. Site 9989 is IntLit-only; 8362/8559/8905/9054 key Call/Member ids. Nothing overwrites the poisoned Index annotation.

Only mitigating observation (not a refutation): when resolved_type_to_c returns Err (e.g. `Self` outside any receiver), the consumer falls to later channels/legacy, so a subset of poisoned annotations is inert. But the two dominant wrong outcomes lower Ok — `Named{T}`→Nova_T* fabrication or coincidental subst, and in-method `Self`→enclosing receiver — i.e., the channel asserts false types as facts, exactly the §1 anti-pattern of the removed Member fallback (fd3207d6). Concrete failure: `fn f(m MyMap[str,int]) -> int { m["k"] }` — checker channels `m["k"] : Named{V}`, codegen emits `Nova_V*` (or the caller-context V) instead of nova_int. POISON verdict stands.

### types/mod.rs:6490 [SUSPECT]
WHAT: RecordLit с явным именем типа (`T{...}`), гейт: последний сегмент имени найден в self.types И у декларации пустые generics. Пишет ResolvedType::Named{name: last-сегмент, module: [], args: []}. Ранний (грубый) RecordLit-слайс; для обычных non-generic record его запись позже перезаписывается более аккуратным слайсом на 6630 (last-wins), т.е. его значение ВЫЖИВАЕТ ровно в тех случаях, которые слайс 6630 сознательно исключил.
REASON: Само значение (имя объявленного типа литерала) — факт, но гейты дырявые в трёх местах, и все три — ровно зоны, которые поздний слайс 6630 исключает: (1) нет проверки TypeDeclKind::Record — срабатывает для ЛЮБОЙ non-generic декларации (Alias/Newtype/NamedTuple/Sum); для alias-на-record аннотация Named{alias-имя} лоуэрится по имени алиаса, которого нет в record_schemas; (2) нет исключения RUNTIME_DEFINED_TYPES (Error, RuntimeError, guards, str... — emit_c.rs:27-43) — комментарий соседнего слайса (6577-6581) прямо говорит, что аннотация их реального типа ДИВЕРГИРУЕТ от legacy void* (:36484); (3) module-путь стирается (module: vec![]) и lookup идёт только по last-сегменту — модульно-квалифицированный литерал `m.T{...}` гейтится по одноимённому локальному T и аннотируется как он. Не POISON: тип не выдуман (имя литерала = его тип в типичном случае), нет int-подобного дефолта; но вывод небезупречен и дублирует/пересекается с 6630 (last-wins).
EVIDENCE: mod.rs:6487-6493: `if e.id.is_set() && self.types.get(rl_last.as_str()).map_or(false, |td| td.generics.is_empty()) { self.resolved_types_buf.borrow_mut().insert(e.id, ResolvedType::Named { name: rl_last.clone(), module: vec![], args: vec![] }); }` — нет ни `TypeDeclKind::Record`-гейта, ни `RUNTIME_DEFINED_TYPES.contains` (ср. 6583-6585 в позднем слайсе), module отброшен. Поздний слайс объясняет, почему это опасно: 6579-6581 «annotating their REAL type (e.g. `str{…}` → `nova_str`) would DIVERGE».
VERIFY: Не смог опровергнуть — аудит (SUSPECT) подтверждается кодом на всех трёх пунктах; гейта/инварианта, гарантирующего корректность записанного типа в переживающих случаях, нет.

САЙТ (types/mod.rs:6485-6496): ранний RecordLit-слайс. Гейт = `type_name: Some`, последний сегмент есть в `self.types`, и у декларации `generics.is_empty()`. Пишет `Named{name: rl_last, module: vec![], args: vec![]}`.

ПОРЯДОК/last-wins подтверждён: поздний аккуратный слайс на 6574-6636 в том же преамбула-проходе перезаписывает запись через `.insert()` ТОЛЬКО когда: (a) last НЕ в `RUNTIME_DEFINED_TYPES` И (b) `self.types.get(last)` даёт `TypeDeclKind::Record`. Никакого другого RecordLit-инсерта позже нет (блок литералов 6685 покрывает лишь IntLit/StrLit/…). Значит для исключённых 6574 случаев значение раннего слайса ВЫЖИВАЕТ и доходит до codegen.

ПОТРЕБЛЕНИЕ без страховки: dispatcher `infer_expr_c_type` Channel 2 (emit_c.rs:37137-37180) для RecordLit просто делает `if let Ok(ir_c) = resolved_type_to_c(rt) { return ir_c }` — под-гарды (SelfAccess/Ident/Member) к RecordLit не применяются, проверки `record_schemas` нет. `resolved_named_to_c` (emit_c.rs:2117-2291) возвращает Err лишь для usize/isize/ptr/Self-без-ресивера (не могут быть RecordLit-именем) → для любого пользовательского имени финальный arm отдаёт `Nova_{full}*` (или NovaBox для протокола, или `type_aliases[full]` для алиаса). Т.е. Ok практически всегда → нет fall-through в legacy как предохранителя.

ДИВЕРГЕНЦИЯ подтверждена прямым сравнением с legacy-armом (emit_c.rs:37647-37717):
- Concern 2 (RUNTIME_DEFINED_TYPES): для `Error{...}` legacy идёт not-variant → not-generic → `record_schemas.contains("Error")` = false (Error — header-backed, не в record_schemas) → else → `"void*"`. Канал же пишет `Named{"Error"}` → `resolved_named_to_c` → финальный arm → `Nova_Error*`. void* ≠ Nova_Error* — ровно та дивергенция, о которой прямо предупреждает комментарий соседнего слайса (6577-6581). Гейт раннего слайса требует лишь наличия в `self.types` с пустыми generics — Error/RuntimeError non-generic, а `self.types` наполняется из ВСЕХ Item::Type включая prelude (mod.rs:1198), так что случай достижим.
- Concern 1 (нет проверки TypeDeclKind::Record): для alias/newtype `Foo` legacy → `record_schemas.contains("Foo")` = false → `void*`; канал → `Named{"Foo"}` → либо `type_aliases["Foo"]` (не-void*), либо `Nova_Foo*` — в обоих случаях расходится с legacy void*.
- Concern 3 (стирание module): `m.T{...}` (name=["m","T"]) — гейт смотрит `self.types.get("T")` (локальный одноимённый T, не m.T), пишет `Named{name:"T", module:[]}`; legacy же ключует по `raw_name="m_T"`. Ключи разные → расхождение.

Ни один из трёх переживающих случаев не защищён гейтом/инвариантом, который аудитор пропустил бы; наоборот, соседний слайс 6574 намеренно добавил именно эти три ограничения (Record-kind, RUNTIME_DEFINED_TYPES-исключение, полный путь) с комментариями о byte-identity/divergence — прямое доказательство их значимости. Тип не «выдуман» и не int-дефолт (потому не POISON), но записанный тип МОЖЕТ быть неверным (расходиться с legacy) в исключённых зонах. Опровержение недостоверно → refuted=false.

### types/mod.rs:6521 [SUSPECT]
WHAT: Control-flow probe: Block / If / IfLet / Path — пишет результат infer_expr_type(e). Фактически ЖИВЫ только Block и If: у infer_expr_type НЕТ arm'ов для ExprKind::Path и ExprKind::IfLet (падают в `_ => None` @10959) — для них probe инертен, ничего не пишет.
REASON: If-ветка аккуратна (divergence-aware, D275 unit-доминирование, требует СОВПАДЕНИЯ веток, разногласие/неразрешённость → None — 10462-10513). Но: (a) Block с непустыми stmts И trailing проходит МИМО осторожных arm'ов 10441/10445 и попадает в поздний arm 10956, который ИГНОРИРУЕТ trailing и делает reverse first-resolvable скан stmts — может записать тип ОТБРОШЕННОГО промежуточного statement-выражения как тип блока (та же first-wins-патология, из-за которой Match явно исключён из probe — комментарий 6501-6505 сам называет её «poisoning the channel»); при этом stmt-выражения инферятся во ВНЕШНЕМ scope, т.е. Ident, затенённый внутренним let, резолвится в чужой внешний биндинг; (b) совпавшие литеральные хвосты If пишут контекстно-слепой дефолт (IntLit→int @10590 — тот самый §5-hazard, который Binary-arm сам избегает: 10564-10565 «bare literals default to int/f64/char which may mismatch the declared context»), напр. `x u8 = if c { 1 } else { 2 }` → Scalar int; (c) RecordLit-хвост инферится с ПУСТЫМИ generics (10390-10396 `generics: Vec::new()`) — стирание generic-аргументов пишется в канал; (d) комментарий probe «infer_expr_type returns None for unit/never» устарел — Unit/never реально возвращаются и пишутся (10445, 10488, 10515), включая Unit для дивергирующего блока.
EVIDENCE: Запись без гейта на источник: 6514+6521 `if let Some(tr) = self.infer_expr_type(e, scope) { ... insert(e.id, rt) }`. Достижимый Block-arm: 10956-10958 `ExprKind::Block(b) => b.stmts.iter().rev().find_map(|s| { if let Stmt::Expr(e) = s { self.infer_expr_type(e, scope) } else { None } })` — trailing не участвует, хотя парсер кладёт хвост ТОЛЬКО в trailing (parser/mod.rs:9956-9962: expr перед `}` → `trailing = Some(...)`, иначе → stmts). Отладочный трейс NOVA_DEBUG_IF_INFER (6516-6519) — след прошлых инцидентов именно на этом канале.
VERIFY: Refutation attempt failed on all sub-claims. (Path/IfLet inert) infer_expr_type (types/mod.rs:10357-10961) has no ExprKind::Path or ExprKind::IfLet arms — both fall to `_ => None` @10959, so the probe @6506-6524 never writes for them; confirmed. (a) Block arms are exactly 10441 (stmts empty), 10445 (no trailing), 10956 (unguarded): a block with non-empty stmts AND trailing reaches 10956, which reverse-scans ONLY stmts (ignores b.trailing — the block's actual value) and can write the type of a discarded intermediate stmt-expression, inferred in the OUTER scope (Stmt::Let bindings skipped → shadowing hazard). The comment @10433-10440 claiming "Blocks with statements stay None" is stale/contradicted. No corrective later insert exists — the probe @6521 is the only Block annotation site. (b) `if c {1} else {2}`: both tails hit IntLit→int @10590, agree, and get written regardless of declared u8 context; the Binary arm explicitly guards this exact §5 hazard (10576-10583) while the If arm does not. (c) RecordLit @10390-10396 returns Named with generics: Vec::new(); the direct probe @6485 gates on non-generic types but the If-tail/Block-tail path has no gate → generic-arg erasure is written. (d) Comment @6499-6500 ("returns None for unit/never") is stale: Unit returned @10445/@10515, never @10488, all written; Block arms never call block_diverges (unlike If @10467), so `{ throw e }` value-block gets Unit instead of never. Decisive: no consumer-side distrust gate neutralizes wrong writes — the D275 regression documented inside this function (10453-10461) proves a wrong If annotation from this same probe already overrode the emit fallback and caused a CC-FAIL; that fix covered only unit-domination, leaving (a)/(b)/(c) unguarded. The written type CAN be wrong → audit's SUSPECT verdict stands.

### types/mod.rs:6630 [POISON]
WHAT: RecordLit non-runtime record-типа (гейт: не в RUNTIME_DEFINED_TYPES, td.kind==Record). Пишет Named{path имени литерала, args: generic-аргументы, «отзеркаленные» с legacy-дерайва emit_c.rs:36444-36461}: для каждого generic-параметра берётся inferred-тип ПЕРВОГО поля литерала, чей шаблонный тип — ровно этот bare-параметр; НЕсопоставленный параметр ДЕФОЛТИТСЯ В `int`.
REASON: Это точный аналог удалённого прецедента (f1 Member-arm fallback «не зарезолвили → пиши int, byte-identical legacy», commit fd3207d6): unwrap_or_else(int) пишет ЗАВЕДОМО возможно-неверный generic-аргумент в канал КАК ФАКТ — §1-нарушение («никаких авто-выводимых неверных типов»), оправданное только byte-identical зеркалом legacy. Кейсы, где int гарантированно ложь: (a) параметр не встречается как bare-тип поля (`type Wrap[T] { items Vec[T] }`) → find_map всегда None → `Wrap[str]{...}` аннотируется Wrap[int] → mono-имя Nova_Wrap_int вместо Nova_Wrap_str; (b) поле опущено в литерале (default-значение) или f.value==None → int; (c) infer_expr_type значения поля вернул None (напр. method-call — arm намеренно return-type-free, 10732-10738) → int; (d) IntLit-значение в контексте `Box[u8]` → int (контекстно-слепой литеральный дефолт). Плюс first-field-wins при нескольких T-полях. Внешний комментарий слайса (6565-6569 «generic records STAY on legacy until the generic-mono materialization slice») противоречит коду — код УЖЕ аннотирует generic-инстансы с int-дефолтом; drift комментария маскирует ядовитость. Byte-identical сегодня (пока legacy так же врёт и есть парный guard) — но канал содержит ложь, которой любой будущий потребитель поверит.
EVIDENCE: 6618-6622: `.unwrap_or_else(|| TypeRef::Named { path: vec!["int".to_string()], generics: Vec::new(), span: e.span })` — с собственным признанием в комментарии 6588-6590: «an unmatched param defaults to `int` (legacy `nova_int`)». Запись как факт: 6625-6630 `let rt = ResolvedType::from_type_ref(&TypeRef::Named { path: name.clone(), generics: gen_args, .. }); self.resolved_types_buf.borrow_mut().insert(e.id, rt);`.
VERIFY: Cannot refute — POISON verdict stands, verified against code. (1) The audited block (types/mod.rs:6574-6636, insert at 6630) has NO generics gate: gates are only type_name Some, id set, not RUNTIME_DEFINED_TYPES, td exists in self.types, td.kind==Record. Generic records DO enter: self.types holds all Item::Type decls including generic ones (mod.rs:3330/3349-3351). The sibling RecordLit block at mod.rs:6485-6496 IS gated `td.generics.is_empty()` — showing the gate the outer comment (6562-6573 'GATE to NON-generic … generic records STAY on legacy') describes exists only in that OTHER block; for the audited block the comment/code drift the auditor flagged is real. (2) The int-default is reachable in compiling programs: for each td.generics param, find_map matches only field decls whose template type is a BARE Named equal to the param (mod.rs:6603-6614); `type Wrap[T]{items Vec[T]}` never matches (fg non-empty) → unwrap_or_else(int) (6618-6622) → `Wrap[str]{…}` annotated Named{Wrap,args:[int]} — a semantic lie written as fact. Same default fires when the field is omitted, f.value==None, or infer_expr_type(v)==None (e.g. method-call values, deliberately uncovered). No checker diagnostic rejects generic literals with un-inferrable params (f1 RecordLit checking at 5812+ covers only assoc-consts/priv fields). (3) The lie is consumed authoritatively: dispatcher Channel 2 (emit_c.rs:37137-37179) has no RecordLit guard; resolved_named_to_c (emit_c.rs:2246-2264) computes the mono mangle FROM the args and registers worklist/instance-info side effects — wrong `int` yields Nova_Wrap_int for an actual str-instance, exactly the removed f1-Member-fallback pattern (fd3207d6). (4) Aggravating, unrefutable mirror imperfection: legacy (actual location emit_c.rs:37667-37706, line drift from audit's 36444) is LAST-match-wins and skips empty/void* results from codegen's infer_expr_c_type, while the checker probe is FIRST-match-wins (find_map) over checker infer_expr_type with different coverage — so even byte-identity with legacy is not structurally guaranteed. All refutation avenues checked (hidden gate, self.types exclusion, checker rejection invariant, from_type_ref sanitization, consumer distrust guard) — none exists. The written type CAN be wrong; §1 violation confirmed.

### types/mod.rs:6765 [SUSPECT]
WHAT: Call-выражение вариант-конструктора `Some(x)` (bare ident, 1 аргумент, не затенён локалом): аннотируется как Option[infer_expr_type(x)].
REASON: Две небезупречности. (1) Контекстно-зависимый дефолт payload'а: для литерального аргумента infer_expr_type даёт wide-default int, и Call штампуется Option[int], хотя в контексте `ro x Option[u8] = Some(200)` литерал ЛЕГАЛЬНО narrow'ится (D227 Rule 1) — literal-coercion канал (mod.rs:9993-9999) коэрсит ТОЛЬКО args[0].id (payload), но НЕ переаннотирует сам Call-id, так что аннотация Call'а (Option[int]) расходится и с коэрцированным payload'ом (u8), и с декларированным типом биндинга; Channel 2 потребляет её авторитетно без guard'а. Для НЕ-литеральных payload'ов (uint_var) вывод точен — потому не POISON, а SUSPECT. (2) Имя-ключевая эвристика «bare Some ≡ Option.Some»: guard `!scope.contains_key(fname)` покрывает только локальные переменные — не user-sum с вариантом Some и не module-level fn Some; codegen же резолвит bare-варианты ГЛОБАЛЬНО по имени с priority-эвристикой (sum_schema_registry.rs:388 — два sum'а МОГУТ зарегистрировать один variant-name, prefer «shorter name»), т.е. допущение чекера и резолюция codegen могут разойтись.
EVIDENCE: mod.rs:6756-6766 «if fname == "Some" && args.len() == 1 && !scope.contains_key(fname) { if let Some(payload_tr) = self.infer_expr_type(args[0].expr(), scope) { let opt = TypeRef::Named { path: vec!["Option"…], generics: vec![payload_tr] … insert(e.id, rt) }»; коэрция не чинит Call-id: mod.rs:9994-9999 «ExprKind::Call { func, args, .. } if args.len() == 1 => … materialize_literal_coercion(args[0].expr(), elem)» (Call-id не трогается); глобальная вариант-резолюция: sum_schema_registry.rs:238 find_variant_v2(variant_name), :388 «два sum'а … prefer'ит первое (shorter name)».
VERIFY: NOT REFUTED. Claim (1) is confirmed end-to-end by reading the code. (a) types/mod.rs:10590 — infer_expr_type(IntLit) unconditionally returns wide-default `int` without consulting resolved_types_buf, so the arm at mod.rs:6756-6768 stamps the Call `Some(200)` as Option[int] regardless of context. (b) The scenario `ro x Option[u8] = Some(200)` is legal and reaches codegen: assignable's non-literal path reads the buffered Option[int] back (mod.rs:10838-10842), would_narrow_into is Scalar-only, and cat_compatible_rt (mod.rs:11831 + test at 774-777) compares Named types by NAME ONLY, ignoring generic args → Compat::Ok, no E7301 gate. (c) materialize_literal_coercion (mod.rs:9993-9999, invoked from the definite let-site 7356) recurses only into args[0] via ctor_payload_expected and annotates the IntLit as u8; nothing ever re-annotates the Call id. (d) emit_c.rs Channel 2 (37136-37178) consumes Call annotations with no guard (guards exist only for SelfAccess/Ident/Member), and resolved_named_to_c (emit_c.rs:2148-2170) lowers Option[int] → NovaOpt_nova_int vs the binding's NovaOpt_nova_u8 — distinct C typedefs. Worse than the audit stated: the poisoned Call annotation SHADOWS Channel 6 (emit_c.rs:37276-37294), which would derive the CORRECT NovaOpt_nova_u8 from the correctly-coerced payload annotation — the buffer is internally inconsistent (parent Option[int], child u8). Claim (2) is partially weakened but not fully refutable: find_variant_v2 sorts candidates DESC by SchemaSource (DeclaredFromPrelude=2 > DeclaredFromUser=1 > HardcodedBaseline=0; sum_schema_registry.rs:79-90, 193-199), and Option IS registered DeclaredFromPrelude (registry.rs:639-649, since prelude declares Option methods), so for "Some" a user sum can never win — the cited shorter-name tie-break only applies within one source level; that half of claim 2 is refuted. However the module-level `fn Some` gap in the checker guard is real (the guard at 6756 checks only the local value scope; infer_method_call_channel_type bails for non-Member callees at 11248-11249, so the arm fires and stamps Option[payload] on a call to a user fn Some), masked in infer_expr_c_type only when Channel 1 (resolved_callees→fn_ret_by_span) covers the callee. Since claim (1) — the primary basis for SUSPECT — survives fully and is even worse than described, the audit's verdict stands.

### types/mod.rs:6875 [POISON]
WHAT: Binary-выражения: comparison/logical → Bool; арифметика при двух numeric-операндах → promote_arith_rt; при non-numeric операндах → ТИП ЛЕВОГО ОПЕРАНДА (Some(l)) как тип результата.
REASON: Ядовита non-numeric ветка (6850-6864, записывается на 6875): для operator-overload операндов (`a @plus b`, str, generic T) в канал пишется тип ЛЕВОГО операнда как тип результата Binary. Но возврат @-оператора в Nova НЕ ограничен типом левого операнда: spec даёт прецеденты `fn char @minus(other char) -> int` (open-questions.md:7001) и `@compare(o) -> int` (syntax.md:312) — user-overload `fn Point @minus(other Point) -> f64` штампует Point при фактическом f64. Комментарий сайта сам признаёт, что резолв возврата перекладывается на codegen («codegen resolves the overload return»), т.е. записываемое значение ЗАВЕДОМО не результат-тип для overload'ов — а Channel 2 (emit_c.rs:37136-37178) потребляет его авторитетно БЕЗ Binary-guard'а. Это точный аналог удалённого Member-int-fallback (commit fd3207d6): «byte-identical legacy» ложь в канале, требующая парного недоверия потребителя. Второй вектор: в отличие от соседних сайтов НЕТ gs-гейта — голый Named{T} левого generic-операнда уходит в канал с расчётом на consumer-side current_type_subst (тот самый «gap #2 subst-timing divergence», от которого сайты 6745/6786 явно гейтятся, mod.rs:6735-6740). Численная (promote_arith_rt — общее §0-правило) и Bool-части чисты; caveat: is_num включает char, так что `c1 - c2` промоутится в char — если char@minus→int (spec-рассматриваемая сигнатура) станет реальностью, ветка тоже начнёт врать. D263-fix (6867-6873) убрал int-fallback для НЕразрезолвленных операндов, но left-operand-guess для разрезолвленных non-numeric оставлен.
EVIDENCE: mod.rs:6850-6864 «numeric.or_else(|| match (self.infer_expr_type(left…), self.infer_expr_type(right…)) { (Some(l_tr), Some(r_tr)) => { … if is_num(&l) && is_num(&r) { Some(promote_arith_rt(&l, &r)) } else { Some(l) } } … })» + 6874-6876 «if let Some(rt) = res_rt { self.resolved_types_buf.borrow_mut().insert(e.id, rt); }»; признание в комментарии 6809-6811 «Non-numeric operands (operator-overload @plus, str, generic T) keep the prior left-operand annotation (codegen resolves the overload return / mono-substitutes T via current_type_subst)»; spec-прецедент свободного возврата: spec/open-questions.ru.md:7001 «fn char @minus(other char) -> int =>»; потребитель без guard'а: emit_c.rs:37136-37139 «Channel 2: resolved_types → resolved_type_to_c … return ir_c» (для Binary исключений нет).
VERIFY: NOT refuted — the POISON verdict stands, and verification found an even stronger spec precedent than the audit cited. (1) Wrongness is spec-sanctioned: D46 (spec/decisions/03-syntax.md:2577-2590) declares the return type of @plus/@minus/@times/@div/@rem/bitwise «свободный» (unconstrained), and gives a NORMATIVE accepted example at 03-syntax.md:2607: `fn Vector @times(other Vector) -> f64` (dot product). For `v1 * v2` the non-numeric branch (types/mod.rs:6850-6864, written at 6875) stamps Named{Vector} into resolved_types_buf while the true result is f64. This is stronger than the audit's citations (@compare->int is actually irrelevant — Lt/Gt/Eq go to the Bool arm at 6815-6818; char@minus->int is only an open question). (2) No checker gate: the Binary validation arm (types/mod.rs:12907-12992) checks only D214 ptr-arith ban, D248 ordered-operand, D405 mixed-width — nothing constrains overload return type, and D46 explicitly refuses such a constraint. (3) No consumer guard: Channel 2 (emit_c.rs:37137-37178) consumes the annotation authoritatively; its only distrust carve-outs are SelfAccess/Ident; resolved_type_to_c(Named{Vector}) returns Ok, so the Ok-filter is not a gate. (4) gs-gate absence confirmed: unlike the adjacent Try/Bang arm (mod.rs:6906-6912, gated by typeref_mentions_any(gs)), the Binary Some(l) write is ungated for generic operands. One nuance the audit understated (severity, not refutation): the Some(l) guess is byte-identical to legacy — infer_expr_c_type_legacy's Binary arm (emit_c.rs:37503-37558) also returns lt for non-numeric operands, its comment (37516-37520) hard-coding the same false 'result is the managed type itself' assumption — so unlike the removed Member-int fallback (which diverged from legacy), this poison reproduces legacy's wrong answer with no behavior regression today. But per conventions §1 the authoritative channel still stores a known-possibly-wrong type requiring paired consumer distrust, which is exactly the audited anti-pattern; the refutation bar (show the written type CANNOT be wrong) is not met.

### types/mod.rs:6897 [POISON]
WHAT: Весь Unary-expr (e.id): Neg/Not (`-x`, `!x`), Deref (`*p`), AddrOf/RawAddrOf (`&x`). Тип берётся из infer_expr_type(e) (op-диспетчеризованный Unary-arm на строке 10521), а при его промахе — из channel-аннотации ОПЕРАНДА через resolved_to_typeref (or_else на 6887-6894).
REASON: Два дефекта. (1) Fallback op-АГНОСТИЧЕН: копирует тип операнда на результат унарной операции. Для Neg/Not это тождество (верно), но для AddrOf/RawAddrOf теряется Pointer-обёртка, для Deref она не снимается. Reachable-путь: `&b.v` где b: GMBox[int] (generic-инстанцированный ресивер) — infer_expr_type Member-arm требует generics.is_empty() на obj_tr (строка 10545) → None → primary infer_expr_type(e)=None; но f3_check_member (8882-8905, subst_receiver_generics) уже аннотировал операнд b.v = Scalar{int} → fallback пишет `&b.v : Scalar{int}` вместо TypedPtr(int). Канал утверждает «адрес = nova_int» как факт — та же категория, что удалённый Member-int-fallback (fd3207d6). Deref-вектор частично прикрыт СЛУЧАЙНО: resolved_to_typeref возвращает None для Ptr/TypedPtr (10352). (2) Отсутствует gs-gate: соседние арки As/Is (6784), Try/Bang (6908), Coalesce (6923) гейтят `!typeref_mentions_any(&tr, gs)` («a `T` inside a generic body stays on legacy»), Unary-arm пишет безусловно → `-x`/`!x`/`*p` при x:T / p:*T в generic-body материализует Named{"T"} в канал.
EVIDENCE: let tr_opt = self.infer_expr_type(e, scope).or_else(|| { if operand.id.is_set() { self.resolved_types_buf.borrow().get(&operand.id).and_then(|rt| Self::resolved_to_typeref(rt, e.span)) } else { None } }); if let Some(tr) = tr_opt { let rt = ResolvedType::from_type_ref(&tr); self.resolved_types_buf.borrow_mut().insert(e.id, rt); } — fallback не различает op; ср. Unary-arm infer_expr_type: `UnOp::AddrOf | UnOp::RawAddrOf => Some(TypeRef::Pointer(Box::new(op_tr), expr.span))` (10533-10535) — правильный тип обязан быть Pointer-обёрнут, fallback обёртку не добавляет; gs-gate `!typeref_mentions_any(&tr, gs)` из соседних арок здесь отсутствует.
VERIFY: Опровергнуть вердикт POISON не удалось. Проверил код сам; ключевые факты подтверждаются построчно.

Дефект (1) — op-агностичный fallback (types/mod.rs:6879-6899) — ПОДТВЕРЖДЁН, reachable-цепочка сходится полностью:
- Паттерн арма `ExprKind::Unary { operand, .. }` ДЕМОНСТРАТИВНО отбрасывает `op` — fallback на 6887-6894 копирует тип операнда на результат для ЛЮБОГО унарного оператора, включая AddrOf/RawAddrOf.
- Цепочка `&b.v` (b: GMBox[int]): (a) f1_expr Member-arm (6930-6934) вызывает f3_check_member, который на 8882-8905 субститутит receiver-generics (subst_receiver_generics) и при primitive_gate пишет operand-аннотацию b.v = Scalar{int}; комментарий 8878-8879 сам называет ровно этот сценарий («a generic field `v T` on a now-typed receiver `b: GMBox[int]` materializes `b.v: int`»). (b) infer_expr_type(e) → Unary-arm 10521: op_tr = infer_expr_type(Member)? — Member-arm 10542-10560 требует obj_tr Named с generics.is_empty() (10545); GMBox[int] несёт generics=[int] → None → `?` → primary=None. (c) or_else: resolved_types_buf[operand.id]=Scalar{int}; resolved_to_typeref возвращает None только для Unit/Readonly/Any/Ptr/TypedPtr/Func (10351-10352) — Scalar проходит → в канал пишется `&b.v : Scalar{int}` вместо указателя. Канал лжёт объективно (тип pointer-значного выражения = int).
- Гейта у потребителя нет: Channel 2 (emit_c.rs:37136-37179) для Unary-expr без каких-либо guard'ов возвращает resolved_type_to_c(Scalar{int}) = "nova_int". `&x` — БЕЗОПАСНАЯ операция вне unsafe (types/mod.rs:24724-24725, «&x is safe — no E_UNSAFE_REQUIRED»), синтаксических ограничений операнда до Ident не нашёл — Member-операнд легален. Это та же категория, что удалённый Member-int-fallback (fd3207d6), только хуже: там int-fallback хотя бы совпадал по категории (скаляр), здесь скаляр утверждается для адреса.
- Deref-вектор действительно прикрыт СЛУЧАЙНО (честная Ptr/TypedPtr-аннотация операнда → resolved_to_typeref=None на 10352), что аудитор корректно оговорил.

Дефект (2) — отсутствие gs-gate — фактически ВЕРЕН (соседние арки As/Is 6784, Try/Bang 6908, Coalesce 6923 гейтят typeref_mentions_any, Unary-arm пишет безусловно), но его тяжесть слабее, чем подан: Binary-arm (6809-6811) НАМЕРЕННО пишет generic `T` в канал с in-code утверждением «codegen mono-substitutes `T` via current_type_subst», и resolved_named_to_c (emit_c.rs:2129-2135) действительно субститутит Named{T} через type_subst_overrides/current_type_subst при потреблении внутри mono-эмиссии. То есть Named{"T"} в канале — не безусловная ложь, а установленный в кодовой базе паттерн; риск сводится к §0.95 subst-timing (потребление вне активного subst → Nova_T* стаб без guard'а в Channel 2), который я не могу ни подтвердить конкретным reachable-путём, ни исключить. Частичное смягчение (2) вердикт не спасает.

Итог: дефект (1) один достаточен для POISON — op-агностичный fallback пишет заведомо неверный (не-pointer) тип для AddrOf-выражений по достижимому пути без единого гейта на пути записи или чтения. refuted=false.

### types/mod.rs:6910 [SUSPECT]
WHAT: Try/Bang-expr (`x?` / `x!!`, e.id): распакованный payload-тип через infer_expr_type Try/Bang-arm (10401-10413) — Result[T,E]→T, Option[T]→T по name-match path.last(); gs-gated (typeref_mentions_any).
REASON: Sound-ветка (name-matched unwrap generics[0]) верна и gs-гейтится. Но miss-ветка infer_expr_type Try-arm возвращает inner_tr БЕЗ распаковки — сайт тогда пишет тип КОНТЕЙНЕРА как тип уже-распакованного значения. Срабатывает когда контейнер скрыт: (а) alias `type MyRes = Result[int,E]` (TypeDeclKind::Alias существует, ast/mod.rs:1060; Try-arm alias НЕ разворачивает — сравнивает только path.last() с литеральными именами "Result"/"Option") → `r?` аннотируется Named{MyRes} вместо int; (б) Named{Option/Result} с пустыми generics → пропуск контейнера как есть. gs-gate эти случаи не ловит (generic-параметры не упомянуты). Плюс name-keyed match по path.last() — тип, ЛОКАЛЬНО названный Result с иным порядком параметров, распакуется в generics[0] неверно. Не POISON: primary-механизм фактовый, а живучесть miss-ветки зависит от того, принимает ли try-валидатор alias-of-Result (валидатор consume-scope на 4731-4743 использует тот же name-keyed паттерн, т.е. alias, вероятно, отвергается раньше) — но ветка «верни как есть» = признанная в комментарии догадка, записываемая как факт.
EVIDENCE: infer_expr_type 10404-10412: `if (base == Some("Result") || base == Some("Option")) && !generics.is_empty() { return Some(generics[0].clone()); } // Unknown container (user type, generic T) → return inner type unchanged. Some(inner_tr)` — на промахе имя-теста тип try-выражения = НЕраспакованный тип операнда; сайт 6906-6912 гейтит только `!typeref_mentions_any(&tr, gs)`.
VERIFY: Refutation attempt failed on every axis. (1) The miss-branch exists exactly as claimed: infer_expr_type Try/Bang arm (types/mod.rs:10401-10413) name-matches path.last() against literal "Result"/"Option" with non-empty generics and on miss returns Some(inner_tr) — the container TypeRef unchanged — which the site at 6906-6911 then records as the type of the already-unwrapped Try/Bang expr. (2) No alias expansion protects the path: scope holds declared TypeRefs verbatim (scope.insert(p.ty.clone()) at 6085/6263) and ResolvedType::from_type_ref (mod.rs:131) is a static fn with no self.types access — Named{MyRes} for `type MyRes = Result[int,E]` survives to the channel; the gs-gate (typeref_mentions_any) passes since no generic params are mentioned. Alias transparency exists only in resolved_cat_of_depth (11674), which is not on this path. (3) The audit's own hedge — 'try-validator probably rejects alias-of-Result earlier' — is not even true: exhaustive search found NO type-based validation of `?`/`!!` inner types in the checker at all; the only diagnostics are D158 (defer-body Fail-in-signature, 21704-21721), constraining the enclosing fn, and infer_consume_init_typeref (4731-4753) is a D196 consume-slot helper producing no rejection. So the alias case reaches codegen without any error. (4) The lie is consumed authoritatively with real harm: Channel 2 (emit_c.rs:37136-37179) returns resolved_type_to_c(rt) for Try/Bang exprs with no exemption; resolved_named_to_c lowers the alias via self.type_aliases (emit_c.rs:2237) to the container C type (Ok, not an inert Err), and lowers empty-generics Result/Option to erased container types (2169/2191). Meanwhile the actual Try emit (21867-21971) types the scrutinee by the INNER expr's alias-expanded C type (var_types) → is_result_like → emits the unwrapped payload `(tmp->payload.Ok._0)`; so the channel-annotated type (container pointer) diverges from the emitted value's type (unwrapped T) for e.g. `ro v = r?`. The sound branch and the Fail-effect main case (inner already T, returned unchanged = correct) work as the audit acknowledged, but the container-hidden cases write a factually wrong type into the authoritative channel with no gate or invariant preventing it. SUSPECT verdict stands.

### types/mod.rs:6925 [SUSPECT]
WHAT: Coalesce-expr (`a ?? b`, e.id): тип = распакованный inner из `a` (Option[T]/Result[T,E] → generics[0]) через infer_expr_type Coalesce-arm (10420-10432); намеренно НЕ из `b` (литеральный fallback `?? 0` не должен диктовать тип); gs-gated.
REASON: Та же структура, что Try/Bang: sound-ветка (name-matched unwrap `a`) верна, направление выбора источника (a, не b) обосновано комментарием (`v.first() ?? 0` на Vec[uint] обязан быть uint, не int). Но miss-ветка `Some(a_tr)` пропускает НАРУЖНЫЙ тип `a` (контейнер) как тип результата коалесинга: alias-of-Option/Result (alias не разворачивается, сравнение только path.last()=="Option"/"Result") или bare-Named без generics → `a ?? b` аннотируется типом контейнера, тогда как значение — payload T. gs-gate ловит только упоминания generic-параметров. Вывод небезупречен: догадка miss-ветки записывается как факт, но её достижимость на валидной программе ограничена (`??` на не-Option, вероятно, отвергается чекером раньше; остаётся alias-канал).
EVIDENCE: infer_expr_type 10422-10431: `if (base == Some("Option") || base == Some("Result")) && !generics.is_empty() { return Some(generics[0].clone()); } // Unknown: return the outer type of `a` unchanged. Some(a_tr)` — сайт 6921-6927 пишет этот результат в канал с единственным гейтом `!typeref_mentions_any(&tr, gs)`.
VERIFY: Could not refute; the SUSPECT verdict stands and is in fact slightly understated. Verified chain: (1) No semantic gate exists for Coalesce in the checker — every ExprKind::Coalesce arm in types/mod.rs (4198, 4546, 5756, 12995, 14497, 15544) is a plain walker; `??` on a non-Option LHS or an alias-typed LHS is never rejected, so the miss-branch is reachable on checker-accepted programs. (2) Alias channel is concrete: Nova has `type Name alias T` (TypeDeclKind::Alias, ast/mod.rs:1060, D52); the checker stores the declared TypeRef UNEXPANDED in scope (types/mod.rs:6262-6266), so infer_expr_type(a) for `x: Maybe` (alias of Option[int]) returns Named{Maybe}; the Coalesce arm (10420-10432) compares only path.last()=="Option"/"Result" → miss → returns the outer container type; ResolvedType::from_type_ref (types/mod.rs:131) is context-free and cannot expand aliases; the gs-gate (typeref_mentions_any) only checks generic params → annotation written. (3) The consumer is genuinely misled: dispatcher Channel 2 (emit_c.rs:37136) reads resolved_types authoritatively for any expr kind and resolved_named_to_c expands the alias via type_aliases (emit_c.rs:2237, populated at 10562-10566; Option arm 2148 → NovaOpt_nova_int), so the coalesce expr's C type becomes the CONTAINER NovaOpt_nova_int; but the Coalesce emit (emit_c.rs:22219-22231) dispatches on the alias-expanded LHS C-type string, recognizes NovaOpt_, and emits the unwrapped PAYLOAD value (tmp.value : r, nova_int). Container-typed annotation for a payload-typed value = wrong type in the channel, reaching consumers (e.g. `ro y = x ?? 0` binds y as NovaOpt_nova_int for a nova_int value). (4) Legacy (emit_c.rs:40669-40687) handled this case correctly (strips NovaOpt_ from the lowered, alias-transparent C type), so the annotation is a regression relative to legacy for the alias channel — same class as the removed f1 Member int-fallback precedent. One correction to the audit's hedge: `??` on non-Option is NOT rejected by the checker at all, so even the plain miss (a-type ≠ b-type, emit else-branch yields rhs value) can diverge; reachability is broader than the audit assumed, not narrower.

### types/mod.rs:7018 [SUSPECT]
WHAT: If/else-ВЫРАЖЕНИЕ (e.id): общий примитивный тип обеих веток через infer_if_common_primitive (8015-8042) — обе ветки обязаны быть блоками БЕЗ stmts с РАВНЫМИ non-unit примитивными trailing-типами; else-if chain и отсутствие else → None → legacy.
REASON: Гейты сильные и вывод почти безупречен: равенство типов веток, non-Unit, primitive_gate; Never НЕ проходит is_primitive_lowerable (нет в списке 346-351 → false) → диверджящие/never-пары уходят в legacy; блоки со stmts отвергнуты (trailing не может ссылаться на внутренние let). Единственная небезупречность — КОНТЕКСТНО-ЗАВИСИМЫЙ ЛИТЕРАЛЬНЫЙ ДЕФОЛТ: branch_rt берёт infer_expr_type(trailing), а для голого IntLit/FloatLit это безусловный дефолт (`ExprKind::IntLit(_) => Some(prim_ref("int"))`, 10590) — `ro x: u8 = if c { 1 } else { 2 }` аннотирует if-expr как Scalar{int}, хотя в u8-контексте литералы нарроуятся. Сам файл признаёт это §5-проблемой и гейтит В ДРУГОМ МЕСТЕ — Binary-arm отказывается выводить из литералов («bare literals default to int/f64/char which may mismatch the declared context, §5», 10576-10583) — здесь такого literal-guard нет. Смягчение (не POISON): byte-identical legacy (legacy If-arm выводил тот же then_ty тем же контекстно-слепым способом, комментарий 8013-8014), а в C-lowering расхождение даёт value-preserving narrowing при присваивании, не категориальную ложь.
EVIDENCE: let branch_rt = |blk| { if !blk.stmts.is_empty() { return None; } let tr = self.infer_expr_type(blk.trailing.as_deref()?, scope)?; ... }; if then_rt == else_rt && then_rt != ResolvedType::Unit && Self::primitive_gate(&then_rt) { Some(then_rt) } (8022-8041) — литерального гейта нет, тогда как Binary-arm: `let left_is_lit = matches!(&left.kind, ExprKind::IntLit(_) | ...); if left_is_lit { None }` (10577-10583).
VERIFY: НЕ ОПРОВЕРГНУТО. Прочитал сайт (types/mod.rs:7004-7021), хелпер infer_if_common_primitive (8015-8042), is_primitive_lowerable (344-354), infer_expr_type литеральные arms (10562-10596) и — ключевое — literal-coercion channel D55 (materialize_literal_coercion 9961-10054, call-site f1_check_assign_let:7356).

Гейты аудитора подтверждены как сильные: else обязателен (8021), else-if chain → None (8032), блоки со stmts → None (8023-8025), равенство веток + non-Unit + primitive_gate (8034-8037); never не проходит is_primitive_lowerable (не Scalar/Float/Bool/Str/Unit, Named только "char") → диверджящие пары уходят в legacy.

Единственная слабость аудитора ПОДТВЕРЖДАЕТСЯ, опровергнуть не удалось — литерального гейта на trailing действительно НЕТ: branch_rt (8022-8028) зовёт infer_expr_type(trailing), а для голого IntLit это безусловный дефолт `Some(prim_ref("int"))` (10590; FloatLit→f64 10591), тогда как Binary-arm того же файла ЯВНО отказывается выводить из bare-литералов с формулировкой «may mismatch the declared context, §5» (10576-10583).

Более того, нашёл ДОПОЛНИТЕЛЬНОЕ подтверждение, а не опровержение: у чекера ЕСТЬ механизм контекстной коэрции литералов (D55, materialize_literal_coercion, вызывается в f1_check_assign_let:7356 для аннотированного let), и его If-arm (10025-10033) рекурсирует в trailing ОБЕИХ веток и аннотирует ЛИСТЬЯ-литералы коэрцированным sized-типом (Scalar{u8}), но НИКОГДА не пишет id самого If-выражения. infer_expr_type(IntLit) канал не читает (10590 — прямой дефолт), порядок не спасает. Итог для `ro x: u8 = if c { 1 } else { 2 }`: канал одновременно содержит resolved_types[if_expr]=Scalar{int} и resolved_types[literal-листьев]=Scalar{u8} — внутренне противоречивая аннотация, т.е. канал по собственной модели чекера (D55) помечает if-expr контекстно-неверным типом. Это ровно §1-паттерн «авто-выводимый неверный тип», от которого Binary-arm защищён guard'ом, а If-arm — нет.

Смягчение аудитора тоже подтверждаю: consumer Channel 2 (emit_c.rs:37136-37179) вернёт nova_int — то же, что выводил legacy If-arm (комментарий 8002-8003, 8013-8014: legacy re-derive давал тот же then_ty), литералы при этом эмитятся уже коэрцированными ((uint8_t)N) через channel_int_c_type, и temp nova_int → uint8_t store value-preserving. Т.е. byte-identical, не POISON — но вердикт SUSPECT (канал может врать о контекстном типе if-выражения при голых литеральных trailing) устоял.

### types/mod.rs:7061 [SUSPECT]
WHAT: ExprKind::Match expr id (e.id) — the match EXPRESSION's result type, computed by infer_match_common_primitive as the common non-unit primitive type of all non-diverging arm bodies (U.4.4 Match-arm half).
REASON: Gates are strong (every non-Never arm must resolve to the SAME non-unit primitive; blocks with statements bail; disagreeing arms bail), but arm bodies are inferred in the OUTER scope. The helper's soundness assumption 'pattern-binding reference — bindings are absent from the outer scope → bail' fails under SHADOWING: an arm `Some(x) => x` whose pattern binding shadows an outer `x` of a different primitive type resolves to the OUTER type. Example: outer `x: int`, payload `i64`, arms `Some(x) => x, None => 0` → common = Scalar{wide_default:true} (nova_int) written as fact while the true type is i64 (int64_t) — different C types (int=intptr_t vs i64=int64_t). Narrow window (requires name shadowing + resulting arm agreement) but the channel can carry a wrong scalar as truth.
EVIDENCE: mod.rs:7969 `MatchArmBody::Expr(e) => self.infer_expr_type(e, scope)?` (outer scope, no arm-local bindings) and doc lines 7954-7956: 'ANY arm whose body type is unresolvable here (a pattern-binding reference — bindings are absent from the outer scope ...) → None → the whole match bails' — the absence assumption is defeated by shadowing; the gate at 7992 `if rt != ResolvedType::Unit && Self::primitive_gate(&rt)` then admits the wrong scalar.
VERIFY: NOT refuted — the audit's shadowing mechanism is real and I could not find any gate that blocks it. Verified chain, all by direct code reading:

1. PRODUCER SCOPE CONTAINS OUTER LOCALS: f1_stmt's Stmt::Let arm (types/mod.rs:6262-6269) inserts every let-binding's type into the mutable `scope` map, so at the ExprKind::Match arm (types/mod.rs:7032-7063) `scope` holds the enclosing function's locals. The Match arm walks arm bodies with this OUTER scope and never extends it with pattern bindings (lines 7036-7048 — only check_priv_pattern_recursive on patterns, no scope insertion).

2. HELPER RESOLVES SHADOWED NAMES TO THE OUTER TYPE: infer_match_common_primitive (types/mod.rs:7961-7997) calls infer_expr_type(arm_body, scope) with that same outer scope. infer_expr_type's Ident arm (types/mod.rs:10363-10366) is a plain `scope.get(name)` lookup. So for `Some(x) => x` where the pattern binds `x` (payload i64) shadowing an outer `let x: int`, the helper resolves the arm body to the OUTER int — the doc-comment's soundness claim "pattern-binding reference — bindings are absent from the outer scope → unresolvable → bail" (lines 7954-7956) is false precisely under shadowing: the name IS present, with the WRONG type. The bail gates (non-empty-stmt blocks return None at 7973-7975; disagreeing arms return None at 7986; Never arms skipped at 7980) do not fire: a bare Ident body passes, and arms can agree on the wrong type (e.g. `Some(x) => x, None => throw ...` — Never contributes no constraint, common = outer int). ResolvedType for int (nova_int/intptr_t) vs i64 (int64_t) are distinct C types/mangles.

3. NO LANGUAGE-LEVEL SHADOWING BAN: grep over compiler src and spec/decisions found only prelude/import shadowing diagnostics (W_PRELUDE_SHADOW, E_PREFIX_SHADOWS_NAMED_TYPE); local/pattern shadowing is legal — escape_analyze.rs:190 explicitly says "shadowing rare in Nova bodies" (i.e. permitted), and f1_block itself implements let-shadow snapshot/restore (types/mod.rs:6180-6211). No checker error would reject the shadowing program before the fact is written.

4. CONSUMER TRUSTS IT: emit_c.rs infer_expr_c_type Channel 2 (emit_c.rs:37136-37179) consumes resolved_types authoritatively for the Match expr id — the only distrust guards are for SelfAccess/Ident (var_types / pattern_binding_overrides preference, 37142-37163); a Match expression has no guard and returns the channeled C type directly at 37177. Notably codegen DOES track pattern-binding types (pattern_binding_overrides, 37158) — legacy would derive the true i64 — so the channel and legacy genuinely diverge here.

Mitigating notes (do not amount to refutation): the window is narrow (requires an arm body referencing a pattern binding that shadows an outer local of a DIFFERENT primitive type, plus all other arms agreeing with the outer type); and for the int-vs-i64 pair both are 64-bit on the target so a plain C assignment stays value-correct — but the wrong C type feeds mangle-sensitive downstream decisions (method dispatch keys, mono instantiation), and per compiler-conventions §1 the channel must never carry an auto-derived wrong type. The same hole exists symmetrically in infer_if_common_primitive only via its trailing-expr path when the branch block has zero stmts (pattern bindings can't occur there, but IfLet is not annotated, so If is less exposed). Verdict SUSPECT stands for types/mod.rs:7061.

### types/mod.rs:7107 [SUSPECT]
WHAT: ExprKind::ArrayLit expr id (e.id) — the array's own type, written as ResolvedType::Named{name:"Vec", args:[elem]} where elem is inferred from the FIRST ArrayElem::Item only, gated by primitive_gate (U.4.4 composite Array).
REASON: Two imperfections. (1) Context-dependent default: for a sized-annotated array `[]u8 x = [1,2,3]` the entry is the wide-default collapse Vec[int] — contextually wrong — and the SAME buffer then gets the element IntLit ids re-annotated to u8 by materialize_literal_coercion's ArrayLit arm (mod.rs:10012-10019), leaving the channel SELF-INCONSISTENT (array id says Vec[int], its elements say u8). Correctness rests on a consumer-side routing invariant ('an ANNOTATED array takes its C-type from the ANNOTATION ... routing AROUND this infer_expr_c_type consumer entirely') rather than a producer-side gate — the weaker form of the removed Member-arm precedent (channel entry that can be wrong, kept safe only by paired consumer behavior). (2) First-item heuristic: elements after the first are never unified against the recorded elem type (`elems.iter().find_map(... ArrayElem::Item ...)` takes the first), so `[1, u8_var]`-style mixes record the first element's type as fact. Mirrors legacy byte-for-byte, but 'byte-identical to legacy' is not 'true type'.
EVIDENCE: mod.rs:7094-7097 `let first_item = elems.iter().find_map(|el| match el { ArrayElem::Item(x) => Some(x), _ => None });` — first Item only; comment 7083-7088 admits the annotated case is handled by 'routing AROUND this infer_expr_c_type consumer entirely. Proven empirically (§7.2 detect-mode ... 0 DIFF)' — an empirical consumer-routing guarantee, not a type-truth gate; materialize_literal_coercion ArrayLit arm (10012-10019) rewrites only element ids, never the array id.
VERIFY: Could not refute; both audit claims verified against code. (1) Producer mod.rs:7093-7118 writes resolved_types_buf[array.id]=Named{Vec,[elem]} where elem for an IntLit first item is the wide-default int (infer_expr_type IntLit arm mod.rs:10590), and primitive_gate=is_primitive_lowerable (mod.rs:344-353) admits ANY Scalar including wide_default:true — no context gate. For `[]u8 x = [1,2,3]`, f1_check_assign_let (mod.rs:7356) then runs materialize_literal_coercion whose ArrayLit arm (mod.rs:10012-10019) re-annotates only the ELEMENT ids to u8 and never rewrites the array's own id — the buffer is provably self-inconsistent (array.id=Vec[int], elements=u8). The consumer Channel 2 (emit_c.rs:37137-37179) consumes resolved_types for any expr id with no ArrayLit guard; the annotated case is safe only via the consumer-side routing invariant the producer comment itself justifies merely empirically (0 DIFF detect-mode), i.e. the weaker form of the removed Member-arm precedent. Aggravator: infer_expr_type's ArrayLit arm (mod.rs:10862-10866) reads the buffered entry back into checker inference. (2) First-item heuristic confirmed (find_map over ArrayElem::Item, mod.rs:7094-7097); exhaustive search found NO element-homogeneity/unification check anywhere (all other ArrayLit arms are plain walkers at mod.rs:5792/13037/14539 or the same first-element heuristic at mod.rs:7902 and 13935-13941), and no spec rule makes first-element typing authoritative — mixes like [u8_var, int_var] record the first element's type as fact unchecked. Refutation attempts that failed: gate excludes wide-default int (false — any Scalar passes); materialize rewrites array id (false — items only); language rule legitimizes first-element typing (no enforced rule found); consumer never consults channel for arrays (Channel 2 has no structural guard). SUSPECT verdict stands.

### types/mod.rs:8559 [SUSPECT]
WHAT: Call expr id (call_id) of the unambiguously-resolved callee at f1_check_call's common tail (free-fn single overload, Type.method single/unique-compat via Path, module-qualified fn via Member) — the callee's declared return type, with `-> Self` substituted to the receiver type (call-return type channel, mirrors §3a/U.4.3).
REASON: Generic erasure hazard on `-> Self` for a GENERIC receiver. The Self-substitution constructs TypeRef::Named{path:[recv.type_name], generics: Vec::new()} — the receiver's type parameters/args are DROPPED. Both typeref_mentions_any gates run BEFORE substitution on the raw `Self` TypeRef, and 'Self' is not a member of fn_generic_scope (which contains fd.generics + receiver.generics names like 'T'), so `-> Self` always passes the gate; there is NO post-substitution re-check. A static on a generic type (`fn Box[T].empty() -> Self`, called as `Box.empty()` / turbofish-peeled `Box[int].empty()` through the Path(len==2) arm, which only excludes PRIMITIVE receiver names) would write Named{Box, args:[]} into the channel as fact — a generic type with erased args. For non-generic receivers (the documented Mutex.new d172_lm fix) the substitution is exact, and returns mentioning a named generic (`-> T`, `-> Vec[T]`) ARE caught by the pre-subst gate — the hole is exactly the Self-of-generic-receiver combination.
EVIDENCE: mod.rs:8542-8550 `if let Some(recv) = &callee.receiver { ... m.insert("Self".to_string(), TypeRef::Named { path: vec![recv.type_name.clone()], generics: Vec::new(), span: callee.span });` — generics hardcoded empty; gates at 8531-8532 `!typeref_mentions_any(ret_ty, &callee_gs_inner) && !typeref_mentions_any(ret_ty, gs)` evaluate the PRE-substitution `Self` (mentions no generic name per typeref_mentions_any at 11982); then 8558-8559 `let rt = ResolvedType::from_type_ref(ret_ty); self.resolved_types_buf.borrow_mut().insert(call_id, rt);` with no post-subst gate.
VERIFY: NOT REFUTED — CONFIRMED, with one mechanism detail corrected and an end-to-end repro. (1) Gates verified in code: fn_generic_scope (types/mod.rs:11886-11901) contains only fd.generics + receiver generic names — never "Self"; typeref_mentions_any (11982-12003) on Named{path:["Self"]} returns false; caller gs doesn't contain "Self" either. So `-> Self` always passes both gates at 8531-8532. The substitution at 8542-8551 builds TypeRef::Named{path:[recv.type_name], generics: Vec::new()} — receiver type args DROPPED — with no post-substitution re-check; ResolvedType::from_type_ref (mod.rs:131-163) has no generic-template guard and yields R::Named{name, args:[]}, inserted at 8559 as fact. (2) No masking on the read side: fn_ret_by_span explicitly does NOT index receiver-generic callees (emit_c.rs:770-773 'generic / receiver-generic ... NOT indexed'), so Channel 1 misses and Channel 2 (emit_c.rs:37136-37179) consumes the erased annotation authoritatively; resolved_named_to_c's default arm (emit_c.rs:2288) lowers Named{Set,[]} to the bare erased stub `Nova_Set*`, whereas the true type Set[int] lowers to the mono `Nova_Set____nova_int*` (2246-2265). (3) Reachability proven empirically: bare `Set.from_iter(v)` (fn Set[T].from_iter(items []T) -> Self, std/collections/set.nv:78) parses as Path(["Set","from_iter"]) (parser/mod.rs:7904-7959) → passes the non-primitive Path(len==2) arm → common tail → write fires. Repro (scratchpad/bare_static.nv, built with the worktree debug nova.exe): compiles WITHOUT error, at runtime `assert failed: s.len() == 3` — generated C shows Nova_Set* s = Nova_Set_static_from_iter(v) where the stub returns NULL and len/contains are do-nothing stubs, while the adjacent turbofish call Set[int].from_iter(v) gets the correct mono Nova_Set____nova_int* and passes. The channel thus records Named{Set,[]} for an expression whose true type is Set[int] — a factually wrong type written as fact, the exact §1-forbidden pattern and the same shape as the removed fd3207d6 precedent (lie consistent with byte-identical legacy). CORRECTION to the audit's trigger claim: turbofish calls do NOT reach the site — `Set[int].from_iter(...)` parses as Member{obj: TurboFish{Ident}}, and the Member arm early-returns at mod.rs:8456 (obj not Ident); the TurboFish peel at 8306 only handles func-level turbofish (free fns). Only the BARE static-call form (Path) reaches the tail — but that form is legal, live, and demonstrably mis-typed, so the hole (Self-of-generic-receiver) is real. Non-generic receivers (Mutex.new) remain exact, and `-> T`/`-> Vec[T]` returns are caught by the pre-subst gate, as the audit stated. SUSPECT verdict stands; suggested fix shape: gate the Self-substitution on callee.receiver.generics.is_empty() (or substitute Self with the receiver's full generic TypeRef and re-run the mentions gate post-substitution).

### types/mod.rs:9054 [SUSPECT]
WHAT: ExprId of a Member expression `recv.field` where the receiver resolves to a NamedTuple type declaration (f3_check_member, TypeDeclKind::NamedTuple arm). Writes ResolvedType::from_type_ref(field_ty) where field_ty = subst_receiver_generics(&field.ty, &td.generics, recv_type_args) — the declared field type with the inferred receiver's type-args substituted in. Mirror of the Record arm at line 8905 (Plan 172.1 RANK 2 named-priority de-collapse).
REASON: Not a fallback — the type is derived from the found field declaration (`fields.iter().find(|f| f.name == name)`) plus receiver substitution, and double-gated (primitive_gate OR concrete_value_named, deterministic C-types only, member_id.is_set()). So no 'unresolved → int' poison. BUT the derivation is not airtight on two axes. (a) RESIDUAL-GENERIC name collision: subst_receiver_generics silently returns the RAW field type when `type_params.len() != type_args.len()` (mod.rs:11284-11286 `return field_ty.clone();`), and inside a generic type's own methods the substitution is identity (T→T). A residual param lowers to Named{name:"T", args:[]}; the `concrete_value_named` gate then does `self.types.get(name)` by SHORT NAME ONLY — if the program declares a concrete Record/Sum/Newtype/NamedTuple literally named like the generic param (nothing forbids this: no shadow diagnostic exists for generic-param-vs-type-name; only W_PRELUDE_SHADOW for prelude collisions), the gate passes and the channel receives the DECLARED type instead of the (unknown) type-arg — codegen consumes it authoritatively → wrong C type. primitive_gate has the same theoretical hole only for a param named `char` (is_primitive_lowerable line 351). (b) Last-wins: plain `insert(member_id, rt)` overwrites any earlier f1-preamble annotation for the same Member ExprId; today both derive from the same field decl so values should agree, but nothing enforces agreement. Narrow, collision-dependent edge → SUSPECT, not POISON.
EVIDENCE: 9038-9054: `let field_ty = self.subst_receiver_generics(&field.ty, &td.generics, recv_type_args); let rt = ResolvedType::from_type_ref(&field_ty); let concrete_value_named = matches!(&rt, ResolvedType::Named { name, args, .. } if args.is_empty() && self.types.get(name).map_or(false, |td| matches!(&td.kind, TypeDeclKind::Record(_) | TypeDeclKind::Sum(_) | TypeDeclKind::Newtype(_) | TypeDeclKind::NamedTuple(_)))); if Self::primitive_gate(&rt) || concrete_value_named { ... self.resolved_types_buf.borrow_mut().insert(member_id, rt); }` — and the substitution escape hatch at 11284: `if type_params.is_empty() || type_params.len() != type_args.len() { return field_ty.clone(); }` leaves the generic param name unsubstituted, after which `self.types.get(name)` matches any same-named concrete declared type.
VERIFY: Cannot refute. I found one genuine partial counter the auditor missed: codegen's resolved_named_to_c (emit_c.rs:2128-2136) checks type_subst_overrides/current_type_subst BY NAME BEFORE the type-table for empty-args Named — so in the identity-subst sub-case (inside a generic type's own mono-emitted methods) a residual param name like Named{"Elem"} lowers correctly via the mono subst even under a name collision (subst priority beats the declared type). But this does NOT close the arity-mismatch sub-path: walk_typeref (types/mod.rs:5493-5501) explicitly sanctions OMITTED type-args as a legal idiom (error only when args are given and wrong), so a legal binding `p Pair` (bare generic NamedTuple) yields recv Named{Pair, generics:[]} → subst_receiver_generics early-returns the RAW field type (11284-11286) → residual Named{param} hits the gates; in a non-generic consumer fn current_type_subst is empty, so the collision hole is live: concrete_value_named passes on self.types.get(short-name) if a declared type shares the param's name (no diagnostic forbids this — E_PREFIX_SHADOWS_NAMED_TYPE at 4905-4906 loops only fd.generics, and the parser routes receiver-carrier typevars into receiver.generics at parser/mod.rs:2955-2959; check_type_decl has no shadow check for td.generics; E_TYPE_NAME_TOO_SHORT bans only 1-char type names, params may be multi-char). Worse, the primitive_gate hole needs NO collision: a param literally named `char` (arity-0, actual==0 exempt → accepted) residual as Named{"char"} passes is_primitive_lowerable (line 351) and lowers to nova_char while the inferred mono arg may be str — wrong C type consumed authoritatively by Channel 2 (emit_c.rs:37136-37178, Member guard removed). Last-wins (b) confirmed as plain insert at 9054 with factual-but-unenforced agreement between the two producers. So the audit's SUSPECT verdict survives; the omitted-type-args idiom makes the arity-mismatch axis somewhat more reachable than the audit's 'collision-dependent' framing (char variant), though still requiring perverse naming.

