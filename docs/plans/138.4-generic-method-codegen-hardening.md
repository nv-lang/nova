<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 138.4 — Generic-method codegen hardening (unblock 4-marker cluster)

> **Создан:** 2026-06-10.  **Статус:** 📋 PLANNED.  **Эстимат:** ~2-4 dev-day (deep .rs codegen).
> **Model:** Opus + Thinking ON (самая глубокая codegen-работа сессии).
> **Триггер:** сессия 138.x вскрыла, что 4 заблокированных маркера имеют ОДИН корень —
> codegen-инфраструктура generic-методов (emit_c.rs mono/dispatch/mangling).

---

## Зачем

4 cleanup-маркера были эмпирически проверены и **REVERTED** прошлыми workflow,
потому что упёрлись в gaps codegen-инфраструктуры generic-методов — НЕ в .nv-код.
Починка инфраструктуры **один раз** разблокирует все 4 + готовит капстоун.

| Gap (codegen) | Блокирует cleanup | Anchor (из marker-анализа) |
|---|---|---|
| G-A: full-signature overload **mangling** для generic-type методов | `[M-138-mutindex-rename]` | mono/template имя голое → read `@index(i)->T` и write `mut @index(i,val)` → один C-символ (collision t2_vec_mut_index.c:559/561); emit_c.rs:2566-2607 (`register_vec_mono_method` + template-fwd-decl) |
| G-B: recv-mut overload **return-type tiebreak** в `infer_expr_c_type` | `[M-138-getmut-rename]` | ExprKind::Call multi-overload (emit_c.rs ~30248-30571) фильтрует по param-types only → return всегда первый-объявленный; mangle_fn 9723-9729 tiebreak'ает CALLER-mut → footgun |
| G-C: primitive-T generic `.clone()` **dispatch** | `[M-138.3-clone-bound-unsupported]` | `Vec[int].clone()` per-element `@data[i].clone()` мис-диспатчит для primitive T (нет user @clone) → crash; нужен identity-clone для примитивов |
| G-D: generic-**pointee** type preservation под right-binding (`*T`) | `[M-138.2-v2-propagation-impl-gap]` | `mut data *T` в generic-поле → pointee лоуэрится в `Nova_any*` (T теряется); V2 §V2.1 не реализован в codegen для generic pointee subst |

---

## Фазы (атомарные; каждая: fix codegen → re-attempt cleanup → verify-or-document)

### Ф.1 — G-C: primitive-T generic `.clone()` dispatch (~0.5-1d)

Самый ценный (user подтвердил deep-clone). Проблема: в deep `Vec[T].clone()` тело
`@data[i].clone()` — для primitive T (int/bool/f64/...) у элемента нет user-`@clone`,
mono мис-диспатчит → crash. Fix: `.clone()` на primitive T = **identity-copy** (built-in:
primitives и `value`-типы без user-clone → bitwise self). Generic `.clone()` call должен
резолвить: user-`@clone` если есть, иначе identity для primitive/POD.

Re-attempt: восстановить deep `Vec[T Clone] @clone()` (138.3 Ф.2 форма) + HashMap/Set deep
(138.3 Ф.3). Test plan138_3 t1 (Vec[Point] deep — мутация копии ≠ оригинал) + Vec[int] (identity).
**Commit:** `fix(plan138.4 Ф.1 G-C): primitive-T generic .clone() = identity; unblock deep collection clone`

### Ф.2 — G-A: full-signature overload mangling (generic-type methods) (~0.5-1d)

Проблема: mangling generic-type методов не дизамбигуирует по сигнатуре → read `@index(i)->T`
и write `mut @index(i,val)` коллизят в один C-символ. Fix: mangle generic-method C-name по
**полной сигнатуре** (arity + param-c-types + recv_mutable), на template-fwd-decl И
`register_vec_mono_method` (emit_c.rs:2566-2607) — синхронно с multi-overload mangler.

Re-attempt: `[M-138-mutindex-rename]` `@index_set` → `mut @index` (3-way overload: read scalar,
read range, write). Test plan138_1/plan138_2.
**Commit:** `fix(plan138.4 Ф.2 G-A): full-signature mangling for generic-method overloads; unblock mut @index`

### Ф.3 — G-B: recv-mut overload return-type tiebreak в infer (~0.5d)

Проблема: `infer_expr_c_type` для method-call (emit_c.rs ~30248-30571) фильтрует overload только
по param-types → return-тип всегда первого-объявленного. Fix: tiebreak return-overload по
receiver mutability (использовать `is_obj_mutable`, как mangle_fn 9723).
**Caveat (footgun):** даже с fixed infer, getmut-rename даёт mut-recv-dispatch footgun
(`mut v; v.get(1)` read → mut-overload). Поэтому Ф.3 = **починить инфраструктуру** (полезно
для differing-return-type overloads вообще); но getmut-rename САМ — **отдельное решение**:
рекомендация оставить `@get_mut` distinct (Rust precedent), т.к. footgun реален. Re-attempt
getmut ТОЛЬКО если footgun решаем (напр. mut-recv read дизамбигуируется явно).
**Commit:** `fix(plan138.4 Ф.3 G-B): infer return-type tiebreak by recv-mut for differing-return overloads`

### Ф.4 — G-D: generic-pointee type preservation под right-binding (`*T`) (~0.5-1d)

Проблема: `mut data *T` в generic-поле → pointee `Nova_any*` (T теряется при mono subst).
Fix: V2 §V2.1 right-binding в codegen — `Mut(Pointer(T))` сохраняет generic T в pointee при
мономорфизации (TypeRef::Mut transparent + pointer-pointee subst). Это часть
`[M-118.5-right-binding-migration]`.

Re-attempt: `[M-138.2-vec-type-priv]` part-b `*mut T` → `*T`. Test plan138_*.
**Commit:** `fix(plan138.4 Ф.4 G-D): preserve generic pointee type under right-binding; enable *T`

### Ф.5 — Docs + close

Markers DONE/BLOCKED + commits; spec D216/D230 amends если impl догнал prose; simplifications/
project-creation/discussion-log/memory; push.

---

## Risk register

| # | Риск | Sev | Mitigation |
|---|---|---|---|
| R1 | Эти gaps уже дефитили прошлые попытки — глубокая mono/codegen работа, может не поддаться | 🔴 HIGH | atomic per-gap; verify-or-document; частичный успех ОК (разблок даже 2/4 — выигрыш) |
| R2 | Mangling-изменение (Ф.2) — риск регрессий по ВСЕМ generic-методам (C-имена меняются) | 🔴 HIGH | broad targeted regression после Ф.2; mangling backward-compat где возможно |
| R3 | getmut footgun не решается даже с infra-fix → getmut-rename остаётся won't-fix | 🟡 MED | Ф.3 чинит инфру (ценно вообще); getmut-rename — отдельное design-решение |
| R4 | primitive identity-clone (Ф.1) — взаимодействие с consume/value-типами | 🟡 MED | identity только для POD/primitive без user-clone; value-records через memberwise |

---

## Acceptance criteria

- **H1** — primitive-T `.clone()` = identity; `Vec[int].clone()` работает; deep `Vec[Point]`/HashMap/Set clone landed (138.3 G2/G4 разблокированы).
- **H2** — generic-method overloads различимы по full signature; `mut @index` (3-way) компилируется (mutindex-rename landed).
- **H3** — infer return-tiebreak по recv-mut работает для differing-return overloads.
- **H4** — `mut data *T` (generic pointee) компилируется writable (type-priv `*T` landed).
- **H5** — 0 регрессий vs baseline (особенно после mangling-изменения Ф.2).
- _Частичный успех допустим: разблок ≥2/4 + документация остального._

---

## Связь
- Umbrella маркер `[M-codegen-generic-method-hardening]` (этот план).
- Разблокирует: 138.3 (clone-deep), 138-mutindex-rename, 138.2-vec-type-priv (`*T`); getmut — conditionally.
- Готовит капстоун (universal Vec) — generic-method codegen станет надёжнее.
