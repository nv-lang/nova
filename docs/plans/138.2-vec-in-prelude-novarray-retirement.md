<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 138.2 — Universal `Vec` + `str`-in-Nova + NovaArray retirement

> **Создан:** 2026-06-10.  **Статус:** 📋 PLANNED (capstone Ф.0+Ф.2-Ф.4 ещё впереди; Phase-A + design-cleanup batch N1-N4 приземлены).
> **Sequencing (решено 2026-06-10):** **139-first** — Ф.0 → Plan 139 Ф.0-Ф.2 → Ф.2-Ф.4.
> Ф.1 (string-слой) **subsumed by Plan 139** (str→value-record).
> **Закрывает:** `[M-138.1-vec-in-prelude]` — блокер Ф.5 из Plan 138.1.
> **Эстимат:** ~4-6 dev-day (multi-day, как и предсказано в 138.1 Ф.5 blocker-анализе).
> **Model:** Opus + Thinking ON (рискованный codegen + prelude integration).
> **Worktree/branch:** `nova-p138` / `plan-138.1` (или новый `plan-138.2` off `plan-138.1`).
> **Зависит от:** Plan 138.1 (partial-landing: флип работает где Vec присутствует + typed-storage gap закрыт).

---

## Зачем (recap блокера)

Plan 138.1 приземлился **частично**: `[]T`→`Vec[T]` флип в `type_ref_to_c`
**гейтится** через `generic_type_templates.contains_key("Vec")` —
[emit_c.rs:2055](../../compiler-codegen/src/codegen/emit_c.rs) +
[:4938](../../compiler-codegen/src/codegen/emit_c.rs). `Vec` **не в prelude**,
поэтому юниты, не упоминающие Vec, всё ещё лоуэрят `[]T` в `NovaArray`. Полное
удаление `NOVA_ARRAY_DECL/IMPL` (138.1 Ф.5) заблокировано пятью load-bearing
зависимостями:

1. **Не-универсальный флип** (gate выше) — primitive/Vec-free юниты → NovaArray.
2. **string-слой** `[]T`-producers (`external fn`, C-backed → NovaArray).
3. **parfor** — NovaArray usage.
4. **closure-array** `[]fn` → `NovaArray_void_p*` ([emit_c.rs:4918](../../compiler-codegen/src/codegen/emit_c.rs) explicit exclusion).
5. **Vec→NovaArray bulk-op bridge** — Vec-методы частично маршрутят через `nova_array_*`.

Цель 138.2: снять все пять, завершить retire NovaArray, закрыть D239/D144 **полностью**.

---

## Факты из разведки (verified anchors)

| Что | Где | Деталь |
|---|---|---|
| Gate флипа | emit_c.rs:2055, 4938 | `if self.generic_type_templates.contains_key("Vec")` |
| Closure-array exclusion | emit_c.rs:4918 | `[]fn(...)` → `NovaArray_void_p*` (намеренно) |
| vec_owned imports | std/collections/vec_owned.nv:31,40 | только `raw_mem.{RawMem}` + локальный `external fn panic` — без `#prelude` |
| Vec в prelude | std/prelude.nv | **отсутствует** (grep пуст) |
| Range precedent | std/prelude.nv:114 | `export import std.collections.range.{Range, RangeIter}` + range.nv `#prelude(core,runtime,errors)` (Plan 62.D bis-1 — вскрыл 4 latent codegen-бага) |
| str `[]T`-producers | std/runtime/string.nv | `@to_bytes→[]u8`(77), `@as_bytes→ro []u8`(80), `@to_chars→[]char`(94), `@split→ro []str`(97), `from_bytes_{lossy,unchecked,unchecked_steal}`(84-91) |
| str byte-примитивы | std/runtime/string.nv | `@byte_at(i)→u8`(25), `@len`(19), `@char_at(i)→Option[char]`(49), `@char_len`(22); str-slice `s[a..b]` (Plan 138 Ф.3) |

---

## Фазы

### Ф.0 — Универсальная доступность `Vec` (ядро `[M-138.1-vec-in-prelude]`) (~1-2d)

> **🔴 ИСХОД (2026-06-10): BLOCKED — попытка приземления, откат по HIGH-RISK протоколу.**
> Механика **доказана и работает** для узкого acceptance-критерия, но универсальный
> флип **архитектурно неотделим** от Ф.2-Ф.4 (NovaArray-API reconciliation): включение
> флипа в изоляции каскадит в **40+ регрессий** по корпусу. Дерево оставлено GREEN
> (F0 откатан, 0 tracked-file изменений). **C1 не достигнут на landed-ветке** (флип OFF).
>
> **Доказательство (флип OFF, текущее состояние ветки):** Vec-free юнит
> (`mut x []int = [1,2,3]`) компилируется в `NovaArray_nova_int*`
> (`Nova_Vec____nova_int` = 0 вхождений, `NovaArray` = 112). Это **ожидаемое**
> reverted-состояние.
>
> **Доказательство (флип ON, до отката — acceptance MET):** тот же Vec-free юнит
> лоуэрился в `Nova_Vec____nova_int*` (32 вхождения), residual NovaArray = 2
> (намеренный `Vec.from([]T)` literal-bridge param). 4 gate-сайта проходили
> универсально для prelude-юнитов и корректно degrade-или для `#no_prelude`.
>
> **Механизм (Option A — ЕДИНСТВЕННЫЙ жизнеспособный):**
> - `std/prelude.nv`: `export import std.collections.vec_owned.{Vec, VecIter}`
>   сразу ПОСЛЕ Range re-export (строка 114; порядок важен — vec_owned зависит от Range).
> - `std/collections/vec_owned.nv`: директива `#prelude(core, runtime, collections,
>   protocols)` перед `module` + явный `import std.collections.range.{Range}`
>   (Range живёт только в facade, не в prelude-submodule).
> - 4 gate-сайта `contains_key("Vec")` (emit_c.rs:2055/4938/26100/31637) **НЕ удалять** —
>   они проходят универсально для prelude-юнитов и graceful-degrade для `#no_prelude`.
> - Rust-rebuild НЕ нужен (std .nv читаются с диска в runtime, не include_str).
>
> **Option B (codegen-seed) — ОТКЛОНЁН после spike.** Seeding только template в
> `generic_type_templates` НЕ достаточно: 4 gate-сайта зависят от полного STRUCT
> DEFINITION + всех method-bodies (`generic_type_methods["Vec"]`), которые приходят
> только через import-merge pipeline. Seed только template → dangling
> `Nova_Vec____<T>` + undefined `Nova_Vec____<T>_method_push` → CC-FAIL. Spec-фрейминг
> «B легче» был основан на ложной предпосылке, что template один достаточен.
>
> **5 классов регрессий при флипе ON (требуют Ф.2-Ф.4, НЕ codegen-фикса в Ф.0):**
> 1. **insert/append API divergence** (plan90_1 20→10): []T extend-family (D141)
>    `dst.insert(i, src_ARRAY)` = bulk-insert, но Vec `insert(i, v T)` = single-elem.
>    Одно имя, несовместимая семантика → нужен либо bulk-overload на Vec, либо переписка тестов.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
> 2. ✅ **[]u8 literal int64-stride** (plan118_1 11→6, buffers) = `[M-138.2-vec-u8-literal-stride]`
>    — **PRE-FIXED Phase-A (commit 5c41f72a6e9)**: literal sized-int element coercion в обоих
>    путях (emit `try_emit_typed_vec_literal` ~26305 + infer `infer_expr_c_type::ArrayLit` ~31692).
> 3. ✅ **Vec-of-record write-index `_p` typedef bug** (plan138_1 10→9): `v[i]=Point{...}`
>    эмитил `(Nova_Point_p)(tmp)` — typedef не существует, должно быть `Nova_Point*`.
>    **PRE-FIXED Phase-A (commit c7a8f365aa3)**: write-path теперь использует registry-based
>    element-type recovery (как read-path), кастит в реальный `Nova_X*`.
> 4. **map_literals каскад** (28→9): map-литералы строят []T-backed bucket-arrays.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
> 5. **SHADOW collision** (plan123_3_1 E7310): user `type Vec { x, y }` (non-generic)
>    коллизит с prelude `Vec[T]`; prelude-методы выживают и arity-check'аются против
>    user-Vec. Расширить W_PRELUDE_SHADOW methods-skip на user-redeclared receiver type.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
>
> **🟢 Phase-A de-risk (2026-06-10):** классы #2 и #3 (узкие codegen-баги) **pre-fixed**
> отдельными атомарными коммитами на `plan-138.1` ДО флипа — сужает поверхность будущего
> атомарного capstone (Ф.0+Ф.2+Ф.3+Ф.4) до 3 классов: #1 (insert/append API), #4 (map_literals),
> #5 (SHADOW). Дополнительно Phase-A добавил fluent-chain `-> @` на Vec-мутаторы (commit d22c96f7423,
> `[M-138.2-vec-fluent-push]`). Два rename-followups ([M-138-getmut-rename], [M-138-mutindex-rename])
> исследованы и **BLOCKED** на codegen-уровне (детали в Followups). См. `docs/simplifications.md`
> «Plan 138.2 Phase-A».
>
> **РЕКОМЕНДАЦИЯ:** приземлять Ф.0 + Ф.2 + Ф.3 + Ф.4 как ОДИН атомарный multi-day
> unit; сначала фиксить `[M-138.2-vec-u8-literal-stride]` + Vec-of-record `_p` typedef
> bug, потом флип. Re-create `nova_tests/plan138_2/t3_universal_novec_unit.nv` когда
> флип реально приземлится.

Цель: `[]T` лоуэрится в `Vec[T]` в **КАЖДОМ** юните, не только где Vec упомянут.
Снять gate `contains_key("Vec")` (emit_c.rs:2055 + 4938).

**Две механики (выбрать после spike):**

- **Option A — Prelude integration.** Добавить в `std/prelude.nv`:
  `export import std.collections.vec_owned.{Vec, VecIter}`. Дать `vec_owned.nv`
  директиву `#prelude(core, runtime, errors)` для разрыва цикла
  prelude→vec_owned→prelude (зеркало `range.nv`). **Риск:** как с Range
  (Plan 62.D bis-1) может вскрыть latent codegen-баги (match-inference,
  closure-capture, W_PRELUDE_SHADOW). **Бонус:** `Vec` становится видимым именем
  без import (DX-плюс).
- **Option B — Codegen-seed (рекомендуется первым).** Безусловно регистрировать
  Vec[T] template на codegen-init (грузить шаблон из vec_owned в каждый юнит),
  чтобы gate проходил универсально **без** prelude-auto-import семантики. Легче по
  cycle-риску; `Vec` как имя не авто-видим (юзеры пишут `[]T` — что и нужно для
  сахара). Точечно решает реальную потребность (lowering `[]T`), не трогая
  prelude-видимость.

**Рекомендация:** spike Option B (легче, целит ровно в потребность); fallback на A
если codegen-seed не дотягивается до шаблона чисто. В обоих случаях — убрать gate.

**Acceptance Ф.0:** Vec-free юнит (`fn main(){ mut x []int = [1]; x.push(2) }` без
`import ...Vec`) генерит `Nova_Vec____nova_int*`, не `NovaArray`. Targeted suites
строятся.

**Commit:** `feat(plan138.2 Ф.0): universal []T->Vec[T] (remove contains_key gate)`

---

### Ф.1 — String-слой → **SUBSUMED by Plan 139** (решение 2026-06-10: 139-first)

> **Развилка решена (139-first, без двойной работы).** Вместо lightweight-переписки
> `[]T`-producers с сохранением примитива `nova_str`, string-слой закрывается
> **полностью** через **Plan 139** (`str` → Nova value-record `{ ptr *ro u8, len int }`).
> Plan 139 Ф.2 переписывает `@to_bytes/@to_chars/@split/@as_bytes/from_bytes_*` на
> Nova → `Vec` как часть превращения str в value-тип. `@as_bytes` zero-copy при этом
> **не требует** нового `as_ptr`-примитива — поле `@ptr` уже доступно методам str.
>
> Поэтому эта фаза в 138.2 **не выполняется** — см. `docs/plans/139-str-as-nova-value-type.md`.
> Канонический порядок: **138.2 Ф.0 → 139 Ф.0-Ф.2 → 138.2 Ф.2-Ф.4**.

---

### Ф.2 — Остальные NovaArray-producers (~1d)

- **parfor** — мигрировать NovaArray usage на Vec (или задокументировать почему остаётся).
- **closure-array `[]fn`** — решить: мигрировать на `Vec[fn]` (предпочтительно для
  полного retire) ЛИБО оставить документированным исключением `NovaArray_void_p*`
  → followup `[M-138.2-closure-array-vec]`. Исследовать feasibility `Vec[fn]` mono
  (closures = void_p).
- **Vec→NovaArray bulk-op bridge** — удалить (Vec-методы больше не через `nova_array_*`).

**Acceptance Ф.2:** grep не находит `[]T`→NovaArray lowering-путей, кроме
документированных исключений.

**Commit:** `refactor(plan138.2 Ф.2): migrate parfor/closure-array/bulk-bridge off NovaArray`

---

### Ф.3 — Удаление `NOVA_ARRAY_DECL/IMPL` + special-cases (настоящая 138.1 Ф.5) (~1d)

**Prerequisite:** `[M-138-vec-bulk-parity]` закрыт — Vec имеет copy_from/copy_within/fill/
append_zero/compare через RawMem (иначе `[]T` extend-family API регрессирует).

Теперь безопасно: удалить макросы из `array.h` (сохранить `nova_alloc`/realloc;
`nova_str`-helpers только если ещё нужны), убрать emit_c.rs NovaArray special-cases
(==, slice, field, static, instance-block, for-in Case 2).

**Acceptance Ф.3:** `grep NOVA_ARRAY_DECL/IMPL` пуст; build зелёный.

**Commit:** `refactor(plan138.2 Ф.3): remove NOVA_ARRAY_DECL/IMPL + special-cases`

---

### Ф.4 — Полная регрессия-to-green (~1-2d)

Широкая регрессия по всему array-использующему корпусу. Iterate per element-class.
Масштаб большой ([]T везде) — при согласии запустить full `nova test`; иначе broad-
curated + явный лог покрытия (no silent caps).

**Acceptance Ф.4:** 0 новых FAIL vs 138.1-landing baseline; 3 typed-storage FAIL'а
(nested/record/tuple) остаются fixed.

**Commit (несколько):** `fix(plan138.2 Ф.4): regression-to-green`

---

### Ф.5 — Docs + close (~0.5d)

- D239 **полностью** закрыт (NovaArray retired), D144 закрыт.
- D26 amend (str: `@as_ptr` примитив + `to_bytes`/`to_chars`/`split` теперь Nova).
- D216 amend если добавлен новый ptr-примитив.
- simplifications.md, project-creation.txt, nova-private/discussion-log.md, README, plan-status, memory.

**Commit:** `docs(plan138.2 Ф.5 D239): NovaArray fully retired; str in Nova; close`

---

## Risk register

| # | Риск | Sev | Mitigation |
|---|---|---|---|
| R1 | Vec-in-prelude вскрывает latent codegen-баги (Range-прецедент: 4 бага) | 🔴 HIGH | Ф.0 spike + Option B fallback (codegen-seed без prelude-семантики) |
| R2 | string-слой фундаментален — поломка `to_bytes`/`split`/`as_bytes` каскадит в encoding/text/hashmap | 🔴 HIGH | Ф.1 изолированно + string/encoding тесты ДО продолжения |
| R3 | `@as_bytes` zero-copy: GC interior-pointer (str-буфер жив пока view достижим) | 🟡 MED | conservative-scan покрывает; тест на view-after-str-drop |
| R4 | `Vec[fn]` closure-array mono может не собраться чисто | 🟡 MED | fallback: документированное исключение + `[M-138.2-closure-array-vec]` |
| R5 | масштаб финальной регрессии ([]T везде) | 🟡 MED | per-element-class fix order; лог покрытия |

---

## Acceptance criteria

- **C1** — 🔴 **НЕ ДОСТИГНУТ (BLOCKED 2026-06-10).** Vec-free юнит должен лоуэрить
  `[]int` → `Nova_Vec____nova_int*` (gate снят). Механика доказана (флип ON давал
  `Nova_Vec____nova_int*` × 32), но универсальный флип неотделим от Ф.2-Ф.4 (40+
  регрессий) — F0 откатан, флип на landed-ветке OFF (Vec-free → `NovaArray_nova_int*`).
  **B6/Ф.3 (retire NOVA_ARRAY_DECL/IMPL) остаётся ЗАБЛОКИРОВАННЫМ** — Ф.0 не приземлился.
- **C2** — `str.to_bytes/to_chars/split` — Nova-body, возвращают `Vec[u8]`/`Vec[char]`/`Vec[str]`.
- **C3** — `str.as_bytes` — zero-copy `Vec[u8]` view через `str.as_ptr()` примитив; GC-safe.
- **C4** — `from_bytes_*` принимают `Vec[u8]` (ABI layout-идентичен), тесты round-trip зелёные.
- **C5** — `grep NOVA_ARRAY_DECL\|NOVA_ARRAY_IMPL` в array.h — пусто.
- **C6** — `grep NovaArray_` в emit_c.rs — только документированные исключения (если closure-array отложен).
- **C7** — 0 новых FAIL vs 138.1-landing; 3 typed-storage baseline FAIL'а остаются fixed.
- **C8** — D239/D144 закрыты; D26 amend (str primitives) задокументирован.

---

## Followups

- `[M-138.2-closure-array-vec]` — `[]fn` → `Vec[fn]` если в Ф.2 отложено.
- `[M-138.2-codepoint-cursor]` — O(n) codepoint-iterator примитив для `to_chars` (вместо O(n²) `@char_at`).
- `[M-138.2-fixed-array]` — `[N]T` FixedArray (наследие 138.1, вне scope).
- ✅ `[M-138-rawmem-bulk-ops]` — DONE (commit f0bec0e6717): `@realloc_to`/`@append`/`@clone` →
  `RawMem.copy_nonoverlapping`; `@insert`/`@remove` shift → `RawMem.copy` (memmove). GC-safe casts.
- ✅ `[M-138-vec-bulk-parity]` — DONE (commit f0bec0e6717): `@copy_from`/`@copy_within` (RawMem.copy),
  `@fill`/`@append_zero` (RawMem.fill), `@compare` (RawMem.compare, normalized -1/0/+1) добавлены на Vec.
  plan138_2 t1 2/2. **PREREQUISITE для Ф.3 закрыт.**
- ✅ `[M-138-rawmem-dedup-fill]` — DONE (commit 43fb6a736ea): `write_bytes` удалён, `fill` остался.
- ✅ `[M-138-mutindex-rename]` — **CLOSED (Plan 138.4 Ф.2 G-A, 2026-06-11, commit `14e6da56175`).**
  Vec `mut @index_set(i, val)` переименован в третий `mut @index(i int, val T)` overload (3-way:
  read scalar `@index(i)->T` / read range `@index(r Range)->Self` / write `mut @index(i,val)`),
  все три эмитят DISTINCT C-символы (`Nova_Vec_method_index` / `_index__nova_int_void_p` /
  `_index__Nova_Range_p`). ROOT CAUSE (уточнён эмпирически): collision был в ERASED-template пути
  (`Nova_Vec_method_*` via `emit_generic_method_erased`→`mangle_fn`), НЕ в mono-пути как
  предполагалось — mono-путь (`register_vec_mono_method`) вызывается только для non-overloaded
  методов, а `v[i]`/`v[i]=val` codegen-инлайнятся и никогда не диспатчат по имени. `mangle_fn`
  `want_params` (via `type_ref_to_c`, bare T → `nova_int`) расходился с registry pre-pass
  `erased_type_ref_c` (bare T → `void*`). FIX: helper `mangle_want_params(recv, f)` реплицирует
  erasure точно; single-overload методы сохраняют base C-name → zero mass regression. D240 prose
  уже был исправлен (94d39606d6a); теперь Vec удовлетворяет MutIndex и через именованный overload.
- ✅ `[M-138.2-vec-u8-literal-stride]` — **DONE (Phase-A, commit 5c41f72a6e9).** `[]u8 = [1,2,3]` литерал
  хранил int64-страйд вместо 1-байтового u8 (typed-storage gap для sized-int литералов, 138.1 Ф.2). Root:
  element C type брался из `infer_expr_c_type` первого item'а, int-литерал коллапсит в erased `nova_int`.
  Fix в двух симметричных сайтах: emit (`try_emit_typed_vec_literal` ~26305) + infer
  (`infer_expr_c_type::ArrayLit` ~31692) — когда первый item collapse'ит в `nova_int` дефолт, но
  присутствует sized-int element-hint (из `[]u8`/`[]iNN` annotation), hint выигрывает → литерал
  монорфизирует `Vec[u8]`/`Vec[i32]` с корректным страйдом. Fixture t5_u8_literal_stride.nv (4 теста).
  **Класс регрессий #2 pre-fixed.** Discovered (отдельный, НЕ тронут): `Vec[i32].new()` эмитит undeclared
  `Nova_Vec____Nova_i32_p` (explicit sized-int Vec instantiation boxes i32 как `Nova_i32*` вместо `int32_t`)
  → candidate followup `[M-138.2-vec-sized-int-explicit-mangle]`.
- ✅ `[M-138.2-vec-fluent-push]` — **DONE (Phase-A, commit d22c96f7423).** Все 11 chainable Vec[T]
  мутаторов в `std/collections/vec_owned.nv` переведены с `-> ()` на `-> @` (return self), зеркало
  StringBuilder.append D181: @push, @extend, @insert, @append, @reverse, @clear, @truncate, @copy_from,
  @copy_within, @fill, @append_zero. std .nv читается с диска — no rebuild. Statement-form callsites
  (`v.push(x)`) работают (return discard). Fixture t6_fluent_push.nv (7 тестов). Три PRE-EXISTING codegen-gap
  при fluent chaining вскрыты (НЕ этой фазы, candidate followups): (A) `[M-138.2-vec-fluent-chain-int-mixed]`
  — Vec[int] (erased nova_int) chains мис-диспатчат на erased `Nova_Vec_method_push`; (B) mixed-method chains
  (`.reverse().truncate().fill()`) резолвят post-first-call receiver по голому имени метода через ВСЕ типы
  (picked Range::reverse / StringBuilder::truncate); (C) `[M-138.2-vec-extend-generic-fluent-return]` — @extend
  (method-level generic `[S Iter[T]]`) chained `-> @` мис-типизирует возврат в `void*`. Все три — отсутствие
  full-signature mangling + pinning chained-receiver type к concrete Vec[T] mono (та же область gate-сайтов).
- 🟡 `[M-138-getmut-rename]` — **INFRA-DONE + WON'T-FIX-FOOTGUN (Plan 138.4 Ф.3 G-B, 2026-06-11,
  commit `7c82ff2061c`).** Infra-gap (1) `[M-138-getmut-rename-infer-gap]` **ЗАКРЫТ**: `infer_expr_c_type`
  теперь tiebreak'ает RETURN-тип method-call по receiver mutability (widened `recv_and_method` tuple до
  4-эл с call-site recv-mut via `is_obj_mutable`; multi-overload return-pool предпочитает overload с
  `recv_mutable == recv_mut` BEFORE `pool.first()`, guarded `pool.len()>1`) — зеркало `mangle_fn`
  recv-mut tiebreak'а (commit `14e6da56175` со стороны emit). Differing-return recv-mut overloads теперь
  корректно инферятся (проверено probe'ом: `Box @peek()->int` ro vs `mut @peek()->str`).
  **ОДНАКО** сам rename НЕ выполнен: footgun (2) `[M-138-getmut-dispatch-mut-recv-footgun]` не снят —
  call-mangle tiebreak'ает recv_mutable к CALLER mutability, поэтому на `mut` receiver plain read
  `v.get(1)` ВСЕГДА диспатчил бы mut/pointer-overload `Option[*mut T]`, ломая read-by-value (plan131
  `assert(v.get(1)==Some(99))`). DECISION: `@get_mut` ОСТАЁТСЯ distinct (Rust get/get_mut precedent —
  no footgun); rename re-attempt ТОЛЬКО если mut-recv read-intent дизамбигуируется отдельно. Infra-fix
  ценен для legit differing-return recv-mut overloads вообще.

> **Phase-A итог (de-risk, 2026-06-10, branch `plan-138.1`):** 3 marker'а DONE
> ([M-138.2-vec-u8-literal-stride], Vec-of-record `_p` typedef = класс #3, [M-138.2-vec-fluent-push]),
> 2 изначально BLOCKED+REVERTED ([M-138-getmut-rename], [M-138-mutindex-rename] — оба упёрлись в
> отсутствие full-signature overload mangling для generic-type Vec методов). **UPDATE (2026-06-11,
> Plan 138.4):** оба разблокированы codegen-фиксом — `[M-138-mutindex-rename]` ✅ CLOSED (G-A
> `14e6da56175`); `[M-138-getmut-rename]` 🟡 INFRA-DONE (G-B `7c82ff2061c`), сам rename = won't-fix-footgun
> (`@get_mut` distinct). Регрессионные классы #2+#3 из Ф.0 §«5 классов» pre-fixed → атомарный capstone
> сужен до #1/#4/#5.

---

### Design-cleanup batch (N1–N4, 2026-06-10, branch `plan-138.1`)

Пакет осознанных std-уровневых cleanup'ов поверх Phase-A (все per-commit-green,
std `.nv` читается с диска — rebuild не требовался). Каждый — один marker = один коммит:

- ✅ `[M-138.2-next-pow2-branchless]` — **DONE (commit `1e8bdcc147f`).** Приватный
  `next_pow2` в `std/collections/hashmap.nv` (~стр. 570): O(log n) цикл → канонический
  O(1) branchless bit-smear (`x = n-1; x|=x>>1; >>2; >>4; >>8; >>16; >>32; x+1`), guard
  `if n <= 1 { return 1 }` (n=0/1 + защита от n-1 underflow). `|`/`>>` на int подтверждены
  по `std/encoding/base64.nv`. `next_pow2` приватная (не экспортится) → equivalence-тест
  (n=0→1,1→1,2→2,3→4,4→4,5→8,8→8,1000→1024,1024→1024) добавлен **inline** в hashmap.nv,
  где fn в scope (внешний plan138_2-тест не может вызвать приватную fn). hashmap.nv inline 1/0.
- ✅ `[M-138.2-vec-type-priv]` — **DONE part (a) (commit `6e1ac8ace72`).** Type-level `priv`
  flip на `Vec[T]` в `std/collections/vec_owned.nv` (Plan 124 V2 / spec 02-types.md §1):
  `export type Vec[T] {` → `export type Vec[T] priv {`, сняты 3 per-field `priv` модификатора
  (`priv mut data/len/cap` → `mut …`). Effective visibility идентична — все поля private-by-default
  через header. **Part (b)** ✅ **VERIFIED + SUPERSEDED (C8, Plan 138.5 model, 2026-06-11, commit
  `<C8-DOCS-COMMIT>`).** Поле `data` теперь снова **`mut data *mut T`** — финальная 138.5 модель.
  **История:** Plan 138.4 Ф.4 G-D (commit `38360c30d80`) флипнул `*mut T` → `*T`, опираясь на D216 V2
  §V2.1 right-binding + binding-mut auto-mutate (`mut data *T ≡ mut data *mut T` через
  `field_type_with_binding_mut`/`promote_pointer_pointee_mut`). **Plan 138.5 РЕТИРИТ это
  auto-mutate-сцепление:** под 138.5 голый `*T` — это **read-only pointee** (ro-pointee), а
  pointee-mutability задаётся явным POSTFIX `*mut T`. Поэтому `*T` здесь дал бы WRONG const-буфер
  (нечем было бы сделать `@data[i] = val`). Под 138.5 binding-mut (`mut` перед именем) управляет только
  reassign-ability *поля* (`@data = dst`), а pointee-mut — отдельный postfix `*mut T`. C8 вернул поле к
  `mut data *mut T` и подтвердил GREEN (plan138_1 10/0, inline vec_owned 1/0, остальные targeted suites
  без регрессий). Блокер `[M-138.2-v2-propagation-impl-gap]` (закрытый в 138.4 как enabler флипа на `*T`)
  стал **MOOT** под 138.5 — auto-mutate-путь больше не используется этим полем.
- ✅ `[M-138.2-vec-self-return]` — **DONE return-position subset (commit `af6b0b59718`).**
  В `vec_owned.nv` (Plan 51 «type declared once» / D182 / D66): return-типы `-> Vec[T]`,
  равные receiver'у, → `-> Self` (`new`, `with_capacity`, `from`, range `@index(r Range)`,
  `@clone`; `-> Option[Vec[T]]` → `-> Option[Self]` на range `@get`). Конструктор-литералы
  де-типизированы: `new()` → bare arrow `=> { data:…, len:0, cap:0 }`; multi-statement тела
  → `Self { … }` (D66, spec 02-types.md:3078). **Param-position `Self`** (`@append`/`@copy_from`/
  `@compare`/`@equal`) **BLOCKED+REVERTED** → spawned `[M-138.2-self-in-param]` (см. ниже).
- ✅ `[M-138.2-stdlib-separator-mojibake]` — **DONE (commit `2e57e3d701b`).** Box-drawing
  separator mojibake (double-encoded U+2500, байты `d0 b2 e2 80 9d d0 82`, рендерит `в"Ђ`)
  в std-комментах: ровно 2 файла — `vec_owned.nv` (29 строк) + `prelude/protocols.nv` (10 строк);
  examples/ — 0 hits. Byte-precise transform только на `^\s*//` строках, run-длины сохранены
  → корректный `─` (U+2500, `e2 94 80`), как уже в `crc32.nv`. Доказано: re-apply на pristine
  backup даёт byte-identical output; 39 ins == 39 del, ни одной non-`//` строки в diff.

**Spawned gap-markers (codegen, требуют `.rs` + rebuild — НЕ workable из std/*.nv):**

- ✅ `[M-138.2-v2-propagation-impl-gap]` — **CLOSED (Plan 138.4 Ф.4 G-D, 2026-06-11, commit
  `38360c30d80`).** `data *mut T` → `*T` теперь компилируется (plan138_1 10/0). ROOT CAUSE отличался
  от изначальной гипотезы: НЕ «generic T лоуэрится в Nova_any» (T-subst через `current_type_subst`
  работал) — а binding-mut `const`-pointee. `mut data *T` — это canonical *readonly* pointer (D216 §1),
  поэтому для mut-bound поля codegen давал `const`-pointee; для DEAD prelude-induced `Vec[any]` инстанса
  (из variadic `...items []any` в `print`/`println`) это давало `const Nova_any** data`, и forward-typedef
  pre-pass (emit_c.rs ~12849, `starts_with("Nova_")` после strip `*`) SKIP'ал typedef (`const Nova_any`
  не начинается с `Nova_`) → `unknown type name 'Nova_any'` CC-FAIL. FIX: `field_type_with_binding_mut`
  + `promote_pointer_pointee_mut` — для mutable-поля immediate `Pointer(pointee)` (non-Mut/non-Unsafe)
  переписывается в `Pointer(Mut(pointee))`, applying `mut p *T ≡ mut p *mut T` (D33 §2 / D216 §V2.6);
  transparent над Readonly/Mut/Unsafe wrappers → generic pointee T сохраняется через mono. Applied на
  bare-template (emit_c.rs ~8848) И mono-path (~12891). VecIter `priv data *mut T` (read-only, не
  mut-bound) оставлен как есть — вне scope. (Optional followup: harden forward-typedef pre-pass strip'ать
  leading `const ` перед `starts_with("Nova_")` — не нужно сейчас, binding-mut снял const для mut-поля.)
- 🔴 `[M-138.2-self-in-param]` — **OPEN.** `Self` в PARAM-позиции generic-type метода
  (`@append(mut other Self)` и т.п.) не компилируется: codegen лоуэрит param-тип БЕЗ
  receiver-substitution → forward-decl ≠ definition → `conflicting types for
  'Nova_Vec_method_append'` (все 5 plan138_1 CC-FAIL). Return-position `Self` и `Self { … }`
  литералы работают; блокирована только param-позиция. Тот же класс отсутствия generic-method
  param-type substitution в emit_c.rs. Блокирует param-Self для `@append`/`@copy_from`/`@compare`/`@equal`.

---

### Vec std-cleanup batch (C1–C8, 2026-06-11, branch `plan-138.1`)

Пакет атомарных std-уровневых cleanup'ов на `std/collections/vec_owned.nv` поверх
Plan 138.4 (post-138.4 baseline, HEAD `de71fd1`). Все per-commit-green, std `.nv`
читается с диска — rebuild не требовался. Каждый — один marker = один коммит (+ migrated
fixtures). Pointer-model — финальная **138.5** (postfix pointee-mut `*ro T`/`*mut T`,
binding-mut через `mut` перед именем, без prefix-модификаторов).

- ✅ `[M-138.2-vec-reserve-dedup]` — **DONE (commit `60030e09106`).** Слиты `@reserve` +
  приватный `@ensure_capacity` в единый public `@reserve(additional int) -> ()` (×2 growth,
  initial cap 8, доублинг до needed, делегат в `@realloc_to`). 3 внутренних вызова repointed.
- ✅ `[M-138.2-vec-drop-shrink]` — **DONE (commit `4a187b966fa`).** Удалены `@shrink_to` +
  `@shrink_to_fit` (GC-niche, 0 реальных потребителей — GC сам реклеймит старые буферы).
  `@realloc_to` (единственный реаллокатор) сохранён — нужен `@reserve`. 3 теста (vec_capacity_pos,
  vec_grow_pos, inline) migrated.
- ✅ `[M-138.2-vec-getmut-reconsider]` — **DONE (commit `3434c58d868`).** Удалён
  `@get_mut(i) -> Option[*mut T]` (неидиоматичный raw-pointer API). Единственный потребитель
  (plan131/vec_access_pos.nv) migrated на safe `v[i] = val` (D240 MutIndex write).
- ✅ `[M-138.2-vec-as-ptr]` — **DONE (commit `6a7965806b7`).** Добавлен buffer-pointer accessor
  `@as_ptr` (recv-mut overload: `-> *ro T` на ro-receiver, `mut -> *mut T` на mut-receiver, оба
  `=> @data`). Fixture t7_vec_as_ptr.nv (5 sub-tests). **Известное ограничение:** call-site
  dispatch перехватывается pre-existing `Nova_Vec____` as_ptr-bridge (emit_c.rs:21143/31537) →
  `*ro T`/`*mut T` distinction НЕ enforced (ro-recv write ошибочно разрешён). Functional GREEN
  для общего FFI (read+write+ptr-arith). Followup `[M-138.2-vec-as-ptr-recv-mut-dispatch]`.
- ✅ `[M-138.2-vec-from-fluent]` — **DONE (commit `8a95a23f59e`).** `Vec[T].from(items []T)` →
  fluent one-liner `=> Vec[T].with_capacity(items.len()).extend(items)` — shallow value-copy
  (любой T, без T:Clone bound), НЕ clone.
- ✅ `[M-138.2-vec-inline-unsafe-elem]` — **DONE (commit `229047a854f`).** Инлайнены 4 single-use
  `unsafe { @data[i] }` temps: `@pop`/`@get` (Some-wrap), `@display`/`@debug` (`(unsafe {...}).m(sb)`).
  Подтверждено: postfix-method-on-unsafe-block form парсится+лоуэрится корректно.
- ✅ `[M-138.2-vec-while-to-for]` — **DONE (commit `98c57e3a9ac`).** 6 forward unit-step
  index-loops `while i<@len` → `for i in 0..@len` (`@fill`/`@equal`/`@clone`/`@display`/`@debug`/
  `@retain` dual-cursor). 2 KEPT-as-while (`@reserve` ×2-step, `@reverse` two-pointer) с комментами.
- ✅ `[M-138.2-vec-type-priv]` **part-b** — **VERIFIED + SUPERSEDED (C8, commit `<C8-DOCS-COMMIT>`).**
  Поле возвращено к `mut data *mut T` под 138.5 (см. подробности в Followups выше); `*T` 138.4-флип
  ретирован как ro-pointee под 138.5. FULL final targeted regression (см. ниже) — 0 регрессий.

**Final targeted regression (C8, post-batch, все vs post-138.4 baseline):**
plan138_1 10/0, plan138_2 6/0, plan138_3 2/0, plan131 27/1 (pre-existing vec_debug_pos
`${v:?}` interp gap), plan90_1 20/0, plan118_1 11/0, plan126_2 9/1 (pre-existing p5_printable),
inline std/collections/vec_owned.nv 1/0. **0 новых FAIL.**

---

## Связь

| План / D | Что |
|---|---|
| Plan 138.1 | partial-landing (флип где Vec есть + typed-storage gap); этот план — добивание |
| D239 | закрывается ПОЛНОСТЬЮ здесь (NovaArray retired) |
| D144 | закрывается |
| D26 | amend — str `@as_ptr` + Nova-body `to_bytes`/`to_chars`/`split` |
| D216 | amend если новый str ptr-примитив |

---

# PRODUCTION-GRADE ATOMIC DECOMPOSITION (capstone re-plan, 2026-06-11)

> Назначение. Превратить «один атомарный multi-day capstone (Ф.0+Ф.2+Ф.3+Ф.4)» в
> последовательность атомарных, всегда-зелёных фаз. 138.4 закрыл codegen-инфраструктурный
> блокер, делавший Ф.0 неотделимым от Ф.2-Ф.4 (флип в изоляции = 40+ регрессий по ~5 классам).
> Теперь каждый оставшийся регрессионный класс гасится СВОЕЙ atomic-фазой ДО флипа — так
> универсальный флип (Ф.0-final) приземляется на de-risked-дереве.
>
> Дополняет (не заменяет) разрез «## Фазы» выше: Ф.0 (исход BLOCKED), Ф.1 (subsumed by 139),
> Ф.2/Ф.3/Ф.4/Ф.5 переиспользуются с production-grade acceptance ниже.

## Что разблокировало этот разрез (138.4 + Phase-A)

| Регрессионный класс при флипе ON | Статус | Где гасится |
|---|---|---|
| #1 insert/append bulk-API divergence (plan90_1 20-to-10) | OPEN | Ф.0a; G-A mangling 138.4 14e6da56175 дал distinct C-символы |
| #2 []u8 literal int64-stride (plan118_1 11-to-6) | PRE-FIXED Phase-A 5c41f72a6e9 | — |
| #3 Vec-of-record write-index _p typedef (plan138_1 10-to-9) | PRE-FIXED Phase-A c7a8f365aa3 | — |
| #4 map_literals []T-bucket cascade (28-to-9) | ✅ CLOSED Ф.0b | hashmap.nv `import vec_owned.{Vec}` → typed Vec bucket; `new_buckets` rewritten to explicit `Vec[Slot]` API (value-pos `[]T` still NovaArray-erased pre-flip) |
| #5 user type Vec SHADOW (plan123_3_1 E7310) | ✅ CLOSED Ф.0c | shadow-set `user_shadowed_generic_types` (entry-vs-merged arity-diff) guards type-checker arity/assignability passes + 4 codegen generic-registration sites + method-receiver prepop + should_skip_fn; D29 method-level full-shadow; t14/t15/t16 |
| codegen-инфра (mangling/dispatch/pointee/clone) | DONE 138.4 | G-A/G-B/G-C/G-D |

## Стандартный мандат «production-grade» (применяется к КАЖДОЙ фазе)

Каждая фаза — production-grade, без упрощений; всё реализуется для прода, не под узкий
acceptance. Для каждой фазы ОБЯЗАТЕЛЬНО:

1. Один coherent change в один commit. Несколько под-задач в несколько коммитов (по одному
   на под-задачу), каждый per-commit-green. Никаких half-states в дереве.
2. Spec / Decision / Open-Question touch В ТОМ ЖЕ коммите — amend релевантного D-блока
   (D239/D144/D141/D27/D26/D216/D238/D240/D29) при любом наблюдаемом изменении семантики;
   новый дизайн-вопрос в Open-Question в spec/decisions.
3. Docs-апдейты В ТОМ ЖЕ коммите (НЕ потом): docs/project-creation.txt (дневник);
   docs/simplifications.md (что упрощено/ретрактировано + per-commit SHA-таблица + строка
   Регрессии); docs/plans/backlog-followups.md (новые floating-маркеры в OPEN-view; убрать
   закрытые; plan-bound в индекс-строку с home); nova-private/discussion-log.md (narrative;
   живёт в d:/Sources/nv-lang/nova-private/ — SHARED, ВНЕ worktree); этот план (Followups +
   per-phase статус); README/plan-status/memory где релевантно. Коммитить ПОСЛЕ каждой
   big-task (несколько задач в несколько коммитов).
4. Positive И negative фикстуры, именованные (per-phase ниже). Negative помечается
   EXPECT_CC_ERROR / EXPECT_COMPILE_ERROR / EXPECT_COMPILE_WARNING / runtime-panic ассерт.
   Проверяются через RELEASE nova binary (на DOC-ONLY шаге — только специфицируются).
5. Явные machine-checkable acceptance (grep-инвариант + suite-счёт pass/fail, no silent caps)
   + per-phase риск + миграция.
6. Commit-message дан per-phase как шаблон.
7. Worktree/branch. Все фазы на plan-138.2 off plan-138.1 (или прямо plan-138.1); НЕ на
   default-ветке. Не коммитить / git add, пока пользователь не попросит (для DOC-фазы — НЕ
   коммитить вовсе).

> Anchor-drift note (re-grep ПЕРЕД правкой). 4 gate-сайта contains_key Vec на 2026-06-11
> (HEAD plan-138.1) — emit_c.rs:2058 / 4995 / 26402 / 32030 (исторические 2055/4938/26100/31637
> сдвинулись). Closure-array exclusion: emit_c.rs:4972-4979 ([]fn в NovaArray_void_p*). D141
> extend-family bulk-dispatch: emit_c.rs:21399-21419 (append/insert/copy_from/copy_within/fill/
> append_zero/reserve/truncate/compare в nova_array_*_elem). W_PRELUDE_SHADOW skip (Types/Fns/
> Consts by name+span): emit_c.rs:2246-2315; lint: lints.rs:1459. array.h macros: NOVA_ARRAY_DECL
> / NOVA_ARRAY_IMPL (lines 48-226) + per-primitive instantiations (239-342).

---

## Ф.0a — Класс #1: insert/append bulk-API parity на Vec[T] (~0.5-1d, ДО флипа)

Почему первым. Под D141 []T extend-family имеет bulk-семантику: dst.insert(i, src) вставляет
ВСЕ элементы массива src в позицию i; dst.append(src) дописывает весь src в хвост (фикстуры
nova_tests/plan90_1/insert_middle.nv, append_basic.nv; C-builtin nova_array_insert_* /
nova_array_append_*, array.h:109-151). Сегодня лоуэрятся в nova_array_*_elem (emit_c.rs:21399-
21419). После флипа []T == Vec[T] начнут диспатчить на Vec-методы: у Vec @append(mut other
Vec[T]) УЖЕ bulk (совпадает), а @insert(i int, v T) = single-element (несовместимо = класс #1).
Чинится ДО флипа: G-A (138.4) даёт двум @insert overload distinct C-символы; дерево остаётся
зелёным (NovaArray-путь ещё активен, новые Vec-overload доступны).

Scope (без упрощений):
- std/collections/vec_owned.nv: добавить bulk-insert overload
  export fn Vec[T] mut @insert(i int, other Vec[T]) -> @ — вставляет все other.len() элементов
  в позицию i одним overlap-safe RawMem.copy (memmove) после @reserve(other.len()); семантика
  ТОЧНО воспроизводит nova_array_insert_* (array.h:129-151: prefix-copy + hole + tail-shift,
  self-insert через memmove, OOB-panic i<0 или i>len). Existing single @insert(i int, v T)
  СОХРАНЯЕТСЯ (Rust-парность: Vec.insert single + наш bulk-overload).
- @append(mut other Vec[T]) уже bulk — ТОЛЬКО проверить byte-паритет с nova_array_append_*
  (self-append safety + 2x growth) фикстурой; кода менять не нужно при подтверждённом паритете.
- Решить + задокументировать overload-resolution single-vs-bulk: dst.insert(i, src) где
  src: Vec[T] = bulk; dst.insert(i, x) где x: T = single. Конфликт Vec[Vec[U]].insert(i, inner)
  (оба arg-типа = Vec[U]) = precise-T-wins: single (вставка вложенного вектора как одного
  элемента). Если резолвер не дизамбигуирует чисто = Open-Question + fallback: bulk переименовать
  в @splice/@insert_all (API-change = запись в simplifications + миграция фикстур plan90_1).

Spec / D: D141 (02-types.md / 08-runtime.md) — extend-family bulk-insert/append теперь Vec-методы
(RawMem-backed), не C-builtin nova_array_*; зафиксировать overload-resolution. D239 Правила —
extend-family parity как часть []T == Vec[T].

Фикстуры:
- pos plan138_2/t8_vec_bulk_insert_pos.nv — Vec[int].from([1,2,5,6]).insert(2, Vec[int].from([3,4]))
  = [1,2,3,4,5,6]; insert@0; insert@len (=append); Vec[u8] (stride-1); self-insert.
- pos plan138_2/t9_vec_bulk_append_parity_pos.nv — append self-append, 2x growth amortized,
  empty-src no-op (зеркало plan90_1/append_*).
- pos plan138_2/t10_vec_insert_single_vs_bulk_pos.nv — single vs bulk на Vec[int] дают DISTINCT
  поведение; Vec[Vec[int]] single-insert вложенного вектора.
- neg plan138_2/t11_vec_bulk_insert_oob_neg.nv — i<0 или i>len = runtime panic index out of bounds
  (зеркало plan90_1/insert_oob_*).

Acceptance Ф.0a: plan138_2 t8/t9/t10 PASS, t11 panic-as-expected; vec_owned.nv inline 1/0;
plan90_1 20/0 БЕЗ флипа (NovaArray-путь активен — paritet-baseline); grep @insert в vec_owned.nv
= ровно 2 overload, distinct C-символы в .c (_method_insert + _method_insert__nova_int_Nova_Vec);
0 новых FAIL vs post-138.4 baseline (plan138_1 10/0, plan138_3 2/0, plan131 27/1, plan118_1 11/0,
plan126_2 9/1).

Risk: MED — single-vs-bulk резолюция для Vec[Vec[U]]; mitigation = precise-T-wins + fallback
rename @splice.

✅ ЗАКРЫТ 2026-06-11 (через fallback). plan138_2 6/0 → 10/0 (t8 splice middle/front/end/
empty/u8/self; t9 append parity; t10 single-insert vs bulk-splice DISTINCT routing; t11
splice OOB panic). Broad regression 0 NEW FAIL: plan138_1 10/0, plan138_3 2/0, plan90_1 20/0
(parity-baseline), plan131 27/1, plan126_2 9/1 (all pre-existing).

РЕЗОЛЮЦИЯ Open-Question (FALLBACK сработал). Резолвер НЕ дизамбигуирует single-vs-bulk:
generic-метод overloads коллапсят в монорфизации (`mono_method_decls` emit_c.rs ~8404 keyed
`(type, name)` — один FnDecl на key; mono-sentinel `MethodSig` ~8408 несёт пустой
`param_c_types` → `resolve_overload` emit_c.rs:9913 force-fit'ит Vec-arg в single `insert(i,T)`,
verified: t10 «DISTINCT routing» FAIL до rename, Vec-pointer→nova_int garbage). Применён
санкционированный fallback: bulk = **`@splice(i int, other Vec[T])`**, single `@insert(i int, v T)`
сохранён. Distinct имена → distinct C-символы через single-overload short-circuit (per-T mono'd:
`Vec____nova_int_method_insert` + `_method_splice`). Open-Questions: `[M-138.2-bulk-insert-overload]`
(fold splice→insert-overload) + `[M-138.2-generic-method-overload-mono]` (codegen mono
overload-routing). DISCOVERED orthogonal pre-existing: `[M-138.2-nested-vec-elem-readback]`
(`Vec[Vec[T]]` второй push+get → повреждённый элемент / CC-FAIL missing `NovaOpt_nova_int_p`;
t10 нер-вер read-back на nested снят, проверяется только len-routing).

Commit: feat(plan138.2 Ф.0a #1): bulk-insert overload on Vec[T] (D141 extend-family parity); pre-flip

---

## Ф.0b — Класс #4: map_literals в Vec-backed bucket-arrays (~0.5-1d, ДО флипа)

Почему ДО флипа, изолированно. Map-литералы ({a:1, b:2}) строят []T-backed bucket-массивы внутри
HashMap-coercion (nova_tests/map_literals/ — 50+ фикстур; positive_* строят/итерируют/spread/
clone-merge/record-punning, negative_* ловят shadow/no-from-fields/nan-key/dup-key). При глобальном
флипе bucket-[]T станет Vec[T] = 28-to-9 регрессий (класс #4). HashMap-юнит УЖЕ тянет Vec
(std/collections тянет коллекции) = gate contains_key Vec для него уже проходит, значит
bucket-путь мигрируется на Vec изолированно (только map-coercion codegen + std/collections/
hashmap.nv), остальной []T-мир остаётся на NovaArray до Ф.0-final.

Scope (без упрощений):
- Аудит map-literal coercion (MapLitCtx.build, hashmap #from_fields/from_pairs путь; grep
  from_fields/MapLit/MapLitCtx в codegen) — где строятся bucket-[]T. Гарантировать typed
  Vec-backing (НЕ int64-erasure) для ВСЕХ типов значений из фикстур: int/str/bool/record/
  nested-map/JsonValue/spread.
- std/collections/hashmap.nv: bucket-массивы + next_pow2 growth ([M-138.2-next-pow2-branchless]
  DONE) работают на Vec-backing без NovaArray-bridge.
- Canonical-identity shadow-check (peer_files.path фильтр для stdlib HashMap) — не ломается при
  Vec-backing (negative_user_shadows_hashmap.nv остаётся EXPECT_CC_ERROR).

Spec / D: D239 Связь — map-literal bucket lowering на Vec. Если hashmap-layout меняется наблюдаемо
— amend map/HashMap D-блока (свериться с README decisions-index по номеру).

Фикстуры: переиспользовать весь nova_tests/map_literals/ как regression-gate (не дублировать), плюс:
- pos plan138_2/t12_map_vec_bucket_pos.nv — {1:"a", 2:"b"} insert/get/iter с Vec-backed bucket;
  record-value map; nested map-in-map.
- neg (existing) map_literals/negative_user_shadows_hashmap.nv — user type HashMap shadow всё ещё
  EXPECT_CC_ERROR (canonical-identity intact).
- neg plan138_2/t13_map_empty_in_map_position_neg.nv — пограничный {}-в-map-позиции (зеркало
  positive_empty_in_map_position; Vec-backed empty-bucket не падает).

Acceptance Ф.0b: map_literals suite 0 новых FAIL (positive_* PASS, negative_* error-as-expected)
на Vec-backed bucket — счёт логируется явно (no silent caps); plan138_2 t12 PASS, t13 как ожидается;
grep в hashmap CU .c: bucket = Nova_Vec____*, НЕ NovaArray_; 0 новых FAIL vs post-138.4 baseline.

Risk: MED — map-coercion codegen сложен (record-punning/spread/from_fields); mitigation =
50-фикстурный map_literals suite как полный gate ДО флипа.

Commit: refactor(plan138.2 Ф.0b #4): Vec-backed map-literal bucket arrays (isolated to hashmap CU); pre-flip

### Ф.0b — CLOSED (что реально сделано)

Two-part fix, isolated to the HashMap compilation unit, no Rust rebuild (std `.nv`
read from disk at runtime):

1. **`std/collections/hashmap.nv`: `import std.collections.vec_owned.{Vec}`.** Registers
   `generic_type_templates["Vec"]` whenever hashmap.nv is a peer file, so the `_buckets
   []Slot[K,V]` field and `new_buckets` return type lower (via the type-reference gate at
   `emit_c.rs:2074`) to a TYPED `Nova_Vec____Nova_Slot____*` backing instead of the
   int64-erased `NovaArray_nova_int*`. vec_owned only imports raw_mem → no cycle with
   hashmap (verified import graph). NOT a prelude change — that is Ф.0-final.

2. **`new_buckets[K,V]` rewritten to the explicit `Vec[Slot[K,V]]` API.** ROOT-CAUSE
   FINDING: `[]T` in **value/expression position** (`[]Slot[K,V].with_capacity(cap)`,
   `arr.push(...)`) is NOT Vec-gate-aware pre-flip — the D38 array-static-method dispatch
   (`emit_c.rs:19863-19925`) unconditionally routes `[]T.with_capacity`/`.new` to
   `nova_array_new_<suffix>` (int64-erased NovaArray), with no `contains_key("Vec")` guard.
   Meanwhile the `[]Slot[K,V]` **type-reference** position (field / return type) already
   resolves to typed Vec under the gate. That desync made `new_buckets` build an int-erased
   buffer and return it under a typed-Vec pointer → corrupted readback after `@maybe_grow`
   rehash (insert-from-empty past load factor returned garbage / wrong-len). Rewriting to
   explicit `Vec[Slot[K,V]].with_capacity` + `.push` builds typed storage consistent with
   the field/return type. Value-position `[]T`→Vec lowering remains a Ф.0-final concern
   (tracked by t3); only the HashMap CU is migrated here.

Regression class #4 was NOT a map-coercion codegen problem (record-punning/spread/from_fields
all passed unchanged) — it was this rehash-path corruption, which the original 50-fixture
map_literals gate did NOT catch (those build via `with_capacity`+`insert_new`, never the
runtime resize/rehash loop). t12's "growth past load factor (rehash)" test is the new guard.

Acceptance MET: map_literals 28/1 (= baseline; only pre-existing `positive_const_map`
E_CONST_NOT_CONSTEXPR); plan138_2 12/0 (t12/t13 PASS); hashmap CU `.c` bucket =
`Nova_Vec____Nova_Slot____*`; 0 NEW FAIL across the broad regression corpus.

Commits: `<f0b-1>` (hashmap.nv import + new_buckets rewrite + t12/t13 fixtures + docs),
see SHA-table in simplifications.md.

NO new [M-138.2-*] markers needed — the value-position `[]T`-NovaArray-erasure is the
EXPECTED pre-flip state (Ф.0-final flips it; t3 is its guard), not a defect to track.

---

## Ф.0c — Класс #5: user type Vec SHADOW под prelude Vec[T] (~0.5d, ДО флипа)

Почему ДО флипа. После входа Vec в prelude (Ф.0-final) user-код с собственным type Vec { x, y }
(non-generic) коллизит с prelude Vec[T] (класс #5, plan123_3_1 E7310). W_PRELUDE_SHADOW skip
(emit_c.rs:2246-2315) убирает merged DUPLICATE по name+span для Type/Fn/Const — но prelude-Vec
МЕТОДЫ (push/get) приходят отдельно через generic_type_methods Vec и НЕ скипаются: выживают и
arity-check против user-Vec = E7310. Семантический shadow на уровне МЕТОДОВ, не типов.
Воспроизводится ДО флипа через явный import std.collections.vec_owned.{Vec} в юните с user-type
Vec (тот же merge-эффект, что prelude-auto-import даст позже) = фикс landed и проверен заранее.

Scope (без упрощений):
- Расширить W_PRELUDE_SHADOW methods-skip: когда user redeclares receiver-type Vec — skip merged
  prelude-методы этого receiver (по (receiver_type_name, span-not-user)), не только сам type-item.
  Реализация зеркалит should_skip_fn (emit_c.rs:2304), добавляя receiver-aware ветку: merged
  fn Vec[T] @m(...) скипается, если user объявил type Vec (любой arity) в entry-peer-files. User
  wins per D29.
- Surface W_PRELUDE_SHADOW на user-type Vec (lints.rs:1459 уже покрывает Type-shadow; убедиться,
  что methods-shadow тоже surface либо явно молчит по дизайну).
- Зафиксировать: user-type Vec (non-generic) + prelude Vec[T] = user выигрывает целиком; его Vec
  без push/etc = вызов v.push() на user-Vec = честный no method error (НЕ routed на prelude-Vec).

Spec / D: D29 (user-shadow wins) clarify/amend — shadow распространяется на МЕТОДЫ prelude-типа,
не только на type-item. Open-Question, если есть кейс partial-override (вероятно нет — full-shadow).

Фикстуры:
- neg plan138_2/t14_user_vec_shadow_neg.nv — EXPECT_COMPILE_WARNING (W_PRELUDE_SHADOW) + юнит
  компилируется: user type Vec { x int, y int } + explicit import vec_owned.{Vec}; user-Vec{x:1,y:2}
  работает, prelude-методы НЕ leak (no E7310 CC-FAIL).
- neg plan138_2/t15_user_vec_no_push_neg.nv — EXPECT_COMPILE_ERROR: .push(1) на user-type Vec =
  честный no method push (не silently routed на prelude-Vec).
- pos plan138_2/t16_user_vec_shadow_suppress_pos.nv — #allow(shadow) перед module подавляет
  W_PRELUDE_SHADOW; компилируется чисто.

Acceptance Ф.0c: t14 compiles с ожидаемым W_PRELUDE_SHADOW (НЕ CC-FAIL); t15 — ожидаемая
compile-error; t16 чисто; plan123_3_1 suite 0 FAIL (E7310 не возникает); grep в t14 .c: НЕТ
Nova_Vec_method_push для user-Vec receiver; 0 новых FAIL vs post-138.4 baseline.

Risk: MED — methods-skip может over-skip (user легитимно импортит prelude-Vec без shadow);
mitigation = skip ТОЛЬКО когда user-entry объявил type name с тем же receiver-именем.

Commit: fix(plan138.2 Ф.0c #5): skip prelude-Vec methods under user type-Vec shadow (D29 method-level); pre-flip

### Ф.0c — CLOSED (что реально сделано)

✅ ЗАКРЫТ 2026-06-11. Класс #5 (user-Vec SHADOW) de-risked ДО флипа. Реальный
шаблон де-shadow'а оказался ШИРЕ, чем «расширить W_PRELUDE_SHADOW methods-skip»:
shadow прорывался на ЧЕТЫРЁХ уровнях, не только в method-emit. Fix (`.rs` + rebuild):

ROOT CAUSE (полный, эмпирически): user `type Vec { x, y }` (arity 0) + explicit
`import vec_owned.{Vec}` (arity 1) дают MERGED-модуль с ОБОИМИ декларациями. User
выигрывает как ТИП (already: `should_skip_type` по name+span, emit_c.rs:2314), но
imported generic `Vec[T]` всё ещё (a) арити-чекался в type-checker'е → **E7310**
(`Vec` is not generic, but 1 arg provided — merged-Vec method type-refs `Vec[T]`
проверялись против user-Vec arity 0); (b) регистрировался в codegen `generic_types`/
`generic_type_templates` на ЧЕТЫРЁХ сайтах → record-литерал `Vec{x,y}` монорфизировался
через import-template (`Nova_Vec____nova_int`), и `infer_expr_c_type` RecordLit
(emit_c.rs:30497, `generic_types.contains("Vec")` → true) типизировал let-binding в
`void*` → field-read `v.x` давал NULL (runtime assert FAIL); (c) merged-Vec методы
эмитились (E7001 `Self outside receiver context` при method-receiver pre-population,
emit_c.rs:2478-loop).

Fix = единый shadow-set «user redeclared a generic type with DIFFERENT arity»,
вычисляется ОДИН раз (entry-peer `items_here` type-decl vs merged `module.items`
max-arity), guard'ит ВСЕ сайты:
- **types/mod.rs** (type-checker): новое поле `user_shadowed_generic_types:
  HashMap<name,user_arity>` в `TypeCheckCtx`; helper `is_shadowed_import_method(fd)`
  (true если `fd.receiver.type_name` ∈ set И `recv.generics.len() != user_arity`);
  skip merged-method в обоих check-loop'ах (arity-pass `check_fn` + assignability-pass
  `f1_check_fn`). Закрывает E7310.
- **emit_c.rs** (codegen): локальный `user_shadowed_generic_types: HashSet` (тот же
  merged-vs-entry arity-diff критерий); guard на 4 generic-registration сайтах
  (module.items 2060, peer_files 2094, 1d-prepop 2878 — пропуск `generic_types.insert`)
  + 1c method-receiver pre-population loop (skip imported-method by `recv.generics
  non-empty`) + `should_skip_fn` closure (skip imported-method emit). User-Vec
  record-literal теперь монорфизируется в user `Nova_Vec*`, field-access корректен.

W_PRELUDE_SHADOW DEFERRAL (verified, не дефект). Lint `lint_prelude_shadow`
(lints.rs:1459) fires ТОЛЬКО на prelude-visibility (`collect_prelude_visibility`).
Pre-flip `Vec` НЕ в prelude → explicit `import vec_owned.{Vec}` = ordinary import
collision, НЕ prelude-shadow → W_PRELUDE_SHADOW корректно НЕ срабатывает. Поэтому
t14 переформулирован как **positive clean-compile** (user-Vec wins, no E7310, no
method-leak), а не EXPECT_COMPILE_WARNING. Warning заработает после Ф.0-final (Vec
в prelude) — t16 (`#allow(shadow)`) пиннит suppress на тот момент. Это **ожидаемое**
pre-flip поведение, не упрощение: семантически warning о prelude-shadow не должен
fire'иться, пока Vec не в prelude.

D29 amended: shadow user-типа над imported/prelude generic распространяется на
**МЕТОДЫ** этого типа (full-shadow), не только на type-item — merged-методы
imported-generic'а с different-arity carrier'ом не leak'ают на user-redeclared
receiver. User wins целиком.

Acceptance MET: plan138_2 12/0 → **15/0** (t14 pos, t15 neg CC-FAIL `no member named
push`, t16 suppress pos); plan123_3_1 4/0 (gate, E7310 не возникает); 0 NEW FAIL
across plan138_1 10/0, plan138_3 2/0, plan90_1 20/0, plan131 27/1, plan118_1 11/0,
plan126_2 9/1, plan79 25/0 (E7310 arity tests intact — критично), plan72 14/2
(pre-existing, verified via stash-rebuild), map_literals 28/1, plan101_1/2, plan120
8/0, plan124_8 40/0, plan126 21/0, plan137 16/0, plan59 23/6 (pre-existing tuple-mangle).

Commits: см. SHA-таблицу в simplifications.md.

NO new [M-138.2-*] markers. Никакие markers не закрыты этой фазой (класс #5 был
OPEN, теперь de-risked); index-строка #5 в de-risk table обновлена на ✅ CLOSED.

---

## Ф.0-final — Универсальный флип []T == Vec[T] (Vec-in-prelude) (~0.5-1d)

Дерево de-risked (классы #1/#4/#5 погашены Ф.0a-c; #2/#3 — Phase-A). Флип приземляется одним
coherent commit без каскада 40+ регрессий. Это и есть оригинальный Ф.0 acceptance C1 — теперь
ДОСТИЖИМЫЙ.

Scope (Option A — prelude integration, единственный жизнеспособный per Ф.0 spike; Option B
codegen-seed ОТКЛОНЁН — seed только template даёт dangling Nova_Vec____T + undefined method-bodies
= CC-FAIL):
- std/prelude.nv: export import std.collections.vec_owned.{Vec, VecIter} сразу ПОСЛЕ Range
  re-export (~строка 114; порядок важен — vec_owned зависит от Range). Bump PRELUDE_VERSION.
- std/collections/vec_owned.nv: директива #prelude(core, runtime, collections, protocols) перед
  module + явный import std.collections.range.{Range} (Range в facade, не в prelude-submodule) —
  разрыв цикла prelude-vec_owned-prelude (зеркало range.nv #prelude).
- 4 gate-сайта contains_key Vec (emit_c.rs:2058/4995/26402/32030) — НЕ удалять: проходят
  универсально для prelude-юнитов, graceful-degrade для #no_prelude. (Удаление — задача Ф.3.)
- Rust-rebuild НЕ требуется (std .nv читаются с диска в runtime).

Spec / D: D239 Статус — снять partial gated on Vec-in-prelude, пометить ACTIVE (universal);
D144 — migration-path []T == Vec[T] закрыт (полный retire NovaArray в Ф.3).

Фикстуры:
- pos plan138_2/t3_universal_novec_unit.nv (re-create — был удалён при откате Ф.0) — Vec-free юнит
  (fn main(){ mut x []int = [1,2,3]; x.push(4); assert(x.len()==4) } БЕЗ import Vec) = .c содержит
  Nova_Vec____nova_int*, NovaArray = 0 (кроме intentional literal-bridge).
- pos plan138_2/t17_novec_typed_storage_pos.nv — []Option[int] / []Point / [][]int в Vec-free юните
  = typed storage (не int64-erasure).
- neg plan138_2/t18_no_prelude_graceful_neg.nv — #no_prelude юнит с []int = graceful degrade
  (gate OFF = NovaArray-путь), компилируется (не CC-FAIL).

Acceptance Ф.0-final (= оригинальный C1, теперь ДОСТИЖИМ): t3 .c: Nova_Vec____nova_int > 0,
NovaArray_nova_int = 0 (или только intentional Vec.from([]T) literal-bridge param); все targeted
suites (plan138_1/2/3, plan90_1, plan118_1, plan131, plan126_2, map_literals, plan123_3_1) строятся;
0 новых FAIL vs post-138.4 baseline; PRELUDE_VERSION bumped + prelude-version тест PASS.

Risk: HIGH исторически (40+ регрессий) = понижен до MED фазами Ф.0a-c. Остаточный: prelude-cycle /
latent codegen-баг (Range-прецедент — 4 бага). Mitigation = классы погашены; новый latent-баг
изолировать как мини-фазу Ф.0d, НЕ расширять флип.

Commit: feat(plan138.2 Ф.0-final): universal []T->Vec[T] via Vec-in-prelude (C1 met)

---

## Ф.2 (production-grade re-statement) — Остальные NovaArray-producers (~1d)

Дополняет ### Ф.2 выше. После Ф.0-final остаются три load-bearing NovaArray-producer:

Под-задача Ф.2.1 — parfor. Мигрировать parfor NovaArray usage на Vec ЛИБО задокументировать,
почему остаётся (если parfor-runtime требует фикс. C-layout). Решение = D-amend или followup
[M-138.2-parfor-vec].
- pos plan138_2/t19_parfor_vec_pos.nv — parfor над []int/Vec[int] даёт корректный результат.
- neg plan138_2/t20_parfor_vec_bounds_neg.nv — OOB / пустой диапазон = ожидаемое поведение.

Под-задача Ф.2.2 — closure-array []fn. Исследовать feasibility Vec[fn] mono (closures = void_p).
Решить: мигрировать на Vec[fn] (предпочтительно для полного retire) ЛИБО оставить документированным
исключением NovaArray_void_p* (emit_c.rs:4972-4979) = followup [M-138.2-closure-array-vec].
- pos plan138_2/t21_closure_array_pos.nv — []fn(int)->int build/index/call.
- neg plan138_2/t22_closure_array_empty_neg.nv — пустой/OOB closure-array.

Под-задача Ф.2.3 — Vec в NovaArray bulk-op bridge. Удалить bridge (Vec-методы больше не маршрутят
через nova_array_*); [M-138-vec-bulk-parity] уже дал RawMem-нативные copy_from/copy_within/fill/
append_zero/compare (DONE f0bec0e6717).
- pos plan138_2/t23_vec_bulk_no_bridge_pos.nv — все bulk-ops дают тот же результат, что старый
  nova_array_* (паритет).

Spec / D: D141 — закрыть переход extend-family на Vec; D-amend для parfor/closure-array если
семантика наблюдаемо меняется.

Acceptance Ф.2: grep НЕ находит []T в NovaArray lowering-путей, кроме явно документированных
исключений (closure-array если отложен); под-задачные suites PASS; 0 новых FAIL.

Commit (несколько, по под-задаче):
- refactor(plan138.2 Ф.2.1): migrate parfor off NovaArray
- refactor(plan138.2 Ф.2.2): Vec[fn] closure-array OR document NovaArray_void_p exception
- refactor(plan138.2 Ф.2.3): remove Vec->NovaArray bulk-op bridge

---

## Ф.3 (production-grade re-statement) — Удаление NOVA_ARRAY_DECL/IMPL + special-cases (~1d)

Дополняет ### Ф.3 выше. Prerequisite: [M-138-vec-bulk-parity] закрыт (DONE).

Scope (без упрощений):
- Удалить 4 gate-сайта contains_key Vec (emit_c.rs:2058/4995/26402/32030) — теперь unconditional
  Vec-lowering. Если #no_prelude без Vec остаётся легитимным кейсом — сохранить graceful-fallback
  (решить + задокументировать; см. риск RE).
- Удалить макросы NOVA_ARRAY_DECL/NOVA_ARRAY_IMPL + per-primitive инстанциации из array.h
  (lines 48-342). Сохранить nova_alloc/realloc; nova_str-helpers (nova_str_to_bytes/nova_str_split/
  nova_str_to_chars/nova_str_as_bytes) только если ещё нужны (закрываются Plan 139 Ф.2 — если 139
  уже landed, удалить и их; см. риск RG).
- Убрать emit_c.rs NovaArray special-cases: ==, slice (nova_array_slice), field-access, static,
  instance-block, for-in Case 2, D141 bulk-dispatch (emit_c.rs:21399-21419), closure-array (если
  Ф.2.2 мигрировал).

Spec / D: D239 Статус = ACTIVE (полный, NovaArray retired); D144 = закрыт; D27 — отметить
NovaArray-backing удалён.

Фикстуры:
- pos plan138_2/t24_full_retire_smoke_pos.nv — широкий smoke: push/pop/insert(bulk)/append/slice/
  for-in/==/clone на []int/[]u8/[]str/[]Record — всё через Vec.
- neg plan138_2/t25_no_novaarray_in_emit_neg (build-time grep-gate) — grep
  NOVA_ARRAY_DECL/NOVA_ARRAY_IMPL в array.h = пусто; grep NovaArray_ в emit_c.rs = только
  документированные исключения.

Acceptance Ф.3: grep NOVA_ARRAY_DECL/NOVA_ARRAY_IMPL в array.h = пусто (C5); grep NovaArray_ в
emit_c.rs = только документированные исключения (C6); build зелёный; targeted suites 0 новых FAIL.

Commit: refactor(plan138.2 Ф.3): remove NOVA_ARRAY_DECL/IMPL + gate sites + special-cases

---

## Ф.4 (production-grade re-statement) — Полная регрессия-to-green (~1-2d)

Дополняет ### Ф.4 выше. Широкая регрессия по всему array-использующему корпусу, per element-class.
Масштаб большой ([]T везде) — при согласии пользователя запустить full nova test (RELEASE binary);
иначе broad-curated + явный лог покрытия (no silent caps — каждый suite со счётом pass/fail).

Fix-order (per element-class, каждый = свой commit): primitives (int/u8/char/bool/f64/f32/
sized-int) = str = records = nested ([][]T) = Option/sum-payload = closures (если Ф.2.2 мигрировал).

Acceptance Ф.4 (= C7): 0 новых FAIL vs 138.1-landing baseline; 3 typed-storage FAIL
(nested/record/tuple) остаются fixed; pre-existing FAIL (vec_debug_pos interp gap, plan126_2
p5_printable) остаются как есть (не регресс).

Commit (несколько, per element-class): fix(plan138.2 Ф.4 class): regression-to-green

---

## Ф.5 (production-grade re-statement) — Docs + close (~0.5d)

Дополняет ### Ф.5 выше. Финальный docs-sweep + close:
- D239 полностью закрыт (NovaArray retired); D144 закрыт; D27 amended.
- D26 amend (str: Plan 139 координация — to_bytes/to_chars/split Nova-body в Vec; as_ptr если новый
  ptr-примитив).
- D216 amend если добавлен новый str ptr-примитив.
- docs/simplifications.md (полный per-phase SHA-журнал + Регрессии); docs/project-creation.txt
  (дневник всего капстоуна); docs/plans/backlog-followups.md (закрыть #1/#4/#5 markers; spawn новые);
  nova-private/discussion-log.md (narrative); README; plan-status; memory.

Acceptance Ф.5 (= C8): D239/D144 закрыты; D26/D27 amend задокументирован; все mandate-файлы
обновлены; план помечен CLOSED.

Commit: docs(plan138.2 Ф.5 D239): NovaArray fully retired; close capstone

---

## Risk register (production-grade, дополняет верхний)

| # | Риск | Sev | Mitigation |
|---|---|---|---|
| RA | Класс #1 single-vs-bulk insert overload не дизамбигуируется для Vec[Vec[U]] | MED | precise-T-wins rule; fallback rename bulk в @splice/@insert_all (API-change в simplifications) |
| RB | Класс #4 map-coercion codegen сложен (record-punning/spread/from_fields) | MED | 50-фикстурный map_literals suite как полный gate ДО флипа; изолировать в hashmap CU |
| RC | Класс #5 methods-skip over-skip легитимный import без shadow | MED | skip только при user-entry type name с тем же receiver-именем |
| RD | Ф.0-final: prelude-cycle / latent codegen-баг (Range-прецедент 4 бага) | MED (был HIGH) | классы погашены Ф.0a-c; новый latent-баг = мини-фаза Ф.0d, НЕ расширять флип |
| RE | Ф.3 #no_prelude без Vec теряет graceful-fallback после удаления gate | MED | решить судьбу #no_prelude-кейса ДО удаления gate; сохранить fallback или явно ретирить #no_prelude-array |
| RF | Ф.4 масштаб финальной регрессии ([]T везде) | MED | per-element-class fix order; явный лог покрытия (no silent caps) |
| RG | Plan 139 координация (str-слой) — порядок 138.2 Ф.0 = 139 Ф.0-Ф.2 = 138.2 Ф.2-Ф.4 | MED | не удалять nova_str_* array-helpers в Ф.3, пока 139 Ф.2 не landed |

---

## Acceptance section (production-grade, машинно-проверяемая)

> Оригинальные C1-C8 (см. ## Acceptance criteria выше) остаются каноном. Ниже — per-phase
> machine-checkable инварианты этого разреза (grep + suite-счёт), которые в сумме дают C1-C8.

| Phase | Gate (grep-инвариант) | Suite (pass/fail, no silent caps) |
|---|---|---|
| Ф.0a ✅ | vec_owned.nv: single @insert(i,T) + bulk @splice(i,Vec[T]) (fallback-rename, overload коллапсит в mono — [M-138.2-bulk-insert-overload]); .c distinct _method_insert + _method_splice (per-T mono'd) | plan138_2 t8/t9/t10/t11 PASS (6/0→10/0); plan90_1 20/0; 0 NEW FAIL |
| Ф.0b ✅ | hashmap CU .c: `_buckets = Nova_Vec____Nova_Slot____*` (typed, NOT int64-erased NovaArray); `new_buckets` body builds typed Vec via explicit `Vec[Slot].with_capacity`+`.push` | map_literals 28/1 (= baseline, pre-existing const_map); plan138_2 t12/t13 PASS (12/0); 0 NEW FAIL across plan138_1/3, plan90_1, plan131, plan126_2, plan79, plan139, plan55, plan91_13, plan91_fe1, map_literals |
| Ф.0c ✅ | t14 .c: user-Vec literal → `Nova_Vec*` (not `Nova_Vec____nova_int`); no `Nova_Vec_method_push` for user recv; user wins | plan123_3_1 4/0 (gate, no E7310); plan138_2 12/0→15/0 (t14 clean-compile pos [W_PRELUDE_SHADOW deferred to Ф.0-final — Vec not yet in prelude], t15 CC-FAIL `no member push`, t16 `#allow(shadow)` clean); plan79 25/0 (E7310 intact); 0 NEW FAIL |
| Ф.0-final | t3 .c: Nova_Vec____nova_int > 0, NovaArray_nova_int = 0 | все targeted suites build; 0 новых FAIL; PRELUDE_VERSION bumped |
| Ф.2 | grep нет []T в NovaArray путей кроме документированных | t19-t23 PASS |
| Ф.3 | array.h grep NOVA_ARRAY_DECL/NOVA_ARRAY_IMPL = пусто (C5); emit_c.rs grep NovaArray_ = только исключения (C6) | t24 smoke PASS; build зелёный |
| Ф.4 | — | 0 новых FAIL vs 138.1-landing (C7); 3 typed-storage остаются fixed |
| Ф.5 | D239/D144 closed; D26/D27 amend | mandate-файлы обновлены (C8); план CLOSED |

Definition of Done (весь капстоун):
1. C1-C8 (верхняя секция) удовлетворены.
2. grep NOVA_ARRAY_DECL/NOVA_ARRAY_IMPL в array.h = пусто.
3. grep NovaArray_ в emit_c.rs = 0 (или только явно документированные исключения с followup).
4. Vec-free юнит лоуэрит []int в Nova_Vec____nova_int*.
5. 0 новых FAIL vs 138.1-landing baseline по full / broad-curated corpus.
6. Все mandate-файлы (project-creation.txt + simplifications.md + backlog-followups.md +
   nova-private/discussion-log.md) обновлены; per-big-task коммиты.
7. D239 + D144 закрыты; D141/D27/D26/D29 amended.

> Порядок исполнения капстоуна: Ф.0a в Ф.0b в Ф.0c в Ф.0-final в (Plan 139 Ф.0-Ф.2, см. RG) в
> Ф.2 в Ф.3 в Ф.4 в Ф.5. Каждая фаза — один или несколько per-commit-green коммитов с полным
> мандатом docs/spec/fixtures/acceptance.
