<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 138.2 — Universal `Vec` + `str`-in-Nova + NovaArray retirement

> **Создан:** 2026-06-10.  **Статус:** 📋 PLANNED (capstone Ф.0+Ф.2-Ф.4 ещё впереди; Phase-A + design-cleanup batch N1-N4 приземлены).
> **Sequencing (решено 2026-06-10):** **139-first** — Ф.0 → Plan 139 Ф.0-Ф.2 → Ф.2-Ф.4.
> Ф.1 (string-слой) **subsumed by Plan 139** (str→value-record).
> **Закрывает:** `[M-138.1-vec-in-prelude]` — блокер Ф.5 из Plan 138.1.
> **Эстимат:** ~4-6 dev-day (multi-day, как и предсказано в 138.1 Ф.5 blocker-анализе).
> **Model:** Opus + Thinking ON (рискованный codegen + prelude integration).
> **Worktree/branch:** `nova-p138` / `plan-138.1` (или новый `plan-138.2` off `plan-138.1`).
> **Зависит от:** Plan 138.1 (partial-landing: флип работает где Vec присутствует + typed-storage gap закрыт).

---

## Зачем (recap блокера)

Plan 138.1 приземлился **частично**: `[]T`→`Vec[T]` флип в `type_ref_to_c`
**гейтится** через `generic_type_templates.contains_key("Vec")` —
[emit_c.rs:2055](../../compiler-codegen/src/codegen/emit_c.rs) +
[:4938](../../compiler-codegen/src/codegen/emit_c.rs). `Vec` **не в prelude**,
поэтому юниты, не упоминающие Vec, всё ещё лоуэрят `[]T` в `NovaArray`. Полное
удаление `NOVA_ARRAY_DECL/IMPL` (138.1 Ф.5) заблокировано пятью load-bearing
зависимостями:

1. **Не-универсальный флип** (gate выше) — primitive/Vec-free юниты → NovaArray.
2. **string-слой** `[]T`-producers (`external fn`, C-backed → NovaArray).
3. **parfor** — NovaArray usage.
4. **closure-array** `[]fn` → `NovaArray_void_p*` ([emit_c.rs:4918](../../compiler-codegen/src/codegen/emit_c.rs) explicit exclusion).
5. **Vec→NovaArray bulk-op bridge** — Vec-методы частично маршрутят через `nova_array_*`.

Цель 138.2: снять все пять, завершить retire NovaArray, закрыть D239/D144 **полностью**.

---

## Факты из разведки (verified anchors)

| Что | Где | Деталь |
|---|---|---|
| Gate флипа | emit_c.rs:2055, 4938 | `if self.generic_type_templates.contains_key("Vec")` |
| Closure-array exclusion | emit_c.rs:4918 | `[]fn(...)` → `NovaArray_void_p*` (намеренно) |
| vec_owned imports | std/collections/vec_owned.nv:31,40 | только `raw_mem.{RawMem}` + локальный `external fn panic` — без `#prelude` |
| Vec в prelude | std/prelude.nv | **отсутствует** (grep пуст) |
| Range precedent | std/prelude.nv:114 | `export import std.collections.range.{Range, RangeIter}` + range.nv `#prelude(core,runtime,errors)` (Plan 62.D bis-1 — вскрыл 4 latent codegen-бага) |
| str `[]T`-producers | std/runtime/string.nv | `@to_bytes→[]u8`(77), `@as_bytes→ro []u8`(80), `@to_chars→[]char`(94), `@split→ro []str`(97), `from_bytes_{lossy,unchecked,unchecked_steal}`(84-91) |
| str byte-примитивы | std/runtime/string.nv | `@byte_at(i)→u8`(25), `@len`(19), `@char_at(i)→Option[char]`(49), `@char_len`(22); str-slice `s[a..b]` (Plan 138 Ф.3) |

---

## Фазы

### Ф.0 — Универсальная доступность `Vec` (ядро `[M-138.1-vec-in-prelude]`) (~1-2d)

> **🔴 ИСХОД (2026-06-10): BLOCKED — попытка приземления, откат по HIGH-RISK протоколу.**
> Механика **доказана и работает** для узкого acceptance-критерия, но универсальный
> флип **архитектурно неотделим** от Ф.2-Ф.4 (NovaArray-API reconciliation): включение
> флипа в изоляции каскадит в **40+ регрессий** по корпусу. Дерево оставлено GREEN
> (F0 откатан, 0 tracked-file изменений). **C1 не достигнут на landed-ветке** (флип OFF).
>
> **Доказательство (флип OFF, текущее состояние ветки):** Vec-free юнит
> (`mut x []int = [1,2,3]`) компилируется в `NovaArray_nova_int*`
> (`Nova_Vec____nova_int` = 0 вхождений, `NovaArray` = 112). Это **ожидаемое**
> reverted-состояние.
>
> **Доказательство (флип ON, до отката — acceptance MET):** тот же Vec-free юнит
> лоуэрился в `Nova_Vec____nova_int*` (32 вхождения), residual NovaArray = 2
> (намеренный `Vec.from([]T)` literal-bridge param). 4 gate-сайта проходили
> универсально для prelude-юнитов и корректно degrade-или для `#no_prelude`.
>
> **Механизм (Option A — ЕДИНСТВЕННЫЙ жизнеспособный):**
> - `std/prelude.nv`: `export import std.collections.vec_owned.{Vec, VecIter}`
>   сразу ПОСЛЕ Range re-export (строка 114; порядок важен — vec_owned зависит от Range).
> - `std/collections/vec_owned.nv`: директива `#prelude(core, runtime, collections,
>   protocols)` перед `module` + явный `import std.collections.range.{Range}`
>   (Range живёт только в facade, не в prelude-submodule).
> - 4 gate-сайта `contains_key("Vec")` (emit_c.rs:2055/4938/26100/31637) **НЕ удалять** —
>   они проходят универсально для prelude-юнитов и graceful-degrade для `#no_prelude`.
> - Rust-rebuild НЕ нужен (std .nv читаются с диска в runtime, не include_str).
>
> **Option B (codegen-seed) — ОТКЛОНЁН после spike.** Seeding только template в
> `generic_type_templates` НЕ достаточно: 4 gate-сайта зависят от полного STRUCT
> DEFINITION + всех method-bodies (`generic_type_methods["Vec"]`), которые приходят
> только через import-merge pipeline. Seed только template → dangling
> `Nova_Vec____<T>` + undefined `Nova_Vec____<T>_method_push` → CC-FAIL. Spec-фрейминг
> «B легче» был основан на ложной предпосылке, что template один достаточен.
>
> **5 классов регрессий при флипе ON (требуют Ф.2-Ф.4, НЕ codegen-фикса в Ф.0):**
> 1. **insert/append API divergence** (plan90_1 20→10): []T extend-family (D141)
>    `dst.insert(i, src_ARRAY)` = bulk-insert, но Vec `insert(i, v T)` = single-elem.
>    Одно имя, несовместимая семантика → нужен либо bulk-overload на Vec, либо переписка тестов.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
> 2. ✅ **[]u8 literal int64-stride** (plan118_1 11→6, buffers) = `[M-138.2-vec-u8-literal-stride]`
>    — **PRE-FIXED Phase-A (commit 5c41f72a6e9)**: literal sized-int element coercion в обоих
>    путях (emit `try_emit_typed_vec_literal` ~26305 + infer `infer_expr_c_type::ArrayLit` ~31692).
> 3. ✅ **Vec-of-record write-index `_p` typedef bug** (plan138_1 10→9): `v[i]=Point{...}`
>    эмитил `(Nova_Point_p)(tmp)` — typedef не существует, должно быть `Nova_Point*`.
>    **PRE-FIXED Phase-A (commit c7a8f365aa3)**: write-path теперь использует registry-based
>    element-type recovery (как read-path), кастит в реальный `Nova_X*`.
> 4. **map_literals каскад** (28→9): map-литералы строят []T-backed bucket-arrays.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
> 5. **SHADOW collision** (plan123_3_1 E7310): user `type Vec { x, y }` (non-generic)
>    коллизит с prelude `Vec[T]`; prelude-методы выживают и arity-check'аются против
>    user-Vec. Расширить W_PRELUDE_SHADOW methods-skip на user-redeclared receiver type.
>    **(всё ещё открыт — НЕ pre-fixed в Phase-A)**
>
> **🟢 Phase-A de-risk (2026-06-10):** классы #2 и #3 (узкие codegen-баги) **pre-fixed**
> отдельными атомарными коммитами на `plan-138.1` ДО флипа — сужает поверхность будущего
> атомарного capstone (Ф.0+Ф.2+Ф.3+Ф.4) до 3 классов: #1 (insert/append API), #4 (map_literals),
> #5 (SHADOW). Дополнительно Phase-A добавил fluent-chain `-> @` на Vec-мутаторы (commit d22c96f7423,
> `[M-138.2-vec-fluent-push]`). Два rename-followups ([M-138-getmut-rename], [M-138-mutindex-rename])
> исследованы и **BLOCKED** на codegen-уровне (детали в Followups). См. `docs/simplifications.md`
> «Plan 138.2 Phase-A».
>
> **РЕКОМЕНДАЦИЯ:** приземлять Ф.0 + Ф.2 + Ф.3 + Ф.4 как ОДИН атомарный multi-day
> unit; сначала фиксить `[M-138.2-vec-u8-literal-stride]` + Vec-of-record `_p` typedef
> bug, потом флип. Re-create `nova_tests/plan138_2/t3_universal_novec_unit.nv` когда
> флип реально приземлится.

Цель: `[]T` лоуэрится в `Vec[T]` в **КАЖДОМ** юните, не только где Vec упомянут.
Снять gate `contains_key("Vec")` (emit_c.rs:2055 + 4938).

**Две механики (выбрать после spike):**

- **Option A — Prelude integration.** Добавить в `std/prelude.nv`:
  `export import std.collections.vec_owned.{Vec, VecIter}`. Дать `vec_owned.nv`
  директиву `#prelude(core, runtime, errors)` для разрыва цикла
  prelude→vec_owned→prelude (зеркало `range.nv`). **Риск:** как с Range
  (Plan 62.D bis-1) может вскрыть latent codegen-баги (match-inference,
  closure-capture, W_PRELUDE_SHADOW). **Бонус:** `Vec` становится видимым именем
  без import (DX-плюс).
- **Option B — Codegen-seed (рекомендуется первым).** Безусловно регистрировать
  Vec[T] template на codegen-init (грузить шаблон из vec_owned в каждый юнит),
  чтобы gate проходил универсально **без** prelude-auto-import семантики. Легче по
  cycle-риску; `Vec` как имя не авто-видим (юзеры пишут `[]T` — что и нужно для
  сахара). Точечно решает реальную потребность (lowering `[]T`), не трогая
  prelude-видимость.

**Рекомендация:** spike Option B (легче, целит ровно в потребность); fallback на A
если codegen-seed не дотягивается до шаблона чисто. В обоих случаях — убрать gate.

**Acceptance Ф.0:** Vec-free юнит (`fn main(){ mut x []int = [1]; x.push(2) }` без
`import ...Vec`) генерит `Nova_Vec____nova_int*`, не `NovaArray`. Targeted suites
строятся.

**Commit:** `feat(plan138.2 Ф.0): universal []T->Vec[T] (remove contains_key gate)`

---

### Ф.1 — String-слой → **SUBSUMED by Plan 139** (решение 2026-06-10: 139-first)

> **Развилка решена (139-first, без двойной работы).** Вместо lightweight-переписки
> `[]T`-producers с сохранением примитива `nova_str`, string-слой закрывается
> **полностью** через **Plan 139** (`str` → Nova value-record `{ ptr *ro u8, len int }`).
> Plan 139 Ф.2 переписывает `@to_bytes/@to_chars/@split/@as_bytes/from_bytes_*` на
> Nova → `Vec` как часть превращения str в value-тип. `@as_bytes` zero-copy при этом
> **не требует** нового `as_ptr`-примитива — поле `@ptr` уже доступно методам str.
>
> Поэтому эта фаза в 138.2 **не выполняется** — см. `docs/plans/139-str-as-nova-value-type.md`.
> Канонический порядок: **138.2 Ф.0 → 139 Ф.0-Ф.2 → 138.2 Ф.2-Ф.4**.

---

### Ф.2 — Остальные NovaArray-producers (~1d)

- **parfor** — мигрировать NovaArray usage на Vec (или задокументировать почему остаётся).
- **closure-array `[]fn`** — решить: мигрировать на `Vec[fn]` (предпочтительно для
  полного retire) ЛИБО оставить документированным исключением `NovaArray_void_p*`
  → followup `[M-138.2-closure-array-vec]`. Исследовать feasibility `Vec[fn]` mono
  (closures = void_p).
- **Vec→NovaArray bulk-op bridge** — удалить (Vec-методы больше не через `nova_array_*`).

**Acceptance Ф.2:** grep не находит `[]T`→NovaArray lowering-путей, кроме
документированных исключений.

**Commit:** `refactor(plan138.2 Ф.2): migrate parfor/closure-array/bulk-bridge off NovaArray`

---

### Ф.3 — Удаление `NOVA_ARRAY_DECL/IMPL` + special-cases (настоящая 138.1 Ф.5) (~1d)

**Prerequisite:** `[M-138-vec-bulk-parity]` закрыт — Vec имеет copy_from/copy_within/fill/
append_zero/compare через RawMem (иначе `[]T` extend-family API регрессирует).

Теперь безопасно: удалить макросы из `array.h` (сохранить `nova_alloc`/realloc;
`nova_str`-helpers только если ещё нужны), убрать emit_c.rs NovaArray special-cases
(==, slice, field, static, instance-block, for-in Case 2).

**Acceptance Ф.3:** `grep NOVA_ARRAY_DECL/IMPL` пуст; build зелёный.

**Commit:** `refactor(plan138.2 Ф.3): remove NOVA_ARRAY_DECL/IMPL + special-cases`

---

### Ф.4 — Полная регрессия-to-green (~1-2d)

Широкая регрессия по всему array-использующему корпусу. Iterate per element-class.
Масштаб большой ([]T везде) — при согласии запустить full `nova test`; иначе broad-
curated + явный лог покрытия (no silent caps).

**Acceptance Ф.4:** 0 новых FAIL vs 138.1-landing baseline; 3 typed-storage FAIL'а
(nested/record/tuple) остаются fixed.

**Commit (несколько):** `fix(plan138.2 Ф.4): regression-to-green`

---

### Ф.5 — Docs + close (~0.5d)

- D239 **полностью** закрыт (NovaArray retired), D144 закрыт.
- D26 amend (str: `@as_ptr` примитив + `to_bytes`/`to_chars`/`split` теперь Nova).
- D216 amend если добавлен новый ptr-примитив.
- simplifications.md, project-creation.txt, nova-private/discussion-log.md, README, plan-status, memory.

**Commit:** `docs(plan138.2 Ф.5 D239): NovaArray fully retired; str in Nova; close`

---

## Risk register

| # | Риск | Sev | Mitigation |
|---|---|---|---|
| R1 | Vec-in-prelude вскрывает latent codegen-баги (Range-прецедент: 4 бага) | 🔴 HIGH | Ф.0 spike + Option B fallback (codegen-seed без prelude-семантики) |
| R2 | string-слой фундаментален — поломка `to_bytes`/`split`/`as_bytes` каскадит в encoding/text/hashmap | 🔴 HIGH | Ф.1 изолированно + string/encoding тесты ДО продолжения |
| R3 | `@as_bytes` zero-copy: GC interior-pointer (str-буфер жив пока view достижим) | 🟡 MED | conservative-scan покрывает; тест на view-after-str-drop |
| R4 | `Vec[fn]` closure-array mono может не собраться чисто | 🟡 MED | fallback: документированное исключение + `[M-138.2-closure-array-vec]` |
| R5 | масштаб финальной регрессии ([]T везде) | 🟡 MED | per-element-class fix order; лог покрытия |

---

## Acceptance criteria

- **C1** — 🔴 **НЕ ДОСТИГНУТ (BLOCKED 2026-06-10).** Vec-free юнит должен лоуэрить
  `[]int` → `Nova_Vec____nova_int*` (gate снят). Механика доказана (флип ON давал
  `Nova_Vec____nova_int*` × 32), но универсальный флип неотделим от Ф.2-Ф.4 (40+
  регрессий) — F0 откатан, флип на landed-ветке OFF (Vec-free → `NovaArray_nova_int*`).
  **B6/Ф.3 (retire NOVA_ARRAY_DECL/IMPL) остаётся ЗАБЛОКИРОВАННЫМ** — Ф.0 не приземлился.
- **C2** — `str.to_bytes/to_chars/split` — Nova-body, возвращают `Vec[u8]`/`Vec[char]`/`Vec[str]`.
- **C3** — `str.as_bytes` — zero-copy `Vec[u8]` view через `str.as_ptr()` примитив; GC-safe.
- **C4** — `from_bytes_*` принимают `Vec[u8]` (ABI layout-идентичен), тесты round-trip зелёные.
- **C5** — `grep NOVA_ARRAY_DECL\|NOVA_ARRAY_IMPL` в array.h — пусто.
- **C6** — `grep NovaArray_` в emit_c.rs — только документированные исключения (если closure-array отложен).
- **C7** — 0 новых FAIL vs 138.1-landing; 3 typed-storage baseline FAIL'а остаются fixed.
- **C8** — D239/D144 закрыты; D26 amend (str primitives) задокументирован.

---

## Followups

- `[M-138.2-closure-array-vec]` — `[]fn` → `Vec[fn]` если в Ф.2 отложено.
- `[M-138.2-codepoint-cursor]` — O(n) codepoint-iterator примитив для `to_chars` (вместо O(n²) `@char_at`).
- `[M-138.2-fixed-array]` — `[N]T` FixedArray (наследие 138.1, вне scope).
- ✅ `[M-138-rawmem-bulk-ops]` — DONE (commit f0bec0e6717): `@realloc_to`/`@append`/`@clone` →
  `RawMem.copy_nonoverlapping`; `@insert`/`@remove` shift → `RawMem.copy` (memmove). GC-safe casts.
- ✅ `[M-138-vec-bulk-parity]` — DONE (commit f0bec0e6717): `@copy_from`/`@copy_within` (RawMem.copy),
  `@fill`/`@append_zero` (RawMem.fill), `@compare` (RawMem.compare, normalized -1/0/+1) добавлены на Vec.
  plan138_2 t1 2/2. **PREREQUISITE для Ф.3 закрыт.**
- ✅ `[M-138-rawmem-dedup-fill]` — DONE (commit 43fb6a736ea): `write_bytes` удалён, `fill` остался.
- ✅ `[M-138-mutindex-rename]` — **CLOSED (Plan 138.4 Ф.2 G-A, 2026-06-11, commit `14e6da56175`).**
  Vec `mut @index_set(i, val)` переименован в третий `mut @index(i int, val T)` overload (3-way:
  read scalar `@index(i)->T` / read range `@index(r Range)->Self` / write `mut @index(i,val)`),
  все три эмитят DISTINCT C-символы (`Nova_Vec_method_index` / `_index__nova_int_void_p` /
  `_index__Nova_Range_p`). ROOT CAUSE (уточнён эмпирически): collision был в ERASED-template пути
  (`Nova_Vec_method_*` via `emit_generic_method_erased`→`mangle_fn`), НЕ в mono-пути как
  предполагалось — mono-путь (`register_vec_mono_method`) вызывается только для non-overloaded
  методов, а `v[i]`/`v[i]=val` codegen-инлайнятся и никогда не диспатчат по имени. `mangle_fn`
  `want_params` (via `type_ref_to_c`, bare T → `nova_int`) расходился с registry pre-pass
  `erased_type_ref_c` (bare T → `void*`). FIX: helper `mangle_want_params(recv, f)` реплицирует
  erasure точно; single-overload методы сохраняют base C-name → zero mass regression. D240 prose
  уже был исправлен (94d39606d6a); теперь Vec удовлетворяет MutIndex и через именованный overload.
- ✅ `[M-138.2-vec-u8-literal-stride]` — **DONE (Phase-A, commit 5c41f72a6e9).** `[]u8 = [1,2,3]` литерал
  хранил int64-страйд вместо 1-байтового u8 (typed-storage gap для sized-int литералов, 138.1 Ф.2). Root:
  element C type брался из `infer_expr_c_type` первого item'а, int-литерал коллапсит в erased `nova_int`.
  Fix в двух симметричных сайтах: emit (`try_emit_typed_vec_literal` ~26305) + infer
  (`infer_expr_c_type::ArrayLit` ~31692) — когда первый item collapse'ит в `nova_int` дефолт, но
  присутствует sized-int element-hint (из `[]u8`/`[]iNN` annotation), hint выигрывает → литерал
  монорфизирует `Vec[u8]`/`Vec[i32]` с корректным страйдом. Fixture t5_u8_literal_stride.nv (4 теста).
  **Класс регрессий #2 pre-fixed.** Discovered (отдельный, НЕ тронут): `Vec[i32].new()` эмитит undeclared
  `Nova_Vec____Nova_i32_p` (explicit sized-int Vec instantiation boxes i32 как `Nova_i32*` вместо `int32_t`)
  → candidate followup `[M-138.2-vec-sized-int-explicit-mangle]`.
- ✅ `[M-138.2-vec-fluent-push]` — **DONE (Phase-A, commit d22c96f7423).** Все 11 chainable Vec[T]
  мутаторов в `std/collections/vec_owned.nv` переведены с `-> ()` на `-> @` (return self), зеркало
  StringBuilder.append D181: @push, @extend, @insert, @append, @reverse, @clear, @truncate, @copy_from,
  @copy_within, @fill, @append_zero. std .nv читается с диска — no rebuild. Statement-form callsites
  (`v.push(x)`) работают (return discard). Fixture t6_fluent_push.nv (7 тестов). Три PRE-EXISTING codegen-gap
  при fluent chaining вскрыты (НЕ этой фазы, candidate followups): (A) `[M-138.2-vec-fluent-chain-int-mixed]`
  — Vec[int] (erased nova_int) chains мис-диспатчат на erased `Nova_Vec_method_push`; (B) mixed-method chains
  (`.reverse().truncate().fill()`) резолвят post-first-call receiver по голому имени метода через ВСЕ типы
  (picked Range::reverse / StringBuilder::truncate); (C) `[M-138.2-vec-extend-generic-fluent-return]` — @extend
  (method-level generic `[S Iter[T]]`) chained `-> @` мис-типизирует возврат в `void*`. Все три — отсутствие
  full-signature mangling + pinning chained-receiver type к concrete Vec[T] mono (та же область gate-сайтов).
- 🟡 `[M-138-getmut-rename]` — **INFRA-DONE + WON'T-FIX-FOOTGUN (Plan 138.4 Ф.3 G-B, 2026-06-11,
  commit `7c82ff2061c`).** Infra-gap (1) `[M-138-getmut-rename-infer-gap]` **ЗАКРЫТ**: `infer_expr_c_type`
  теперь tiebreak'ает RETURN-тип method-call по receiver mutability (widened `recv_and_method` tuple до
  4-эл с call-site recv-mut via `is_obj_mutable`; multi-overload return-pool предпочитает overload с
  `recv_mutable == recv_mut` BEFORE `pool.first()`, guarded `pool.len()>1`) — зеркало `mangle_fn`
  recv-mut tiebreak'а (commit `14e6da56175` со стороны emit). Differing-return recv-mut overloads теперь
  корректно инферятся (проверено probe'ом: `Box @peek()->int` ro vs `mut @peek()->str`).
  **ОДНАКО** сам rename НЕ выполнен: footgun (2) `[M-138-getmut-dispatch-mut-recv-footgun]` не снят —
  call-mangle tiebreak'ает recv_mutable к CALLER mutability, поэтому на `mut` receiver plain read
  `v.get(1)` ВСЕГДА диспатчил бы mut/pointer-overload `Option[*mut T]`, ломая read-by-value (plan131
  `assert(v.get(1)==Some(99))`). DECISION: `@get_mut` ОСТАЁТСЯ distinct (Rust get/get_mut precedent —
  no footgun); rename re-attempt ТОЛЬКО если mut-recv read-intent дизамбигуируется отдельно. Infra-fix
  ценен для legit differing-return recv-mut overloads вообще.

> **Phase-A итог (de-risk, 2026-06-10, branch `plan-138.1`):** 3 marker'а DONE
> ([M-138.2-vec-u8-literal-stride], Vec-of-record `_p` typedef = класс #3, [M-138.2-vec-fluent-push]),
> 2 изначально BLOCKED+REVERTED ([M-138-getmut-rename], [M-138-mutindex-rename] — оба упёрлись в
> отсутствие full-signature overload mangling для generic-type Vec методов). **UPDATE (2026-06-11,
> Plan 138.4):** оба разблокированы codegen-фиксом — `[M-138-mutindex-rename]` ✅ CLOSED (G-A
> `14e6da56175`); `[M-138-getmut-rename]` 🟡 INFRA-DONE (G-B `7c82ff2061c`), сам rename = won't-fix-footgun
> (`@get_mut` distinct). Регрессионные классы #2+#3 из Ф.0 §«5 классов» pre-fixed → атомарный capstone
> сужен до #1/#4/#5.

---

### Design-cleanup batch (N1–N4, 2026-06-10, branch `plan-138.1`)

Пакет осознанных std-уровневых cleanup'ов поверх Phase-A (все per-commit-green,
std `.nv` читается с диска — rebuild не требовался). Каждый — один marker = один коммит:

- ✅ `[M-138.2-next-pow2-branchless]` — **DONE (commit `1e8bdcc147f`).** Приватный
  `next_pow2` в `std/collections/hashmap.nv` (~стр. 570): O(log n) цикл → канонический
  O(1) branchless bit-smear (`x = n-1; x|=x>>1; >>2; >>4; >>8; >>16; >>32; x+1`), guard
  `if n <= 1 { return 1 }` (n=0/1 + защита от n-1 underflow). `|`/`>>` на int подтверждены
  по `std/encoding/base64.nv`. `next_pow2` приватная (не экспортится) → equivalence-тест
  (n=0→1,1→1,2→2,3→4,4→4,5→8,8→8,1000→1024,1024→1024) добавлен **inline** в hashmap.nv,
  где fn в scope (внешний plan138_2-тест не может вызвать приватную fn). hashmap.nv inline 1/0.
- ✅ `[M-138.2-vec-type-priv]` — **DONE part (a) (commit `6e1ac8ace72`).** Type-level `priv`
  flip на `Vec[T]` в `std/collections/vec_owned.nv` (Plan 124 V2 / spec 02-types.md §1):
  `export type Vec[T] {` → `export type Vec[T] priv {`, сняты 3 per-field `priv` модификатора
  (`priv mut data/len/cap` → `mut …`). Effective visibility идентична — все поля private-by-default
  через header. **Part (b)** (`*mut T` → `*T` по D216 V2 §V2.1 right-binding) ✅ **DONE (Plan 138.4
  Ф.4 G-D, 2026-06-11, commit `38360c30d80`)** — `mut data *mut T` → `mut data *T` (блокер
  `[M-138.2-v2-propagation-impl-gap]` CLOSED, см. ниже).
- ✅ `[M-138.2-vec-self-return]` — **DONE return-position subset (commit `af6b0b59718`).**
  В `vec_owned.nv` (Plan 51 «type declared once» / D182 / D66): return-типы `-> Vec[T]`,
  равные receiver'у, → `-> Self` (`new`, `with_capacity`, `from`, range `@index(r Range)`,
  `@clone`; `-> Option[Vec[T]]` → `-> Option[Self]` на range `@get`). Конструктор-литералы
  де-типизированы: `new()` → bare arrow `=> { data:…, len:0, cap:0 }`; multi-statement тела
  → `Self { … }` (D66, spec 02-types.md:3078). **Param-position `Self`** (`@append`/`@copy_from`/
  `@compare`/`@equal`) **BLOCKED+REVERTED** → spawned `[M-138.2-self-in-param]` (см. ниже).
- ✅ `[M-138.2-stdlib-separator-mojibake]` — **DONE (commit `2e57e3d701b`).** Box-drawing
  separator mojibake (double-encoded U+2500, байты `d0 b2 e2 80 9d d0 82`, рендерит `в"Ђ`)
  в std-комментах: ровно 2 файла — `vec_owned.nv` (29 строк) + `prelude/protocols.nv` (10 строк);
  examples/ — 0 hits. Byte-precise transform только на `^\s*//` строках, run-длины сохранены
  → корректный `─` (U+2500, `e2 94 80`), как уже в `crc32.nv`. Доказано: re-apply на pristine
  backup даёт byte-identical output; 39 ins == 39 del, ни одной non-`//` строки в diff.

**Spawned gap-markers (codegen, требуют `.rs` + rebuild — НЕ workable из std/*.nv):**

- ✅ `[M-138.2-v2-propagation-impl-gap]` — **CLOSED (Plan 138.4 Ф.4 G-D, 2026-06-11, commit
  `38360c30d80`).** `data *mut T` → `*T` теперь компилируется (plan138_1 10/0). ROOT CAUSE отличался
  от изначальной гипотезы: НЕ «generic T лоуэрится в Nova_any» (T-subst через `current_type_subst`
  работал) — а binding-mut `const`-pointee. `mut data *T` — это canonical *readonly* pointer (D216 §1),
  поэтому для mut-bound поля codegen давал `const`-pointee; для DEAD prelude-induced `Vec[any]` инстанса
  (из variadic `...items []any` в `print`/`println`) это давало `const Nova_any** data`, и forward-typedef
  pre-pass (emit_c.rs ~12849, `starts_with("Nova_")` после strip `*`) SKIP'ал typedef (`const Nova_any`
  не начинается с `Nova_`) → `unknown type name 'Nova_any'` CC-FAIL. FIX: `field_type_with_binding_mut`
  + `promote_pointer_pointee_mut` — для mutable-поля immediate `Pointer(pointee)` (non-Mut/non-Unsafe)
  переписывается в `Pointer(Mut(pointee))`, applying `mut p *T ≡ mut p *mut T` (D33 §2 / D216 §V2.6);
  transparent над Readonly/Mut/Unsafe wrappers → generic pointee T сохраняется через mono. Applied на
  bare-template (emit_c.rs ~8848) И mono-path (~12891). VecIter `priv data *mut T` (read-only, не
  mut-bound) оставлен как есть — вне scope. (Optional followup: harden forward-typedef pre-pass strip'ать
  leading `const ` перед `starts_with("Nova_")` — не нужно сейчас, binding-mut снял const для mut-поля.)
- 🔴 `[M-138.2-self-in-param]` — **OPEN.** `Self` в PARAM-позиции generic-type метода
  (`@append(mut other Self)` и т.п.) не компилируется: codegen лоуэрит param-тип БЕЗ
  receiver-substitution → forward-decl ≠ definition → `conflicting types for
  'Nova_Vec_method_append'` (все 5 plan138_1 CC-FAIL). Return-position `Self` и `Self { … }`
  литералы работают; блокирована только param-позиция. Тот же класс отсутствия generic-method
  param-type substitution в emit_c.rs. Блокирует param-Self для `@append`/`@copy_from`/`@compare`/`@equal`.

---

## Связь

| План / D | Что |
|---|---|
| Plan 138.1 | partial-landing (флип где Vec есть + typed-storage gap); этот план — добивание |
| D239 | закрывается ПОЛНОСТЬЮ здесь (NovaArray retired) |
| D144 | закрывается |
| D26 | amend — str `@as_ptr` + Nova-body `to_bytes`/`to_chars`/`split` |
| D216 | amend если новый str ptr-примитив |
