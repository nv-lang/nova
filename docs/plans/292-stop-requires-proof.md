<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 292 — остановка требует доказательства: очередь становится данными, Stop-хук её проверяет

**Статус:** ПОДКЛЮЧЁН 2026-09-19 по прямому слову владельца («помоги закрыть 292… чтобы окна перестали останавливаться»). Ш.1, Ш.2, Ш.3, Ш.4 СДЕЛАНЫ; Ш.5 и Ш.6 ведёт окно nova-90. Прежде: ЧЕРНОВИК 2026-09-18. **Р.3 ПРИНЯТО интегратором 2026-09-18** с тремя условиями,
внесёнными в приёмку Ш.2 (см. «Слово интегратора»); остальные Р НЕ приняты.
**Ш.2 СДЕЛАН 2026-09-18 окном `nova-90`** (снимок, самотест 12/12, сшивка с хуком —
доказательства в самом Ш.2); Ш.3 остаётся черновиком. **Очередь:** Ш.4 (подключение) и слияние ветки — ПОСЛЕ 274.11 (274.10 почти пройдена),
главная работа это блокеры Карины. Заведён по вопросу
владельца: «работа по плану постоянно останавливается и в интеграторе, и в Карине; `/flow` не
помогает, `/push-controller` сам иногда останавливается, `/goal` тоже; как решить раз и
навсегда». **Ведёт:** интегратор. **Родитель:** нет; ближайшая родня — план 290 («правило без
механизма есть симптом») и 291 (инфраструктура двух окон).

Черновик хука и его самотест написаны и прогнаны этим же окном:
`scripts/claude-hooks/guard-stop-v2.py`, `scripts/claude-hooks/selftest/test-guard-stop-v2.py`
(22 клетки, PASS 22 FAIL 0, обратные пробы краснеют). **Хук НЕ подключён** —
в `.claude/settings.json` стоит прежний `guard-stop.py`; подключение это Ш.4.

## 1. Замер, а не ощущение

Одиннадцать стенограмм проекта (`~/.claude/projects/D--Sources-nv-lang/*.jsonl`), скрипт
разбора в `scripts/tools/` не заводился — разовый счёт, воспроизводится по описанию ниже.

| величина | значение |
|---|---|
| концов хода (сообщение окна → сообщение владельца) | 1104 |
| из них с объявленным следующим шагом («делаю дальше», «продолжаю», …) | 7 |
| из них заблокировано действующим `guard-stop.py` | 5 |

**Один процент.** Действующий хук ловит ровно тот случай, который сам и называет: «я сказал,
что делаю дальше, и не сделал». Остальные остановки МОЛЧАЛИВЫЕ: в них нет слов, по которым
можно судить. Это не изъян v1 — его докстринг честно говорит, что ловит один случай без
ложняков. Это граница подхода.

## 2. Корень: очередь выводится, а значит в момент остановки её нет

`docs/dev/prompts/integrator-handoff.md`, раздел «Очередь — СПРАШИВАЕТСЯ, а не ведётся»
(решение 2026-08-10): таблицу очереди удалили, она расходилась с действительностью и была
вторым домом правды; очередь теперь выводится командами (три зеркала, `git branch --no-merged`,
`registry-routes-scan.py`, гейт). Для истины это верно и пересмотру не подлежит.

Следствие, которого никто не вывел: **выводимая очередь не существует как факт.** Она
появляется только после того, как окно запустит пять команд и истолкует их. Значит в момент
конца хода машина не может ответить «работа осталась?» — и любой страж остановки обречён
судить ТЕКСТ. Отсюда и один процент.

**Тот же довод уже записан в проекте, но применён к другому.** `CLAUDE.md`: «приказ работает,
только пока агент послушен, — то есть держится на внимании ровно там, где мы везде заменяем
внимание механизмом». Принцип применили к ЗАГРУЗКЕ правил (импорт, хуки). К ПРОДОЛЖЕНИЮ работы
не применяли: `/flow`, `/goal`, `/push-controller` — текст, который читает то самое окно, чью
остановку он должен предотвратить.

## 3. Решения владельца (до кода)

| № | решение | рекомендация окна |
|---|---|---|
| Р.1 | **Умолчание переворачивается**: остановка требует машинно проверяемой причины, продолжение не требует ничего | да |
| Р.2 | Причина называется КОДОМ в последней строке доклада: `СТОП: очередь-пуста` / `СТОП: вопрос` / `СТОП: неавторизовано <действие>`. Кодов ровно три — это три законные остановки `/flow`, других не бывает | да |
| Р.3 | Очередь получает МАШИННЫЙ СНИМОК `target/queue.json`, который ВЫВОДИТСЯ теми же командами, что и сегодня. Решение 2026-08-10 в силе: это кэш ответа, а не второй дом правды; устаревший снимок доказательством не считается | да |
| Р.4 | Предохранитель обязателен: третья блокировка подряд пропускается, побег пишется в `target/.stop-guard/escapes.log` и виден в `/status`. Хук, который может запереть окно, однажды запрёт | да |
| Р.5 | Молчаливая смерть окна (лимит, падение, ожидание фона) — НЕ остановка, Stop-хук там не срабатывает вовсе. Её стережёт возраст файла: задача по расписанию сравнивает возраст последнего `/save` каждой роли с порогом и зовёт владельца | да |
| Р.6 | `/push-controller` перестаёт быть окном-сторожем и становится потребителем того же снимка: обход сессий остаётся, «замечать остановку» переезжает в Р.4+Р.5 | да; окно болеет той же болезнью, что стережёт |
| Р.7 | **Ворота по роли.** Хук требует код ТОЛЬКО в ролевом окне (роль по ветке: `main` — интегратор, `p274-novac` — Карина, ветка со словом `controller` — контролёр; `NOVA_WINDOW_ROLE` переопределяет). Окно без роли (исследование, пакет, разовая волна) хук не судит вовсе | да |
| Р.8 | **Две законные концовки сверх трёх кодов, обе проверяемые:** прерывание владельца (`[Request interrupted by user]`) пропускается молча; `СТОП: смена` (сдача смены через `/stop`) проверяется свежестью ролевой записки (10 минут) | да |

## 3а. Со всеми ли командами это работает (вопрос владельца 2026-09-18)

**Да, автоматически, и ни одну команду править НЕ НУЖНО, чтобы хук срабатывал.**
Stop-хук объявлен в `.claude/settings.json` один раз и срабатывает на конце ЛЮБОГО хода,
какой бы командой тот ни начался; команды о нём знать не обязаны. Но из той же
автоматичности следуют три вещи, которые решить надо, иначе он будет мешать там, где не
должен.

| случай | без ворот было бы | решение |
|---|---|---|
| исследовательское окно, окно пакета, разовая волна: очереди нет, каждый ход кончается ответом владельцу | хук требует код на КАЖДОМ ответе — шум, и его снимут первым днём | Р.7: роль по ветке; роли нет — хук нем |
| в ролевом окне владелец спросил `/explain`, `/status`, `/whoami` | хук требует код после ответа | **так и надо, это не правка команд:** ответ владельцу очередь не опустошает, `/flow` прямо говорит «доклад — запятая». Ответил и взял следующий пункт |
| владелец прервал ход (Esc) | хук блокировал бы остановку, которую вызвал сам владелец | Р.8: `[Request interrupted by user]` пропускается молча |
| `/stop` — сдача смены | остановка законна, а кода для неё в трёх нет | Р.8: код `смена`, доказательство — записка обновлена в последние 10 минут |
| субагенты (`/delegate`) | — | не задеты: у субагентов свой хук `SubagentStop`, этот план его не трогает (followup) |

**Что всё-таки правится в командах, и это текст, а не механика:** `/flow` получает форму
(дом правила), `/stop` — строку «заканчивай ход кодом `СТОП: смена` ПОСЛЕ записи записки»,
`/save` и `/push-controller` — ссылку на форму. Ни одна команда не становится обязательной
для работы хука: снимите все правки — хук продолжит работать, просто окна будут узнавать
форму из его же отказа.

## 3б. Слово интегратора — Р.3 принято, три условия (2026-09-18)

Снимок вторым домом правды НЕ становится, работу можно начинать. Три условия — часть
ПРИЁМКИ Ш.2, не пожелание:

| № | условие | что оно меняет |
|---|---|---|
| И.1 | Снимок живёт вне git (`target/`, под `.gitignore`) и несёт В СЕБЕ команду, которой получен, и метку времени. «Дом правды — тот, кого можно процитировать в споре; кэш, называющий свой источник, цитируется источником» | в контракт файла добавлены поля `produced_by` и `at`; `target/` уже под `.gitignore` |
| И.2 | Ни одно решение на снимок не опирается: он открывает ворота Stop-хука и ничего больше — не закрывает строку реестра, не двигает базу, не выбирает следующий шаг. Та же граница, что у выдачи агента-читателя | записано строкой в «Не делаем» |
| И.3 | **Отказ снимка не имеет права выглядеть пустой очередью.** «Не смог посчитать» обязан быть ГРОМКИМ: пропустить окно с записью побега в лог, но не «очередь пуста, можно стоять». Иначе первая же сломанная зависимость (нет сети до зеркала, не ответил `gh`) превращает хук в разрешение молчать, и это невидимо | **реализовано в хуке** — см. ниже |

**И.3 реализовано и проверено.** В хуке различаются два «плохо», и это не одно и то же:
снимка нет или он просрочен — окно чинит это САМО одной командой, поэтому блокируем и
называем её; снимок есть, но сам себя объявил неполным (`ok: false`, `failed_sources`) —
**окно не отвечает за чужую мёртвую сеть** (формулировка одобрена интегратором дословно
и оставлена здесь по его просьбе), поэтому пропускаем и пишем ПОБЕГ в
`target/.stop-guard/escapes.log` с именем сломавшегося источника. Клетка самотеста
«снимок ok=false: пропуск + побег в лог» проверяет обе половины: и пропуск, и наличие
записи с именем источника; обратная проба (снять ветку `ok is False`) краснеет на
отсутствии записи в логе.

**Следствие для Ш.2, обязательное:** скрипт снимка при неудаче источника обязан писать
`ok: false` и `failed_sources`, а НЕ пустой `open`. Пустой `open` при недостижимом
источнике — ровно тот случай, который И.3 запрещает.

**Очередь работ по плану, со слов интегратора (2026-09-18).** Граница между «можно сейчас»
и «нельзя» проведена не по важности, а по ЭФФЕКТУ: Ш.2 и Ш.3 — работа в своём дереве,
которая ничего не подключает; Ш.4 ставит хук в `.claude/settings.json`, после чего он
начинает судить КАЖДОЕ окно проекта, включая Карину в разгар приёмки, — а неверно
настроенный Stop-хук останавливает работу там, где останавливать нельзя. Поэтому Ш.4 и
слияние ветки `research/regex-nlib-0917` берутся после 274.11 (274.10 почти пройдена:
№1155 закрыт, №675 и №701а закрыты решениями владельца, №895 и №762 ждут слова владельца,
№763 получил фикстуру; запушено на три зеркала). Конфликт в
`docs/plans/221.1-bug-sweep.md` при слиянии ОЖИДАЕМ и разрешается дописыванием обеих строк
`№TBD` — номера назначаются при слиянии, а не заранее.

## 4. Границы — «Не делаем»

- Не возвращаем рукописную таблицу очереди (снята 2026-08-10 и по делу).
- Не учим хук угадывать работу по тексту: v1 пробовал узко и уткнулся в один процент.
- Не лечим этим планом дрейф цели («окно работает, но не над тем») — это домен `/goal`.
- Не трогаем гейт, пуш и права: план про конец хода, а не про разрешения.
- **На снимок не опирается ни одно решение** (И.2): он открывает ворота хука и ничего
  больше — не закрывает строку реестра, не двигает базу, не выбирает следующий шаг.

## 5. Шаги

### Ш.1 — форма остановки в нормах (до кода)

**СДЕЛАНО 2026-09-19** (коммит `cf8edd0b6`). Форма `СТОП: <код>` записана в
`/flow` — он приезжает в каждое окно слоем контекста, значит норма известна
раньше, чем по ней судят.

**ВСТАВКА ПЕРЕПИСЫВАЛАСЬ ЧЕТЫРЕЖДЫ, и страж бюджета был прав каждый раз.**
Постоянный слой едет в КАЖДОЕ окно при каждом старте и каждом сжатии, поэтому
цена абзаца умножается на окна и ходы. Осталась голая форма: объяснение даёт
сам хук в сообщении отказа, где оно бесплатно.

**Последнее сокращение нашло не байты, а ДУБЛЬ**, и он изобличал себя сам:
`/flow` говорил «правило живёт в `/explain`, здесь только ссылка, чтобы копии
не разошлись» — и тут же давал копию, притом что `/explain` едет В ТОМ ЖЕ
СЛОЕ. Копия в одном окне контекста с оригиналом не страхует от расхождения —
она его готовит, и платится байтами с каждого окна. Снята. Итог
`16982/17000`, потолок НЕ поднят.

`/flow` получает абзац: конец хода несёт `СТОП: <код>`; коды и что каждый означает, включая
`смена` (Р.8) и правило «ворота по роли» (Р.7) одной строкой — чтобы окно без роли не искало
у себя очередь. `/stop` получает строку «записку пиши ПЕРВОЙ, ход заканчивай `СТОП: смена`»;
`/save` и `/push-controller` — ссылку на форму, не копию. `AGENTS.md` («Staying alive») —
строку про то, что молчаливый конец хода в ролевом окне теперь блокируется.
**Приёмка:** `grep "СТОП:"` находит форму ровно в одном нормативном доме (`/flow`), остальные
ССЫЛАЮТСЯ; страж `check-rules-page-complete.sh` не краснеет; снятие всех этих правок НЕ ломает
хук — проверяется тем, что самотест Ш.3 их не читает.

### Ш.2 — `scripts/tools/queue-snapshot.py`: очередь как данные

Запускает ровно то, что уже прописано в записке интегратора, и кладёт результат:

```json
{ "role": "integrator|carina|controller|none",
  "at": "2026-09-18T16:20:00+03:00",
  "produced_by": "python scripts/tools/queue-snapshot.py .",
  "ok": true,
  "failed_sources": [],
  "open": ["274.13 Ш.1 — пин этапа", "№1147 — …"],
  "sources": {
    "mirrors_behind": { "value": 0,    "cmd": "git ls-remote --heads gitverse main" },
    "unmerged_branches": { "value": 2, "cmd": "git branch --no-merged main" },
    "blockers": { "value": 74,         "cmd": "python scripts/guards/registry-routes-scan.py ." } }
}
```

**Гейта среди источников НЕТ, и это не забывчивость:** авторитетный гейт идёт сорок минут,
снимком его не снять, а записанный вчерашний вердикт — ровно тот протухший кэш, ради борьбы
с которым очередь и перестали вести руками. Готовность спрашивается гейтом, а не снимком.

`produced_by` и `at` — условие И.1: кэш, называющий свой источник, цитируется источником,
а не собой. `ok`/`failed_sources` — условие И.3: недостижимый источник даёт `ok: false`, а
не пустой `open`.

**Уточнение интегратора к И.1 (2026-09-18):** каждое поле `sources` несёт КОМАНДУ, которой
получено, **дословной строкой** — не имя источника и не его пересказ. Разница не
косметическая: `produced_by` говорит, кто снял снимок, а `cmd` говорит, чем ПРОВЕРИТЬ
число, не веря снявшему. Снимок без `cmd` можно только принять на веру у автора; снимок с
`cmd` читатель повторяет сам и получает то же или ловит расхождение. Поэтому `sources` — не
плоские числа, а `{ "value": …, "cmd": "…" }`.

**Очередь ИНТЕГРАТОРА выводится, очередь КАРИНЫ — нет, и это надо сказать вслух.** Команды
выше — из его записки («Очередь — СПРАШИВАЕТСЯ, а не ведётся»): зеркала, несли́тые ветки,
блокеры реестра. У Карины такого раздела в записке НЕТ, и вывести её очередь сегодня не из
чего. Половина жалобы владельца была именно про Карину, поэтому умолчать нельзя. Следствие
по И.3, и оно правильное: для роли без источника скрипт пишет `ok: false` и
`failed_sources: ["очередь роли carina не выводится: источник не назван"]`, то есть хук её
окно ПРОПУСКАЕТ и пишет побег. Хук, который держал бы Карину по невыведенной очереди, врал
бы; побег каждый день в логе — ровно то давление, которое заставит источник назвать.
**Что нужно от владельца/интегратора:** чем является очередь Карины машинно — открытые
строки реестра с её меткой, шаги её плана, что-то третье. Пока ответа нет, Ш.2 закрывается
для роли `integrator`, а `carina`/`controller` честно отдают `ok: false`.

Правила: `open` СОБИРАЕТСЯ из источников, а не пишется руками; пустой `open` при непустых
`sources` — ошибка скрипта, не пустая очередь; `role: none` (исследовательское окно, разовая
волна) делает код `очередь-пуста` всегда законным. **Приёмка:** снимок живёт в `target/`
под `.gitignore`, несёт `produced_by` и `at` и у КАЖДОГО источника — дословную команду
`cmd` (И.1), причём приёмка не глазная: взять `cmd` из снимка, выполнить как есть и
получить то же значение; воспроизводим дважды подряд с тем же
содержимым; подмена одного источника меняет ровно своё поле; **недостижимый источник даёт
`ok: false` и `failed_sources`, а не пустой `open` (И.3) — проверяется прогоном с заведомо
недоступным зеркалом**; **время работы ≤ 15 с при опросе источников ПАРАЛЛЕЛЬНО**.

**Число «5 с» снято замером (2026-09-18), и замер стоит привести.** Оно было написано на
глаз. На деле: `registry-routes-scan.py .` — 0,48 с, а три зеркала `git ls-remote --heads
<r> main` — gitverse 0,7 с, origin 1,4 с, **sourcecraft 9,8 с**; последовательно это ~12 с.
Порог 5 с означал бы, что живое и исправное зеркало объявляется отказавшим на каждом снимке:
хук по И.3 пропускал бы окно с побегом в лог, побеги пошли бы потоком, и первое же
предложение было бы «снять хук». Поэтому источники опрашиваются параллельно, таймаут на
источник 15 с, и отказом считается ТОЛЬКО реальный отказ.

**Купленный тем же замером урок для приёмки:** первая попытка измерить три зеркала показала
отказ всех трёх — на самом деле в Git Bash нет `/usr/bin/time -f`, и падала измерялка, а не
зеркала. Прежде чем писать источник в `failed_sources`, скрипт обязан различать «команда
вернула ошибку» и «команду не удалось запустить»; второе — дефект скрипта, и в лог оно идёт
именно так.

**СДЕЛАНО 2026-09-18, 22:34–22:49 (окно `nova-90`, ветка `research/regex-nlib-0917`).**
Файлы: `scripts/tools/queue-snapshot.py`, самотест `scripts/tools/selftest/test-queue-snapshot.py`
(12 клеток парами, PASS 12 FAIL 0). Самотест хука Ш.3 остался **PASS 22 FAIL 0** — снимок его
не задел.

| приёмка Ш.2 | чем доказано |
|---|---|
| `produced_by`, `at`, `cmd` у каждого источника (И.1) | поля в `target/queue.json`; путь дерева пишется ПРЯМЫМИ слэшами — см. урок ниже |
| `cmd` не декоративен | команды взяты ИЗ снимка и выполнены дословно: несли́тых веток 12 = 12, `blockers=73` = 73, `origin` даёт тот же объект `5360cf431` |
| воспроизводимость | два прогона подряд совпали побайтово, кроме `at`/`elapsed_sec` (клетка самотеста) |
| изоляция источников | подмена зеркала меняет только поле `mirrors_behind`, `blockers` и `unmerged_branches` неподвижны |
| И.3 — отказ не выглядит пустой очередью | прогон с зеркалом `nosuchremote`: `ok=false`, источник назван, `open` НЕ пуст |
| две беды различимы | `ИСТОЧНИК ОТКАЗАЛ` (команда вернула ошибку) против `ЗАПУСК НЕ СОСТОЯЛСЯ` (`NOVA_QUEUE_PYTHON=no-such-python-executable`); килсвитч, сливающий обе формулировки в одну, красит ровно 2 клетки из 12 |
| бюджет | **2,1–2,6 с** при параллельном опросе трёх зеркал (порог 15); один прогон дал 12,3 с, когда `sourcecraft` отвечал медленно — запас есть, но он не десятикратный |
| снимок вне git | `git status --short` после прогона показывает только два новых файла скрипта; `target/` под `.gitignore` |
| сшивка с хуком | `open=85` → **блок** с перечислением (снято на снимке ДО вердикта по `blockers`; после него та же ветка проверена на `open=12`); `ok=false` → **пропуск + побег** в `target/.stop-guard/escapes.log` с именем источника; пустой `open` при `ok=true` → **пропуск**; роль `none` → пропуск по воротам роли |

**Три урока этого шага, все куплены прогоном, а не рассуждением.**

1. **CRLF съел разбор.** Первый же настоящий прогон дал `blockers: null` — `^blockers=(\d+)$`
   не видит строку `blockers=73\r`. Снимок при этом НЕ соврал: он назвал отказ вслух, и
   поймал собственный дефект тем самым правилом И.3, ради которого написан.
2. **Обратные слэши в `cmd` ломают приёмку И.1, а выглядит это как расхождение снимка.**
   Проверка «выполни `cmd` из снимка» показала расхождение в четырёх источниках из пяти —
   на деле `shlex` съедал `\` в `D:\Sources\...`, и падала ПРОВЕРЯЛКА. Тот же класс, что и
   `/usr/bin/time -f` в замере бюджета. Поэтому путь дерева пишется прямыми слэшами: строка
   `cmd` обязана ВСТАВЛЯТЬСЯ в оболочку, иначе условие И.1 не исполняется, а лишь объявлено.
3. **Проба сшивки дважды мерила не свой предмет.** Код `СТОП:` стоял в середине фразы —
   хук отвечал «ход окончен без причины», то есть проверялся разбор текста, а не очередь; а
   «пропуск» в клетке пустой очереди дал ПРЕДОХРАНИТЕЛЬ хука (третья блокировка подряд при
   общем `session_id`), а не проверяемая ветка. Обе клетки были зелёными по неверной причине.

**Расхождения план ↔ код, найденные шагом 1 (прав хук, план приведён в соответствие).**

* `sources` в контракте нарисован плоским (`value` + `cmd`); снимок КРОМЕ них несёт разбор,
  без которого `open` не собрать: `mirrors_behind.mirrors` (объект каждого зеркала и своя
  `cmd` у каждого), `unmerged_branches.branches`, `blockers.numbers`. Хук этих полей не
  читает — расширение контракта, не спор с ним.
* Роль `none` в снимке отдаёт `ok: true` с пустыми `open` и `sources`, но БЕЗ похода к
  источникам: у окна без роли очереди нет, а хук по такой роли пропускает раньше, чем
  смотрит файл.
* Добавлена проверка, которой в плане не было: **несуществующее дерево — это `СНИМОК УПАЛ`,
  а не `role: none`.** Без неё путь с опечаткой давал бы «ворота открыты» — отказ в костюме
  законной пустой очереди, ровно то, что И.3 запрещает (случай назван интегратором
  2026-09-18: ломается не источник, а сам скрипт).

**`blockers` — ХРАПОВИК, А НЕ ОЧЕРЕДЬ (вердикт интегратора 2026-09-18, исполнено в коде).**
Источник считает строки реестра с полем `БЛОКИРУЕТ ТЕГ: ДА`, и поначалу они шли в `open` —
73 пункта из 85. Это было неверно, и обе половины ответа уже лежали в плане 274, в двух
разных местах: поле осталось от **мёртвого события** — тега ОРАКУЛА, которого не будет
(слово владельца 2026-09-15, строки 46–50: «не строй на нём ни приоритетов, ни докладов»), —
а замены источника не будет вовсе, потому что «его смысл перечитывается как „блокирует релиз
Карины“ **при работе со строкой, а не сплошной заменой**» (там же, ~4963: сплошная разметка
1062 строк отвергнута замером). Обе цитаты сверены по дереву мной, а не приняты со слов.

Что сделано: `blockers` остаётся в `sources` со своей командой — как счётчик он законен и
сторожит рост строк «нельзя выпускать», — но в `open` НЕ ИДЁТ: «73 открытых пункта»
прочитывалось бы как работа, которой нет. Снимок роли интегратора теперь даёт `open=12`
(несли́тые ветки), а не 85. Клетка самотеста «blockers в open НЕ попадает» стережёт границу;
килсвитч, возвращающий номера в очередь, красит 2 клетки из 12.

**Урок, и он про поиск ответа, а не про код:** снятый предмет я нашёл, а решение, чем его
заменить, лежало двумя абзацами ниже в том же файле. Найдя в документе снятый предмет —
дочитай документ: тот, кто снимал предмет, обычно тут же сказал, чем его заменить.

**ДЛЯ РОЛИ `carina` МАШИННОГО ИСТОЧНИКА ОЧЕРЕДИ НЕТ ПО СУЩЕСТВУ, А НЕ ПО НЕДОДЕЛКЕ.**
Решение интегратора 2026-09-19, и записано оно РЕШЕНИЕМ намеренно: оставленное «пунктом»,
оно позвало бы следующее окно «доделать» и построить ложный источник.

Довод. Машинные меры у её ступени есть — отставание дифференциала
(`check-novac-differential.sh`), самосборка, налог оракула. Но это меры **ПРОГРЕССА, а не
перечень ПУНКТОВ**: число «сколько осталось до цели» не есть список того, что делать.
Втиснуть их в `open` значило бы ровно ту подмену, которая на этом же шаге уже отвергнута по
`blockers`, — и отвергнута тем же доводом, только с другого конца. Второе, и оно решающее:
снимок нужен **Stop-хуку**, чтобы отличить «работа осталась» от «очередь пуста»; для окна
Карины на этот вопрос сегодня отвечает её план и её собственное суждение, а не прогон.
Значит `ok: false` с названной причиной — не пробел, а **честное описание положения**, и хук
её окно пропускает с побегом в лог. Выдумать источник было бы хуже, чем признать, что его
нет: ложный источник не виден никому, а побег в логе виден каждый день.

То же и для `controller`. Работой это станет тогда и только тогда, когда у роли появится
перечень пунктов, выводимый командами, — а не когда кому-то покажется, что снимок «неполон».

### Ш.3 — хук v2 (СДЕЛАНО)

**СДЕЛАНО 2026-09-19**: `cf8edd0b6` (коммит хука и самотеста) + `40d38fd18`
(замена на полную редакцию). До первого из них механизм существовал только
как файл на диске у одного окна.

**И ТУТ ЖЕ НАЙДЕН ДЕФЕКТ ХУДШЕЙ ВОЗМОЖНОЙ ФОРМЫ — окном nova-90, замером,
через минуты после подключения.** Я закоммитил ЧЕРНОВУЮ копию (422 строки)
вместо готовой (504): в ней не было вывода вердикта БАЙТАМИ и обработчика
падения. Печать кириллицы в консоль cp1251 бросает `UnicodeEncodeError` → хук
умирает → stdout пуст → **для Stop-хука пустой stdout означает ПРОПУСК**. То
есть поломка стража выглядела бы как «всё в порядке, окно можно отпустить», и
узнать об этом было бы НЕОТКУДА.

**Класс назван им же: НЕМОЙ ОТКАЗ В КОСТЮМЕ УСПЕХА** — третий случай за
сутки (самотест, воспроизводивший дефект на не-латинском пути; дверь на
проверке, которой нет; этот). Самотест вырос 18 → 22, и лишние четыре клетки —
ровно случаи падения: число само называет, чего не хватало.

`scripts/claude-hooks/guard-stop-v2.py`: нет кода — блок; `очередь-пуста` сверяется со
снимком (нет снимка / старше 30 мин / `open` непуст — блок с перечислением); `вопрос` требует
знака вопроса и не допускает двух вопросов подряд без единого вызова инструмента;
`неавторизовано` требует действия из закрытого списка. Ноль запусков процессов.
Плюс ворота по роли (Р.7), пропуск прерывания и код `смена` (Р.8) — всё внутри того же хука,
без единого запуска процесса: ветка берётся чтением `.git/HEAD`, свежесть записки — её mtime.
**Приёмка (выполнена этим окном):** `python scripts/claude-hooks/selftest/test-guard-stop-v2.py`
— **PASS 22 FAIL 0**, среди клеток: окно без роли (хук нем), прерывание владельца (пропуск),
`смена` со свежей запиской (пропуск) и с трёхчасовой (блок), снимок `ok: false` (пропуск И
побег в логе с именем источника — условие И.3); обратные пробы: заменить `if items:` на
`if False:` — краснеет; снять ветку `ok is False` — краснеет на отсутствии записи в логе.

**Предупреждение интегратора (2026-09-18) — и оно попало в цель.** Проверять надо не только
поломку ИСТОЧНИКА данных, но и поломку САМОГО скрипта: исключение, битый JSON, отсутствующий
python. У интегратора в тот день фикс прошёл самотест и всё равно молча ушёл в запасную ветку
на кириллическом пути. **Замер по черновику:** подстроенное исключение внутри `main()` давало
`rc=1` и ПУСТОЙ stdout, то есть немой пропуск. Упавший хук неотличим от снятого, и заметить
это нечем — отказ источника снимок хотя бы называет (`ok: false`), а упавший скрипт не
говорит ничего.

Почему это ломается именно на кириллице: решение печаталось через `print`, а `print`
кириллицей в консоль cp1251 роняет процесс `UnicodeEncodeError` — то есть страж умирает ровно
от собственного текста. Сделано три правки: решение печатается БАЙТАМИ (`emit`), сырой вход
читается ДО всего, что может упасть, и любое исключение ловится `except BaseException` и
превращается в БЛОКИРОВКУ с именем ошибки и местом плюс запись в `escapes.log`. Запереть окно
эта блокировка не может: на следующем заходе Claude Code ставит `stop_hook_active`, который
читается из сырого входа — то есть до кода, который упал.

Четыре новые клетки: падение хука (блок, а не немота, и след в логе), повторное падение (не
запирает), битый JSON снимка (блок), кириллический путь при `PYTHONIOENCODING=cp1251` (хук
говорит, причина читаема). Обратная проба: сузить `except BaseException` до `except ValueError`
— самотест краснеет на «падение не названо».

**Чего эта правка НЕ закрывает, и это честно:** отсутствующий или неверный интерпретатор в
`settings.json` изнутри не ловится — хук в этом случае не запускается вовсе. Это проверяется
при подключении (Ш.4), а не собой.

**Урок, купленный на самотесте и записанный, чтобы не купили второй раз:** первая редакция
клетки «два вопроса, но между ними работа» падала, и виновата была ФИКСТУРА, а не хук —
вызовы инструментов были положены в предыдущий ход, тогда как работа принадлежит тому ходу,
который кончается вторым вопросом. Проверка, которая мерит не свой предмет, выглядит точно
как дефект предмета.

### Ш.4 — подключение и снятие v1 (СДЕЛАНО)

**СДЕЛАНО 2026-09-19** (`cf8edd0b6`): `.claude/settings.json` переведён на v2,
v1 снят ТЕМ ЖЕ диффом. Два хука на одном событии были бы двумя судьями с
разными правилами: окно, прошедшее одного, падало бы на другом, и нельзя было
бы сказать, чей отказ верен.

**ЗАМЕР, ПОЛЕЗНЫЙ ВСЕМ ОКНАМ: перезапуск сессии для подключения НЕ
ПОТРЕБОВАЛСЯ.** Правка сделана в идущей сессии, и v2 заблокировал её же
автора через минуты — значит хуки читаются на срабатывании, а не кэшируются
при старте. Норма из `/flow` — другое дело: она едет импортом при старте и
хуком после сжатия, но хук САМООБУЧАЮЩИЙ (печатает форму в отказе), поэтому
цена незнания — одна лишняя блокировка.

**ГРАНИЦА, которую надо знать: окна в СВОИХ деревьях получают хук СЛИЯНИЕМ, а
не перезапуском** — у них свой `.claude/settings.json`. Порядок для них: пуш →
слияние → (при желании) перезапуск.

Одной строкой в `.claude/settings.json` (`Stop` → `guard-stop-v2.py`), тем же коммитом
снимается `guard-stop.py` и его самотест. **Приёмка, ПЕРВЫМ пунктом — что хук вообще
запускается:** подставить заведомо незаконный конец хода в живом ролевом окне и увидеть
блокировку СВОИМИ глазами. Это единственный класс немоты, который изнутри не ловится
(интерпретатор не тот, путь к файлу не тот, ключ в `settings.json` не тот): хук в этих
случаях не запускается вовсе, и ни одна его клетка об этом не узнает. Дальше — неделя
работы двух окон; счёт по
`escapes.log` — побегов не больше одного на окно в сутки (больше — форма не прижилась, и это
повод чинить форму, а не снимать хук); ни одного случая, когда окно заперто и не может
закончить ход законно.

### Ш.5 — побеги видны

`/status` печатает строку из `escapes.log` за сутки; почасовое напоминание
(`remind-session-save.py`) — тоже, если побеги есть. **Приёмка:** побег, поставленный руками,
виден в обоих местах.

### Ш.6 — пульс РЯДОМ С контролёром (Р.5, поправлено владельцем 2026-09-19)

Задача по расписанию: возраст последнего `/save` каждой ролевой записки против порога (час,
как у напоминания). Порог превышен — зовёт владельца. **Приёмка:** порог занижен до минуты на
пробу, зов приходит; возвращён — молчит.

**ПОПРАВКА К ЗАГОЛОВКУ, И ОНА МЕНЯЕТ ПОСЫЛКУ ШАГА. Слово владельца 2026-09-19:
«контролёр пусть пока работает, если не мешает».** Значит пульс ДОБАВЛЯЕТСЯ, а не
ЗАМЕНЯЕТ: `/push-controller` пункта «замечать остановку» пока НЕ лишается, и ссылки
сюда вместо него не ставится. Прежний заголовок «вместо контролёра» обещал замену,
которой не будет до отдельного решения.

**ЧТО ХУК У КОНТРОЛЁРА ЗАБРАЛ, А ЧТО НЕТ (разбор интегратора, принят владельцем как
основание «пусть работает»):** забрал обход живых окон каждые семь минут — это ловится
теперь на конце каждого хода автоматически. НЕ забрал и забрать не может трёх вещей:
(1) **умершее окно не останавливается — оно замолкает**, а хук срабатывает на КОНЦЕ
хода, которого у повисшего окна не будет; (2) **побеги** — окно, раз за разом упирающееся
в предохранитель, снаружи выглядит работающим; (3) часовые напоминания владельца
(лимиты, `/delegate`) — они не про остановки вовсе.

**КРИТЕРИЙ СВОРАЧИВАНИЯ — ЗАМЕР, А НЕ МНЕНИЕ, и он назначен здесь:** сутки работы хука,
и счёт того, **сколько раз контролёр толкнул окно, которое хук ПРОПУСТИЛ**. Ноль —
роль сворачивается до пульса; не ноль — мы узнали, чего хук не видит, и это факт, а не
ощущение. Отсчёт с 2026-09-19 01:44 (подключение).

## 5а. ПЕРЕНОС В ДРУГОЙ ПРОЕКТ — всё, что нужно, в одном месте

Раздел заведён по просьбе владельца 2026-09-19: план должен читаться как
самодостаточный рецепт, а не как заметка для тех, кто уже знает наше дерево.
Ниже — из чего механизм состоит, что в нём ОБЩЕЕ, что НАШЕ, в каком порядке
ставить и чем проверять каждый шаг.

### А. Части и их назначение

| часть | что делает | без неё |
|---|---|---|
| `scripts/claude-hooks/guard-stop-v2.py` (504 стр.) | судит конец хода: есть код — проверяет, нет — блокирует | механизма нет вовсе |
| `scripts/claude-hooks/selftest/test-guard-stop-v2.py` (306 стр., 22 клетки) | доказывает обе стороны, включая СМЕРТЬ самого хука | хук ломается молча |
| `scripts/tools/queue-snapshot.py` (498 стр.) | кэширует ответ «есть ли работа», с `ok`/`failed_sources` | код «очередь-пуста» нечем проверить |
| `scripts/tools/selftest/test-queue-snapshot.py` (278 стр., 12 клеток) | доказывает, что отказ источника НЕ выглядит пустой очередью | снимок врёт молча |
| норма в команде режима (у нас `/flow`) | форма `СТОП: <код>`, три кода | окна узнают форму только из отказов |
| `scripts/tools/stop-escapes.py` + вызов из `/status` | показывает побеги предохранителя | запертые окна не видны |
| `scripts/tools/role-pulse.py` | ловит ЗАМОЛЧАВШЕЕ окно (хук его не видит) | мёртвое окно невидимо |

**Обязательны первые пять. Побеги и пульс — вторая волна:** без них механизм
работает, но слепнет на двух своих же краях.

### Б. Что ОБЩЕЕ, а что НАШЕ

**Общее (переносится как есть):** остановка требует КОДА, а продолжение —
ничего; ровно три кода, по числу законных причин; код ПРОВЕРЯЕТСЯ, а не
принимается на слово; предохранитель (N-я блокировка подряд пропускается и
пишется побегом); ворота по РОЛИ — окно без роли не судится вовсе; отказ
хука печатает форму, то есть учит ей в момент нужды.

**Наше (заменяется под проект):** список источников очереди (у нас три
зеркала, несли́тые ветки, реестр дефектов); имена ролей и правило их
определения (у нас ветка плюс визитка в общем `.git`); имя команды, несущей
норму (`/flow`); пороги (полчаса на свежесть снимка, час на записку).

**Граница между ними и есть главное, что даёт перенос:** если в чужом проекте
нет «очереди» как данных — код `очередь-пуста` там нечем проверять, и честный
ответ не «выдумать источник», а свести коды к двум.

### В. Порядок установки и проверка КАЖДОГО шага

1. **Снимок очереди.** Поставить скрипт, назвать источники, прогнать самотест.
   *Проверка:* отключить один источник — снимок обязан дать `ok:false` с его
   именем, а НЕ пустую очередь.
2. **Норма.** Записать форму в то, что приезжает в каждое окно.
   *Проверка:* открыть новое окно и увидеть форму в контексте.
3. **Хук, но ЕЩЁ НЕ подключённый.** Прогнать самотест целиком.
   *Проверка:* килсвитч — сломать условие, увидеть, какие клетки покраснели.
4. **Подключение.** Одна строка в настройках; прежний хук снять ТЕМ ЖЕ
   диффом — два судьи с разными правилами дают отказы, о которых нельзя
   сказать, чей верен.
   *Проверка, и она ПЕРВАЯ по важности:* закончить ход заведомо незаконно и
   УВИДЕТЬ блокировку своими глазами.
5. **Побеги и пульс.** Ставятся после того, как основное прижилось.

### Г. Класс немоты, который изнутри НЕ ловится

**Хук может не запускаться вовсе** — не тот интерпретатор, не тот путь, не тот
ключ в настройках, — и ни одна его клетка об этом не узнает: самотест
проверяет ЛОГИКУ, а не то, что систему позвали. Поэтому пункт 4 требует
увидеть блокировку ГЛАЗАМИ.

**И вторая немота, которая нам стоила дорого 2026-09-19:** если хук падает,
его stdout пуст, а пустой stdout Stop-хук читает как ПРОПУСК. То есть поломка
стража неотличима от его довольства. Лечится двумя вещами в самом хуке —
вывод БАЙТАМИ (печать не-латиницы в консоль с узкой кодировкой бросает
исключение) и перехват падения с явным сообщением, — и клетками самотеста
именно на это. У нас их четыре, и они появились после того, как подключённой
оказалась редакция без них.

### Д. Чего перенос НЕ даёт

Механизм заставляет окно НАЗВАТЬ причину остановки и проверяет названное. Он
не делает работу правильной: окно, работающее не над тем, честно напишет
«очередь-пуста», и код будет верным, а работа — нет. Это другой предмет, и
здесь он назван, чтобы перенос не читался как обещание большего.

## 6. Чего этот план НЕ вылечит, и это надо знать заранее

- Окно, которое работает не над тем: код `очередь-пуста` будет честным, а работа — не той.
- Окно, которое пишет «СТОП: очередь-пуста» при непустом снимке НАРОЧНО: хук заблокирует, но
  третий раз пропустит (Р.4). Предохранитель и дыра здесь — одно и то же, и выбор в пользу
  предохранителя сознателен.
- Сам снимок может врать, если врут его источники. Ровно поэтому `sources` печатаются рядом:
  расхождение видно глазами, а не выводится из пустоты.

## 7. Followups

- Форма `СТОП:` по-английски для внешних читателей истории — решать, когда появится внешний
  читатель; сейчас это внутренняя дисциплина, доклады по-русски (AGENTS.md).
- Счёт «сколько ходов подряд окно работало без остановки» как мера здоровья смены.
