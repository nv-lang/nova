<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 196.3 — Волна-2: встречная D-driven миграция сиблингов второго окна

**Родитель:** [196-one-truth-closeout.md](196-one-truth-closeout.md) (матрица одного окна + инвентарь второго
окна + целевая архитектура + rustc-эталон). **Пара:** [196.2](196.2-class-c-relocation.md) (волна-1,
`infer_call_ret_c`). **Статус:** 🔄 в работе [opus, worktree `nova-wave2`].

## Идея — две НЕЗАВИСИМЫЕ волны («встреча» НЕ критерий; АСИММЕТРИЯ: волна-2 покрывает ВЕСЬ 196, волна-1 — только `infer_call_ret_c` [необходимая часть, не весь план] — владелец 2026-07-13)

- **Волна-1 (196.2, bottom-up):** по 114 веткам `infer_call_ret_c` — удаляет legacy-ветки (gate-1). Держит
  ВСЮ `infer_call_ret_c` (`emit_c.rs` 46293-48883).
- **Волна-2 (196.3, top-down, D-driven):** по ИНВЕНТАРЮ второго окна (таблица ~30 функций, см. 196) — каждую
  функцию → её D-фиче → полный тест-набор → переписать на новый путь → закрыть. Держит СИБЛИНГ-функции ВНЕ
  диапазона 46293-48883 + `callnorm`/`argbind`/резолверы.

## ★ ПЕРВЫЙ ШАГ — инвентаризация недостающих тестов (директива владельца 2026-07-12)

**ДО миграции** — сначала инвентаризация тест-покрытия. Для КАЖДОЙ D-фичи (инвентарь-функции) определить,
каких тестов НЕ ХВАТАЕТ **по плану** (матрица 196: формы `static` / `generic-static` / `+кросс-модуль` /
`приватные` × аспекты `args` / `generic-args` / `default` / `generic-default` + edge-формы, что legacy
обрабатывал в РАЗНЫХ местах). Сравнить с 307 существующими conformance (таблица ниже) → **записать ПРОБЕЛЫ per
D сюда** (какие ситуации не покрыты).

По ходу — можно СРАЗУ добавить недостающие тест-файлы, ЕСЛИ это оптимально, **НО без исправлений компилятора и
без прогонов/проверок** (только НАПИСАТЬ тесты — установить покрытие; НЕ чинить компилятор, НЕ верифицировать,
НЕ мигрировать). Миграция/закрытие D — отдельным заходом ПОТОМ.

**Пробелы тест-покрытия по D-фичам (заполняет волна-2, первый заход):**

| D-фича / инвентарь-функция | Формы×аспекты по матрице (что должно быть) | Существующие тесты | НЕДОСТАЮЩИЕ ситуации | Добавлен тест-файл |
|---|---|---|---|---|
| **D102/D46/D50 named-args + defaults** (`callnorm`/`argbind`) | `{default, named}` × `{free-fn, static, generic-static, method, кросс-модуль}` — call-site backfill + named-override по ВСЕМ формам | d102 (free-fn: default / named-override / out-of-order); d372 `Vec[int].new()` + `new(cap: 8)` (**кросс-модуль generic-static default+named через std — ПОКРЫТ**); d88 (generic type-param defaults) | **USER-type static-метод × {default, named}** на call-site; **USER-type generic-static × {default, named}** упражнён в runtime (`types_generic_static_ctor` ДЕКЛАРИРУЕТ `make(cap=42)`, но **без test-блоков → не вызывается**) | ✅ **`d102_static_generic_static_defaults.nv`** (5 test-блоков: static all-default / static named-override / static both-named-out-of-order / generic-static default per-элемент-тип / generic-static named-override) |
| **D182 static return** (`infer_static_method_ret`) | `return` × `{static, generic-static, кросс-модуль, private}` | d182 (parametric `Self`: Box[int/str], Pair[A,B], sum-ctor Opt[T]); d143 (protocol-static); static_ctor_instance_method_named_tuple | USER-type **кросс-модуль** static return (отдельный ПАКЕТ) — **не выразимо в single-file conformance** (кросс-модуль = импорт std; std-форма покрыта d372 `Vec`) | — (структурное ограничение формата; std-кросс-модуль покрыт) |
| **D372/D239 generic-static ctor** (`infer_generic_static_ctor_ret`) | `return` × `{generic-static, slice-static, кросс-модуль}` | d372 (primitive/str/`[]T`/`Vec`.new + `.cap(n)` + `new(cap:)`); types_generic_static_ctor; slice_static_generic_method (method-level generics `[]T.echo2[D]`→Result); d239 | — (формы покрыты; runtime default-упражнение generic-static добавлено строкой D102 выше) | — |
| **D48/D402 closures** (`infer_lambda_return_type_with_params`) | `return` × closure-формы (typed / light / unit-body / value-position) | d22_closures; d402_closure_return_width; d157_view_default_closures; neg d417_closure_scalar_return | unit-возврат `\|\| { side_effect() }` (motivating case в `infer_lambda`:42255) — явного позитив-файла нет (возможно внутри d22) | — (minor; в scope пере-проверки при миграции D48) |
| **D43 trailing-block** (`infer_trailing_block_sig`) | `return`/sig × trailing-block (`Lazy[T].new { }`, `get_or_init { }`) | d43_trailing_block_and_fn | — | — |
| **D30/D85/D325 Result/Option** (`resolve_result_option_ret`/`resolve_result_te(_strict)`/`infer_result_type_params`) | `T`/`E`-распаковка × `{?-оператор, match, method-chain}` | d325_result_everywhere (4 файла); d85_question_return; d30_try_op_unwrap_pair; d408_option_chain_sized_width; d85_result_payload_width | — | **ОБНОВЛЕНО 2026-07-17 (Zone RET, сверка ПО КОДУ):** `resolve_result_option_ret`'s СИГНАТУРА — НЕ то, что старые доки Q5 описывали. Актуальный код (emit_c.rs ~18959): `fn(ty: &TypeRef, subst: &[(String, Option<String>)]) -> Option<String>` — механический маппер УЖЕ-СУБСТИТУИРОВАННОГО `Result[Ok,Err]`/`Option[Inner]` TypeRef в mono `NovaRes_`/`NovaOpt_` C-имя (Plan 180, доккомент сам формулирует «not inference, just naming/registration»). Это ЗАКОННЫЙ lowering (rustc-эталон: mono=подстановка), НЕ Tier-2 re-inference — subst приходит от УЖЕ channel-осведомлённого `resolve_instance_call_subst` (единый POST-mono резолвер, флипнут W1-i, SHADOW 0/0, `docs/plans/wip/196.5-stage-d-w1i-notes.md`). Функция остаётся SHARED (вызывается из B06a внутри frozen) — снять НЕЛЬЗЯ до опустошения caller'а (frozen-агент), но её СОБСТВЕННАЯ логика мигрировать в чекер НЕЧЕГО — там нет инференции. **Настоящий остаток Q5** — `infer_result_type_params`'s legacy Ident/Call/`.map`/`.map_err` ре-деривация (для `?`/match/method-chain T,E-ИЗВЛЕЧЕНИЯ, отдельная задача от «вычислить mono-имя возврата вызова») — блокер НЕ снят: чекер по-прежнему не аннотирует `resolved_types[call.id]` для generic free-fn/ctor Result-возврата вне method-generic-класса (`typeref_mentions_any`-guard ~10478 «skip generic-возврат» — сверено, комментарий актуален, строка сдвинулась незначительно) → Зона CH пока НЕ доставила «сперва sum/Result-return» расширение канала (только B1 closure-arg/node_substs producer в main, ОРТОГОНАЛЕН этому классу — сверено по диффу `a36d2caed`, чисто additive к node_substs, resolved_types-поведение НЕ меняет). Честный красный: Q5 в этой части ОСТАЁТСЯ блокирован Зоной CH |
| **D16/D53/D72 generics/протоколы** (`infer_type_param_binding`×3, `infer_protocol_structural_binding`) | type-param вывод + protocol-dispatch × `{structural, blanket, bounded}` | d53_type_protocol_kind_token; d42_structural_protocol; d355_blanket_protocol; d72_generic_bound_protocol; d135_bare_generic_inferred; d16_generics_brackets | — | — |
| **D119/D122 mono / method-generics** (`resolve_method_level_subst`, `resolve_mono_type_args`) | subst + mono type-args × `{bound-method, tuple, value-record, anon-tuple}` | d119_method_level_type_params; d122_bound_method_mono_dispatch; d122_generic_bound_forwarding; d277_generic_value_record_mono; d123_tuple_mono; d354_generic_anon_tuple_mono | — | — |
| **D239/D373 элемент массива/слайса** (`compute_array_elem_type_for_obj`) | elem-тип × `{array, slice, generic-array, sized-elem}` | d239_slice_vec_alias; d373_generic_array_api; d403_vec_sized_element_read; d411_array_sized_elem_width | — | — |
| **D109/D74 методы примитивов** (`primitive_instance_method_known`) | builtin-методы примитивов (hash/eq/ord/clone/math/abs) — §3 хардкод-зеркало | d109_primitive_builtin_methods; d74_math_methods | — (существование зовётся из чекера `types/mod.rs:9946`; §3-снос хардкода = задача МИГРАЦИИ, не покрытия) | — |
| **D52/D407/D406 методы sum** (`infer_method_level_return_for_sum`) | возврат метода sum × `{Option, Result, user-enum, sized-payload}` | d52_sumint; d52_type_forms; d407_enum_payload_width; d406_enum_kind_token; d85_result_payload_width | **Верифицировано 2026-07-13** ([отчёт](wip/196.5-perd-d52-verification.md)): 0 вызовов на всей матрице кластера (в файлах нет dot-call'ов); user-enum-часть матрицы СТРУКТУРНО недостижима — оба caller'а (`infer_call_ret_c` 49363/49389) передают только литералы "Option"/"Result"; тело канал/resolved_types не читает (100% legacy). Классификация SHARED, закрытие = Stage-D; фактическая матрица функции = только Option/Result. **ОБНОВЛЕНО 2026-07-17 (Zone RET, сверка ПО КОДУ, `docs/plans/wip/196.5-stage-d-notes.md`+`-wave2-notes.md`):** Stage-D (отдельная от волны-2 линия работ, тот же автор) ИЗМЕРИЛА и ЧАСТИЧНО закрыла легаси-трафик этой функции: 3 чекер-продюсер фикса (static-вызов-на-примитиве в closure-теле; unwrap-семья материализована как чекер-интринсик, зеркалируя ретрактированные `.nv`-декларации; poison-фильтр отравленных spelling-аргументов) убрали B11q/B11r легаси-трафик на conformance **35→0**. Остаток: **16 хитов на std/src/data** — `u64.try_from(a)`-класс (D77 codegen-интринсик, НАМЕРЕННО стёртая репрезентация `NovaRes_nova_int_nova_str*`; чекер честно НЕ материализует `Result[u64,ParseError]`, т.к. разошлось бы с фактическим эмитом = сломан byte-parity). Функция остаётся легаси (не удалена — реальный, живой, структурный трафик), но **conformance-часть кластера (Option/Result method-generic, включая d52/d406/d407 фикстуры) фактически 0-hit уже сегодня** — межблокер закрытия ФИНАЛЬНОГО снятия функции = отдельный D77-follow-up (вне scope и Q2, и волны-2), не Zone CH channel-work | — |
| **D315 тип→C** (`type_ref_to_c`, ретайр → `resolved_type_to_c`) | лоуэринг ВСЕХ типов (покрыт транзитивно всем корпусом) | d315_resolvedtype_lossless_canon + весь корпус | — | — |

## Метод (идти по инвентарю → D)

Для КАЖДОЙ сиблинг-функции инвентаря:
1. Сопоставить её **D-фиче** (языковой фиче, которую она обслуживает; уточнить точные D-номера).
2. Покрыть фичу **полным conformance-набором** (`d<NNN>_*.nv`; red-до/green-после где применимо). Тесты —
   независимые файлы, параллельно-безопасны обеим волнам.
3. **Переписать на новый путь** (резолв/инференс → чекер → `resolved_types`/`resolved_callees`, per «Целевая
   архитектура» в 196; rustc-эталон; **no new window / §3** — сходимость на существующий путь, не новый сайт).
4. **ЗАКРЫТИЕ D = D работает через новый компилятор (по архитектуре) + удаление legacy-функции ИЛИ
   `panic!("[MIGRATED-<D>]")`** (если переплетена / SHARED-хелпер зовётся волной-1 → detach/panic, НЕ удалять
   пока зовётся).

## ★ Цикл закрытия КАЖДОГО D (дисциплина, ОБЯЗАТЕЛЬНО)

тест → миграция → закрытие → **(1) отчёт** (ледер `wip/196.wave2-progress.md`) → **(2) коммит** → **(3) обновить
план** (статус D в таблице ниже) → **(4) СИНК С MAIN** (`fetch github main` → merge main в ветку [взять свежее,
вкл. волну-1] → merge D-закрытия в main, push github+gitverse). Частый per-D синк = волны сходятся на main.

## ★★ ПРИЁМКА КАЖДОГО D (принятие работ оркестратором — ПО КОДУ, НЕ по отчёту агента)

Оркестратор при принятии закрытого D ОБЯЗАН перепроверить ПО КОДУ (греп + чтение), не доверяя отчёту:
1. **Одно окно в ПРАВИЛЬНОМ месте.** D резолвится ОДИН раз в ЧЕКЕРЕ → `resolved_types`/`resolved_callees` →
   codegen ЧИТАЕТ (не перевыводит). В колонке «Закрыто в» — КОНКРЕТНАЯ функция нового пути; убедиться, что это
   ПРАВИЛЬНОЕ место (чекер/канал, а НЕ новый codegen-сайт = не «второе окно №2»).
2. **Legacy для D МЁРТВ.** Соответствующие legacy-функции/ветки/места УДАЛЕНЫ ИЛИ `panic!("[MIGRATED-<D>]")`.
   Грепом убедиться: работа D физически НЕ может пойти через legacy (нет тихого fallback).
3. **Legacy было РАЗБРОСАНО → новое СВЕДЕНО.** Legacy реализовывал эту фичу в РАЗНЫХ местах (несколько функций/
   веток). Убедиться ПО КОДУ, что ВСЕ они сведены в ОДНО окно — не осталось параллельного legacy-обработчика той
   же фичи в другом месте (грепнуть все места, что обслуживали D).
4. **Полное тест-покрытие ВСЕХ ситуаций.** Тесты покрывают ВСЕ возможные ситуации, которые legacy обрабатывал в
   разных местах (не только happy-path: edge-формы, generic/кросс-модуль/приватные варианты из матрицы 196).
   Перепроверить, что ни одна legacy-ситуация не осталась непокрытой.
5. **Гейты зелёные.** conformance 95/0 + byte-parity + `nova test std` без новых фейлов.

**Только при (1)-(5), подтверждённых оркестратором ПО КОДУ, статус D → ✅.** Иначе — вернуть агенту с
конкретикой (что не сведено / не покрыто / где ещё жив legacy).

## Инвентарь → D-фича (стартовая карта; волна-2 уточняет точные D-номера)

| Legacy-функция | D-фича | Тест покрывает |
|---|---|---|
| `infer_static_method_ret` | static-методы (D182/D372) | возврат `T.static()` по типам |
| `infer_generic_static_ctor_ret` | generic-static ctor (D372/D239) | `Vec[T].new()` / `[]T.static()` |
| `infer_method_level_return_for_sum` | методы sum (Option/Result/enum) | возврат метода sum-типа |
| `infer_lambda_return_type_with_params` | замыкания (D48) | вывод возврата closure |
| `infer_trailing_block_sig` | trailing-block (D43) | типы хвостового блока |
| `resolve_result_option_ret`, `resolve_result_te` | Result/Option (D30/D85) | распаковка `Result[T,E]`/`Option` |
| `infer_type_param_binding`×3, `infer_protocol_structural_binding`, `infer_result_type_params` | generics/протоколы (D16/D53/D72) | вывод type-param, protocol-dispatch |
| `resolve_method_level_subst`, `resolve_mono_type_args` | mono / method-generics (D119/D122) | подстановка / mono type-args |
| `compute_array_elem_type_for_obj` | элемент массива/слайса (D239) | тип элемента |
| `callnorm`/`argbind` | named-params + defaults (D102/D69) | арг-биндинг + default (generic-static ← Vec-fix ✅) |
| `primitive_instance_method_known` | методы примитивов (D109) | возвраты → из .nv-деклараций (maximize-nv-sourcing) |
| `type_ref_to_c` | тип→C (D315) | лоуэринг (покрыт всеми) |

## Партиция (без конфликта регионов)

**Область волны-2 = ВЕСЬ инвентарь второго окна КРОМЕ `infer_call_ret_c`** (это единолично волна-1/196.2). Все
сиблинг-`infer_*`/`resolve_*` + `callnorm`/`argbind` + method/static-резолверы + `primitive_instance_method_known`
+ `type_ref_to_c` + 56× `_c_type`-хелперы — ВСЕ ВНЕ диапазона 46293-48883 (разные регионы/файлы → merge-чисто).
**Сам диспетчер `infer_expr_c_type` удаляется на ФИНАЛЕ** (обе волны завершены). **mono-машинерия**
(`compute_mono_name`/`compute_generic_type_c_name`/`register_generic_instances`) НЕ мигрируется — остаётся в
codegen (целевая арх/rustc). **SHARED-хелперы** (зовутся из `infer_call_ret_c`): мигрировать логику в чекер,
удалять только когда волна-1 больше не зовёт (до того detach/panic). **Тесты — параллельны обеим.**

## Очередь диспетчеризации (режим: волна-1 × 1 + волна-2 × 8 агентов ПОСТОЯННО — владелец 2026-07-12, обновлено)

**Режим.** Оркестратор держит 1 агента на волне-1 (единственный регион `infer_call_ret_c` — больше нельзя без конфликтов) и до 8 агентов на волне-2 (предел непересекающихся регионов types/mod.rs + emit_c-функций) непрерывно. Как только агент отчитывается —
оркестратор (1) принимает работу ПО КОДУ (ПРИЁМКА (1)-(5) выше), (2) интегрирует ветку в main + обновляет
трекер/план централизованно (single-writer), (3) СРАЗУ выдаёт агенту следующую задачу из очереди. Цикл — до
полного закрытия волны-2 (все инвентарь-функции мигрированы + весь 307-трекер ✅). Падение агента = re-dispatch
той же задачи с чекпойнта (закоммиченное цело, незакоммиченное живёт в worktree). Задания — по одному,
файлово-непересекающиеся: ≤2 агента в `emit_c.rs` одновременно на ЗАВЕДОМО разнесённых функциях; ≥1 агент всегда
на std/conformance вне `emit_c.rs`. НЕ трогать зону волны-1 (`infer_call_ret_c` 46293-48883).

**Очередь (каждый пункт = одна инвентарь-функция → миграция в чекер + закрытие D + приёмка по коду):**
- [🔄 идёт] Агент-1: Ф.C — generic-static ctor + defaults (`callnorm`/`argbind`, #2/#10 карты) [nova-wave2]
- [🔄 идёт] Агент-2: std call-sites `.new().cap()`→`.new(cap:)` (consumer-сторона) [nova-capmig]
- [🔄 идёт] Агент-3: conformance coverage-fill (🔍→полное покрытие) [nova-spike]
- [Q1] `infer_static_method_ret` — static-методы D182/D372
- [Q2] `infer_method_level_return_for_sum` — sum-методы D30/D85
- [Q3] `infer_lambda_return_type_with_params` — closures D48 (учесть NovaOpt-from-int protяжку)
- [Q4] `infer_trailing_block_sig` — trailing-block D43
- [Q5] `resolve_result_option_ret` + `resolve_result_te` — Result/Option D30/D85
- [Q6] `compute_array_elem_type_for_obj` — элемент слайса/массива D239
- [Q7] `primitive_instance_method_known` — методы примитивов D109 (→ .nv-sourcing, maximize-nv)
- [Q8] `type_ref_to_c` — ретайр D315
- [Q9] generics/протоколы: `infer_type_param_binding`×3 + `infer_protocol_structural_binding` + `infer_result_type_params` — D16/D53/D72
- [🔒 заблокировано, sonnet] Q10: mono: `resolve_method_level_subst` + `resolve_mono_type_args` — D119/D122.
  Классифицированы: второе окно (unification-движки), Tier-2 СТРУКТУРНО заблокировано (нет per-call
  node_substs-канала; постройка трогает forbidden `f1_check_call`). Doc-комментарии добавлены, легаси не
  тронут. Подробности: §«Q10-подтверждение» выше, `wip/196.wave2-progress.md` §«Заблокированные D — Tier-2».
  Ждёт владельца (та же развилка (A)/(B), что у Q5).
- [Q+] std new(cap) rollout остальным типам (после Ф.C приземлится) + str→priv; верификация 307-трекера по коду
**Финал волны-2:** дисетчер `infer_expr_c_type` удаляется (обе волны пусты) → второе окно физически нет.

## Прогресс миграции волны-2 (приёмка оркестратором ПО КОДУ)

| D / функция | Статус | Закрыто в (функция нового пути) | Одно окно ✔ (оркестратор) | Доказательство (что сделано ВЕРНО — проверяемое) |
|---|---|---|---|---|
| D43 / `infer_trailing_block_sig` (29709) | ✅ | `infer_trailing_block_sig` стала тонким читателем: ret → `infer_expr_c_type` (диспетчер), params → `type_ref_to_c` (механическая AST-конверсия). Удалены: мёртвый `fn_ret_{name}` lookup + эвристический fallback `"void"→nova_int`; пустой блок → `nova_unit` | **✔** — своего второго окна НЕТ; сходится на диспетчер (финал диспетчера — на финале волны). Влито main `e6391b931` | **1)** conformance **95/0** — я перепроверил ИЗОЛИРОВАННО (release-бинарь nova-spike, env выставлен), исключив timeout-флак «94/1» под параллельной нагрузкой (лог `bechrslby.output`: `PASS 95 FAIL 0`). **2)** Код-диф `git show 27e8e5dd5`: старый `fn_ret_{name}` lookup = мёртвый (ключ вычислялся, никогда не читался) — удалён; больше НЕТ локального вывода типа. **3)** Пустой блок → `nova_unit` = ровно правило чекера (Block без trailing ⇒ Unit, types/mod.rs ~12807), не догадка. **4)** Silent-fallback `"void"→nova_int` удалён → работа НЕ может тихо пойти по легаси-эвристике. **5)** Targeted std vec **6/6 PASS**. |
| D239 / `compute_array_elem_type_for_obj` | 🔄 (обновлено sonnet, worktree `nova-196w2`, 2026-07-16) | `channel_array_elem_c` channel-first на всех вне-frozen сайтах. **Ретроспективная сверка** обнаружила: 5-сайтовый снос `.or_else`-fallback УЖЕ приземлился на main `979df4cce` (2026-07-13, conformance 97/0) — предыдущая строка этой таблицы (говорившая «`.or_else` fallback жив + 1 прямой вызов в замороженной зоне 48409») была СТАРОЙ (устарела уже на 2026-07-13, никогда не обновлялась после факта). Перепроверено ПО КОДУ этой сессией: заявленный вызов в frozen-зоне (`48409`/`46293-48883` старые координаты) **НЕ существует** — грепом 0 совпадений в текущем диапазоне `infer_call_ret_c` (49927-52022); либо снесён самой волной-1, либо координаты просто устарели по мере роста файла. **Новая находка + правка этой сессии:** `compute_array_elem_type_for_obj` имел ДВА вызова, не один — второй был байт-в-байт ДУБЛИКАТ `ExprKind::Index`-арма внутри `infer_expr_c_type`'s хвостового `match`, СТРУКТУРНО недостижимый (не эмпирически, а по control-flow: предшествующий «Channel 6k» — исчерпывающий `if let ExprKind::Index {..} = &expr.kind {..}`, каждая ветвь которого либо `return`, либо терминальный `panic!`, — гарантированно перехватывает и завершает выполнение ДО хвостового match для любого Index-выражения). Дубликат-арм (~130 строк) удалён; доккомент `compute_array_elem_type_for_obj` обновлён (было: «SHARED, нельзя удалять — вызывается из frozen-зоны»; стало: SEP, единственный вызывающий — Channel-6k fallback, живой из-за реального пробела канала на deep field-chain `obj.f1.f2.field[i]`). Влито main (5-сайтовый снос) `979df4cce`/`7e61446af`; текущая правка — некоммичена (этот заход) | **1)** Control-flow доказательство удаления дубликата — НЕ измерение, а структурный факт Rust `if let`+always-terminating-body (см. новый коммент в коде). **2)** `cargo build --release` (оба крейта, compiler-codegen + nova-cli) — чисто, без новых warning'ов. **3)** conformance-гейт (98/0 + byte-parity) **НЕ подтверждён эмпирически в этой сессии** — хост под severe contention (7+ параллельных чужих `nova.exe test` процессов одновременно из других worktree на этой машине в момент прогона: nova-cmpxchg/nova-161diag/nova-208impl/nova-200dur/nova-205end/nova-2061 и др.; попытки full-corpus и даже single-file прогона не завершились за ~10-30 минут каждая — воспроизводимо на 3 независимых попытках). Честно оставляю 🔄 (не флип на ✅) до подтверждения гейта при менее контендированном хосте. **5)** ЧЕСТНОЕ ограничение (почему всё ещё 🔄 даже без учёта гейта): единственный ОСТАВШИЙСЯ вызов `compute_array_elem_type_for_obj` (Channel-6k fallback) НЕ проверен на 0-hit — deep non-self field-access chain остаётся реальным пробелом покрытия канала (`channel_array_elem_c` читает только `resolved_types[obj.id]` для непосредственного `obj`, не рекурсивной цепочки полей), НЕ пытался его снимать в этом заходе. |

## ★★ СТРУКТУРНАЯ НАХОДКА (Q5, 2026-07-12) — граница Tier-1/Tier-2: чекер НЕ аннотирует Call-выражения

**Доказано ПО КОДУ + эмпирически (icr_trace, `NOVA_TRACE_ICR=1`):** миграция `resolve_result_option_ret`
(D30/D85) на channel-first СТРУКТУРНО невозможна в текущем «окне». Два независимых блокера:
1. **Оба вызывающих внутри замороженной зоны волны-1** (`B06a` ~46827, `B10j` ~47721 ∈ 46293-48883) —
   редактировать нельзя до финала волны-1.
2. **Канала для этого класса НЕТ.** `ty` здесь — ОБЪЯВЛЕННЫЙ возвратный тип каллиси (`TypeRef` из `FnDecl`,
   generic-пространство каллиси), НЕ `Expr` → нет `ExprId` для ключа `resolved_types`. Чекер (`f1_expr_inner`,
   `types/mod.rs` ~7233) аннотирует resolved-type для `Ident/SelfAccess/Index/Member/Range/RecordLit`, но для
   **`ExprKind::Call` — НИ ОДНОЙ записи** (грепом: `resolved_types_buf` не ключуется id самого Call).
   Channel-1 (`fn_ret_by_span`) тоже промахивается — пропускает generic-стабы (`debt_is_generic_stub_c`), а
   любой generic-возврат `Result[T,E]`/`Option[T]` — стаб.

**Следствие — ДВА ТИРА инвентаря волны-2:**
- **Tier-1 (мигрируемы СЕЙЧАС):** резолвят тип НЕ-Call выражения (Ident/Member/Index/…) или чистый TypeRef.
  Успехи: **D239** (elem-тип слайс-Expr), **D43** (trailing-block Expr). Вероятно также: `type_ref_to_c`
  (TypeRef→C, канал не нужен), generics-binding.
- **Tier-2 (ЗАБЛОКИРОВАНЫ фундаментом):** резолвят тип РЕЗУЛЬТАТА вызова (call/method-call return). Требуют,
  чтобы ЧЕКЕР писал per-call-site полностью подставленный `ResolvedType` в `resolved_types_buf[call.id]` для
  generic `Call` — это отдельная **ФАЗА КОЛЛЕКТОРА/mono** (rustc-эталон: mono = ФАЗА, не codegen-side-effect),
  = отложенный Stage-2/near-MIR. Кандидаты Tier-2: `resolve_result_option_ret`, `resolve_result_te`,
  `infer_static_method_ret`, `infer_method_level_return_for_sum`, mono-резолверы.

**Развилка (владельцу):** **(A)** построить канал Call-expr `ResolvedType` (фаза коллектора, rustc-aligned) —
крупно, но разблокирует БОЛЬШИНСТВО ядра волны-2 и ЯВЛЯЕТСЯ целевой архитектурой; **(B)** закрывать Tier-1
сейчас + Tier-2 отложить до постройки фундамента (волна-2 закрывает 196 ЧАСТИЧНО; полное — только с волной-1 +
фундаментом). Q5 оставила: матрицу покрытия `d30_result_option_ret_generic.nv` + `icr_trace`-доказательство
(коммит `4d9fc2298` на `cap-migration-200`).

### Q10-подтверждение (2026-07-12): mono-резолверы — Tier-2, но по ДРУГОЙ причине чем Q5

**Задача:** определить судьбу `resolve_mono_type_args` (emit_c.rs ~18421) и `resolve_method_level_subst`
(~19355) — D119/D122 (method-level type-params + mono type-args). Ранняя карта (`wip/196.wave2-progress.md`,
написана ДО Q5) разошлась во мнениях по этим двум функциям: `resolve_method_level_subst` → «➡ чекер» (SEP от
W1, миграция ожидалась простой), `resolve_mono_type_args` → «✅ остаётся (lowering-subst), читает канал» —
обоснование "rustc: mono = подстановка, не инференс".

**Q10 проверил ПО КОДУ и это обоснование НЕВЕРНО для Nova в текущем состоянии.** Разница с Q5: обе функции
принимают НЕ `Call`-Expr (у Q5-класса блокер — нет `ExprId` у Call), а `fn_decl` + `args: &[CallArg]` +
(опц.) `turbofish` — им нужен НЕ `resolved_types[call.id]`, а собственно ЗНАЧЕНИЕ подстановки T/U, вычисленное
УНИФИКАЦИЕЙ param-`TypeRef` против arg-C-типа (400+/240+ строк, Sources 1..4 с ~дюжиной ad-hoc веток из
независимых планов 48/54/55/72/91/98/101/109/172.13/186/M-property-testing-rot). Это ТА ЖЕ unification, которую
`f1_check_call` (`types/mod.rs` ~10242, forbidden-зона Q10, маркер 10452 лежит буквально внутри) обязан
выполнить для type-check generic-вызова — но **не сохраняет**: `typeref_mentions_any`-guard (~10478) явно
пропускает generic-возврат, комментарий чекера сам это признаёт («they'd need type-subst, not available
here»). У rustc `mono = подстановка` работает ТОЛЬКО потому, что `typeck` уже пишет `node_substs` — у нас
эквивалентного канала (per-call `ExprId → Vec<ResolvedType>`) НЕТ, значит то, что делают эти две функции
СЕЙЧАС, суть повторный ИНФЕРЕНС под видом lowering, а не механическая подстановка.

**Прямое кодовое доказательство второго окна** (не гипотеза): `resolve_mono_type_args` Source 4 несёт
комментарий «Mirror of the identical fix in `resolve_method_level_subst` (Step 3) and the inline
instance-method dispatch block — **three independent, hand-duplicated inference engines** all needed the
same fix» — прежние инженеры сами зафиксировали, что один и тот же баг чинился синхронно в ТРЁХ местах.
Это canonical-симптом второго окна per 196-доктрине.

**Вердикт: Tier-2, СТРУКТУРНО ЗАБЛОКИРОВАНО** — как и у Q5-класса, но блокер здесь — отсутствие
per-call node_substs-канала (не отсутствие ExprId у Call). Постройка канала требует правки
`f1_check_call`/instance-method sibling (types/mod.rs) — Q10-forbidden зона, вне scope одного окна. Решение о
постройке — за владельцем, та же развилка (A)/(B) что у Q5. **Действие этого захода:** doc-комментарии на
обеих функциях (emit_c.rs) фиксируют вердикт + ссылку на этот раздел; легаси НЕ тронут (behavior-preserving);
`wip/196.wave2-progress.md` строки 61-62 и итоговая сводка — исправлены (см. там). Тест-покрытие D119/D122
перепроверено — пробелов нет (`d119_method_level_type_params`, `d122_bound_method_mono_dispatch`,
`d122_generic_bound_forwarding`, `d123_tuple_mono`, `d277_generic_value_record_mono`,
`d354_generic_anon_tuple_mono` — все 6 файлов существуют в `spec_tests/conformance/`).

| П5 / `[M-vec-new-static-arity-overload]` (callnorm + emit_c ~32605) | ✅ | **(0) одно окно: через `pick_static_params`** (callnorm, overload-aware дизамбиг по bind_call_args) + turbofish-ветка per-overload mono (`resolved_callees`-span чекера, схема 5b) | **✔** — static_methods больше не выбрасывает overload-пары; 0-арг/3-арг `new` различаются структурно; фолд `from_raw_parts`→`new(ptr,len,cap)` (форма владельца, requires len>=0&&cap>=len); лint W_RETIRED_NAME банит старое имя | **1)** `vec_of_empty_panic` neg PASS (репро cross-wiring). **2)** `nova test --full std/collections/vec` — folder-CU (сам класс бага) PASS. **3)** conformance 95/0 (--jobs 4; 3 TIMEOUT при -j16 изолированно PASS = контенция). **4)** спек-амендменты 02-types ПОПРАВКА-2 + 08-runtime D26/D232 в том же слиянии. main `6d0c24447` |
| D239-fallback снятие (follow-up) | ⛔ откат (ИСТОРИЧЕСКИЙ; см. успешный повтор ниже) | — | — fallback ЖИВ: снятие `.or_else` на 4 вне-frozen сайтах → 93/2 (D102 `StringBuilder.new(128)` + d371 cap-арность) — канал `channel_array_elem_c` НЕ покрывает контейнер-cap кейсы. Повторить ПОСЛЕ Stage-1b/расширения канала | **Честный красный** (haiku, protocol: попробовал→откатил→точный отчёт): 4 сайта сняты → conformance 93/2 → полный откат до `bdf880c10`. D239 остаётся 🔄. **Апдейт (сверено sonnet 2026-07-16):** повторная попытка ПОСЛЕ Stage-1a УДАЛАСЬ — main `979df4cce`/`7e61446af` (2026-07-13) сняли `.or_else`-fallback на всех 5 вне-frozen сайтах чисто, conformance 97/0 (эта таблица не была обновлена по факту тогда — расхождение обнаружено и исправлено этой сессией, см. строку D239 в «Прогресс миграции волны-2» выше). |
| Q7 D109 / `primitive_instance_method_known` (~43001) | ✅ prune | Канал `method_overloads` → `self.sig.method_table` (.nv-декларации первичны; хардкод был мёртвым дублем для str-методов) | **✔** — str-методы (to_upper/trim/concat/split/…) физически удалены из хардкода; остались ТОЛЬКО обоснованные интринсики (bootstrap prim-методы D109-doc, extern-математика f64 — в `ExternalRegistry`, не в method_table, lang-items str D117/D249) | **1)** conformance 95/0. **2)** Эмпирика: удаление f64-ветки → `(9.0).sqrt()` даёт `[E_UNKNOWN_METHOD]` → откачено+doc (доказательство, что f64 НЕ дубль). **3)** +2 теста: `d109_primitive_methods_f64_f32_math` (f32 ранее не покрыт!), `d109_primitive_methods_str_nv_sourced`. main `0830664d6` |
| D315 / `type_ref_to_c` ×3 | ✅ | Канон = `CEmitter::type_ref_to_c` → `resolved_type_to_c`; `external_registry` делегирует `CEmitter::primitive_name_to_c` | **✔** — устранён РЕАЛЬНЫЙ 4-й хардкод-дубль prim-таблицы (15 строк побайтово идентичны); 2 других варианта обоснованы по коду (pre-CEmitter bootstrap / намеренное схлопывание ширин) + doc | **1)** conformance 95/0. **2)** targeted std net/sync/vec (проходят через ExternalRegistry-путь). **3)** Диф: `external_registry.rs` −15 строк дубля → делегация. main `4c02d346d` |
| Ф.C / callnorm+`check_keyword_only` (D102/D372) | ✅ | `try_normalize_call` (self_type-резолв `Self.new()` в generic-static методах) + `check_keyword_only` (канонич. cap-ctor позиционно легален — единое правило для всех 6 типов) | **✔** — default-backfill сходится на СУЩЕСТВУЮЩИЙ static_methods-путь (не новый резолвер); callnorm-детур (peer_files-скан) диагностирован и ОТКАЧЕН (обнулял static_methods) | **1)** conformance 95/0, d372-тест зелёный (был ICE). **2)** Repro-цепь: `Set.from_iter`'s `Self.new()` работает; `HashMap/Queue/Set.new(64)` позиционно компилятся. **3)** Найден+починен reachability-gap: Queue не prelude-auto → import в тест (изолированный repro). main `bdf880c10` |
| 196.4 Stage-1a / фундамент | ✅ | `resolve_return_channel` (types/mod.rs 9594) + новая ветка `resolve_instance_method_return_arity` (~13842): method-generic возврат материализуется в `resolved_types[call.id]` | **✔** — флип SHADOW→authoritative ТОЧЕЧНО (только новый класс, ранее ВСЕГДА бейлил → регресс невозможен по построению); carrier-путь сознательно не флипнут (non-injectivity `[]T→Vec[T]`, doc) | **1)** conformance 95/0 debug+release (SHADOW-parity assert не падает). **2)** Гейт propose-then-verify В КАЖДОЙ сборке: материализация только при независимом согласии solver-канала. **3)** Smoke: `Option.ok_or` → `Result[int,str]` вместо residual `TypeParam(E)` (runtime-верно). main `9b637776e` |
| Волна-1 (196.2) баланс | 🔄 | 16 атомов снято post-16: Path-формы B12c/e/f/j/k/m + B11aj/b + B12g/i/n; итого 25/114 веток, тело −115 строк | — (релокация 56 живых — через фундамент, идёт) | **1)** GATE-1 на каждом снятии: conformance 95/95 после каждого. **2)** NO-HIT-доказательство по трейс-файлам корпуса (conformance+std+examples+nova_tests). **3)** main перепроверен 95/0 после merge. main `1ac6b37dd` |

| receiver_c_type + builtin_sum_receiver_c_type (15637/15680) | 🔄 Tier-3 | — (миграция N/A в этом окне) | **Tier-3: канала НЕТ by construction** — `Receiver` = ДЕКЛАРАЦИЯ метода (ast без `id`), не Expr; `resolved_types` ключуется ExprId. Все 11 вызывающих передают `type_name`-строку. Канал для recv-ВЫРАЖЕНИЙ (`recv_c_type_materialized`, 46261) — другая задача (диспатч вызова, не ABI nova_self). Retire запланирован в «receiver-aware resolved_type_to_c» (U.4.5/FIN, doc в самой функции). «No new window §3» запрещает синтетический канал | **(0)** generic-subst внутри УЖЕ идёт через `resolved_type_to_c` (subst_c→subst_val_c) — подтверждено тестами. **1)** +12 юнит-тестов (примитив/sum/generic/кросс-модуль матрица), 36/36 PASS. **2)** conformance 95/0. **3)** cargo test 981 PASS (4 фейла предсущ., верифицировано на чистом bdf880c10 stash-тестом). main-merge |

**Правило приёмки (напоминание):** ✅ ставлю ТОЛЬКО когда легаси физически не может выполниться (нет fallback) И сведение полное. `.or_else`-fallback = ещё 🔄. Колонку **«Доказательство»** заполняю на КАЖДОМ закрытии проверяемыми фактами — ОБЯЗАТЕЛЬНАЯ структура (владелец 2026-07-12):
**(0) ОДНО ОКНО: через `<функция нового пути>`** — явная проверка по коду, что работа D теперь идёт через НОВОЕ одно окно (конкретная функция чекера/канала, та же что в колонке «Закрыто в»), и легаси-путь для мигрированных вызывающих физически не выполняется (удалён/detach/канал-first без достижимого fallback);
(1) conformance-tally + лог; (2) код-диф (коммит); (3) подтверждённые call-сайты; (4) targeted-std. Не «на слово агента» — перепроверяю по коду и логам. Для уже заполненных строк выше пункт (0) = содержимое колонок «Закрыто в» + «Одно окно ✔» (функция нового пути названа там; далее дублирую его внутрь Доказательства).

## Приёмка волны-2 (итог)

Закрытие 196 = полная волна-2 (весь инвентарь) + пустая `infer_call_ret_c` (волна-1); структурный финал-гейт (`infer_call_ret_c` пуст/удалён, сиблинги через одно окно; «встреча волн» как критерий удалена — владелец 2026-07-13); матрица
«одного окна» (196) зелёная; codegen ничего не перевыводит; LSP читает каналы.

## Таблица регресс-baseline + трекер закрытия (307 conformance-файлов)

**Статус:** ⬜ не начато · 🔍 **покрытие тестами ПРОВЕРЕНО по D** — исход один из двух: **всё ОК** (пробелов по
плану нет) ЛИБО **нашёл и добавил** недостающие тест-файлы (БЕЗ фиксов компилятора / БЕЗ прогонов-проверок) ·
🔄 миграция в работе · ✅ закрыт (приёмка (1)-(5) пройдена ПО КОДУ). В колонке «Закрыто в» на шаге 🔍 — либо
«coverage-ok», либо список добавленных тест-файлов.
**Закрыто в** — конкретная функция НОВОГО пути (чекер/канал), где D теперь резолвится (заполняет волна-2, сверяет
оркестратор). Волна-2 мапит каждый d<NNN> на инвентарь-функцию, покрывает/проверяет тесты всех ситуаций,
мигрирует, закрывает — флип статуса + запись «Закрыто в».
**Одно окно ✔ (проверено оркестратором по коду)** — заполняется ТОЛЬКО оркестратором при приёмке ✅, НЕ агентом.
Значение = вердикт ПО КОДУ (греп+чтение), а не по отчёту агента: D резолвится ОДИН раз в чекере → канал → codegen
ЧИТАЕТ (не перевыводит); legacy для D физически мёртв (удалён / `panic!("[MIGRATED-<D>]")`, тихого fallback нет);
все ранее разбросанные legacy-места сведены в ОДНО окно; покрытие полное (все ситуации матрицы 196). `✔` = приёмка
(1)-(5) подтверждена мной по коду; `—` = ещё не принято (миграции/закрытия нет — 🔍 значит лишь coverage-проверено).
Эта колонка ОТДЕЛЯЕТ «покрыто тестами» (🔍, «Закрыто в») от «закрыто через одно окно в правильном месте и лично
проверено оркестратором» (✅).

### Позитивные (231 — +1 добавлен волной-2)

| D-файл | Статус | Закрыто в (функция нового пути) | Одно окно ✔ (проверено оркестратором по коду) |
|---|---|---|---|
| c_keyword_ident_mangling | 🔍 | coverage-ok | — |
| d102_named_args_default_params | ✅ | `try_normalize_call` (self_type+default-backfill) + `check_keyword_only` (позиц. cap-ctor) | ✔ main `bdf880c10` |
| d102_static_generic_static_defaults | ✅ | `try_normalize_call` (self_type+default-backfill) + `check_keyword_only` (позиц. cap-ctor) | ✔ main `bdf880c10` |
| d104_doc_comment_syntax | 🔍 | coverage-ok | — |
| d108_from_pairs_user_type | 🔍 | coverage-ok | — |
| d108_map_literal | 🔍 | coverage-ok | — |
| d109_primitive_builtin_methods | ✅ | `method_overloads`→`sig.method_table` (.nv-первичны; existence-прун; bootstrap-интринсики doc) | ✔ main `0830664d6` |
| d110_ghost_state_bindings | 🔍 | coverage-ok | — |
| d117_size_accessors_call | 🔍 | coverage-ok | — |
| d119_method_level_type_params | ✅ | producers-widen (f07cd0991) закрыл классы 1-2 канала; closure-lowering-fix закрыл хвост — чекер материализовал ложный `Func{ret:Named(T)}` для замыкания-аргумента (фикс: subst вызова перед материализацией, честный пропуск при residual); репро d30_closure_return_generic зелёный | ✔ main (merge closure-lowering-fix; [notes](wip/196.5-closure-lowering-notes.md)) |
| d122_bound_method_mono_dispatch | ✅ | node_substs дожат producers-widen (класс-2: продюсер-A пишет всегда при непустых generics); d122-сайты 0/6→8/8 producer=A + consumer hit; erased-body caller (`d122f_leaf`) — канал честно молчит, корректно | ✔ main `f07cd0991` (трейс в [notes](wip/196.5-producers-widen-notes.md)) |
| d122_generic_bound_forwarding | ✅ | то же (класс-2 закрыт, 8/8 hit, mismatch=0) | ✔ main `f07cd0991` |
| d123_tuple_mono | 🔍 | coverage-ok | — |
| d124_wall_monotonic_separation | 🔍 | coverage-ok | — |
| d128_char_distinct_codepoint | 🔍 | coverage-ok | — |
| d129_assoc_const_width | 🔍 | coverage-ok | — |
| d129_int_i64_alias | 🔍 | coverage-ok | — |
| d130_uint_inferred_arith | 🔍 | coverage-ok | — |
| d130_uint_literal_width | 🔍 | coverage-ok | — |
| d130_uint_method_compare | 🔍 | coverage-ok | — |
| d131_consume_qualifier | 🔍 | coverage-ok | — |
| d132_fluent_return | 🔍 | coverage-ok | — |
| d133_consume_type_must_consume | 🔍 | coverage-ok | — |
| d135_bare_generic_inferred | 🔍 | coverage-ok | — |
| d13_with_fail_recoverable_vs_panic | 🔍 | coverage-ok | — |
| d141_ptr_bitcast_roundtrip | 🔍 | coverage-ok | — |
| d141_ptr_read_write_unaligned | 🔍 | coverage-ok | — |
| d142_protocol_literal_symmetry | 🔍 | coverage-ok | — |
| d143_protocol_static_method | 🔍 | coverage-ok | — |
| d144_sub_slice_views | 🔍 | coverage-ok | — |
| d145_fn_prefix_receiver_generic | 🔍 | coverage-ok | — |
| d156_generic_consume_bound | 🔍 | coverage-ok | — |
| d157_view_default_closures | 🔍 | coverage-ok | — |
| d158_failable_defer_body | 🔍 | coverage-ok | — |
| d158_suppressed_pocket | 🔍 | coverage-ok | — |
| d15_structural_compat | 🔍 | coverage-ok | — |
| d161_multi_defer_lifo | 🔍 | coverage-ok | — |
| d162_consume_defer_cover | 🔍 | coverage-ok | — |
| d164_cross_module_consume | 🔍 | coverage-ok | — |
| d168_sized_atomics | 🔍 | coverage-ok | — |
| d169_mutex_family | 🔍 | coverage-ok | — |
| d16_generics_brackets | 🔍 | coverage-ok | — |
| d170_coordination_primitives | 🔍 | coverage-ok | — |
| d171_once_family | 🔍 | coverage-ok | — |
| d172_realtime_blocking_attrs | 🔍 | coverage-ok | — |
| d174_sync_consume_guards | 🔍 | coverage-ok | — |
| d175_ro_field_freeze | 🔍 | coverage-ok | — |
| d176_ro_type_modifier | 🔍 | coverage-ok | — |
| d179_stringbuilder_cross_fn_consume | 🔍 | coverage-ok | — |
| d17_type_decls_punning | 🔍 | coverage-ok | — |
| d181_fluent_mut_chain | 🔍 | coverage-ok | — |
| d182_self_return_parametric_static | ✅ | `infer_static_method_ret` (legacy static-return хелпер, старые коорд. 44728) УДАЛЁН ЦЕЛИКОМ — не detach/panic, а полный снос: оба caller'а (Member/Ident-twin `B11t_static_method_ret_ident` и Path-twin `B12a_path_static_method_ret`) сняты как NO-HIT на conformance+std/{collections,data,net} ДО удаления самой функции (Stage-D волна-1, `9b63fd145`). Static-return для serde `deserialize`-контракта и прочих измеренных кейсов материализуется в `resolved_types` РАНЬШЕ `infer_call_ret_c` (Channel-2 дispatcher-уровня перехватывает раньше legacy) | ✔ Zone RET сверила ПО КОДУ 2026-07-17: `grep -c "fn infer_static_method_ret"` = 0 в текущем `emit_c.rs` (только REMOVED-докоммент на месте функции, ссылается на `docs/plans/wip/196.5-stage-d-notes.md` §3.2); 0 вызовов `infer_static_method_ret(` где-либо в файле; targeted `nova test` d182_self_return_parametric_static + d143_protocol_static_method + static_ctor_instance_method_named_tuple + d372_canonical/types_generic_static_ctor — PASS (см. `wip/196-ret-notes.md` для полного гейт-лога, включая honest-red про НЕСВЯЗАННЫЙ блокер conformance-мега-CU и его фикс this session). main `9b63fd145` (Stage-D, НЕ 196-flотская ветка — Q1 закрылся ДО раздачи карты 196-campaign-map, трекер был не в курсе) |
| d183_canonical_comparison_protocols | 🔍 | coverage-ok | — |
| d186_impl_annotation | 🔍 | coverage-ok | — |
| d188_consumable_scope_block | 🔍 | coverage-ok | — |
| d189_defer_scope_exit | 🔍 | coverage-ok | — |
| d194_consumable_never_hot_path | 🔍 | coverage-ok | — |
| d196_consume_scope_init_forms | 🔍 | coverage-ok | — |
| d199_const_fn_comptime | 🔍 | coverage-ok | — |
| d19_match_arrow | 🔍 | coverage-ok | — |
| d200_associated_const | 🔍 | coverage-ok | — |
| d209_protocol_method_at_recv_mut | 🔍 | coverage-ok | — |
| d20_unit_type_arrows | 🔍 | coverage-ok | — |
| d214_opaque_ptr_handle | 🔍 | coverage-ok | — |
| d215_named_tuple_eq | 🔍 | coverage-ok | — |
| d215_named_tuple_fluent | 🔍 | coverage-ok | — |
| d215_named_tuple_value | 🔍 | coverage-ok | — |
| d216_ptr_methods_174_5 | 🔍 | coverage-ok | — |
| d216_uninit_rename_174_5 | 🔍 | coverage-ok | — |
| d216_unused_unsafe_pos | 🔍 | coverage-ok | — |
| d220_priv_field_visibility | 🔍 | coverage-ok | — |
| d221_pattern_destructure_spread | 🔍 | coverage-ok | — |
| d222_priv_record_protocol | 🔍 | coverage-ok | — |
| d224_priv_escape_hatches | 🔍 | coverage-ok | — |
| d226_signed_len_arith | 🔍 | coverage-ok | — |
| d227_numeric_literal_inference | 🔍 | coverage-ok | — |
| d228_value_record_copy_contract | 🔍 | coverage-ok | — |
| d229_debug_format_spec | 🔍 | coverage-ok | — |
| d22_closures | ✅ | `closure_channel_ret_c` + чекер `ResolvedType::Func` для ClosureFull; легаси-ветка СНЯТА | ✔ main `c4818131c` |
| d230_clone_deep_autoderive | 🔍 | coverage-ok | — |
| d231_rawmem_alloc | 🔍 | coverage-ok | — |
| d232_vec_growable | 🔍 | coverage-ok | — |
| d232_vec_methods | 🔍 | coverage-ok | — |
| d237_protocol_method_naming | 🔍 | coverage-ok | — |
| d237_value_record_user_equal | 🔍 | coverage-ok | — |
| d238_index_protocol | 🔍 | coverage-ok | — |
| d239_slice_vec_alias | 🔄 | `channel_array_elem_c` (resolved_types[obj.id]) channel-first на всех вне-frozen сайтах (5-сайтовый снос `.or_else` уже приземлился main `979df4cce`, эта строка была не обновлена по факту — исправлено 2026-07-16). Остающийся живой fallback (Channel-6k, deep field-chain) — один вызов, не проверен на 0-hit | — гейт (98/0) на текущей правке (дубликат-арм prune) не подтверждён эмпирически в этой сессии (host contention), см. «Прогресс миграции волны-2» D239-строку |
| d23_return_early | 🔍 | coverage-ok | — |
| d240_mut_index_protocol | 🔍 | coverage-ok | — |
| d241_type_modifier_order | 🔍 | coverage-ok | — |
| d246_mutability_axes_split | 🔍 | coverage-ok | — |
| d246_param_ro_mut_view | 🔍 | coverage-ok | — |
| d248_split_bounds_predicate | 🔍 | coverage-ok | — |
| d249_str_coordinate_model | 🔍 | coverage-ok | — |
| d250_chars_iter | 🔍 | coverage-ok | — |
| d251_str_surface | 🔍 | coverage-ok | — |
| d252_char_ascii_api | 🔍 | coverage-ok | — |
| d254_str_byte_compare | 🔍 | coverage-ok | — |
| d255_utf16_interop | 🔍 | coverage-ok | — |
| d256_contract_self_field | 🔍 | coverage-ok | — |
| d259_vec_of_vs_from | 🔍 | coverage-ok | — |
| d25_throw_fail | 🔍 | coverage-ok | — |
| d260_lazy_boxiter | 🔍 | coverage-ok | — |
| d262_slice_ops | 🔍 | coverage-ok | — |
| d263_vec_concat_plus | 🔍 | coverage-ok | — |
| d264_vec_hash_collect_target | 🔍 | coverage-ok | — |
| d267_method_coherence_extension | 🔍 | coverage-ok | — |
| d268_opt_in_conformance_impl | 🔍 | coverage-ok | — |
| d275_if_match_unit_coerce | 🔍 | coverage-ok | — |
| d277_generic_value_record_mono | 🔍 | coverage-ok | — |
| d27_fixed_array | 🔍 | coverage-ok | — |
| d280_heap_ref_pointer_size | 🔍 | coverage-ok | — |
| d281_module_priv_field | 🔍 | coverage-ok | — |
| d282_ffi_abi | 🔍 | coverage-ok | — |
| d284_enumerate_index | 🔍 | coverage-ok | — |
| d289_import_last_segment | 🔍 | coverage-ok | — |
| d290_value_record_iterators | 🔍 | coverage-ok | — |
| d294_str_as_ptr | 🔍 | coverage-ok | — |
| d299_as_slice_append | 🔍 | coverage-ok | — |
| d300_vec_body_tuple_fwddecl | 🔍 | coverage-ok | — |
| d307_file_private_visibility | 🔍 | coverage-ok | — |
| d30_try_op_unwrap_pair | 🔄 | `channel_result_type_params_c` channel-first; Q5-класс за Call-каналом. **Верифицировано 2026-07-13** ([отчёт](wip/196.5-perd-d30-verification.md)): сам файл — конкретные типы, канал N/A; Q5-класс (`resolve_result_option_ret`/`resolve_result_te`) node_substs НЕ читает — оба caller'а (B06a/B10j) в замороженной зоне волны-1 → закрытие = Stage-D, не пробел канала. Матрица d30_result_option_ret_generic.nv — страховочная ветка `keep/d30-matrix` (4d9fc2298), воспроизводит CC-FAIL closure-return (fallback-класс-1) | — |
| d310_type_set_bound | 🔍 | coverage-ok | — |
| d315_resolvedtype_lossless_canon | ✅ | `CEmitter::type_ref_to_c`→`resolved_type_to_c` (4-й дубль снят) | ✔ main `4c02d346d` |
| d316_time_effect_typed_surface | 🔍 | coverage-ok | — |
| d317_duration_overflow_policy | 🔍 | coverage-ok | — |
| d318_monotonic_non_regression | 🔍 | coverage-ok | — |
| d325_result_everywhere | 🔍 | coverage-ok | — |
| d326_chain_gating_ro_at | 🔍 | coverage-ok | — |
| d326_fluent_value_argpos | 🔍 | coverage-ok | — |
| d326_fluent_value_return | 🔍 | coverage-ok | — |
| d326_fluent_value_stmt | 🔍 | coverage-ok | — |
| d326_mode_overload_axis | 🔍 | coverage-ok | — |
| d326_ref_heap_norm | 🔍 | coverage-ok | — |
| d326_reflocal | 🔍 | coverage-ok | — |
| d326_value_record_fluent | 🔍 | coverage-ok | — |
| d327_codepoint_u32 | 🔍 | coverage-ok | — |
| d328_value_record_eq | 🔍 | coverage-ok | — |
| d32_param_passing_mut_ref_vs_value | 🔍 | coverage-ok | — |
| d337_brotli_ffi | 🔍 | coverage-ok | — |
| d33_immutability_axes | 🔍 | coverage-ok | — |
| d347_same_scope_rebinding | 🔍 | coverage-ok | — |
| d34_pattern_bind_conditions | 🔍 | coverage-ok | — |
| d354_generic_anon_tuple_mono | ✅ | B1 `resolve_mono_type_args_ch` → node_substs (канал) | ✔ пер-D верификация: 4/4 hit, mismatch=0, изоляционный прогон (wip/196.5-perd-verification.md, c7f012f63) | coverage-ok | — |
| d355_blanket_protocol | 🔍 | coverage-ok | — |
| d35_instance_method_at_syntax | 🔍 | coverage-ok | — |
| d363_operator_dispatch_protocols | 🔍 | coverage-ok | — |
| d364_value_record_handle_wrap | 🔍 | coverage-ok | — |
| d367_u8_canonical | 🔍 | coverage-ok | — |
| d368_generic_return_width | 🔍 | coverage-ok | — |
| d368_no_int_collapse | 🔍 | coverage-ok | — |
| d36_field_default_mutable | 🔍 | coverage-ok | — |
| d372_canonical_new_defaults | ✅ | то же + `pick_static_params` (overload-aware) + фолд new(ptr,len,cap) | ✔ main `bdf880c10`/`6d0c24447` |
| d373_generic_array_api | 🔍 | coverage-ok | — |
| d374_write_sink_decouple | 🔍 | coverage-ok | — |
| d37_field_access_dot_syntax | 🔍 | coverage-ok | — |
| d38_array_creation | 🔍 | coverage-ok | — |
| d39_embed_delegation | 🔍 | coverage-ok | — |
| d401_match_sized_arm_width | 🔍 | coverage-ok | — |
| d402_closure_return_width | ✅ | closures через канал (см. d22) | ✔ main `c4818131c` |
| d403_vec_sized_element_read | 🔍 | coverage-ok | — |
| d404_sized_arith_width | 🔍 | coverage-ok | — |
| d406_as_cast_roundtrip_width | 🔍 | coverage-ok | — |
| d406_enum_kind_token | 🔍 | coverage-ok | — |
| d407_enum_payload_width | 🔍 | coverage-ok | — |
| d408_option_chain_sized_width | 🔍 | coverage-ok | — |
| d409_nested_record_sized_field | 🔍 | coverage-ok | — |
| d409_self_return_auto | 🔍 | coverage-ok | — |
| d40_function_body_arrow_vs_brace | 🔍 | coverage-ok | — |
| d410_tuple_destructure_sized | 🔍 | coverage-ok | — |
| d411_array_sized_elem_width | 🔍 | coverage-ok | — |
| d411_record_binding_destructure | 🔍 | coverage-ok | — |
| d412_default_const_sized_width | 🔍 | coverage-ok | — |
| d412_hex_blob_embed | 🔍 | coverage-ok | — |
| d413_if_expr_sized_branch_width | 🔍 | coverage-ok | — |
| d42_structural_protocol | 🔍 | coverage-ok | — |
| d43_trailing_block_and_fn | ✅ | `infer_trailing_block_sig`=тонкий читатель → `infer_expr_c_type` (ре-деривация удалена) | ✔ main `e6391b931` |
| d44_numeric_literals | 🔍 | coverage-ok | — |
| d45_inferred_return_type | 🔍 | coverage-ok | — |
| d46_operator_overload_at_methods | 🔍 | coverage-ok | — |
| d48_tagged_template | ✅ | closures через канал (см. d22) | ✔ main `c4818131c` |
| d49_statement_separator_newlines | 🔍 | coverage-ok | — |
| d52_sumint | 🔍 | coverage-ok | — |
| d52_type_forms | 🔍 | coverage-ok | — |
| d53_type_protocol_kind_token | 🔍 | coverage-ok | — |
| d54_as_cast_conversions | 🔍 | coverage-ok | — |
| d55_hashmap_from_fields | ✅ | `assignable` single-wrap + `MapLitAnnotator` (sum+newtype, литералы+переменные, tag); спек-амендмент | ✔ main `f72a27c8b` |
| d55_literal_coercion | ✅ | `assignable` single-wrap + `MapLitAnnotator` (sum+newtype, литералы+переменные, tag); спек-амендмент | ✔ main `f72a27c8b` |
| d55_recordlit_callarg | ✅ | `assignable` single-wrap + `MapLitAnnotator` (sum+newtype, литералы+переменные, tag); спек-амендмент | ✔ main `f72a27c8b` |
| d55_sized_literal_contexts | ✅ | `assignable` single-wrap + `MapLitAnnotator` (sum+newtype, литералы+переменные, tag); спек-амендмент | ✔ main `f72a27c8b` |
| d58_range_for_iter | 🔍 | coverage-ok | — |
| d59_array_tuple_partial_patterns | 🔍 | coverage-ok | — |
| d60_spread_literals | 🔍 | coverage-ok | — |
| d66_self_universal | 🔍 | coverage-ok | — |
| d69_variadic_params | 🔍 | coverage-ok | — |
| d72_generic_bound_protocol | 🔍 | coverage-ok | — |
| d74_math_methods | 🔍 | coverage-ok | — |
| d83_keywords_not_identifiers | 🔍 | coverage-ok | — |
| d84_function_overloading_axes | 🔍 | coverage-ok | — |
| d85_question_return | 🔄 | то же (Result/Option каналы частично). Верифицировано 2026-07-13: файл — конкретные типы, канал N/A по конструкции; Q5 → Stage-D (см. d30-строку + [отчёт](wip/196.5-perd-d30-verification.md)) | — |
| d85_result_payload_width | 🔄 | то же (Result/Option каналы частично). Верифицировано 2026-07-13: файл — конкретные типы, канал N/A по конструкции; Q5 → Stage-D | — |
| d86_coalesce_operator | 🔍 | coverage-ok | — |
| d86_coalesce_width | 🔍 | coverage-ok | — |
| d86b_coalesce_fallback_width | 🔍 | coverage-ok | — |
| d88_default_generic_params | 🔍 | coverage-ok | — |
| d90_defer_cleanup | 🔍 | coverage-ok | — |
| dispatch_receiver_type_vs_name | 🔍 | coverage-ok | — |
| m178_variant_ctor_crosssum_option_collision | 🔍 | coverage-ok | — |
| match_tuple_option_value | 🔍 | coverage-ok | — |
| module_const_instance_method | 🔍 | coverage-ok | — |
| no_prelude_panic_assert/d13_no_prelude_panic_assert_intrinsic | 🔍 | coverage-ok | — |
| partial_prelude/d371_partial_prelude_collections | 🔍 | coverage-ok | — |
| plan174_1_typed_numeric_parse | 🔍 | coverage-ok | — |
| slice_static_generic_method | 🔍 | coverage-ok | — |
| static_ctor_instance_method_named_tuple | 🔍 | coverage-ok | — |
| types_generic_static_ctor | 🔍 | coverage-ok (декларация; runtime default-упражнение → d102_static_generic_static_defaults) | — |

### Негативные `neg/` (77)

| D-файл | Статус | Закрыто в (функция нового пути) | Одно окно ✔ (проверено оркестратором по коду) |
|---|---|---|---|
| neg/d032_param_passing | 🔍 | coverage-ok | — |
| neg/d124_monotonic_as_unix_secs | 🔍 | coverage-ok | — |
| neg/d124_monotonic_minus_timestamp | 🔍 | coverage-ok | — |
| neg/d128_char_distinct_from_int | 🔍 | coverage-ok | — |
| neg/d133_branch_maybe_consumed | 🔍 | coverage-ok | — |
| neg/d133_field_marker_missing | 🔍 | coverage-ok | — |
| neg/d133_not_consumed | 🔍 | coverage-ok | — |
| neg/d156_for_consume_skip | 🔍 | coverage-ok | — |
| neg/d156_strict_forget | 🔍 | coverage-ok | — |
| neg/d157_closure_double_invoke | 🔍 | coverage-ok | — |
| neg/d157_view_consume_call | 🔍 | coverage-ok | — |
| neg/d158_defer_fail_no_sig | 🔍 | coverage-ok | — |
| neg/d158_defer_return | 🔍 | coverage-ok | — |
| neg/d162_no_cover | 🔍 | coverage-ok | — |
| neg/d172_blocking_park | 🔍 | coverage-ok | — |
| neg/d172_realtime_park | 🔍 | coverage-ok | — |
| neg/d172_realtime_via_fn | 🔍 | coverage-ok | — |
| neg/d174_double_unlock | 🔍 | coverage-ok | — |
| neg/d174_mutexguard_leak | 🔍 | coverage-ok | — |
| neg/d175_ro_field_freeze | 🔍 | coverage-ok | — |
| neg/d192_cleanup_timeout_retracted | 🔍 | coverage-ok | — |
| neg/d196_init_not_consumable | 🔍 | coverage-ok | — |
| neg/d199_effect_in_body | 🔍 | coverage-ok | — |
| neg/d199_non_const_arg | 🔍 | coverage-ok | — |
| neg/d199_param_mut_conflict | 🔍 | coverage-ok | — |
| neg/d209_method_needs_at | 🔍 | coverage-ok | — |
| neg/d209_mod_conflict | 🔍 | coverage-ok | — |
| neg/d209_ro_for_mut | 🔍 | coverage-ok | — |
| neg/d216_ptr_add | 🔍 | coverage-ok | — |
| neg/d216_ptr_deref_read | 🔍 | coverage-ok | — |
| neg/d216_ptr_deref_write | 🔍 | coverage-ok | — |
| neg/d216_ptr_index_read | 🔍 | coverage-ok | — |
| neg/d216_ptr_index_write | 🔍 | coverage-ok | — |
| neg/d216_ptr_lt | 🔍 | coverage-ok | — |
| neg/d216_ptr_sub_dist | 🔍 | coverage-ok | — |
| neg/d216_ptr_write_at_ro | 🔍 | coverage-ok | — |
| neg/d216_unsafe_ptr_modifier_renamed | 🔍 | coverage-ok | — |
| neg/d216_unsafe_type_modifier_renamed | 🔍 | coverage-ok | — |
| neg/d216_unused_unsafe | 🔍 | coverage-ok | — |
| neg/d227_numeric_literal_range | 🔍 | coverage-ok | — |
| neg/d241_priv_consume | 🔍 | coverage-ok | — |
| neg/d241_priv_value | 🔍 | coverage-ok | — |
| neg/d246_redundant_param_ro_prefix | 🔍 | coverage-ok | — |
| neg/d246_redundant_param_ro_type | 🔍 | coverage-ok | — |
| neg/d246_redundant_pointer_ro | 🔍 | coverage-ok | — |
| neg/d246_redundant_return_mut | 🔍 | coverage-ok | — |
| neg/d282_heap_record | 🔍 | coverage-ok | — |
| neg/d282_option_nonptr | 🔍 | coverage-ok | — |
| neg/d282_result_param | 🔍 | coverage-ok | — |
| neg/d282_unit_param | 🔍 | coverage-ok | — |
| neg/d282_vec_param | 🔍 | coverage-ok | — |
| neg/d289_redundant_alias | 🔍 | coverage-ok | — |
| neg/d316_monotonic_non_serializable | 🔍 | coverage-ok | — |
| neg/d316_realtime_sleep | 🔍 | coverage-ok | — |
| neg/d316_sleep_bare_int | 🔍 | coverage-ok | — |
| neg/d316_sleep_until_timestamp | 🔍 | coverage-ok | — |
| neg/d325_retracted_parse_int_opt | 🔍 | coverage-ok | — |
| neg/d325_retracted_read_try_prefix | 🔍 | coverage-ok | — |
| neg/d325_retracted_str_parse_method | 🔍 | coverage-ok | — |
| neg/d326_call_marker_removed | 🔍 | coverage-ok | — |
| neg/d326_chain_gating_ro_at | 🔍 | coverage-ok | — |
| neg/d326_consume_returns_at | 🔍 | coverage-ok | — |
| neg/d326_mut_arg_not_mutable | 🔍 | coverage-ok | — |
| neg/d326_param_form_bare_ref | 🔍 | coverage-ok | — |
| neg/d326_param_form_mut_ref | 🔍 | coverage-ok | — |
| neg/d326_param_form_ro_ref | 🔍 | coverage-ok | — |
| neg/d326_ref_alias_overlap_mut_any | 🔍 | coverage-ok | — |
| neg/d326_ref_escape_addr_self | 🔍 | coverage-ok | — |
| neg/d326_ref_type_position_field | 🔍 | coverage-ok | — |
| neg/d326_ref_type_position_typearg | 🔍 | coverage-ok | — |
| neg/d326_size_of_ref_typearg | 🔍 | coverage-ok | — |
| neg/d353_c_fnptr_nonabi_in_nova_fn | 🔍 | coverage-ok | — |
| neg/d353_callback_effect | 🔍 | coverage-ok | — |
| neg/d353_closure_env | 🔍 | coverage-ok | — |
| neg/d353_nova_fnptr | 🔍 | coverage-ok | — |
| neg/d36_ro_field_write | 🔍 | coverage-ok | — |
| neg/d405_mixed_width_arith | 🔍 | coverage-ok | — |
| neg/d406_enum_kind_token | 🔍 | coverage-ok | — |
| neg/d406_enum_multiline_no_pipe | 🔍 | coverage-ok | — |
| neg/d409_explicit_self_return | 🔍 | coverage-ok | — |
| neg/d409_return_wrong_value | 🔍 | coverage-ok | — |
| neg/d411_partial_no_rest | 🔍 | coverage-ok | — |
| neg/d411_sum_variant_refutable | 🔍 | coverage-ok | — |
| neg/d412_embed_not_found | 🔍 | coverage-ok | — |
| neg/d412_embed_not_literal | 🔍 | coverage-ok | — |
| neg/d412_hex_blob_char | 🔍 | coverage-ok | — |
| neg/d412_hex_blob_odd | 🔍 | coverage-ok | — |
| neg/d417_closure_scalar_return | 🔍 | coverage-ok | — |
| neg/d52_redundant_field_literal | 🔍 | coverage-ok | — |
| neg/d54_int_as_bool | 🔍 | coverage-ok | — |
| neg/d55_lit_out_of_range | 🔍 | coverage-ok | — |
| neg/d85_try_in_fail_fn | 🔍 | coverage-ok | — |

_(Итого 231 + 77 = 308 [+1 добавлен волной-2: d102_static_generic_static_defaults]. Первый заход волны-2: ВСЕ
строки помечены 🔍 (покрытие проверено) — инвентарь-релевантные D матрично-проанализированы (см. таблицу пробелов
выше), прочие D помечены coverage-ok батчем как НЕ-инвентарные фичи (существующие проходящие тесты, не цель
миграции волны-2, нет пробела в аспектах волны-2). Далее волна-2 флипает 🔍→🔄→✅ + заполняет «Закрыто в» per-D;
оркестратор принимает по критериям (1)-(5) ПО КОДУ. Ледер деталей — `wip/196.wave2-progress.md`.)_
