# Plan 62.F.bis: Edition versioning + W_PRELUDE_SHADOW lint + ambient runtime effects + spec amendments

> **Status:** proposed (2026-05-18). Sub-task of [Plan 62](62-prelude-hardcode-migration.md) Phase 62.F: deferred parts из main phase. Plan 62 закрыт без этих items — они purely additive (no user-visible regression).
>
> **Estimate:** 4-6 dev-days в 4 incremental phases. Каждая phase merge-able отдельно.
>
> **Dependencies:**
> - **Hard:** Plan 62.F closed (effects.Fail[E] + no_prelude + partial_prelude уже в проде).
> - **Soft:** Plan 42 (manifest threading) — нужен для Phase 1 (edition).
> - **Soft:** Plan 45 (nova doc) — потребует Phase 4 (D-block amendments).

---

## Цель в одной фразе

Завершить production-grade Plan 62 закрытие: **(1)** edition-versioned prelude (`nova.toml [package].edition` → `std/prelude/<edition>.nv` resolver-mapping), **(2)** W_PRELUDE_SHADOW warning при shadowing prelude names, **(3)** формальные declarations Time/Mem ambient effects (для `nova doc` surface, не runtime change), **(4)** 2 spec D-block amendments — фиксируя current behavior post-62.F.

---

## Проблема (verified state, 2026-05-18)

### 1. Edition не существует

`nova.toml` парсер (`compiler-codegen/src/manifest.rs:41-89`) знает только `[package].name` + `[lib].src`. Поле `edition = "2026.05"` не парсится — silently ignored. Spec [D26 + 07-modules](../../spec/decisions/) предполагает edition-based prelude versioning (per Plan 62 §«Production-grade versioning»).

Resolver (`compiler-codegen/src/imports.rs:103-186`) auto-import'ит фиксированный `std/prelude.nv` без любой edition logic. Если в будущем prelude content изменится breaking-way (e.g. new method on Option) — старые пакеты сломаются. **Industry standard:** Rust uses `edition = "2021"`, Go uses `go 1.21` — стабильность через explicit pin.

### 2. W_PRELUDE_SHADOW lint отсутствует

Per Plan 62 §«Acceptance criteria»: user может shadow'ить prelude names (`type Option { foo int }`) — currently it just works (Plan 62.A разрешил file-based decls). Spec предполагает W_PRELUDE_SHADOW warning, не error — обучающее AI-friendly diagnostic. Currently silent.

### 3. Time/Mem без formal declaration

Plan 62.F документировал DEFER для Time/Mem/Detach (effects.nv DEFER block). Time особенно — это **user-overridable effect** (`with Time = handler Time { sleep(ms) {...} } { body }` — см. examples/effects/effects.nv). Hardcoded в `emit_c.rs:1071` без `.nv` декларации → не виден в `nova doc`.

### 4. Spec D-block amendments

Per Plan 62 §«Spec changes»:
- **D26** (08-runtime.md §249-431): обновить таблицу prelude items (отметить мигрированные vs hardcoded).
- **07-modules.md §962-979** («Prelude opt-out»): добавить `partial_prelude(...)` clause syntax (currently spec только описывает `no_prelude`).

Currently spec немного отстаёт от impl — minor drift.

---

## Phases

### Phase 1: Edition versioning (2-3 days)

**Steps:**

1. Extend `Manifest` struct (`compiler-codegen/src/manifest.rs:19-22`):
   ```rust
   pub struct Manifest {
       pub package_name: String,
       pub source_root: PathBuf,
       pub edition: Option<String>,  // NEW (e.g. "2026.05")
   }
   ```

2. Parse `edition` в `parse_manifest`:
   ```rust
   match (section, key) {
       ("package", "name")    => package_name = Some(val),
       ("package", "edition") => edition = Some(val),
       ("lib", "src")         => lib_src = Some(val),
       _ => {}
   }
   ```

3. Thread Manifest через CLI flow → `resolve_imports_inline`. Currently `resolve_imports_inline(module, entry_path, stdlib_dir, ...)` не получает Manifest. Нужно добавить `manifest: Option<&Manifest>`.

4. В auto-import logic: если `manifest.edition` set, look up `std/prelude/<edition_sanitized>.nv` first (e.g. "2026.05" → "2026_05.nv"), fallback `std/prelude.nv`.

5. Создать `std/prelude/2026_05.nv` initial edition pin (content identical to current `std/prelude.nv` для bootstrap).

6. Test: 2 sub-workspace в `nova_tests/plan62/editions/` с разной edition → каждый видит свой prelude content.

**Acceptance Phase 1:**

- [ ] Manifest parses `[package].edition`.
- [ ] `std/prelude/2026_05.nv` exists, identical to facade.
- [ ] 2 workspace tests с разной edition resolve independently.
- [ ] No regression: 709/0/44 PASS preserved.

### Phase 2: W_PRELUDE_SHADOW lint (1 day)

**Steps:**

1. В `compiler-codegen/src/lints.rs`: добавить function `check_prelude_shadow(module: &Module) -> Vec<Warning>`.

2. Build HashSet known prelude names (Option/Result/Some/None/Ok/Err/Error/Ordering/Less/Equal/Greater/Iter/From/Into/Hashable/Equatable/Comparable/Display/Fail + 4 runtime fns).

3. Iterate `module.items` — для каждого `Item::Type(td)` или `Item::Fn(f)` проверить если `td.name` / `f.name` ∈ prelude names → emit W_PRELUDE_SHADOW warning со span'ом.

4. Test: `nova_tests/plan62/user_shadow_prelude.nv` — `type Option { foo int }` → expect warning, тест должен compile (warning, не error).

**Acceptance Phase 2:**

- [ ] W_PRELUDE_SHADOW emitted при shadowing.
- [ ] Не блокирует compilation (warning, не error).
- [ ] Test verifies warning text + still compiles.

### Phase 3: Time/Mem formal declarations (1 day)

**Steps:**

1. Добавить в `std/prelude/effects.nv`:
   ```nova
   export type Time effect {
       sleep(ms int) -> ()
       now() -> int
       after(ms int) -> Chan[int]  // requires Chan typedef
   }

   export type Mem effect {
       alloc_count() -> int
       free_count() -> int
       live() -> int
       reset() -> ()
   }
   ```

2. Add `Time`/`Mem` в `RUNTIME_DEFINED_TYPES` skip-list (analog Fail per 62.F Part 1b).

3. Update facade `std/prelude.nv` re-export строку:
   ```nova
   export import std.prelude.effects.{Fail, Time, Mem}
   ```

4. Tests: handler tests на Time/Mem уже существуют (`nova_tests/concurrency/*`). Verify regression preserved.

**Acceptance Phase 3:**

- [ ] Time/Mem formally declared.
- [ ] `nova doc` показывает Time/Mem в prelude section.
- [ ] No regression: existing handler tests preserved.
- [ ] Detach остаётся keyword-construct (НЕ formal effect).

### Phase 4: Spec D-block amendments (1 day)

**Steps:**

1. `spec/decisions/08-runtime.md` D26 §249-431: обновить таблицу prelude items, добавить status column (file-based / hardcoded). Mark migrated items.

2. `spec/decisions/07-modules.md` §962-979 «Prelude opt-out»: добавить subsection «Partial opt-in» с syntax `module X partial_prelude(core, runtime)` + 6 valid sub-modules list.

3. Cross-reference: link spec amendments в Plan 62 plan-doc.

**Acceptance Phase 4:**

- [ ] D26 status column accurate.
- [ ] 07-modules.md partial_prelude documented.
- [ ] Plan 62 plan-doc updated с cross-refs.

---

## Total estimate: 4-6 dev-days через 4 phases

Каждая phase independent merge-able. Не блокирует другие planned items.

## Not in scope

- **TryFrom/TryInto** — Plan 62.E.bis (otra sub-plan, требует Plan 56 ext или D77 revisit).
- **print/println migration** — Plan 62.B.bis (variadic + type-polymorphic).
- **StringBuilder/WriteBuffer/ReadBuffer opaque types** — Plan 62.D.bis (требует `external type` D-block).
- **RuntimeNoneError** — bootstrap parser empty-sum limitation (же блокер что `Never`).
- **Removal pre-populated sum_schemas[Option/Result]** — Plan 62.A.bis Ф.4 (требует Plan 14 Q-result-monomorphization fix).
