<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 152.5 — Сравнение и collation (D254, Q-string-collation)

> **Родитель:** [Plan 152 (umbrella)](152-string-coordinate-model.md).
> **Фазы: 152.5a = A (now), 152.5b = B (later, зависит от 152.4).**
> **Создан:** 2026-06-13. **Статус:** ✅ **152.5a (core, вкл. D-R4) ЗАКРЫТ** (2026-06-13) +
> ✅ **152.5b DUCET-collation ЗАКРЫТ 2026-06-14** (`collate_compare`/`collate_sort_key`/
> `Collator`, UCA/UTS #10, **Shifted**-вариант, **+ S2.1 discontiguous contractions**) +
> ✅ **152.5b eq_ignore_case ЗАКРЫТ 2026-06-16** (`str`/`char @eq_ignore_case`, D254 §eq_ignore_case).
> Остаётся: CLDR-tailoring (`[M-152-collation-tailoring]`). P1. **Model:** Sonnet 4.6 + High.

## Прогресс ✅ 152.5b DUCET-collation 2026-06-14

`std/unicode/collate.nv` (opt-in, НЕ prelude — D254: `str` Ord = byte-lex по умолчанию):
- **`collate_compare(a,b)->int`** (-1/0/+1), **`collate_sort_key(s)->[]int`**,
  **`collate_eq`**, **`Collator.root()`** (`@order`/`@key`/`@same`).
- Алгоритм UTS #10: NFD → collation elements (longest-match contractions **+ S2.1
  discontiguous** через interposed lower-ccc марки + implicit-веса CJK/Tangut) →
  multi-level sort key (**Shifted** variable-weighting) → лексикографическое сравнение.
- Данные: `nova-codegen unicode` → `std/unicode/collate_data.nv` из DUCET `allkeys.txt`
  (38443 single / 77 contraction-keys / 21 implicit ranges).
- **G0:** UTS #10 `CollationTest_SHIFTED.txt` (INDEPENDENT oracle): **50000-пар uniform
  spread = 200/200 PASS** out-of-band (≈22% от 227800; коммит — 1500). Полный 227800
  как один тест слишком большой (clang) → через slow-lane Plan 156
  (`[M-152-collation-full-conformance]`). Surrogate-строки CollationTest пропускаются
  (Nova `str` = валидный UTF-8). Hand-cases: primary/secondary/tertiary порядок.
- **Scope (честно):** DUCET (non-tailored) + Shifted. **CLDR locale-tailoring НЕ
  реализован** → `[M-152-collation-tailoring]` (как Rust `unicode-collation` DUCET-mode
  / ICU root collator без tailoring). `eq_ignore_case` — отдельный мелкий followup.
- Codegen-баг попутно: `match`-as-return с `mut found = None` выводил тип результата
  как `bool` → workaround типизированным `mut found Option[str] = None`.
> **Зависит от:** Plan 137 (`Compare`/`Equal`/`Hash`), 152.4 (folding/UCA для 152.5b).
> **Владеет:** D254, Q-string-collation.

---

## Идея

Дефолтное сравнение — byte-lexicographic (быстрое, детерминированное, locale-
независимое, как Rust/Go). Locale-aware collation — отдельный явный opt-in слой; `str`
никогда не делает collation молча.

## D254. Модель сравнения

### 152.5a — Ядро (Фаза A)
- **`@compare`/`Ord`/`Equal`/`Hash`** на `str` — byte-lexicographic (есть `@compare`,
  D178; закрепить как дефолт `Ord`). Детерминированная сортировка.
- **`@eq_ignore_ascii_case(other) -> bool`** — ASCII case-insensitive eq (без таблиц).

### 152.5b — Unicode/locale (Фаза B, зависит 152.4)
- **`@eq_ignore_case(other) -> bool`** — Unicode case folding (`std/unicode.fold_case`).
- **Locale collation:** `std/unicode/collate.Collator` (UCA / DUCET ordering +
  опц. CLDR-tailoring), аналог JS `localeCompare` / Java `Collator`.

## Q-string-collation — ЗАКРЫТО

Дефолт = byte-`Ord` (быстрый, детерминированный, locale-независимый). Locale-aware —
явный `std/unicode/collate.Collator` на UCA (DUCET) + опц. CLDR-tailoring (Phase B).
`str` никогда не делает collation молча. Прецедент: Rust (byte Ord + crate), Go (byte
+ `x/text/collate`).

## Фазы

- **Ф.0 — Spec D254 + Q-string-collation** (запись-решение).
- **Ф.1 — Закрепить byte-`Ord`** дефолтом + `eq_ignore_ascii_case` (152.5a).
  - **D-R4 (перенос из 152.0, апрув автора 2026-06-13).** Декомиссия хардкода
    str-операторов в `emit_c.rs:17302`: `<`/`<=`/`>`/`>=` синтезировать из `@compare`
    (Compare-протокол уже даёт 6 операторов семантически, Plan 139 — убираем только
    codegen-short-circuit в C), `==`/`!=` из `@eq`, `+` из `@concat`. После снятия
    хардкода в реестре остаётся **только `@hash`** (DoS-seed, C). Закрывает
    `[M-139.1-operator-lowered-methods]`.
    - **Порядок (важно):** удаление реестровых `eq`/`lt`/`le`/`gt`/`ge` делать **в одной
      фазе** с codegen-reroute — чтобы не было промежутка, где оператор не резолвится.
    - **Полная декомиссия — БЕЗ perf-retain (override автора проекта 2026-06-13):** всё в
      `.nv`, C-lowering операторов/concat НЕ оставляем ни при каком регрессе. «Оставить
      C как perf-опт» — **отклонено**: всё переносится в Nova.
    - **Perf — через RawMem-примитивы в Nova-body (а не через C-lowering):** причина, по
      которой C было быстрее, — **byte-loop** в Nova-body. Но Nova-body может звать **те же
      примитивы**, что и C: `@compare` → `RawMem.compare` (= memcmp, нормализ. -1/0/+1),
      `@concat` → `RawMem.copy_nonoverlapping` (= memcpy, через `_buffer` 152.0). Тогда
      `.nv`-тело ≈ C по скорости, компромисс не нужен. (Сейчас `@compare`/`@concat` —
      byte-loop'ы; переписать на RawMem — часть этой фазы / 152.0 Ф.3.)
    - **Бенч до/после** на str-сравнении/сортировке и concat-хот-пути — **чтобы
      ПОДТВЕРДИТЬ паритет** RawMem-body ≈ C (а не чтобы решать «оставлять ли C»).
      Зафиксировать числа в D-R4. Если RawMem-body всё же медленнее — оптимизировать
      Nova-body, не возвращать C-lowering.
- **Ф.2 — `eq_ignore_case`** через folding (152.5b, после 152.4).
- **Ф.3 — `Collator`** (UCA) — 152.5b / roadmap `[M-152-collation]`.
- **Ф.4 — Фикстуры + close.**

## Тесты

- **POS (A):** byte-`Ord` сортировка детерминирована и стабильна; `["b","A","a"]`
  сортируется byte-lex; `eq_ignore_ascii_case("Foo","foo")==true`.
- **POS (B):** `eq_ignore_case("STRASSE","straße")==true` (folding);
  `Collator(locale).compare(...)` упорядочивает по UCA (если в scope).
- **NEG:** `eq_ignore_ascii_case("é","É")==false` (не-ASCII вне ASCII-folding);
  collation без locale-данных → fallback на byte-`Ord` + диагностика.

## Критерии приёмки

- **A1.** `str` реализует `Compare`/`Equal`/`Hash` byte-lexicographic; сортировка
  детерминирована, locale-независима.
- **A2.** `eq_ignore_ascii_case` корректен (ASCII-only, без таблиц).
- **A3 (B).** `eq_ignore_case` через Unicode folding совпадает с эталоном.
- **A4 (B).** `Collator` (UCA) упорядочивает по DUCET; tailoring опционален; `str`
  не делает collation неявно.
- **A5.** Полный `nova test` без новых FAIL; plan152_5-фикстуры PASS.

## Связь

D254; Q-string-collation (закрыт). 152.5b зависит от 152.4 (folding/UCA-данные).
Roadmap: `[M-152-collation]`.

## Прогресс ✅ 152.5b eq_ignore_case ЗАКРЫТ 2026-06-16 (ветка plan-152)

- **`str @eq_ignore_case(other str) -> bool`** — Unicode full-case-fold equality: делегирует
  `fold_case(@) == fold_case(other)`. `"STRASSE".eq_ignore_case("straße") == true` (немецкий ß
  фолдится в `ss` через CaseFolding C+F). Multi-codepoint folds применяются. ASCII-fast-path —
  `eq_ignore_ascii_case` (нет таблиц).
- **`char @eq_ignore_case(other char) -> bool`** — аналог для одного codepoint: конвертит каждый
  символ в `str.from(cp)`, затем делегирует в `fold_case`. Opt-in (`import std.unicode`).
- **Тесты:** `plan152_5/t5_eq_ignore_case` (позитивные: ß/SS, Latin, Greek, ASCII) PASS;
  `plan152_5/n_eq_ignore_case_ascii_boundary` (негативный: `"é" @eq_ignore_case "É" == false`
  через ASCII-вариант — проверяет что `eq_ignore_ascii_case` НЕ фолдит non-ASCII) PASS.
- D254 §eq_ignore_case ЗАКРЫТ. Критерий **A3 (B)** выполнен.

## Прогресс ✅ 152.5a (core, кроме D-R4) ЗАКРЫТ 2026-06-13 (ветка plan-152)

- **byte-`Ord`** — `str @compare` (D178, byte-lex) уже дефолт; задокументирован в D254.
- **`@eq_ignore_ascii_case`** — str (core.nv, byte-fold A-Z/a-z, длины должны совпадать) +
  char (defaults.nv, через to_ascii_lowercase). Не-ASCII не фолдится (é≠É). Spec D254 +
  Q-string-collation ([open-questions.md](../../spec/open-questions.md)). Фикстура
  [plan152_5/comparison](../../nova_tests/plan152_5/comparison.nv) 1/0.
- **D-R4 ✅ ВЫПОЛНЕН** (2026-06-13, commits ae465fce + step2): декомиссия хардкода
  str-операторов в emit_c.rs. `==`/`!=` → Nova-body `@eq` (length-first + RawMem.compare),
  `<`/`<=`/`>`/`>=` → `@compare`(RawMem) `OP 0`, `+` → `@concat` (уже RawMem). Реестр str
  (`runtime_registry.rs` + `str_method_to_rt`) = **только `@hash`**. Бенч-паритет подтверждён
  (5M cmp + 200k concat: before 23.5–28.2s ≈ after 22.6–29.2s, дельта в compile-шуме).
  C `nova_str_eq`/`concat` остались ТОЛЬКО codegen-внутренними (структурная eq полей в
  emit_field_eq для HashMap-ключей/record-`==`; ScopeOutcome cancel + concat-acc) — не
  user-facing операторы. Маркер `[M-139.1-operator-lowered-methods]` ЗАКРЫТ. 0 регрессий +
  reachability-проба. Фикстуры: plan152_5/bench_operators (perf-smoke) + comparison.
- **Остаток:** 152.5b (eq_ignore_case via folding + Collator UCA) — Phase B, gated на 152.4.
  Критерии A1/A2 (core) ✅.
