<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 152.4 — `std/unicode`: нормализация / сегментация / folding / case-mapping (D253, Q-unicode-data)

> **Родитель:** [Plan 152 (umbrella)](152-string-coordinate-model.md). **Фаза B** (later) —
> крупнейший лифт; обязательна для паритета Java/JS/Kotlin по Unicode, но отделяема.
> **Создан:** 2026-06-13. **Статус:** 🟢 **152.4.1+152.4.2 (нормализация) + 152.4.3 (graphemes)
> + 152.4.4 (case folding/mapping) + 152.4.5 (word-сегментация + title-casing) ЗАКРЫТЫ 2026-06-14**;
> остаётся sentence-сегментация (`[M-152-sentence-boundaries]`). P1. **Эстимат:** ~4–6 dev-day.
> **Model:** **Opus** + Thinking ON (Unicode-data pipeline + алгоритмы).
> **Зависит от:** 152.1 (`as_chars`/линзы), 152.0 (`_buffer`), nova-codegen
> (генерация таблиц). **Владеет:** D253, Q-unicode-data.

---

## Идея

Новый модуль `std/unicode` с Unicode-data, закрывающий разрыв с Java `Normalizer`/
`BreakIterator`, JS `normalize`/`Intl.Segmenter`, Rust crates `unicode-*`. Подключается
**явно** — за размер таблиц платит импортирующий.

## D253. `std/unicode` — что в ядре vs библиотеке

Ядро (152.1–152.3a): байты, codepoint decode/encode, ASCII case/classification,
byte-`Compare`. **`std/unicode` (здесь):** полная нормализация, grapheme/word
сегментация, case folding, Unicode case mapping, (152.5b) collation.

API:
- **Нормализация:** `normalize_nfc/nfd/nfkc/nfkd(s) -> str` (decomposition + canonical
  ordering по CCC + composition; compat-формы для NFKC/NFKD).
- **Grapheme-сегментация (UAX #29):** `str.@as_graphemes() -> GraphemesView`
  (extended grapheme clusters: combining marks, ZWJ-emoji, regional-indicator флаги).
  Третья линза, симметрично `as_bytes`/`as_chars`.
- **Case folding:** `fold_case(s) -> str` (для caseless matching, 152.5).
- **Unicode case mapping:** `to_uppercase`/`to_lowercase`/`to_titlecase`
  (multi-codepoint, без локали) — апгрейд ASCII-only `string.nv`.
- **Word/sentence boundaries (UAX #29):** `word_indices`/`sentences` — **roadmap**
  (152.4.4 / `[M-152-word-boundaries]`).

## Q-unicode-data — ЗАКРЫТО (стратегия)

Таблицы генерируются build-time-инструментом **`nova-codegen unicode`** из официального
UCD (`UnicodeData.txt`, `CaseFolding.txt`, `GraphemeBreakProperty.txt`,
`DerivedNormalizationProps.txt`, `CompositionExclusions.txt`, `emoji-data.txt`) в
компактные **range-кодированные** Nova-таблицы `std/unicode/data/`. Пин к версии
(константа `UNICODE_VERSION`, напр. `16.0`), lazy-инициализация. **Без ICU/ОС-зависимостей.**
Прецедент: Rust `unicode-*` (codegen), Go `maketables`.

## Внутренняя декомпозиция (sub-sub-plans)

- **152.4.1 — Unicode-data pipeline** (`nova-codegen unicode` + range-таблицы + версия-пин).
- **152.4.2 — Нормализация** (NFC/NFD/NFKC/NFKD; CCC ordering; composition exclusions).
- **152.4.3 — Grapheme-сегментация** (UAX #29; `as_graphemes()` + `GraphemesView`).
- **152.4.4 — Case folding + Unicode case mapping** (fold/upper/lower + Final_Sigma).
- **152.4.5 — Word-сегментация (UAX #29) + title-casing** (`as_words()` + `to_titlecase`).

## Фазы (верхнеуровнево)

- **Ф.0 — Spec D253 + Q-unicode-data** (запись-решение, см. umbrella §4).
- **Ф.1 — Data pipeline** (152.4.1).
- **Ф.2 — Нормализация** (152.4.2) + эталонные тесты UAX #15.
- **Ф.3 — Graphemes** (152.4.3) + `as_graphemes()` линза + UAX #29 эталоны.
- **Ф.4 — Folding + case mapping** (152.4.4); апгрейд `str.to_upper/lower` на Unicode.
- **Ф.5 — Close** + `docs/strings.md` Unicode-раздел; снять `[M-152-unicode-*]`.

## Прогресс ✅ 152.4.1 + 152.4.2 + 152.4.3 + 152.4.4 + 152.4.5 ЗАКРЫТЫ 2026-06-14

| Sub | Статус | Что сделано |
|---|---|---|
| **152.4.1** data pipeline | ✅ | `nova-codegen unicode` субкоманда (`compiler-codegen/src/codegen/unicode_data.rs`): парс UCD → `std/unicode/norm_data.nv` (`;`-таблицы NFD/NFKD/CCC/comp, пин 16.0); `--check` CI-guard; `--emit-conformance`. Таблицы байт-идентичны reference (2081 nfd / 5913 nfkd / 934 ccc / 961 comp). D253 + Q-unicode-data ✅. |
| **152.4.2** нормализация | ✅ | `std/unicode/normalize.nv`: `normalize_nfc/nfd/nfkc/nfkd(s)->str`; decompose (таблицы + алгоритмический Hangul) → canonical ordering (CCC, стабильно) → canonical composition (blocking-rule + Hangul L+V/LV+T). Таблицы — lazy-static `ro HashMap`. plan152_4/normalize 16 кейсов PASS; **UAX #15 conformance** (NormalizationTest.txt): Part 0 целиком + stride-выборка Parts 1-5 = **1500 cases / 6000 asserts** (по всему диапазону scripts: Latin/Greek/Cyrillic/CJK-compat/Hangul/Arabic/math), PASS. |
| **152.4.3** graphemes | ✅ | `str.@as_graphemes() -> GraphemesView` (3-я линза): UAX #29 extended grapheme clusters, правила GB1-GB13 **+ GB9c** (Indic Conjunct Break, U15.1). Range-таблицы (GCB + Extended_Pictographic + InCB) binary-search в `grapheme_data.nv` (GraphemeBreakProperty + emoji-data + DerivedCoreProperties). **Полный UAX #29 conformance: GraphemeBreakTest.txt 1093/1093 (content-checked) PASS** + hand-picked (é/флаги/ZWJ-emoji/Hangul/CRLF). 2 compiler-бага попутно: method-return type-qualified collision (CharsIter vs GraphemesView `next`), parser SOH-sentinel vs literal U+0001. |
| **152.4.4** folding/case-map | ✅ | `fold_case` + `to_uppercase`/`to_lowercase(s)->str` (locale-independent, multi-cp: ß→SS, ﬁ→FI, İ→i̇; **Final_Sigma** Σ→ς/σ через Cased/Case_Ignorable). Данные: `nova-codegen unicode` → `case_data.nv` (FOLD/LOWER/UPPER maps + CASED/CASE_IGNORABLE ranges из CaseFolding C+F + SpecialCasing unconditional + UnicodeData[12,13] + DerivedCoreProps). G0 (2 слоя): breadth-conformance **2981/2981** out-of-band (коммит uniform-spread 1500, ASCII…supplementary) проверяет парсер+lookup+emission; **independent hand-oracle** в `case.nv` пиннит выборку (Turkic-excl I→i, field-index, 3-cp ﬃ→FFI/ŉ→ʼN, Final_Sigma+case-ignorable). Locale (tr/az/lt) исключены (не упрощение — отд. фича). Закрывает `[M-152-case-fold]`. Codegen-баг попутно: fluent-`@`-хвост в `if`-ветке (workaround helper'ами → `[M-codegen-fluent-tail-if-unify]`). |
| **152.4.5** word-сегментация + title-casing | ✅ | `str.@as_words() -> WordsView` (4-я линза): UAX #29 word boundaries WB1-WB16 (lookahead WB6/7/11/12 + WB4 ignore-rule + RI-parity WB15/16) над `word_data.nv` (WordBreakProperty range-таблица; ExtPict для WB3c reused). **`to_titlecase(s)->str`**: титулкейс первой cased-буквы слова (TITLE-маппинг, не upper — ǆ→ǅ) + lowercase остального (с Final_Sigma). **Полный UAX #29 conformance: WordBreakTest.txt 1826/1826 (content-checked, INDEPENDENT oracle) verified out-of-band** (коммит uniform-spread 1500) + hand-authored (segments/can't/3.14/flag/digraph-title/Greek). 0 регрессий. Закрывает `[M-152-word-boundaries]`. Попутно: 2-буквенное free-fn имя `wb` коллизировало с runtime-локалом → renamed `wbcat` (`[M-codegen-short-freefn-name-collision]`). Sentence-сегментация → `[M-152-sentence-boundaries]`. |

**Foundation enablers (закоммичены отдельно):** module-level `ro` lazy-static globals
(Plan 152.4.0, commit `52db92c1`) + GC-rooting под `GC_set_no_dls(1)` + cross-module
`Item::Let` merge (commit `e22baf23`). Без них таблицы-в-HashMap не работали (collected
под GC / undeclared cross-module). Commits: foundation `52db92c1` + hardening `e22baf23`
+ std/unicode `a8df78a5`.

### Критерии приёмки (152.4.1 + 152.4.2) — production-grade, без упрощений

Прогон через **релизные** `nova` + `nova-codegen` (не debug).

- **G0 (ОБЯЗАТЕЛЬНО: без упрощений, production-grade)** ✅ для поставленного scope —
  алгоритм нормализации это **полная** UAX #15 реализация (decompose + canonical
  ordering + canonical composition + алгоритмический Hangul), без shortcut'ов/заглушек.
  Доказательство: прогон против **ПОЛНОГО** `NormalizationTest.txt` — **19965 / 19965
  cases (79860 asserts, 40 test-блоков) PASS** (out-of-band, релизный nova). Единственное
  санкционированное упрощение — **РАЗМЕР коммитнутой фикстуры** (1500 репрезентативный
  сэмпл, а не 80k asserts: раздуло бы репо/CI; полнота проверена out-of-band, согласовано
  с автором). Graphemes (152.4.3) + folding/case-map (152.4.4) — Phase B: НЕ упрощены, а
  вне scope этой поставки (явные маркеры).
- **A1 (NFC/NFD/NFKC/NFKD корректны на UAX #15 эталоне)** ✅ — верифицировано против
  **полного** `NormalizationTest.txt` (19965/19965, см. G0). Коммитнутая CI-фикстура:
  Part 0 целиком + uniform stride-выборка Parts 1-5 (полный диапазон scripts:
  Latin/Greek/Cyrillic/CJK-compat/Hangul/Arabic/math), 1500 cases / 6000 asserts, чанки
  по 500; выборка документирована в шапке (не silent-truncation), регенерится
  `--conformance-limit`.
- **A5 (таблицы версионированы, `nova-codegen unicode`, без ICU)** ✅ — пин
  `UNICODE_VERSION = "16.0"`; `--check` CI-guard; UCD-файлы не коммитятся (`--ucd-dir`).
- **A6 (opt-in: ядро собирается без `std/unicode`)** ✅ — НЕ prelude; негативный тест
  `n_unicode_opt_in` пиннит: `normalize_nfc` без `import std.unicode` → compile-error.
- **A7 (0 новых FAIL)** ✅ — broad regression (plan148/basics/generics/plan139/plan153/
  plan152/plan114), 2 fail plan114 verified pre-existing на main.
- Foundation-guard (152.4.0) ✅ — негативный тест `n_ro_constexpr_prefers_const`:
  constexpr `ro` на module-level → `E_RO_FOR_CONSTEXPR_PREFER_CONST` (strict-партиция).
- **A2/A3 (grapheme-сегментация UAX #29)** ✅ (152.4.3) — `str.@as_graphemes()`
  третья линза; правила GB1-GB13 + GB9c; **полный** `GraphemeBreakTest.txt`
  **1093/1093** (content-checked, не count-proxy); G0 без упрощений соблюдён.
- **A4 (folding / Unicode case-mapping)** ✅ (152.4.4) — `fold_case`/`to_uppercase`/
  `to_lowercase(s)->str`, locale-independent, multi-codepoint; **Final_Sigma** context
  (Σ→ς/σ). G0 без упрощений (двухслойная проверка):
  - **Breadth-conformance** `case_conformance.nv` — **полный 2981/2981 mapped codepoints**
    out-of-band (коммит — uniform spread 1500, охватывает ASCII…лигатуры FB00…
    supplementary plane). ВАЖНО (честно): expected выводится из того же генератора →
    проверяет рантайм **парсер+lookup+emission** (round-trip), НЕ **выборку** таблиц.
  - **Independent oracle** (hand-typed из UCD-спеки, в `case.nv`) — пиннит ВЫБОРКУ,
    которую breadth не ловит: Turkic/locale-исключение (I→i не ı, i→I не İ, İ→i̇),
    field-index sentinels, 3-cp expansions (ﬃ→ffi/FFI, ŉ→ʼN), Final_Sigma с
    case-ignorable interleaving (’ U+2019).
  - Locale (tr/az/lt) исключены **по дизайну** (D253 §no-locale) — не упрощение.
  Закрывает `[M-152-case-fold]`.
- **A8 (word-сегментация UAX #29 + title-casing)** ✅ (152.4.5) — `str.@as_words()`
  четвёртая линза (WB1-WB16: lookahead WB6/7/11/12 + WB4 ignore-rule + RI-parity);
  **`to_titlecase(s)->str`** (первая cased-буква слова через TITLE-маппинг, не upper;
  остальное lowercase с Final_Sigma). G0 без упрощений: **полный `WordBreakTest.txt`
  1826/1826** (content-checked, **INDEPENDENT** UCD-oracle — boundaries в тест-файле, не
  из генератора) verified out-of-band; коммит — uniform-spread 1500. title≠upper пиннит
  hand-case ǆ→ǅ. Sentence-сегментация исключена **по дизайну** (отд. UAX #29 алгоритм →
  `[M-152-sentence-boundaries]`). Закрывает `[M-152-word-boundaries]`.

**Тесты (релизный прогон):** 9 позитивных (`lazy_static_global`, `normalize` 16 кейсов,
`normalization_conformance` 1500, `graphemes`, `grapheme_conformance` 1093, `case`,
`case_conformance` 1500, `words`, `word_conformance` 1500) + 4 негативных (`n_unicode_opt_in`,
`n_ro_constexpr_prefers_const`, `n_case_opt_in`, `n_words_opt_in`) = **13/13 PASS**
(релизные `nova` + `nova-codegen`).

## Тесты

- **POS:** NFC(«e»+◌́)==NFC(«é»); NFD обратно; NFKC(ﬁ-лигатура)→«fi»;
  graphemes(«👨‍👩‍👧»)==1, («🇺🇸»)==1, («é»=e+◌́)==1; `fold_case(«ß»)==«ss»`;
  `to_uppercase(«straße»)==«STRASSE»`. Прогон по официальным эталонам UAX #15
  (`NormalizationTest.txt`) и UAX #29 (`GraphemeBreakTest.txt`).
- **NEG:** невалидный UTF-8 в normalize → policy (инвариант R-UTF8 ⇒ не должен
  возникать; тест-страховка); неизвестная Unicode-версия данных → ошибка сборки/init.

## Критерии приёмки

- **A1.** NFC/NFD/NFKC/NFKD проходят официальный `NormalizationTest.txt` (UAX #15).
- **A2.** Grapheme-count проходит `GraphemeBreakTest.txt` (UAX #29): эмодзи-ZWJ,
  флаги-RI, combining — каждый = 1 grapheme.
- **A3.** `as_graphemes()` — линза, симметрична `as_bytes`/`as_chars`, не
  `Index[int]`-индексируема (только итерация/`at`).
- **A4.** `fold_case`/Unicode `to_uppercase`/`to_lowercase` корректны (multi-cp);
  `str.to_upper/lower` апгрейжены с ASCII-only.
- **A5.** Unicode-таблицы версионированы (`UNICODE_VERSION`), генерируются `nova-codegen
  unicode`, не зависят от ICU; размер задокументирован.
- **A6.** `std/unicode` подключается явно (импорт) — ядро без него собирается и
  работает (ASCII-complete).
- **A7.** Полный `nova test` без новых FAIL; plan152_4-фикстуры PASS.

## Связь

D253; Q-unicode-data (закрыт). Разблокирует 152.3b (char Unicode) и 152.5b (collation).
Roadmap-маркеры: `[M-152-unicode-norm]`, `[M-152-graphemes]`, `[M-152-case-fold]`,
`[M-152-word-boundaries]`.
