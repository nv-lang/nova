// Заведомо НЕСУЩЕСТВУЮЩИЙ квалифицированный тип в позиции аннотации.
// Ожидание, записанное ДО прогона: если `check` ЗЕЛЁН — корень тот же, что у
// №1082: неразрешённое имя принимается молча, и эмиттер выдумывает C-имя.
// Если `check` КРАСЕН — значит квалифицированные типы резолвятся, и у №1077
// причина другая, более узкая.
module nonsense
import lib.thing

fn main() Io -> () {
    ro w nope.Nothing = thing.make(7)
    print("id=${w.id}")
}
