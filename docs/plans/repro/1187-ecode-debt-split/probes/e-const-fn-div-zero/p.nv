// EXPECT_COMPILE_ERROR E_CONST_FN_DIV_ZERO -- does NOT reproduce, see NOTE.md
#no_prelude
module neg.d199_div_zero_test

fn divide(const a int, const b int) -> const int => a / b

fn use_it() {
    const R = divide(5, 0)
}
