// Законный сосед: тот же файл, отличается ОДНИМ символом -- ширина первого параметра.
// Без этой половины отказ на соседнем файле доказывал бы лишь «что-то отвергнуто»,
// а не «отвергается именно правило под проверкой».
module probe.d405

fn same(a u16, b u16) -> u16 {
    a + b
}

fn main() {
    println("r = ", same(1, 2))
}
