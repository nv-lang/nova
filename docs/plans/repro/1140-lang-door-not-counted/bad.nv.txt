// Запрещённая половина пробы D405. Форма скопирована у examples/basics/arithmetic.nv --
// файла из базы объёма эмиссии, то есть заведомо читаемого Кариной.
// Кладётся в СВОЙ каталог: папка в Nova -- один модуль, и второй main столкнулся бы.
module probe.d405

fn mixed(a u8, b u16) -> u16 {
    a + b
}

fn main() {
    println("r = ", mixed(1, 2))
}
