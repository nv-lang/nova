// Имя несёт спецификаторы printf: %s %d %g %% — до фикса №1073 они
// подставлялись как формат и печатали мусор вместо имени.
module pct_fail

test "border %s and %d and %g and %% here" {
    assert(1 == 2)
}
