// #1328 carrier: out-of-canon `value consume`. Oracle E_MODIFIER_ORDER; novac reads it as a legal form (no E_MODIFIER_ORDER).
// Run: `nova check <file>` (oracle) and `novac/target/novac.exe check <file>` from the root of Carina's tree at 979ba60eb.
type Job value consume {
    n int
}

fn Job consume @close() -> () {
}

fn main() -> () {
}
