// Законный сосед: то же самое, но названо ОБЪЯВЛЕННОЕ поле. Отличие ровно в имени
// поля внутри фигурного паттерна -- значит отказ на соседнем файле может быть только
// про «поля нет», а не про форму деструктуризации.
module probe.d411b

type Point value {
    x int
    y int
}

fn main() -> () {
    ro p = Point { x: 1, y: 2 }
    ro { x, .. } = p
    println(x)
}
