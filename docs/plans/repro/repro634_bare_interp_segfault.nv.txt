// Проба: интерполируется ли `Date` сам по себе, без `.to_str()`.
// От ответа зависит, настоящая ли находка линта W_REDUNDANT_TO_STR_INTERP
// в n601 или ложная (правило видит ИМЯ метода, а не тип — оно само об этом
// предупреждает, реестр №520).
module probe_display

import std.time.civil

fn main() {
    ro d = Date.new(2026, Jul, 10)!!
    println("bare=${d}")
}
