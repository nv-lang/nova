// Третье правило абзаца `02-types.md:1735-1956`: ЛИШНЕЕ поле в литерале — тоже ошибка.
//
// Несовпадение типа реализовано (`E_RECORD_CTOR_TYPES`), пропущенное поле — нет (№1096).
// Осталось проверить лишнее: если и оно принимается, это ОДНА дверь и один фикс, а не
// два. Отдельным файлом, потому что ошибка компиляции здесь — ОЖИДАЕМЫЙ исход, и она не
// должна скрыть другие пробы.

type Pair value {
    mut a int
    mut b int
}

fn with_extra() -> Pair => { a: 1, b: 2, c: 3 }

fn main() Io -> () {
    ro p = with_extra()
    print("extra field accepted: a=")
    print(p.a)
    print(" b=")
    print(p.b)
    print("\n")
}
