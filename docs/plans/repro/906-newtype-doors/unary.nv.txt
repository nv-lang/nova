// door: unary minus keeps the newtype (must stay FnRow, printing needs as)
module unary

type FnRow int
type TyId int
const N int = 5
const ONE TyId = 1 as TyId

fn main() Io -> () {
    ro a = 1 as FnRow
    ro b = -a
    println(b as int)
}
