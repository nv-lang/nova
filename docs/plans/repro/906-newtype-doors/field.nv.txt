// door: record field r FnRow initialised with TyId (must be an error)
module field

type FnRow int
type TyId int
fn FnRow @hash() -> u64 => (@ as int) as u64
fn FnRow @equal(o FnRow) -> bool => (@ as int) == (o as int)
fn TyId @hash() -> u64 => (@ as int) as u64
fn TyId @equal(o TyId) -> bool => (@ as int) == (o as int)

type Rec { r FnRow }
fn main() Io -> () {
    ro t = 1 as TyId
    ro rec = Rec { r: t }
    println(rec.r as int)
}
