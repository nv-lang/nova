// door: string interpolation of a newtype (Display through representation?)
module interp

type FnRow int
type TyId int
const N int = 5
const ONE TyId = 1 as TyId

fn main() Io -> () {
    ro a = 7 as FnRow
    println("row ${a}")
}
