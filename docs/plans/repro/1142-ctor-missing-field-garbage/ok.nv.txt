// D17, законный сосед: перечислены ВСЕ объявленные поля. Отличие от запрещённой
// половины — ровно одно поле в конструкторе, значит отказ на соседнем файле может
// быть только про пропущенное поле.
module probe.d17

type Point value {
    x int
    y int
}

fn main() -> () {
    ro p = Point { x: 1, y: 2 }
    println(p.x)
}
