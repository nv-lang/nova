// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 73.1 — `consume` binding syntax (D131 V2 extension)

> **Создан 2026-05-28 как followup [Plan 73](73-consume-qualifier.md) (D131
> foundation ✅ closed 2026-05-21).**
>
> **Статус:** 📋 proposed
> **Приоритет:** P1 (closes ergonomics gap для consume-types на binding site)
> **Оценка:** ~3-5 dev-day (большая часть — sweep tests + stdlib миграция)
> **Зависимости:** Plan 73 ✅ (D131 foundation), Plan 100.1 ✅ (D133 type-level
> consume), Plan 100.3 ✅ (D157 view-default + closure capture), Plan 100.7 ✅
> (D165 stdlib migration playbook).
> **D-блок:** **D131 amendment** (или новый D175 — TBD в Ф.1).
>
> **Recommended model:** Sonnet 4.6 HIGH + Thinking ON для Ф.1 (spec amend
> design), Ф.2 (type-checker enforcement). Mechanical sweep'ы (Ф.3-Ф.5) — OFF.
>
> **Production-grade требование:** реализация без упрощений. Никаких temporary
> shortcuts, mock'ов, "TODO позже", silent fallback'ов или partial-mode'ов.
> Если фича требует cross-function inference — делать честно, не stub'ить.
> Если что-то не влезает в scope — выносить в отдельный followup-план с явным
> [M-73.1-xxx] маркером + record'ить в simplifications.md как «explicitly
> deferred, not silently dropped».

## Worktree convention

`nova-p73` (descriptor `73`).

## Зачем

Plan 73 (D131) ввёл `consume` keyword **на receivers и params**:
```nova
fn StringBuilder consume @into() -> str       // consume receiver
fn drain(consume t Token) -> int               // consume param
```

Но **на let-binding** consume-обязательство **невидимо**:
```nova
let sb = StringBuilder.new()    // ← неявно: sb имеет consume-obligation
sb.into()                        // ← consume happens silently
```

Это создаёт **3 проблемы:**

1. **Невидимость ownership** на binding site — reviewer не видит что переменная
   будет потреблена. В Rust `let x = ...` тоже implicit move/copy, но borrow
   checker делает это безопасным через lifetimes. Nova lifetime-free → нужна
   syntactic visibility.

2. **Dangling view problem.** Сейчас можно написать:
   ```nova
   let sb = StringBuilder.new()
   let twin = sb           // ← view-borrow? alias? move?
   sb.into()               // ← sb потреблён
   twin.append("late")     // ← dangling — UB
   ```
   Текущий type-checker (Plan 73 D131 + Plan 100.3 D157) частично ловит это,
   но семантика `let twin = sb` неоднозначна.

3. **Inconsistency с D157 view-default на params.** D157 говорит: «не-consume
   param = view-borrow». Но `let X = consume_var` внутри тела — что? Move?
   Alias? Borrow без lifetime?

## Цель

Сделать ownership/borrow **explicit на каждом binding site** через:
1. **`consume X = expr`** — ownership-binding, X владеет
2. **`let X = expr`** — value-binding, разрешён ТОЛЬКО для не-consume-обязательных RHS
3. **Запретить view-binding внутри тела функции** — view существует ТОЛЬКО как
   function param (D157 carry-over, расширенный на reject в-теле)

## Правила (полный сет)

### Правило 1 — `consume` keyword обязателен на binding ownership-instance

`consume X = expr` требуется когда `expr` возвращает **consume-обязательную**
инстанс. Без keyword'а → ошибка.

```nova
type Token consume { val int }
fn Token.new(v int) -> Token => { val: v }

// ❌ ОШИБКА E_CONSUME_KEYWORD_MISSING
let tok = Token.new(7)

// ✅ OK
consume tok = Token.new(7)
```

**Когда RHS считается consume-обязательным:**
- Constructor consume-type'а (D133): `Token.new(...)`
- Функция возвращающая consume-type: `fn open_file() -> File consume`
- Member access consume-поля: `let x = self.consume_field` (если self — consume-aware)
- Возврат consume-метода: TBD — обычно methods consume self не возвращают instance, но edge case

**Когда RHS НЕ consume-обязательный (regular `let`):**
- Primitive: `let n = 42`
- String literal: `let s = "hi"`
- Regular record: `let r = Point { x: 1, y: 2 }` (если Point не consume)
- Non-consume method call: `let len = sb.len()` (len returns int)
- View-borrow result (внутри fn): не возникает потому что views forbidden in body
- View-borrow param (внутри fn получивший view): `let view_param_result = ...`

### Правило 2 — view-binding в теле fn запрещён

```nova
consume sb = StringBuilder.new()
sb.append("hi")

// ❌ ОШИБКА E_VIEW_BINDING_FORBIDDEN
let twin = sb

// ✅ OK — move ownership
consume twin = sb
// sb теперь dead; twin владеет

// ✅ OK — передать как view через function
fn read_len(view T) -> int => view.len()
let n = read_len(sb)  // sb остаётся Live; read_len получает view-param
```

**Error message (E_VIEW_BINDING_FORBIDDEN):**
```
error[E_VIEW_BINDING_FORBIDDEN]: view-binding на consume-обязательную переменную
запрещён внутри тела функции.
  --> file.nv:42:5
   |
42 |     let twin = sb
   |     ^^^^^^^^^^^^^
   |
   = note: view-bindings разрешены ТОЛЬКО как function-параметры (D157).
   = note: dangling-view risk: если `sb` потом потреблён, `twin` указывает в "никуда".
   = suggestion: для transfer ownership используй:
                 consume twin = sb
   = suggestion: для view используй function call:
                 fn use_view(view T) -> R => …
                 let r = use_view(sb)  // sb остаётся Live
```

### Правило 3 — `consume X = consume_var` = move

```nova
consume sb = StringBuilder.new()
consume sb2 = sb        // ← move: sb dead, sb2 owns
// sb.append("late")    // ← ERROR D131-use-after-consume
```

### Правило 4 — `consume mut X = expr` запрещён (уже)

Существующий reject в [parser/mod.rs:3311](compiler-codegen/src/parser/mod.rs#L3311) сохраняется.

### Правило 5 — view как параметр сохраняет lifetime caller'а

```nova
fn read_len(view sb T) -> int => sb.len()

fn main() {
    consume sb = StringBuilder.new()
    let n = read_len(sb)      // view-borrow на duration of call
    sb.append("more")          // OK — sb всё ещё Live (view вернулся)
    consume v = sb.into()      // OK — consume в конце
}
```

Safe by construction: view живёт только время вызова; owner (`sb`) переживает
вызов by stack semantics.

### Правило 6 — consume obligation в-scope check (carry from D131)

Если переменная объявлена `consume X = …`, она ОБЯЗАНА быть consumed до конца
scope'а. Это уже D131; здесь только syntactically более visible.

```nova
fn must_consume() {
    consume tok = Token.new(7)
    // ❌ ОШИБКА D131-forgot-to-consume — tok не потреблён до конца fn
}
```

## Scope

### In scope

- **Type-checker enforcement** в `compiler-codegen/src/types/mod.rs` (production-grade flow analysis, не shortcut'ы):
  - Полный tracking consume-obligated RHS expressions: ctor of consume-type, fn return of consume-type, member of consume-field, return of consume method (если applicable), generic propagation Option[T]/Result[T,E] где T consume (per D156)
  - При `let X = consume_obligated_expr` → emit `E_CONSUME_KEYWORD_MISSING` с
    machine-applicable suggestion (`consume X = ...`)
  - При `let X = consume_var` (alias) → emit `E_VIEW_BINDING_FORBIDDEN`
  - При `consume X = expr` где expr НЕ consume-obligated → emit warning
    `W_CONSUME_KEYWORD_UNNECESSARY` (производственно полезно — ловит cargo-cult)
- **Error codes + Diagnostic infra** (`compiler-codegen/src/diag.rs`):
  - `E_CONSUME_KEYWORD_MISSING` — Suggestion::insert("consume "), Applicability::MachineApplicable
  - `E_VIEW_BINDING_FORBIDDEN` — Suggestion с 2 вариантами (move / pass to function)
  - `W_CONSUME_KEYWORD_UNNECESSARY` — Suggestion::delete("consume ")
  - Все 3 — Plan 50 D102 format (header + code + span + note + suggestion)
- **Spec amendment** в `spec/decisions/05-memory.md`:
  - **D131 amendment** (consume binding syntax — 3 правила, полный сет)
  - Или **новый D175** если решено в Ф.1 (decision-point Ф.1: amend vs new) — TBD by design review в Ф.1
- **Q-section updates** в `spec/open-questions.md`:
  - Любой Q про ownership-visibility / binding-syntax — пометить closed by D131-amend/D175
  - Если открыт Q-view-lifetime (lifetime tracking) — пометить closed-by-restriction («не вводим — view только как param»)
- **Документация** (`docs/`):
  - `docs/consume-types.md` (если есть) или новый idiom doc — описание `consume X = …` pattern
  - `docs/migration/d131-binding-syntax.md` — migration guide (для разработчиков-пользователей)
  - `docs/nova-cli.md` — обновить если `nova consume-migrate` extended на binding syntax
  - `nova doc` integration — error codes должны рендериться correctly в hover (Plan 100.8 D166)
- **Тестовое покрытие** — миграция всех plan73/ тестов + добавление comprehensive neg-tests
- **Stdlib sweep** — все `let X = …` где X consume-obligated → `consume X = …`
  - sync.nv (после Plan 103.9 — там MutexGuard/ReadGuard/WriteGuard/Permit/OnceGuard массив)
  - stdlib `with_lock(fn)`, `with_permit(fn)` helpers — internal `let guard = …` сайты
  - std/io (File), std/net (TcpStream/UdpSocket — Plan 83.12), std/sync (Plan 103.9)
- **plan100_/plan103_9/plan73/ tests sweep** — migrate `let` → `consume` где применимо

### Out of scope

- **Полная linear-type semantic** (must-consume = ровно 1 раз) — это Plan 80
- **Cross-function inference** — пользователь annot'ит каждое binding
- **Auto-migration tool** (`nova consume-bind-migrate`) — Plan 100.7 territory;
  можно extend существующий `nova consume-migrate`
- **Closure capture rules** — view-by-default closure capture (D157) сохраняется;
  если closure capture'ит consume-obligated var, рассматривается как consume-closure
  (Plan 100.3)

## Industry comparison

| Аспект | Nova 73.1 | Rust | Go | TypeScript | Swift |
|---|---|---|---|---|---|
| Explicit ownership на binding | ✅ `consume X = …` | ⚠ implicit move (`let x = y`) | ❌ GC | ❌ GC | ⚠ implicit move на `consuming func` |
| View-binding в теле | ❌ запрещён (no lifetime) | ✅ `&x` + lifetime | n/a | n/a | ✅ borrow с lifetime |
| Lifetime аннотации | ❌ не требуются | ✅ `<'a>` | n/a | n/a | partial (~implicit) |
| Dangling view возможен | ❌ by construction | ⚠ требует borrow-checker | n/a | n/a | ⚠ Swift exclusivity |
| Visible move on assignment | ✅ keyword | ❌ silent | n/a | n/a | ⚠ implicit |

**Nova edge:**
- Visible ownership transfer на каждом binding (unique)
- Zero lifetime annotations (vs Rust)
- No dangling view by construction (vs Rust borrow-checker complexity)
- Безопасность achieved через restriction (view only as param) вместо lifetime tracking

## Фазы

### Ф.0 — Baseline audit (~0.3d)

- Найти все `let X = …` в:
  - `std/` где X consume-obligated (sync.nv MutexGuard/ReadGuard/Permit/OnceGuard
    + любые plan100/103.9 миграции)
  - `nova_tests/plan73/`, `plan100_*/`, `plan103_9/`, `concurrency/sync_*`
- Список migration candidates
- Decision-point: full strict mode vs warning-then-error (опционально через
  edition gate Plan 62.F.bis)

### Ф.1 — Spec amend (~0.3d)

- D131 amendment в `spec/decisions/05-memory.md` (или новый D175 — TBD by Ф.1
  designer)
- Полный сет правил (см. выше)
- Cross-refs к D133, D157, D170 (consume guards)

### Ф.2 — Type-checker enforcement (~1.5d)

- `consume_obligated_expr(e) -> bool` helper в types/mod.rs
- При `Stmt::Let` без consume keyword:
  - Если RHS consume-obligated → `E_CONSUME_KEYWORD_MISSING`
  - Если RHS — consume-var (alias case) → `E_VIEW_BINDING_FORBIDDEN`
- При `Stmt::Let` с consume keyword:
  - Если RHS не consume-obligated → `W_CONSUME_KEYWORD_UNNECESSARY` (optional)
- Машинно-применимые suggestions через `Applicability::MachineApplicable`

### Ф.3 — Migrate plan73/ tests (~0.3d)

12 файлов: positive — заменить `let` на `consume` где применимо; negative —
обновить EXPECT_COMPILE_ERROR на новые error codes если затронуты.

### Ф.4 — Migrate plan100_*/plan103_9 tests (~0.7d)

Mechanical sweep. Использовать sed-like + manual review для пограничных cases.

### Ф.5 — Stdlib sweep (~0.5d)

`std/` — `let guard = mu.lock()` → `consume guard = mu.lock()` и т.д. для всех
sync guards (после Plan 103.9 много мест).

### Ф.6 — Tests (~0.3d)

Новые negative tests для каждого error code:
- `binding_let_consume_obligated_neg.nv` → E_CONSUME_KEYWORD_MISSING
- `binding_let_view_in_body_neg.nv` → E_VIEW_BINDING_FORBIDDEN
- `binding_consume_unnecessary_warn.nv` → W_CONSUME_KEYWORD_UNNECESSARY (opt)
- Positive tests для каждой нормальной формы

Плюс property test: «после Ф.4 sweep, full plan100_/plan103_9 PASS».

### Ф.7 — Close (~0.2d)

**Обязательные обновления (none-optional, делать все):**

1. **`docs/plans/73.1-consume-binding-syntax.md`** — header status →
   ✅ ЗАКРЫТ YYYY-MM-DD; финальный «## Status — closure summary» блок (см. ниже)
2. **`docs/plans/README.md`** — добавить row для 73.1 → ✅
3. **`spec/decisions/README.md`** — добавить D131-amend (или D175) в «Свежие
   D-решения»
4. **`spec/open-questions.md`** — Q-cleanup (см. Ф.1)
5. **`docs/project-creation.txt`** — финальная запись по Plan 73.1: что
   зашиплено + metrics (X positive, Y negative, Z migration sites swept)
6. **`docs/simplifications.md`** — записать любые design simplifications
   (например «view-binding в теле запрещён вместо lifetime tracking» — это
   conscious simplification относительно Rust); + любые deferred-with-marker
   items
7. **`d:/Sources/nv-lang/nova-private/discussion-log.md`** (отдельный приватный
   репо — см. memory project-worktree-nova-test-setup для контекста) —
   нарратив YYYY-MM-DD entry: что узнали, ключевые design-решения, lessons,
   как влияет на ergonomics
8. **`spec/decisions/05-memory.md`** — D131 amendment (или D175) finalized (не draft)

**Закрывающая запись в plan-doc (раздел `## Status — closure summary`):**

```markdown
## Status — closure summary

> Заполняется при ✅ closure (не раньше).

- **Дата закрытия:** YYYY-MM-DD
- **Merge hash в main:** `xxxxxxx`
- **Status:** ✅ ЗАКРЫТ
- **Реализовано без упрощений (production-grade):** да / honest-defer items below
- **Test results:** X/Y plan73_1 PASS (Z neg + W prop); 0 регрессий relative pre-73.1 baseline
- **Migration sweep stats:**
  - plan73/: A tests migrated
  - plan100_*/: B tests migrated
  - plan103_9/: C tests migrated
  - std/: D files swept
- **Spec D-блок:** D131-amend (или D175) finalized в spec/decisions/05-memory.md
- **Q-section status updates:** [список Q's closed/partial-closed]
- **Honest-defer markers** (если есть):
  - `[M-73.1-xxx]` — описание
- **Lessons learned:** [2-3 пункта]
- **Reference в memory:** [[project-plan73_1-status]] (создать в Ф.7)
```

Дополнительно создать memory entry `project-plan73_1-status.md` (формат как
другие plan-status файлы) + добавить в `MEMORY.md` index.

## Tests breakdown

Все тесты — в `nova_tests/plan73_1/` (underscore convention, D29). EXPECT_TIMEOUT_MS
≥60000 для safety (cold-cache compile под jobs=16).

### Positive (≥10)

1. `binding_consume_simple.nv` — `consume tok = Token.new(7); tok.release()`
2. `binding_consume_move.nv` — `consume a = ctor(); consume b = a; b.release()` (move chain)
3. `binding_consume_returned.nv` — function returns consume var: `fn f() -> Token consume => Token.new(1)`
4. `binding_consume_in_record_field.nv` — `Record { consume_field: consume X }` constructor
5. `binding_let_primitive.nv` — `let n = 42` (без consume, regular path)
6. `binding_let_value_record.nv` — `let p = Point { x: 1 }` regular type
7. `binding_let_non_consume_method.nv` — `consume sb = …; let n = sb.len()` (len NOT consume)
8. `binding_view_as_param.nv` — view-borrow через function param works
9. `binding_view_call_chain.nv` — fn принимает view, view внутри callee читает поля
10. `binding_consume_generic_propagation.nv` — `consume opt = Some(Token.new(1))` (Option[T] где T consume — D156)
11. `binding_consume_match.nv` — `consume tok = …; match consume tok { ... }` (D157 match consume)

### Negative (≥8)

12. `binding_let_consume_obligated_ctor_neg.nv` — `let tok = Token.new(1)` → E_CONSUME_KEYWORD_MISSING
13. `binding_let_consume_obligated_fn_neg.nv` — `let f = open_file(path)` где open_file → File consume → E_CONSUME_KEYWORD_MISSING
14. `binding_let_view_in_body_neg.nv` — `consume sb = …; let twin = sb` → E_VIEW_BINDING_FORBIDDEN
15. `binding_let_view_after_consume_neg.nv` — `consume sb = …; sb.into(); let dangling = sb` (двойная ошибка: D131 + E_VIEW_BINDING_FORBIDDEN — verify primary diagnostic chosen correctly)
16. `binding_consume_unnecessary_warn.nv` — `consume n = 42` → W_CONSUME_KEYWORD_UNNECESSARY
17. `binding_consume_mut_neg.nv` — `consume mut tx = …` → existing parser error preserved
18. `binding_consume_use_after_neg.nv` — D131-use-after-consume (regression coverage)
19. `binding_consume_forgot_neg.nv` — `consume tok = Token.new(1)` без consume в scope → D131-forgot-to-consume (regression)

### Property (≥3)

20. `binding_consume_obligation_scope_prop.nv` — для всех valid bindings: каждая
    consume-obligation удовлетворена ровно один раз (parse фикстуры + check via test-only counter)
21. `binding_no_dangling_view_prop.nv` — после Ф.4 sweep, не существует pattern
    «`consume X = …; let Y = X` где Y используется после consume(X)» в любом
    тесте/std-файле — grep-based property
22. `binding_migration_completeness_prop.nv` — после Ф.5 sweep, `nova check std/`
    clean (0 E_CONSUME_KEYWORD_MISSING / E_VIEW_BINDING_FORBIDDEN); регрессионный
    guard

### Stress / regression

- Full `nova test --filter "plan73|plan73_1|plan100|plan103_9|concurrency/sync"` — 0 regressions relative pre-Plan-73.1 baseline (cached в audit-report Ф.0)

## Acceptance criteria

**Production-grade — все пункты обязательны, не cherry-pick.**

### Compiler / type-checker
- [ ] Type-checker enforce полный — consume-obligation flow analysis работает на all source kinds: ctor, fn return (включая Result/Option propagation per D156), member-access, generic substitution
- [ ] 3 error codes (`E_CONSUME_KEYWORD_MISSING`, `E_VIEW_BINDING_FORBIDDEN`,
      `W_CONSUME_KEYWORD_UNNECESSARY`) с machine-applicable suggestions Plan 50 D102 format
- [ ] Compile-error spans точны (указывают на `let`, не на whole statement)
- [ ] LSP integration — quick-fix предлагает `consume X = …` (Plan 100.8 D166 hover)

### Spec / docs
- [ ] D131 amended (или новый D175) finalized — не draft — в `spec/decisions/05-memory.md`
- [ ] `spec/decisions/README.md` обновлён (свежие D-решения)
- [ ] `spec/open-questions.md` — relevant Q's закрыты/partial-closed с D-block refs
- [ ] `docs/consume-types.md` или idiom doc обновлён
- [ ] `docs/migration/d131-binding-syntax.md` migration guide написан
- [ ] `docs/plans/README.md` row 73.1 → ✅
- [ ] `docs/plans/73.1-*.md` header status → ✅ + closure summary block заполнен

### Tests
- [ ] ≥10 positive + ≥8 negative + ≥3 property в `nova_tests/plan73_1/` PASS
- [ ] Полная миграция 12 plan73/ тестов на новый синтаксис — все PASS
- [ ] Plan 100.x sweep: plan100_1-8 tests PASS после миграции (или с
      documented `[M-73.1-test-migration-X]` markers — НЕ silent regressions)
- [ ] Plan 103.9 sweep: plan103_9 + concurrency/sync_*_guard тесты PASS
- [ ] Stdlib `std/` мигрирован — `nova check std/` clean (0 ошибок)

### Regression
- [ ] Full `nova test` regression — 0 НОВЫХ FAIL relative pre-Plan-73.1 baseline
      (записать pre-baseline в Ф.0 audit-report для precise сравнения)
- [ ] Cross-platform Windows + Linux clang (Linux best-effort если нет CI)

### Process / logs
- [ ] `docs/project-creation.txt` — финальная Plan 73.1 entry
- [ ] `docs/simplifications.md` — design simplifications + any deferred-with-marker items
- [ ] `nova-private/discussion-log.md` — нарратив 2026-XX-XX entry
- [ ] Memory `project-plan73_1-status.md` создан + linked в MEMORY.md
- [ ] Все [M-73.1-xxx] honest-defer markers documented, не silent

### Production-grade gate
- [ ] **НИ ОДНОГО silent shortcut'а / partial-mode / mock'а** — если что-то не
      сделано, оно в честно-documented `[M-73.1-xxx]` маркере с rationale.
- [ ] **НИ ОДНОЙ выдуманной фичи** — каждое API decision backed spec D-блоком
      или existing precedent.

## Risks + mitigations

| Риск | Mitigation |
|---|---|
| Sweep ломает много существующего кода | Edition-gate через Plan 62.F.bis (опционально) — V1: warnings, V2: errors; миграция через `nova consume-migrate` extension |
| `consume_obligated_expr` flow analysis сложен (cross-fn return types) | Conservative initial impl: только direct ctor + direct fn-call с decl'ed consume return; remote inference deferred |
| Существующие plan100_/103.9 tests могут сломаться массово | Ф.4 sweep по одному файлу с verify cycle; deferred tests → `[M-73.1-test-migration-X]` markers |
| Stdlib sync guards (Plan 103.9) везде `let guard = …` → массивный sweep | Грeplable; semantic-неизменный rewrite; minimal merge conflict risk |
| Закрытие зависит от parallel work других agents (main движется) | Worktree base zafix; merge при close — конфликты в std/ resolved by re-running sweep |

## Связи

- [Plan 73](73-consume-qualifier.md) — D131 foundation (consume на receiver/param)
- [Plan 100.1](100.1-core-must-consume.md) — D133 type-level consume (`type T consume { … }`)
- [Plan 100.3](100.3-borrow-and-view.md) — D157 view-default + closure capture
- [Plan 100.7](100.7-stdlib-migration-playbook.md) — D165 stdlib migration playbook
- [Plan 100.8](100.8-performance-ide-tooling.md) — D166 consume DX layer (LSP)
- [Plan 103.9](103.9-consume-guards-migration.md) — D174 sync guards (массив sweep target)
- [Plan 62.F.bis](62.F.bis-edition-shadow-and-runtime-effects.md) — edition gating
  для migration warnings→errors

## Appendix A — Migration sweep audit (Ф.0 result, 2026-05-28)

### Migration target counts

| Категория | Files affected | Approx `let X = …` consume-obligated sites |
|---|---|---|
| `nova_tests/plan73/` | 12 | ~12 (1-2 per fixture) |
| `nova_tests/plan100_*/` | 38 (out of ~150) | ~50 (требует precision-check Ф.4) |
| `nova_tests/plan103_9/` | 20 | ~1 (большая часть уже `consume X = …` синтаксисом) |
| `nova_tests/concurrency/sync_*_guard.nv` + permit/once_guard/rwlock_guards | 4 | 23 (pilot fixtures from Plan 103.9) |
| `std/runtime/sync.nv` | 1 | 0 — уже использует `consume X = …` (Plan 103.9 implementation) |
| `std/runtime/*` other | TBD Ф.5 | TBD |

### Key findings

1. **stdlib `std/runtime/sync.nv` УЖЕ использует `consume X = …` syntax**
   (sites 1545, 1657, 1668, 2391 — Plan 103.9 implementation). Это означает
   парсер давно поддерживает syntax; Plan 73.1 добавляет **enforcement** на
   type-checker уровне.
2. **plan103_9 fixtures** в большинстве уже мигрированы (написаны
   изначально Plan 103.9 agent'ом со `consume X = …`); только ~1 site
   нуждается в дальнейшей проверке (Ф.4).
3. **plan73/ fixtures** (12) — все 100% старый `let X = …` syntax; требуют
   полную миграцию (Ф.3).
4. **plan100_*/** — большой scope ~38 файлов; нужна precise classification
   в Ф.4 (consume-obligated vs regular `let`).
5. **D-block numbering** — следующий свободный slot D180 (D175-D179 заняты:
   readonly, str API, StringBuilder). D173/D174 имеют collisions между файлами
   (06-concurrency vs 08-runtime/07-modules) — известная проблема spec,
   не scope 73.1.

### Decision: D-block

**Новый D180** (а не D131-amend). Rationale в D180 §«Что отвергнуто».

### Migration strategy

- Ф.3 plan73/: full migration `let → consume` (mechanical sweep + test re-run)
- Ф.4 plan100/plan103_9/concurrency: per-file classification; consume-obligated `let` → `consume`
- Ф.5 std/: проверить остальные std/ files (std/io, std/net если есть consume types)

## Appendix B — Pre-baseline regression snapshot (Ф.0 result, 2026-05-28)

> Targeted baseline (только areas потенциально затронутые Plan 73.1).
> Полный nova test — отдельно при close (Acceptance criterion).

| Filter | PASS | FAIL | Notes |
|---|---|---|---|
| `plan73` | 12 | 0 | clean baseline |
| `plan100` | TBD (выполняется) | TBD | TBD |
| `plan103_9` | TBD | TBD | TBD |
| `concurrency/sync_` | TBD | TBD | TBD |

Полное обновление Appendix B будет при закрытии Ф.7.

## Status — closure summary

> ⚠️ Заполняется при ✅ closure (Ф.7), не раньше. Все поля обязательны для
> production-grade close.

- **Дата закрытия:** TBD
- **Merge hash в main:** TBD
- **Status:** 📋 proposed (после close → ✅ ЗАКРЫТ)
- **Реализовано без упрощений (production-grade):** TBD
- **Test results:** TBD (X/Y plan73_1 PASS — Z neg + W prop)
- **Pre-baseline regression check:** full `nova test` results before/after
- **Migration sweep stats:**
  - plan73/: TBD tests migrated
  - plan100_*/: TBD tests migrated
  - plan103_9/: TBD tests migrated
  - concurrency/sync_*: TBD tests migrated
  - std/: TBD files swept
- **Spec D-блок:** D131-amend OR D175 finalized в spec/decisions/05-memory.md
- **Q-section status updates:** TBD list closed/partial-closed
- **Documentation written:**
  - [ ] docs/consume-types.md (или idiom doc)
  - [ ] docs/migration/d131-binding-syntax.md
- **Logs updated:**
  - [ ] docs/project-creation.txt
  - [ ] docs/simplifications.md
  - [ ] nova-private/discussion-log.md
  - [ ] Memory project-plan73_1-status.md + MEMORY.md index entry
- **Honest-defer markers** (если есть — НЕ silent):
  - `[M-73.1-xxx]` — TBD description + rationale
- **Lessons learned:** TBD 2-3 пункта
- **Reference в memory:** TBD `[[project-plan73_1-status]]`

## Appendix B — Pre-baseline regression snapshot (TODO Ф.0)

> Снять `nova test` baseline перед Ф.2 start. Записать сюда:
> `PASS: NNNN  FAIL: NN  SKIP: NN` + список FAIL test names (для precise
> сравнения post-implementation).
