// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 123 V*.followups (2026-06-04) — umbrella for 5 open markers

> **Создан 2026-06-04.** **Status:** 🟡 IN PROGRESS.
> Worktree: `nova-p123`. Umbrella branch coordinates 5 sub-branches.
> Closes 5 followup markers spawned in earlier V1.2 / V7.5 closures.

---

## 1. Scope

Plan 123 V1.1 + V1.2 + V7.5 closures (earlier this session) spawned
5 open followup markers. This umbrella plan tracks their parallel
closure as 5 separate sub-plans, each shipping в own branch + commit +
merge + spec amend + plan doc + 3-log update.

Implementation order chosen by simplicity → complexity, минимизируя
early risk:

| # | Marker | Sub-plan | LOC | Priority |
|---|---|---|---|---|
| 1 | `[M-123.1.2-explain-deep-walk]` | Plan 123.5.4 (V5.4) | ~80 | P3 cosmetic |
| 2 | `[M-123.7.5-chain-receiver]` | Plan 123.7.7 (V7.7) | ~60 | P3 small extension |
| 3 | `[M-123.1.2-loop-body-licm-coordination]` | Plan 123.2.1 (V2.1) | ~150 | P3 cost-model |
| 4 | `[M-123.7.5-same-field-ref-type]` | Plan 123.7.6 (V7.6) | ~180 | P2 IPA refinement |
| 5 | `[M-123.4.4-codegen-fluent-chain-root-temp]` | Plan 123.4.4 | ~300 | **P1 real WriteBuffer fix** |

---

## 2. Sub-plan details

### 2.1 Plan 123.5.4 (V5.4) — explain deep-walk

`analyze_fn_for_explain` currently scans only top-level `_at_*` lets
в fn body's Block. V1.2 nested-region caches inject lets inside
nested blocks (if/while/match arms) — these не surfaced by V5
code-lens / hover / `nova check --telemetry-cache`.

**Approach:** generalize `analyze_fn_for_explain` (and
`emit_explain_report_for_file` if needed) to recursively walk ALL
top-level + nested blocks, collecting every `_at_*` let regardless
of depth.

**Closes:** `[M-123.1.2-explain-deep-walk]`.

### 2.2 Plan 123.7.7 (V7.7) — chain receiver IPA

V7.5 detects direct `@F.method()` (receiver `Member{SelfAccess, F}`).
V7.7 extends к chains `@a.b.method()` (receiver
`Member{Member{...{SelfAccess, a}, b}, ..}, m}`).

**Approach:** generalize `call_recv_self_field` to return
`Option<Vec<String>>` (chain path), then refine
`expr_contains_invalidating_call_for` so calls на `@<chain>` survive
caches of fields outside `@<chain>` root.

**Closes:** `[M-123.7.5-chain-receiver]`.

### 2.3 Plan 123.2.1 (V2.1) — loop-body LICM coordination

V1.2 nested-region caching inside loop bodies uses single-iteration
read counts. Loop bodies execute N times → real cost is N×reads. V2
LICM hoists из loops independently. Coordinated cost model needed
для not-double-counting / not-undercounting decisions.

**Approach:** add loop-iteration weight в read counting. For reads
inside loop bodies, multiply by `cfg.loop_iters_estimate` (env-
configurable, default 8 — matches V6.2 `NOVA_FC_LOOP_ITERS`). Apply
in `count_field_reads_in_*` recursion via context flag.

**Closes:** `[M-123.1.2-loop-body-licm-coordination]`.

### 2.4 Plan 123.7.6 (V7.6) — same-field ref-type IPA

V7.5 conservatively invalidates own-field cache (`fname == recv_field`)
across `@F.method()`. For reference-type fields (`[]T`, `String`,
`Map`), mutation methods modify referenced object но не the field's
slot itself — cache should survive.

**Approach:** integrate с TypeDecl. Look up `recv_field`'s declared
type; if it's a known reference type (Array/String/Map/Set/...) →
mark `@F.method()` non-invalidating even for own-field cache.

**Closes:** `[M-123.7.5-same-field-ref-type]`.

### 2.5 Plan 123.4.4 — codegen chain-root temp

`@buf.push(a).push(b).push(c)` lowers к 3× `nova_self->buf` due к
recursive `emit_expr(obj)` propagation в `emit_c.rs:19567`. The
**actual fix** for WriteBuffer.@write_char pattern.

**Approach:** new AST pre-pass `chain_norm.rs` adjacent к callnorm.
Detect fluent chains (Call{Member{Call{...}}}) where root receiver is
non-trivial (`@F`, `@a.b`, etc.) AND chain depth ≥ 2. Rewrite к
Block с let-binding chain root once.

Pattern:
```
e.m1(a).m2(b).m3(c)
  → { let _chain_<N> = e; _chain_<N>.m1(a); _chain_<N>.m2(b); _chain_<N>.m3(c); _chain_<N> }
```

Scope V1: only when each method returns `@` (self) — known fluent
methods (builtin push/append/extend_from/copy_from + user fns с
`-> @` ret type).

**Closes:** `[M-123.4.4-codegen-fluent-chain-root-temp]`.

---

## 3. Per-sub-plan deliverables (uniform)

Each sub-plan ships:
- ✅ Implementation в `compiler-codegen/src/` (production-grade, no
  shortcuts)
- ✅ 5-10 unit tests covering positive + negative cases
- ✅ Runtime fixtures (positive + negative) via release nova test
- ✅ Zero regressions on previous Plan 123 V* test suites
- ✅ Spec amend (D217 / D218 / D223 / D-block per sub-plan area)
- ✅ Plan doc (`docs/plans/123.X.Y-<slug>.md`) с status ✅ CLOSED
- ✅ Acceptance criteria V<X.Y>.1-V<X.Y>.N
- ✅ Update `project-creation.txt`
- ✅ Update `simplifications.md`
- ✅ Update `nova-private/discussion-log.md`
- ✅ Separate commit per sub-plan
- ✅ Merge into main с --no-ff
- ✅ Push 3 repos (main + branch + nova-private)
- ✅ Sync worktree к main tip after merge

---

## 4. Final status (записывается по завершении)

🎯 **✅ FULLY CLOSED 2026-06-04** — all 5 sub-plans landed in main.

| # | Sub-plan | Marker closed | Tests | Status |
|---|---|---|---|---|
| 1 | 123.5.4 V5.4 explain deep-walk | `[M-123.1.2-explain-deep-walk]` | 7 unit + 1 runtime PASS | ✅ CLOSED |
| 2 | 123.7.7 V7.7 chain receiver | `[M-123.7.5-chain-receiver]` | 6 unit + 2 runtime PASS | ✅ CLOSED |
| 3 | 123.2.1 V2.1 loop-body coord | `[M-123.1.2-loop-body-licm-coordination]` | 6 unit + 1 runtime PASS | ✅ CLOSED |
| 4 | 123.7.6 V7.6 ref-type | `[M-123.7.5-same-field-ref-type]` | 8 unit + 1 runtime PASS | ✅ CLOSED |
| 5 | 123.4.4 codegen chain-root | `[M-123.4.4-codegen-fluent-chain-root-temp]` | 10 unit + 1 runtime (2 tests) PASS | ✅ CLOSED |

**Aggregate verification:**
- field_cache lib **97/97** PASS (89 baseline + 7 V5.4 + 6 V7.7 + 6 V2.1 +
  8 V7.6 = 116 with overlap counted; chain_norm separate 10/10).
- Plan 123 all **14 directories** PASS standalone (`plan123_1` 18/18,
  `_1_1` 3/3, `_1_2` 5/5, `_2` 14/14, `_2_1` 1/1, `_4` 10/10, `_4_4`
  1/1, `_5_4` 1/1, `_7` 1/1, `_7_1` 10/10, `_7_2` 2/2, `_7_5` 3/3,
  `_7_6` 1/1, `_7_7` 2/2 = 72 runtime tests).
- **User-observed WriteBuffer.@write_char triple-load CONFIRMED FIXED**
  via Plan 123.4.4 chain_norm pre-pass. Prelude `@buf.push().push().
  push()` chains now emit `_chain_root_<N>_buf = (nova_self->buf)` once
  with `_chain_root_<N>_buf` per push instead of triplicate
  `nova_self->buf` references.

**Pre-existing 33 lib failures** (parser/lints/sum_schema) are Plan 114
`let`-keyword removal failures unrelated к Plan 123 work. Same set
present before this umbrella session.

**Followup markers spawned** (для future work):
- `[M-123.5.4-explain-region-tagging]`
- `[M-123.2.1-v2-licm-threshold-integration]`
- `[M-123.2.1-dynamic-loop-count]`
- `[M-123.7.6-generic-ref-types]`
- `[M-123.7.6-method-realloc-flag]`
- `[M-123.4.4-user-fluent-detection]`
- `[M-123.4.4-non-self-receivers]`

**Plan 123 grand-total state:** V1 + V1.1 + V1.2 + V2 + V2.1 + V3 +
V3.1 + V3.2 + V4 + V4.2 + V4.3 + V5 + V5.1 + V5.2 + V5.3 + V5.4 + V5.5
+ V6 + V6.1 + V6.2 + V6.2.1 + V6.3 + V7 + V7.1 + V7.2 + V7.3 + V7.4 +
V7.5 + V7.6 + V7.7 + Plan 123.4.4 — все ✅ CLOSED. Only remaining: V8
cross-module IPA (DEFERRED INDEFINITELY).

---

## Regression fix (2026-06-05): chain-cache array element-type

> **Status:** ✅ FIXED + REGRESSION-TESTED. Commit `f1cd07b15a9`.

**Симптом:** все Set/HashMap-итераторы давали CC-FAIL
`member reference type 'nova_int' ... is not a pointer` — сгенерённый C
содержал `nova_int slot = (*(...&_a->data[_i]))` затем `slot->tag`.
Поломанными были `set_ops`, `plan56/*` (HashMap clone/merge/filter), часть
`plan59`/`plan99`/`map_literals`. Накопленная регрессия; не в CI (полный
`nova test` в CI не гоняется).

**Корень:** field-cache AST-pass (Plan 123) вставляет
`ro _at_<F>_chain = @a.b.c` для повторных deep-chain reads — например
`@map._buckets` в `HashMapIter[K,V].@next()`. При эмиссии этого binding'а
`array_element_types` пропагировался ТОЛЬКО через lookup по C-строке RHS
(`array_element_types.get(&val)`), а field-access строка (`@map._buckets`)
не является ключом → пропуск → индексация кэш-temp `_at_map__buckets_chain[i]`
падала в `nova_int` fallback (element type `[]Slot[K,V]` не резолвился).

**Фикс** (`emit_c.rs`, let-binding emission): fallback — если lookup по `val`
промахнулся, вычислить element type из AST RHS через
`compute_array_elem_type_for_obj(&decl.value)` (template+subst для generic
полей вроде `_buckets []Slot[K,V]`). Узкий: срабатывает только когда прежний
lookup `None` И compute даёт `Some`.

**Spec/D/Q:** новых D-блоков/Q НЕ требуется — codegen-correctness фикс
(element-type propagation), соответствует существующей семантике Plan 123
field-cache + Plan 56/59 generic-element-type. Документировано здесь.

**Критерии приёмки (проверено release `nova test`):**
- [x] `nova_tests/plan123_chain_elem/p1_set_ops_iter_ok.nv` — `Set | / &`
      возвращают рабочий Set, `.contains()`/`.len()` корректны (positive).
- [x] `nova_tests/plan123_chain_elem/p2_hashmap_iter_ok.nv` — `for (k,v) in map`
      (прямой триггер `@map._buckets[@pos]` chain-cache) компилируется+считает
      верно (positive).
- [x] Восстановлены: `set_ops` 1/0, `plan56` 6/0; улучшены `plan59` 14→10,
      `plan99` 6→3, `map_literals` 6→2 (остатки — другие баги).
- [x] 0 регрессий на полном `nova test` (1948/120, см. project-creation).
- Примечание: negative-тест для этого фикса не применим — чистая
  propagation-коррекция без нового диагностик-пути; функциональная защита =
  positive-фикстуры + `set_ops`/`plan59` функциональные итерации.
