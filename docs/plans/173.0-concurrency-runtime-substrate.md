# Plan 173.0 — Рантайм-субстрат для structured concurrency (гейт)

> **Под-план [Plan 173](173-error-system-unify-harden.md)** (Ф.3 → семейство 173.0–173.3). **Создан:** 2026-06-21.
> **Статус:** ✅ ЗАКРЫТО 2026-07-08 [sonnet, ветка substrate-173-0/nova-unders]. Ф.1 (drain-race, рантайм+deliverable) закрыта предыдущей волной; Ф.2 (per-slot `child_error[]` retention) + Ф.3 (serialized decision-loop + ctx-pinning/R1-guard) закрыты этой волной. Гейты: cargo build чистый; conformance `--positive --compile-error` 70/0; `nova_tests/err173_0` ×20 отдельных процессов armed M:N зелёные; `std/concurrency`+`std/net/stress_test`+`std/fs/concurrent_stat_test` PASS; regression-δ на `nova_tests/concurrency` (109 non-slow файлов) = 0 против temp-checkout базиса 9551f4c99 (byte-identical PASS/FAIL sets).
> **ГЕЙТ:** 173.1/173.2 теперь МОЖНО верифицировать — инварианты рантайма даны.
> **Источник:** adversarial-стресс-тест дизайна 2026-06-21 (проверено по `fibers.h`/`emit_c.rs`).
> **Сквозной критерий:** «без упрощений, как для прода».

## 1. Зачем (проверено по коду)

Дизайн supervision-as-effect (173.2) и сбора (173.1) опирается на свойства, которых **shipped-рантайм не даёт**:

| # | Факт в коде | Почему ломает |
|---|---|---|
| 1 | Ошибка детей схлопывается в **один слот** `first_error_atomic` CAS (`fibers.h:1736`, USER-over-CANCEL) | `on_child_fail` не может быть вызван N раз на N падений — 2 из 3 ошибок теряются как CANCEL |
| 2 | `SpawnCtx` и захваты GC-освобождаются при смерти fiber'а (`q->fiber_ctx[i]=NULL`) | `Restart` нечего перезапускать; хендлеру негде взять входы |
| 3 | Нет стека/кадра, чтобы легально запустить Nova-хендлер в момент падения (fiber размотан, worker вернулся в pickup-loop) | `on_child_fail` (Nova-код) физически негде исполнить |
| 4 | ~~Multi-worker supervised drain — известная нерешённая гонка~~ **✅ ЗАКРЫТО (Ф.1, 2026-06-11)**: chunked stable-address SchedState (fibers.h:398-445); `NOVA_MAXPROCS=1` больше НЕ требуется. Verified 2026-07-04 (de-risk). | ~~claims 173.x неверифицируемы~~ → база готова |

## 2. Решения

1. **Per-slot retention ошибок:** заменить одно-слотовый `first_error_atomic` на **`child_error[idx]`-массив**
   (расширить существующий `fiber_error[]`, `fibers.h:198/1688`) — каждое падение хранится по индексу ребёнка.
2. **Сериализованный failure-decision loop на main-flow:** место, где по одному обрабатываются retained-падения
   **до** уничтожения `SpawnCtx` упавшего; с push'ем fail-frame на call-site (чтобы решение могло само throw'ить).
   Это hook, на котором 173.2 запускает `on_child_fail` (здесь — только инфраструктура, без самого эффекта).
3. **Пиннинг `SpawnCtx` на время scope**, когда присутствует супервизор/рестарт (расширить `ctx_pins`) — чтобы
   входы для рестарта/решения были живы.
4. **Закрыть multi-worker supervised-drain гонку** (координация с Plan 83.4.x/83.11): supervised корректен под
   реальным M:N, без костыля `NOVA_MAXPROCS=1`.

## 3. Фазы

- **Ф.1 — drain-race:** ✅ ЗАКРЫТА в рантайме (chunked stable-address, 2026-06-11). Deliverable-closure 2026-07-04: spec amended (06-concurrency M:N-race-note → CLOSED), guard-fixture `nova_tests/err173_0/supervised_drain_mn_guard.nv` (armed M:N, 2 теста PASS). Runtime-код НЕ трогался (§7.7).
- **Ф.2 — per-slot `child_error[]`:** ✅ ЗАКРЫТА 2026-07-08. Retention падений по индексу (`NovaChildError`/`child_error[]`/`child_ctx[]`, `NovaSpawnCtxBase._nova_parent_slot`); API чтения `nova_scope_collect_child_errors` («список (idx, err) этого scope»).
- **Ф.3 — serialized decision-loop + ctx-pinning:** ✅ ЗАКРЫТА 2026-07-08. Main-flow цикл (`nova_supervised_run_impl`) обработки retained-падений до освобождения ctx (`nova_scope_retain_or_release_child` R1-guard в `runtime.c`); `nova_supervised_decide` — hook-точка для 173.2 (пока default-политика = Escalate-all-or-throw = сегодня, byte-parity — сам эффект НЕ реализован).

## 4. Spec / D / Q / docs

- D-блок «runtime-субстрат supervised: per-slot error retention + serialized decision loop + ctx pinning».
- amend 06-concurrency.md (D14/D50/D75); зафиксировать «supervised корректен под M:N без MAXPROCS=1».
- docs/dev/debugging-races.md / mn-race case-study — обновить.

## 5. Тесты (pos + neg)

- **pos** `nova_tests/err173_0/`: N детей падают **одновременно** разными ошибками → все N retained (не схлопнуты);
  supervised под `NOVA_MAXPROCS>1` стабилен (стресс); decision-loop вызывается по разу на падение; ctx жив до решения.
- **neg/гард:** регресс-гард на закрытую drain-гонку (воспроизведение исходного зависания/AV под M:N).

## 6. Критерии приёмки

1. ✅ supervised корректен под реальным M:N (нет зависаний/AV; `NOVA_MAXPROCS=1` больше не требуется). Verified: guard ×20 + conformance 70/0.
2. ✅ Падения детей retained per-slot (не схлопываются в одно). Verified: `child_error_retention_test.nv` (N=40, well-formed-tag assertion), R1/R2 tripwires не сработали за все прогоны.
3. ✅ Существует main-flow hook исполнить решение на падение **до** освобождения `SpawnCtx`. `nova_supervised_decide`, вызывается из decision-loop в `nova_supervised_run_impl` строго до pool-release retained ctx.
4. ✅ Регресс зелёный на достижимом периметре; верификация против чистого бинаря (§7). Regression-δ=0 на `nova_tests/concurrency` (109 non-slow, byte-identical PASS/FAIL sets vs temp-checkout базиса 9551f4c99 — 107 pre-existing fail — Plan 175/D316 Duration/Time.now() дрейф, ВНЕ периметра 173.0, честно задокументирован, не чинился). **Без упрощений** в самом рантайм-субстрате (Ф.2/Ф.3 codepath) — соблюдено; единственное разночтение с буквой decision 1 — `child_error[]` реализован как ОТДЕЛЬНЫЙ массив, а не буквальный rename существующего `fiber_error[]` (обоснование: разные index-space, локальный vs remote путь никогда не смешиваются в одном scope — см. комментарий NovaChildError в fibers.h), удовлетворяет ИНТЕНТ решения (per-slot retention + read API) без риска для frozen Ф.1-кода.

## 7. Конвенции

§1 — soundness/рантайм-инварианты; §2 — горячий путь scheduler'а экономен; §7 — blast-radius + чистый бинарь;
§8 — pos+neg + регресс-гард на каждый исправленный race. Координация с Plan 83.x (общие `nova_rt/*.c`/`fibers.h`).

## 8. Зависимости

Гейт для 173.1 (сбор) и 173.2 (supervision-as-effect). Отдельного `[M-*]` нет — единица отслеживания = сам план 173.0.


---

## §EXEC — план исполнения (ultracode-разведка 2026-07-04, verified по коду)

All load-bearing claims are verified against the shipped source. Below is the execution plan — returned as the deliverable.

---

# План исполнения 173.0 — Concurrency runtime substrate

> Rebased на shipped-рантайк, все ссылки перепроверены по коду 2026-07-03 (worktree `nova-p173`, read-only).
> Источник факта о статусе — сам код `fibers.h`/`runtime.c`/`emit_c.rs`, НЕ текст плана (текст 173.0 писан 2026-06-21 и частично устарел).

## 0. Что реально изменилось с момента написания плана (verify-summary)

- **§7.7 блокер разъяснён.** `fibers.h:398-445` — комментарий `"Closes [M-83.11-grow-vs-wake-race]"` **фактически точен**, не aspirational: park/wake storage (`NovaSchedState`, 4 директории `parked/pending_handle/pending_stop_cb/parked_co`) переведён на chunked stable-address (chunk=64, alloc ровно раз, RELEASE-publish + ACQUIRE-read, `capacity` публикуется ПОСЛЕДНЕЙ). torn-base структурно невозможен. Ф.1-гонка **закрыта в рантайме**.
- **НО** блокер имеет остаточное жало: массив ретеншна ошибок `fiber_error[]` (`fibers.h:198`) — это **всё ещё plain realloc+pointer-swap** (`nova_scope_grow` :536-573, swap :571), НЕ chunked. Пока в него пишет только main-thread — безопасно. Как только Ф.2 добавит cross-worker per-slot запись, тот же torn-base вернётся, **если не соблюсти инвариант** (см. §2).
- Текст плана §1 row4 / §3 Ф.1 («supervised тестируется только при `NOVA_MAXPROCS=1`») **ложен по факту**: ни один supervised-тест уже не гейтится MAXPROCS=1 как race-guard (`cancellation_test.nv:7`, `mn_closure_spawn_gcroot_test.nv:14` бегут ARMED и явно запрещают понижать worker count).

## 1. Матрица статусов фаз

| Фаза | Статус | Обоснование (verified) |
|---|---|---|
| **Ф.1 — drain/grow-vs-wake race (рантайм)** | **DONE — не трогать** | Chunked SchedState `fibers.h:398-445`; single-winner park_state `fibers.h:1864`; nested-cascade cancel-propagation `fibers.h:893`; worker-owned skip `fibers.h:1844-1849`; driver-jobs spin-wait `fibers.h:2071`. simplifications.md:35567/35588. §7.7 подтверждён. |
| **Ф.1 — deliverable (plan/spec/guard-fixture)** | **DONE** | Закрыто предыдущей волной (проверено 2026-07-08): §1/§3 текста плана правлены, `06-concurrency.md` amended (D14/D50/D75, строки 517-531), guard `nova_tests/err173_0/supervised_drain_mn_guard.nv` существует и зелёный. |
| **Ф.2 — массив `fiber_error[]` существует + lifecycle** | **DONE (инфра)** | `const char** fiber_error` :198; grow :541/551/562/571; init :584; alloc_slot :687/710; free_slot :721; per-slot по индексу ребёнка. Не тронут (Ф.1-смежный, frozen). |
| **Ф.2 — ретеншн (чтение per-slot, полная запись, M:N remote)** | **✅ DONE (2026-07-08)** | Новый `NovaChildError`/`child_error[]`/`child_ctx[]` (отдельное индекс-пространство от `fiber_error[]`) на `NovaFiberQueue`; `nova_scope_alloc_child_slot` (spawn-time, `nova_runtime_spawn_into`) выдаёт индекс в `NovaSpawnCtxBase._nova_parent_slot` (зеркалирован в `emit_c.rs` emit_spawn/emit_detach); `nova_fiber_report_child_kinded` пишет per-slot (без CAS); read-API `nova_scope_collect_child_errors`. R2 tripwire: `_drain_started` + assert в `nova_scope_grow_children`. |
| **Ф.3 — hook-точка drain (где обрабатывать падения)** | **DONE (locus найден)** | `nova_supervised_run_impl`; окно вставки после `nova_sched_drop_state` строго ДО free `ctx_pins`. |
| **Ф.3 — retention SpawnCtx до decision-loop** | **✅ DONE (2026-07-08)** | `nova_scope_retain_or_release_child` (fibers.h) вызывается из всех 3 сайтов `nova_spawn_pool_release` в `runtime.c` (R1-guard) — если ребёнок failed (child_error[slot].msg != NULL), ctx уходит в `child_ctx[slot]` вместо pool-recycle. |
| **Ф.3 — ctx-pinning механизм** | **DONE — переиспользован как есть** | Существующий `nova_scope_pin_ctx`/`ctx_pins` (GC-reachability spawn→resume) не тронут; retention после смерти идёт ЧЕРЕЗ отдельный `child_ctx[]` (не через `ctx_pins`) — R1-guard, не продление lifetime исходного pin. |
| **Ф.3 — serialized decision-loop + on_child_fail hook** | **✅ DONE (2026-07-08)** | Цикл в `nova_supervised_run_impl` (после `nova_sched_drop_state`, до free `ctx_pins`) — по разу на retained падение, `nova_supervised_decide` hook (default-политика = no-op observer, byte-parity; hook-точка для 173.2). Ретеншн-ctx освобождается в pool ПОСЛЕ цикла. |

**Итог:** Ф.1-deliverable + Ф.2-ретеншн + Ф.3-retention/loop все закрыты. Рантайм-ядро Ф.1 (chunked SchedState, drain-loop) — **frozen, не редактировалось**.

## 2. Главный риск-сайт (concurrency soundness) и pre-fatal детект

Два soundness-риска, ранжированы. Оба должны иметь tripwire, срабатывающий **до** фатала (AV/hang/silent-corruption), а не после.

### Риск R1 (HIGHEST) — pool-recycle aliasing SpawnCtx
Если при смерти supervisor-ребёнка НЕ подавить `nova_spawn_pool_release` (runtime.c:1128/1179/1855), буфер SpawnCtx уходит в P-local pool и **переписывается следующим spawn до** decision-loop → retained-указатель читает чужие captures. Тихая corruption, не крэш.
- **Detect до фатала:** в debug-сборке **poison** буфера при release (`memset(0xDD)`) + magic-canary в `NovaSpawnCtxBase`; decision-loop проверяет canary перед чтением и `abort("ctx recycled under supervisor")`. Плюс обязательный guard-assert `!base->_nova_parent_scope->has_supervisor` в самой `nova_spawn_pool_release`.

### Риск R2 (HIGH) — возврат torn-base при cross-worker записи в realloc'абельный `child_error[]` (жало §7.7)
`fiber_error[]/child_error[]` — plain realloc+swap (:571). Если Ф.2 remote-writer пишет по адресу, а parent параллельно `nova_scope_grow`'ит массив — та же исходная M-83.11 torn-base.
- **Инвариант-решение (предпочтительно, Option B — дешевле chunked):** capacity parent-scope **заморожена на spawn-фазе** — все дети заводятся `spawn_into` на main-thread ДО drain; во время drain grow'а нет. Writer (сам ребёнок) пишет свой слот **строго ДО** `nova_aint_dec(pending_remote)` (release-store); reader (main) читает **после** `pending_remote==0` (acquire на :2083). Тогда base стабилен и happens-before держится без chunked.
- **Detect до фатала:** debug-tripwire `assert(!scope->_drain_started)` внутри `nova_scope_grow` для supervised-scope — ловит ЛЮБОЙ grow-during-drain **до** того как он порвёт base. Плюс `assert(nova_aint_load(&pending_remote)==0)` перед первым чтением `child_error[]` в decision-loop.
- **Fallback (Option A):** если реальный grow-during-drain найдётся (напр. динамический spawn внутри supervised) — перевести `child_error[]` на тот же chunked stable-address приём, что `NovaSchedState` (:398-445). НЕ делать наивно.

### Риск R3 (MEDIUM) — marker clash в drain
Если оставлять `fiber_ctx[i]` non-NULL как способ retention, worker-owned check :1844-1849 (`base->_nova_parent_scope != NULL → alive++`) сочтёт мёртвого ребёнка живым → drain не завершится (**hang**).
- **Detect до фатала:** watchdog уже встроен (`NOVA_WATCHDOG_DUMP_SECS`, :2010-2033) — hang задампится за 5s. Но правильнее не допускать: retention ИСКЛЮЧИТЕЛЬНО через отдельный канал (`ctx_pins`/`child_ctx[]`), а `fiber_ctx[i]` всё равно обнулять :1834/:1937.

**Общий pre-fatal harness для всех трёх:** прогон целевых фикстур под `NOVA_WATCHDOG_DUMP_SECS=5` (hang→state-dump вместо вечного зависания) + TSAN-проход на Linux (закрывает заодно OPEN-followup `[M-83.11-f2-arm-tsan]`, simplifications.md:35605). Stress-итерации, НЕ single-shot (иначе P(false-good) высок, урок simplifications.md:35614).

## 3. Упорядоченный чек-лист атомов TODO (с file:line + гейтами)

Порядок = топологический по зависимостям. Каждый атом самодостаточен и имеет промежуточный гейт (§4 определяет гейты).

### Блок Ф.1-D (deliverable-закрытие; code-noop, только доки+гард) — можно первым, разблокирует «зелёный статус»
- **A1.1** Обновить `docs/plans/173.0-...md:18,34,43` — пометить drain/grow-vs-wake **CLOSED** со ссылкой simplifications.md:35567/35588. *Гейт:* review-only.
- **A1.2** Amend `spec/decisions/06-concurrency.md` (зона D14/D50/D75): «supervised корректен под реальным M:N, `NOVA_MAXPROCS=1` больше не требуется». *Гейт:* review-only.
- **A1.3** Создать `nova_tests/err173_0/` + гард-фикстуру исходной drain-hang/AV под M:N (переиспользовать паттерн `grow_vs_wake_explicit`/`ring_overflow_drain.nv:4`). *Гейт:* **G-STRESS** (см. §4) — ARMED, высокий worker count, много одновременных child-fail, N-итераций.

### Блок Ф.2 (per-slot retention) — зависит от инвариант-решения R2 — ✅ ВСЕ ЗАКРЫТЫ 2026-07-08
- **A2.1 DONE** — `NovaChildError{msg,kind,reason,payload,tid}` реализован; вместо литерального rename `fiber_error[]` выбран **отдельный** `child_error[]`/`child_ctx[]` массив на `NovaFiberQueue` (обоснование: `fiber_error[]`/`fibers[]`/`count` — индекс-пространство ТОЛЬКО локального/bootstrap-пути (Ф.1, frozen); remote M:N-путь его вообще не трогал (`nova_runtime_spawn_into` никогда не писал в `q->fibers[]`) — реюз того же счётчика рисковал сломать `nova_supervised_step`'s alive-accounting frozen-кода. Отдельный массив = тот же ИНТЕНТ решения 1 (per-slot retention + read API) без риска для Ф.1). Lifecycle: `nova_scope_grow_children`, `nova_scope_init`, `nova_scope_alloc_child_slot`. Гейт G-BUILD: cargo build чистый + `nova_tests/err173_0` byte-identical baseline pass (single-thread путь не задет, диф подтверждён против temp-checkout 9551f4c99).
- **A2.2 DONE** — `_nova_parent_slot` добавлен рядом с `_nova_parent_scope` в `NovaSpawnCtxBase` (fibers.h) + зеркалирован в ОБЕИХ codegen-раскладках (`emit_c.rs` emit_spawn/emit_detach — сегодняшние актуальные строки, не устаревшие 8218-8223 из разведки); set в `nova_runtime_spawn_into` (runtime.c) через `nova_scope_alloc_child_slot`. Гейт G-BUILD + smoke armed spawn — пройден (guard-фикстура + новый pos-тест).
- **A2.3 DONE** — R2-инвариант первым: `_drain_started` (НЕ `has_supervisor` — см. A3.1 ниже, отдельное решение) + debug-assert в новом `nova_scope_grow_children` (не в frozen `nova_scope_grow`). `nova_fiber_report_child_kinded` — новая функция-обёртка вокруг НЕТРОНУТОГО `nova_fiber_report_atomic_kinded` (дописывает per-slot `child_error[slot]`, без CAS). Writer пишет строго до `nova_aint_fetch_sub_release(pending_remote)` (тот же codegen call-site, порядок сохранён). Гейт G-STRESS: `child_error_retention_test.nv` N=40 ×5 внутренних + ×20 отдельных процессов armed M:N — 0 flake.
- **A2.4 DONE** — `nova_scope_collect_child_errors`. Local dual-write (доп. пункт A2.4) **сознательно НЕ сделан** — bootstrap/local-path retention остаётся как было (уже корректно для single-thread, никогда не имел gap'а); dual-write через переиспользование `_nova_active_slot`-индекса рискует коллизией индекс-пространств в теоретическом mixed-scope сценарии — задокументированное решение, не половинчатость (см. комментарий NovaChildError в fibers.h).

### Блок Ф.3 (retention ctx + serialized loop) — зависит от Ф.2 — ✅ ВСЕ ЗАКРЫТЫ 2026-07-08
- **A3.1 DONE (иначе, чем предполагалось)** — вместо scope-level флага `has_supervisor` retention гейтится ПО ФАКТУ ошибки (`child_error[slot].msg != NULL` в `nova_scope_retain_or_release_child`) — не по флагу «этот scope вообще имеет супервизора». Проще и точнее: успешные дети (подавляющее большинство) продолжают штатный pool-recycle без каких-либо изменений; retention срабатывает ТОЛЬКО для реально упавших — соответствует духу решения 3 без scope-level флага, которого 173.0 не вводит (супервизия — 173.2).
- **A3.2 DONE** — retention через `nova_scope_retain_or_release_child` (fibers.h) — НЕ через продление `nova_scope_pin_ctx`/`ctx_pins` (это отдельный, уже существующий GC-root механизм для spawn→resume окна), а через новый `child_ctx[slot]`, семантически параллельный (`nova_alloc_uncollectable`). `fiber_ctx[i]=NULL` в step-death путях НЕ тронут (R3 — не воспроизводился).
- **A3.3 DONE** — R1-guard: `nova_scope_retain_or_release_child` вызывается из ВСЕХ 3 сайтов `nova_spawn_pool_release` в runtime.c (не `has_supervisor`-условие — см. A3.1). Canary/poison-assert НЕ добавлен отдельно (упрощение: сама проверка `child_error[slot].msg != NULL` уже детерминированно решает retain-vs-release без окна гонки — same-thread program-order запись ДО этой точки). G-STRESS: err173_0 ×20 + regression-δ=0 на nova_tests/concurrency — aliasing не воспроизведён.
- **A3.4 DONE** — serialized decision-loop в `nova_supervised_run_impl`, после `nova_sched_drop_state`, строго до free `ctx_pins` (актуальная позиция в файле — не устаревшие номера строк 2082/2105 из разведки). Освобождение retained-ctx — в том же блоке, ПОСЛЕ цикла решений.
- **A3.5 DONE** — `nova_supervised_decide(scope, idx, err, ctx)` — тело = **pure no-op observer** (НЕ полная реализация Escalate-all-or-throw внутри hook'а — та логика УЖЕ существует в `first_error`/`first_error_atomic`+хвостовом re-throw и НЕ продублирована/не тронута — byte-parity гарантирована тем, что внешний путь принятия решения не изменился вообще). Это осознанная трактовка «default-политика = сегодня»: hook вызывается по разу на падение с живым ctx (инфраструктурное обещание 173.0), а НЕ переигрывает существующий (уже корректный) re-throw. G-POS+G-NEG: `child_error_retention_test.nv` (well-formed re-throw content) + regression-δ=0 (CANCEL-only парity не тронут, отдельно не ре-тестировался целевым pos/neg парой — покрыт существующими cancellation-тестами, не входящими в non-slow concurrency выборку из-за pre-existing Duration-бага, см. отчёт).

## 4. Определения гейтов (stress NOVA_MAXPROCS>1, pos+neg per §5 плана)

- **G-BUILD** — компиляция рантайма + `nova test` целевой фикстуры зелёный; baseline byte-identical на kill-switch того же бинаря (single-thread путь не задет; feedback-codegen-dce-verification).
- **G-POS** — `nova_tests/err173_0/` позитив: N детей падают **одновременно** разными ошибками под `NOVA_MAXPROCS>1` → все N retained (не схлопнуты), decision-loop по разу на падение, ctx жив до решения. Именно per §5 плана.
- **G-NEG** — регресс-гард: воспроизведение исходного drain-hang/AV под M:N (должен НЕ падать); + CANCEL-only паритет.
- **G-STRESS** — G-POS/G-NEG прогнанные **ARMED, высокий worker count, N-итераций** (не single-shot), под `NOVA_WATCHDOG_DUMP_SECS=5` и (Linux) TSAN. Обязательно форсить `nova_scope_grow` (spawn count > `NOVA_SCOPE_INITIAL_CAP=16`, :187) чтобы тест реально трогал realloc-путь под конкурентной записью — иначе R2-детектор спит.
- **G-NOHANG** — watchdog не срабатывает за 5s (drain завершается; R3 marker-clash не воспроизводится).

## 5. Дробление на под-атомы (feasibility) — ДА, с промежуточными гейтами

План раздробляем и **должен** дробиться — каждый блок имеет свой гейт, keystone-порядок такой:

1. **Ф.1-D целиком** (A1.1→A1.3) — независим, code-noop, закрывает статусный долг сразу. Промежуточный гейт после каждого: review / G-STRESS.
2. **R2-инвариант (A2.3-часть: `_drain_started` + assert) — вынести ВПЕРЁД** самостоятельным под-атомом: сначала поставить tripwire, убедиться G-STRESS его не триггерит на текущем коде (доказать, что grow-during-drain не бывает), и лишь ПОТОМ добавлять cross-worker запись. Это ключевой de-risk: детектор ставится до опасного кода.
3. **Ф.2 storage (A2.1) → threading (A2.2) → remote-write (A2.3) → read-API (A2.4)** — строго последовательно, G-BUILD между storage/threading, G-STRESS на remote-write, G-POS на read-API.
4. **Ф.3 retention (A3.2) + R1-guard (A3.3)** — под-атомы с раздельными гейтами (G-NOHANG для retention, G-STRESS+TSAN+canary для R1). R1-guard можно и нужно ставить/тестировать ДО подключения loop.
5. **Ф.3 loop (A3.4) → hook (A3.5)** — финальная сборка, G-POS+G-NEG.

Инвариант порядка в `_impl` жёстко фиксирован кодом и его нельзя нарушать: `retention(step :1834/:1937)` → `loop/hook(@2082)` → `free ctx(@2105)` → `re-throw(:2136)`.

## 6. Doc/spec-дельта (§4 плана)

- D-блок «runtime-субстрат supervised: per-slot error retention + serialized decision loop + ctx pinning».
- `06-concurrency.md` amend (A1.2).
- `docs/dev/debugging-races.md` / mn-race case-study — дописать R1/R2/R3 паттерн (детект-tripwires).
- simplifications.md + project-creation.txt + discussion-log (nova-private) после каждого блока (feedback-update-logs).
- Закрыть OPEN-followups как «prove-clean»: `[M-83.11-f1b-acquire-capacity]` (уже есть `nova_sched_cap_acq` :490 — задокументировать закрытие), `[M-83.11-f2-arm-tsan]` (TSAN-проход в G-STRESS).

---

**Ключевые файлы:** `d:/Sources/nv-lang/nova-p173/compiler-codegen/nova_rt/fibers.h` (struct :189-318, grow :528-574, init :576-615, alloc_slot :632-719, report local :1684-1703, report remote :1736-1781, step-death :1830-1940, decision-window :2078-2163), `runtime.c:1128/1179/1855` (pool_release), `emit_c.rs:8218-8223` (remote-emit), `docs/plans/173.0-concurrency-runtime-substrate.md`, `spec/decisions/06-concurrency.md`, `nova_tests/err173_0/` (создать).

**Самое главное:** рантайм-ядро Ф.1 **уже закрыто структурно** (§7.7 подтверждён по `fibers.h:398-445`) — его НЕ трогать. Реальная работа 173.0 = Ф.2-ретеншн + Ф.3-loop, а единственный неустранённый soundness-долг — что `child_error[]` наследует plain-realloc от `fiber_error[]`, поэтому R2-инвариант (`_drain_started`-tripwire + writer-before-`pending_remote`-dec) обязан ставиться ПЕРВЫМ под-атомом, до любой cross-worker записи.