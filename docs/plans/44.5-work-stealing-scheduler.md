// SPDX-License-Identifier: MIT OR Apache-2.0
# План 44.5: Work-stealing scheduler + per-worker libuv loop

> **Статус:** executable sub-plan, начат 2026-05-13.
> **Parent:** [Plan 44 (M:N runtime roadmap)](44-mn-runtime-roadmap.md).
> **Prerequisite:** [Plan 44.4](44.4-mn-runtime-stage0.md) closed
> (NovaWorker infrastructure live).
>
> **Цель:** реальное распределение workload между workers с work-stealing
> (Chase-Lev deque) + параметризация `nova_supervised_run` per-worker
> libuv loop. После этого `runtime.spawn_global` actually работает —
> fiber'ы запускаются на разных threads, blocked fibers (Time.sleep/
> Channel.recv) корректно park'ятся на own worker loop.
>
> **Соответствие с Go:** Chase-Lev deque = тот же алгоритм что Go's
> `runtime/proc.go::runqsteal`. Per-worker loop = Go's M-P-G model
> (каждый P имеет own scheduler state).

---

## Что должно работать после Plan 44.5

```nova
import std.runtime.runtime

fn main() {
    runtime.init(4)  // 4 OS workers

    ro ch = Channel.new(100)
    supervised {
        // 100 fibers spawned — Plan 44.5 распределяет round-robin,
        // workers steal'ят work если очередь uneven.
        for i in 0..100 {
            spawn {
                Time.sleep(10)         // park на own worker loop
                ch.tx.send(i * 2)      // cross-worker channel safe
            }
        }
        spawn {
            var sum = 0
            while Some(v) = ch.rx.recv() {
                sum += v
            }
            assert(sum == 9900)        // 0+2+4+...+198
        }
    }

    runtime.shutdown()
}
```

**Acceptance:**
- 4 workers полностью загружены (через `runtime.current_worker_id()` — fibers распределены).
- Total throughput ≥ 3× single-thread на 4-core.
- Time.sleep в worker fiber park'ится на own loop, не global.
- Cross-worker channel send/recv корректно работают.
- Work-stealing: если один worker занят, другие steal'ят.

---

## Архитектура

### Layer 1: Chase-Lev deque (lock-free work-stealing)

`compiler-codegen/nova_rt/deque.h` — single-producer multi-consumer deque.
Каноническая реализация (Chase & Lev, 2005). ~250 LOC C11 atomics.

```c
typedef struct {
    /* Power-of-2 size, циркулярный буфер. */
    nova_atomic_intptr     top;       /* steal-side index (FIFO read) */
    nova_atomic_intptr     bottom;    /* owner-side index (LIFO push/pop) */
    nova_atomic_intptr*    buffer;    /* указатель на array[size] */
    size_t                 mask;      /* size - 1 для modulo */
} NovaDeque;

/* Owner-side ops (called только current thread). Wait-free. */
void   nova_deque_init(NovaDeque* d, size_t initial_size);
void   nova_deque_push(NovaDeque* d, void* item);   /* LIFO push */
void*  nova_deque_pop(NovaDeque* d);                 /* LIFO pop, NULL if empty */

/* Steal-side op (called any thread). Lock-free. */
void*  nova_deque_steal(NovaDeque* d);              /* FIFO steal, NULL if empty/contended */

void   nova_deque_destroy(NovaDeque* d);
```

**Properties (Chase-Lev guarantees):**
- Owner push/pop **wait-free** (no atomic CAS in hot path).
- Steal **lock-free** (CAS may fail under contention, but progresses).
- No false steals от same item.
- Resize support через atomic buffer pointer swap (рост по требованию).

### Layer 2: Worker integration

Замена `NovaWorker.scope` (текущий push queue) на Chase-Lev deque:

```c
typedef struct NovaWorker {
    int               id;
    uv_thread_t       thread;
    uv_loop_t         loop;
    uv_async_t        wake_handle;
    NovaDeque         local_deque;       /* NEW — replaces queue_mu+scope */
    /* fiber scope для cancellation propagation — отдельно от deque. */
    NovaFiberQueue    scope;
    nova_atomic_bool  stop;
    nova_atomic_int   parked;            /* workers idle counter */
} NovaWorker;
```

`scope` остаётся для supervised-style cancel propagation; **fiber'ы в
deque** распределяются между workers, но **per-fiber state** ссылается
на parent scope.

### Layer 3: Worker main loop с work-stealing

```c
static void _worker_main(void* arg) {
    NovaWorker* self = (NovaWorker*)arg;
    _current_worker_id = self->id;
    _nova_active_scope = &self->scope;

    while (!nova_abool_load(&self->stop)) {
        mco_coro* co = NULL;

        /* (1) Local deque — owner-side LIFO pop. */
        co = nova_deque_pop(&self->local_deque);

        /* (2) Idle — try steal у соседей. */
        if (!co) {
            for (int i = 0; i < _n_workers; i++) {
                if (i == self->id) continue;
                co = nova_deque_steal(&_workers[i].local_deque);
                if (co) break;
            }
        }

        /* (3) Still nothing — block в libuv (own loop). */
        if (!co) {
            nova_aint_inc(&self->parked);
            uv_run(&self->loop, UV_RUN_ONCE);
            nova_aint_dec(&self->parked);
            continue;
        }

        /* (4) Run fiber. */
        _nova_active_slot = /* slot index в self->scope */;
        mco_resume(co);

        /* (5) Fiber finished or yielded — handle accordingly. */
        if (mco_status(co) == MCO_DEAD) {
            mco_destroy(co);
        } else if (mco_status(co) == MCO_SUSPENDED) {
            /* Yielded — back to deque. Если fiber парк'нулся (waiter
             * на channel), он НЕ в deque — wake'нет его cross-worker
             * push через uv_async_send. */
            nova_deque_push(&self->local_deque, co);
        }
    }
}
```

### Layer 4: Per-worker libuv loop параметризация

Сейчас `nova_supervised_run` использует global `nova_evloop()`. Под M:N
worker должен использовать own loop. Изменения:

1. `nova_supervised_run(scope)` → `nova_supervised_run(scope, loop)`.
2. `Nova_Time_after(ms)` — registers `uv_timer_t` на current TLS loop:
   ```c
   uv_loop_t* loop = _nova_current_loop();  /* TLS, set by worker_main */
   uv_timer_init(loop, &timer);
   ```
3. `nova_sched_park(scope, slot)` — если slot park'ится waiting libuv
   event, callback fires на том же thread'е что зарегистрировал handle
   (libuv guarantee). Cross-worker wake через `uv_async_send`.

### Layer 5: Cross-worker channel correctness

Plan 44.1 (channels) уже **M:N-ready** (atomics, mutex, doubly-linked,
direct-copy). Под Plan 44.5 ничего не меняется в channel runtime.

Edge case: `send` блокируется до recv. Сейчас sender park'ится на own
scope. Когда recv'er на другом worker'е → cross-worker wake через
`nova_sched_wake(sender->scope, sender->slot)`. Это вызывает
`uv_async_send(&sender_worker->wake_handle)`. Sender worker awakes,
видит sender в parked list, resume'ит его на own thread.

**Critical:** waiter state (BaseWaiter struct) живёт на sender's fiber
stack. Если sender fiber **мигрирует** на другой worker между park и
wake — `_nova_active_scope` / `_nova_active_slot` invalid. Plan 44.5
**запрещает migration** — fiber pin'ится к worker'у где park'нулся.

Migration = Plan 44.6 (TLS migration + per-fiber state).

### Layer 6: Boehm GC_THREADS

Workers вызывают `nova_alloc` (channels, NovaSpawnCtx, NovaAfterState
malloc'нут). Boehm под multi-thread требует `GC_THREADS=1` build +
`GC_register_my_thread()` per worker.

- **Linux Docker**: `libgc-dev` Ubuntu 22.04 уже built с threads.
- **Windows vcpkg**: `bdwgc[multithreaded]` feature flag (custom vcpkg config).

Plan 44.5 включает GC registration в worker main loop:
```c
struct GC_stack_base sb;
GC_get_stack_base(&sb);
GC_register_my_thread(&sb);
```

И `GC_unregister_my_thread()` в worker cleanup.

Если Boehm build не имеет GC_THREADS — вернуться к Plan 44.4 single-thread
PoC через runtime flag.

---

## Этапы реализации

### Этап 5.1 — Chase-Lev deque (~250 LOC)

`compiler-codegen/nova_rt/deque.h` standalone module. Тесты:
- Single-threaded: push/pop корректный LIFO.
- Concurrent: 1 owner + N stealers, no lost items, no double-take.
- Resize: deque starts size 64, grows до 1024 без data loss.

C-level smoke test (как `nova_tests/plan40_sanitizers/`) либо Nova-side
через runtime.deque API (если exposed).

### Этап 5.2 — Worker integration (~150 LOC refactor)

- Заменить `NovaWorker.scope` push queue на `NovaDeque local_deque`.
- Adapt `nova_runtime_spawn_global` — push в target worker's deque
  через `nova_deque_push`.
- Replace `mutex_lock(queue_mu)` paths с lock-free deque ops.
- `_worker_main` rewrite per Layer 3 above.

### Этап 5.3 — Per-worker libuv loop (~200 LOC refactor)

- Add TLS `_nova_current_loop` set by worker_main.
- `Nova_Time_after` использует `_nova_current_loop()` вместо
  `nova_evloop()`.
- `nova_evloop_install_sigint` остаётся на main thread (single SIGINT
  handler на процесс).
- `nova_supervised_run(scope, loop)` parameterized — codegen emit'ит
  current loop в вызов.

### Этап 5.4 — Boehm GC_THREADS verify

- Linux Docker: verify `libgc-dev` built с threads через test что
  `GC_register_my_thread` returns success.
- Windows: попробовать vcpkg `bdwgc[multithreaded]`. Fallback на flag
  что отключает Plan 44.5 на Windows если build не доступен.
- `nova_runtime_init()` regsiter main thread с GC, workers register
  per thread.

### Этап 5.5 — Smoke + integration tests

- `mn_runtime_actual_workload.nv` — 4 workers, 100 fibers, sum verify.
- `mn_runtime_steal.nv` — uneven push, verify all workers participate.
- `mn_runtime_cross_channel.nv` — channel send/recv across workers.
- Regression: existing 271 tests still PASS (single-thread default).

### Этап 5.6 — D-decision + docs

- D98 в spec/decisions/06-concurrency.md (M:N scheduler initial impl).
- Update D71 (bootstrap concurrency runtime — упоминание M:N).
- Plan 44 roadmap update (Layer 1+2+4 closed).
- project-creation.txt + simplifications.md.

---

## Acceptance

1. Chase-Lev deque test PASS (single + multi-thread stress).
2. `mn_runtime_actual_workload.nv` PASS — 4 workers visible через
   `current_worker_id()` distribution.
3. `mn_runtime_steal.nv` PASS — uneven workload balanced.
4. `mn_runtime_cross_channel.nv` PASS — channels работают cross-worker.
5. Default `nova test` (single-thread): 271 PASS / 0 FAIL — no regress.
6. Linux Docker: same.
7. Throughput bench: 4 workers ≥ 3× single-thread на CPU-bound workload.

---

## Не входит в Plan 44.5

- **TLS migration** — fiber pin'ится к worker'у. Migration = Plan 44.6.
- **Preemption** — cooperative-only. Long CPU-bound fiber монополизирует
  worker. = Plan 44.7.
- **Blocking pool** — `Blocking` effect остаётся inline. = Plan 44.8.
- **std.sync Atomic[T]** — Plan 18 P0.
- **Concurrent GC tuning** — BDW-GC default, свой GC = отдельный milestone.

---

## Сравнение с Go runtime

| Feature | Go | Nova Plan 44.5 | Status |
|---|---|---|---|
| Work-stealing deque | Chase-Lev | Chase-Lev | ✅ паритет |
| Per-P scheduler state | g/M/P model | NovaWorker | ✅ similar |
| Per-worker event loop | runqueue + netpoll | uv_loop_t per worker | ✅ similar |
| Cross-worker wake | runqput + futex/event | uv_async_send | ✅ similar |
| GC thread registration | allgs list | GC_register_my_thread | ✅ паритет |
| park/wake (gopark/goready) | gopark/goready | nova_sched_park/wake + dispatch_ready | ✅ паритет |
| Time.sleep в worker | goroutine parks | fiber parks + dispatch_ready | ✅ паритет |
| Stack size | 2KB start, grows via copy | 8MB virtual mmap (demand-paged) | ⚠️ Plan 44.9 (B) |
| Preemption | safepoint-based (10ms) | cooperative; `runtime.yield()` explicit | ⚠️ Plan 44.7 (C+D) |
| Fiber migration | g hops P | pinned to worker | ⚠️ Plan 44.6 (E) |
| Blocking pool | netpoll + spawn M | inline | ⚠️ Plan 44.8 |
| Goroutine spawn cost | ~1 µs | ~2-5 µs (Boehm alloc) | близко |
| Concurrent GC | <1ms ZGC | Boehm STW ~10ms | ⚠️ separate plan |
| Structured concurrency | errgroup (library) | supervised { } (language) | ✅ лучше Go |

После 44.5+6+7+8 — **архитектурный паритет** с Go runtime. Concurrent GC
останется gap (BDW vs ZGC) — отдельный milestone.

---

## Риски Plan 44.5

- **R1: Chase-Lev complexity.** Канонический алгоритм 200 строк, но
  edge cases (ABA problem, resize correctness) тонкие. Тестировать
  под TSan на Linux Docker.
- **R2: Per-worker libuv loop**: handle migration между threads —
  libuv `uv_xxx_t` handles **thread-bound**. Если fiber park'ится на
  worker A's timer, потом worker A освобождает thread → handle
  orphan'ится. Решение: pin fiber к worker'у пока handle active.
- **R3: Boehm GC_THREADS Windows build**: vcpkg feature flag может
  не быть, custom build complex. Fallback на single-thread.
- **R4: Cross-worker contention** на steal. Chase-Lev минимизирует через
  FIFO steal-side, но 16 workers × 16 stealers = N² CAS per cycle.
  Mitigation: try steal только от neighbor, random victim selection.

---

## Status: CLOSED — Layers 1–5 + cross-channel + runtime.yield + 8MB stacks + merge main (2026-05-14)

**Re-merge note (2026-05-13):** «Plan 44.6» был **искусственным split'ом**
Layer 3 на отдельный sub-plan. После feedback'а пользователя re-merged
обратно в Plan 44.5 — это была одна work unit с самого начала, разделение
не было оправданным. File `44.6-per-worker-libuv-loop.md` удалён;
git archaeology достаточна — commits `5c735afc716` .. `6c62cb70f81`
содержат старое название «Plan 44.6» в messages, что preserved в логе.

**Honest scope correction:** original Plan 44.5 acceptance включал
3 actual workload tests (`mn_runtime_actual_workload.nv`,
`mn_runtime_steal.nv`, `mn_runtime_cross_channel.nv`) + 4× speedup
bench. Закрытие как «Layers 1+2+4 partial» было неправильным — это не
satisfied original acceptance. True closure требует actual parallelism
working.

### Closed (через Plan 44.6 commits, теперь часть Plan 44.5)

- ✅ **Layer 1 (Chase-Lev deque)** — commits `2c63d015016` + `9c29a6126f5`.
  Lock-free single-producer/multi-consumer deque, PPoPP 2013 memory ordering.
- ✅ **Layer 2 (worker integration)** — commit `9c29a6126f5`. Replace
  mutex+scope push queue на deque ops. Owner LIFO pop, FIFO steal у
  neighbors, idle uv_run.
- ✅ **Layer 3 (per-worker libuv loop)** — TLS `_nova_current_loop`
  (eventloop.h/c + runtime.c + 8 call-site replacements) — commits
  `151acb2dde7` + `73a06a271bc` + `f62b519792e`. Codegen changes не
  потребовались — TLS-fallback на `nova_evloop()` сохраняет single-thread
  behaviour для main-thread'а.
- ✅ **Layer 4 (Boehm GC_THREADS)** — commit `b27f7c819a2`. Per-worker
  GC_register_my_thread на Linux/macOS. Windows conditional skip.
- ✅ **Layer 5 partial (regression tests):** 3 mn_runtime regression теста —
  commit `70654ea177a` (main_sleep_after_init, supervised_after_init,
  init_shutdown_cycles). Это validates что single-thread baseline не
  ломается под `runtime.init(N)`.
- ✅ **Layer 6 partial (D98 spec):** commit `6c62cb70f81`.
- ✅ **Layer 5 park/wake infrastructure (2026-05-14):** dispatch_ready callback
  на NovaFiberQueue + nova_scope_alloc_slot/free_slot + _worker_dispatch_ready
  (same/cross-thread) + wake_pending mutex queue + slot allocation в codegen
  entry preamble/epilogue. **Single-fiber** Time.sleep/Channel.recv в worker
  fiber'е работает (`mn_runtime_sleep_in_worker.nv`, 30/30 PASS под loop).
  Multi-fiber Time.sleep на Windows — known flake (~14% segfault), root cause
  vcpkg bdwgc БЕЗ `[multithreaded]` feature → workers не зарегистрированы
  с Boehm GC → use-after-free risk. На Linux libgc-dev обычно собран
  с GC_THREADS, multi-fiber должен работать (требуется Docker validation).

### Open: Layer 5 — Approach A (implicit M:N, Go-style)

**Design decision (2026-05-14):** один и тот же `spawn { body }` работает
в single-thread (без runtime.init) и distributed M:N (с runtime.init).
Программист **не различает** режимы. Это match'ит Go's `go func()` и
Nova spec ([D71](../../spec/decisions/06-concurrency.md#d71)) обещание
прозрачного M:N. Отвергнут explicit `runtime.spawn { body }` API
как degraded UX.

### Implementation план (Layer 5)

**1. Runtime: NovaFiberQueue.pending_remote atomic counter**

Поле в `NovaFiberQueue` (`compiler-codegen/nova_rt/fibers.h`):
```c
nova_atomic_int pending_remote;  // fiber'ов, running на workers
```
`nova_scope_init` инициализирует к 0.

**2. Runtime: nova_runtime_spawn_into(scope, entry, user)**

Новая API в `runtime.h/.c`:
```c
void nova_runtime_spawn_into(NovaFiberQueue* scope,
                              void (*entry)(mco_coro*),
                              void* user);
```
Логика:
- Increment `scope->pending_remote` atomically (release).
- Heap-allocate `NovaRemoteSpawn { scope, user, entry }` (или wrap'аем
  user pointer).
- Push wrapper entry в next worker's deque (round-robin как
  `spawn_global`).
- Worker resume'ит fiber → wrapper entry call'ит original entry с user;
  после finish — decrement `scope->pending_remote` (acq_rel) + `uv_async_send`
  на main thread для wake'а supervised_run.

**3. Runtime: nova_supervised_run ждёт pending_remote == 0**

```c
for (;;) {
    int alive = nova_supervised_step(q);              // local fibers
    int remote = nova_aint_load(&q->pending_remote);
    if (alive == 0 && remote == 0) break;
    // если все local parked || remote > 0 → uv_run ONCE wake'ом проснёмся
    int parked = nova_sched_count_parked(q);
    if ((parked > 0 && parked == alive) || (alive == 0 && remote > 0)) {
        uv_run(nova_current_loop(), UV_RUN_ONCE);
    }
}
```
Same для `nova_supervised_drain_main_scope`.

**4. Runtime: cross-worker error propagation**

Wrapper entry (см. шаг 2) пишет в `scope->first_error` через **atomic CAS**:
```c
const char* expected = NULL;
nova_aptr_cas(&scope->first_error_atomic, &expected, err_msg);
nova_abool_store(&scope->cancel_requested, true);
// + wake_all_workers_running_this_scope() — TBD механизм
```

Cancel: worker fiber на yield-point check'ит `scope->cancel_requested` →
throw "scope cancelled" → wrapper catches → decrement pending_remote +
report.

**5. Codegen: emit_spawn conditional**

В `emit_c.rs::emit_spawn` (около line 2227) заменить:
```rust
self.line(&format!("nova_fiber_spawn_into(&{q}, {id}, {ctx});", ...));
```
на:
```rust
self.line(&format!("if (nova_runtime_is_initialized()) {{"));
self.line(&format!("    nova_runtime_spawn_into(&{q}, {id}, {ctx});"));
self.line(&format!("}} else {{"));
self.line(&format!("    nova_fiber_spawn_into(&{q}, {id}, {ctx});"));
self.line(&format!("}}"));
```
Это affects всех spawn calls — single-thread baseline (без runtime.init)
unchanged (else-branch), под runtime.init — distribute.

**6. GC roots для cross-worker SpawnCtx**

scope.fiber_ctx[] array уже tracking SpawnCtx pointers как GC roots.
Расширить semantics: для distributed spawn — `scope.fiber_ctx` тоже
populate'ed (slot allocation такой же), `nova_runtime_spawn_into`
обновляет slot.

**7. 3 acceptance-теста:**
- `mn_runtime_actual_workload.nv` — 4 workers × 25 spawn'ов c Time.sleep,
  total < 100ms (vs sequential ~500ms). Distribution через
  `runtime.current_worker_id()` histogram.
- `mn_runtime_steal.nv` — uneven workload, verify все workers
  participated.
- `mn_runtime_cross_channel.nv` — producer на worker A, consumer на
  worker B, sum verify.

**8. Layer 6 finalize:** D98 update (Layer 5 + Approach A rationale),
project-creation + simplifications.

### Риски Layer 5

- **R1: Atomic ordering** для `first_error_atomic` CAS + `pending_remote`.
  Mitigation: use **acq_rel** для CAS, **release** для increment,
  **acquire** для load.
- **R2: Deadlock** если workers все idle и main ждёт `pending_remote > 0`.
  Possible если fiber на worker'е парковался на Channel waiting send'а,
  а sender тоже park'нулся waiting recv. Это **deadlock by program design**,
  не runtime bug. Diagnostic timeout (NOVA_RUNTIME_DEADLOCK_TIMEOUT env)
  для debug.
- **R3: GC** — distributed SpawnCtx должны оставаться reachable через
  scope.fiber_ctx[]. Боуэм conservative scanner walks workers' stacks
  (GC_register_my_thread из Layer 4) — должно работать.
- **R4: Boundary main thread sees stale scope state.** Main thread
  read'ит scope.first_error / pending_remote — atomic acquire/release
  достаточна.

**Регрессия baseline:** 276/277 PASS Windows + Linux Docker.

---

## Completed: Layer 5 park/wake — Time.sleep в worker fiber (2026-05-14)

**Commit:** `8fcbc67fddb` `feat(44.5): park/wake — Time.sleep в worker fiber (D92 fixed)`

### Что было сделано

**1. -DGC_THREADS (корневая причина segfault).**  
Диагностика показала: bdwgc скомпилирован с `-DGC_THREADS` (подтверждено
`build.ninja` для vcpkg и `libgc-dev` Ubuntu), но клиентский код включал
`<gc.h>` без этого define → `GC_register_my_thread` скрыт `#ifdef GC_THREADS`
→ workers не регистрировались с Boehm → use-after-free при GC STW.
Исправление: добавить `-DGC_THREADS` во все 3 compiler path'а в
`test_runner.rs`; убрать `&& (defined(__linux__) || defined(__APPLE__))` из
`NOVA_GC_THREADS_REGISTER` в `runtime.c` — теперь Windows тоже регистрирует.

**2. dispatch_ready hook (nova_sched_wake → worker deque).**  
`NovaFiberQueue.dispatch_ready` function pointer: NULL = single-thread
(wake только mark unparked); non-NULL = M:N (_worker_dispatch_ready).
`nova_sched_wake` вызывает hook если установлен.  
`_worker_dispatch_ready`: same-thread → owner LIFO deque push; cross-thread
→ wake_pending mutex list + uv_async_send к owner worker.  
Worker loop drain wake_pending перед deque pop на каждой итерации.

**3. nova_scope_alloc_slot в entry preamble (D92 fix).**  
Worker fiber на first resume вызывает `nova_scope_alloc_slot(_nova_active_scope, _co)`
→ получает реальный slot → `_nova_active_slot >= 0` → D92 invariant выполнен.
`nova_scope_free_slot` в epilogue перед decrement `pending_remote`.  
Pre-alloc: `nova_scope_grow(64)` + `nova_sched_get_state` на main thread (GC-safe).

### Результаты

- **Windows:** 279/279 PASS (был 276/277; +3 теста, sleep_precision_bench bound fix)
- **Linux Docker:** `mn_runtime_sleep_in_worker` PASS; pre-existing Linux hangs
  (`sleep_bench`, `expected_runtime/defer_*`, `expected_runtime/contracts_*`)
  присутствуют на main и mn-runtime одинаково — не наша регрессия.

### Pre-existing Linux issues (не наша регрессия)

Следующие тесты висят как на `main`, так и на `mn-runtime` (проверено `2026-05-14`):
- `concurrency/sleep_bench` — `1M sleep(0) yields` либо `10k concurrent sleeps` зависает под Docker
- `expected_runtime/defer_throw_*`, `defer_panic_*`, `errdefer_on_panic` — zависают ~62s
- `expected_runtime/contracts_*_fail` — зависают ~62s
- `concurrency/sleep_precision_bench` — "sleep(0) zero-overhead yield <5ms" под Docker slow

Root cause не исследован. Возможно: Docker network namespace + libuv + SIGPIPE interaction,
либо libgc STW deadlock в сложных test scenarios. Отдельная задача (Plan 44.9 или hotfix).

---

## Завершённые улучшения (post-Layer-5, закрыты 2026-05-14)

### A. mn_runtime_cross_channel.nv — ✅ CLOSED (commit `b7514c02c2d`)

**Root cause (work-stealing deadlock):** когда fiber мигрировала на другой
worker через steal, `_nova_active_scope` (TLS stealing worker'а) не
совпадал со scope'ом где slot был выделен. Channel waiter записывал
неправильный scope → `nova_sched_wake` находил `scope->fibers[slot]=NULL`
→ dispatch_ready не вызывался → permanent hang.

**Fix:** добавлено поле `NovaFiberQueue* _nova_fiber_scope` в
`NovaSpawnCtxBase`. Worker loop restore'ит `_nova_active_scope =
base->_nova_fiber_scope` перед каждым `mco_resume` — channel ops всегда
используют правильный home scope. Codegen emit'ит `_c->_nova_fiber_scope
= _nova_active_scope` в preamble.

Тесты: 3 теста в `mn_runtime_cross_channel.nv` — basic send/recv,
4-producer pipeline, Channel + Time.sleep.

### B. Виртуальная память для стеков — ✅ CLOSED (commit `0a8f6635640`)

**Реализация:** `fiber_arena.h` — `NOVA_FIBER_STACK_SIZE = 8MB`.  
Linux/macOS: `mmap(MAP_ANONYMOUS | MAP_PRIVATE | MAP_NORESERVE, 8MB)` —
demand-paged, физические страницы выделяются при обращении. 4096 slots ×
8MB virtual = 32GB virtual, физически минимально.  
Windows: сохранён `calloc` fallback (SEH guard pages отложены на Plan 42+).

**Почему 8MB:** достаточно для глубокой рекурсии; guard page внизу стека
даёт понятный SIGSEGV вместо тихого memory corruption. Не требует write
barriers как Go stack copying.

### C. Cooperative preemption — runtime.yield() — ✅ CLOSED (commit `0a8f6635640`)

`runtime.yield()` → `nova_fiber_yield()`: fiber re-queue'ится в worker deque
(не park), другие fibers получают CPU. Семантика: "я готов уступить CPU".

Реализация: 1 built-in в codegen (`emit_c.rs`), 1 extern в `std/runtime/runtime.nv`.
3 теста в `mn_runtime_yield.nv` — cooperative progress, yield вне fiber
(no-op), yield без runtime.init (single-thread).

Выбрано **явное** `runtime.yield()`, а не автоматический insert в циклы
(overhead на IO-bound loops, codegen не знает длину итерации).

### D. Sync mn-runtime ← main — ✅ CLOSED (commit `cf860aecc86`)

Merged Plan 42.09 (reexport), 42.10 (forbid), 42.12 (cfg) + D100+D101 spec.
Конфликты в 5 файлах (comment-only → `--ours`); `fibers.h` — ручной merge:
сохранены `_nova_fiber_scope` + main's GC pinning (`ctx_pins` fields).

**Итог:** 307/307 PASS (26 новых тестов из Plan 42.09/42.10/42.12).

---

## Открытые задачи (post-Plan-44.5)

### E. Preemption signal-based (Plan 44.7 full — сложно)

Go использует `SIGURG` на Linux (поднимается от GC STW trigger или
preempt ticker каждые 10ms). При получении signal, goroutine прерывается
на ближайшем safe point и делает yield. Safe points в Go — все function
call boundaries (compiler вставляет `morestack` checks).

Для Nova: нужны safe points в codegen (каждый function prologue?),
signal handler который делает `mco_yield`, и TLS флаг "fiber должна yield".
**Оценка:** 2-3 недели engineering + TSan validation. Высокий риск,
не приоритет пока CPU-bound workloads не станут реальным use case.

### G. D98 spec update (Layer 5 official) — малая задача

**Что нужно:** обновить `spec/decisions/06-concurrency.md` раздел D98 чтобы
отразить реализованный M:N:
- dispatch_ready hook + `_worker_dispatch_ready` — описание механизма.
- `NovaSpawnCtxBase._nova_fiber_scope` — home scope pinning, почему нужен.
- `runtime.yield()` добавить в spec (сейчас только в std/runtime/runtime.nv).
- D92 invariant (`_nova_active_slot >= 0`) — как выполняется через preamble slot alloc.

Объём: ~1-2 часа.

### H. Fiber migration между workers (Plan 44.6 / deferred)

Сейчас fiber pin'нута к worker'у где park'нулась. После wake — всегда
возвращается на тот же worker. Go мигрирует goroutine между P при work-steal.

Migration требует: per-fiber `_nova_active_scope`/`_nova_active_slot` в TLS
вместо global; atomic swap при миграции; libuv handles остаются на owner loop
(handles не мигрируют, только fiber control). Сложность высокая, benefit
неочевиден (pinned scheduling тоже обеспечивает balance через initial round-robin).
