<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 118.1 — FFI memory intrinsics + C-string convention

> **Создан 2026-06-01** (extracted из Plan 118 family decomposition).
> **Ревизия 2026-06-03** (design discussion finalized — `RawMem` namespace,
> `CStr` newtype, D73 from/into conversions, write_bytes generalization).
> **Статус:** 🆕 PLANNED (refined).

---

## Design refinement 2026-06-03 (user discussion finalization)

### Naming + namespace

**`RawMem`** — namespace type (unit-type declaration per spec/02-types.md:233):
- `type RawMem` (no body, no fields) — Nova valid unit type
- Static methods: `RawMem.copy_from[T](...)` style
- Use case: low-level memory operations (driver/embedded/perf-critical)
- Rejected alternatives: `Raw` (too short, semantic vague), `Unsafe` (doubled keyword
  с `unsafe { }` block), `Intrinsics` (broad), `Mem` (too narrow — addr_of не fits)

### CStr type

**`type CStr(*u8)`** — tuple newtype wrapping null-terminated bytes:
- Plan 115 D52 precedent (canonical FFI handle pattern, zero-overhead)
- C ABI: `const char*` (single pointer, NO header)
- PascalCase user-declared type (lowercase `cstr` would require compiler
  special-case как `ptr`/`str` — deferred к future plan если ergonomic demand)
- Location: `std/ffi/cstr.nv` (либо `std/prelude/cstr.nv` если global-visible)

### Conversions через D73 From/Into protocols

```nova
fn CStr.from(s str) -> CStr            // D73 From[str] — str → CStr (copy + NUL)
fn str.from(c CStr) -> str             // D73 From[CStr] — CStr → str (strlen + copy)
fn str.from_view(c CStr) -> str        // unsafe view variant — no copy, no NUL-validation
```

D73 auto-derive дает `.into()` форму бесплатно:
```nova
ro c = my_str.into()                   // → CStr (via str.from(...) auto-derive)
ro s = my_cstr.into()                  // → str (via CStr.from(...) auto-derive)
```

**Naming convention** (cross-ref convention follow-on в spec): `from_view` suffix —
unsafe view variant (no-copy, lifetime-aware); fits существующий `as_X view` /
`to_X copy` pattern.

### RawMem method declarations

Все pointer-touching methods требуют `#unsafe` attribute (Plan 118 D216 §9 —
caller must wrap в `unsafe { }`):

```nova
type RawMem                              // unit type namespace

#unsafe
fn RawMem.copy_from[T](src *T, dst *mut T, count int) -> ()
// memmove (overlap-safe). Mirror []T.copy_from semantics (spec/08-runtime.md §3267).

#unsafe
fn RawMem.copy_nonoverlapping[T](src *T, dst *mut T, count int) -> ()
// memcpy (undefined при overlap; perf-critical optimization).
// Rust precedent: ptr::copy vs ptr::copy_nonoverlapping.

#unsafe
fn RawMem.copy_within[T](p *mut T, src_offset int, dst_offset int, count int) -> ()
// memmove внутри одного буфера. Mirror []T.copy_within (§3268).

#unsafe
fn RawMem.fill[T](dst *mut T, value T, count int) -> ()
// Element-typed fill. Mirror []T.fill (§3269). Compile-time fast-path:
// u8/i8 → memset, wider T → scalar loop (auto-vectorizable).

#unsafe
fn RawMem.write_bytes[T](dst *mut T, byte_val u8, count int) -> ()
// memset(dst, byte_val, count * sizeof(T)).
// Generalized byte-pattern fill (covers fill_zero via byte_val=0).
// Rust precedent: ptr::write_bytes. Use cases: zero-init, debug patterns
// (0xCC/0xDEADBEEF), all-ones (-1) init.

#unsafe
fn RawMem.compare[T](a *T, b *T, count int) -> int
// memcmp. Returns 0=equal, <0=a less, >0=a greater.

#unsafe
fn RawMem.read[T](src *T) -> T
// Unaligned-safe single-element load (no auto-deref).

#unsafe
fn RawMem.write[T](dst *mut T, value T) -> ()
// Unaligned-safe single-element store.

#unsafe
fn RawMem.volatile_read[T](src *T) -> T
// MMIO / driver primitive. Compiler hint: do not optimize away, do not reorder.
// Distinct от Atomic* (which adds LOCK prefix / thread synchronization).

#unsafe
fn RawMem.volatile_write[T](dst *mut T, value T) -> ()
// MMIO / driver primitive.

fn RawMem.addr_of[T](x ro T) -> ptr      // NO #unsafe — pure read of address
// Returns raw `ptr` (opaque integer). NO escape analysis, NO auto-promote
// (vs `&x` which promotes к heap if escape detected). For address-as-identity
// (HashMap key, hash, debug ID).
```

**Все signatures используют `int` для count/offset (D226 signed indexing)**.
FFI boundary calls к C `size_t`-typed args требуют explicit `as usize` cast.

### Volatile vs Atomic differentiation

Documented в spec D216 §6 или new D217:

| Need | Use | Why not the other |
|---|---|---|
| Shared mutable state across fibers/threads | `Atomic{I8..I64,U8..U64,Ptr}` + MemOrdering | Atomic emits LOCK prefix (CPU sync), не подходит для MMIO |
| MMIO register / signal handler / volatile semantics | `RawMem.volatile_read/write` | Volatile is compiler hint only (no CPU sync); Atomic over-synchronizes |

### CStr edge cases (str → CStr view path)

`as_cstr()` view path требует проверки `str.ptr[str.len] == 0`:
- Если Nova str allocator ALWAYS adds trailing NUL (current behavior — verify) → view always safe
- Если NUL guarantee нет → runtime check + conditional copy
- Implementation note: либо invariant в allocator (preferred — zero overhead),
  либо `fn str @as_cstr() -> CStr` всегда returns owned copy (safer, but loses
  zero-copy benefit). Decision pending runtime audit (`[M-118.1-cstr-nul-invariant]`).

### Plan 118.2 simplification (cross-impact)

Per design discussion: **NO new `Slice[T]` type** в Plan 118.2. Resolution per D144
single-type design ("`[]T` — один тип для owner и view"). Instead:
```nova
fn []T @ptr() -> *T          // NEW — internal data pointer access (FFI)
fn []T @len() -> int          // exists (D32)
fn []T @cap() -> int          // exists (D32)
```

User accesses parts directly:
```nova
unsafe {
    ro view = arr[0..1024]                          // []T view (D144)
    read(fd, view.ptr() as *mut u8, view.len() as usize)
}
```

This is `[]T` parts extraction, not new slice type. Plan 118.2 refactored accordingly
(see Plan 118.2 design refinement section).

---

## Original PLANNED specification (2026-06-01)
> **Приоритет:** P1 — production-grade FFI требует typed memory access
> primitives + null-terminated string interop. Без них FFI к C buffer APIs
> (libpng image data, libcurl headers, sqlite blobs) вынуждает писать
> C-shim для каждой операции.
>
> **Оценка:** ~3-4 dev-day.
>
> **Зависимости:**
>   - **Plan 118 (core)** — REQUIRED предварительно merged. `*T` family +
>     unsafe model + NPO foundations.
>   - **Plan 115 V1** ✅ merged (D214 amend through Plan 118) — `ptr` + tuple FFI.
>   - **Plan 114** ✅ merged (D184) — `ro`/`mut` keywords.
>
> **D-блоки (изменения):**
>   - **D217 NEW** — FFI memory primitives (read/write/copy/volatile + addr_of)
>   - **D26 cross-ref** — string types (str + cstr); add C-string convention section
>
> **Worktree convention:** `nova-p118-1` (separate sibling of main, parallel
> с другими Plan 118 family sub-plans).
>
> **Recommended model:** Opus 4.7 + Thinking ON — safety-critical (memory
> primitives errors = silent corruption); ABI cross-platform attention.
>
> **Workflow:**
>   - Commit per phase (Ф.0..Ф.5)
>   - Per-fix verify (targeted fixture)
>   - Full nova test only at end of phase
>   - ABI snapshot tests для cross-platform memory ops
>   - Update logs (project-creation.txt + simplifications.md + discussion-log.md)
>
> **Production-grade требование:** intrinsics first-class в codegen
> (emit к C `memcpy`/`memmove`/`volatile` qualifier), не fallback к bytecode
> interpretation; cross-platform validated.

---

## Зачем

После Plan 118 core у нас есть typed `*T` family + auto-deref `p.field` /
`p.method()`. Но **memory access intrinsics** для FFI отсутствуют:

```nova
// Plan 118 core gives us:
unsafe {
    ro buf *u8 = some_ffi_call()
    ro byte = (*buf)              // ✓ one byte deref
    ro field = buf.field          // (if buf points to struct)
}

// What Plan 118 core CANNOT do:
unsafe {
    // memcpy 1024 bytes from src to dst
    src.copy_to(dst, 1024)        // ❌ no such method

    // read 4 bytes as i32 at typed offset
    ro x = base.offset(16).read()  // ❌ no .read() / .offset()

    // memory-mapped register I/O
    register_addr.write_volatile(0xDEAD_BEEF)  // ❌ no volatile

    // FFI с C-string (null-terminated)
    sqlite_open(cstr"db.sqlite3")  // ❌ no cstr"..." literal
}
```

**Сценарии без 118.1:**
1. **Bulk buffer copy** (image data, network packets) — user пишет C-shim per call
2. **Memory-mapped I/O** (hardware registers, framebuffers) — недоступно
3. **Typed reads из FFI buffers** (parse binary protocols) — manual cast hacks
4. **C-string FFI** (sqlite, curl, png — все принимают `const char*`) — boilerplate
   string-conversion в shim'ах
5. **Out-params** (`int func(out int* result)`) — нужен `addr_of_mut!`

### Mainstream comparison

| Язык | Bulk copy | Typed read/write | Volatile | C-string |
|---|---|---|---|---|
| **C** | `memcpy` / `memmove` | `*p = v` / `v = *p` | `volatile` qualifier | `"..."` (null-term по умолчанию) |
| **Rust** | `ptr::copy` / `copy_nonoverlapping` | `ptr::read` / `ptr::write` | `read_volatile` / `write_volatile` | `c"..."` literal (2024) / `CStr` |
| **Zig** | `@memcpy` builtin | `p.*` / `p.* = v` | `volatile` modifier (`@as(*volatile T, ...)`) | `[*:0]const u8` (sentinel-term) |
| **C#** | `Buffer.MemoryCopy` / `Unsafe.CopyBlock` | `Unsafe.Read<T>` / `Write<T>` | `Volatile.Read` / `Write` | `MarshalAs(UnmanagedType.LPStr)` |
| **Swift** | `UnsafeMutableRawPointer.copyMemory` | `pointee` accessor | `withUnsafePointer(volatile:)` | `withCString { ... }` |
| **D** | `core.stdc.string.memcpy` import | `*p` | `core.volatile` package | `toStringz` |
| **Go** | `copy(dst, src)` для slice; `memmove` via cgo | через `unsafe.Pointer` casts | `sync/atomic.LoadPointer` | `C.CString(...)` |
| **Nova V1** (Plan 118 core) | (нет) | one-byte `*p` only | (нет) | (нет — wrap в C-shim) |
| **Nova V2** (Plan 118.1) | `.copy_to()` / `.copy_to_nonoverlapping()` | `.read()` / `.write()` | `.read_volatile()` / `.write_volatile()` | `cstr"..."` literal + interop |

---

## Дизайн

### 1. Typed read/write primitives

```nova
unsafe {
    ro buf *u8 = malloc(16).unwrap()

    // Read 1 byte
    ro byte = buf.read()                       // u8

    // Read typed value (offset already encoded в pointer type)
    ro int_ptr = buf as *i32
    ro x = int_ptr.read()                       // i32 (4 bytes consumed)

    // Write
    int_ptr.write(0xDEAD_BEEF)
}
```

**API:**
- `(*T).read() -> T` — typed read; equivalent to `*p` но explicit form для clarity
- `(*mut T).write(v T) -> ()` — typed write; equivalent to `*p = v` но explicit

**Why duplicate `*p`?** Two reasons:
1. **Method form** интегрируется с `.copy_to()` / `.read_volatile()` chain
   pattern (mainstream familiarity)
2. **Explicit для `*unsafe T`**: nested unsafe wrap proxied через method call
   reads cleaner

### 2. Bulk copy primitives

```nova
unsafe {
    ro src *ro u8 = source_buffer()
    ro dst *mut u8 = dest_buffer()
    ro n usize = 1024

    src.copy_to(dst, n)                 // memmove (overlapping safe)
    src.copy_to_nonoverlapping(dst, n)  // memcpy (UB if overlap)
}
```

**API:**
- `(*ro T).copy_to(dst *mut T, count usize) -> ()` — memmove; handles overlap
- `(*ro T).copy_to_nonoverlapping(dst *mut T, count usize) -> ()` — memcpy;
  faster но UB on overlap
- Both compile to direct `memmove` / `memcpy` calls (linker resolves
  к platform libc)

**Units:** `count` — element count (not bytes); compiler multiplies by
`sizeof(T)` for actual byte count.

**Type safety:** `src` и `dst` должны иметь same `T` (or one cast в unsafe).

### 3. Volatile reads/writes (memory-mapped I/O)

```nova
unsafe {
    ro reg *mut u32 = 0xFFFF_0010 as *mut u32   // memory-mapped register

    reg.write_volatile(0xDEAD_BEEF)              // visible to hardware immediately
    ro status = reg.read_volatile()              // reread (no compiler caching)
}
```

**API:**
- `(*T).read_volatile() -> T` — volatile read; compiler не cache'ит
- `(*mut T).write_volatile(v T) -> ()` — volatile write; immediate side-effect

**Codegen:** emit C `volatile T*` cast at access point:
```c
((volatile uint32_t*)reg)[0]               // read_volatile
*((volatile uint32_t*)reg) = 0xDEAD_BEEF;  // write_volatile
```

**Compiler guarantee:** no reordering, no caching, no elision (per C
`volatile` semantics). Critical для hardware registers + lock-free protocols.

### 4. `addr_of!` / `addr_of_mut!` macros

```nova
type Packed {
    a u8
    b u32                                // unaligned field!
}

unsafe {
    ro packed = Packed { a: 0xFF, b: 0xDEAD_BEEF }

    // ❌ &packed.b — может dereference unaligned value temporarily
    // ✓ addr_of!(packed.b) — pointer without deref
    ro p_b = addr_of!(packed.b)                  // *ro u32 (may be unaligned!)

    // Similarly для mut:
    mut packed_mut = ...
    ro p_b_mut = addr_of_mut!(packed_mut.b)      // *mut u32
}
```

**API:**
- `addr_of!(expr)` — macro/intrinsic; returns `*ro T` where T = type of expr
- `addr_of_mut!(expr)` — returns `*mut T`
- **Does not deref** the value — safe even on packed/uninit memory
- Compile-time check: expr must be lvalue (addressable)

**Why needed:** `&packed.b` semantically creates a `*T` через value access,
which requires aligned load (UB на packed structs). `addr_of!` provides
pointer construction без implicit load — Rust 1.51+ pattern, critical для
FFI с C-packed structs.

**Codegen:** emit direct `&(packed.b)` C expression (no temporary value
materialization).

### 5. C-string convention

C libraries универсально принимают/возвращают **null-terminated `const char*`**.
Nova `str` тип — length-prefixed (`{ data: *ro u8, len: usize }`); NOT
null-terminated. Conversion was C-shim boilerplate в Plan 115 V1.

**`cstr"..."` literal:**

```nova
unsafe {
    // Plan 115 V1 — needed C-shim:
    //   char path_buf[1024];
    //   memcpy(path_buf, path.ptr, path.len);
    //   path_buf[path.len] = '\0';
    //   sqlite3_open(path_buf, &db);

    // Plan 118.1:
    ro path = cstr"database.sqlite3"             // *ro u8, null-terminated literal
    sqlite3_open(path, &mut db)                  // direct FFI
}
```

**Literal semantics:**
- `cstr"foo"` — compile-time string literal с trailing `\0` byte
- Type: `*ro u8` (pointer to first byte)
- Stored в read-only segment (`.rodata`)
- Length: `4` bytes (`"foo\0"`); user code не sees the null
- **Cannot** contain embedded `\0` (compile error `E_CSTR_EMBEDDED_NULL`)

**Interop functions:**
- `(*ro u8).cstring_len() -> usize` — runtime `strlen` (counts to first `\0`)
- `(*ro u8).from_cstring() -> str` — copy bytes to GC-allocated Nova str
  (length determined by `strlen`)
- `str.to_cstring(buf *mut u8, buf_len usize) -> usize` — copy str + null
  terminator to provided buffer; returns bytes written (or err if buf too small)

**Why explicit `cstr"..."` vs auto-convert `"..."`:**
- `str` literals в Nova — Nova-managed (length-prefixed); auto-convert
  на каждом FFI call = silent heap allocation
- Explicit `cstr"..."` — compile-time string в `.rodata`, zero allocation,
  zero copy

**Limits:**
- `cstr"..."` — only ASCII/UTF-8 byte content (no escape для NULs)
- Maximum литерала length — 64KB (compile-time limit для `.rodata` sanity)
- Runtime `str.to_cstring(buf, len)` — for dynamic case с user-provided buffer

### 6. Out-param FFI pattern

Combining `addr_of_mut!` + `Option[*T]`:

```nova
external fn try_alloc(out *mut Option[*u8]) -> i64
//                       ^^^^^^^^^^^^^^^^^^^
//                       caller passes pointer to Option storage

unsafe {
    mut buf Option[*u8] = None
    ro rc = try_alloc(addr_of_mut!(buf))         // C fills *buf via address
    match buf {
        Some(p) => use_buffer(p),
        None    => Fail.throw(OutOfMemory),
    }
}
```

**Pattern documented в ffi-cookbook.md update (Plan 118.1 Ф.5).**

### 7. Alignment + size queries

```nova
unsafe {
    ro align = align_of[*ro u8]()                  // 1
    ro align32 = align_of[*ro u32]()                // 4
    ro size = size_of[*ro Account]()                // 8 (pointer-width)
    ro size_t = size_of[Account]()                  // size of Account record
}
```

**API:**
- `align_of[T]() -> usize` — compile-time constant; alignment of T
- `size_of[T]() -> usize` — compile-time constant; size of T

**Use case:** manual buffer allocation + alignment compliance for FFI.

---

## Фазы

### Ф.0 — GATE: design freeze + D217 draft + audit (~½ dev-day)

- **Ф.0.1** Worktree `nova-p118-1` create.
- **Ф.0.2** Draft D217 в `spec/decisions/02-types.md` (после D216).
- **Ф.0.3** Draft D26 cross-ref для C-string convention.
- **Ф.0.4** Audit: existing Plan 115 C-shim string conversion patterns —
  candidates для cstr"..." migration.
- **Ф.0.5** Audit: existing FFI fixtures using manual offset/copy — candidates
  для intrinsic migration.
- **Ф.0.6** Acceptance + test plan finalized.
- **Ф.0.7** Commit `feat(plan118.1 Ф.0): GATE — design freeze + D217 draft`.

### Ф.1 — Typed read/write + bulk copy primitives (~1 dev-day)

- **Ф.1.1** Built-in method registry: `(*T).read()`, `(*mut T).write(v)`.
- **Ф.1.2** Built-in method registry: `(*ro T).copy_to(dst, count)`,
  `(*ro T).copy_to_nonoverlapping(dst, count)`.
- **Ф.1.3** Codegen: emit direct memory access (`*p`) для read/write; emit
  `memmove` / `memcpy` для copy.
- **Ф.1.4** Type-checker: all methods require unsafe context (inherited).
- **Ф.1.5** Type-checker: copy_to types must match (`src *ro T`, `dst *mut T`).

**Tests:** T1 series (12+ fixtures).

**Commit:** `feat(plan118.1 Ф.1): typed read/write + bulk copy primitives`

### Ф.2 — Volatile reads/writes + alignment/size queries (~½-1 dev-day)

- **Ф.2.1** Built-in: `(*T).read_volatile()`, `(*mut T).write_volatile(v)`.
- **Ф.2.2** Codegen: emit `volatile` qualifier на access point.
- **Ф.2.3** Built-in: `align_of[T]()`, `size_of[T]()` — compile-time const.
- **Ф.2.4** Type-checker: volatile methods require unsafe context.
- **Ф.2.5** Test memory-mapped register pattern (mock via fixture).

**Tests:** T2 series (8+ fixtures).

**Commit:** `feat(plan118.1 Ф.2): volatile RW + alignment/size queries`

### Ф.3 — `addr_of!` / `addr_of_mut!` (~½ dev-day)

- **Ф.3.1** Parser: `addr_of!(expr)` / `addr_of_mut!(expr)` macro syntax (built-in
  macros, не user-defined).
- **Ф.3.2** Type-checker: expr must be lvalue (addressable).
- **Ф.3.3** Codegen: emit `&(expr)` direct (без temp materialization).
- **Ф.3.4** Test packed struct + out-param patterns.

**Tests:** T3 series (8+ fixtures).

**Commit:** `feat(plan118.1 Ф.3): addr_of! / addr_of_mut! intrinsics`

### Ф.4 — C-string convention (`cstr"..."` literal + interop) (~1 dev-day)

- **Ф.4.1** Lexer: `cstr"..."` literal token (rstring-style prefix).
- **Ф.4.2** Parser: `cstr"..."` literal expression (compile-time).
- **Ф.4.3** Codegen: emit static `const char[]` в `.rodata` segment;
  literal evaluates к `(const char*)&_cstr_<hash>`.
- **Ф.4.4** Built-in methods: `(*ro u8).cstring_len()`, `(*ro u8).from_cstring()`,
  `str.to_cstring(buf, buf_len)`.
- **Ф.4.5** Diagnostic: `E_CSTR_EMBEDDED_NULL` для `cstr"\0"`.
- **Ф.4.6** Diagnostic: `E_CSTR_TOO_LARGE` для cstr > 64KB.
- **Ф.4.7** Migration: ffi-cookbook examples migrated к `cstr"..."` где applicable.

**Tests:** T4 series (15+ fixtures).

**Commit:** `feat(plan118.1 Ф.4): cstr"..." literal + C-string interop`

### Ф.5 — Regression + cross-platform + ffi-cookbook + closure (~½ dev-day)

- **Ф.5.1** Full nova test ≥ baseline.
- **Ф.5.2** Cross-platform CI (5+ combos) — all PASS.
- **Ф.5.3** ABI snapshot tests `tests/abi/plan118_1/*` validated.
- **Ф.5.4** Performance: memcpy/memmove benchmark — within 5% of native libc.
- **Ф.5.5** `docs/ffi-cookbook.md` update с typed buffer examples (libpng, libcurl,
  sqlite blob).
- **Ф.5.6** `docs/typed-pointers.md` extension (intrinsics + cstr sections).
- **Ф.5.7** `examples/typed_pointers/07_bulk_copy.nv` + `08_cstring.nv` +
  `09_addr_of.nv`.
- **Ф.5.8** Spec D217 promoted; D26 cross-ref committed.
- **Ф.5.9** Memory `project-plan118_1-status.md`.
- **Ф.5.10** Status section в этом plan-файле.
- **Ф.5.11** Commit + PR review.

---

## D-block changes

### D217 (NEW) — FFI memory access primitives + C-string convention

**Локация:** `spec/decisions/02-types.md` (после D216).

#### §1. Typed read/write

- `(*T).read() -> T` — typed read; equivalent to `*p`
- `(*mut T).write(v T) -> ()` — typed write; equivalent to `*p = v`
- All в unsafe context

#### §2. Bulk copy

- `(*ro T).copy_to(dst *mut T, count usize) -> ()` — memmove (overlap safe)
- `(*ro T).copy_to_nonoverlapping(dst *mut T, count usize) -> ()` — memcpy
- Units: element count (compiler scales by sizeof(T))

#### §3. Volatile

- `(*T).read_volatile() -> T` — no caching
- `(*mut T).write_volatile(v T) -> ()` — immediate side-effect
- Codegen: C `volatile` qualifier на access point

#### §4. Address-of intrinsics

- `addr_of!(expr) -> *ro T` — pointer без temp materialization
- `addr_of_mut!(expr) -> *mut T` — same, mut variant
- Critical для packed structs / uninit memory

#### §5. Alignment + size

- `align_of[T]() -> usize` — compile-time const
- `size_of[T]() -> usize` — compile-time const

#### §6. C-string convention

- `cstr"..."` literal — null-terminated, `.rodata`, type `*ro u8`
- Cannot contain embedded `\0` (`E_CSTR_EMBEDDED_NULL`)
- Maximum 64KB literal length (`E_CSTR_TOO_LARGE`)
- Interop: `.cstring_len()` (runtime strlen), `.from_cstring()` (copy to str),
  `str.to_cstring(buf, len)` (write null-term to buffer)

**Cross-ref:** D216 (typed pointer foundations), D26 (str type), D214 (ptr).

### D26 cross-ref (existing string types) — C-string convention paragraph added

Cross-ref existing D26 (str type) с new `cstr"..."` literal pattern и
interop conversion functions. No D26 amend — just §«FFI interop» paragraph.

---

## Tests

### T1 — Typed read/write + bulk copy (Ф.1)

**Positive:**
- **T1.1** `(*u8).read()` returns byte value
- **T1.2** `(*mut u8).write(0xFF)` writes byte
- **T1.3** `(*i32).read()` returns 4-byte int
- **T1.4** `copy_to(dst, 16)` memmove correctly
- **T1.5** `copy_to_nonoverlapping(dst, 16)` memcpy correctly
- **T1.6** Cross-platform: memcpy semantics identical
- **T1.7** Unit scaling: `(*i32).copy_to(dst, 4)` = 16 bytes (4 ints × 4 bytes)

**Negative:**
- **NEG-T1.8** `.read()` outside unsafe — `E_UNSAFE_REQUIRED`
- **NEG-T1.9** `(*ro T).write(v)` — `E_POINTER_RO_ASSIGN`
- **NEG-T1.10** Type mismatch: `(*u8).copy_to(*u32, n)` — `E_PTR_COPY_TYPE_MISMATCH`
- **NEG-T1.11** copy_to_nonoverlapping with detected overlap (compile-time when
  possible) — `W_PTR_COPY_OVERLAP_DETECTED` warning

### T2 — Volatile + alignment/size (Ф.2)

**Positive:**
- **T2.1** `read_volatile()` — codegen emits `volatile` qualifier (inspect)
- **T2.2** `write_volatile(v)` — codegen emits `volatile` qualifier
- **T2.3** `align_of[*ro u8]() == 1`, `align_of[*ro u64]() == 8`
- **T2.4** `size_of[Account]()` matches struct size
- **T2.5** Volatile read sequence not reordered (compile asm inspection)

**Negative:**
- **NEG-T2.6** `read_volatile()` outside unsafe — `E_UNSAFE_REQUIRED`
- **NEG-T2.7** `align_of[invalid_type]()` — `E_TYPE_NOT_FOUND`

### T3 — addr_of! / addr_of_mut! (Ф.3)

**Positive:**
- **T3.1** `addr_of!(packed.b)` returns `*ro T` без deref
- **T3.2** `addr_of_mut!(packed.b)` returns `*mut T`
- **T3.3** addr_of! on uninit `MaybeUninit[T]` (cross-ref Plan 118.2 — stub
  for V1)
- **T3.4** Out-param pattern: `try_alloc(addr_of_mut!(buf))` works
- **T3.5** Codegen: `addr_of!(expr)` emits `&(expr)` direct C (no temp)

**Negative:**
- **NEG-T3.6** `addr_of!(42)` — `E_ADDR_OF_NOT_LVALUE`
- **NEG-T3.7** `addr_of_mut!(ro_binding)` — `E_ADDR_OF_MUT_REQUIRES_MUT_BINDING`
- **NEG-T3.8** `addr_of!()` outside unsafe — `E_UNSAFE_REQUIRED`

### T4 — cstr"..." literal + C-string interop (Ф.4)

**Positive:**
- **T4.1** `cstr"hello"` type `*ro u8`, length-incl-null = 6
- **T4.2** `cstr"hello".cstring_len()` returns 5 (excluded null)
- **T4.3** `cstr"hello".from_cstring()` returns Nova `str = "hello"`
- **T4.4** `"hello".to_cstring(buf, 16)` writes "hello\0" to buf, returns 6
- **T4.5** FFI roundtrip: pass `cstr"..."` to external fn, library reads correctly
- **T4.6** Storage: cstr"..." literal в `.rodata` (inspect linker map)
- **T4.7** Same cstr"..." in multiple places — deduplication через linker

**Negative:**
- **NEG-T4.8** `cstr"foo\0bar"` — `E_CSTR_EMBEDDED_NULL`
- **NEG-T4.9** `cstr"<65KB string>"` — `E_CSTR_TOO_LARGE`
- **NEG-T4.10** `cstr"..."` assigned к `str` type — `E_TYPE_MISMATCH_STR_VS_CSTR`
- **NEG-T4.11** `str.to_cstring(buf, len)` with len too small — runtime error (Fail)

### R — Regression

- **R1** Full nova test ≥ post-Plan 118 core baseline
- **R2** Cross-platform CI all combos PASS
- **R3** ABI snapshots stable
- **R4** Performance: memcpy/memmove ≥ 95% native libc speed
- **R5** Plan 115 C-shim examples migrated к cstr"..." — fewer LOC, identical
  behavior

---

## Acceptance criteria

| # | Критерий | Verification |
|---|---|---|
| A1 | `(*T).read()` / `(*mut T).write(v)` work in unsafe context | T1.1-T1.3 + NEG-T1.8 |
| A2 | `copy_to()` / `copy_to_nonoverlapping()` emit memmove/memcpy correctly | T1.4-T1.5 + asm inspection |
| A3 | Bulk copy unit scaling (element count × sizeof(T) = bytes) | T1.7 |
| A4 | Volatile read/write emits `volatile` qualifier — no reordering | T2.1-T2.2, T2.5 |
| A5 | `align_of[T]()` / `size_of[T]()` compile-time constants correct | T2.3-T2.4 |
| A6 | `addr_of!()` / `addr_of_mut!()` return pointers без temp materialization | T3.1-T3.2, T3.5 |
| A7 | Packed struct field pointer access works (no UB on unaligned load) | T3.1 |
| A8 | Out-param pattern via `addr_of_mut!()` works end-to-end | T3.4 |
| A9 | `cstr"..."` literal compiles к null-terminated `.rodata` static | T4.1, T4.6 |
| A10 | `cstr"..."` direct FFI to C library (no shim conversion needed) | T4.5 |
| A11 | C-string interop methods (cstring_len / from_cstring / to_cstring) work | T4.2-T4.4 |
| A12 | Embedded null + oversized cstr literal errors | NEG-T4.8, NEG-T4.9 |
| A13 | D217 promoted в active spec; D26 cross-ref committed | spec diff |
| A14 | Cross-platform PASS (5+ combos); full nova test ≥ baseline | R1-R3 |
| A15 | Memcpy/memmove performance ≥ 95% native libc | R4 |
| A16 | Plan 115 V1 C-shim examples migrated; LOC reduction documented | R5 |

---

## Risk register

| # | Риск | Митигация |
|---|---|---|
| R-1 | memcpy/memmove linkage cross-platform (libc resolution) | Use compiler intrinsics where available (`__builtin_memcpy`); fallback к libc; tested on each combo |
| R-2 | Volatile semantics divergence between compilers | C `volatile` is well-specified; emit through C qualifier (delegated к C compiler); ABI snapshot validates |
| R-3 | `addr_of!` macro parsing — distinguish from regular fn call | Built-in macro recognition (compiler-internal); `!` suffix marker per syntax convention |
| R-4 | cstr"..." encoding (UTF-8 default) vs C `const char*` (no encoding spec) | Document: cstr"..." stores bytes verbatim (UTF-8 by default; user may use `\xNN` для raw bytes); C functions interpret per their contract |
| R-5 | Cross-platform `volatile` codegen — MSVC vs clang differences | Both emit access through `volatile` qualifier; standard C; tested all combos |
| R-6 | Performance: copy primitives slower than native libc | Direct compiler intrinsic emission (no wrapper); benchmark target ≥ 95% native; if miss, investigate codegen path |
| R-7 | `align_of[T]()` для generic T — must work at monomorphization time | Compiler emits `_Alignof(T)` C-level; resolved at C compile time |

---

## Migration impact

### Plan 115 V1 C-shim simplification

Existing C-shim string conversion boilerplate:
```c
// Plan 115 V1:
char path_buf[1024];
if (path.len >= sizeof(path_buf)) { ... too long ... }
memcpy(path_buf, path.ptr, path.len);
path_buf[path.len] = '\0';
sqlite3_open(path_buf, &db);
```

→ Plan 118.1 Nova-side:
```nova
unsafe {
    sqlite3_open(cstr"db.sqlite3", addr_of_mut!(db))
}
```

**LOC reduction:** typical FFI binding loses ~10-15 lines per fn (string
conversion boilerplate removed).

### `nova_rt/sqlite_mini_ffi.h` migration

Migrate Plan 115 fixture shim к use cstr"..." for hardcoded test strings;
keep dynamic str path through to_cstring(buf, len) pattern.

---

## Compiler error/warning codes

### New errors

- `E_PTR_COPY_TYPE_MISMATCH` — copy_to src/dst pointer types differ
- `E_ADDR_OF_NOT_LVALUE` — `addr_of!(<not lvalue>)` (literal, expression)
- `E_ADDR_OF_MUT_REQUIRES_MUT_BINDING` — `addr_of_mut!(ro_binding)`
- `E_CSTR_EMBEDDED_NULL` — `cstr"...\0..."`
- `E_CSTR_TOO_LARGE` — `cstr"..."` exceeds 64KB
- `E_TYPE_MISMATCH_STR_VS_CSTR` — `cstr"..."` assigned to `str` type

### New warnings

- `W_PTR_COPY_OVERLAP_DETECTED` — copy_to_nonoverlapping with statically-
  detectable overlap

---

## Documentation deliverables

| File | Status | Phase |
|---|---|---|
| `docs/plans/118.1-ffi-intrinsics-and-cstring.md` | this file | Ф.0 |
| `docs/typed-pointers.md` | UPDATE (extend) | Ф.1-Ф.4 |
| `docs/ffi-cookbook.md` | UPDATE | Ф.4-Ф.5 |
| `examples/typed_pointers/07_bulk_copy.nv` | NEW | Ф.5 |
| `examples/typed_pointers/08_cstring.nv` | NEW | Ф.5 |
| `examples/typed_pointers/09_addr_of.nv` | NEW | Ф.5 |
| `spec/decisions/02-types.md` (D217 new + D26 cross-ref) | UPDATE | Ф.0 draft, Ф.5 promote |
| `docs/project-creation.txt` | UPDATE | per phase |
| `docs/simplifications.md` | UPDATE | per phase |
| `nova-private/discussion-log.md` | UPDATE | per phase |

---

## Cross-references

- **Plan 118 core** — REQUIRED dependency; `*T` family + unsafe + NPO foundations
- **Plan 118.2** (slice fat-pointer + uninit) — complementary; uses 118.1
  primitives (read/write/copy)
- **Plan 118.3** (concurrency) — orthogonal; doesn't depend on 118.1
- **Plan 115 V1** — `ptr` + tuple FFI + C-shim foundations
- **D26** (str type) — cross-ref для C-string interop
- **D216** (typed pointer family) — foundation
- **D217** (NEW, this plan) — FFI memory primitives

---

## Status — closure summary

> Заполняется агентом по завершении.
