<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Подплан 274.5 — читать свой исходник: шесть форм, названных замером

**Статус:** 🚧 В РАБОТЕ с 2026-08-29. Родитель — [274](274-novac-self-hosted-compiler.md)
(Э2, рост подмножества). Подплан 274.4 «одна дверь на корень» закрыт 2026-08-29 и снял
препятствие, ради которого заводился.

**Приоритет:** первый по Э2 — самодистанция и есть смысл всего плана 274: компилятор
Nova на Nova, который не читает себя, самоприменения не достиг.

## 1. Замер, который называет волну (2026-08-29)

Прогон `novac check` по ВСЕМ 52 своим файлам, первый отказ каждого файла
(сырой протокол — `target/self-refusals.txt`, воспроизводится одной командой:
`for f in novac/src/*/*.nv novac/src/*.nv; do ./novac/target/novac.exe check "$f"; done`).
Полнокорпусный дифф того же дня: contract-match 13, behavior-match 6,
**self-distance 52/52** — своих файлов novac не читает НИ ОДНОГО.

| файлов | форма первого отказа | причина, установленная ПРОБОЙ |
|---|---|---|
| 7 | `field must declare a type` | поле типа `[]T` (`out []Diagnostic`, `seen []bool`, `rows []TyRow`). Проба: `xs []int` в записи — отказ; `n int` — чисто. **Сообщение при этом ЛЖЁТ**: тип объявлен, не читается ПРИМЕНЕНИЕ в позиции поля |
| 7 | `syntax error: this is not a form of the language` | два разных корня: верхнеуровневый `const N = 7` (проба — отказ) и модификатор `priv` у типа (`export type FnIndex priv {`, D47; проба — отказ) |
| 6 | `body ends without a value` | хвостовой ВЫЗОВ как значение возврата: `fn wrap(x int) -> int { helper(x) }` (проба — отказ). В подмножестве вызов в позиции инструкции — только `println` |
| 6 | `a test block arrives with the test runner` | файлы `*_test.nv`; законная отсрочка — раннер тестов не входит в Э2 |
| 5 | `an effect attribute is read but not compiled yet` | атрибут `#realtime nogc` на своих же дверях |
| 5 | `match only as a binding initializer` | `match` в позиции инструкции и возврата |
| 3+3+2+1+1+1 | прочее | нераспознанный токен (3), конструктор записи вне инициализатора (3), вызов в позиции инструкции (2), локальный объявленный тип, форма типа (срезы/кортежи/fn-типы), метод не найден |

**Как читать таблицу:** это ПЕРВЫЙ отказ файла, не единственный. Снятие формы двигает
файл к следующему отказу, а не сразу в подмножество; поэтому мерой волны служит
самодистанция (52/52 сегодня), а не число строк таблицы.

## 2. Волны, в этом порядке, и почему в таком

Порядок — от самой дешёвой и самой частой формы к самой дорогой; каждая волна
самостоятельна и заканчивается зелёным ярусом.

* **В1. `[]T` в позиции ПОЛЯ.** Применение уже читается в параметре и возврате (шаг Б,
  2026-08-26) — недостаёт ровно позиции поля. Плюс правка ЛЖИВОГО сообщения: «field must
  declare a type» там, где тип объявлен, а не читается его форма.
* **В2. Верхнеуровневый `const`.** Своя константа — это `builtins`, то есть корень «имя
  языка строкой»; без неё novac не прочтёт даже свой список имён.
* **В3. Модификатор `priv` у типа (D281/D47).** Форма языка, принятая и реализованная
  оракулом; novac её не читает, а собственный резольвер уже написан на ней. Читается
  ВЕСЬ спектр формы — `priv`, `priv(type)`, `priv(file)` и будущий `priv(package)`
  (план [269](269-package-visibility.md), D457, К1 до тега): квалификатор берётся
  сбалансированной скобочной группой без перечисления имён, так что новая ступень
  шкалы не потребует правки чтения. СУДИТЬ видимость подмножество пока не может по
  построению (компиляция одного файла); проверка границы пакета входит с межмодульной
  компиляцией (Э4) — названная отсрочка, и по слову владельца 2026-08-29 реализация
  269 ложится в рамки плана 274. Правило из 269/Ф.1 переносится сюда дословно:
  границу пакета определять ТЕМ ЖЕ кодом, что резолюцию модулей, — вторая дверь к
  этому знанию запрещена.
* **В4. Хвостовой вызов как значение.** Снимает ограничение «в позиции инструкции только
  `println`» для формы «последнее выражение тела — вызов».
* **В5. `match` в позиции инструкции и возврата.**
* **В6. Атрибут `#realtime nogc`** — ЧИТАТЬ и игнорировать (эффектов в подмножестве нет;
  атрибут не меняет семантику C-эмиссии, а его отказ закрывает пять файлов).
* **В7. Шестнадцатеричные литералы** (`0x0A`) — найдено пробой В2, см. ниже: лексер их
  не знает, носитель — `lex/lex.nv`.

Файлы `*_test.nv` (6 штук) волнами НЕ берутся: раннер тестов — отдельный предмет, и это
названная отсрочка, а не забытый хвост.

## 2а. В1 ЗАКРЫТА 2026-08-29 — что сделано и чем доказано

**Сделано, две половины, и одной было мало.** Парсер: тип поля читается ТОЙ ЖЕ дверью,
что тип параметра (`fk.append(@type_ref())` вместо одного `Ident`, `parse.nv:456`).
Чекер: ветка в слоте типа — объявленный тип, а не отказ (`rules.nv`), тем самым снято
ЛЖИВОЕ сообщение. И третья часть, которую вскрыла фикстура: `sem` регистрировал поле по
`leaf_text` — одному имени, — поэтому поле-срез в реестр не попадало и отказ говорил
«unknown field» о поле, написанном прямо там; теперь регистрация идёт через
`ty_of_type_ref`, ту же дверь ссылки на тип, что у параметра, а `field_type_at` сведён
к `field_name_at` + `type_slot_after` — одна дверь на вопрос вместо второго обхода.

**Чем доказано.**
1. **Замер самодистанции до и после** (агент, haiku): форма «field must declare a type»
   стояла первым отказом у 7 файлов — стало **0**; семь файлов уехали к следующему
   отказу (`check/check.nv`, `parse/parse.nv` → «тело без значения»; `check/scope.nv`,
   `sem/defs.nv`, `sem/protocols.nv` → «литерал массива»; `mono/mono.nv` → «форма типа»;
   `types/types.nv` → «атрибут эффекта»). Самодистанция остаётся 52/52 — это ожидаемо:
   у файла много форм, и волна двигает его к следующей, а не в подмножество.
2. **Фикстура** `examples/basics/record_slice_field.nv` (поле-срез рядом с обычным
   полем) — смоук `novac-e1-smoke.sh`: «поведение идентично оракулу», оракул принимает.
3. **Проба обеими сторонами**: со снятой правкой парсера фикстура краснеет прежним
   «field must declare a type», с правкой — чиста.
4. **Храповик корпуса поднят тем же слиянием**: contract 13 → **14**, behavior 6 → **7**,
   причина записана в самом файле базы.

**НАХОДКА О МЕТОДЕ (2026-08-29), стоила получаса.** Обратную пробу я снял командой
`git checkout -- <файл>` — и она откатила НЕ ТОЛЬКО пробу, но и саму правку волны: та
была ещё не закоммичена. Следующие двадцать минут я искал в парсере дефект, которого не
существовало (`type_ref` якобы съедал остаток записи), потому что мерил уже откаченным
бинарём. **Правило, выведенное замером: обратная проба снимается ОБРАТНОЙ ПРАВКОЙ, а
`git checkout` допустим только к закоммиченному состоянию.** Признак, по которому это
ловится за секунды, а не за полчаса: после снятия пробы грепнуть СВОЮ правку на месте.

## 2б. В2 — карта мест (разведка 2026-08-29, агент; ДО кода)

**Главный факт: `const` — вообще НЕ ключевое слово нашего лексера.** Перечень
`TokenKind` (`lex.nv:47-73`) его не содержит, таблица `keyword_kind()`
(`lex.nv:248-303`) ветки `"const"` не имеет, и слово падает в `_ => Ident`
(`lex.nv:302`). Поэтому `export const N = 7` не «частично читается» — он вообще не
доходит до разбора объявления: диспетчер верхнего уровня (`parse.nv:232-262`) знает
`KwFn`, `KwType`, `KwExtern`, `KwExport`, `KwImport`/`KwUse`, `Hash`, `KwTest`,
`KwModule`, а всё прочее копит в junk-корзину до `TOP_LEVEL_STOPS` (`parse.nv:193`).

**Где придётся тронуть, по цепочке соседнего объявления `type X`** (образец, который
волна повторяет): начало разбора `parse.nv:289`, узел `NodeKind.TypeDecl`
(`tree.nv:53`), читатели — `sem/harvest.nv:133`, `sem/sem.nv:442`, `sem/sem.nv:517`,
`check/check.nv:296`. Плюс ветка `export` (`decls.nv:33-52`), которая сегодня знает
`fn`/`extern`/`type`/`import` и отвечает `Err` на всё прочее (`decls.nv:46`).

**Доноры формы:** rustc — `const`-элемент ПОДСТАВЛЯЕТСЯ в каждой точке употребления и
своего символа не имеет (это и есть наше решение об эмиссии); Go — нетипизированная
константа берёт тип у инициализатора (поэтому написанный тип остаётся необязательным).
Нынешний компилятор в этой роли не выступает (П25): он неподвижная точка ПОВЕДЕНИЯ, с
которой сверяется фикстура, и образцом устройства не служит. Его `ConstDecl`
(`compiler-codegen/src/ast/mod.rs:1620`) назван здесь только как адрес, по которому
видно, что форма компилируется.

**Нормативный дом формы — D184 «Keyword refresh» (`spec/decisions/03-syntax.md:7569`),
и это поправка к моей же записи** (ответ координатора 2026-08-29 на вопрос, который я
завёл часом раньше). Я искал формулировку не в том блоке и уже собрался просить номер;
дыры нет. D184 записывает голую форму прямым текстом — «`const X = expr` принимает
только constexpr-eligible RHS», — и все его примеры идут без типа
(`const MAX_PAYLOAD = 4096`, `const GREETING = "hello"`); обе формы стоят там рядом
(`const ORIGIN Point = { … }` с типом и `const COMPUTED = make_point(7, 14)` без).
D200 — про АССОЦИИРОВАННЫЕ константы (`const Type.NAME`), обычную module-const он лишь
цитирует, и цитирует в типизированном варианте, потому что у ассоциированных тип есть
всегда; эта цитата и читалась как «форма обязана быть с типом». Нового D-блока и номера
не нужно: координатор правит цитату в D200 сам, ссылкой на D184 как на дом.
**Числом:** в `std/src` голых `const` — 157, типизированных — 61, то есть голая форма и
есть основная. Компилятор согласен с обеими: `export const N = 7` и
`export const M int = 8` собираются.

**Цена и выгода, числом:** 61 строка `^const `/`^export const ` в 7 файлах —
`check/rules.nv` 25, `builtins/builtins.nv` 16, `check/strings.nv` 8, `lex/lex.nv` 5,
`check/binds.nv` 4, `emit_c/shell.nv` 2, `sem/mangle.nv` 1. Из них `builtins` — корень
«имя языка строкой»: пока константы не читаются, novac не прочтёт собственный список
имён, то есть модуль, на котором стоит правило П5.

## 2в. В2 ЗАКРЫТА 2026-08-29 — что сделано и чем доказано

**Сделано.** `const` стал токеном (`lex.nv`), верхнеуровневый диспетчер и обёртка
`export` его знают, разбор читает `const NAME [Тип] = expr` ТОЙ ЖЕ дверью ссылки на тип
и обычной дверью выражения, `sem` регистрирует имя в едином реестре строкой своего
пространства (`ConstRow`/`ConstTable`), чекер типизирует имя ПО ИНИЦИАЛИЗАТОРУ, а
эмиттер ПОДСТАВЛЯЕТ инициализатор на месте употребления.

**Решение, которое стоит назвать: подстановка вместо C-символа.** Константа не заводит
своего имени, поэтому корень «C-ИМЯ сущности» не получает новой двери, мэнглинг не
трогается, а в C выходит ровно то, что компилятор C и так делает с файловым
`static const`. КАКАЯ это константа, эмиттер узнаёт из КАНАЛА (новая рука `Resolution`),
а не спросив реестр по имени: последнее — разрешение имени вне `check/`, ровно то, что
запретило правило E подплана 274.4. Эта волна — первая, построенная уже под ним.

**Дисциплина П21 в работе:** добавление руки к сумме уронило одиннадцать исчерпывающих
`match`, каждый компилятор назвал поимённо, и каждый отвечен по смыслу. `ConstRow` лежит
в `callables.nv` рядом с `FieldRow` и `VariantRow` — третий случай
`[LEGACY-#761-newtype-order-in-sum-arm]`.

**Чем доказано.** Фикстура `examples/basics/module_const.nv` — три формы (голая int,
голая str, типизированная) — байт-в-байт с оракулом. Замер по своим файлам: из шести
файлов, стоявших на «syntax error», **четыре уехали дальше** (`builtins` со строки 48 на
214, `emit_c/shell`, `sem/mangle`, `source/source`), пятый (`resolve`) стоит на `priv` —
это волна В3, шестой — на находке ниже.

**НАХОДКА О МЕТОДЕ ЗАМЕРА.** Первый прогон сравнения сказал «не изменилось ничего», и это
было неверно: он сравнивал ТЕКСТЫ сообщений, а файл уехал на 166 строк вперёд с тем же
текстом. Метрика волны — пара (файл, СТРОКА), а не (файл, сообщение); сравнение по тексту
прячет движение внутри одной формы.

**НАХОДКА, ЗАВОДЯЩАЯ ВОЛНУ В7: шестнадцатеричный литерал не читается.** `const B = 10`
чист, `const B = 0x0A` — «syntax error: this is not a form of the language»; проба
показала, что дело в ЛИТЕРАЛЕ, а не в форме константы. Носитель — `lex/lex.nv` (пять
своих констант вида `const B_LF u8 = 0x0A`), и это лексер, а не подмножество: форма
языка, которую наш лексер не знает. Волна В7 — «шестнадцатеричные и двоичные литералы»,
цена оценивается замером, когда до неё дойдёт очередь.

## 2г. Находка о ФОРМЕ кода (2026-08-29), полезная всем волнам

Волна В2 столкнула два стража на одной строке, и это не противоречие в конвенции, а
неназванное следствие двух правил:

* `check-novac-branch-complete` требует, чтобы у ветвления был `else` с телом, ЛИБО
  терминатор в then-ветке (`return`/`continue`/`break`/`ice`), ЛИБО пустой `else` С
  КОММЕНТАРИЕМ;
* `check-novac-no-default-branch` запрещает `else` за проверкой ВАРИАНТА закрытого
  множества: такой `else` молча значит «все прочие варианты».

Проверка вида `if leaf_kind(c) == TokenKind.Assign` попадает под оба сразу: пустой
`else` с комментарием удовлетворяет первого и краснит второго. **Пересечение —
ТЕРМИНАТОР:** инвертировать условие и выйти `continue`. Тогда `else` нет вовсе, и оба
стража зелены. Та же форма уже стоит в соседних дверях (`field_name_at`,
`const_name_at`) — то есть правило не новое, просто нигде не записанное:

> Проверка ОДНОГО варианта закрытого множества внутри цикла пишется инверсией с
> `continue`, а не `if`-ом с `else`. `else` остаётся для предметов, у которых обе
> стороны содержательны.

Цена незнания этого — два лишних прогона яруса по десять минут; поэтому правило здесь, а
не в чьей-то памяти.

## 2д. В3 ЗАКРЫТА 2026-08-29 — что сделано и чем доказано

**Сделано.** Маркер `priv` читается в объявлении типа ровно как `value`: лист между
именем/generics и телом, ПЛЮС необязательная скобочная группа квалификатора
(`priv(type)`, `priv(file)`; будущий `priv(package)` плана 269 пройдёт той же дорогой —
группа берётся сбалансированным проходом, имена не перечисляются). До волны парсер
глотал `priv` веткой newtype и ронял тело записи в junk. Семантика — названная
отсрочка: в компиляции одного файла судить видимость не по чему (D47: внутри модуля
приватное поле читаемо), проверка входит с межмодульной волной (Э4), правило границы
пакета — из 269/Ф.1, второй двери к нему не заводить.

**Чем доказано.** (1) `resolve.nv` — файл с двумя настоящими `priv` — уехал со строки
79 на 86 (следующая форма: литерал массива); форма «syntax error» в переписи 7 → 6.
(2) Фикстура `examples/basics/priv_type.nv` байт-в-байт с оракулом; корпус 15/8 →
**16/9**, храповик поднят тем же слиянием. (3) Обратная проба ОБРАТНОЙ ПРАВКОЙ (урок
В1): с выключенным чтением фикстура краснеет прежним «not a form», после возврата —
чиста, `grep REVERSE PROBE` = 0. (4) Тесты 10/10, пять смоуков байт-в-байт.

## 2е. В4 ЗАКРЫТА 2026-08-29 — что сделано и чем доказано

**Сделано.** Хвостовой вызов — значение возврата. Класс держался на двух ЗЕРКАЛЬНЫХ
исключениях `!= NodeKind.Call` — в правиле хвоста (`rules.nv`) и в условии печати
`return` у эмиттера — плюс на правиле вызова-инструкции, которое не отличало хвост.
Оба исключения сняты; правило инструкции получило аргумент `tail_is_value`, и контекст
«этот блок — тело функции со значением» несёт одно поле обхода (`tail_value_body`,
по образцу `scope`/`self_probe`), которое в ОБЕИХ фазах ставит одна дверь
`value_body_of` — у субсет-обхода и у типизации один источник, расходиться нечему.
Типизация хвостового вызова идёт ЧЕРЕЗ выражение (`@type_expr`), а не через дверь
инструкции: та молча пропускает не-println, и вызываемый не попал бы в канал — mono не
дошёл бы до функции, и отказ пришёл бы от линкера.

**Границы держат, доказано пробами:** вызов-инструкция в теле `while` и хвостовой вызов
в `main` (без объявленного значения) отвергаются как прежде — исключение не протекает.

**Чем доказано.** Перепись: форма «body ends without a value» 8 → 4, пять файлов уехали
(три — к «no such method», по одному — к литералу массива и дальше по своей форме).
Фикстура `examples/basics/tail_call.nv` байт-в-байт; корпус 16/9 → **17/10**; тесты
10/10.

## 2ж. ПЕРЕДАЧА ПО /stop 2026-08-29 ~04:55 — ИСПОЛНЕНА тем же днём

Все пять пунктов закрыты следующим окном: заголовок `operators.nv` поправлен (страж
`module-donor` зелёный), ярус `NOVAC-GATE OK (final)`, В4 закоммичена `3f9cd8988`
готовым сообщением, синк с main слит (`f8d886dc0`, два конфликта разрешены: комментарий
spec-queue — сторона main, леджер — обе стороны), пуш через `push-after-gate.sh`.
Попутно бюджет яруса push поднят 400 → 560 по замеру 484с на чистой машине — рост
поимённо назван в файле бюджета. Текст передачи ниже сохранён как есть.

### Исходный текст передачи — В4 сделана, НЕ закоммичена

Код волны В4 в дереве, рабочий (сборка зелёная, фикстура `tail_call.nv` байт-в-байт,
перепись 8 → 4, корпус намерен 17/10 и причина храповика уже вписана в baseline), но
коммита НЕТ: последний ярус нашёл одну придирку — у нового `check/operators.nv` строка
`Guarded by:` в заголовке не называет ни стража, ни теста по форме П27 (страж
`check-novac-module-donor`, текст требования в его выводе). Следующему окну:

1. поправить заголовок `operators.nv` (три строки Donor/Role/Guarded по форме П27 —
   образец в шапке `check/binds.nv`);
2. прогнать `NOVAC_TIER=push bash scripts/gate-novac.sh` до `NOVAC-GATE OK (final)`;
3. закоммитить: сообщение ГОТОВО в `target/msg-b4.txt`; файлы:
   `novac/src/check/{typing,rules,check,operators}.nv`, `novac/src/emit_c/emit_c.nv`,
   `examples/basics/tail_call.nv`, `scripts/guards/novac-corpus.baseline`,
   `docs/plans/274.5-read-own-source.md`, `docs/dev/novac-time-ledger.md`
   (`git add` по именам: operators.nv и tail_call.nv новые);
4. СИНК с main после `e788d1f96` (передача координатора 2026-08-29 ~04:47): там
   спека литерала `[5, 7, 3]`, уточнение D200 (дом — D184), spec-queue=29 (наш
   `novac/nova.toml` уже стоит на 29). Слияние — по `/commit-push`;
5. пуш через `push-after-gate.sh`.

Решения координатора той же передачи, уже НЕ требующие действий: ёмкость D239 —
v0.1 выходит с оговоркой в release notes, точную ёмкость реализует novac (уже
реализована), оракул до тега не трогается (реестр №783); битовые имена остаются
`@bitand/@bitor/@bitxor/@bitnot`; план 276 закрыт целиком; номера реестра — №TBD.

Очередь волн после В4: В5 (`match` в позиции инструкции/возврата, 5 файлов),
В6 (атрибут `#realtime nogc` читать и игнорировать, 6 файлов), В7 (hex-литералы,
находка В2), плюс новый лидер переписи — «no such method in the declarations handed»
(4 файла, вырос после В4: хвостовые вызовы дошли до подбора — смотреть, каких
деклараций не хватает).
## 2и. В5 — карта мест и решение (разведка 2026-08-29, агент sonnet; ДО кода)

**Отказ — ОДНО место:** `check.nv:475`, ветка `MatchExpr` обхода, срабатывает при
`init_pos == false`. Флаг взводится ровно в двух позициях: последний ребёнок `BindStmt`
(инициализатор) и `FnExprBody` (`=>`-тело). Парсер позиционно не ограничен — вся
граница в чекере.

**Что уже есть.** Эмиттер умеет размещать match целиком: `@emit_match`
(`emit_c.nv:501`) заводит `_novac_scr_t`/`_novac_match_t`/`_novac_matched_t` и печатает
рукава цепочкой `if (!matched && ...)`; оба нынешних входа (`@emit_bind`,
`FnExprBody`) зовут его и читают временную результата. `is_expr_kind` уже перечисляет
`MatchExpr`, поэтому правило хвоста «нет значения» match НЕ отказало бы — отказ
приходит раньше, из стены формы.

**Чего нет, поимённо (это и есть волна).**
1. **Инструкция:** `@type_stmt` (`typing.nv:399`) для `MatchExpr` попадает в голый
   `@type_expr`, а тот для match — пустая ветка: рукава не типизируются, полнота не
   проверяется. Нужен маршрут `MatchExpr → @type_match` — тем же приёмом, что уже
   стоит в `@type_bind` (`binds.nv:105`) и `FnExprBody` (`typing.nv:202`). Эмиттеру —
   инструкция-match без чтения временной результата (рукава исполняются ради эффекта).
2. **Хвост блочного тела:** `@type_block` (`typing.nv:226`) даёт особый путь только
   `Call` (наследие В4) — match на хвосте нужен тот же маршрут; стена формы пропускает
   хвост через контекст `@tail_value_body` (дверь В4 `value_body_of`, один источник на
   обе фазы); `@emit_callable` печатает хвостовой match как `FnExprBody`-ветка:
   `@emit_match` + `return _novac_match_t`.

**Решение о инструкции-match без результата:** временная результата НЕ заводится, когда
match стоит инструкцией — печатаются только scrutinee, флаг и рукава. Тип первого
рукава при этом не спрашивается вовсе (не «спрашивается и выбрасывается»): вопрос без
читателя — это К7.

**Разблокирует по переписи:** 5 файлов (`check/typing.nv`, `diag/diag.nv`,
`emit_c/emit_expr.nv`, `parse/expr.nv`, `tree/tree.nv`).

## 2к. В5 ЗАКРЫТА 2026-08-29 — что сделано и чем доказано

**Сделано.** `match` читается в двух новых позициях. **Инструкция:** стена формы
(`check.nv`) получила флаг `stmt_pos`, считаемый в ОДНОМ месте — рекурсия обхода по
детям `Block`; типизация идёт через `@type_match_stmt`, который типизирует scrutinee,
связывает образцы, типизирует стражи, обходит тела КАК ИНСТРУКЦИИ и судит полноту, но
НЕ спрашивает тип рукавов. **Хвост блочного тела:** маршрут рядом с хвостовым вызовом
волны В4, через `@type_match` (там рукава обязаны согласиться — это значение), тип
записывается в канал; эмиттер печатает `@emit_match` + `return` временной, как всегда
делал для `=>`-тела.

**Решение о рукавах-инструкциях.** Тип рукава не спрашивается вовсе, а не
«спрашивается и выбрасывается»: `{}` рядом с `println(...)` — законная пара, и свёртка
типов отвергла бы её. Вопрос без читателя — это К7, и волна его не заводит.

**Одна дверь, два хвоста — дважды.** Разбор образца вынесен в `@bind_arm_pattern` и
служит обоим вариантам рукава в чекере; у эмиттера `@emit_arm` получил параметр
`as_stmt`, переключающий ТОЛЬКО хвост. Копия шестидесяти строк разбора образца в обоих
местах разошлась бы на первой правке — тот же довод, что у `report_if_bad_call_stmt`
волны В4.

**Чем доказано.** Форма «match only as a binding initializer» исчезла из переписи
целиком: **5 → 0**, все пять файлов уехали к следующему отказу. Фикстура
`examples/basics/match_stmt.nv` — обе позиции в одном файле — байт-в-байт с оракулом;
семь прежних фикстур прогнаны на регрессии, все зелёные. Обратная проба: со снятым
маршрутом инструкции сборка падает `ice`-ом о дыре в канале, с маршрутом — чисто
(проба снята ОБРАТНОЙ ПРАВКОЙ, по правилу раздела 2а). Храповик корпуса поднят тем же
слиянием: contract 17 → **18**, behavior 10 → **11**. Ярус `NOVAC-GATE OK (final)`.

**Три придирки гейта, все по делу:** у `@type_arm` пропал ///-док (уехал вместе с
вынесенной дверью), текст `ice` повторился в двух местах (П12: текст заменяет адрес —
statement-вариант называет себя), и ветвление по виду узла в `@emit_arm_value` просило
терминатор вместо `else` (правило раздела 2г, второе применение).

**Перепись после волны (53 файла — `check/operators.nv` новый):** лидеры теперь
«syntax error» 9, «литерал массива только инициализатором» 7, `test`-блок 6, атрибут
эффекта 6, «нет такого метода» 5, «конструктор записи только инициализатором» 5.

## 2л. В6 ЗАКРЫТА 2026-08-29 — а «самодистанция сорвалась с пола» оказалось ЛОЖЬЮ ЗАМЕРА

**ПОПРАВКА 2026-08-30 (см. 2с).** Число 41/53 этой волны было артефактом: пачечный
`novac check` умирал на ICE в `interop.nv` (код 2 проходит порог «вне 0/1/2»), и
двенадцать файлов ПОЗАДИ точки смерти выходили «чистыми», не будучи досуженными.
Честная самодистанция после В6 была и осталась «все отвергнуты». Раннер починен той же
волной; заголовок ниже сохранён как история ошибки.

**Сделано.** `#realtime` (с модификатором `nogc` и без) ЧИТАЕТСЯ и НЕ ПРОВЕРЯЕТСЯ.
Имя атрибута живёт в `builtins` (П5: имя языка строковым литералом — только там),
ветка атрибутов чекера пропускает два имени и отвергает все прочие.

**Решение обосновано ЗАМЕРОМ, а не рассуждением.** Игнорировать гарантию опасно ровно
тем, что подмножество станет ШИРЕ языка, — а это класс, который здесь ловят громче
всего. Проба 2026-08-29: оракул ПРИНИМАЕТ аллоцирующее тело под `#realtime nogc`
(`ro v = [1, 2]` внутри), то есть на этих формах он гарантию не проверяет. Значит
чтение-без-проверки совпадает с неподвижной точкой ПОВЕДЕНИЯ, и регистрировать
расхождение нечего. D172 и его амендмент описывают больше, чем оракул исполняет; этот
зазор — оракульский, и в день, когда он закроется, корпусный дифф покраснеет здесь.

**Границы волны.** Всякий ДРУГОЙ атрибут остаётся вне подмножества: неизвестный атрибут
может менять смысл объявления, и принять его не читая — тот же способ стать шире языка.
`#impl(Serialize)` из `diag/diag.nv` сохраняет отказ, пока никто не измерил, что он
значит (в `spec/decisions/` его нет — искал агент).

**ГЛАВНОЕ ЧИСЛО ВОЛНЫ — НЕ КОРПУС.** Самодистанция впервые сошла с пола:
**53/53 → 41/53**. Двенадцать собственных файлов novac проходит своей же проверкой
целиком. Ради этого числа подплан и заведён; корпусный храповик 18/11 → 19/12 идёт
рядом, носитель — `examples/basics/realtime_attr.nv` (обе формы записи).

**Находка о ПОРЯДКЕ РАБОТ (стоила двух прогонов).** Ярус, измеряемый под моей же
параллельной нагрузкой, врёт: сначала 484с при потолке 400 (подняли до 560 по замеру),
затем 650с при 560 — оба раза рядом крутился корпусный прогон. На чистой машине тот же
ярус зелёный. **Правило: гейт меряется ОДИН, без соседних тяжёлых команд** — иначе
поднимаешь потолок под собственный шум и теряешь настоящий сторож.

## 2м. НАХОДКА ПО ДОРОГЕ (2026-08-29): флаки перф-тест `nova-lsp` блокирует чужой пуш

**Что.** `symbols::tests::refidx_perf_faster_than_full_scan` в полном гейте даёт
`432 passed; 1 failed` (паника `nova-lsp/src/symbols.rs:1470`), а в одиночку зелен —
**3 прогона из 3, по 0.14с**. За смену он остановил мой пуш ДВАЖДЫ.

**Класс, а не носитель.** Утверждение `idx_dur < scan_dur` сравнивает два
`Instant::elapsed` микросекундного порядка БЕЗ запаса и БЕЗ повторений: любое вытеснение
планировщиком между замерами переворачивает неравенство, а под нагрузкой гейта
(параллельные крейты плюс мега-CU) это происходит регулярно. Тест меряет не алгоритм, а
везение. Чинится классом: утверждать свойство, ради которого индекс заведён (не обходит
все файлы), либо сравнивать медиану N прогонов с явным запасом. Форма и порог — решение
владельца `nova-lsp`; сам не чинил, потому что молча сдвинутый порог в чужом тесте меняет
смысл проверки. **Оговорка о носителе:** перед закрытием грепнуть `elapsed()` в тестах
остальных крейтов — форма «сравнить два wall-clock замера и назвать это перф-тестом»
может стоять не в одном месте.

**Почему запись здесь, а не в реестре 221.1.** Запись для реестра написана и готова, но
`check-registry-entry-shape` не пускает в гейт строку с номером `TBD` — номер присваивает
интегратор ДО прогона. Держать из-за этого волны незапушенными неправильно, поэтому
находка живёт в текущем плане (законный адрес для находок novac), а строка реестра
заводится, как только придёт номер. Приоритет по моей оценке — P2: тег не блокирует, но
регулярно краснит авторитетный гейт и съедает по двадцать минут чужого прогона.

## 2н. НАХОДКА РАЗВЕДКИ (2026-08-29, агент sonnet): лидер переписи — не форма языка

Группа «no such method in the declarations handed to novac» (5 файлов:
`check/calls.nv`, `check/operators.nv`, `sem/sem.nv`, `sem/slots.nv`, `sem/typeref.nv`)
разобрана поимённо, и картина другая, чем у всех прежних волн. Недостающие методы —
`kind_of` (36+ употреблений), `type_decl`, `is_ident`, `lookup`, `compiles_body` — все
СУЩЕСТВУЮТ в самом novac (`tree.nv:381`, `defs.nv:200`, `tree.nv:327`,
`resolve.nv:362`, `callables.nv:304`). Не хватает не форм, а ДЕКЛАРАЦИЙ: `novac check
<файл>` судит файл в одиночку и деклараций его импортов не видит.

**Что из этого следует.** Следующая граница самодистанции — МЕЖФАЙЛОВОЕ чтение: разбор
импортов файла и передача чекеру сигнатур соседних модулей (той же дорогой, которой
`harvest` уже читает std-декларации). Это ЭТАП с собственным дизайном (порядок чтения,
циклы модулей — D291 у оракула, цена на 52 файлах), а не волна на день; его место —
после исчерпания одинарных форм.

**Методическая деталь, оплаченная агентом:** у диагностик `primary.start/end` —
БАЙТОВЫЕ смещения UTF-8; построчный пересчёт через питон-строку дрейфует на файлах с
многобайтовыми символами в комментариях. Считать носителей — только побайтовым чтением.

**Очередь одинарных форм, оставшихся до этапа межфайлового чтения (по переписи В6):**
«литерал массива только инициализатором» (7), «конструктор записи только
инициализатором» (5), «syntax error» (9 — там смесь: hex-литералы В7 и прочее, мерить
поимённо), «вызов-инструкция только println» (2), «голова for — только диапазон» (3).

## 2о. В8 — карта мест (разведка 2026-08-30, агент sonnet; ДО кода)

Группа «array literal only as a binding initializer» разобрана поимённо по семи файлам,
и 56 диагностик распались на ТРИ предмета:

1. **Литерал в поле конструктора записи** — 12 носителей (`binds: []T.new()` и родня).
2. **Литерал аргументом вызова** — 9 носителей (`branch(0, kind, []Node.new())`).
   Голого литерала в возврате/хвосте и bare-присваивании в корпусе НЕТ — ноль случаев,
   значит эти позиции волна НЕ берёт (граница по замеру, а не по симметрии).
3. **ЛГУЩАЯ ДИАГНОСТИКА: 35 из 56 — вовсе не литералы.** Это ИНДЕКСАЦИЯ `expr[idx]`
   (`@shadowed[i]`, `table.rows[i]`, `@next[raw_row(...)]`), помеченная тем же текстом.
   Различаются по span: настоящий литерал — 2 байта (`[]`), индексация — 1 байт (`[`).
   Проба воспроизвела на минимальном файле: `@field[i]` в методе record-типа даёт
   «array literal only...». Сообщение обязано назвать индексацию индексацией — даже
   если сама форма остаётся вне подмножества, отказ про ЧУЖУЮ форму — это класс,
   который уже чинился в В1 («field must declare a type»).

**Методическая деталь:** классификация по span работает только на байтовых смещениях
(правило раздела 2н).

**Порядок внутри волны:** сперва (3) — честное имя для индексации (дёшево, снимает 35
ложных меток и открывает настоящий счёт следующей переписи), затем (1) и (2) — два
законных положения литерала, каждое со своей фикстурой и пробой обеими сторонами.

## 2п. ПЕРЕДАЧА ПО /stop 2026-08-30 ~02:25 — В8-Б работала, но дерево В МОМЕНТЕ КРАСНОЕ

**Что случилось в последнюю минуту.** Разрез `emit_c.nv` (1015 строк при пределе 1000)
захватил лишние функции — якорь конца был неверный. `emit_match.nv` удалён,
`emit_c.nv` ВОССТАНОВЛЕН к закоммиченному состоянию `git checkout` — и вместе с
разрезом откатилась его половина части Б. **Сборка сейчас падает**: поле `lit_tmp`
объявлено в `Emitter` (`shell.nv`), а конструктор в `emit_c.nv` его не заполняет, и
двери hoist в `emit_c.nv` больше нет.

**Что УЖЕ было доказано (до отката):** обе позиции литерала работали end-to-end —
фикстура `examples/basics/array_lit_positions.nv` байт-в-байт, обратная проба (hoist
снят) падала именованным ice, корпус вырос 19/12 → 20/13 (база УЖЕ поднята с причиной),
модульные тесты 10/10 после честного обновления d5 (отказ переехал с позиции литерала
на печатаемость аргумента — «println covers»).

**Что в дереве (незакоммичено):** `check.nv` — arg_pos в обходе; `typing.nv` — ветка
ArrayLit в @type_expr; `slots.nv` — ArrayLit в is_expr_kind; `shell.nv` — поле lit_tmp;
`emit_expr.nv` — вызов hoist в emit_call_stmt + ветка ArrayLit в печати + импорт
raw_node; `pipeline_test.nv` — d5; `novac-corpus.baseline` — 20/13; фикстура (untracked).
`emit_c.nv` — ЧИСТЫЙ (восстановлен).

**Порядок доделки следующему окну:**
1. Переприменить emit_c-половину скрипта `b8b_emit.py` (скретчпад окна умирает с
   сессией — правки описаны здесь): (а) в `emit()` перед конструктором `Emitter`
   построить `mut lt = []str.new()` размером `out.types.len()` из "" и передать
   `lit_tmp: lt`; (б) дверь `@hoist_array_lits(e Node)` — пост-порядковый обход,
   на ArrayLit: `@lit_tmp[raw_node(e.id_of())] = @emit_array_lit(e, @out.type_of(...))`,
   ФОРМА — терминатор `if kind != NodeKind.ArrayLit { return }` (не else!);
   (в) в CtorField-ветке `emit_record_ctor`: `if fk.len() > 1 { @hoist_array_lits(fk[2]) }`
   с пустым else-комментарием; (г) импорт `raw_node` в `../tree`-строку emit_c.nv.
2. Сборка, смоук фикстуры, обратная проба (снять hoist-вызов ОБРАТНОЙ ПРАВКОЙ).
3. РАЗРЕЗ файла заново, якоря ТОЧНЫЕ: блок от дока `emit_match` («/// the tag and
   unpacks payload fields») до дока `emit_block_stmts_fn` («/// tail=true: the LAST
   expression») — семья emit_match/emit_tag_test/emit_arm/emit_arm_value/emit_match_stmt,
   и ТОЛЬКО она, в со-равный `emit_match.nv` с полной шапкой П27 (образец, включая
   `Guarded by` с расширениями, — в шапке `check/operators.nv`).
4. Ярус `novac push` ОДИН на машине (правило 2л); при 560+ на чистой машине —
   поднять бюджет по замеру. Прошлый прогон: 659с при 560 на занятой машине плюс два
   структурных красных (оба закрываются пунктами выше).
5. Коммит (области перечислены выше + fixture add), пуш `push-after-gate.sh`.

**Хвосты за пределами В8-Б:** perf-тест nova-lsp ждёт номера реестра (раздел 2м);
следующие волны — В7 (hex-литералы), «конструктор записи по позициям» (5 файлов),
затем этап межфайлового чтения (раздел 2н). CI на `5c98048ff` зелёный; последний
пуш — все волны по В8-А включительно.
## 2р. В8 ЗАКРЫТА 2026-08-30 (обе части); передача 2п исполнена

**Часть А (запушена ранее):** узел `Index` в дереве, честный отказ для индексации,
разводка с турбофишем по токену за скобками (неразрушающий взгляд вперёд — инвариант
«take() — единственный движитель» цел).

**Часть Б (эта волна):** литерал массива законен АРГУМЕНТОМ ВЫЗОВА и ПОЛЕМ
КОНСТРУКТОРА. Позиция считается в одной рекурсии обхода (`arg_pos`, как `stmt_pos`
В5); тип решает та же дверь `type_array_lit`, что у инициализатора (ArrayLit вошёл в
`is_expr_kind` — без этого обход аргументов молча пропускал литерал, и подбор не
находил кандидата); эмиттер ПОДНИМАЕТ многооператорную сборку во временную до
инструкции (карта узел→временная в `Emitter.lit_tmp`, печать читает её, дыра —
именованный ice). Границы по замеру: голый хвост и bare-присваивание НЕ взяты (ноль
носителей в переписи).

**Исполнение передачи 2п:** эмиттерная половина переприменена по её же тексту (форма
двери — терминатор с рождения), доказательства повторены (фикстура байт-в-байт,
обратная проба снятым hoist-ом умирает именованным ice и снята ОБРАТНОЙ правкой),
разрез `emit_c.nv` сделан верными якорями с самопроверкой содержимого: ровно пять
функций семьи match, 213 строк в `emit_match.nv` с полной шапкой П27, `emit_c.nv`
825 строк при пределе 1000.

**Честное обновление теста:** `pipeline_test` d5 ждал отказа «only as a binding
initializer» на `println([7, 8])` — позиция теперь законна, и единственный отказ
переехал на печатаемость аргумента («println covers»); утверждение обновлено на
новую правду, отказов по-прежнему требуется ровно один.

**Числа:** корпус 19/12 → **20/13** (база поднята с причиной); самодистанция 41/53 —
без изменений, как и предсказывала карта 2о: носители литерала стоят ЗА другими
отказами своих файлов, волна снимает форму, а не файл.

## 2с. НАХОДКА 2026-08-30: «41/53» был ложью замера, и что её вскрыло

**Цепочка, по шагам.** Свежая перепись В8 показала у `interop.nv` первым отказом ICE
«method call reached type_of with no decision» — мой же ice порядка проходов из шага 2
подплана 274.4. Класс: ветка `Bin` брала метку ПОСЛЕ левого операнда, и отказ в нём
(«unknown field» на поле неврученного типа) протекал в `type_of`. Починено меткой до
обоих операндов (`typing.nv`), interop.nv отвечает честным отказом.

**И тут самодистанция «ухудшилась» 41/53 → 54/54.** Разбор бинарём-до и бинарём-после:
пачечный `novac check` РАНЬШЕ умирал на этом ice с кодом 2, порог раннера «код вне
0/1/2» смерть не ловил, и двенадцать файлов, стоявших в списке ПОЗАДИ interop.nv
(`sem/*`, `source/*`, `tree`, `types`), выходили «чистыми», не будучи досуженными.
Заголовок волны В6 «самодистанция сорвалась с пола» — ложь замера; правда: она
оставалась «все отвергнуты» и остаётся (54/54 с новым `emit_match.nv`).

**Починено той же волной:** (1) метка Bin до операндов; (2) раннер: ICE в выводе пачки
= откат на пофайловый проход, как при смерти процесса; (3) заголовок 2л несёт поправку;
(4) комментарий базы корпуса — тоже.

**Правило, оплаченное этой находкой:** пачечный вердикт достоверен, только если пачка
ДОЖИЛА до конца; «процесс вышел с приемлемым кодом» — не то же самое. Красивое число,
появившееся без названного механизма («почему именно эти 12?»), — повод для разбора, а
не для заголовка: вопрос «какие именно файлы стали чистыми» я не задал, и ложь прожила
сутки.

## 2т. В9 — карта мест (разведка 2026-08-30, sonnet + пробная батарея haiku; ДО кода)

Группа «record constructor only as a binding initializer» разобрана: из 50 меток
настоящих конструкторов 11, и позиции считаны. Пробная батарея по шести формам (оракул
принимает ВСЕ шесть):

| проба | оракул | novac |
|---|---|---|
| конструктор аргументом вызова | ok | «record constructor only...» |
| конструктор правой частью присваивания | ok | «record constructor only...» |
| конструктор хвостом блочного тела | ok | «body ends without a value» |
| конструктор полем конструктора | ok | «record constructor only...» |
| if-let (`if Some(v) = e {}`) | ok | «an `if` in value position...» |
| if-let по методу (`xs.first()`) | ok | «syntax error» |

**Волна В9 берёт две половины.**
1. **Лгущая метка на телах веток (29 из 50):** `{ return 1 }` после `=>` в match и
   тела if/else за деструктурирующим условием парсер читает АНОНИМНЫМ КОНСТРУКТОРОМ
   (ветка `LBrace && !@no_ctor` в @atom) — и отказ говорит про конструктор там, где
   написан блок. Чинится в парсере: позиция тела ветки — блок, не конструктор.
2. **Конструктор аргументом вызова (10 носителей):** `out.push(ArgSpec { ... })` —
   тот же механизм, что у литерала в В8: позиция `arg_pos` уже считается в обходе,
   эмиттер уже умеет hoisting (`emit_record_ctor` возвращает временную) — карта
   узел→временная расширяется с литерала на конструктор.

**Не берём, по замеру:** хвост тела (0 носителей; проба rc3 отказывается ДРУГИМ
правилом — хвостовая машинерия, как у match В5, отдельная волна при появлении
носителей); правая часть присваивания (1 носитель, и тот за индексной формой —
`protos.sigs[si] = ...` ждёт волну индексации); вложенное поле (0 носителей); if-let —
отдельная форма списка волн (носители массовые, судя по 38 меткам на телах).

## 2у. В9-А ЗАКРЫТА 2026-08-30 — тело ветки перестало быть «конструктором»

**Сделано.** Парсер разводит `{` по СОДЕРЖИМОМУ (`@brace_opens_ctor`, неразрушающий
взгляд вперёд — та же форма, что `@after_bracket_run` волны В8): поле `имя:` или спред
открывают конструктор, всё прочее — блок. Тело ветки match теперь читается блоком.

**Достройка по цепочке, каждый шаг — по замеру, а не наугад:** значение-ветка
типизирует тело-блок и отвечает `None` (ветка уходит через `return` и значения матчу не
даёт — статья-вариант умел это с В5); решётка отвечает `no_ty()` для матча, чья первая
ветка — блок (иначе #652-ice о форме, которую никто не писал как значение); эмиттер
печатает хвостовой матч БЕЗ значения в statement-форме — без временной результата и без
`return` того, чего никто не произвёл.

**Чем доказано.** Фикстура `examples/basics/match_block_arm.nv` байт-в-байт с оракулом;
пять прежних фикстур без регрессий; модульные тесты 10/10; корпус 20/13 → **21/14**.
Проба формы-конструктора в той же позиции (`_ => { a: 1 }`) по-прежнему отвергается —
это вторая половина волны (В9-Б), и она не размылась в первую.

**По дороге [LEGACY-#676] в третий раз:** `ak[at].kind_of()` — метод на индексированном
выражении, обойдён связыванием строки, как в соседних дверях.

## 3. Приёмка — машинная, у каждой волны одинаковой формы

1. **Самодистанция падает и это записано числом:** `novac check` по 52 своим файлам до и
   после волны, обе цифры в отчёте. Волна, не сдвинувшая число, не закрыта.
2. **Фикстура на форму** в `examples/basics/` (или в корпусе novac), взятая смоуком
   `novac-e1-smoke.sh` — байт-в-байт с оракулом.
3. **Проба обеими сторонами:** форма до правки отвергается с названной причиной, после —
   принимается; обратная проба (сломать условие) краснит фикстуру.
4. **Ярус `novac push` зелёный**, храповики корпуса и поверхности сдвинуты с причиной в
   том же слиянии.

## 3а. Заметка на Э4: мега-CU позитивных фикстур (вопрос владельца 2026-08-29)

Вопрос: что мешает собирать позитивные фикстуры одним CU, как
`spec_tests/conformance` у оракула? Ответ по факту: половина «проверить» уже пачкой
(страж `check-novac-batch` зовёт novac одним процессом), а половине «запустить» один CU
сегодня противопоказан — каждая фикстура есть целая программа со своим `main`, своим
stdout и кодом возврата, дифф с оракулом сверяет их ПОФАЙЛОВО байт-в-байт, и один отказ
отравил бы весь юнит (каскад, §6). Мега-CU оракула ловит МЕЖФАЙЛОВЫЕ эффекты — коллизии
имён, порядок typedef; у novac это свойство появляется вместе с межмодульной компиляцией.
**Когда Э4 даст модули, «все pos одним CU» становится осмысленным гейтом** — тем же
приёмом, что мега-CU в главном гейте, — и берётся отдельной волной с замером цены.

## 4. Не делаем (границы)

* **Не берём раннер тестов** — это отдельный предмет и отдельная волна.
* **Не берём эффекты.** В6 читает атрибут и игнорирует его; компиляция эффектов входит в
  подмножество отдельной волной и, по заметке в плане 274, СРАЗУ в форме 272/D459.
* **Не переписываем сообщения диагностик оптом.** Правится одно — то, что ЛЖЁТ (В1);
  остальные тексты честны и меняются только вместе со своей формой.
