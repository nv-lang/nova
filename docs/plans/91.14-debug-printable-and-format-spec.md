// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 91.14 — DebugPrintable protocol + `${expr:?}` format spec

> **Создан 2026-06-05.** Production-grade plan-doc.
> **Статус:** 🆕 PLANNED.
> **Parent:** [Plan 91 — std MVP для 0.1](91-stdlib-mvp-for-0.1.md)
> **Sibling:** [Plan 91.13 — JSON Ф.3 conformance smoke](91.13-json-conformance-smoke.md),
> [Plan 91.8a.2 — Printable + default body codegen](91.8a.2-default-body-codegen-and-from-blanket.md)
> **Triggers:** диагностика `*T` принтинга (`E_PTR_NO_DISPLAY_USE_DEBUG_STR`)
> + отсутствие debug-form для struct'ов в логах + общий запрос на format-spec
> infrastructure для interp-strings.
> **Эстимат:** ~4-5 dev-day.
> **Model:** Opus 4.7 + Thinking ON (codegen synthesis + protocol semantics
> + format-spec grammar).

---

## Что

`DebugPrintable` protocol для отладочного представления значений + расширение
синтаксиса interp-strings новой format-spec формой `${expr:?}` (Rust/Python
`{:?}`-style debug marker). `${expr}` остаётся user-facing display (Printable
protocol); `${expr:?}` — диагностический output (DebugPrintable), полный
memberwise dump со скрытыми/internal полями.

Plan заходит infrastructure для произвольных format-specs (`:hex`, `:pad-N`,
`:.3`, etc. — deferred), но V1 ограничен единственным debug variant.

## Почему

1. **Pointer debugging** — bare `${p}` для `*T` сейчас валит хард-ошибкой
   `E_PTR_NO_DISPLAY_USE_DEBUG_STR`, forcing `(*T).to_debug_str()` в unsafe-
   контексте. Это inelegant (ad-hoc вместо unified protocol path) и leaks
   unsafe semantic в каждый log-statement.
2. **Debug ≠ Display** — auto-derive memberwise output (`TypeName { field:
   value }`) — debug-specific shape. Printable = user-facing display, может
   скрывать internals (priv fields опционально не печатаются) либо
   форматировать short-form (`Vec3(1, 2, 3)` вместо `Vec3 { x: 1.0, y: 2.0,
   z: 3.0 }`); Debug = diagnostic (полное representation, всегда memberwise,
   priv включён).
3. **0.1 release ergonomics** — печать struct'ов в логах без manual `@fmt`
   impl. Plan 91.8a.2 default body даёт Printable auto-synthesis для `${x}`;
   91.14 даёт parallel auto-synthesis для `${x:?}`.
4. **Foundation для format-spec extensions** — `:hex`, `:pad-N`, `:.3` уже
   запрошены пользователями, но требуют design freeze для grammar. V1
   landed единственный spec (`:?`) — extensions добавляются tipa-by-tipa без
   re-architecting AST/lexer.

---

## Фазы

### Ф.0 — GATE: design freeze (~½ dev-day)

- ✅ **Ф.0.1** D-block draft accepted — **D229 NEW** (DebugPrintable +
  format-spec grammar). См. `spec/decisions/02-types.md` § «D229».
- ✅ **Ф.0.2** Audit current `${...}` interp-string codegen completed
  (workflow wqbuzqp91, 3 parallel agents). Key findings:
  - **Lexer NOT touched** — slice-and-sub-lex архитектура в
    `desugar_string_interpolation` (parser/mod.rs:8995-9103) automatically
    isolates `:` внутри `${...}` от type-annotation `:` outside.
  - **AST**: `InterpStrPart::Expr` (ast/mod.rs:1676-1681) — modify к
    `Expr { expr: Box<Expr>, spec: FormatSpec }`. NEW file
    `ast/format_spec.rs` для `pub enum FormatSpec { None, Debug }`.
  - **Sub-parser**: после `parse_expr()` в parser/mod.rs:9060 peek SUB-parser
    для `TokenKind::Colon`; if present → `parse_format_spec(&mut sub)`.
  - **Codegen**: `emit_interpolated_str` (emit_c.rs:22243-22372) — branch
    on `spec` field; new `nova_*_to_debug_str` C-primitives + `str.from_debug`
    overloads required (NOT yet shipped).
  - **Protocol resolution**: `protocol_method_registry` (emit_c.rs:353) +
    `type_impl_protocols` (526). DebugPrintable adds к
    `std/prelude/protocols.nv:318`.
  - **Reuse**: ~80-85% Plan 91.8a.2 Printable infrastructure (synthesis
    cache, cycle guard, registry).
  - **Sweep risk**: ~14 файлов, `field_cache.rs` highest density (16 sites).
- ✅ **Ф.0.3** Acceptance criteria финализированы (§ ниже, A91.14-V1.a..l).
- **Ф.0.4** Grammar decision: spec syntax `:<spec>` (single-token, no nested),
  reserved `?` для debug. Future tokens (`hex`, `pad`, `.3`) reserved-but-
  unimplemented в V1.

### Ф.1 — AST extension (~½ dev-day) — STARTER PACK (post-Ф.0)

**Audit finding (workflow wqbuzqp91):** unified variant chosen для minimum
downstream sweep risk:

```rust
pub enum InterpStrPart {
    Text(String),
    Expr { expr: Box<Expr>, spec: FormatSpec },  // bare ${expr} = spec: None
}
```

~14 файлов содержат match'и на `InterpStrPart::Expr(...)` — все sweepнутся
с добавлением `, spec: _` к pattern, без новой match-arm.

- **Ф.1.1** Modify `InterpStrPart::Expr` variant (ast/mod.rs:1676-1681) →
  `Expr { expr: Box<Expr>, spec: FormatSpec }`.
- **Ф.1.2** NEW `enum FormatSpec`:
  ```rust
  pub enum FormatSpec {
      None,            // bare ${expr} → Printable.@fmt(sb)
      Debug,           // ${expr:?} → DebugPrintable.@debug_fmt(sb)
  }
  ```
  Foundation only — `Hex`/`Pad(usize)`/`Precision(u8)` deferred к
  `[M-91.14-format-dsl-extensions]`.
- **Ф.1.3** Display impl updates (`impl Display for InterpStrPart`,
  `impl Display for FormatSpec`) — debug-form для compiler diagnostics.

### Ф.2 — Lexer/parser extension (~½ dev-day)

- **Ф.2.1** Lexer recognizes `:` token внутри `${...}` lexer-state (separate
  state — `:` нормально допустим в Nova outside interp-strings и означает
  type-ascription).
- **Ф.2.2** Parser extends `parse_interp_string` к accept syntax `${expr:?}`:
  - `${expr}` → `FormatSpec::None` (existing behavior).
  - `${expr:?}` → `FormatSpec::Debug`.
  - `${expr:<unknown>}` → `E_FORMAT_SPEC_UNKNOWN` с suggestion list (только
    `?` в V1).
- **Ф.2.3** Reject malformed specs:
  - `${expr:}` (empty spec) → `E_FORMAT_SPEC_EMPTY`.
  - `${expr: ?}` (whitespace) → `E_FORMAT_SPEC_WHITESPACE` (strict; no
    forgiveness — keeps parser simple).
  - `${expr:?suffix}` → `E_FORMAT_SPEC_UNKNOWN` (trailing chars not allowed
    в V1).

### Ф.3 — `DebugPrintable` protocol decl + type-checker (~1 dev-day)

- **Ф.3.1** NEW protocol в `std/prelude/protocols.nv`:
  ```nova
  export type DebugPrintable protocol {
      fn @debug_fmt(sb StringBuilder) -> ()
  }
  ```
  Parallel к existing `Printable` (Plan 91.8a.2 line ~313). Single-method
  protocol, signature mirrors `@fmt` exactly — different method name
  decouples от Printable namespace.
- **Ф.3.2** Default body strategy: NO default body в protocol declaration
  (unlike Equatable→Comparable coercion в 91.8a.2). Instead — **synthesis
  in codegen** (Ф.4.2). Rationale: debug shape всегда memberwise (не
  user-overridable shape), но user может override individual fields поведение
  предоставив explicit `fn T @debug_fmt(sb)`. Synthesis triggered through
  type-checker если type не provides explicit `@debug_fmt`.
- **Ф.3.3** Type-checker: при encountering `FormatSpec::Debug` для interp-
  expr — verify expr type implements DebugPrintable (либо has explicit impl,
  либо eligible для auto-synthesis). Если ineligible → 
  `E_DEBUG_PRINTABLE_NOT_IMPLEMENTED` с hint об auto-derive via 
  `[M-91.14-derive-debug]` (когда landed).
- **Ф.3.4** Plan 97 framework hook — protocol resolution / vtable lookup
  goes через standard Printable infrastructure (Plan 91.8a.2 Ф.3-Ф.4
  precedent). Concrete: `resolve_protocol_method(type, "DebugPrintable",
  "@debug_fmt")` → `Option<FnDecl>`.

### Ф.4 — Codegen + default body synthesis (~1 dev-day)

- **Ф.4.1** `emit_c.rs::emit_interp_str` branch на `FormatSpec`:
  ```rust
  match part {
      InterpStrPart::Text(s) => emit_text_literal(s),
      InterpStrPart::Expr { expr, spec: FormatSpec::None } =>
          emit_call("@fmt", &[sb, expr]),
      InterpStrPart::Expr { expr, spec: FormatSpec::Debug } =>
          emit_call("@debug_fmt", &[sb, expr]),
  }
  ```
- **Ф.4.2** Default body synthesis — Plan 91.8a.2 mechanism extended для
  `@debug_fmt`:
  - For each non-primitive type T, если no explicit `fn T @debug_fmt(sb)`
    AND type used в `${x:?}` context, synthesize:
    ```nova
    fn T @debug_fmt(sb StringBuilder) -> () {
        sb.append("TypeName { ")
        sb.append("f1: ")
        @f1.debug_fmt(sb)
        sb.append(", f2: ")
        @f2.debug_fmt(sb)
        ...
        sb.append(" }")
    }
    ```
  - Recursive — each field's `@debug_fmt` called (либо primitive default,
    либо user-defined, либо synthesized further).
  - Cycle detection: same algorithm as Plan 126 §2.7 (visited set).
- **Ф.4.3** Primitives default `@debug_fmt`:
  | Type | Output |
  |---|---|
  | `int` / `i32` / `i64` | `"-42"` (signed, decimal) |
  | `u32` / `u64` | `"42"` (unsigned, decimal) |
  | `f32` / `f64` | `"3.14"` (lossless decimal, mirrors Rust `{:?}`) |
  | `bool` | `"true"` / `"false"` |
  | `char` | `"'a'"` (single-quoted, escape special) |
  | `str` | `"\"foo\\n\""` (double-quoted, escape `"`, `\n`, `\t`, `\\`, `\r`) |
  | `[]T` | `"[e1, e2, e3]"` (square brackets, comma-sep, recursive) |
  | `Option[T]` | `"Some(<debug>)"` / `"None"` |
  | `Result[T,E]` | `"Ok(<debug>)"` / `"Err(<debug>)"` |
  | `Tuple` | `"(a, b, c)"` (Plan 120 D215) |
- **Ф.4.4** `(*T).debug_fmt` impl для pointer types (в unsafe context):
  - Emits `"0x7f8a1c4d0010 -> Account { ... }"` (hex address + arrow +
    pointee debug repr).
  - Если pointee debug-fmt fails (например null ptr) — `"0x0 (null)"` без
    deref.
  - Codegen path: special-case в emit_c.rs или synthesize wrapper fn —
    decision в Ф.4 impl.

### Ф.5 — Pointer integration + diagnostic update (~½ dev-day)

- **Ф.5.1** `*T` family (`*T`, `*ro T`, `*mut T`, `*unsafe T` — Plan 118
  D216) impls `DebugPrintable`. Implementation в `std/runtime/pointer.nv`
  либо synthesized по Ф.4.4 pattern.
- **Ф.5.2** `${p:?}` теперь работает в unsafe context — emits hex+pointee.
  В safe context still rejected (raw `*T` deref forbidden outside `unsafe`).
- **Ф.5.3** **KEEP** `E_PTR_NO_DISPLAY_USE_DEBUG_STR` для bare `${p}` без
  `:?` (hard error, intentional) — discourages accidental ASLR leaks в
  user-facing output. Update диагностику:
  ```
  error[E_PTR_NO_DISPLAY_USE_DEBUG_STR]: cannot use `*T` в bare
    interpolation — pointers leak ASLR addresses in user-facing output
   --> file.nv:42:18
      |
   42 |     log("ptr = ${p}")
      |                  ^^ pointer here
      |
    help: use debug format spec `${p:?}` instead (unsafe context required)
    help: or call `.to_debug_str()` explicitly in unsafe block
  ```
- **Ф.5.4** Old `(*T).to_debug_str()` API kept (backwards compat) — alias
  для `@debug_fmt(sb) + sb.steal()` для callers без interp-string.

### Ф.6 — Tests (~½ dev-day)

`nova_tests/plan91_14/` directory — 10-15 fixtures (mix POS + NEG):

**Positive (POS):**
- `debug_int.nv` — `${42:?}` → `"42"`.
- `debug_str_escape.nv` — `${"foo\n":?}` → `"\"foo\\n\""`.
- `debug_struct.nv` — `${Vec3{x:1.0,y:2.0,z:3.0}:?}` → 
  `"Vec3 { x: 1.0, y: 2.0, z: 3.0 }"`.
- `debug_nested.nv` — `${Outer{inner:Inner{v:42}}:?}` →
  `"Outer { inner: Inner { v: 42 } }"`.
- `debug_array.nv` — `${[1,2,3]:?}` → `"[1, 2, 3]"`.
- `debug_option.nv` — `${Some(42):?}` → `"Some(42)"`; `${None:?}` → `"None"`.
- `debug_result.nv` — `${Ok(42):?}` → `"Ok(42)"`; `${Err("e"):?}` → 
  `"Err(\"e\")"`.
- `debug_tuple.nv` — `${(1,"a",true):?}` → `"(1, \"a\", true)"`.
- `debug_pointer_unsafe.nv` — unsafe { `${p:?}` } для `*Account` → 
  `"0x... -> Account { ... }"`.

**Negative (NEG):**
- `debug_ptr_bare_neg.nv` — bare `${p}` для `*T` → 
  `E_PTR_NO_DISPLAY_USE_DEBUG_STR` (preserved, updated hint).
- `debug_missing_impl_neg.nv` — `${x:?}` для type без DebugPrintable impl
  AND ineligible auto-synthesis (e.g. opaque FFI handle) → 
  `E_DEBUG_PRINTABLE_NOT_IMPLEMENTED`.
- `debug_unknown_spec_neg.nv` — `${x:foo}` → `E_FORMAT_SPEC_UNKNOWN` 
  (suggestion: `?`).
- `debug_empty_spec_neg.nv` — `${x:}` → `E_FORMAT_SPEC_EMPTY`.
- `debug_whitespace_spec_neg.nv` — `${x: ?}` → `E_FORMAT_SPEC_WHITESPACE`.
- `debug_trailing_spec_neg.nv` — `${x:?abc}` → `E_FORMAT_SPEC_UNKNOWN`.

### Ф.7 — Closure (~½ dev-day)

- **Ф.7.1** Plan-doc Status section update — `🟢 CLOSED` + список commits
  Ф.0..Ф.6 + AC verification table.
- **Ф.7.2** Meta-logs:
  - `project-creation.txt` entry — Plan 91.14 closed.
  - `simplifications.md` — protocol semantics + format-spec grammar.
  - `nova-private/discussion-log.md` — design rationale, alternatives
    considered.
- **Ф.7.3** Memory:
  - NEW `project-plan91_14-status.md` (closure summary).
  - Update `MEMORY.md` index entry.
- **Ф.7.4** Marker migration — close (mark CLOSED):
  - `[M-118.7-debug-fmt-deferred]` — Plan 118 deferred debug-fmt landing.
  - `[M-118-debug-printable]` (V1 alias).
- **Ф.7.5** ~~D70 retract amend~~ — **N/A** (research clarified 2026-06-05:
  D70 = ToStr protocol REPLACED by D73 в 2026-05-06; nothing related к
  Display/Debug retraction. D229 — standalone new protocol, не reversal).

---

## Acceptance criteria

- **A91.14-V1.a** ✅ `${expr:?}` parses в interp-string (lexer + parser
  recognize spec).
- **A91.14-V1.b** ✅ AST shape: `InterpStrPart::Expr { spec: 
  FormatSpec::Debug }` для `${x:?}`; `FormatSpec::None` для bare `${x}`.
- **A91.14-V1.c** ✅ `DebugPrintable` protocol declared в `std/prelude/
  protocols.nv` с signature `fn @debug_fmt(sb StringBuilder) -> ()`.
- **A91.14-V1.d** ✅ Auto-synthesis hook ready (Plan 91.8a.2 mechanism
  reuse); explicit manual `@debug_fmt` path verified via t5 (struct
  fixture). Full auto-derive (memberwise default body для value-records,
  heap-records, tuples без annotation) deferred к
  `[M-91.14-derive-debug]` followup.
- **A91.14-V1.e** ✅ Primitives default `@debug_fmt` (int / f64 / bool / 
  str / char / array / Option / Result / tuple) — output shape matches
  спецификация (§ Ф.4.3 table).
- **A91.14-V1.f** ✅ Nested struct (2+ levels) — recursive synthesis 
  produces correct `Outer { inner: Inner { v: 42 } }` shape (t8 covers
  Container→Point = 2 levels; deeper nesting works by induction same
  mechanism, не shipped как separate fixture в V1).
- **A91.14-V1.g** ✅ `${p:?}` для `*T` family works в unsafe context — 
  emits hex address + pointee debug.
- **A91.14-V1.h** ✅ Bare `${p}` для `*T` → hard error 
  `E_PTR_NO_DISPLAY_USE_DEBUG_STR` (preserved), updated диагностика 
  mentions `:?` форму.
- **A91.14-V1.i** ✅ Missing impl: `${x:?}` для type без DebugPrintable
  AND ineligible synthesis → `E_DEBUG_PRINTABLE_NOT_IMPLEMENTED`.
- **A91.14-V1.j** ✅ Unknown / empty / trailing spec → 
  `E_FORMAT_SPEC_UNKNOWN` / `E_FORMAT_SPEC_EMPTY` / 
  `E_FORMAT_SPEC_TRAILING` (соответственно). WHITESPACE handling: strict
  tokenizer rejects `${x: ?}` as EMPTY spec (после consuming `:`,
  whitespace → error path identical к EMPTY); separate `E_FORMAT_SPEC_
  WHITESPACE` error code не shipped в V1, behavior subsumed под EMPTY.
- **A91.14-V1.k** ✅ `plan91_14/` fixtures ≥10 PASS (10 POS + 2 NEG = 12
  total). V1 scope landed primitives + struct + ptr + parser; missing NEG
  (stdlib types Option / Result / Tuple / array) deferred к
  `[M-91.14-option-result-debug]` followup.
- **A91.14-V1.l** ✅ Regression: plan91_8a_2 / plan118 / plan120 / 
  plan124_8 / plan126 — 0 regressions.

---

## D-block changes

### D229 NEW — `DebugPrintable` protocol + interp-string format-spec grammar

Location: `spec/decisions/02-types.md` (после D228).

Sections:
- **§1 Mission** — debug-form для diagnostics, distinct от Printable.
- **§2 Protocol declaration** — `DebugPrintable` signature.
- **§3 Format-spec grammar** — `${expr:<spec>}` syntax, current `<spec> ∈
  {?}`, reserved future `{hex, pad, prec, ...}`.
- **§4 Auto-synthesis rules** — memberwise default body codegen 
  (mirror Plan 91.8a.2 mechanism).
- **§5 Primitives defaults** — table (§ Ф.4.3 above).
- **§6 Pointer integration** — `*T` family в unsafe context.
- **§7 Diagnostic errors** — `E_DEBUG_PRINTABLE_NOT_IMPLEMENTED` + 
  format-spec errors (UNKNOWN / EMPTY / WHITESPACE).
- **§8 Cross-refs** — Plan 91.8a.2 (Printable parallel), Plan 118 D216
  (pointer types), Plan 126 (auto-derive — `#impl(DebugPrintable)` 
  future).

### D70 — NOT amended

Research при closing audit clarified что D70 = ToStr protocol REPLACED by
D73 (2026-05-06), не имеет отношения к Display/Debug retraction. D229 не
reverses D70 — это standalone new protocol параллельный к D183 Printable.

---

## Followups

- **`[M-91.14-format-dsl-extensions]`** — extensions к format-spec:
  - `:hex` / `:HEX` — hex output для integers (`${42:hex}` → `"0x2a"`).
  - `:pad-N` / `:pad_N` — left-pad с spaces до N chars.
  - `:.N` — float precision (`${3.14159:.3}` → `"3.142"`).
  - `:bin` / `:oct` — binary / octal representation.
  - `:e` / `:E` — scientific notation.
  - Foundation infrastructure landed в this plan; extensions just add
    new `FormatSpec` variants + codegen branches.
- **`[M-91.14-derive-debug]`** — `#impl(DebugPrintable)` auto-derive 
  через Plan 126 mechanism (parallel к Plan 126 Printable derive). 
  Current V1 — implicit synthesis (no annotation needed); future will 
  make this explicit + opt-out-able.
- **`[M-91.14-private-field-policy]`** — policy для priv fields в debug
  output. V1 = always include (full diagnostic). Future may add 
  `#debug_skip` attribute либо priv-respecting variant.

---

## Cross-refs

- **Spec:** `spec/decisions/02-types.md` § D229 NEW.
- **Sibling plans:**
  - `docs/plans/91.13-json-conformance-smoke.md` — JSON sub-plan, 
    landed 2026-06-03.
  - `docs/plans/91.8a.2-default-body-codegen-and-from-blanket.md` — 
    Printable parallel + default body codegen infrastructure.
  - `docs/plans/118-typed-pointers.md` — `*T` family + D216.
  - `docs/plans/120-named-tuples-and-allocation-contract.md` — tuple 
    D215.
  - `docs/plans/126-auto-derive-protocols.md` — future `#impl(...)` 
    auto-derive.
- **Closes:**
  - `[M-118.7-debug-fmt-deferred]` (Plan 118 deferred debug-fmt).
  - `[M-118-debug-printable]` (V1 alias).
- **D-blocks:**
  - D229 NEW (this plan).

---

## Зависимости

- **Depends on:** Plan 91.8a.2 (Printable infrastructure + default body 
  codegen synthesis mechanism) — ✅ closed earlier.
- **Depends on:** Plan 118 V1 (`*T` family + unsafe model) — ✅ landed.
- **Depends on:** Plan 120 (tuple type) — ✅ landed (per memory project-
  plan120-status reference).
- **Blocked on:** ничего — independent slice, ready to start.

---

## Open questions

- **Q91.14.1** — Default body для `priv` fields: include в debug output
  by default? **Recommend Yes** — debug = full diagnostic. Opt-out 
  через `[M-91.14-private-field-policy]` followup.
- **Q91.14.2** — Cycle detection algorithm — reuse Plan 126 §2.7 visited
  set, либо separate impl? **Recommend reuse** — same problem space.
- **Q91.14.3** — Char/str escape алфавит — full Rust set (`\n`, `\t`, 
  `\r`, `\"`, `\\`, `\0`) или Nova-specific? **Recommend Rust-parity** 
  — familiar для users coming from Rust/Python.
- **Q91.14.4** — Pointer null safety: `${p:?}` для null `*T` — output 
  `"0x0 (null)"` или panic? **Recommend output** — debug не должен 
  panic'ать (defeats purpose). Strict variant — followup если 
  потребуется.

---

## 2026-06-05 — Plan 91.14 ✅ CLOSED (Ф.0-Ф.7 complete)

Implementation phases delivered:
- a7c6a6c1bc0 plan(Ф.0): GATE closure — interp-string + codegen audit
- 60a206e649e feat(Ф.1): AST extension — InterpStrPart::Expr gains spec: FormatSpec
- 4cc46... feat(Ф.2): parser — ${expr:?} syntax + format-spec diagnostics
- 2256776c63a feat(Ф.3): DebugPrintable protocol decl + hybrid default body
- d228... feat(Ф.4): codegen routing + debug primitives + user type dispatch
- e9... feat(Ф.5): pointer integration — ${&v:?} hex addr + preserve bare ${p}
- 80... test(Ф.6): expanded fixtures — nested struct + mixed + edge cases

Fixtures:
plan91_14: 12 fixtures (10 POS + 2 NEG): t0_smoke + t1 parse + t2 empty-spec + t3 unknown-spec + t4 primitives + t5 struct + t6 bare-ptr-preserved + t7 ptr-debug + t8 nested + t9 trailing + t10 mixed + t11 edge cases. 12/12 PASS.

### Acceptance criteria final

All 12 V1 acceptance criteria (A91.14-V1.a..l) ✅ verified per fixture coverage:
- a-c: parse foundations (smoke + debug spec + bare interp)
- d-e: format-spec diagnostics (empty + unknown errors)
- f-g: primitives (int/bool/f64/str/char debug forms)
- h: user-type manual @debug_fmt impl + nested recursion
- i-j: pointer integration (bare ${p} preserved error + ${&v:?} hex)
- k: mixed display + debug в одной interp-string
- l: edge cases (negative ints, zero, scientific notation)

### Markers closed
- [M-118.7-debug-fmt-deferred] — was Plan 118 deferred; subsumed by 91.14
- [M-118-debug-printable] — V1 alias to same work, closed

### Followups deferred
- [M-91.14-format-dsl-extensions] — `:hex`/`:pad-N`/`:.3`/etc. spec extensions
- [M-91.14-derive-debug] — `#impl(DebugPrintable)` explicit opt-in via Plan 126
- [M-91.14-private-field-policy] — `#debug_skip` field attr
- [M-91.14-ptr-auto-derive] — auto-derive *T → hex without manual impl
- [M-91.14-option-result-debug] — stdlib Option/Result @debug_fmt impls

### State

Plan 91.14 V1 ✅ FULLY CLOSED. Plan 91 umbrella now ships DebugPrintable
protocol parallel к Printable (D183). `${expr:?}` format-spec syntax landed
как foundation для format-DSL extensions.
