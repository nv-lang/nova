// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 91.13 — JSON Ф.3 conformance smoke (partial)

> **Статус:** 🟡 PARTIAL CLOSED 2026-06-03
> **Parent:** [Plan 91 — std MVP для 0.1](91-stdlib-mvp-for-0.1.md)
> **Sibling:** [Plan 48.1 — cross-module generic mono](48.1-cross-module-generic-template-registration.md)
> **Triggers:** Plan 91 Ф.3 deferred — JSON `encode/decode` roundtrip
> conformance. Закрытие требует pipeline-readiness: Plan 48.1 разблокировал
> cross-module `Option[HashMap[K,V]]`; Plan 91.13 закрывает следующий
> stdlib API gap — `str.from_codepoint(int) -> str`.

## Зачем

Plan 91 (std MVP для 0.1 release) держит `Ф.3 JSON conformance` как
deferred. JSON `Json.parse` / `JsonValue.@into()` — критический shipping
API; conformance test (roundtrip `parse → encode == original`) подтверждает
готовность к 0.1.

При попытке написать conformance test обнаружено что **`std/encoding/json.nv`
использует `str.from_codepoint(code)` для Unicode escape parsing (`\uXXXX`),
но функция НЕ объявлена в stdlib**. Compiler выдавал auto-generated
`Nova_str_static_from_codepoint(...)` call но C runtime не имел этого
символа → linker errors при попытке полной conformance проверки.

## Что вошло (V1 partial — этот PR)

### Ф.1 — `str.from_codepoint(int) -> str` API

**Зачем:** D54 (Plan 34 Ф.5.2) запретил `int as char` direct cast. Workaround
— `char.try_from(n)?` — adds Fail effect indirection. Иногда caller уже
validated codepoint range (JSON parser проверяет hex digits `⊆ [0,
0x10FFFF]`) — нужен **прямой "known-valid" path** без Fail unwrap.

**Реализация:**
- `std/runtime/char.nv`: `export external fn str.from_codepoint(cp int) -> str`
- `compiler-codegen/src/codegen/runtime_registry.rs`: добавлен RuntimeFn
  entry (rooted в `std.runtime.char`)
- `compiler-codegen/nova_rt/string_builder.h`: inline alias
  `Nova_str_static_from_codepoint(cp) → Nova_str_static_from_char(cp)`.
  Тот же UTF-8 encode impl (Plan 04, ~80 строк up в file).
- Semantics: invalid codepoint (`< 0` или `> 0x10FFFF`) → empty `""`
  (silent — predictable degradation, не panic). Strict variant — followup
  если потребуется.

### Acceptance criteria (V1 — этот PR)

- [x] `str.from_codepoint(65) == "A"` (ASCII)
- [x] `str.from_codepoint(0x041F) == "П"` (BMP — Cyrillic, 2-byte UTF-8)
- [x] `str.from_codepoint(0x1F600) == "😀"` (supplementary — emoji,
      4-byte UTF-8)
- [x] `str.from_codepoint(0) == "\\x00"` (NUL — 1-byte, len = 1)
- [x] `str.from_codepoint(-1) == ""` (negative — invalid)
- [x] `str.from_codepoint(0x110000) == ""` (out-of-range — invalid)
- [x] Targeted regression: plan48_1 / plan90_1 / plan73 / plan77 /
      plan91_8a_2 — 0 regressions

### Tests

`nova_tests/plan91_13/`:
- `from_codepoint_test.nv` — 4 positive cases (ASCII / BMP / supplementary / NUL)
- `from_codepoint_invalid_neg.nv` — 2 negative cases (negative cp, out-of-range)

## Что НЕ вошло (V2 followup'ы)

### Ф.2 — divergence-aware if-expr type inference

**Status (2026-06-05):** ✅ **MIGRATED → [Plan 125](125-divergence-aware-inference.md)**.

Вопрос оказался **шире чем codegen workaround** — это implementation
gap для `Ty::Never` bottom-type contract из Plan 76 + spec D25. Вынесен
в отдельный top-level plan для производственно-grade обработки:

- 7 фаз (Ф.0 baseline + Ф.1-Ф.4 codegen whitelist + Ф.5 type-checker
  side опционально + Ф.6 closure)
- 12 acceptance criteria
- 8 risks с mitigations + 6 prior-attempt lessons (полный разбор
  24-регрессий)
- ≥15 positive + ≥8 negative fixtures
- Spec amendments к D25 (04-effects.md) + 08-runtime.md §«never»

**Followup `[M-91.13-if-expr-divergence-aware-inference]` ✅ CLOSED**
(migrated to Plan 125, 2026-06-05).

### Ф.3 — full JSON roundtrip conformance suite (BLOCKED on Ф.2)

**Что:** `nova_tests/plan91_13/json_roundtrip_*.nv`:
- primitives (null/bool/num/str) — encode → decode = original
- arrays — `[1, "a", true]` parse + stringify
- objects — `{"k": v}` parse + stringify (requires Plan 48.1 ✓)
- nested — `{"arr": [1, {"k": "v"}]}` deep recursion
- edge cases — escapes (`\"`, `\n`, `\t`, `\\`), unicode (`\uXXXX`),
  whitespace, malformed input

**Followup:** `[M-91.13-json-roundtrip-full]` после Ф.2.

## Acceptance criteria (Plan 91.13 polны — V2)

- [ ] Ф.2 divergence-aware if-expr fix (PARTIAL — see followup)
- [ ] `json.nv` compiles в test context без CC-FAIL
- [ ] `json_roundtrip_primitives` PASS (4/0)
- [ ] `json_roundtrip_arrays` PASS
- [ ] `json_roundtrip_objects` PASS (cross-module Option[HashMap] —
      unblocked by Plan 48.1)
- [ ] `json_roundtrip_nested` PASS
- [ ] `json_edge_cases_*` PASS (escapes / unicode / whitespace)
- [ ] Plan 91 Ф.3 marked CLOSED в parent doc

## Зависимости

- **Depends on:** Plan 48.1 (cross-module generic mono — Option[HashMap]
  signature/typedef ordering) — closed 2026-06-03
- **Depends on:** Plan 04 (str/StringBuilder external typedef) — `Nova_str_
  static_from_char` runtime helper — closed earlier
- **Blocked on:** [Plan 125 — Divergence-aware result-type inference](125-divergence-aware-inference.md)
  (migrated from `[M-91.13-if-expr-divergence-aware-inference]` 2026-06-05)
  — для полного Plan 91.13 closure

## Spec impact

`std/runtime/char.nv` — public API extension (additive, breaking-free).
No new D-block (consistent с existing `str.from(char)` pattern — same
runtime helper, alternative entry point bypassing D54 char-cast ban).

## Followups

- ✅ `[M-91.13-if-expr-divergence-aware-inference]` **CLOSED 2026-06-05**
  — migrated to [Plan 125](125-divergence-aware-inference.md). Scope
  was broader than codegen workaround — implementation gap для `Ty::Never`
  bottom-type contract.
- `[M-91.13-json-roundtrip-full]` — full JSON conformance suite,
  blocked on Plan 125 closure.
- `[M-91.13-strict-from_codepoint]` — strict variant
  `str.try_from_codepoint(cp int) -> Result[str, InvalidCodepoint]`
  для callers что хотят explicit error на invalid input. Optional —
  current silent-empty acceptable для known-valid contexts (JSON).
