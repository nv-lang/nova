# Plan 52.3: Remaining HashMap-литерал features

> **Создан 2026-05-16.** Закрытие оставшихся M-52-* маркеров из
> simplifications.md.
>
> **СТАТУС:** ✅ ЗАКРЫТ (2026-05-22). Ф.1 (empty `[]`) сделана ранее;
> Ф.2 (mono-pass quirk), Ф.3 (spread), Ф.4 (type-inference no-annotation)
> — закрыты Plan 55.4/55.5/56 (mono corruption + name-collision сняты).
> All-spread `[...a, ...b]` без аннотации добивался отдельным фиксом
> аннотатора (вывод map-типа из spread-источников, 2026-05-22).
> Регрессия: `nova_tests/map_literals/positive_spread.nv` +
> `positive_clone_merge.nv`.
>
> **Реализует:**
> - M-52-empty-array-in-map-position: `let m HashMap[K,V] = []` codegen-fail
> - M-52-spread-not-supported: spread `[...defaults, k: v]` (после mono-pass fix)
> - Investigation: mono-pass quirk для generic helper-methods с pattern-match
> - M-52-type-inference-no-annotation: investigate root cause
>
> **Зависит от:** Plan 52, 52.1, 52.2 (Ф.1 empty {} fix, Ф.2 const map).
>
> **Приоритет:** P2 — финальное закрытие Plan 52 limitations.

---

## Что осталось

### Ф.1 — Empty array literal в map-position (`let m HashMap[K,V] = []`)

**Описание:** Codegen treats `[]` как пустой array, не пустую мапу.
Workaround: `HashMap[K,V].new()`.

**Подход:**
1. В `emit_array_lit` (или предшествующем pass) проверить
   `expected_record_type` или contextual hint — если expected это
   HashMap (или any #from_pairs тип), эмитить как `Target.new()`.
2. Альтернатива: в desugar.rs обнаружить `let x T = []` где T помечен
   #from_pairs и заменить на `T.new()` сразу.

### Ф.2 — Mono-pass quirk для generic helper-methods

**Investigation needed:** `HashMap.@clone()` / `@merge_from()` с
pattern-match в while-body падают с **разными** ошибками в mono pass
(`if condition must be bool` etc).

Шаги:
1. Reproducer внутри test (не через stdlib)
2. Найти где mono pass конкретизирует pattern-match path  
3. Понять что именно генерирует bad condition
4. Fix

После закрытия — реализовать Ф.3 spread.

### Ф.3 — Spread `[...defaults, k: v]`

После Ф.2 (clone/merge_from работают):

1. AST: `MapLitElem::Pair(Expr, Expr)` | `MapLitElem::Spread(Expr)`
   (новый enum, миграция из текущего Vec<(Expr,Expr)>)
2. Parser: распознать `...expr` в map-литерале
3. Type-check: validate что spread-expr типизирован совместимо
4. Desugar: `[...m, k: v]` → `{ let _t = m.@clone(); _t.@insert(k, v); _t }`

### Ф.4 — Type inference no annotation edge cases — DEFERRED

**Описание:** `let m = ["a": 1]` без аннотации фейлится в test-блоках
(M-52-type-inference-no-annotation).

**Investigation:** trace показал `inferred_target_type = Some(["HashMap"])`
**уже устанавливается** annotate-pass'ом. Но C-output:
```c
nova_int _m0 = Nova_WriteBuffer_static_with_capacity(((nova_int)2LL));
```
То есть mono-pass выбирает `WriteBuffer.with_capacity` вместо
`HashMap.with_capacity` несмотря на turbofish hint в desugar.

Это **mono-pass name-collision** между нескольким types которые
имеют `with_capacity` (HashMap, StringBuilder, WriteBuffer, ReadBuffer).
Without proper type-arg resolution, mono выбирает first overload.

Fix через `inferred_target_type` works для **explicit annotation**
случаев (там expected_is_from_pairs trigger), но без annotation —
fallback не помогает (mono всё равно ambig).

**Зависит от:** Plan 48 mono-pass priority — explicit-turbofish call
должен **выигрывать** над erased name-resolve. Это та же категория
issues что Ф.2/Ф.3.

Workaround: всегда писать explicit annotation `let m HashMap[K,V] = [...]`.

---

## Порядок выполнения

1. **Ф.1 empty `[]` in map-position** — компактная, незав-я от других
2. **Ф.2 mono-pass quirk investigation** — нужно для Ф.3
3. **Ф.3 spread** — после Ф.2
4. **Ф.4 type inference edge cases** — independent

## Acceptance

- [x] `let m HashMap[K,V] = []` работает (Ф.1 ✅)
- [ ] HashMap.@clone() / @merge_from() — Ф.2 deferred
      (mono-pass corruption, требует Plan 48 followup)
- [ ] Spread `[...base, k:v]` — Ф.3 deferred (зависит от Ф.2)
- [ ] Type inference no-annotation — Ф.4 deferred
      (mono name-collision, требует priority-based resolve)
- [x] Без регрессий vs baseline (522 PASS / 25 FAIL preserved)
- [x] Каждая фаза — отдельный commit
- [x] M-52-empty-array-in-map-position закрыт (Ф.1)

## Status summary

| Ф | Result | LOC delta |
|---|---|---|
| Ф.1 empty `[]` | ✅ ЗАКРЫТО | +17 (annotate) +27 test |
| Ф.2 mono-pass quirk | ⛔ DEFERRED | investigation only |
| Ф.3 spread | ⛔ DEFERRED (depends on Ф.2) | — |
| Ф.4 inference | ⛔ DEFERRED (mono name-collision) | — |

Все 3 deferred фазы упираются в **mono-pass infrastructure**, что
вне scope Plan 52.x. Создан material для Plan 48 followup.
