<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 231 — Выход из цикла точечных фиксов (систематизация качества компилятора)

**Статус:** ✅ УТВЕРЖДЁН владельцем (2026-07-26, «ДА» — порядок: аудит+матрица немедленно, IR — дизайн-нота).
**Связи:** 196 (единый канал — родитель трека В), 221.1 (реестр — симптоматика), test-conventions.md.

> **231 — ЦЕНТРАЛЬНЫЙ ПЛАН ПО АВТОПРОВЕРКАМ** (решение владельца 2026-07-27: «центральный план
> по автопроверкам будет 231, все автопроверки там должны быть и статус по ним»). Любой новый
> механизм автопроверки — линт, страж, хук, гейт-шаг — заносится в реестр §0а НИЖЕ, в том же
> слиянии, что и сам механизм. Реестр — единственный источник правды о том, что у нас
> автоматически проверяется и в каком это состоянии.

## 0а. РЕЕСТР АВТОПРОВЕРОК (единый источник правды)

**Правило владельца (2026-07-27, дословно):** «в скрипте нет толку, если не подключён к
автопроверке или сам содержит ошибки». Отсюда три обязательных свойства КАЖДОГО механизма:
**Док** — содержательная шапка (зачем / что проверяет / как запускать) + ссылка на план;
**Подкл** — вызывается гейтом/хуком, а не лежит мёртвым файлом; **Тест** — самотест, доказывающий
оба свойства: ловит нарушение / не даёт ложняка. Соблюдение первых трёх колонок проверяется
машиной — `scripts/check-guard-wiring.sh` (мета-страж; сам покрыт самотестом).

**Установка/переустановка всего — одной командой:** `scripts/install-guards.sh`
(`--check` — только проверить). Ставит то, что живёт в НАСТРОЙКАХ и не переживает чистый клон:
`core.hooksPath` во всех пяти репах, права на исполнение, объявление хуков агентов; в конце
прогоняет мета-страж и все самотесты — установка засчитывается, только если механизмы РАБОТАЮТ.

| Механизм | Что проверяет | Док | Подкл | Тест |
|---|---|---|---|---|
| `scripts/gate.sh` | авторитетный гейт: ratchet → стражи → самотесты → сборка → мега-CU → check std → флагман | ✅ | ✅ сам | ❌ трек Ж |
| `scripts/arch-ratchet.sh` | рост легаси-кодогена `emit_c.rs` только с письменным обоснованием в baseline | ✅ | ✅ gate | ❌ трек Ж |
| `scripts/check-guard-wiring.sh` | **мета-страж**: каждый страж документирован/подключён/покрыт | ✅ | ✅ selftest-цикл | ✅ |
| `scripts/check-no-runtime-copy.sh` | копия рантайма внутри пакетной репы (реестр 221.1 №138) | ✅ | ✅ gate | ✅ |
| `scripts/check-no-manual-status-table.sh` | рукописная сводная таблица статусов планов | ✅ | ✅ gate | ✅ |
| `scripts/lint-no-silent-int-fallback.sh` | тихий int-fallback в кодогене (класс «молчаливых дыр») | ✅ | ручной (проверено 2026-07-27: НЕ в gate.sh/CI) | ❌ трек Ж |
| `scripts/hardcode-audit.sh` | хардкод-схемы вместо .nv-источника (196) | ✅ | ручной (проверено 2026-07-27: НЕ в gate.sh/CI; сейчас КРАСНЫЙ — кат.B/E выросли сверх baseline) | ❌ трек Ж |
| `scripts/strict_effects_smoke.sh` | сборка std/examples под `--strict-effects` | ✅ | ручной (проверено 2026-07-27: НЕ в gate.sh/CI) | ❌ трек Ж |
| `scripts/tsan_concurrency.sh` | гонки в M:N-рантайме (TSan) | ✅ | ручной | ❌ трек Ж |
| `scripts/gen-plan-status.sh` | генерация `docs/plans/STATUS.md` (не страж — генератор) | ✅ | ручной | н/д |
| `scripts/claude-hooks/guard-git.py` | до-исполнения: `git config user.*`, `git add -A/.`, `git stash` | ✅ | ✅ settings.json | ❌ трек Ж |
| `scripts/claude-hooks/guard-memory.py` | feedback-заметка памяти без поля `enforcement:` | ✅ | ✅ settings.json | ❌ трек Ж |
| `scripts/githooks/pre-commit` | конфликт-маркеры, авторство≠Claude, амендмент без обзорной спеки | ✅ | ✅ 5 реп (независимые копии — см. `scripts/githooks/README.md`) | ❌ трек Ж |
| D-number uniqueness | дубли `## DNNN.` между файлами спеки | — | ✅ в gate.sh | ❌ трек Ж |

**Состояние на 2026-07-27:** самотесты есть у 3 механизмов из 14 (форма задана, остальные — трек Ж §4в).
Колонка «Док» для хуков ✅ **ЗАКРЫТА волной `p231-scripts-docs`** (полные шапки guard-git.py/
guard-memory.py/pre-commit по образцу check-no-runtime-copy.sh + README в каждой подпапке
`scripts/`, реорганизованной в `scripts/gate.sh` + `scripts/guards/` + `scripts/tools/` +
`scripts/claude-hooks/` + `scripts/githooks/`).
Пометка ⚠️ «проверить» = механизм есть и документирован, но не подтверждено, что его реально
зовёт гейт — проверено той же волной: `lint-no-silent-int-fallback.sh`/`hardcode-audit.sh`/
`strict_effects_smoke.sh` НЕ вызываются ни `gate.sh`, ни CI — переведены в «ручной» ниже.

**НАЙДЕНО УСТАНОВЩИКОМ 2026-07-27 (почему реестр и нужен):** `pre-commit` числился настроенным
в пяти репах, а фактически каталог `scripts/githooks` существовал ТОЛЬКО в nova — в nova-http,
nova-tls, nova-polaris, nova-compress машинной защиты от конфликт-маркеров и чужого авторства
не было вовсе. Разнесено и запушено (nova-http `5257f30`, nova-tls `ac7de2a`, nova-polaris
`411dcf3`, nova-compress `75919d9`). Урок: «числится настроенным» ≠ «настроено»; поэтому
установщик заканчивается ПРОГОНОМ механизмов, а не отчётом о скопированных файлах.

## 0. Диагноз (по фактам сессий 221.x, честно)

1. **AST-only эмиссия без IR** — семантика размазана по emit_c per-синтаксическую-позицию.
   Доказательство: fn-newtype семья = 7 разрывов ОДНОЙ конструкции по позициям
   (№53→78→90→96→97→104→116: let / method-return / member-call / closure-param /
   match-bind / record-literal). Фикс каждой позиции локален ПО ПОСТРОЕНИЮ — ощущение
   владельца «чинится локально, соседнее бажит» структурно точное.
2. **Контекстозависимость CU** — код зелёный standalone и в малом CU, красный в
   folder-CU (№122 Permit/@cleanup, №99 entry-порядок). Спека валидировалась на малых
   файлах; реестры компилятора несут per-CU состояние с порядковыми эффектами.
3. **Энфорс-гэпы** — спека декларирует «иначе compile error», проверка не написана:
   №95 (if-let), №106 (launder-паттерны), №113 (`!!`/Fail), №114 (D55-⛔), №119 (линт R9),
   №120 (handler-типы) — шесть за три дня, все найдены вопросами владельца, не гейтом.
4. **Покрытие от фич, не от матрицы** — одна фикстура на решение; «конструкция × позиция
   × контекст CU» не покрывается. №107: «паттерн не покрывался ни одной фикстурой».
   Живой код (polaris) находит больше, чем спек-корпус.

## 1. Трек А — Спека-энфорс-аудит ✅ ИСПОЛНЕН 2026-07-26 (таблица: [231.1-enforce-audit-table.md](231.1-enforce-audit-table.md); ~34 беззубых нормы, 6-7 доки-дрейфов, 150 кодов без neg-фикстур; хвост §9 таблицы — 453 строки прозы без кодов, выборочно)

Механическая инвентаризация: греп spec/decisions по маркерам нормативности
(«иначе compile error», «запрещён», «обязан», «= ошибка», «E_[A-Z_]+» без реализации)
→ таблица «норма | D-блок | энфорс реализован? | neg-фикстура есть?».
Выход: конечный список беззубых норм → пакет энфорс-окон (класс №113/106/120).
Исполнение: sonnet-волна (греп+сверка с чекером), приёмка интегратора.
Мерило: 0 норм без энфорса ЛИБО явная D-пометка «não энфорсится, потому что …».

## 2. Трек Б — Матрица контекстов + дифференциальный прогон (средняя цена, до релиза)

1. **Матрица фикстур**: генератор «конструкция × позиция» для горячих конструкций
   (fn-newtype, Option[fn], sum-lift, consume/@cleanup, generic-инстанс) ×
   (let / arm / param / return / field / element / generic-arg / map-literal).
   Не все клетки — приоритет клеткам, где семьи уже стреляли.
2. **Дифференциальный прогон**: каждый runtime-тест исполняется в ДВУХ контекстах —
   standalone и внутри folder-CU; расхождение вердиктов = автоматический FAIL
   класса №122/№99 (ловится машиной, не вопросом владельца).
3. Инвариант тихих дыр: «check зелёный ⇒ build зелёный» на всём корпусе —
   CC-FAIL после чистого check (класс №114б) = красный гейта, не «известное».

## 3. Трек В — 196-финал: инвентаризация легаси-веток (форсировать)

Сейчас канал — надстройка: данные в resolved_*, потребители — те же разбросанные
ветки emit_c. Перейти от «сносим по мере встречи багов» к инвентаризации:
полный список легаси-веток разбора/типов в emit_c (icr_trace-класс) → для каждой:
канальный потребитель есть? NO-HIT-доказательство есть? → снос пакетами.
Мерило: счётчик живых легаси-веток → 0. Ведётся в 196-плане, здесь — обязательство темпа.

## 4. Трек Г — Дизайн-нота typed-IR (стратегический выход)

rustc-путь: AST → typed-IR (типы/резолв ИЗ ЧЕКЕР-КАНАЛА, один лоуэринг конструкции
для всех позиций) → C. Единственный ход, закрывающий класс «per-position багов»
по построению. Реалистичный горизонт — финал 196 ПОСЛЕ v0.1; СЕЙЧАС — дизайн-нота
(структура IR, какие каналы уже дают нужные данные, порядок миграции эмиссии),
чтобы сегодняшние канальные фиксы ложились в его сторону, а не поперёк.

## 4а. Трек Д — Машинное принуждение норм (исполнительный дом: [231.2](231.2-enforcement-infra.md)) (вопрос владельца 2026-07-26: «конвенции не работают — работает только линт»)

Принцип: норма живёт долгосрочно ТОЛЬКО в машинно-исполняемой форме; текст — источник,
код — принуждение. Пять механизмов, от дешёвого:
1. **scripts/gate.sh** — единый авторитетный гейт (мега-CU + флагман + check std байт-канон;
   PASS-строки ассертит скрипт, не интегратор) + тот же скрипт в GitHub CI на каждый пуш.
2. **Git-хуки в репе** (pre-commit/pre-push): конфликт-маркеры, авторство (%an ≠ Claude —
   урок 349 коммитов), запрет add -A, behind-check перед push.
3. **Линт-паритет с конвенциями**: таблица «правило nv-coding-style → линт есть?» (аналог
   трека А для конвенций кода); мета-норма — новая код-конвенция вливается ВМЕСТЕ с линтом
   либо помечена «немашинная».
4. **Quality-ratchet в CI**: счётчики долгов (легаси-ветки emit_c, беззубые нормы 231.1,
   E/W-коды без neg-фикстур, warnings) с baseline — CI красный при РОСТЕ счётчика.
5. **Бриф-шаблон агентов файлом** (docs/promts/agent-brief-template.md) — стоячие правила
   копируются, не вспоминаются.

## 4б. Трек Е — Принуждение архитектуры компилятора (исполнительный дом: [231.2](231.2-enforcement-infra.md)) (вопрос владельца 2026-07-26: «196 не соблюдался; как обеспечить? дело не только в чекере»)

Три слоя проблемы и лекарства:
1. **Данные** (типы/резолв) — канал 196 (resolved_*); дожат почти, нарушения были возможны,
   т.к. канальность жила в брифах. → **arch-ratchet** (scripts/arch-ratchet.sh, в gate.sh):
   строки emit_c.rs (62947 @ baseline) и вызовы собственного инференса из эмиссии
   (infer_expr_c_type: 346 @ baseline) НЕ РАСТУТ; исключение = правка baseline тем же
   коммитом (красный флаг в ревью). Фикс «мимо канала» перестаёт проходить гейт.
2. **Код эмиссии** (per-position дубли, 50К+ строк) — канал это НЕ лечит; лечит IR (трек Г)
   через strangler: новые лоуэринги — только в IR-путь, старому расти запрещает ratchet.
   До IR-ноты: волна 196.x «физическая граница» — канал отдельным модулем с узким API,
   инференс-хелперы чекера приватизируются от emit_c → мимо канала НЕ КОМПИЛИРУЕТСЯ.
3. **Состояние** (per-CU реестры, порядковые эффекты — класс №122/№99): инвентаризация
   глобальных mutable-реестров компилятора + скоупинг per-CU — отдельная волна (дом 196).

## 4в. Трек Ж — Самотесты механизмов принуждения (вопрос владельца 2026-07-26: «скрипты и автоматизированные функции проверки должны сами быть подтверждены регресс-тестами, что ничего не пропускают»)

Инструменты треков Д/Е/232-Ф.1в/233 — сами код, который может тихо перестать ловить то,
для чего создан (ландшафт меняется: новые пути в скриптах, рефактор хука). Каждый механизм
принуждения ОБЯЗАН иметь регресс-тест «ловит нарушение / пропускает норму»:

| Механизм | Тест |
|---|---|
| `scripts/gate.sh` | синтетический красный кейс на каждый ассерт (испорченный mega-CU-лог/std-канон/флагман-строку) → gate.sh обязан упасть; синтетический зелёный → пройти |
| `scripts/arch-ratchet.sh` | baseline+1 строка в emit_c.rs (временный fixture-файл) → красный; baseline как есть → зелёный |
| `guard-git.py` | таблица команд: `git config user.email x` (блок), `git config user.email` без записи (пропуск), `git add -A`/`.` (блок), heredoc с текстом «git add -A» внутри (НЕ блок — ложняк-регресс, уже был найден руками 26.07) |
| `guard-memory.py` | Write без `enforcement:` (блок), с полем (пропуск) |
| `pre-commit` (5 реп) | staged-diff с `<<<<<<<` (блок), authorship=Claude (блок), чистый коммит (пропуск) |
| D-number uniqueness (в gate.sh) | дублирующий `## D999.` в двух файлах → красный (кроме allow-листа) |
| `check-no-runtime-copy.sh` ✅ **ГОТОВ 2026-07-27** | ✅ `scripts/selftest/test-check-no-runtime-copy.sh` — 5 свойств: ловит копию в пакетной репе, НЕ ловит главную репу (дом рантайма законен), НЕ ловит чистую репу, ловит одну грязную среди чистых, не ломается на настоящей репе nova. Прогон 5/5 |

**ПЕРВЫЙ МЕХАНИЗМ ТРЕКА Ж ПОСТАВЛЕН 2026-07-27** (по прямому указанию владельца «задокументируй
и включи в автоматизированные проверки 231, чтобы не повторялось»). Повод — реестр 221.1 №138:
копия рантайма компилятора внутри пакетной репы не под git, потому её протухание невидимо, и она
шадовит настоящий рантайм; цена промаха измерена — полтора часа диагностики несуществующей
«регрессии компилятора» в nova-http плюс >1 ГБ мусора (526 МБ polaris + 331 МБ worktree + 203 МБ
http, всё удалено). Копия НЕ нужна: штатные `NOVA_RT_DIR`/`NOVA_CG_INCLUDE` существовали давно,
но были упомянуты одной строкой в конце абзаца онбординга — теперь это заметный копируемый блок
с явным запретом (`docs/promts/read-project.md`, раздел «Сборка вне главной репы — БЕЗ
КОПИРОВАНИЯ»). В `gate.sh` добавлены ДВА шага: сам страж и прогон ВСЕХ самотестов
`scripts/selftest/test-*.sh` (цикл — новые механизмы подхватываются автоматически, без правки
gate.sh). Форма самотеста — образец для остальных строк таблицы выше.

Формат: `scripts/selftest/` — по одному .sh/.py на механизм, каждый молча (exit 0) или с
report; сводный `scripts/selftest/run_all.sh` — часть gate.sh (после arch-ratchet, до
cargo build — дёшево и быстро). Мерило: 0 механизмов без самотеста.

## 5. Порядок и гейты

А (волна, дни) → Б.2 дифф-прогон (в test_runner, дни) → Б.1 матрица (итеративно,
приоритет по семьям) → В (постоянный темп в 196) → Г (нота сейчас, реализация после v0.1).
Каждая волна — стандартные гейты (мега-CU + флагман + внешние репы). Найденное
трек-А/Б — в реестр 221.1 обычным порядком.
