// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 110.2: Cancel-shield + async cleanup + 3-level timeout (Ф.3)

> **Создан 2026-05-31.** Sub-plan Plan 110 §«Возможный split». Реализует
> Ф.3 umbrella Plan 110.
>
> **Цель:** cancel-доставка маскируется внутри cleanup body до `exit_timeout`;
> suspend разрешён в `on_exit`; 3-level timeout resolution через runtime
> функцию + `Application` effect (level 2) + hardcoded fallback (level 3);
> realtime context bypass'ит 3-level resolution.
>
> **D-блоки:** активирует D191 (async cleanup), D192 (taxonomy +
> resolution), D198 (realtime conflict).
>
> **Acceptance:** A3, A4, A34 из Plan 110.
>
> **Зависимости:** Plan 110.1 (core compiler pipeline ready); Plan 82
> (Windows fiber arena ready); Plan 83.10/11 (cancel routing); Plan 113
> (#realtime attribute).

## Фазы

### Ф.3 — Cancel-shield + async cleanup + 3-level timeout

- **Ф.3.1** Runtime `nv_consume_enter_shield(timeout_ns)` — устанавливает
  `fiber->cancel_masked = true` + регистрирует deadline.
- **Ф.3.2** На каждой suspend-точке cleanup-body: deadline check →
  `CleanupTimeoutError`.
- **Ф.3.3** `Duration.zero` enforcement (D192): runtime panic если cleanup
  делает suspend.
- **Ф.3.4** `Duration.MAX` warning (D192) — compile-time
  `D192-infinite-timeout-warn`.
- **Ф.3.5** Application effect fallback (D192 level-2): codegen resolve
  зовёт `perform Application.default_exit_timeout()`. (Сам Application
  effect — в Plan 110.4.)
- **Ф.3.6** Realtime integration (D198): `#realtime` → `exit_timeout = zero`,
  bypass 3-level resolution. Compile-time warning
  `D198-realtime-application-override`.
- **Ф.3.7** Cross-platform validation: Windows fiber-arena (Plan 82) +
  Linux libuv.
- **Ф.3.8** Tests T3.1–T3.12.

## Tests

См. Plan 110 §Tests T3.1-T3.12.

## Acceptance

A3 (cancel-shield + async D191), A4 (D192 taxonomy + 3-level), A34
(D198 realtime).

## Оценка

~2 dev-day. Sequential. Recommended model: Opus 4.7.

## Связь

- [Plan 110 umbrella](110-scoped-resources-radical-simplification.md)
- [Plan 110.1 core pipeline](110.1-core-protocol-syntax-codegen.md)
- [Plan 82 Windows fiber arena](82-windows-fiber-arena.md)
- [Plan 113 realtime attribute](113-realtime-blocking-attribute-only.md)
