# Plan 172.12 — Typed-IR mono path

**Статус:** 🔧 в работе 2026-07-06 (заход 1: Ф.0 карта закрыта; заход 2: A1 попытка → нулевой итог, карта пересеквенирована; заход 3: A1′ структурный subst-НОСИТЕЛЬ ЗАКРЫТ — `current_type_subst: HashMap<String, ResolvedType>`; заход 4: A1″ структурная mono-инференция ЧАСТИЧНО — `infer_type_param_binding_rt` + byte-identity-guarded adoption в 3 канал-доступных сидера; **заход 5: A1‴ реестры RT-native ЗАКРЫТ — `mono_worklist`/`generic_type_worklist`/`generic_type_instance_info` несут `Vec<ResolvedType>` вместо `Vec<String>`; продюсеры фризят C-строку через `args_lift`/`subst_vec_from_c_pairs`→`Raw`, читатели лоуэрят через `arg_c`/принтер; byte-identical 32/32, 54/0, δ0. `Raw`/`lift_c_name` НЕ выпилены — все продюсеры реестров string-native (§7.2), Raw остаётся до A2/канальной инференции; **заход 6: A2a — структурная concreteness принтера ЗАКРЫТ — армы `Tuple`/`Option`/`Result`/Vec-flip решают через структурный `rt_is_erased_stub` вместо разбора `Nova_`/`____`; `resolved_array_to_c`/`resolved_named_to_c` = 0 разборов, `is_generic_stub_c` вне тел принтера; byte-identical 42/42, 54/0, δ0. Остаток — арм `TypeParam` receiver (5 сайтов) = A2b, блокер строковый `current_receiver_type`; **заход 7: A2b ЗАКРЫТ — `current_receiver_rt: Option<ResolvedType>` структурный носитель (sync в 29 сайтах, `Raw`-долг), 5 receiver-`Nova_`/`____` вынесены в марк. debt-хелперы `debt_receiver_bare`/`debt_receiver_typeparam`/`debt_receiver_erased`; `resolved_type_to_c` тело = 0 decode; content byte-identical 60/60 (6 sorted-identical под emission-ordering нондетерм.), 53/0, δ0. TASK-2 uint-раскол РАЗОБРАН → правка ломает `Vec[uint].new()` (runtime `array.h` фикс-набор без `nova_uint`), ретрагирована, маркер `[M-uint-legacy-array-uint64-until-a4]` (закрытие в A4)**; **заход 8: A4 — потребительские `____`/`Nova_`-decode ЗАКРЫТЫ — греп-инвариант вне debt-хелперов 129→0 (два коммита: 20 string-utility функций + `is_generic_stub_c` переименованы в `debt_`-префикс, ~65 встроенных сайтов извлечены в 26 новых debt-хелперов, несколько дословных дублей внутри `infer_expr_c_type`/`emit_call` вылечены разом через replace_all); 51 debt-функция итого. `[M-172.1-lifted-legacy-arms]` остаётся классом-открытым (`legacy_inner` как отдельный wrapper уже не существует — тело давно влито в `infer_expr_c_type`, IR-лоуэринг армов 6c-6z — горизонт Ф.2-Ф.4, не один заход); uint-маркер `[M-uint-legacy-array-uint64-until-a4]` ПЕРЕАДРЕСОВАН в A5 (тот же root cause — NovaArray/Vec dual representation). Byte-identity/conformance/регресс — все δ0; **заход 9: A5 — крашер `bench.opaque` ЗАКРЫТ (infer зеркалит emit-intrinsic, обе триплет-копии, коммит `b9b8008f4`, conf 66/0 δ0); [M-array-vec-unify]+uint ЭМПИРИЧЕСКИ подтверждены как широкий runtime+codegen substrate (эксперимент-флип primitive `.new()`→Vec дал Frankenstein `NovaArray* = Vec_ctor()` + `nova_array_push` — layout-склейка, НЕ слияние; ретрагирован по whole-not-half); bind/chunks — checker-аннотация-гэп (chan=None, types/mod.rs). Остаток трека A — честный хвост §14.6**; **заход 10: A6 — str-семейство runtime-примирение ЗАКРЫТО (§14.7-14.11): ЭМПИРИКА опровергла §14.4.1 — str-C-функции (`nova_str_to_bytes`/`bytes`/`as_bytes`/`split`/`to_chars`/`chars` + `from_bytes_*`) МЁРТВЫ (ноль call-site, retired Plan 139.2 → Nova-body `Vec[T].from_raw_parts`; even `ro b = s.bytes()` даёт `Nova_Vec____nova_byte*` через checker-канал). Сделано: снос 9 мёртвых C-функций (array.h ×6 + string_builder.h ×3, byte-identical: headers `#include`d не splice) + align 8 устаревших infer-fallback'ов NovaArray→Vec (обе триплет-копии). Typedef-слой-вопрос МООТ (ни одна rt-функция не называет Vec-mono). Гейты: build оба крейта clean, conf 66/0 δ0, byte-identity 36/40 identical (4 diff = generic-stub ordering-нондетерм., base-vs-base доказано; NovaArray→Vec дельта-список ПУСТ — канал перекрывает), nova test выборка δ0 (все fail = pre-existing stale-тесты retired API). A7 de-risked: str-runtime уже Vec, остаток = user-`[]T` codegen-флип 5 сайтов + `NOVA_ARRAY_DECL`-снос**; **заход 11: A7 старт — координированный флип `[]T.new()/.with_capacity()` + `receiver_c_type` ЗАКРЫТ (§14.12-14.13): эмпирика показала объём УЖЕ 3 из 5 «сайтов» из §14.11-инвентаря (binding-type, array-литералы, for-loop) были САМИ Vec-native ДО этого захода (resolved_array_to_c Vec-flip + try_emit_typed_vec_literal + iter()/next()-протокол уже покрывали primitive `[]T`) — реальный разрыв был ТОЛЬКО в static-ctor dispatch (`nova_array_new_<suffix>` вызов на Vec-типизированный биндинг, layout-совместимая, но текстуально несогласованная дуальность) и в `receiver_c_type`'s `[]T` арме (редкий extension-method-receiver путь). Оба закрыты одним координированным заходом (НЕ повторяет заход-9 Frankenstein: там флипался ТОЛЬКО ctor без выравнивания с уже-Vec биндингом; здесь ctor теперь СОГЛАСОВАН с уже-Vec биндингом). uint закрыт (`nova_uint`, не `uint64_t`) с explicit-тестом. Гейты: build оба крейта clean, conf 66/0 δ0 (d27/d38/d403/d232 явно PASS), uint round-trip зелёный, wide nova test (strings/buffers/json/serde/compress/unicode/plan138_2/153_5/98/99/std-net) 18/2/1(skip) — 2 fail байт-в-байт идентичны на baseline (pre-existing D249 stale), спот-C-check 0 `NovaArray_` для []int/[]uint (2 residual hits = pre-existing bare-`Vec` erasure-sentinel decl, не связан). НЕ сделано (по контингенси-правилу плана «пп.1-2 целиком, снос отдельным заходом»): снос `NOVA_ARRAY_DECL`/`IMPL`. Найдены 3 доп. блокера для сноса (не были в §14.11-инвентаре): Plan-96 parallel-for (сырые `NovaArray_<T>*` локали, ArrayLit/Ident-iter под-кейсы ~9990-10090), array rest-bind destructuring (`[a, ..rest, b]`, сырой `NovaArray_<T>*` для хвоста ~37100-37117), `resolved_array_to_c`'s dead-in-practice primitive-табличка (~2977-2993, текстуально NovaArray, недостижима пока Vec-template всегда в наличии). `emit_array_lit`'s closure/void_p/protocol-box fallback НАМЕРЕННО остаётся NovaArray (closure-arrays вне области A7). Следующий заход: перевести 3 найденных блокера на Vec-mangled construction → затем снос array.h**) — из финализации 172.1, главный структурный разрыв с эталонами
**Зонт:** [172-compiler-rework.md](172-compiler-rework.md)
**Порядок:** ПОСЛЕ [172.4](172.4-value-abi-auto-placement.md) (value-ABI сначала унифицирует representation — IR ложится на ОДИН путь вместо трёх), до/вместе со [172.13](172.13-constraint-inference.md).
**Ветка:** `track-a-172-12` (от main `97100017`; рабочая копия `nova-p173`)

## Мотивация

После 172.1 (единый канал `resolved_types`, legacy удалён) остался главный
структурный разрыв с эталонами (rustc HIR→MIR, Zig ZIR→AIR, Swift SIL,
Go typed-AST+SSA): **AST лоуэрится в C-текст напрямую**, и часть типовой
правды живёт в **C-строках**:

- mono-identity = мангл-строка (`Nova_Vec____nova_str*`) — сравнение/декод по
  строковым префиксам (`strip_prefix("Nova_")`, `contains("____")`);
- dispatcher-каналы 6c–6z ([M-172.1-lifted-legacy-arms]) — дословно поднятая
  state-логика (var_types/схемы C-строк), подлежит замещению;
- generic-тела эмитятся erased/по-mono с `current_type_subst: HashMap<String,String>`
  (имя→C-строка) — d119-класс загрязнений субституции пойман в 172.1
  receiver-instance-map'ом, но носитель остаётся строковым.

## Цель

Типизированное промежуточное представление между checker'ом и C-эмиссией:

1. **Узел IR несёт `ResolvedType`** (D315-канон), НЕ C-строку; C-имя выводится
   ТОЛЬКО в финальном принтере (`resolved_type_to_c` становится деталью принтера).
2. **Mono-инстанцирование на IR**: инстанс = (template, `Vec<ResolvedType>` args)
   — структурная identity, mangling лишь при печати. Убивает строковый
   `current_type_subst`, `generic_type_instance_info`-декодирование и
   `is_generic_stub_c`-эвристики.
3. **Замещение lifted-arms классом**: поднятые армы 6c–6z становятся
   IR-лоуэрингом (один узел — один типизированный лоуэринг), закрывая
   [M-172.1-lifted-legacy-arms] не поштучно.
4. **[M-array-vec-unify]**: NovaArray/Vec-раздвоение решается на IR-уровне
   (одно представление, два принтера → одно).

## Не-цели

- Оптимизации на IR (SSA/DCE) — отдельный горизонт; IR сначала как typed-carrier.
- Инкрементальность/query ([IDEA-172-incremental-queries]) — не здесь.

## Фазы (эскиз, детализация при старте)

- **Ф.0** Замер: инвентарь C-строковых identity-сайтов (grep `____`/`Nova_`-префиксов
  в решениях) — карта потребителей.
- **Ф.1** IR-типы: `IrExpr { kind, ty: ResolvedType, span }`; лоуэринг AST→IR
  ЧИТАЕТ канал (аннотации уже полны после 172.1).
- **Ф.2** Mono на IR: worklist инстансов (template, args:Vec<ResolvedType>);
  принтер манглит.
- **Ф.3** Перевод эмиттера на IR-вход посегментно (fn-тела → выражения →
  деклы), гейты 172.1-класса на каждый сегмент (conformance 38/38 + полный
  nova_tests baseline).
- **Ф.4** Снос строковых носителей (current_type_subst, instance_info-декод,
  stub-эвристики) + закрытие [M-172.1-lifted-legacy-arms].

## Критерии приёмки

- Ни одного решения по `contains("____")`/строковому префиксу вне принтера.
- [M-172.1-lifted-legacy-arms] закрыт (армы 6c–6z удалены, замещены IR-лоуэрингом).
- Гейты: conformance + полный nova_tests == baseline; перф не хуже (§2 замер).

---

## Де-риск, заход 1 (Ф.0 карта) — 2026-07-06

**Ветка:** `track-a-172-12` от main `97100017` (рабочая копия `nova-p173`; на ветке — только
docs-коммит журнала поверх `97100017`, компилятор байт-в-байт равен базису → базис регресса =
тот же дерево-бинарь). Базис conformance замерен **54/0** (pos+neg, `test --positive --compile-error`).

### 1. Объём (замер по коду, grep, 2026-07-06)

Вся строковая типовая-идентичность сосредоточена в ОДНОМ файле — `codegen/emit_c.rs` (48 159 строк);
`types/mod.rs` (29 297) держит канал, но НЕ решает по C-строкам.

| Сайт (строковый носитель / решение) | Всего | В `emit_c.rs` | Где ещё |
|---|---:|---:|---|
| `current_type_subst: HashMap<String,String>` (имя→C-строка) | 139 | 133 | types/mod.rs 6 |
| `type_ref_to_c` (резолв+перевод, ретайрится по 172.1 U.4.8) | 359 | 324 | external_registry 15, sig_registry 6, прочее |
| `strip_prefix("Nova_")` / `starts_with("Nova_")` | 104 | 103 | test_runner 1 |
| `generic_type_instance_info` (mangled→(base,Vec\<C-строка\>)) | 76 | 76 | — |
| `resolved_type_to_c` (принтер — ЦЕЛЕВОЙ единственный путь) | 70 | 70 | — |
| `contains("____")` (decode mono-идентичности по разделителю) | 45 | 45 | — |
| `receiver_c_type` (C-строка ресивера, re-parse) | 35 | 35 | — |
| `is_generic_stub_c` (эвристика «erased type-param C-имя») | 30 | 30 | — |
| `erased_type_ref_c` | 19 | 19 | — |
| `infer_expr_c_type_legacy` (panic-заглушка после подъёма армов) | 2 | 2 | — |

**Ключевая классификация `____`/`Nova_`-prefix РЕШЕНИЙ по вмещающей функции** (grep→awk enclosing-fn):
из ~148 таких решений в принтере (`resolved_type_to_c` 3 + `resolved_named_to_c` 2 +
`resolved_array_to_c` 1 + `is_generic_stub_c` 2 = **~8** легитимных), остальные **~140 — в
ПОТРЕБИТЕЛЯХ**: `infer_expr_c_type` 19, `emit_expr` 15, `emit_call` 6, `emit_record_lit` 4,
`emit_generic_type_instance` 4, `pattern_bind_typed` 3, `emit_module` 3, `emit_array_lit*` 4,
`register_mono_method_instance` 2, `pattern_cond` 2, `emit_monomorphized_method` 2 … хвост по 1–2.
⇒ **приёмочный критерий «решение по `____`/префиксу только в принтере» = снести ~140 потребительских
сайтов**, оставив ~8 в принтере (и их тоже — вывести из СОСТОЯНИЯ типа, не из re-parse C-строки).

### 2. Точки входа (file:line, `emit_c.rs`)

- **Принтер (цель — единственный носитель→C):** `resolved_type_to_c` :2080 (арм `TypeParam` :2099
  делает mono-subst через `current_receiver_type`.re-parse + `generic_type_instance_info` +
  `current_type_subst`; арм `Tuple` :2191 решает concrete по `contains("____")`);
  `resolved_named_to_c` :2507 (`Option`-арм concrete-тест по `contains("____")` :2556);
  `resolved_array_to_c` :2233 (Vec-flip: `is_generic_stub_c`+`contains("____")` :2243).
- **Потребитель типа выражения:** `infer_expr_c_type` :39413 — каналы 1–6, затем **поднятые
  армы 6c–6z** (:39694…:42787) — дословные копии старого `legacy_inner`, финал `legacy_inner`
  :42787 = **panic-заглушка** ([M-172.1-lifted-legacy-arms]).
- **Строковые носители mono-идентичности (структуры):**
  `current_type_subst: HashMap<String,String>` :1196; `mono_worklist: Vec<(String, Vec<(String,
  String)>, String)>` :1186 — (fn-имя, type_subst **имя→C-строка**, mangled C-имя);
  `generic_type_instance_info: RefCell<HashMap<String,(String,Vec<String>)>>` :1249 —
  mangled→(base, **Vec\<C-строка\> args**); `generic_type_worklist` (Vec-инстансы, C-строка args);
  `current_receiver_type: Option<String>` (C-строка, re-parse в принтере).
- **Типизированный канал (172.1 D315, ГОТОВ):** `resolved_types: HashMap<ExprId, ResolvedType>`
  :788; `resolved_callees: HashMap<ExprId, Span>` :799; `fn_ret_by_span: HashMap<Span, String>` :811.
  `ResolvedType` — `types/…:37`.

### 3. Стыковка с 172.1 (что уже сделано; где разрыв)

172.1 закрыл **уровень ВЫРАЖЕНИЯ**: `resolved_types[ExprId] = ResolvedType` (D315-канон, lossless:
width/sign/effects-type-args/L1-L2-L3/module) + принтер `resolved_type_to_c(rt)` + каналы 1–6 в
`infer_expr_c_type`. Т.е. **типизированный носитель на выражениях уже есть** — это ровно Ф.1-цель
«узел несёт `ResolvedType`, C-имя выводится в принтере». D-status: D315 ✅, каналы+lifted-армы 6i–6z
подняты (tally 6224→338, −95%), `legacy_inner` = panic.

**РАЗРЫВ, который берёт 172.12 — уровень МОНО/ИНСТАНСА (всё ещё строковый):**
- `current_type_subst` / `mono_worklist.type_subst` / `generic_type_instance_info.args` /
  `current_receiver_type` несут **C-строки**, не `ResolvedType`. Принтер (`resolved_type_to_c`
  арм `TypeParam`/`Named`/`Tuple`/Array) вынужден **re-parse** этих строк (`trim_start_matches
  ("Nova_")`, `contains("____")`, `is_generic_stub_c`) — потому что источник mono-контекста
  строковый. Это корень «двух окон правды» (172.1.1 §1: bare vs mangled erased-контейнер;
  gap #2 mono-subst-timing) — субстратные блокеры 172.1.1 (SelfAccess/Ident флип, container-channel)
  **гейтнуты именно на строковость mono-субституции** (proxy `is_generic_stub_c` ложно срабатывает,
  `current_type_subst` не синхронен с контекстом).
- ⇒ **172.12 = поднять типизированность с выражения на инстанс**: `mono_worklist`/subst/instance-info
  несут `Vec<ResolvedType>` (структурная identity `(template, Vec<ResolvedType>)`), mangling —
  ТОЛЬКО в `compute_generic_type_c_name` при печати. Тогда принтер выводит bare/mangled из СОСТОЯНИЯ
  `ResolvedType` (concrete vs `TypeParam`), а не из re-parse C-строки → снимается proxy/`____`-decode
  → разблокируются subst-timing/erased-контекст блокеры 172.1.1 ПО ПОСТРОЕНИЮ.

### 4. Какие недавние фиксы 172.12 поглощает/упрощает

- **wave-4 «дыры кодогенератора» (`97100017`, базис):** (a) вывод type-args конструктора из
  аргументов [`M-176-generic-wrapper-mono-inference`] (`infer_expr_c_type` :39436 — эвристика по
  `current_type_subst` re-map C-строки), (b) value-record-ресивер generic-метод-return, (c)
  turbofish на методах в return-subst, (d) дедуп перегрузок по типам параметров. Все четыре —
  **строковые заплатки на mono-инференс** (re-parse `current_type_subst`/mangled). При структурной
  `(template, Vec<ResolvedType>)`-identity вывод type-args и subst-возврата — по типам, а не re-map
  строк ⇒ заплатки субсумируются (не удалять точечно — они уходят вместе с носителем).
- **D381 module-qualified mangling (`ce1b8758`/`d478e72a`):** коллизия одноимённых типов разных
  модулей решена квалификацией C-**имени**. `ResolvedType::Named{module,…}` уже несёт module
  (lossless) → при печати из носителя квалификация — деталь принтера (`resolved_named_to_c` :2513
  уже склеивает `module.join("_")`), НЕ строковая пост-обработка. 172.12 переносит D381-решение
  в единственный принтер, убирая расходящиеся строковые квалификации в потребителях.

### 5. Последовательность под-атомов (каждый — ЗЕЛЁНОЕ дерево, отдельный коммит)

Порядок = «принтер выводит из состояния → потом источник структурируем → потом сносим потребителей».
Направление §0 (единый носитель, не зеркала): двигаем НОСИТЕЛЬ к `ResolvedType`, потребители удаляем.

- **A0 (этот заход, Ф.0):** карта рисков (этот раздел) + верификация базиса 54/0. Коммит docs-only.
- **A1 (Ф.1-substrate) — ⛔ ПЕРЕСЕКВЕНИРОВАН (заход 2, см. ниже §«Де-риск, заход 2»):** исходная
  формулировка «`generic_type_instance_info`/`mono_worklist`/`generic_type_worklist` несут
  `Vec<ResolvedType>` как носитель правды, C-имя — вывод принтера» **не влезает ПЕРЕД структурным
  subst/inference**: type-arg-ЗНАЧЕНИЯ рождаются C-строками у ≥6 из ~8 продюсеров
  (`infer_expr_c_type`/`infer_type_param_binding`/строковый `current_type_subst`), RT там нет.
  RT-как-правда у этих продюсеров = либо reverse `c_type→RT` (запрещённая §0-инверсия), либо
  структурная унификация из канала `resolved_types` (=A3+срез A4). ⇒ подложка начинается НЕ с
  реестров (они — ПРОИЗВОДНОЕ от subst+inference), а с ВХОДОВ. Новый порядок: **A1′ структурный
  subst (`current_type_subst: name→ResolvedType`, бывш. A3) → A1″ структурный `infer_type_param_
  binding_rt` из `resolved_types` → A1‴ реестры (тогда RT-native тривиально, бывш. A1) → A2 принтер
  → A4 потребители → A5.**
- **A2:** принтер `resolved_type_to_c` арм `TypeParam` (:2099) выводит subst из
  `Vec<ResolvedType>`-контекста (concrete→mangle, `TypeParam`-остаток→Err/erased) — убрать
  `is_generic_stub_c`/`contains("____")` ВНУТРИ принтера. Гейт: container-класс byte-identical
  (`NOVA_U45_RLCHECK`=0) + 54/0 + регресс δ0. Разблокирует SelfAccess/Ident флип (172.1.1) — но
  флип отдельным под-атомом (A2b) под soundness-гейтом.
- **A3:** `current_type_subst: HashMap<String,ResolvedType>` (был String) + снос C-строка-args из
  инстанс-реестров (продюсеры переведены в A1). Ripple ~139 сайтов — per-файл loop
  (check→fix→re-check), НЕ full-regress в цикле; финальный гейт 54/0 + регресс.
- **A4 (Ф.4):** снести потребительские `____`/`Nova_`-prefix решения (~140) — армы 6c–6z замещаются
  IR-лоуэрингом из носителя; `legacy_inner` уже panic → удалить поднятые дубли ([M-172.1-lifted-legacy-arms]
  закрыт). Гейт: полный nova_tests baseline + 54/0.
- **A5 ([M-array-vec-unify]):** NovaArray/Vec — один принтер из `R::Array` (`resolved_array_to_c`
  :2233 уже мост). После A2 (нет `is_generic_stub_c`) — механический.

### 6. Точка возобновления (после захода 1)

Закрыт под-атом **A0 (Ф.0 карта)**, коммит docs-only, дерево зелёное (базис 54/0 подтверждён).
[ЗАМЕЩЕНО заходом 2 — см. §«Де-риск, заход 2».] Исходно: «следующий заход стартует с A1 —
keystone-substrate: структурная `(template, Vec<ResolvedType>)` identity инстанса, ЦЕЛЫЙ носитель».
Заход 2 показал: эта формулировка A1 не влезает перед структурным subst/inference; реестры —
производное, не подложка. Точка возобновления обновлена в §«Де-риск, заход 2».

---

## Де-риск, заход 2 (A1 попытка → блокер producer-substrate) — 2026-07-06

**Ветка:** `track-a-172-12` (`nova-p173`), вершина `4045a5ba` (= main + карта A0). **Итог по коду —
НУЛЕВОЙ** (правило дисциплины: «не влезает → стоп ДО правки носителя, не посреди»). Дерево пристинно,
базис 54/0 держится по построению (0 изменений компилятора). Изменён ТОЛЬКО этот план (docs-only).

### 7.1 Что проверялось

A1-keystone: переключить инстанс-реестры (`generic_type_instance_info` :1249, `generic_type_worklist`
:1213, `mono_worklist` :1186) на структурную identity `(template, Vec<ResolvedType>)`, где C-имя —
ВЫВОД принтера из RT, а C-строка args — ПРОИЗВОДНОЕ (§0: co-existence строки допустим ТОЛЬКО как
выведенной ИЗ структуры, не наоборот). Требование: ЦЕЛЫЙ носитель, без окна строка↔RT.

### 7.2 Разведка продюсеров (полный инвентарь, grep→read всех сайтов записи)

Продюсеры type-instance-реестров и ИСТОЧНИК их `type_args_c`:

| Продюсер (file:line) | Источник type-args | RT нативно? |
|---|---|---|
| `resolved_array_to_c` :2246–2257 | `resolved_type_to_c(inner: &ResolvedType)` | **ДА** (`inner` — RT) |
| `resolved_named_to_c` :2646–2666 | `resolved_type_to_c(a: &ResolvedType)` per arg | **ДА** (`args: &[RT]`) |
| module-scan `[]elem` :3920–3947 | `type_ref_to_c(elem: &TypeRef)` | нет (TypeRef; `from_type_ref` даёт RT лишь для concrete) |
| `register_generic_instances_in_typeref` :14985–15001 | `value_aware_subst_to_ref(g, subst)` — **строковый** subst `&[(String,Option<String>)]` | **НЕТ** (name→C-строка) |
| `try_generic_static_ctor_mono` :15158–15174 | `infer_expr_c_type(arg)` + `infer_type_param_binding`→C-строка | **НЕТ** |
| `try_infer_variant_mono_args` :15304–15321 | `infer_expr_c_type(arg)` + `infer_type_param_binding`→C-строка | **НЕТ** |
| turbofish-static `HashMap[..].new()` :27552–27565 | `type_ref_to_c(tr)` per turbofish-ref | нет (TypeRef) |
| record-lit ctor-mono :33734–33752 | `infer_expr_c_type(field.value)`→C-строка | **НЕТ** |
| array-lit Vec-mono :34671–34682 | `elem_c` из emission элемента → C-строка | **НЕТ** |

**Вывод:** только **2 принтер-side продюсера** (`resolved_array_to_c`/`resolved_named_to_c`) имеют
RT нативно; **≥6 остальных** рождают type-arg-значения C-строками через `infer_expr_c_type` /
`infer_type_param_binding` / строковый `current_type_subst` / `type_ref_to_c`. Реестры — ПРОИЗВОДНЫЙ
артефакт subst+inference, а те строково-нативны.

### 7.3 Почему A1-как-реестры не влезает перед A3

Чтобы у этих ≥6 продюсеров type-arg стал RT-правдой, нужно одно из:
1. **reverse `c_type→ResolvedType`** (декод C-строки в RT) — RT становится ВЫВЕДЕННЫМ ИЗ строки,
   прямая инверсия §0 («строка выводится из RT, не RT из строки»). **Запрещено.**
2. **структурная инференция**: `current_type_subst: name→ResolvedType` (это A3) + структурный
   `infer_type_param_binding_rt`, унифицирующий `from_type_ref(param.ty)` против RT аргумента из
   канала `resolved_types[ExprId]` (это срез A4/ядра миграции `infer_expr_c_type`→RT), с fallback на
   channel-miss, byte-identical по всем L1/L2/L3/width-случаям. Это МНОГО-тысячная правка «одним
   срезом» с высоким регресс-риском — НЕ substrate-атом и НЕ «зелёное дерево одним коммитом».

Частичный вариант (RT у 2 принтер-продюсеров, строка у 6) = **ровно запрещённое окно строка↔RT**
(половина реестра с RT, половина без). Дисциплина «whole-not-half» его исключает.

Внутренняя несогласованность исходной карты, вскрытая заходом 2: A1-нота говорила «C-строка args
удаляется в A3, как только ВСЕ продюсеры пишут RT» — т.е. САМА карта не проверила, что все продюсеры
МОГУТ писать RT в A1. Разведка 7.2 показывает — не могут без A3/A4. Порядок A1(реестры)→A3(subst) для
строково-нативных продюсеров **инвертирован по зависимости**.

### 7.4 Пересеквенированная точка возобновления (заход 3 стартует ОТСЮДА)

Подложку начинать с ВХОДОВ subst+inference, не с реестров:
- **A1′ (бывш. A3) — структурный subst:** `current_type_subst: HashMap<String, ResolvedType>` (был
  `String`). Продюсеры subst (`resolve_mono_type_args`, `try_*_mono`, ctor-mono) складывают RT; чтения
  (принтер `resolved_type_to_c` арм `TypeParam` :2142, ~139 сайтов) читают RT и лоуэрят на месте.
  Гейт: byte-identical + 54/0 + регресс δ0. Per-файл loop на ripple.
- **A1″ — структурная mono-инференция:** `infer_type_param_binding_rt` из канала `resolved_types`
  вместо `infer_expr_c_type`-C-строк; унификация `from_type_ref(param.ty)` vs RT-аргумента.
  Гейт: byte-identical + 54/0.
- **A1‴ (бывш. A1) — реестры:** ПОСЛЕ A1′/A1″ type-args у ВСЕХ продюсеров уже RT → реестры
  `(template, Vec<ResolvedType>)` становятся RT-native тривиально, `compute_generic_type_c_name`-
  overload-из-RT даёт те же имена. Тогда это ЦЕЛЫЙ носитель без окна.
- Далее **A2** (принтер `TypeParam`-арм из RT-контекста, снос `is_generic_stub_c`/`____` внутри
  принтера) → **A4** (снос ~140 потребительских решений, армы 6c–6z) → **A5** ([M-array-vec-unify]).

Инструменты де-риска (без изменений): `NOVA_U45_RLCHECK` (container-divergence), `NOVA_TP_TRACE`
(TypeParam-lowering), базис-бинарь `4045a5ba` для δ0-регресса.

---

## Заход 3 (A1′ — структурный subst-носитель ЗАКРЫТ) — 2026-07-06

**Ветка:** `track-a-172-12` (`nova-p173`), от `ddcf2270`. **Итог: A1′ реализован и зелён.**
`current_type_subst: HashMap<String, String>` → **`HashMap<String, ResolvedType>`** — носитель
переключён ЦЕЛИКОМ, без окна строка↔RT (поле уникально RT-типизировано).

### 8.1 Механизм подъёма (§0 — ОДНА помеченная точка долга)

Разведка ВСЕХ продюсеров `current_type_subst` (не путать с продюсерами реестров из захода 2)
подтвердила заход-2 §7.2 в максимуме: **type-arg-ЗНАЧЕНИЯ рождаются C-строками у 100% продюсеров
subst** — источник каждого = string-native mono-реестр (`generic_type_instance_info` / `type_args_c`
/ split мангла) либо C-строка-параметр (`t_c_ty`/`self_c`); **ни одного TypeRef/RT на слое записи
subst** (проверено прямым чтением всех сайтов конструкции `type_subst: Vec<(String,String)>` —
17160, 6900, 6917, 16494, 16597, 6653, все `.zip(type_args_c)`). Премиса задачи «у записывающего
ОБЫЧНО есть более богатый источник» на слое `current_type_subst` **ложна** — богатый источник
(RT-реестры / структурная инференция) = ровно A1‴/A1″, НИЖЕ по потоку.

⇒ подъём строка→RT реализован как **ОДНА помеченная точка** (§0, не размазан):
- `ResolvedType::Raw(String)` — transitional-debt вариант КАНОНА (types/mod.rs): несёт C-имя
  ВЕРБАТИМ, без парса и без фабрикации структуры (ПРОТИВОПОЛОЖНОСТЬ запрещённой заходом-2 инверсии
  `c_type→RT`: `Raw` не выводит структуру ИЗ строки — он транспортирует строку неизменной внутри
  расширенного носителя). Никогда не достигает `cat_compatible_rt`/`distinct_mono`/`from_type_ref`.
- `Emitter::lift_c_name(c) -> Raw(c)` — единственная точка подъёма; `subst_map_from_c_pairs` (для
  ~19 `mem::replace`-продюсеров из string-pair subst) и 4 прямых insert зовут её.
- Читатели: `subst_val_c(rt)` / `subst_c(key)` — C-имя ВЫВОДИТСЯ (для `Raw` = вербатим, byte-identical
  прежнему `String`; для реального RT после A1″ — через принтер `resolved_type_to_c`).
- Принтер `resolved_type_to_c`: арм `R::Raw(s) => s.clone()` — round-trip `lift→Raw→print = c` даёт
  byte-identity ПО ПОСТРОЕНИЮ.

### 8.2 ФАКТЫ перевода

- **Точки записи (продюсеры):** нативно RT — **0**; через подъём-долг `lift_c_name` — **все**
  (~19 `mem::replace` из string-pair subst через `subst_map_from_c_pairs` + 4 прямых insert
  `Self`/generic-param). Честный максимум sanctioned-escape-hatch (не новый блокер: в отличие от
  захода 2, носитель ДОСТИЖИМ — реестры не трогаются).
- **Читатели (~26 C-строко-сайтов):** структурные — 1 (`resolved_array_to_c` back-map через
  `subst_c(name).as_deref()`, ветвление по C-имени сохранено байт-в-байт); через принтер/`subst_c` —
  остальные (printer TypeParam-арм, `resolved_named_to_c`, `builtin_sum_receiver_c_type` ×3,
  `receiver_c_type` ×2, ctor/variant-mono ×4, T.MAX, static-dispatch recv-seg ×4, effect-return ×2,
  filter_map type-args ×3, whole-map iter). Membership-читатели (`contains_key`, ×2) и debug-`{:?}`
  (NOVA_TP_TRACE, whole-map eprintln) — без изменений (RT: Debug).
- **Ripple канона:** `Raw` не ломает структурные функции — `cat_compatible_rt`/`distinct_mono` имеют
  wildcard; единственный exhaustive-match без wildcard (`resolved_to_typeref`) получил арм
  `R::Raw(_) => None` (Raw не восстановим в TypeRef, читается только как C).

### 8.3 Гейты

- Сборка Rust — чистая (nova-codegen + nova-cli).
- **Эталонные:** `nova test --positive --compile-error spec_tests/conformance` = **54 PASS / 0 FAIL**.
- **Byte-identity:** базис-бинарь (`ddcf2270`, до правки) vs A1′-бинарь, `compile --no-annotate-source`,
  сравнение отсортированным содержимым — **0 расхождений** (представительный mono/generics/сумма/
  value-record/serde/turbofish набор + conformance/examples; идентичны и .c, и коды ошибок).
- **δ регрессий:** 0 (byte-identity ⇒ идентичный .c ⇒ идентичные результаты; conformance подтверждает
  отсутствие семантического дрейфа).

### 8.4 Точка возобновления (заход 4 стартует ОТСЮДА)

A1′ закрыт (носитель RT-типизирован, ОДНА точка подъёма-долга `lift_c_name`/`Raw`). Все значения
subst сейчас — `Raw`-долг (структурной правды НЕ добавлено — она приходит с A1″ ПО ПОСТРОЕНИЮ; A1′ дал
RT-типизированный СЛОТ, в который A1″ пишет реальный RT). **Следующий — A1″ (структурная mono-
инференция):** продюсеры subst считают реальный `ResolvedType` из канала `resolved_types[ExprId]`
(унификация `from_type_ref(param.ty)` vs RT-аргумента), заменяя `lift_c_name(...)` на настоящий RT
инкрементально; по мере вытеснения `Raw` из продюсеров — удалить `lift_c_name`/`subst_map_from_c_pairs`
и, когда `Raw` больше нигде не производится, сам вариант `ResolvedType::Raw`. Гейт A1″: byte-identical
+ 54/0. Затем A1‴ (реестры RT-native) → A2 (принтер TypeParam из RT-контекста) → A4 → A5.

---

## Заход 4 (A1″ — структурная mono-инференция из канала, ЧАСТИЧНО; `Raw` остаётся) — 2026-07-06

**Ветка:** `track-a-172-12` (`nova-p173`), от `60f03670` (A1′). **Итог: A1″ реализован для канал-доступных
продюсеров; `Raw`/`lift_c_name` НЕ удалены — основная масса subst-сидеров объективно не переводится до A1‴
(реестры/worklist RT-native), как и предсказано заходами 2/3.** Изменён ТОЛЬКО `codegen/emit_c.rs`
(`types/mod.rs` не трогался — переиспользованы `from_type_ref`/`peel_view`/`Raw`).

### 9.1 Инфраструктура (§0-честная, без обратного `c_type→RT`)

- `infer_type_param_binding_rt(param_ty: &TypeRef, arg_rt: &ResolvedType, slots)` — структурный
  близнец `infer_type_param_binding`, унифицирует декл-`TypeRef` против НАСТОЯЩЕГО RT аргумента
  (bare-T / `[]T`≡`Vec` / Option / Result / user-`Base[..]`) — ЧИСТО структурно, БЕЗ парса C-строк и
  БЕЗ реестр-лукапов (RT уже несёт вложенность; guard-`peel_view`; residual `TypeParam`/`Any`/`Raw`/
  `Unit` НЕ связывается).
- `channel_arg_rt(expr)` = `resolved_types[ExprId]` (D315), `None` при unset/промахе.
- `rt_slots_from_args(param_tys, args, slot_names)` — строит `Vec<(name, Option<RT>)>` инференцией из
  канала; `rt_slots_seed_turbofish(slots, refs)` — сид из декл-`TypeRef` турбофиша через `from_type_ref`
  (ВТОРОЙ структурный источник, приоритет как строковый Source 1).
- **`subst_map_adopt_rt(pairs, rt_slots)` — byte-identity ГЕЙТ:** для каждой записи (name→C-строка)
  ПРИНИМАЕТ структурный RT ТОЛЬКО если он лоуэрится в ТУ ЖЕ C-строку (`resolved_type_to_c(rt)==v`),
  иначе — `Raw`-долг. ⇒ выход byte-identical прежнему all-`Raw` ПО ПОСТРОЕНИЮ, но каждая принятая
  запись заменяет `Raw` настоящей структурной правдой. Это НЕ инверсия §0 (RT рождается из канала/
  `from_type_ref` НЕЗАВИСИМО; сравнение — только предохранитель байт-идентичности). `NOVA_A1PP_TRACE`
  считает adopt/raw.

### 9.2 Продюсеры (ФАКТЫ)

**Переведено — 3 канал-доступных subst-сидера** (RT из канала resolved_types + турбофиш `from_type_ref`):
| Продюсер (fn) | Источник RT | Сид `current_type_subst` |
|---|---|---|
| `try_generic_static_ctor_mono` | канал-арг (`args` несут ExprId) vs template-param `TypeRef` | arg-emission subst |
| free-fn generic-call inner-subst (`resolve_mono_type_args`-путь, ~30881) | турбофиш `from_type_ref` + канал-арг | fn-typed-param inner |
| mono-method static-overload inner-subst (~30593) | канал-арг | fn-typed-param inner |

**Осталось `Raw` (объективно не переводимо до A1‴/A2) — с точечной пометкой:**
- **worklist-fed body-эмиссия** (5 сайтов: `emit_monomorphized_*` :16613/16693/16782/17420/17985) —
  `type_subst` = строковый параметр mono-worklist (**A1‴**: RT приходит, когда реестры RT-native).
- **method-dispatch receiver-elem** (~8 сайтов :26771/27043/27068/27802/28729/29215/29250/29880) —
  `type_subst` = receiver-elem C-строка (из реестра/receiver-контекста) + method-extras; receiver-половина
  строково-нативна (**A1‴**).
- **прямые `Self`/generic-param инсерты** (:6952/6969/16546/16649) — `self_c`/`t_c_ty` C-строка из
  receiver-контекста (**A1‴**).
- **guard-fallback** внутри переведённых продюсеров — channel-miss / lowering-mismatch (напр. Ident-арг с
  `var_types`-override, closure-param, arg без канал-аннотации) → `Raw`. Легальный инкрементальный остаток.

⇒ **`Raw`/`lift_c_name`/`subst_map_from_c_pairs` НЕ удалены** (всё ещё производятся всеми перечисленными).
Удаление — финал A1‴ (когда worklist/реестры RT-native, а `Self`/receiver-контекст структурируется).

### 9.3 Гейты (все зелёные)

- Сборка Rust — чистая (nova-codegen + nova-cli).
- **Эталонные:** `test --positive --compile-error spec_tests/conformance` = **54 PASS / 0 FAIL** (после правки).
- **Byte-identity** (базис `60f03670`, `compile --no-annotate-source`): conf+examples **272/0**;
  mono-heavy nova_tests (generics/plan91/plan91_12/plan98/plan99/plan153_5/plan55/plan138_2/plan101_1)
  **135 сравнено, 0 реальных расхождений** (5 видимых = сдвиг номера строки в `panic!`-стабе legacy-
  neg-тестов на +170 = ровно объём вставки + недетерм. thread-PID; после нормализации PID+panic-line = 0).
- **δ регрессий = 0:** 4 FAIL в широкой выборке (`plan91/sort_basic`, `plan55/f1_closure_array_gc_stress`
  — retired `str.len()` D249; `plan55/neg/f2` — toolchain linker-msg; `plan153_5/flatten` — str-assert) —
  **pre-existing, byte-identically в базисе** (эти файлы компилируются 0-diff) → не регрессия A1″.
- **Yield:** adopt=2 (T=nova_int → реальный `Scalar{64,true,true}`), raw=7 на измеренной mono-выборке.
  Реальный RT течёт byte-identical там, где канал согласуется; остальное — `Raw`-долг.

### 9.4 Точка возобновления (заход 5 стартует ОТСЮДА)

A1″ частично закрыт: структурный путь `infer_type_param_binding_rt` + byte-identity-guarded adoption
проведён в 3 канал-доступных сидера (реальный RT в `current_type_subst`, δ0). **Основная масса subst
остаётся `Raw`** — сидеры body-эмиссии и receiver-контекста строково-нативны (worklist/реестры). Это НЕ
блокер (§0-инкрементальность легальна на продюсерах): носитель един, окна строка↔RT нет. **Следующий —
A1‴ (реестры/worklist RT-native):** `mono_worklist`/`generic_type_instance_info`/`generic_type_worklist`
несут `Vec<ResolvedType>` вместо `type_args_c: Vec<String>`; тогда body-сидеры сеют RT ПО ПОСТРОЕНИЮ, а
`receiver_subst`/`Self` резолвятся структурно → вытесняется остаток `Raw` → удаляются
`lift_c_name`/`subst_map_from_c_pairs`/вариант `ResolvedType::Raw`. Затем A2 (принтер TypeParam из
RT-контекста) → A4 → A5. Инструмент: `NOVA_A1PP_TRACE` (adopt/raw tally).

---

## Заход 5 (A1‴ — реестры/worklist RT-native ЗАКРЫТ) — 2026-07-06

**Ветка:** `track-a-172-12` (`nova-p173`), от `882787ba` (main, вобрал A1′/A1″). **Итог: A1‴ реализован и зелён.**
Все ТРИ mono-реестра переведены на структурную `Vec<ResolvedType>`-идентичность ЦЕЛИКОМ (носитель един,
без окна строка↔RT). Изменён ТОЛЬКО `codegen/emit_c.rs` (`types/mod.rs` не трогался). Byte-identical **32/32**.

### 10.1 Переведённые носители (тип ключа до→после)

| Реестр | До | После |
|---|---|---|
| `mono_worklist` | `Vec<(String, Vec<(String, **String**)>, String)>` | `Vec<(String, Vec<(String, **ResolvedType**)>, String)>` |
| `generic_type_worklist` | `RefCell<Vec<(String, Vec<**String**>, String)>>` | `RefCell<Vec<(String, Vec<**ResolvedType**>, String)>>` |
| `generic_type_instance_info` | `RefCell<HashMap<String, (String, Vec<**String**>)>>` | `RefCell<HashMap<String, (String, Vec<**ResolvedType**>)>>` |

C-имя (3-й элемент worklist / КЛЮЧ instance_info) остаётся `String` — это ВЫВОД принтера
(`compute_generic_type_c_name` из лоуэренных args), не носитель типа. Args-вектор — теперь RT.

### 10.2 Механизм (§0 — единая точка долга, читатели через принтер)

- **Продюсеры** (все string-native, §7.2 подтверждён): фризят УЖЕ ЛОУЭРЕННУЮ C-строку через
  `args_lift(&[String])->Vec<Raw>` (реестры) / `subst_vec_from_c_pairs(&[(String,String)])->Vec<(String,Raw)>`
  (worklist) — единственная точка подъёма C→`Raw`, аналог `lift_c_name` из A1′. **Контекст-независимый
  фриз** (не re-lowering на чтении): отложенное лоуэрение re-связало бы residual type-param под дивергентным
  mono-контекстом (gap #2 subst-timing) — потому byte-identical ПО ПОСТРОЕНИЮ, не «может быть».
- **Читатели** (~40 сайтов): лоуэрят каждый arg через `arg_c(&rt)` (=`subst_val_c`: `Raw`→вербатим,
  реальный RT→принтер). Принтер-арм `TypeParam` (:2376) и `resolved_array_to_c`/`vec_nesting_depth`/
  `infer_type_param_binding`-фидеры/instance_opt-zip'ы/`drain_generic_type_worklist`/пары дублей
  `infer_expr_c_type` (40xxx≡44xxx) — все через `arg_c`.
- **Один genuine RT-flow-through** (:7211): реестр-RT течёт ПРЯМО в RT-типизированный `current_type_subst`
  (`or_insert_with(|| a)`, без lift/lower) — Raw остаётся Raw, byte-identical, но окна нет.
- `drain` re-insert worklist→instance_info (:17538) — оба борта RT → `type_args_c.clone()` без конверсии.

### 10.3 Судьба `Raw`/`lift_c_name` — НЕ выпилены (ожидаемо)

Все продюсеры реестров рождают type-arg C-строками (`type_ref_to_c`/`infer_expr_c_type`/split мангла /
`apply_type_subst_to_ref`) — **ни одного RT-native продюсера реестра** (ровно вывод заходов 2/7.2). Решение
НЕ хранить native RT у принтер-сайд-продюсеров (`resolved_array_to_c`/`resolved_named_to_c`, у которых RT
формально есть) — сознательное: их RT может нести residual `TypeParam`, а read-time лоуэрение под другим
mono-контекстом дало бы дивергенцию (не byte-identical). ⇒ `Raw`/`lift_c_name`/`args_lift`/
`subst_vec_from_c_pairs` ОСТАЮТСЯ; носитель RT-типизирован, значения — `Raw`-долг (как `current_type_subst`
после A1′). Выпил `Raw` — когда продюсеры станут RT-native через канал-инференцию/структурный subst (A2+).

### 10.4 Гейты (все зелёные)

- Сборка Rust — чистая (nova-codegen + nova-cli; только pre-existing dead-code warnings, мои хелперы used).
- **Эталонные:** `test --positive --compile-error spec_tests/conformance` = **54 PASS / 0 FAIL**.
- **Byte-identity** (базис `882787ba`, `build --keep-artifacts`, нормализация per-run `build-<hash>`):
  **32/32 идентичны** — generics/plan91/plan98/plan99/plan55/plan138_2/plan101_1/plan153_5 (14) +
  serde/plan_value_iter/plan91_12(turbofish)/plan172_composition/io/examples (10) +
  plan95/plan153_2/plan101_1/plan59/plan148 (sum/value-record/newtype, 8). 0 содержательных расхождений.
- **δ регрессий = 0:** `nova test` на plan98/generics — baseline == A1‴ (identical PASS/FAIL); byte-identity
  ⇒ идентичный .c ⇒ идентичное поведение.

### 10.5 Точка возобновления (заход 6 стартует ОТСЮДА)

A1‴ закрыт: три реестра RT-типизированы ЦЕЛИКОМ, C-имя — вывод принтера, δ0. Значения args — `Raw`-долг
(структурная правда придёт с канал-инференцией продюсеров). **Следующий — A2 (принтер `TypeParam`-арм из
RT-контекста):** `resolved_type_to_c` арм `TypeParam` (:2348) выводит subst из `Vec<ResolvedType>`-контекста
реестра (concrete→mangle, residual `TypeParam`→Err/erased), снос `is_generic_stub_c`/`contains("____")`
ВНУТРИ принтера. Гейт: container-класс byte-identical + 54/0 + δ0. Разблокирует SelfAccess/Ident-флип
(172.1.1, отдельным A2b под soundness-гейтом). Затем A4 (снос ~140 потребительских `____`/`Nova_`-решений,
армы 6c–6z) → A5 ([M-array-vec-unify]). Инструменты: `NOVA_U45_RLCHECK`, `NOVA_TP_TRACE`; базис-бинарь `882787ba`.

---

## Заход 6 (A2a — структурная concreteness принтера ЗАКРЫТ; A2b receiver-арм = граница) — 2026-07-06

**Ветка:** `track-a-172-12` (`nova-p173`), от `a1a61f5f` (main, вобрал A1‴). **Итог: A2a реализован и зелён.**
Все ЧЕТЫРЕ concreteness-решения принтера (армы `Tuple`/`Option`/`Result`/Vec-flip) выводятся из СТРУКТУРЫ
`ResolvedType` + concrete-type-реестров, БЕЗ разбора `Nova_`/`____` C-строки в теле принтер-функций.
Изменён ТОЛЬКО `codegen/emit_c.rs` (+149/−22). A2 расщеплён на две РАЗНЫЕ семьи решений: **A2a** (concreteness
элемента/inner — закрыт) и **A2b** (mono-резолюция `TypeParam` из receiver-контекста — граница, ниже §11.4).

### 11.1 Что переведено (структурный предикат вместо re-parse C-строки)

Новый структурный предикат `rt_is_erased_stub(rt, full)` (+ `rt_named_is_stub` — зеркало дерева
`resolved_named_to_c`): «лоуэрится ли `rt` в bare `Nova_<param>*`-заглушку (то, что ловит `is_generic_stub_c`)
или в concrete-тип?» — решается по СТРУКТУРЕ `ResolvedType` и тем же реестрам (`record_schemas`/`sum_schemas`/
`generic_types`/`being_defined_sum_types`/`opaque_ffi_types`/`generic_type_templates`/`protocol_types`), `full`
выбирает набор (`true` = набор `is_generic_stub_c`; `false` = узкий Option/Tuple: record/sum/generic). Переведены:

| Арм (fn) | Было (разбор C-строки) | Стало |
|---|---|---|
| `Tuple` (`resolved_type_to_c`) | `c.trim_end_matches('*').strip_prefix("Nova_")` + `contains("____")` | `rt_is_erased_stub(e, false)` |
| `Option` (`resolved_named_to_c`) | `inner_c.strip_suffix('*').strip_prefix("Nova_")` + `contains("____")` | `rt_is_erased_stub(inner, false)` |
| `Result` (`resolved_named_to_c`) | `is_generic_stub_c(&ok_c)` / `is_generic_stub_c(&err_c)` | `rt_is_erased_stub(&args[0/1], true)` |
| Vec-flip (`resolved_array_to_c`) | `is_generic_stub_c(&elem_c) && !elem_c.contains("____")` | `rt_is_erased_stub(inner, true)` |

`void*`/empty-проверки (`inner_c=="void*"`, `ok_c.is_empty()`) ОСТАВЛЕНЫ inline — это plain-equality, не
`Nova_`/`____`-разбор (вне grep-инварианта), и порядок (void* до stub) сохранён байт-в-байт.

### 11.2 §0 — единая помеченная точка долга

`debt_lowered_is_stub(rt, full)` — ЕДИНСТВЕННЫЙ маркированный debt-sink: классифицирует по C-имени ТОЛЬКО
формы, чья concreteness всё ещё string-carried на mono-слое (`Named`/type-param через строковый
`current_type_subst`/overrides/alias, module-qualified colliding-имя, `Self`, `TypedPtr`, `Raw`). Воспроизводит
pre-A2 inline-проверку ТОЧНО (`is_generic_stub_c` для `full`; узкий record/sum/generic-`____` вариант иначе) →
byte-identical ПО ПОСТРОЕНИЮ. Умирает, когда продюсеры subst/реестров станут RT-native (`Raw` выпилен). Для
concrete-форм (Scalar/user-`Named`/mono-`Named`/`Array`/`Tuple`/builtin) решение ЧИСТО структурное — sink не
достигается.

### 11.3 Grep-инвариант принтера (ФАКТ, после A2a)

- `resolved_array_to_c`: **0** `Nova_`/`____`-разборов (было 1: `is_generic_stub_c`+`contains`). ЧИСТ.
- `resolved_named_to_c`: **0** (было 3: Option-strip + Result `is_generic_stub_c`×2). ЧИСТ.
- `resolved_type_to_c`: **5 остаточных — ВСЕ в арме `TypeParam` RECEIVER-контекста** (`trim_start_matches("Nova_")`
  ×2, `strip_prefix("NovaArray_")`, `contains("____")`, `format!("Nova_{}")`). Tuple-арм ЧИСТ.
- `is_generic_stub_c` в телах трёх принтер-функций = **0** (снесены 3 прямых вызова; 1 остался в debt-sink).

### 11.4 Гейты (все зелёные)

- Сборка Rust — чистая (nova-codegen + nova-cli; только pre-existing warnings).
- **Эталонные:** `test --positive --compile-error spec_tests/conformance` = **54 PASS / 0 FAIL**.
- **Byte-identity** (базис `a1a61f5f`, `build --keep-artifacts`, stub-clang для .c без cc/линка; нормализация
  build-hash + `emit_c.rs:<N>` compiler-source-line): **42/42 файла, 0 содержательных расхождений** — Option/
  Result/Tuple (plan98/plan99/plan59/plan95/plan148) + Vec/Array/iterator (plan138_2/plan153_5/plan153_5_nested/
  plan101_4) + composition/examples. 3 видимых .err-diff = ТОЛЬКО сдвиг compiler-source-line P67-panic на +127
  (= объём вставки; тот же span/сообщение/file_id) — pre-existing P67-LEGACY, не регрессия.
- **δ регрессий = 0:** `nova test` base-vs-new — plan59 7/0=7/0, plan153_5 0/1=0/1 (pre-existing fail),
  plan148 1/0=1/0, plan98 1/0=1/0. Идентично.

### 11.5 Точка возобновления (заход 7 стартует ОТСЮДА) — A2b

A2a закрыт: concreteness принтера структурна, две принтер-функции ЧИСТЫ, `is_generic_stub_c` вне тел принтера.
**Следующий — A2b (`TypeParam` receiver-арм):** 5 остаточных `Nova_`/`____` в `resolved_type_to_c` арм
`TypeParam` — это НОРМАЛИЗАЦИЯ ключа реестра из `current_receiver_type: Option<String>` (декорированная C-строка
`Nova_X*`/`NovaArray_<elem>`) для лукапа `generic_type_instance_info` + slice-extension спецкейс. Реестр УЖЕ
RT-native (args: `Vec<ResolvedType>`, A1‴) — блокер только строковый КЛЮЧ-носитель `current_receiver_type`.
Снятие требует структурного receiver-носителя `current_receiver_rt: Option<ResolvedType>` (сет в ~20 сайтах, где
сейчас `.replace(recv.type_name.clone())` — String); значения будут `Raw`-долг до RT-native продюсеров инстанса
(A1′-масштаб substrate, НЕ concreteness-патч) — потому вынесено в отдельный заход под byte-identity-гейт. Затем
A4 (снос ~140 потребительских `____`/`Nova_`) → A5 ([M-array-vec-unify]). Базис-бинарь A2a-регресса: `a1a61f5f`.

---

## Заход 7 (A2b — структурный receiver-носитель ЗАКРЫТ; uint-раскол разобран → маркер) — 2026-07-07

**Ветка:** `track-a-172-12` (`nova-p173`), от `712eb32d` (main, вобрал D409/184-1/of-guard/D410 + в ходе захода —
audit-182 test-move + vec-sweep `65cf91fb` с правками emit_c.rs; merge без конфликтов в emit_c.rs). **Итог: A2b
реализован и зелён.** 5 остаточных `Nova_`/`____` в `resolved_type_to_c` арм `TypeParam` вынесены в помеченные
receiver-debt хелперы, читающие СТРУКТУРНЫЙ носитель `current_receiver_rt`. Изменён `emit_c.rs` + `backlog-followups.md`.

### 12.1 A2b — механизм (§0, единый носитель, byte-identical по построению)

- `current_receiver_rt: Option<ResolvedType>` — структурный близнец строкового `current_receiver_type`,
  синхронизируемый `sync_receiver_rt()` ПОСЛЕ каждой мутации (**29 сайтов** сет+restore; grep-проверено — 0
  мутаций без следующего sync). Значение = `Raw(current_receiver_type)` (продюсеры инстанса ещё строковые →
  `Raw`-долг через `lift_c_name`, аналог A1′). Строковый носитель ОСТАЁТСЯ для ~70 прочих читателей (A4).
- Нормализация ключа реестра вынесена в **марк. receiver-debt хелперы**: `debt_receiver_bare` (bare-ключ:
  `Raw`→trim `Nova_`/`*`, структурный `Named`→принтер), `debt_receiver_typeparam` (slice-extension `NovaArray_<elem>`
  структурно для `R::Array` / `Raw`-разбор + `generic_type_instance_info`-лукап через bare), `debt_receiver_erased`
  (erased-контекст `[]T`/template-carry). Для `Raw`-значений разбор ИДЕНТИЧЕН пре-A2b инлайну → byte-identity ПО
  ПОСТРОЕНИЮ; структурные армы (`R::Array`/`R::Named`) дремлют до RT-native продюсеров.
- **Grep-инвариант (ФАКТ):** `resolved_type_to_c` тело = **0** `Nova_`/`____` decode-операций (было 5);
  `trim_start_matches("Nova_")`/`strip_prefix("NovaArray_")`/`contains("____")`/`format!("Nova_{}")` — ТОЛЬКО в
  receiver-debt хелперах (+ прежний `debt_lowered_is_stub`).

### 12.2 uint-раскол (TASK-2) — РАЗОБРАН, правка ретрагирована, заведён маркер

Задача просила заменить 5 `"uint" => "uint64_t"` (element-key арм legacy-`NovaArray`) на `nova_uint`. Проверкой
установлено: **правка ломает `Vec[uint].new()/.push()`** — `nova_rt/array.h` физически несёт фикс-набор
`NOVA_ARRAY_DECL(uint64_t)`, НЕ `nova_uint` → `NovaArray_nova_uint` undeclared (`unknown type name`); runtime-decl
`nova_uint` ещё и переопределил бы compiler-emitted mono `NovaOpt_nova_uint` (Vec-flip путь эмитит его сам). `uint`
канонически = `nova_uint` НА Vec-flip/примитив-пути (`Nova_Vec____nova_uint`, `.of()`/subscript — d403 уже зелёный там);
legacy-`NovaArray` backing остаётся `uint64_t`, ABI-идентично на 64-бит. **Правка ретрагирована к baseline**, 5 арм
несут one-line note + маркер **`[M-uint-legacy-array-uint64-until-a4]`** (backlog-followups.md, home 172.12 A4). Проба
дуальности (owner req): `NovaArray_uint64_t` (static-call) и `Nova_Vec____nova_uint` (type/Vec-flip) СОСУЩЕСТВУЮТ в
одном CU; при форс-встрече (`Nova_Vec____nova_uint* a = nova_array_new_uint64_t(8)`) — incompatible-pointer-**name**,
но layout-идентично (`{ptr; nova_int len; nova_int cap}`) → компилит+линкует+рантайм-корректно. Pre-existing
(baseline-identical), класс `[M-array-vec-unify]`; НЕ живой U.4.3-клэш.

### 12.3 Гейты (все зелёные)

- Сборка Rust — чистая (nova-codegen + nova-cli; только pre-existing warnings).
- **Эталонные:** `test --positive --compile-error spec_tests/conformance` = **53 PASS / 0 FAIL** (новая база после
  audit-182 test-move; d403 Vec[uint] зелёный внутри позитивного CU).
- **Byte-identity** (базис = main-дерево `emit_c.rs` собрано отдельно, `build --keep-artifacts`): полный корпус 60 файлов
  (plan98/99/59/95/148/138_2/153_5/153_5_nested/172_composition/generics-101_4/examples) — 54 unsorted-identical + 6
  unsorted-divergent-но-**sorted-identical** (эмиссия-ordering нондетерминизм `[M-codegen-emission-nondeterminism]`,
  подтверждено same-binary control = 0) ⇒ **content byte-identical 60/60**.
- **δ регрессий = 0:** content-identity ⇒ идентичное поведение; conformance подтверждает; ретро-проба (файл, ломавшийся
  под-uint-правкой) снова компилится после ретракции.

### 12.4 Точка возобновления (заход 8 стартует ОТСЮДА) — A4

A2b закрыт: `resolved_type_to_c` тело БЕЗ `Nova_`/`____` decode (всё в помеченных debt-хелперах); receiver-носитель
RT-типизирован (`Raw`-долг). **Следующий — A4 (Ф.4, снос ~140 потребительских `____`/`Nova_`-решений):** армы 6c–6z
замещаются IR-лоуэрингом из носителя; `legacy_inner` уже panic → удалить поднятые дубли ([M-172.1-lifted-legacy-arms]).
В A4 ЗАКРЫВАЕТСЯ и `[M-uint-legacy-array-uint64-until-a4]` (снос legacy element-key арм → `.new()` тоже Vec-flip →
`nova_uint`). Затем A5 ([M-array-vec-unify]). Инструменты: `NOVA_U45_RLCHECK`, `NOVA_TP_TRACE`; базис = main-дерево.

---

## Заход 8 (A4 — потребительские `____`/`Nova_`-решения ЗАКРЫТЫ; uint-маркер ПЕРЕАДРЕСОВАН в A5) — 2026-07-07

**Ветка:** `track-a-172-12` (`nova-p173`), от `ae95d2aad` (main, вобрал заход-7 + ref-184-f2 finalize). **Итог: пункт 1
состава (снос потребительских decode-сайтов) ЗАКРЫТ ПОЛНОСТЬЮ — греп-инвариант 129 → 0.** Пункт 3 (uint-маркер)
переисследован, вывод заход-7 подтверждён НЕИЗМЕННЫМ (`nova_rt/array.h` физически не несёт `NOVA_ARRAY_DECL(nova_uint)`)
→ маркер НЕ закрыт, переадресован в A5 (обоснование ниже, §13.4). Два коммита (`d47250eae`+`8c7df4f63`).

### 13.1 Замер ДО (переоценка заход-1 estimate)

Заход-1 (2026-07-04) оценил объём в ~140 потребительских сайтов, считая ТОЛЬКО decode-паттерны (`trim_start_matches
("Nova_")`/`strip_prefix("Nova_"|"NovaArray_")`/`contains("____")`) — БЕЗ `format!("Nova_{}` (заход-1 таблица не включала
конструкцию). Свежий замер на актуальном дереве (после заходов 2–7 + промежуточные main-волны D410/184/record-elem-fix):

| Паттерн | Всего | Уже в debt (заход 7) |
|---|---:|---:|
| `trim_start_matches("Nova_")` | 30 | — |
| `strip_prefix("Nova_")` | 59 | — |
| `strip_prefix("NovaArray_")` | 22 | — |
| `contains("____")` | 25 | — |
| **decode-сумма** | **136** | **7** (`debt_lowered_is_stub`×2, `debt_receiver_bare`×1, `debt_receiver_typeparam`×4) |
| `format!("Nova_{}` (КОНСТРУКЦИЯ, не декод) | 147 | — |

**Решение по классификации (реши по коду):** `format!("Nova_{}`-конструкция ВЫВОДИТ новое C-имя из уже известного
структурного источника (base-name/RT) — это работа принтера/мангла, НЕ «решение по существующей C-строке» (мотивация
172.12 явно называет цель: `strip_prefix`/`contains` decode, не конструкция). Итоговая рабочая база — decode-сумма
**136, вне debt 129** — практически совпадает с заход-1 оценкой (~140), подтверждая, что заход-1 таблица (не заход-8
инструкция) была калиброванной целью. `format!("Nova_{}`-конструкция (147, преимущественно легитимный mangle) —
ЗАФИКСИРОВАНА как отдельная, не декодная категория; не тронута (документировано здесь, не спрятано).

### 13.2 Механизм (§0 — тот же метод, что A2a/A2b: rename в debt_ для чистых string-utility, extract-function для
встроенных сайтов)

Два класса потребителей, разное лечение:

- **Класс А (20 функций, ЦЕЛИКОМ string-utility, сигнатура `c_type: &str → String/bool`, вызывающий структурного
  источника (RT) не имеет):** `per_e_mangle`, `typeid_macro_for`, `register_any_typeinfo`, `call_target_c_name`,
  `call_protocol_params_key`, `c_type_to_nova_name`, `mono_short_name`, `mono_type_args_of`, `value_generic_mono_short`,
  `compute_field_array_elem_type`, `struct_name_from_c_type`, `lookup_variadic_arity`, `is_novaopt_npo`,
  `is_late_emitted_value_payload`, `opt_payload_needs_structural_eq`, `strip_recv_c_prefix`, `nova_type_name_from_c`,
  `mangled_has_nested_placeholder`, `find_variant_ctx`, `current_fn_return_sum` + `is_generic_stub_c` (39 call-сайтов) —
  **МЕХАНИЧЕСКИЙ rename** в `debt_<имя>` (0 изменений логики, byte-identical по построению — чистое переименование).
  Обоснование: RT-native замена сигнатуры этих функций (`&str`→`&ResolvedType`) потребовала бы транзитивно менять ВСЕХ
  их вызывающих (многие из которых сами оперируют только C-строкой на этом этапе) — это A1′-масштаб substrate-работы,
  не A4-ripple (§0 п.4 явно санкционирует debt вместо расширения захода).
- **Класс Б (~65 сайтов, ВСТРОЕННЫЕ в бо́льшие функции — emit_module/emit_field_eq/register_mono_method_instance/
  emit_monomorphized_method/emit_for/pattern_bind_typed/pattern_cond/emit_record_lit/infer_expr_c_type/emit_call/
  emit_expr и др.):** **extract-function** — точный код без изменений вынесен в новый именованный `debt_*`-хелпер,
  вызов на месте. Найдено и вылечено ОДНИМ replace_all несколько ДОСЛОВНЫХ ДУБЛЕЙ (одна и та же логика повторена в
  двух местах внутри infer_expr_c_type/emit_call — видимо, остаток истории «поднятых армов» из 172.1): например
  `obj_ty_pre.strip_prefix("NovaArray_")`-блок (2 копии), `rt.strip_prefix("NovaArray_")` (3 копии),
  `bare.strip_prefix("NovaValue_").or_else(Nova_).or_else(NovaTuple_)` (4 копии), `struct_name`-тройная-цепочка
  (2 копии) — каждая пара/тройка ушла в ОДИН debt-хелпер, вызванный со всех сайтов.
- Несколько сайтов оказались **stale-комментариями** (документация буквально цитирует `strip_prefix("Nova_")`/
  `trim_start_matches("Nova_")` как текст, описывая УЖЕ переведённый на структуру код) — перефразированы без
  изменения смысла (не декод-код, false-positive грепа).

### 13.3 ФАКТЫ (грep-инвариант, до/после)

| Замер | decode вне debt |
|---|---:|
| ДО (после мержа заход-7 + main-волн, до правки) | **129** |
| После батча 1 (20 rename + is_generic_stub_c + ~65 extract вне гигантской тройки) | **18** (все — в infer_expr_c_type/emit_call/emit_expr) |
| После батча 2 (те же паттерны ВНУТРИ Channel 6c-6z диспетчера) | **0** |

`debt_`-функций в файле: **51** (было 4 после заход-7: `debt_lowered_is_stub`/`debt_receiver_bare`/
`debt_receiver_typeparam`/`debt_receiver_erased`). Δ = 47 новых точек долга (20 rename + `is_generic_stub_c` rename +
~26 новых extract-хелперов, часть покрывает несколько call-сайтов через replace_all на дублях).

**Армы 6c–6z / [M-172.1-lifted-legacy-arms]:** разведка ПОКАЗАЛА — литерального `legacy_inner`-wrapper'а (как описано
в заход-1 карте, состояние на 2026-07-04) в коде уже НЕТ: последующие заходы 172.1 FIN влили его тело НАПРЯМУЮ в
`infer_expr_c_type` (сейчас ОДНА функция 40608→47962, ~7350 строк, Channels 1-6 → 6b–6z → generic ExprKind-fallback).
Комментарии, упоминающие `legacy_inner`, — STALE (описывают состояние ДО слияния). Нет «недостижимого кода после
panic» для удаления — весь код в этой функции ДОСТИЖИМ (реальная fallback-логика для разных ExprKind, не мёртвые
дубли). Замещение IR-лоуэрингом (п.2 состава, «армы 6c-6z → IR») — это отдельный, много-больший горизонт (буквально
Ф.1-Ф.4 всего зонта 172.12, не один заход); что СДЕЛАНО заходом 8 — устранены ВСЕ raw string-decode внутри этих арм
(тот же класс работы, что A2a/A2b делали для принтера), что является ПРЕДПОСЫЛКОЙ для будущего IR-лоуэринга, а не им
самим. `[M-172.1-lifted-legacy-arms]` остаётся ОТКРЫТЫМ (носитель дисциплины — Ф.2/Ф.3 172.12), но substrate для его
закрытия (структурные debt-точки вместо россыпи ad-hoc decode) готов.

### 13.4 uint-маркер `[M-uint-legacy-array-uint64-until-a4]` — ПЕРЕАДРЕСОВАН В A5 (не закрыт)

Переисследование (не с нуля — проверка, что вывод заход-7 не устарел): `nova_rt/array.h` **по факту** (2026-07-07)
несёт `NOVA_ARRAY_DECL(uint64_t)` (:292), НЕ `NOVA_ARRAY_DECL(nova_uint)`. `nova_uint` = `typedef uintptr_t nova_uint`
(`nova_rt.h:18`, address-sized — Plan 133, НЕ фиксированный 64-бит: на 32-бит платформе `uintptr_t`≠`uint64_t` шириной).
Блокер заход-7 ПОДТВЕРЖДЁН НЕИЗМЕННЫМ: снос 5 legacy element-key арм (замена `"uint" => "uint64_t"` на `"uint" =>
"nova_uint"` в `receiver_c_type`/аналогах) требует ЛИБО (a) добавления `NOVA_ARRAY_DECL(nova_uint)` в рантайм (новая
специализация, риск коллизии имени с mono-emitted `NovaOpt_nova_uint`/`Nova_Vec____nova_uint` — заход-7 §12.2 уже
пометил этот риск), ЛИБО (b) `.new()`/`.push()` для `[]uint` целиком переводятся на Vec-flip путь (но `receiver_c_type`
однородно обрабатывает ВСЕ примитивы `[]T` через ОДИН legacy `NovaArray_<c>*`-путь — заход-8 §комментарий :13680
«single-level `[]T` case … UNCHANGED (legacy `NovaArray_<elem>*`), so flat dispatch stays byte-identical»; менять
ТОЛЬКО uint внутри этой однородной ветки создало бы асимметрию с соседними примитивами (int/str/bool/…), не
локальный фикс). Оба варианта — cross-cutting rantime+codegen работа с широким test-surface (Vec[uint].new/push/get/
iter + взаимодействие с параллельным Vec-flip представлением), НЕ codegen-consumer ripple уровня A4 (§0 п.4:
«вскрывается зависимость масштаба… — не расширяй заход»).

**Решение (реши по коду, задокументировано):** маркер НЕ закрывается в A4; корень проблемы (`NovaArray`/Vec dual
representation для примитивов) — это ТОЧНО предмет **A5 [M-array-vec-unify]** («NovaArray/Vec-раздвоение решается на
IR-уровне, одно представление, два принтера → одно»). Естественнее закрыть uint-маркер КАК ЧАСТЬ A5 (тот же root
cause), не как отдельный pre-A5 патч. `backlog-followups.md` запись обновлена (см. коммит) — носитель осознанно
смещён с «172.12 A4» на «172.12 A5».

### 13.5 Гейты (все зелёные)

- Сборка Rust — чистая (`cargo check --release`, только pre-existing warnings) на каждом промежуточном шаге.
- **Эталонные:** `test --positive --compile-error spec_tests/conformance` = **54 PASS / 0 FAIL** (батч 1 и батч 2,
  оба; баз = 54/0, δ0).
- **δ регрессий (`nova test`, база vs итоговый бинарь):** `generics`+`plan98`+`plan99`+`plan59`+`plan95`+`plan148`+
  `plan138_2` = 12/12 идентично (1 pre-existing FAIL `plan153_5/flatten` — str-assert, подтверждено присутствующим
  В БАЗИСЕ); `serde`+`plan_value_iter`+`plan91_12`+`io` = 16/1 идентично (1 pre-existing FAIL
  `serde/autoderive` — import-resolution, подтверждено В БАЗИСЕ). 0 новых регрессий.
- **Byte-identity** (`build --keep-artifacts`, нормализация `build-<hash>`): 25 файлов `generics`/`plan59`/`95`/`98`/
  `99`/`148`/`138_2` — 12 sorted-identical (unsorted-diff = ТОЛЬКО эмиссия-ordering нондетерминизм
  [M-codegen-emission-nondeterminism], подтверждено ранее same-binary control), 13 NOGEN — ВСЕ подтверждены
  ИДЕНТИЧНЫМИ baseline (neg-тесты, ожидаемо; + один общий pre-existing P67-LEGACY panic на `.bare_identity` в shared
  prelude, сдвиг ТОЛЬКО compiler-source-line на +86 = ровно объём вставки, тот же span/сообщение). 0 содержательных
  расхождений.

### 13.6 Точка возобновления (заход 9 стартует ОТСЮДА) — A5

A4 закрыт (потребительские decode-сайты: 129→0, 51 debt-функция, оба батча гейтованы). `[M-172.1-lifted-legacy-arms]`
остаётся классом-открытым (armы 6c-6z существуют, но decode внутри них теперь структурирован через debt-точки —
предпосылка для IR-лоуэринга готова). `[M-uint-legacy-array-uint64-until-a4]` переадресован → закрывается ВМЕСТЕ с
**A5 ([M-array-vec-unify]):** NovaArray/Vec — одно представление из `R::Array` (`resolved_array_to_c` :2233 уже мост
после A2a), `receiver_c_type`'s legacy single-level `[]T`-путь и uint-раскол — тот же root cause. Инструменты:
`NOVA_U45_RLCHECK`, `NOVA_TP_TRACE`; базис-бинарь `ae95d2aad` (сохранён `scratchpad/nova-a4-baseline.exe` в
`nova-p173`, не в git).

---

## Заход 9 (A5 — крашер bench.opaque ЗАКРЫТ; array-vec-unify ЭМПИРИЧЕСКИ подтверждён как широкий substrate) — 2026-07-07

**Ветка:** `track-a-172-12` (`nova-p173`), от `a4dd93b33` (main, вобрал 184-заходы-5-6, крашер-фиксы 182,
porting-волну; merge — fast-forward, конфликтов в emit_c нет). Базис-бинарь A5 = current-tree build
(`scratchpad/nova-a5-baseline.exe`, md5 3771056e…, conformance 66/0 подтверждён). Изменён `emit_c.rs` (+24, коммит
`b9b8008f4`) + этот план + `backlog-followups.md`.

### 14.1 Разведка состава A5 (по коду) — что живо, что уже мертво

- **Крашеры (п.4):** прогон 5 маркер-файлов на A5-базисе — `map_literals/positive_insert_new_correctness` и
  `plan60/f6_array_capacity_method` **УЖЕ НЕ крашат** (закрыты porting/крашер-волной в main до A5). **Живы 3** —
  ровно 3 маркера: `plan57/p2_bench_namespace_callable` (43737, bench.opaque), `plan83_12/tcp_bind_used_port_test`
  (43986, bind), `plan153_4/chunks_windows` (41498, nested-collect).
- **Трипликация диспетчера (п.3) — ПОДТВЕРЖДЕНА структурно:** каждая P67-паника имеет БЛИЗНЕЦА
  (43737/47456, 43986/47705, 41498/45249) — infer-диспетчер существует в ДВУХ почти-идентичных копиях
  (~42500-43700 и ~45800-47300) + третья emit-копия (~28300). ⇒ любой крашер-фикс = 2× правки (или коллапс).

### 14.2 Крашер bench.opaque — ЗАКРЫТ (коммит `b9b8008f4`)

`infer_expr_c_type` паниковал на `bench.opaque(x)`: obj_ty="" (Ident(bench) — namespace, не типизированный
ресивер), канал `resolved_types` пуст (чекер не аннотирует intrinsic), все legacy-армы пропускают пустой ресивер →
P67. `emit_expr` уже лоуэрит в `NOVA_BENCH_OPAQUE_PRIM` (black-box identity); **зеркалю в infer**: return-тип =
тип аргумента (`infer_expr_c_type(args[0])`). Вставлено в ОБЕ триплет-копии (43737+47456) через replace_all.
Гейт: build clean; conformance **66/0** (δ0); bench-тест **P67-ICE → PASS**; byte-identity 4-файла + same-binary
control — **0 реальных расхождений** (арм guarded `method=="opaque" && Ident("bench")` → неактивен для не-bench
кода → байт-идентичность ПО ПОСТРОЕНИЮ; все видимые diff'ы = pre-existing emission-ordering нондетерм.,
подтверждено base-vs-base контролем).

### 14.3 bind + chunks — checker-аннотация/mono-return ГЭП (не emit_c-фиксабельно, честный хвост)

Probe в точке паники (43986) для `TcpListener.bind`: **chan=None** (чекер не пишет `resolved_types[call.id]`),
`fnret_keys=[]`, `mo_keys=[]`, `ext=None` — **у кодогена НЕТ источника правды**, при том что `nova check` файла
**PASS** (чекер знает тип `Result[TcpListener, NetError]`, но не ПЛЮМИТ в канал). Аналогично `chunks`: `nova check`
PASS, но `.collect()` на `BoxIter[Vec[T]]` эрейзит mono-return в `nova_int` (маркер: корень — mono-return
nested-generic collect, НЕ Index-сайт 41498). ⇒ оба — **types/mod.rs** (checker-аннотация / mono-return-инференция),
широкая byte-identity поверхность; НЕ codegen-fallback уровня A4. Fallback-заплатки ретрагированы (проба показала
их неэффективность для bind — источника нет), файл минимален. **Оставлены живыми маркерами** (роботы подтверждены):
`[M-182-crash-pathcall-ret-unknown]`, `[M-182-crash-nested-generic-collect-erase]`.

### 14.4 [M-array-vec-unify] (п.1) + uint (п.2) — ЭМПИРИЧЕСКИ = широкий runtime+codegen substrate (НЕ «механически»)

Оптимистичная нота A5 («после A2 — механический») ОПРОВЕРГНУТА кодом+экспериментом (подтверждает заходы 2/7/8):

1. **NovaArray runtime-встроен, НЕ выпиливаем:** `nova_str_to_chars`→`NovaArray_nova_int*`, `to_bytes`/`bytes`/
   `as_bytes`→`NovaArray_nova_byte*`, `split`→`NovaArray_nova_str*` (`array.h:622-709`); `read_buffer.h`/
   `string_builder.h`/`cast.h` ссылаются на NovaArray (`array.h:776`). Рантайм САМ его производит → снести нельзя,
   только сократить codegen-эмиссию для user-`[]T`.
2. **Единичный флип НЕ унифицирует — даёт Frankenstein.** Эксперимент (concrete-primitive `[]T.new()/.with_capacity()`
   → Vec-путь через `elem_needs_vec_mono: Ok(_)=>true`): собрано, но C = `NovaArray_nova_int* a =
   Nova_Vec____nova_int_static_new();` + `nova_array_push_nova_int(a, …)` — **LHS NovaArray, RHS Vec-ctor, push
   NovaArray**, склеено ТОЛЬКО layout-identity {ptr,len,cap} + C incompatible-pointer-leniency (заход-7 §12.2). Не
   слияние представления, а мешанина. **Ретрагирован** (whole-not-half, прецедент заход-2 «код=0»).
3. **Чистое слияние требует КООРДИНИРОВАННОГО флипа ВСЕХ сайтов** для user-`[]T`: (a) binding-type резолюция
   `[]int`→`NovaArray_nova_int*` (типизирует переменную), (b) `receiver_c_type` single-level `[]T` (:13868-13901,
   dispatch push/get/pop), (c) `.new()/.with_capacity()` (:27509), (d) array-литералы (:35949/:10078/:37109),
   (e) for-loop (:10030) — И примирение с runtime-возвратами (str/buffer → NovaArray). ⇒ **substrate-атом =
   миграция runtime-NovaArray-возвращающей поверхности на Vec-flip** (либо: `Nova_Vec____<prim>` = typedef-алиас
   `NovaArray_<prim>` для примитивов, с подавлением mono-struct Vec для примитивов — обратное направление). Оба —
   cross-cutting runtime+codegen, широкий test-surface, byte-НЕ-идентичный (имена меняются). uint-маркер
   (п.2) закрывается ТОЛЬКО этим (тот же blocker: `array.h` физически без `NOVA_ARRAY_DECL(nova_uint)`).

### 14.5 Гейты (по факту сделанного — bench.opaque)

- Сборка обоих крейтов — чистая (pre-existing warnings).
- Conformance `--positive --compile-error` = **66/0** (δ0 vs A5-базис).
- Byte-identity выборка + same-binary control = **0 реальных расхождений** (guarded-арм).
- bench-тест: **PASS** (был P67-ICE). Регресс-δ = 0 (byte-identity ⇒ идентичный .c для не-bench).

### 14.6 Точка возобновления (заход 10) — остаток трека A

**Трек A НЕ ноль — честный хвост (все — types/mod.rs или широкий substrate, НЕ codegen-consumer уровня A1-A4):**
- **A5 keystone [M-array-vec-unify] + uint (пп.1-2):** substrate-атом = миграция runtime NovaArray-возвратов
  (`nova_str_to_chars`/`to_bytes`/`bytes`/`as_bytes`/`split`, `read_buffer`/`string_builder`/`cast`) на Vec-flip,
  ПОТОМ координированный codegen-флип 5 сайтов (§14.4.3). Направление-развилка (Vec-canon vs NovaArray-typedef-alias)
  — решение владельца (перф/ABI). Отдельный многозаходный трек, НЕ один atom.
- **Трипликация (п.3) [M-emit-c-dispatcher-triplication]:** коллапс 2 infer-копий (~42500-43700 ≡ ~45800-47300) в
  одну — большой byte-identity-рискованный рефактор; предпосылка — сделать до keystone (нота A5), но независим.
- **Крашеры bind/chunks (п.4):** checker-аннотация `resolved_types` для Path-static-call return
  (`TcpListener.bind`) + mono-return nested-generic `collect` — types/mod.rs, byte-identity-гейт на 66/0 + nova_tests.
Базис-бинарь: `scratchpad/nova-a5-baseline.exe` (не в git). Инструменты: `NOVA_U45_RLCHECK`, `NOVA_TP_TRACE`.

## Заход 10 (A6 — str-family runtime-примирение: NovaArray-surface УЖЕ мёртв, снесён + infer-align) — 2026-07-07

**Ветка:** `track-a-172-12` (`nova-p173`), от main (merge — fast-forward). Базис-бинарь A6 =
`scratchpad/nova-a6-baseline.exe` (не в git, md5 308d7078…, conformance 66/0). Изменены `nova_rt/array.h`,
`nova_rt/string_builder.h`, `src/codegen/emit_c.rs` (+ этот план + backlog).

### 14.7 Ключевая находка: str-семейство УЖЕ на Vec (опровергает §14.4.1)

A5 §14.4.1 предполагал «NovaArray runtime-встроен, НЕ выпиливаем» — но это по факту СУЩЕСТВОВАНИЯ функций, не
их использования. Эмпирика A6 (fresh-бинарь, `nova build --keep-artifacts`, инспекция .c):

1. **C-функции str-семейства МЁРТВЫ.** `nova_str_to_bytes`/`bytes`/`as_bytes`/`split`/`to_chars`/`chars`
   (`array.h:622-709`) + `nova_str_from_bytes_unchecked`/`steal_bytes`/`from_bytes_lossy` (`string_builder.h`) —
   **ноль call-site'ов** в генерируемом C, **ноль FFI/extern-деклараций** (все ссылки = doc-комментарии). Retired
   роутингом Plan 139.2: `@bytes`/`@chars`/`@as_bytes`/`@split` + `str.from_bytes_*` = Nova-body, строят **реальный
   `Vec[u8]`/`Vec[str]` через `Vec[T].from_raw_parts`**. Подтверждено: `Nova_str_method_bytes` возвращает
   `Nova_Vec____nova_byte*`, `Nova_str_method_split` → `Nova_Vec____nova_str*`.
2. **Even untyped `ro b = s.bytes()` даёт `Nova_Vec____nova_byte* b = Nova_str_method_bytes(s)`** — checker-канал
   (Channel 2, `resolved_types`) всегда плюмит `[]u8`=Vec и ПЕРЕКРЫВАЕТ legacy infer-таблицу. `[]T` в
   сигнатурах/полях/биндингах уже резолвится `type_ref_to_c`→`Nova_Vec____<elem>*` (D239); `.len()`/`.push()`
   диспатчатся в Vec-методы. ⇒ str-runtime-поверхность **уже 100% Vec**; остаток NovaArray для неё = чистый
   мёртвый код + устаревшие infer-fallback'и.

### 14.8 Архитектурный выбор typedef-слоя — МООТ

Вопрос «(а) `NovaVecImage_<elem>` vs (б) real-mono forward-decl + suppress» **снят фактами**: ни одна рукописная
runtime-функция НЕ обязана называть Vec-mono, потому что str-поверхность уже мигрирована на Nova-body (компилятор
per-CU эмитит `Nova_Vec____*` сам через mono-pass). Rt-typedef-слой не нужен — **ноль ре-деклараций/клэшей** по
построению (нечего декларировать в rt).

### 14.9 Сделано (2 компонента, оба byte-identical на практике)

1. **Снос мёртвых C-функций** (owner: «NovaArray умирает целиком»): 6 в `array.h` (str-producers) + 3 в
   `string_builder.h` (from_bytes_* consumers). **Byte-identical для .c**: `nova_rt/*.h` — реальный `#include`
   (строка `#include "nova_rt/nova_rt.h"`), НЕ splice в текст .c; функции `static inline` + unused → ноль
   object-code изменений. `_nova_validate_utf8` (shared, live на Nova-стороне) НЕ тронут.
2. **Align 8 устаревших infer-fallback'ов** (обе триплет-копии, ~43xxx + ~47xxx): `str.bytes()→Nova_Vec____nova_byte*`,
   `str.split()→Nova_Vec____nova_str*`, `WriteBuffer.into()→Nova_Vec____nova_byte*`,
   `ReadBuffer.remaining_bytes()→Nova_Vec____nova_byte*` (были `NovaArray_*`). Все 4 метода — Nova-body `[]u8`/`[]str`,
   перекрыты каналом → smена имени byte-identical на практике (fallback не срабатывает для well-typed кода). Мотив:
   убрать NovaArray-ссылку + называть корректный тип при channel-miss (аналог precedent'а parse_int/read_X :43258).

### 14.10 Гейты (числами)

- Сборка обоих крейтов (`nova-codegen`, `nova`) — чистая (exit 0, pre-existing warnings).
- Conformance `test --positive --compile-error spec_tests/conformance` = **66/0** (δ0 vs 66/0-эталон; 2 CC-FAIL в
  ранних прогонах = clang temp-file env-флейк на 2 крупнейших тестах, исчезли — не код).
- Byte-identity (40 str/buffer/unicode/encoding .nv, A-методика base-vs-new, normalize build-HASH): **36 identical,
  4 diff, 0 nogen**. Все 4 diff (`std/unicode/{ranges,case,collate,cp_utils}`) = **generic-stub fwd-typedef
  ordering** (`typedef struct Nova_S Nova_S;` переставлены, тот же набор), **доказано base-vs-base контролем**
  (baseline сам себе НЕ идентичен на этих файлах). **Список NovaArray→Vec-образных имён-дельт = ПУСТ** (legacy
  infer-путь полностью перекрыт каналом → смена имени не даёт emitted-C дельты). δ0.
- nova test выборка (strings/str/buffers/unicode/json/serde/compress/plan108/plan145/runtime): все FAIL =
  pre-existing stale-тесты на retired API (`CharsIter.nth`, `str.len()` D249) — падают ИДЕНТИЧНО на baseline
  (проверено), fail на checker-стадии ДО codegen → мой runtime/infer-change их не касается. **δ0 регрессий.**

### 14.11 Точка возобновления (A7) — остаток NovaArray

A6 закрыл str-семейство ЦЕЛИКОМ (owner-приоритет). Остаток = A7 (координированный codegen-флип user-`[]T`):
- **5 сайтов user-`[]T`→NovaArray legacy** (НЕ тронуты, A7): `receiver_c_type` single-level `[]T`
  (`emit_c.rs:13901` → `NovaArray_<elem>*`, единственный оставшийся широкий NovaArray-эмиттер для method-dispatch),
  `.new()/.with_capacity()`, array-литералы ×3, for-loop, + type-map таблица (`:2978-2992`, `:14127`).
- **Снос `NOVA_ARRAY_DECL`/`NOVA_ARRAY_IMPL`-набора** (`array.h`) — после флипа 5 сайтов (user-`[]T` перестанет
  требовать NovaArray). uint-маркер `[M-uint-legacy-array-uint64-until-a4]` закрывается автоматически.
- **De-risk для A7:** str-runtime уже Vec (эта находка) → A7 НЕ нуждается в runtime-порте, только codegen-флип
  + макро-снос. Базис-бинарь: `scratchpad/nova-a6-baseline.exe`.

## Заход 11 (A7 старт — `.new()/.with_capacity()` + `receiver_c_type` координированный флип ЗАКРЫТ) — 2026-07-07

**Ветка:** `track-a-172-12` (`nova-p173`), от main (FF-merge, `4ea0abb46`). Базис-бинарь `scratchpad/nova-a7-baseline.exe`
(md5 `336fd3c5dac6828f80f0287cf3e7a98b`, conformance 66/0). После правок: `12415f463945df0ffbdde4df55232446`.
Изменён только `compiler-codegen/src/codegen/emit_c.rs` (+42/-25).

### 14.12 Эмпирика: объём §14.11-инвентаря был переоценён — 3 из 5 «сайтов» уже Vec-native

Проба (`mut a []int = []int.new(); a.push(1); for x in a {} ; mut b = [1,2,3]; ro c = a == b`) на
БАЗИСНОМ бинаре (до этого захода, никаких правок) показала generated C:
- **binding-type** `a` уже `Nova_Vec____nova_int*` (не `NovaArray_nova_int*`) — `resolved_array_to_c`'s
  D239 Vec-flip (заход 6+) уже решает это для КОНКРЕТНЫХ элементов, primitive включительно.
- **array-литерал** `[1, 2, 3]` уже строится через `Nova_Vec____nova_int_static_new()` +
  `Vec____nova_int_method_push` (`try_emit_typed_vec_literal`, Plan 138.1 Ф.2) — НЕ через `nova_array_new_*`.
- **for-loop** над `a` уже идёт через iterator-протокол (`Vec____nova_int_method_iter` /
  `Nova_VecIter____nova_int_method_next`), НЕ через `emit_for`'s Case-2 `NovaArray_`-gate (та ветка мертва
  для primitive `[]T`, поскольку `arr_ty` уже `Nova_Vec____...`, не `NovaArray_...`).
- **`==`/`!=`/`+` на Vec** уже маршрутизируются через `@equal`/`@plus` Nova-body (комментарий в коде explicit:
  «so this also handles the old `arr == arr` element-wise comparison sites» — написан ЗАРАНЕЕ, в
  предвидении этого самого флипа).

Единственное расхождение с уже-Vec биндингом: **конструктор** `[]int.new()` эмитил `nova_array_new_nova_int(8)`
(легаси runtime-функция из `array.h`) присваиваемый В Vec-типизированную переменную — работало ТОЛЬКО через
layout-совместимость (несовпадающие C type-names, совместимый memory layout; тот же паттерн, что уже
задокументирован для uint-дуальности в backlog). Это и есть настоящий разрыв — не «5 широких сайтов», а
ОДНА точка рассинхрона (ctor) + `receiver_c_type`'s редкий extension-method-receiver арм.

### 14.13 Сделано

1. **`emit_call`'s `[]T.new()/.with_capacity()` legacy_static dispatch** (`emit_c.rs` ~27497-27514, ex
   `elem_needs_vec_mono` → переименован `elem_is_erased`): раньше `legacy_static` = true для ЛЮБОГО
   primitive-элемента («scalars stay on the legacy path»); теперь `legacy_static` = true ТОЛЬКО когда
   `type_ref_to_c(elem)` возвращает `Err` (генуинно нерезолвленный type-param в erased-generic mono'd
   теле — ортогональный int64-slot erasure механизм, НЕ трогается). Все конкретные элементы (primitive
   ИЛИ composite) теперь идут через уже существующую «Vec[T] static machinery» (turbofish-rewrite на
   `Vec[T].new()`/`.new().cap(n)`), которая раньше обслуживала только composite-элементы.
2. **`receiver_c_type`'s `[]T` арм** (~13901, единственный оставшийся широкий NovaArray_-эмиттер для
   method-dispatch receiver'а определений `fn[T] []T @method`): теперь манглит + регистрирует
   `Vec[elem]`-инстанс (mirror `resolved_array_to_c`'s Vec-flip: `compute_generic_type_c_name` +
   `generic_type_worklist`/`generic_type_instance_info` side-effects) вместо `format!("NovaArray_{}*", c_elem)`.
   `uint` теперь мапится в `nova_uint` (было `uint64_t`) — здесь и закрывается
   `[M-uint-legacy-array-uint64-until-a4]` для этого сайта. Соседний pass-through арм
   (`other.starts_with("NovaArray_")`) расширен до `|| other.starts_with("Nova_Vec____")` (defensive dual-accept).

НЕ Frankenstein (в отличие от захода 9's ретрагированного эксперимента): там флипался ТОЛЬКО ctor БЕЗ
выравнивания с уже-Vec биндингом (`NovaArray* = Vec_ctor()` — несовместимая пара на СТОРОНЕ декларации).
Здесь ctor приведён в соответствие с УЖЕ Vec-типизированным биндингом (оба Vec) — никакой склейки типов,
чистое согласование.

### 14.14 Гейты (числами)

- Сборка обоих крейтов (`cd nova-cli && cargo build --release`, env `NOVA_GC_LIB_DIR`/`NOVA_INCLUDE_DIR`
  на main-дерево vcpkg) — чистая, exit 0 (только pre-existing warnings).
- Conformance `test --positive --compile-error spec_tests/conformance` = **66/0** (δ0 vs A6-факт 66/0).
  Явно перепроверены d27/d38/d403/d232 (`d27_fixed_array`, `d38_array_creation`, `d403_vec_sized_element_read`,
  `d232_vec_growable`, `d232_vec_methods`) — все **PASS**.
- uint-гейт: `mut u []uint = []uint.new(); u.push(0x8000000000000000); assert(u[0] == 0x8000000000000000)` —
  PASS, generated C = `Nova_Vec____nova_uint` (не `NovaArray_uint64_t`/`uint64_t`). Marker
  `[M-uint-legacy-array-uint64-until-a4]` ЗАКРЫТ для binding/ctor/receiver путей.
- Wide nova test (strings/buffers/std-encoding-json/serde/compress/std-unicode/plan138_2/plan153_5/plan98/
  plan99/std-net-addr): **PASS 18 / FAIL 2 / SKIP 1**. Оба FAIL — байт-в-байт идентичные сообщения на
  `nova-a7-baseline.exe` (pre-edit): `nova_tests/strings/plan131_vec_access_pos` (CODEGEN-FAIL, тянет соседний
  `plan139_1_str_lang_item_basic_ok.nv` с retired `str.len()` D249) и `nova_tests/plan153_5/flatten`
  (RUN-FAIL, тот же assert fail на baseline) — pre-existing stale-тесты, **δ0 регрессий**.
- Спот-C-check: 2 пробных `.nv` (`probe_arr.nv` — `[]int`, `probe_uint.nv` — `[]uint`) → **0** `NovaArray_`
  hits в generated `.c` для user-кода; единственные 2 текстовых hits в `probe_arr.c` — pre-existing
  `Nova_Vec_static_from(NovaArray_nova_int* items)` bare-`Vec` erasure-sentinel declaration (не вызывается,
  не связан с флипом, существовал бы и без правок).

### 14.15 Точка возобновления (A7 продолжение) — снос `array.h`

Флип пп.1-2 (координированные ctor+receiver сайты) ЗАКРЫТ. Снос `NOVA_ARRAY_DECL`/`NOVA_ARRAY_IMPL`
ОТЛОЖЕН (контингенси-правило плана) — при трассировке найдены **3 доп. блокера**, не бывшие в §14.11-инвентаре
(инвентарь 2026-07-07 недооценил объём, писался ДО этой эмпирики):

1. **Plan 96 parallel-for** (`emit_c.rs` ~9990-10090, `emit_supervised`'s array-mode): и `ArrayLit`-iter
   sub-case (~10005-10039), и bare-`Ident`-iter sub-case (~10041-10048), и result-array allocation
   (~10078-10080) строят/читают сырые `NovaArray_<T>*` локали напрямую (`nova_array_new_<T>`/
   `nova_array_push_<T>`/`->data[i]`/`->len`). Layout-совместимо (работает сейчас через ту же
   dual-naming толерантность), но ссылается на функции/тип из `array.h` — блокер сноса.
2. **Array rest-bind destructuring** (`[a, ..rest, b] = arr`, ~37100-37117): строит `rest_tmp` как
   сырой `NovaArray_<elem>*` через `nova_array_new_<elem>`/`nova_array_push_<elem>` — та же ситуация.
3. **`resolved_array_to_c`'s primitive-табличка** (~2977-2993): dead-in-practice (Vec-flip branch
   above всегда успевает раньше, т.к. Vec template всегда в наличии), но текстуально ссылается на
   `NovaArray_<prim>*` имена — при сносе макросов эти имена перестанут существовать как типы, даже
   если ветка недостижима (типы должны компилиться, раз упомянуты в C).

`emit_array_lit`'s closure/protocol-box/void_p-hint fallback (~35796-35877, ~35945-35991) **НАМЕРЕННО
остаётся NovaArray** — closure-arrays (`[]fn(...)`) и `[]Protocol` heterogeneous-боксы вне области A7
(разные представления, не «user `[]T` primitives»); `NOVA_ARRAY_DECL(void_p)` должен остаться в `array.h`
именно ради них.

**Следующий заход:** перевести блокеры 1-3 на Vec-mangled construction (аналогично этому заходу — мангл +
регистрация worklist вместо сырых `nova_array_new_*`/`NovaArray_<T>*` литералов), затем снос
`NOVA_ARRAY_DECL`/`IMPL` для ВСЕХ primitive-суффиксов КРОМЕ `void_p` (тот остаётся ради closure-arrays).
После сноса — итоговый спот-греп `NovaArray_` по `emit_c.rs` вне комментариев-хроник должен показывать
только: `void_p`-путь, erasure-sentinel (`NovaArray_nova_int` для unresolved type-param), и
исторические doc-комментарии. Базис-бинарь для следующего захода: сохранить текущий пост-A7-старт бинарь
(md5 `12415f463945df0ffbdde4df55232446`) как новую точку сравнения.

## Заход 12 (A8 — 3 блокера ЗАКРЫТЫ + снос array.h ВЫПОЛНЕН) — 2026-07-08

**Ветка:** `track-a-172-12` (`nova-p173`), от main (FF, `31b00182f`). Базис-бинарь
`scratchpad/nova-a8-baseline.exe` (md5 `608969c0152686415dc1f421b6e811d6`, conformance 66/0).
Коммиты: `0292f3694` (блокеры 1-2), `65d165e75` (блокер 3), `99b3d2ce2` (снос + чистка эмиттеров).
Финальный бинарь md5 `b7a33f5debe0647e094a6dfeec002cb8`.

### 14.16 Сделано (A8)

1. **Блокер 1 — Plan 96 parallel-for** (`emit_supervised` array-mode + `emit_parallel_for`,
   ~9990-10130): все 4 суб-сайта (ArrayLit-iter, Ident-iter, result-allocation, ctx-поле
   `_nova_par_result` + запись слота в `emit_spawn`) переведены с сырых
   `NovaArray_<T>*`/`nova_array_new_*`/`nova_array_push_*` на Vec[elem]-мангл. Новый общий хелпер
   `vec_mono_ctor_push(elem_c) -> (mangled, ctor, push)` — регистрация Vec[T]-инстанса в worklist +
   new/push method-инстансов (фактор общий с `try_emit_typed_vec_literal`). Result-буфер аллоцируется
   вручную (`nova_alloc` + pre-size под `len`), НЕ через `.new()+.push()`: параллельные волокна пишут
   слоты по индексу, амортизированный push неприменим; Vec[T]-инстанс при этом зарегистрирован.
   **Проба end-to-end** (Range- и ArrayLit-источники, явная `[]int`-аннотация): все слоты корректны.
2. **Блокер 2 — array rest-bind** (`[a, b, ..rest]`, ~37176-37200): `rest_tmp` теперь Vec[elem]
   (ctor + push-цикл через тот же хелпер). **Проба end-to-end** (`if let` с rest в середине/конце,
   пустой rest, `.push()` на результате): PASS.
3. **Блокер 3 — `resolved_array_to_c` primitive-табличка**: A7-гипотеза «dead-in-practice»
   ОПРОВЕРГНУТА эмпирически — ветка достижима через D62.F partial-prelude (`#prelude(...)` без
   `collections` + bare `[]<prim>` — CU без Vec-темплейта). При этом сценарий НЕ покрыт ни одним
   тестом репо (греп всех `#prelude(`-файлов) и уже нефункционален end-to-end по несвязанным
   partial-prelude пробелам (StringBuilder struct-tag, errors.nv `starts_with`) —
   [M-partial-prelude-primitive-method-registry]-класс. Решение: внешний
   `generic_type_templates.contains_key("Vec")`-гейт снят (не нёс нагрузки — общий резолвер не
   требует Vec-темплейта), оба пути слиты в одну Vec[elem]-конструкцию; контракт «Array never
   errors» для 37 call-sites сохранён (при генуинно отсутствующем Vec-темплейте
   `drain_generic_type_worklist` грациозно скипает неизвестную базу → чистый «unknown type name»
   CC-FAIL вместо ссылки на снесённый runtime). Соседний сайт того же класса —
   `emit_array_lit`'s legacy-fallback (не в 3-блокерном списке, но с тем же Vec-гейтом): non-void_p
   ветка → loud-fail `Err` с диагностикой (ссылка на collections-facade + маркер);
   void_p/closure/protocol-box ветка — БЕЗ ИЗМЕНЕНИЙ (намеренно, по заданию).
4. **Снос `array.h`** (53+/88-, файл 1112→1077 строк; главный эффект — не строки файла, а снятые
   ИНСТАНСЫ: каждый `NOVA_ARRAY_IMPL` разворачивался в ~165 строк мёртвого C в каждом generated CU):
   новый узкий `NOVA_OPT_DECL(T)` (только Option[T]-структура); 11 примитивов
   (char/byte/bool/f64/f32/i32/i16/i8/u32/u16/u64) сняты с `NOVA_ARRAY_DECL+IMPL` на `NOVA_OPT_DECL`;
   ручной `nova_str`-блок (~33 строки) и `nova_array_compare_nova_byte` снесены. ОСТАЛИСЬ:
   `NOVA_ARRAY_DECL/IMPL(nova_int)` (erasure-sentinel) и `NOVA_ARRAY_DECL(void_p)`+хелперы
   (closure/protocol-box). uint-маркер `[M-uint-legacy-array-uint64-until-a4]` закрыт полностью
   (infer-твины теперь тоже Vec).
5. **Чистка ставших мёртвыми эмиттеров emit_c.rs**: infer-твины `[]T.new()/.with_capacity()`
   (обе триплет-копии) — primitive-табличка → `elem_is_erased`-гейт + `Vec[T].new()`-synth (зеркало
   A7 emit-сайда); ParallelFor infer-арм → Vec-мангл; `apply_type_subst_to_ref` Array-арм:
   primitive → Vec-мангл, НЕ-primitive — БЕЗ ИЗМЕНЕНИЙ (`NovaArray_nova_int*`-sentinel), т.к.
   unsubstituted placeholder (`Nova_T*` в erased-шаблоне) при Vec-мангле давал никогда не
   эмитящееся имя `Nova_Vec____Nova_T_p` (пойман на `std/collections/vec_seq`, исправлен);
   `compare_nova_byte`-арм NovaArray-диспетча снесён.

### 14.17 Гейты (числами)

- Сборка обоих крейтов — чистая (exit 0, pre-existing warnings).
- Conformance = **66/0**; d27_fixed_array, d38_array_creation, d403_vec_sized_element_read,
  d232_vec_growable, d232_vec_methods — явные **PASS** отдельным targeted-прогоном (5/0).
- Поведенческие пробы: parallel-for array-mode (Range+ArrayLit, слоты по индексам) PASS;
  rest-bind (3 формы, включая пустой rest и push) PASS.
- Широкая выборка (--full): `std/collections` **PASS 5 / FAIL 3** — все 3 pre-existing
  CODEGEN-FAIL checker-стадии (range: errors.nv `starts_with` E7320; vec_iter/vec_lazy:
  E_EXTENSION_METHOD_NEEDS_IMPORT), байт-в-байт на базисном бинаре; `nova_tests/strings`
  **PASS 36 / FAIL 3** — единственный набор-FAIL = pre-existing D249 `plan131_vec_access_pos`
  (тот же, что в A7-журнале; байт-в-байт на базисе); `std/encoding/json` — pre-existing
  P67-LEGACY ICE (Index elem type, span 21747 ExprId 3085) байт-в-байт на базисном бинаре
  (checker-стадия, к array.h не относится); `std/encoding/compress` PASS 2/0;
  `std/net/addr_test` PASS (таймаут в первом батч-прогоне = флака параллельной сборки с
  brotli-CU, одиночный прогон чистый). **δ0 регрессий**; сверх того A8 ЧИНИТ 3 strings-neg CU и
  4 collections CU, которые базисный бинарь ломает против нового header (stale
  `NovaArray_nova_str`-эмиссия — теперь Vec-имена).
- Спот-греп generated .c (user `[]int`/`[]uint`/`[]u8`/`[]str` + parallel-for + rest-bind):
  **0** `NovaArray_` вне (а) `NovaArray_nova_int` erasure-sentinel bare-Vec стабов (6 hits,
  pre-existing класс, задокументирован в §14.14) и (б) `void_p` (в этих пробах 0 hits).

### 14.18 Найдено попутно (вне скоупа A8, для трекинга)

- **[найдено] D71 `parallel for {trailing}` без явной аннотации** (`ro xs = parallel for ...`):
  `infer_expr_type` (types/mod.rs) не имеет арма для `ExprKind::ParallelFor` → binding получает
  `nova_unit` вместо `[]T` → CC-FAIL. Pre-existing (ломается и на базисе ДО A8, проверено),
  codegen-сторона корректна (explicit-аннотация работает). `nova_tests/concurrency/parallel_for_array.nv`
  красный на main по этой причине (не гейт корректности).
- **[найдено] partial-prelude без `collections`** при использовании `[]T`-литералов теперь даёт
  чистый loud-fail `[Plan172.12-A8]` с подсказкой (вместо тихой legacy-эмиссии); полный fix
  сценария = [M-partial-prelude-primitive-method-registry] (там же StringBuilder/errors.nv пробелы).
