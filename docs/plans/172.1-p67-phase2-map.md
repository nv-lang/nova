# P67 ФАЗА 2 — channel-completeness карта (de-risk, workflow wf_0cd92c6b 2026-06-29)

> Источник: ultracode-workflow `wf_0cd92c6b-284` (6 ридеров per-ExprKind + синтез, verified vs живой код).
> Цель ФАЗА 2: checker аннотирует resolved_types для всех value-producing ExprKind → un-annotated
> fall-through к `infer_expr_c_type_legacy` иссякает → ФАЗА 6 удаляет legacy (~3119 строк). Это **основной
> объём** P67. Текущее состояние: ФАЗА 4 (reliable-locals Ident/SelfAccess) + U.4.5 Member-field (RANK 2) +
> Session C container-returns + RANK 1 Binary-арм УЖЕ в канале.

## ⚠️ ГЛАВНЫЙ РИСК (урок 172.1.2, simplifications.md) — decouple, НЕ extend
Расширение общего `infer_expr_type` (types/mod.rs:~9646) для richer-инференса **РЕГРЕССО-ОПАСНО**: прошлая
сессия (172.1.2 STEP 1) получила **4 регрессии** — (a) f3 chain-receiver → ложный `[E_EXTENSION_METHOD_NEEDS_IMPORT]`;
(b) inline-rippled binding-тип флипнул `[]T`/`Vec[T]` spelling → ранний Vec-mono → **conservative-GC segfault (plan154)**.
Решение тогда: отдельная `infer_method_call_channel_type` (канал-only), `infer_expr_type` НЕТРОНУТ. **ФАЗА 2 ОБЯЗАНА
следовать тому же паттерну: channel-аннотация в producer-блоке (f1_expr:6374-6388) через ОТДЕЛЬНУЮ
type-computation, НЕ через extend общего `infer_expr_type`** — иначе вскроется тот же perturbation-класс.

Producer-блок УЖЕ есть (f1_expr:6374-6388): `if matches!(e.kind, Block|If|IfLet|Match|Path) { if let Some(tr) =
infer_expr_type(e) { resolved_types_buf.insert(e.id, from_type_ref(tr)) } }` — **inert**, т.к. infer_expr_type=None
для этих kind. Активация = дать producer'у тип БЕЗ extend-perturbation (decoupled helper).

## Каждый шаг — §7: NOVA_U45A_XCHECK (debug) no-divergence + detect172 pos+neg + ПОЛНЫЙ регресс vs CLEAN (НЕ сэмпл — 172.1.2 ловило только полным). Byte-identical к legacy ОБЯЗАТЕЛЬНО (урок ФАЗА 1a).

## Ранжированный план (synthesis)

### STEP 1 — Path (numeric-type-constant + module/enum-qualified)  ~121 сайтов
- **Legacy:** `numeric_type_constant_mapping(parts)` (emit_c.rs:36527) → c_ty (`int.MAX`→nova_int, `u8.MIN`→nova_byte,
  `f64.EPSILON`→nova_f64). Прямая таблица, НЕ infer.
- **DECOUPLED-подход (рекоменд.):** в producer для Path вычислять primitive ResolvedType ПРЯМО через
  checker-эквивалент numeric_type_constant_mapping (НЕ добавлять Path-арм в `infer_expr_type` — perturbation).
  Нужен checker-side numeric-constant-резолвер (проверить, есть ли; если нет — портировать таблицу).
  Byte-identical (та же таблица). module/enum-qualified Path — отдельный gated subset.
- Risk: low (если decoupled). Coverage: 121.

### STEP 2 — TurboFish (delegate-to-base)  ~448 (но residual мал — ctor-turbofish УЖЕ seeded number_exprs)
- **Legacy:** `infer_expr_c_type(base)` (emit_c.rs:36523) — делегация в **codegen**-инференс base.
- **⚠️ НАХОДКА:** наивный decouple `infer_expr_type(base)` (checker) **INERT** — для type-name base (`Foo[T]`,
  base=Foo) checker `infer_expr_type(Foo)`=None (scope не содержит type-name), тогда как codegen
  `infer_expr_c_type(Foo)` даёт C-тип. checker ≠ codegen для type-base. → TurboFish НЕ чистый quick-win;
  нужен checker-side type-name resolver ИЛИ оставить legacy для TurboFish. Coverage residual мал → низкий приоритет.

### STEP 3 — Member(field-read) + Index — RECEIVER-WIDENING (MOVE A)  ДОМИНИРУЮЩИЙ остаток Member
- Field-read УЖЕ в канале (RANK 2 Record:8438/NamedTuple:8581 gated primitive_gate/concrete_value_named).
- **Остаток = field-on-non-resolvable-receiver** (`a.b.c`, `xs[i].field`, method-chain): f3_check_member бейлит
  на `infer_expr_type(obj)=None` (:8311), т.к. infer_expr_type НЕТ Member/Index-receiver-арма.
- **MOVE A:** дать infer_expr_type Member/Index-receiver-резолв (для concrete Named record/named-tuple receiver)
  → unblock'ает существующий primitive_gate insert. **⚠️ ЭТО ТОЧНО RANK 4 / 172.1.2 perturbation-класс** —
  extend infer_expr_type Member/Index рипплит в extension-policy + GC-layout. HIGH-risk; decouple обязателен;
  ПОЛНЫЙ регресс. Gated subset: container-field (`[]T`→`[]int`) + residual type-param (`[M-172.1-typeparam-infer-erasure]`) — DEFER (mono-hazard §9).

### STEP 4 — Block (primitive-trailing copy)  ~63
- Block-value = trailing expr. infer_expr_type Block-арм partial (empty-block only, :9620). Decoupled:
  channel infer_expr_type(trailing) — ⚠️ trailing-let binding-lookup hazard (number_exprs doc): trailing
  Ident, связанный ВНУТРИ блока, нет в outer scope → None (safe-skip). Concrete-trailing only.

### STEP 5 — If (widen infer_if_common_primitive) — OPTIONAL/LATER  ~275
- If-value = branch-join. Mirror `infer_match_common_primitive`. ⚠️ `[M-match-arm-mixed-int-width-sentinel-coerce]`
  hazard (расхождение арм → sentinel-coerce). Concrete-common branches only.

## Не в плане (gated-defer, §9 mono / 172.1.2)
- Ident-остаток (globals/consts вне var_types), Call-остаток (multi-overload, builtin-без-FnDecl → resolved_callees U.3),
  method-VALUE Member (`obj.method` без call → void*), container/generic-arg субсеты всех kind, residual type-param.
  Ждут типизированного IR / mono-path (172.1.2) где current_type_subst надёжен.

## Вывод
ФАЗА 2 — careful multi-step, регрессо-опасна. Каждый kind = один §7-коммит. НЕ extend `infer_expr_type`
напрямую — decouple (паттерн infer_method_call_channel_type). STEP 1 (Path) — DONE (commit b290d229).

## ⚡ ИЗМЕРЕНИЕ остатка fall-through (NOVA_U45_GAP, debug nova.exe, conformance-CU, 2026-06-29)
`NOVA_U45_GAP=1 nova test spec_tests/conformance` → **9291 fall-through в legacy**, ВСЕ annotated=false.
Распределение (это и есть «что блокирует удаление legacy», по частоте):

| ExprKind | n | % | примечание |
|---|---|---|---|
| **Ident** | 4010 | **43%** | legacy берёт из var_types (надёжно, но legacy). Самый крупный рычаг, самый GC-опасный (172.1.2) |
| **Call** | 1872 | **20%** | частично U.4.5a (resolved_callees); bulk падает. Лучший risk/reward — расширять канал callee |
| **Member** | 995 | **11%** | RANK 2 закрыл concrete-records; остаток = non-resolvable receiver (RANK 4 класс) |
| **Binary** | 754 | **8%** | RANK 1 закрыл arith; остаток = cmp/bool/смешанные операнды |
| TurboFish | 492 | 5% | decoupled INERT (checker≠codegen для type-base) — отложен |
| Index | 318 | 3% | как Member (receiver-widening) |
| If | 286 | 3% | бывш. STEP 5 — копеечный |
| Path | 137 | 1.5% | **STEP 1 DONE** (остаток = module/enum-qualified, не numeric-const) |
| Closure/Other/Block | 124/98/65 | ~3% | Block (бывш. STEP 4) = 0.7% — копеечный |
| Tuple/Match/Range/RecordLit/… | ~140 | <2% | |

**Стратегический вывод (data-driven):** keystone (удалить legacy ~3119 строк) гейтнут на **Ident+Call+Member+Binary
= 82% остатка** — НЕ на Block/If/Path (вместе <6%). Грайндить мелкие kind'ы = почти не двигать иглу к
zero-fallthrough. **Пересмотренный порядок по ЧАСТОТЕ × (1/риск):**
1. **Call (20%)** — расширить resolved_callees-канал (U.4.5a уже есть основа); GC-риск ниже Ident.
2. **Member/Index (11%+3%)** — receiver-widening (RANK 4 класс, decouple+полный регресс).
3. **Binary-residual (8%)** — cmp/bool/смешанные (RANK 1 продолжение).
4. **Ident (43%)** — крупнейший, но опаснейший. Подход: перенести var_types-lookup в CONSUMER как
   first-class (не legacy-re-derive) для un-annotated Ident — НЕ аннотировать каждый в producer.
5. Block/If/Path-residual/Tuple/Match — в самом конце (копейки).
6. Перенести side-effects legacy (typedef/mono-регистрация, emit_c.rs:36329) → ТОЛЬКО потом ФАЗА 6 delete.

(Замер на conformance — малый чистый CU; на std/nova_tests распределение шире, но Ident/Call доминируют тем более.)

## 🎯 ЗАДАЧА (директива владельца 2026-06-29): `[]T` → ОДИН канонический ResolvedType с `Vec[T]`
**Что.** §0/D315 «сахар нормализуется прочь»: `[]T` (D239 alias `Vec[T]`) сейчас даёт ОТДЕЛЬНЫЙ
`ResolvedType::Array(T)` (types/mod.rs:159 `from_type_ref`), а `Vec[T]` → `Named{Vec,[T]}` — **два
варианта RT + два пути лоуэринга** (`resolved_array_to_c` emit_c:1978 vs `resolved_named_to_c` :1976),
СХОДЯЩИЕСЯ к одному C-типу (`Nova_Vec____<elem>*`, когда Vec-шаблон загружен). Нормализовать в ОДИН
канонический RT = `Named{Vec,[T]}`; `R::Array` ретайрить как представление `[]T`.

**РЕКУРСИВНО, любая вложенность (директива):** `[]T`→`Vec[T]`, `[][]T`→`Vec[Vec[T]]`, `[][][]T`→
`Vec[Vec[Vec[T]]]`. Внешний `[]` оборачивает внутренний (`[][]T` ≡ `[]([]T)`). Достигается рекурсивным
`R::from_type_ref(inner)` в Array-арме (by construction). Замер: бинарь даёт `Nova_Vec*` (не NovaArray);
no-std erasure схлопывает глубину в C, но RT несёт `Named{Vec,[Named{Vec,…}]}` — проверить на mono-build.

**§2 (директива): тривиальные read-аксессоры → ПРЯМОЕ поле, не метод-вызов.** `[]T.len` лоуэрится в
поле (`xs->len`) — это ПРАВИЛЬНАЯ/эффективная форма; `Vec[T].len()` сейчас идёт через метод (`xs->len()`).
Аксессор `@len() -> int => @len` — тривиальная обёртка над полем; компилятор может/должен инлайнить его в
прямое чтение поля. End-state: `[]T` и `Vec[T]` (единый RT) оба читают `.len`/`.cap` как прямое поле
(унифицировано + быстро). Метод-вызов = лишняя индиректность для read-аксессоров.

**Почему.** Две дорожки = §0-несовершенность + источник timing-чувствительности 172.1.2 (порядок
Vec-mono-регистрации зависит от написания → перетряхнутый layout → conservative-GC segfault).
Один RT/путь = убирает этот класс by construction.

**Scope (§7.1).** 18 сайтов `R::Array`/`ResolvedType::Array` в 4 файлах (emit_c, types/mod,
const_fn_closure, const_fn_trampoline). Ключевой producer = `from_type_ref:159`. Producers TypeRef::Array:
parser:2866/6066, emit_c:12041, gc_layout:943, auto_derive, ast:3008. **EDGES (риск):**
(a) `resolved_array_to_c` спец-кейсы — closure-array `[]fn`→`NovaArray_void_p*` ([M-138.1]) + LEGACY
`NovaArray_<prim>` fallback когда Vec-шаблон НЕ загружен — `resolved_named_to_c(Vec)` их НЕ имеет →
дивергенция в no-template / closure-array; нужно перенести логику в общий хелпер, оба пути зовут.
(b) 18 R::Array-ПОТРЕБИТЕЛЕЙ (is_primitive_lowerable, is_typed_int, gc_layout char/size) — поведение
для `Named{Vec}` должно совпасть с прежним `Array`.
(c) **FixedArray `[N]T` — ОТДЕЛЬНЫЙ built-in** (stack-allocated fixed-size, `[32]u8`=32б inline;
emit_c:11458 «separate built-in», НЕ Vec/growable). НО `from_type_ref:158` СЕЙЧАС конфлейтит
`Array`+`FixedArray` → оба `R::Array` (pre-existing — теряет `N`). Канонизация ДОЛЖНА разделить:
`[]T`→`Named{Vec}`; `[N]T`→ОТДЕЛЬНЫЙ fixed-array RT (несущий `N`), НЕ Vec и НЕ conflated с `[]T`.

**§7-подход (ОБЯЗАТЕЛЬНО — это 172.1.2 GC-зона):** detect/xcheck байт-идентичности (probe `[]T` vs
`Vec[T]` → тот же C?) → если 0-divergence (ожидаемо где шаблон загружен) land; дивергентные edges
(no-template/closure-array) закрыть переносом resolved_array_to_c-логики в общий Vec-лоуэринг ПЕРЕД
финальным переключением → ПОЛНЫЙ регресс vs clean. Revert при регрессе.

**ЗАМЕР (detect-mode, 2026-06-29, откатан после измерения):** правка `from_type_ref:158` (`[]T`→`Named{Vec}`,
`[N]T` оставлен `R::Array`):
- **conformance ПОЛНЫЙ build (со std) → PASS, 0 FAIL** — в реальном build (Vec-шаблон загружен) канонизация
  НЕ ломает поведение. **Жизнеспособно.**
- **standalone no-std probe → C дивергирует** (артефакт отсутствия Vec-шаблона): `[]int`→legacy
  `NovaArray_nova_int*` (поле `->len`) vs `Named{Vec}`→erased `Nova_Vec*` (метод `->len()`). Подтверждает
  §0-расщепление (`.len` поле-vs-метод для одного алиаса) — ровно то, что канонизация чинит.
- **Остаётся до land:** (1) ПОЛНЫЙ регресс vs clean на std/nova_tests (conformance — малый CU); (2) убрать
  созданную FixedArray/Array асимметрию (решить — `[N]T` тоже канонизировать или нет); (3) перенести
  resolved_array_to_c спец-кейсы (closure-array `[]fn`, no-template fallback) в общий Vec-лоуэринг, чтобы
  edges не дивергировали. → focused-заход (Opus, §7).

---

## ✅ КАНОНИЗАЦИЯ `[]T`→`Vec[T]` — LANDED (09bce062, 2026-06-29)

**Что сделано (целевая форма, §0/§3, НЕ интерим-патч):**
- `from_type_ref`: `[]T`→`Named{name:"Vec",module:[],args:[T]}`, РЕКУРСИВНО (`[][]T`→`Vec[Vec[T]]`).
- **Edge (a) ЗАКРЫТ переносом, не дублём:** новый арм `"Vec" if args.len()==1 => resolved_array_to_c(&args[0])`
  в `resolved_named_to_c`. Теперь `[]T` И `Vec[T]` (оба `Named{Vec}`) лоуэрятся через ЕДИНСТВЕННЫЙ
  Vec→C источник `resolved_array_to_c` — closure-array (`NovaArray_void_p*`), stub-erasure→`nova_int`,
  Vec-mono worklist в ОДНОМ месте. `[]T` byte-identical (его путь и был `resolved_array_to_c`); `Vec[T]`
  теперь СОВПАДАЕТ с ним (раньше расходящийся generic-path — D239 требует идентичности).
- ArrayLit channel-population (`types/mod.rs` U.4.4) → канон `Named{Vec}` (byte-identical лоуэринг).
- `R::Array` больше НЕ носитель `[]T`: только FixedArray + внутренний категорийный ключ `resolved_cat_of`
  (никогда не лоуэрится в C) → D315 «два C-окна правды» закрыто здесь.

**Edge (b) — потребители:** `primitive_gate`/`is_primitive_lowerable`(R::Array)=false ≡ (Named{Vec})=false
→ U.4.4-сайты byte-identical (оба отвергают массивы). const_fn-файлы матчат `TypeRef::Array` (не RT) — нетронуты.

**Верификация (§7):** array-юнит-тесты + новый `nested_array_canonicalizes_recursively` зелёные; byte-identity
C-дифф clean-vs-patched на 42 standalone Vec/array/closure-программах = **0 реальных дивергенций** (1
false-positive = pre-existing недетерминизм overload-pick в neg-тесте `swap_oob_neg`, доказан clean-vs-clean
diff = 2 строки `AtomicU32`vs`AtomicBool`); 0 mixed на 303 файлах; 40 conformance test-build PASS (harnessed,
полный std/prelude+Vec runtime-correct → std-Vec-лоуэринг звучен).

### Оставшиеся под-задачи канонизации

1. **§2 ACCESSOR-INLINE (директива владельца — измеряемая, НЕ rushed).** Тривиальные read-аксессоры
   (`fn T @m() -> F => @field`, без args/эффектов) → инлайн в прямое поле `recv->field` на call-site, НЕ вызов
   `Nova_T_method_m(recv)`. **§3-ОБЩИЙ:** классификатор тел методов `=> @field` строит реестр
   `(TypeName,Method)→Field`; НЕ name-key под Vec — любой тип с тривиальным getter'ом. Примеры Vec:
   `core.nv:234 @len()=>@len`, `:237 @cap()=>@cap`. Insertion: method-dispatch ветка `emit_call`
   (DeclaredBody-путь, ~emit_c:22937+). **РИСК:** recv pointer-vs-value + C-имя/offset поля — мис-инлайн =
   тихая мис-компиляция чтения. Конвенция §2: «оптимизация ТОЛЬКО когда доказана безопасной» + «измеряем» →
   focused-заход с поведенческим регрессом + перф-замером, не хак в горячем пути. (НЕ byte-identical — намеренно.)
2. **FixedArray `[N]T` → отдельный N-несущий RT** [M-172.1-fixedarray-N]: сейчас делит `R::Array` (N теряется,
   pre-existing). Отдельный вариант, не conflated с `[]T`/Vec.
3. **resolved_cat_of `Vec`/`[]`→`Named{Vec}`** (свернуть категорийную сторону): убрать `R::Array` из
   `cat_compatible_rt`/`distinct_mono` → удалить вариант `R::Array` целиком. ОСТОРОЖНО: меняет компат-проверку
   `Vec[A]`vs`Vec[B]` с поэлементной (R::Array рекурс) на name-permissive (как др. дженерики) + distinct_mono
   strict — net-эквивалентно, но риск false-narrowing → отдельный регресс.

---

## 🎯 ДИАГНОСТИКА БАЗЫ — остаток гейтнут на U.4.3 (2026-06-29, data-driven)

**Замер** (debug + NOVA_U45_GAP, корпус standalone-программ): fall-through распределение —
Call rc=false доминирует, `recv=method.var` (instance-метод на переменной) + `TurboFish`.

**Что УЖЕ работает (не дыра):** не-генерик instance-методы с КОНКРЕТНЫМ возвратом флипаются:
`check_instance_overload` пишет callee-span в `resolved_callees` (7935) → `fn_ret_by_span` индексирует
возврат каждого не-extern метода (emit_c:9853) → consumer-flip (36370) читает. Producer+consumer
замкнуты для не-генериков.

**Что НЕ флипается (настоящий остаток = U.4.3):** GENERIC/mono методы — iterator-адаптеры
(`step_by`/`chunks`/`windows`/`map`/`filter`), turbofish `obj.m[T]()`. Их C-возврат зависит от
type-args НА CALL-SITE → declaration-span C-имя (`fn_ret_by_span`) НЕ контекстно-независимо →
запись привела бы к мис-флипу (wrong mono → CC-FAIL). Поэтому `check_instance_overload` правильно
их НЕ пишет (gated non-generic). Это U.4.3 tuple/mono-hazard.

**U.4.3 = keystone остатка базы (§0-правильный путь):** канал должен нести РЕЗОЛВНУТЫЙ ВОЗВРАТНЫЙ
ТИП как `ResolvedType` (с подстановкой generics приёмника/turbofish), НЕ готовое C-имя. Чекер
ЗНАЕТ резолвнутый возврат вызова (выбрал overload + имеет type-args приёмника) → пишет
`resolved_types[call_id] = <resolved return RT>` (§1 «материализуй резолв, не выбрасывай»); consumer
лоуэрит через `resolved_type_to_c` со СВОИМ mono-контекстом (`current_type_subst`). Тогда генерик-
методы флипаются корректно. **Канонизация `[]T`→`Vec[T]` (09bce062) — ПРЯМОЙ пререквизит:** канал
несёт `ResolvedType`, и `[]T`/`Vec` теперь ОДИН RT, который U.4.3-возврат лоуэрит единым путём.

**Edges U.4.3:** (a) `Self`-возврат (fluent) = тип приёмника, не `from_type_ref(Self)`; (b) generic-
подстановка type-args приёмника в возврат (`Vec[T].first()->Option[T]` + recv `Vec[int]` → `Option[int]`);
(c) byte-identity: `resolved_type_to_c(RT)` со mono-контекстом ДОЛЖЕН совпасть с legacy method-return
derive → §7 detect/xcheck/полный регресс vs clean. GC-зона (172.1.2) — careful, не наскоком.

---

## 🔬 ЭМПИРИЧЕСКАЯ ВЕРИФИКАЦИЯ bottleneck'а (2026-06-29) — U.4.3 подтверждён, multi-overload отвергнут

**Гипотеза:** multi-overload bail в `resolve_instance_method_return` (`[f] else None`) = крупная Call-дыра.
**Тест:** расширил резолвер на multi-overload (unique arg-compat через `overload_applicability`, те же
fully-concrete/container safety-гейты). C-diff clean (только known-флак swap_oob), 7 iterator/Vec/overload
conformance PASS — БЕЗОПАСНО. НО замер NOVA_U45_GAP: head vs mo = **610/610 (standalone), 1918/1918 (d262
+std) — 0 coverage add. ИНЕРТНО.** Откатано (§2.10/§4 — не коммитить инертное как прогресс).

**Что реально падает (method.var rc=false):** **141 из 145 → `legacy=nova_int` (ЭРЕЙЗ)**. Это §1
«auto-выводимый неверный тип»: receiver не резолвится в конкретный тип (generic-body / unresolvable
chain) → возврат эрейзится до нативного nova_int до мономорфизации. multi-overload был red herring —
эти вызовы бейлят ВЫШЕ (receiver-резолв 10074 / `infer_arg_ty` None / return-mentions-generic 6614/10112),
не на overload-count.

**Вывод (data-backed):** Call-bottleneck = generic/unresolvable-receiver method-вызовы с эрейзнутым
возвратом → нужен **U.4.3 mono-aware typed-IR** (канал несёт резолвнутый возвратный RT с подстановкой,
codegen лоуэрит со своим `current_type_subst`). Contained channel-твики (multi-overload) НЕ двигают это —
проверено. Это major focused-рефактор в GC-зоне, не quick win. Канонизация `[]T`→`Vec[T]` остаётся его
пререквизитом (единый RT-носитель возврата).

---

## 🔬 U.4.3 ДЕФИНИТИВНО НЕДЕЛИМО-КОМПЛЕКСНА — 3 контейнированных инкремента провалены (2026-06-29)

Эмпирически (measure-first, revert-if-regress) проверены ТРИ контейнированных подхода к U.4.3
erasure-reduction — ВСЕ провалились:

1. **multi-overload channeling** (resolve_instance_method_return `[f] else None` → unique arg-compat):
   безопасно (C-diff clean, 7 conformance PASS), но **ИНЕРТНО** (0 coverage на 2 корпусах — bail не
   узкое место). Откатан.
2. **gs-gate relax (types/mod.rs:6614)**: **ИНЕРТНО** — глубинный gate 10112 доминирует, отсекает
   generic-возвраты раньше. Откатан.
3. **10112 relax + consumer nested-stub-guard** (`mangled_has_nested_placeholder`): d262 PASS,
   conformance 87/89 (d39/d85 PRE-EXISTING), plan154 6/6, generic 3/3 — НО **регрессит
   iterator-адаптеры** (adapters/chains/flat_map_basic/laziness/phase_b_lazy — все PASS на clean,
   fail с правкой). Причина: 10112 guard'ит НЕ ТОЛЬКО стабы, но и **flatten мис-подстановку**
   (`Vec[Vec[T]].flatten`→неверный `Vec[Vec[str]]` — валидно-выглядящий concrete, НЕ стаб →
   stub-guard пропускает → flip → неверный тип). Откатан.

**ВЫВОД:** U.4.3 требует ОДНОВРЕМЕННО: (a) stub-suppression (erased-тела), (b) flatten/nested-receiver
корректную подстановку (`build_recv_subst` структурную унификацию), (c) self_nested policy-guard,
(d) container-mono plan154-safety. Контейнированный guard любого ОДНОГО недостаточен — остальные
регрессят. Это **неделимый comprehensive рефактор** (subst-population recipe port на ВСЕ consume-сайты
+ flatten-fix + полный регресс), не инкремент. Genchk-инструмент (35f7e543) — субстрат для него.

**Pre-existing находки (ИСПРАВЛЕНО 2026-06-30 — это были PER-FILE артефакты, НЕ канонические RED):**
d39/d85 «CC-FAIL на CLEAN» — наблюдались через **per-file `test-build`**, что НЕВЕРНАЯ методология:
conformance = ОДИН CU (`nova test spec_tests`, см. [[project-conformance-single-cu-run]]). В каноническом
whole-CU прогоне d39/d85 **PASS и на чистом дереве** (порядок эмиссии иной → var_types-протечка не та).
d85-фикс ниже = латентный, не RED-closure. Урок: гонять conformance ТОЛЬКО как whole-CU.

---

## 🔑 U.4.3 КОРЕНЬ partial-flip clash (2026-06-29, инкремент #3 диагностика)

flat_map_basic CC-FAIL с relax-10112+stub-guard: `NovaValue_BoxIter_____NovaTuple2` (erased
generic-tuple) vs `NovaValue_BoxIter_____NovaTuple_2_8_nova_int_8_nova_int` (concrete `(int,int)`).
ТОТ ЖЕ механизм, что d39/d85 (`NovaOpt_nova_byte` vs `NovaOpt_nova_int`): правка флипает ОДИН сайт
(method-return) в concrete, а СВЯЗАННЫЙ сайт (binding/variable type, через legacy) остаётся erased →
C-конфликт типов.

**КЛЮЧЕВОЙ ВЫВОД для comprehensive U.4.3:** erased-форма (`NovaTuple2`/`NovaOpt_nova_int`) используется
КОНСИСТЕНТНО по всему телу. Флип ТОЛЬКО method-returns недостаточен — clash'ит с binding/variable-
типами, которые legacy эрейзит. **Comprehensive U.4.3 ДОЛЖЕН флипать ВЕСЬ type-flow вместе:**
method-returns И binding-типы (`ro x = ...`) И variable-типы — все через канал, консистентно. Это и есть
причина неделимости: не «4 независимых fix», а ОДИН консистентный type-flow channeling (binding-типы =
след. слой после method-returns). Пятое эмпирическое подтверждение неделимости.

---

## ✅ GC-СТРАХ РАЗВЕНЧАН — fresh mono НЕ вызывает segfault (2026-06-29, ОКОНЧАТЕЛЬНО)

> **Запись по требованию владельца: «U.4.3 comprehensive БЕЗ GC-страха». Этот вопрос ЗАКРЫТ.
> НЕ возвращаться к гипотезе «fresh mono → перестройка layout → segfault»: она ФАКТИЧЕСКИ
> ОПРОВЕРГНУТА кодом рантайма. Если страх снова возникнет — перечитать здесь, не перепроверять.**

**Опровергнутая гипотеза (из старых заметок):** «канализация container/mono-возвратов регистрирует
свежую мономорфизацию → возмущает conservative-Boehm-GC layout (`slot_size` = чистая функция набора+
порядка эмитированных структур) → детерминированный segfault (plan154)».

**Факт (доказательство в коде):**
- `slot_size` определяется в [`nova_rt/fiber_arena.c:348`](../../compiler-codegen/nova_rt/fiber_arena.c)
  `_nova_resolve_slot_size()` = `env(NOVA_FIBER_STACK)` ∨ `NOVA_FIBER_STACK_DEFAULT` (4MB), затем
  round+clamp. Это **размер СТЕКА файбера** — рантайм-конфиг, **НЕ зависит** от набора эмитированных
  C-структур/mono. Связи «set of structs → slot_size» НЕТ.
- GC = **conservative Boehm** (точные дескрипторы — 144.1 heap-bitmaps / 144.5 precise-online —
  🔴 **NOT STARTED**). Консервативный GC сканирует память layout-АГНОСТИЧНО по определению
  (любое слово, похожее на указатель = корень). Добавление новой mono-структуры в эмитированный C
  **не меняет** ни поведение сканирования, ни корневые диапазоны (`GC_add_roots(base, base + high*
  slot_size)` — slot_size тот же).
- ⇒ **Регистрация свежей mono под текущим GC безопасна.** Возврат канала может менять C-строку типа
  без GC-последствий.

**Истинный корень провалов U.4.3 (инкременты #1–#3):** чисто **compile-time partial-flip clash** —
несовпадение C-типов (concrete method-return vs erased `nova_int`/`NovaTuple2`/`NovaOpt_nova_int`
binding/variable), которое ловит **C-компилятор** (CC-FAIL), а НЕ runtime-segfault. Это тип-движковая
проблема (полностью в зоне фронтенда, §0), решается консистентным флипом всего type-flow (раздел выше).
plan154-«segfault от mono» был мис-атрибуцией CC-FAIL'ов как GC-падений.

**Следствие для comprehensive U.4.3:** проектировать как compile-time type-consistency рефактор.
Гейт = «C компилируется» (нет clash) + поведенческая звучность, НЕ «не сегфолтит от GC». 144.5 (precise
GC) — **НЕ** предпосылка U.4.3.

---

## ✅ ЛАТЕНТНЫЙ codegen-фикс — [M-codegen-if-branch-locals-overlay] (2026-06-29/30)

> **ВАЖНО (методология):** это НЕ closure RED-конформности. Conformance whole-CU (`nova test spec_tests`)
> зелёный И на чистом, И с фиксом. Симптом ниже виден ТОЛЬКО через неверный per-file `test-build`.
> Фикс корректен (§0, латентная fragility), но прогресс прог-2 он НЕ даёт. Урок про whole-CU усвоен.

**Симптом (per-file build артефакт):** `test-build d85_question_return` CC-FAIL: `if`-выражение в
`d85_opt_q` (тип обязан быть `Option[int]`=`NovaOpt_nova_int`) получает C-тип
`NovaOpt_Nova_Vec____nova_byte_p` (= `Option[str]`) — обе ветки корректно дают `NovaOpt_nova_int`, но
temp-переменная `if`-результата объявлена `Option[str]`. (В whole-CU порядок эмиссии иной → не вылезает.)

**Корень (НЕ U.4.3-generic, а stale-var-leak):** `emit_if_expr` (`emit_c.rs ~28906`) вычисляет
`then_ty = infer_expr_c_type(then.trailing)` **ДО** эмиссии ветки. Ветка = `{ ro b = d85_none()?;
Some(b) }`. `b` ещё не в `var_types` (ветка не эмитнута) → `infer_expr_c_type(b)` хватает **протёкший
одноимённый** `var_types["b"]` = `str` из ДРУГОЙ conformance-функции (var_types НЕ per-fn scoped, долг
`emit_c.rs:36472`) → `Some(b)` инферится `Option[str]`. В изоляции `b` дал бы дефолт и «прошло» — баг
вылезает в folder-module CU (89 файлов вместе).

**Фикс:** новый `branch_trailing_c_type_with_locals(block)` — pre-регистрирует let-локали ветки в
`var_types` (overlay), инферит trailing, восстанавливает. **Точное зеркало уже-существующего паттерна
в `emit_block_expr`** (`emit_c.rs:29641-29673`, тот же stale-var комментарий). `emit_if_expr` теперь
зовёт его для then_ty И else_ty. Класс-фикс (любой `if`-as-value со stmt'd-веткой, trailing которой
ссылается на branch-local). §0: убирает codegen-re-derive по протёкшему scope.
**Валидация:** conformance whole-CU PASS (baseline тоже PASS — фикс нейтрален-или-лучше); if-тяжёлые
plan125 6/6 PASS; чекер не тронут (правка только в codegen). 4 std-FAIL (as_bytes/lazy/StepByIter) —
pre-existing checker-ошибки, не связаны.

---

## ❌ U.4.3 10112-relax — КОНТРПРОДУКТИВЕН (2026-06-30, 6-е подтверждение + ТОЧНАЯ причина)

Re-attempt relax-10112 + consumer stub-guard (теперь С flatten-fix + GC-de-risked). Результат:
- conformance whole-CU PASS; **flatten PASS** (flatten-fix `build_recv_subst` работает!); generics PASS.
- НО `plan153_2/adapters` **CC-FAIL**: `NovaValue_BoxIter_____NovaTuple2` (erased) ⟵ init `NovaValue_
  BoxIter_____NovaTuple_2_8_nova_int_8_nova_int` (concrete). stub-guard НЕ ловит (`NovaTuple2` —
  erased-arity, не placeholder `Nova_U_p`).

**ТОЧНАЯ ПРИЧИНА (снимает прежнюю «неделимость»-формулировку):** relax НЕ нужен для int-collapse —
**legacy УЖЕ резолвит mono-method-возвраты concrete** (d126_generic_return_width/d182/d119 PASS БЕЗ
relax: legacy через mono-инстанс даёт правильный sized-тип). Relax лишь флипает method-return-сайт в
concrete, тогда как **binding/struct-field BoxIter остаётся erased `NovaTuple2`** → clash. Erased
`NovaTuple2` — это **by-value-mono WIP** (commit 0da18125 «by-value monomorphization of generic
value-records; BoxIter value»): element-тип value-record-итератора (`BoxIter<Tuple<int,int>>`)
не резолвится при построении value-record mono.

**ПЕРЕОПРЕДЕЛЕНИЕ keystone:** настоящий остаток int-collapse generic-возвратов = НЕ канал, а
**завершение by-value-mono element-резолюции** (BoxIter<Tuple>/value-record монокарта). Когда
binding/struct-field строится concrete — clash исчезает, И канал-relax становится безопасным, И
открывается путь к legacy-deletion (канал = legacy для mono). До этого 10112-bail ДЕРЖАТЬ.
Канал/legacy-deletion гейтнут на by-value-mono, НЕ наоборот.

---

## ✅ V-ТРЕК int-collapse: 2 реальных бага закрыты + класс literal-coercion картирован (2026-06-30)

**Метод (validated):** различающие conformance-тесты с unsigned `2^63` + UNANNOTATED-биндингом
(`ro got = …`) → наследует inferred-тип; signed-collapse читает `2^63` негативом → `> 100` ложь.
Вскрывает int-collapse как CC-FAIL или RUN-FAIL.

**Закрыто (оба named-priority, помечены [M-172.1-*]):**
1. `Some(<int-literal>) -> Option[uint]` → CC-FAIL (NovaOpt_nova_int vs _uint). Фикс `a0a2a2dd`
   [M-172.1-some-target-coerce]: return-сайты с NovaOpt_/typed-int эмитят trailing через
   `emit_expr_with_target_type` → литерал коэрсится. 4 пути (emit_fn Expr/Block, emit_block_stmts).
2. `Result[uint,str] ?? fb` → RUN-FAIL (Coalesce инферил тип из fallback-литерала, не Ok-типа). Фикс
   `62c0f57f` [M-172.1-coalesce-result-okty]: Coalesce-infer обрабатывает Result через `novares_ok_err`.

**Класс literal-coercion — картирован (record/arg/let/Some/Ok коэрсят; tuple-return НЕТ):**
- record-field (`{v:0x80}`→uint) ✅, call-arg (`f(0x80)`→uint) ✅ — залочены `b1cc0fd2`.
- **ОСТАЁТСЯ:** tuple-литерал-элемент в typed-return (`(0x80,0x80) -> (uint,uint)` строит `(int,int)`).

**ЦЕЛЕВАЯ ФОРМА (директива владельца «единый движок, не piecemeal» + §0 стр.119):** НЕ 4-й per-position
codegen-патч. Корень — чекерский `number_exprs` seed литералов СИНТАКСИЧЕСКИЙ (натуральный `nova_int`),
НЕ context-coerced. Целевая форма = **U.4 literal-coercion канал**: чекер материализует COERCED-тип
литерала (D55) в `resolved_types` во ВСЕХ позициях (tuple-элемент / Some-payload / Ok-payload /
return / arg / field), codegen IntLit ЧИТАЕТ канал. Это субсумирует ВСЕ tactical-фиксы выше И закрывает
часть ≈103 `nova_int`-fallback долга (HARD-AC: 0 `unwrap_or("nova_int")` в финале).

## ✅ U.4 LITERAL-COERCION КАНАЛ — LANDED (4772ba8a + ed30280a, 2026-06-30)

**Реализовано (целевая форма, §0/§1, НЕ piecemeal):**
- **PRODUCER (types/mod.rs `materialize_literal_coercion`):** на DEFINITE coercion-сайтах пишет
  COERCED sized-`ResolvedType` в `resolved_types_buf[lit.id]`, рекурсируя через Some/Ok/Err/tuple/
  array И value-position control-flow (If/Match arm-tails, block-trailing). Сайты: annotated `let`
  (7116), chosen call-args (free-fn/Type.method 8226 + instance-method+recv-subst 7985), arrow-body
  + block-trailing + **explicit `return`** (return-walker `materialize_returns_in_*`, рекурсия по
  same-fn control-flow; НЕ в detach/parallel-for/spawn/closure — иной exec-контекст). НЕ на
  speculative overload-probe (`overload_applicability` 7840). Conservative: только sized scalar
  (не wide `int`, не generic-param).
- **CONSUMER (emit_c.rs `emit_expr` IntLit + `channel_int_c_type`):** читает канал → sized-литерал
  (`((uint)NU)`); un-annotated/`nova_int`-seed → legacy `((nova_int)NLL)` (byte-identical). Литерал
  SELF-типизируется из канала во ВСЕХ позициях, без target-passing.

**Закрыто (RED→GREEN на clean, залочено в d55):** tuple-литерал-элемент в typed-return (`(0x80,0x80)
-> (uint,uint)` строил `NovaTuple..nova_int..` ≠ `..nova_uint..` → CC-FAIL) + Some/array-return +
explicit-`return` (вкл. nested-in-if) + if/match-value-arrow.

**§7-верификация:** conformance whole-CU GREEN; std+detect172 (behavior-gate) IDENTICAL fail-set vs
clean (0 регресс); nova_tests sample (10 dirs: plan70/118/atomics/strings/generics/map_literals/
plan147/syntax/contracts/plan124) IDENTICAL fail-set (0 регресс). Clean baseline = `nova_clean_now.exe`
(CC-FAIL tuple-return подтверждает валидность).

**ОСТАЛОСЬ по каналу:** (1) субсумировать tactical-фиксы (Some a0a2a2dd / Coalesce 62c0f57f /
tuple-destructure 4bfa4ce8 / default-arg da934613) — снять ПОСЛЕ поформного доказательства покрытия
(§10, careful: tactical-Some-arm в emit_expr_with_target_type ≠ канал в emit_expr — проверить, что
return-эмиссия идёт через emit_expr/self-typing). КОРРЕКЦИЯ: tactical-Some/Coalesce делают WRAPPER-тип
(NovaOpt_uint typedef registration), канал — LITERAL-значение; для tuple/array wrapper ДЕРИВИРУЕТСЯ из
элементов (канал субсумирует), для Option/Result wrapper explicit (комплементарны). (2) RecordLit-field
в материализатор. tactical-фиксы — стопгапы, исчезающие с legacy-удалением (ФАЗА6).

## 🔑 BY-VALUE-MONO complex-element ERASURE — DE-RISK MAP (workflow wf_adadd4c3, 2026-06-30)

> Keystone гейтит U.4.3/method-chain/legacy-deletion. **НЕ активный int-collapse RUN-FAIL** (literal-
> coercion канал закрыл активные value-position баги; plan153_2 BoxIter-over-tuple адаптеры PASS = clean).
> Это **legacy-deletion / generic-completeness enabler** — глубокий архитектурный флип, fresh-focus.
> 4 grounded ридера (Explore) + Opus-синтез, verified vs живой код. blast-radius агент частично
> неточен (claimed plan172_boxiter_width — НЕ в HEAD; остальные dirs реальны).

**КОРЕНЬ (high-confidence, grounded):** генерик-инстанс-арм `resolved_named_to_c` (emit_c.rs:2237-2241):
```rust
let type_args_c: Vec<String> = args.iter()
    .map(|a| self.resolved_type_to_c(a).unwrap_or_else(|_| "nova_int".to_string())).collect();
```
Двухслойная эрейзура, обе в `resolved_type_to_c`: **Layer A** (bare type-param `T`: lookup
`type_subst_overrides`/`current_type_subst` :2120-2126 фейлит при пустом subst → `Nova_T*` :2279).
**Layer B** (Tuple/Array арм :1982-2011 / :2024-2052: рекурсия в элементы → каждый type-param-элемент
бьёт Layer A → `is_concrete`=false :1989-1997 → `register_legacy_tuple` :2010 → `_NovaTuple2` :13754).
Эрейзнутая строка → `compute_generic_type_c_name` :2242 → `Nova_BoxIter____nova_int` вместо
`...NovaTuple_2_8_nova_int_8_nova_int`; value-record имя `NovaValue_<short>` :2254 наследует.

**АРХИТЕКТУРНЫЙ КОРЕНЬ (почему baked-in):** worklist хранит УЖЕ-ЛОУЭРНУТЫЕ C-строки (`type_args_c`,
push :2246) пока `current_type_subst` в состоянии ВЫЗЫВАЮЩЕГО (пустом). `drain_generic_type_worklist`
:15829 РЕКОНСТРУИРУЕТ subst ИЗ этих эрейзнутых строк → даже `emit_generic_type_instance` :15879
переустанавливает УЖЕ-эрейзнутый subst (`T→"nova_int"`). **Эрейзура коммитится в push-time; instance-
emit контекст не может восстановить.** = ТОТ ЖЕ `unwrap_or("nova_int")` HARD-AC долг (директива #1).

**SELF-CONTAINED? НЕТ — context-threading, НЕ one-liner.** Эрейзящий контекст (bare-binding) НЕ имеет
живого subst для консультации → фикс должен СОЗДАТЬ subst-контекст. 3 формы: **(a)** протянуть
subst-override в `resolved_type_to_c`/`resolved_named_to_c` (меняет сигнатуру канонического type→C пути
D315 → все callers, mechanical); **(b)** pre-substitute Tuple/Array args ПЕРЕД :2240 (локально, НО
receiver-subst обычно недоступен в bare-binding — не закроет gap); **(c)** [РОБАСТНО] хранить
UN-lowered `ResolvedType` args на worklist (сменить payload-тип), лоуэрить в drain-time под ПРАВИЛЬНО-
установленным `current_type_subst` (touches worklist-тип + push :2246/2042/15824 + drain :15772-15832).
Прецедент: `type_subst_overrides` escape-hatch (:2121, populated :34542 для method-on-generic-receiver)
— но fires только для `args.is_empty()`, Tuple/Array-рекурсия его минует.

**CONSUMER CLASH = producer NON-DETERMINISM (фикс producer'а ОДНОГО достаточно — consumers pass-through):**
тот же value-record лоуэрится по-разному: concrete-путь (method-body emission ставит subst :5802/14567/
15083/24323/34542 → 2240 резолвит верно) vs erased-путь (bare let-binding `infer_expr_c_type` :18464 +
TupleLit `infer_expr_c_type_legacy` :36875 → пустой subst → `_NovaTuple2`). struct-field consumer :10784
(`type_ref_to_c`, concrete) ← получает эрейзнутое binding-имя → **CC-FAIL**. `register_tuples_in_typeref`
:13828 abort'ит при `apply_type_subst_to_ref`=None по той же причине. Фикс producer-детерминизма →
оба consumer-пути сходятся, БЕЗ consumer-правок.

**BLAST-RADIUS:** код = 1 файл emit_c.rs (блок 2237-2257 + Tuple-арм 1982-2011 + Array-арм 2024-2052 +
type-param lookup 2120-2127; shape-c доп: worklist push/drain). ~33 фикстуры: plan153_2 (adapters/chains/
zip_basic), plan153_4 (chunks_windows), plan153_5 (flatten), plan162 (enumerate_*), d284_enumerate_index.
Witness был plan172_boxiter_width/uint_enumerate ((int,uint) не должен схлопнуться) — НЕ в HEAD, восстановить.

**§7-ПЛАН (ordered, detect-first):** (1) detect-mode: расширить bail-gap инстр-цию (2265-2278) логировать
эрейзуру элемента ВНУТРИ value-record-mono Tuple/Array-рекурсии (debug-gated, behavior-unchanged),
прогнать 33 фикстуры под NOVA_BAIL_GAPLOG → подтвердить модель. (2) clean baseline (kill-switch на ТОМ ЖЕ
бинаре). (3) shape-(b) smallest-flip, byte-identity для уже-concrete типов. (4) byte-identity diff vs
baseline — изменения ТОЛЬКО эрейз-таргеты. (5) correctness gate (восстановить uint_enumerate witness) +
whole-CU. (6) если residual → shape-(c) архитектурный флип. (7) полный регресс (detect172 = звучность-гейт).
**Bottom line:** producer-детерминизм-баг :2237-2241; producer-фикс закрывает consumer clash; НЕ one-liner
(нет живого subst в эрейз-контексте) → shape-(c) drain-time lowering = робастная форма. Fresh-focus.

---

## 🎯 U.4.3 ТОЧНАЯ ЛОКАЛИЗАЦИЯ — keystone-проба + bail-трасса (2026-06-30, ЭМПИРИЧЕСКИ, КОРРЕКЦИЯ карты выше)

> **Две runtime-пробы на витнессе `nova_tests/plan172_boxiter_width` (debug, env-gated, откатаны после замера)
> ПЕРЕОПРЕДЕЛЯЮТ задачу. Раздел «BY-VALUE-MONO shape-(c)» выше — ЧАСТИЧНО ОПРОВЕРГНУТ: эрейзура НЕ на
> keystone-лоуэринге, а UPSTREAM (recv/closure-subst в method-return).**

### Проба 1 — keystone (emit_c.rs:2237, args-структура при лоуэринге BoxIter)
- `BoxIter[Tuple([Scalar signed, Scalar unsigned])]` (= `(int,uint)`) доходит до keystone с **subst_len=0** и
  лоуэрится **КОРРЕКТНО** (оба Scalar concrete → `register_mono_tuple` → ширина сохранена). **Витнесс PASS.**
- Эрейз `_NovaTuple2` бьёт ТОЛЬКО `Tuple([concrete, bare-T])` где `T` не подставлен.
- ⟹ **shape-(c) drain-time для bare-T был бы ИНЕРТЕН** (нет конкретной инфо сохранять): инфо потеряна
  ВЫШЕ, до keystone. Фикс — upstream (recv/closure-subst), НЕ producer-патч keystone.

### Проба 2 — `resolve_instance_method_return` bail-категоризация (types/mod.rs:10268)
| method-return | исход | вывод |
|---|---|---|
| `Vec.lazy` → `BoxIter[uint]` | **OK** channeled | recv-subst конкретизирует ✓ |
| `BoxIter.enumerate` → `BoxIter[Tuple([int,uint])]` | **OK** channeled **ПОЛНОСТЬЮ concrete** | **PART A enumerate-return УЖЕ РАБОТАЕТ** — recv-subst рекурсивно в nested tuple |
| `Vec.first`/`pop`/`find` → `Option[int]` etc | **OK** | scalar/Option-возвраты каналятся (как d126) |
| `BoxIter.map` → `BoxIter[U]` generics={U,T} | **BAIL-residual** | **PART B** — method-level generic `U` = closure-return, НЕ связан recv-subst |
| `BoxIter.next` → `Option[T]` generics={T} | BAIL-residual | **КОРРЕКТНО** (template-definition emission, T genuinely unbound) |

### ПЕРЕОПРЕДЕЛЁННАЯ задача (data-backed, sharpest на сегодня)
1. **PART A (enumerate-class) — НЕ требует фикса.** `resolve_instance_method_return` уже резолвит
   `enumerate`-возврат в concrete `BoxIter[(int,uint)]` и каналит (container-bail снят `_ => {}`).
   Гейт `:10324 typeref_mentions_any` пропускает — нет residual.
2. **PART B (map/filter/flat_map) — РЕАЛЬНЫЙ остаток int-collapse.** Гейт `:10324` БЕЙЛИТ: возврат
   `BoxIter[U]` несёт method-level generic `U`, связываемый ТИПОМ ВОЗВРАТА ЗАМЫКАНИЯ-аргумента
   (`fn(T)->U`), которого `build_recv_subst` (recv-only) не даёт. Фикс = **инференс closure-return**
   (param `T`=recv-element → тип тела) → bind `U` → subst в `BoxIter[U]`. **D402-сопряжено.**
3. **LANDING-HAZARD = partial-flip clash (подтверждён всеми прошлыми заходами).** Флип map-return в
   concrete, пока **binding/struct-field via legacy остаётся `_NovaTuple2`** → CC-FAIL. ⟹ PART B ОБЯЗАН
   co-land с **consistent consumer-flip**: binding-эмиссия (`infer_expr_c_type` :18464) и struct-field-
   эмиссия читают КАНАЛ (`resolved_types[call.id]`), не legacy-re-derive. Это и есть «неделимость».
4. **ЛАТЕНТНОСТЬ (почему нет RED):** эрейз `_NovaTuple2` КОНСИСТЕНТЕН (binding И map-return оба эрейзят)
   + биты сохраняются + сравнение берёт знаковость из binding-типа → ни CC-FAIL, ни RUN-FAIL. Витнессы
   PASS. RED появится ТОЛЬКО при partial-flip. ⟹ гейт корректности PART B = «whole-CU C компилируется
   (нет clash) + detect172 звучность», НЕ «витнесс RED→GREEN» (его нет).

**Bottom line (переопределение):** int-collapse iterator-хвост = **PART B closure-return-U binding
(types/mod.rs:10268, расширить subst замыканием) + consistent consumer-flip канала на binding/struct-field
(emit_c.rs)**. enumerate-класс уже закрыт. Это comprehensive co-land (НЕ инкремент — partial-flip
регрессит), D402-сопряжён. Fresh-focus, §7 (clean baseline `scratchpad/nova_baseline_u43.exe` готов).
