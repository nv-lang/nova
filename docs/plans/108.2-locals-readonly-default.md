# Plan 108.2 — Локальные `let` без `mut` = read-only (enforcement D36)

> **Sub-plan Plan 108** (Plan 108.1 уже flip'нул params).  Plan 108.2 —
> finally enforce D36 для local bindings: `let x = ...` immutable,
> `let mut x = ...` mutable.  Согласует семантику с params (Plan 108.1).

## TL;DR

Спецификация D36 уже говорит: «`let mut acc`/`acc.field = ...` ✓, `let acc`/
`acc.field = 100` ✗ ОШИБКА».  Но компилятор это **не enforce'ит**.  Plan
108.2 закрывает gap: mut-метод/field-assign на `let`-binding без `mut` →
`E_LOCAL_NOT_MUT`.

```nova
// Было (V1 — D36 spec, но no enforcement):
let b = Box.new(1)
b.value = 99                  // компилировалось, silent mutability ✗
b.push(2)                     // тоже OK ✗

// Стало (Plan 108.2 — enforcement):
let b = Box.new(1)
b.value = 99                  // ✗ E_LOCAL_NOT_MUT
b.push(2)                     // ✗ E_LOCAL_NOT_MUT
let mut b2 = Box.new(1)
b2.value = 99                 // ✓
```

## Фазы

### Ф.0 — Audit baseline (~30 мин)

- Снять baseline `nova test` PASS/FAIL.
- Quick-stats:
  - 462 `let mut` в std/ (уже корректно).
  - 1556 plain `let` в std/ (часть требует ревизии).
  - 1752 `let mut` в nova_tests/.

### Ф.1 — Spec amend D36/D175 + plan-doc (~1 час)

- Amend D36 в `spec/decisions/02-types.md`: добавить раздел «Enforcement
  (Plan 108.2)».
- Описать `E_LOCAL_NOT_MUT` (новый код).
- Cross-ref в D176 amend (Plan 108.1) — symmetry.

### Ф.2 — Type-checker enforcement (~2 часа)

В `compiler-codegen/src/types/mod.rs`:

1. `ConsumeCtx.local_mut: HashMap<String, bool>` — отслеживать is_mut
   для каждого local binding.  Заполняется при processing `Stmt::Let`.
2. При вызове mut-метода на Ident:
   - Сначала проверить `param_mut` (Plan 108.1).
   - Затем `local_mut`.  Если is_mut=false и метод mut → `E_LOCAL_NOT_MUT`
     + machine-applicable Suggestion («add `mut` после `let`»).
3. Field assignment (`b.field = ...`) — то же check.
4. Index assignment (`b[i] = ...`) — то же.
5. `consume X = ...` binding — implicit mut (как params).

### Ф.3 — Стдлиб миграция (~3 часа)

Sweep `std/`: для каждого `let x = ...` который потом вызывает mut-метод
или присваивает field — добавить `mut`.

Estimated ~50-150 sites.

### Ф.4 — Тесты миграция + fixtures (~3-4 часа)

- Sweep `nova_tests/`: аналогично.
- ≥6 positive + ≥6 negative + 1 property = ≥13 фикстур.

### Ф.5 — Документация (~1 час)

- `docs/parameters.md` — добавить раздел про locals.
- `docs/migration/d36-let-mut-enforcement.md` — migration recipes.
- Update `docs/consume-types.md` cross-ref.

### Ф.6 — Closure (~1 час)

- Plan-doc closure.
- Logs (`docs/project-creation.txt`, `docs/simplifications.md`,
  `nova-private/discussion-log.md`).
- Memory (`project-plan108_2-status.md` + MEMORY.md).
- Merge `plan-108.2` → `main`.

## Acceptance criteria (production-grade)

### Compiler / type-checker

- [ ] Spec D36 amended раздел «Enforcement (Plan 108.2)».
- [ ] AST: `LetDecl` уже имеет `mutable: bool` — переиспользуется.
- [ ] Type-checker эмитит:
  - `E_LOCAL_NOT_MUT` (mut-method/field-assign/index-assign на let
    без mut) с machine-applicable Suggestion («add mut»).
- [ ] LSP integration — quick-fix предлагает `let mut`.

### Тесты

- [ ] ≥6 positive + ≥6 negative + 1 property фикстура в `nova_tests/plan108_2/`.
- [ ] Verified через release nova.
- [ ] Полная регрессия: 0 NEW FAIL vs baseline.

### Spec / docs

- [ ] D36 amended (active, не draft).
- [ ] `docs/parameters.md` — раздел про locals.
- [ ] `docs/migration/d36-let-mut-enforcement.md` — 4+ recipes.

### Логи и memory

- [ ] `docs/project-creation.txt` — секция Plan 108.2.
- [ ] `docs/simplifications.md` — whys.
- [ ] `nova-private/discussion-log.md` — closure entry.
- [ ] Memory `project-plan108_2-status.md` + MEMORY.md index.

### Commits (per task)

1. Spec D36 amend + plan-doc (Ф.1)
2. Type-checker enforce (Ф.2)
3. Stdlib migration (Ф.3)
4. Tests migration + new fixtures (Ф.4)
5. Docs (Ф.5)
6. Closure: logs + memory + merge (Ф.6)

## Honest-defer (potential)

- `[M-108.2-loop-var-mut]` — `for i in 0..10 { i = i + 1 }` —
  loop variable mutability.  Edge-case; check separately если возникает.
- `[M-108.2-pattern-binding]` — `let (a, b) = pair` — pattern bindings.
  Сейчас оба immutable; OK без изменений.

## Branch & worktree

- Branch: `plan-108.2`
- Worktree: `D:\Sources\nv-lang\nova-p108-2`
- Base: `main` HEAD после Plan 108.1 merge.

## Estimated total: ~10-12 часов
