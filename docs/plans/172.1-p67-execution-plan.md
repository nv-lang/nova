# P67 execution-план — удаление `infer_expr_c_type_legacy` (headline 172.1)

> **Главная конвенция реализации:** [`docs/compiler-conventions.md`](../compiler-conventions.md) —
> нормативный документ, §0 «один источник истины для типов», §0.1 `ResolvedType` = единый носитель,
> §2 никакого хардкода Nova-имён. Всё что делаем — реализует эти принципы. Отклонения только
> по согласованию с владельцем.

> **Для нового агента:** рабочая стратегия и порядок атаки — в memory:
> [`project-plan172-strategy.md`](../../../Users/Евгений/.claude/projects/d--Sources-nv-lang-nova/memory/project-plan172-strategy.md) (kinds-таблица, инертные заходы)
> [`project-conformance-single-cu-run.md`](../../../Users/Евгений/.claude/projects/d--Sources-nv-lang-nova/memory/project-conformance-single-cu-run.md) (команда запуска теста)
> Worktree: `d:\Sources\nv-lang\nova-p172\`, ветка `plan-172-unified-type-engine`.
> Единица прогресса = **kind** из таблицы §СТРАТЕГИЯ ниже. Начинать с мелких (ArrayLit→IfLet→Try…).
> Перед каждым коммитом: замер tally должен упасть на N, 333/333 PASS.

## 📊 СТАТУС ИСПОЛНЕНИЯ (2026-06-30, режим: измеримое падение tally→0)

**Объективный gate удаления = fall-through tally (NOVA_U45_GAP) → 0 на полном корпусе.**
Замер conformance whole-CU (debug):

| дата | TOTAL | Call | Member | Binary | Ident | TurboFish | Index | If | прочее |
|---|---|---|---|---|---|---|---|---|---|
| 2026-06-29 baseline | — | 1736 | 982 | 282 | 3716 | 448 | 310 | 275 | (иной прогон) |
| **2026-06-30 до** | **6507** | 2090 | 1093 | 773 | 664 | 585 | 357 | 307 | ~530 |
| 2026-06-30 после Binary | 6273 | 2090 | 1093 | **539** | 664 | 585 | 357 | 307 | ~530 |
| **2026-06-30 после Some-ctor** | **6223** | **2040** | 1093 | 539 | 664 | 585 | 357 | 307 | ~530 |
| **2026-06-30 conformance CU** | **6224** | 2040 | 1093 | 539 | 664 | 585 | 357 | 307 | Path 153+Closure 124+Other 103+Block 79+TupleLit 47+RecordLit 37+Match 32+Range 28+Coalesce 17+Unary 9+Try 6+IfLet 2+ArrayLit 1 |

(Ident упал 3716→664: ФАЗА 4 reliable-locals relocation уже снимает var_types-Idents; остаток 664 = globals/consts, резолвятся legacy КОРРЕКТНО — nova_char/NovaOpt — deletion-only, не баг.)

**Реализовано (2026-06-30, измеримое исполнение, каждый §7-верифицирован: conformance whole-CU PASS + 0 регресс):**
- ✅ **ФАЗА 0** gate-метрика (NOVA_U45_GAP).
- ✅ **ФАЗА 4** reliable-locals (4B SelfAccess + 4C Ident, emit_c.rs:36654-36750) — 2 из 3 legacy
  exit-points сняты для var_types-случая (consumer-side relocation, БЕЗ producer-перемены типа →
  без GC-пертурбации 172.1.2).
- ✅ **ФАЗА 2 Binary residual** (eb2d3672): channel-fallback операндов (Member-field/Index/Call) →
  numeric promote. **773→539 (−234)** + фикс sized-field-arith int-collapse.
- ✅ **ФАЗА 2 Some-ctor** (fef0cd60): `Some(x)` → `Option[type-of-x]` (payload-ширина) **−50** +
  int-collapse-relevant (`Some(uint)`→Option[uint]).
- **Кумулятив 2026-06-30: tally 6507→6223 (−284).**

**⚠️ ОТБРАКОВАНО как ИНЕРТНОЕ (НЕ повторять — измерено, откатано §2.10):**
- **free-fn-call channeling** (single non-generic fn return): 158 OK-аннотаций, НО эти вызовы в
  typed-контекстах (тип не реинферится codegen) → tally −2. recv=free доминируют overloaded(920)+ctors.
- **If/Match stmt'd-branch channel-trailing**: TOTAL без изменений — If-остаток = else-if chains /
  non-primitive joins, НЕ stmt'd-primitive.
- **PART B call-site closure-return grafting** (см. раздел U.4.3): эрейз в value-record instantiation,
  не call-site. 7-й inert-заход класса call-site/channel-relax.

**ВЫВОД (data-backed, 2026-06-30):** easy single-kind producer-wins ИСЧЕРПАНЫ (Binary+Some — последние
безопасные). Остаток tally 6223 ДОМИНИРУЮТ deep-кейсы, каждый нужен §7-careful/multi-session:
**Call ~2040** (generic/method-returns — keystone = value-record TEMPLATE INSTANTIATION emit_c.rs:2237,
НЕ call-site; consumer-flip нужен) · **Member 1093** (receiver-widening RANK 4) · **Binary-residual 539**
(operator-overloads / non-channel операнды) · **Ident-globals 664** (const-registry channeling — tractable
но deletion-only) · **TurboFish 585** (decoupled INERT — checker≠codegen type-base) · **Index 357**
(receiver-widening) · **If 307** (else-if chains). Следующий headline = value-record instantiation
consumer-flip (fresh-focus, §7 GC-зона).

> **Источник: workflow `p67-readiness-map` (wf_a1e66815, 2026-06-29, 5 read-only агентов по коду).**
> Цель: codegen берёт C-типы ИСКЛЮЧИТЕЛЬНО из channel (resolved_types + единый `resolved_type_to_c`),
> НЕ из второго re-derive. Удалить `infer_expr_c_type_legacy` (emit_c.rs:36501-39620, ~3119 строк) + вызов :36334.
> `is_p67_close = False` — P67 substantial (ФАЗА 1+2 = основной объём).

## Состояние wrapper `infer_expr_c_type` (emit_c.rs:36328-36499)
5 точек выхода:
- :36367 `return ch_ret` — **channel-флип** (Call с resolved_callees+fn_ret_by_span). U.4.5a ✅
- :36427 `return ir_c` — **channel-флип** (любой annotated expr: resolved_types[id]+resolved_type_to_c=Ok). U.4.5/4.4b ✅
- :36417 `return legacy` — SelfAccess с var_types["nova_self"] (ФАЗА 4B).
- :36423 `return legacy` — Ident с var_types[n] (ФАЗА 4C).
- :36498 `legacy` (fall-through C) — **un-annotated surface** (основной блокер, ФАЗА 2).

Legacy гонится ВСЕГДА (:36334) ради side-effects (typedef/mono-reg), даже когда возвращается channel.

## ФАЗА 0 — gate-метрика (✅ ЗАМЕРЕНО 2026-06-29)
`NOVA_U45_GAP=1` (:36436-36497) на spec_tests → `annotated=false` fall-through по kind:
```
Ident 3716 · Call 1736 · Member 982 · TurboFish 448 · Index 310 · Binary 282 · If 275
Closure 124 · Path 121 · Other 89 · Block 63 · TupleLit 42 · Match 30 · RecordLit 26 · Range 25
```
Формат: `[U45GAP] kind=Ident annotated=false legacy=nova_int`. Gate перед ФАЗА 6: tally=0 для всех value-producing kinds на ПОЛНОМ корпусе (метрика non-recursive — проверять на полном, не сэмпле).

## ФАЗА 1 (STRUCTURAL, heaviest, §9-п.1) — `fn_ret_by_span`: String→ResolvedType

> ⛔ **ЭМПИРИКА (2026-06-29): carrier-swap НЕ byte-identical — РЕГРЕССИЯ, ОТКАЧЕНО.** Попытка (4 правки:
> carrier String→ResolvedType + re-lower на read-site через `resolved_type_to_c`, scoping-workflow wegeaq7hb)
> дала **28 новых CC-FAIL** (`ch=nova_str legacy=Nova_StringBuilder*`, 33413 NOVA_U45A_XCHECK divergence).
> ПРИЧИНА: decl-time `ret` = `return_type_c(f)` СПЕЦ-КЕЙСИТ RUNTIME_DEFINED-типы (StringBuilder→`Nova_StringBuilder*`),
> а naive `resolved_type_to_c(from_type_ref(f.return_type))` даёт `nova_str`. Claim «type_ref_to_c ≡
> resolved_type_to_c∘from_type_ref» (workflow) НЕВЕРЕН для RUNTIME_DEFINED (U.4.6 parity НЕполный ЛИБО return_type_c
> делает больше). §7-верификация (baseline-diff + NOVA_U45A_XCHECK) поймала ДО коммита.
> **ВЫВОД: carrier-swap НЕ на критпути удаления legacy** — структурные non-extern returns УЖЕ channel'ятся на :9843
> (без gate); carrier-swap = U.4.x-cleanup. Если делать — ОБЯЗАТЕЛЬНО захватывать return_type_c-спецкейсы (не naive
> from_type_ref) ИЛИ исключать RUNTIME_DEFINED/protocol/bodyless. **Критпуть удаления legacy = ФАЗА 2 (fall-through C).**
Сейчас :776 = `HashMap<Span,String>` (уже-лоуэренный C-string). Популяция :9660/:9843. extern-channel НАМЕРЕННО
гейтнут только на context-independent primitive/RUNTIME_DEFINED (:9648-9663) — tuple/Result/value-record returns
НЕ индексируются (наивный return_type_c→_NovaTuple CC-FAIL, U.4.3-hazard, коммент :9620-9624).
→ Сменить carrier на `ResolvedType` + лоуэрить через `resolved_type_to_c` в точке чтения :36359.
Blast-radius: :776/:1448/:9660/:9843/:36359. Верифицировать `NOVA_U45A_XCHECK` divergence = только legacy-nova_int→primitive.
БЕЗ этого Call-флип :36367 неполон для structured-return.

> **ПОДТВЕРЖДЕНО чтением (2026-06-29):** :9843 (MAIN path) `ret` = codegen ПОЛНОЕ лоуэрение с
> SIDE-EFFECTS на decl-time (`emit_protocol_box_typedef` :9821-9824, params_c, mangle). Carrier→ResolvedType
> ОТЛОЖИТ лоуэрение на read-time → эти side-effects ОБЯЗАНЫ релоцироваться → **ФАЗА 1 СЦЕПЛЕНА с ФАЗА 3**
> (нельзя в отрыве). Byte-identical НЕ тривиален: `resolved_type_to_c`@read-time должен воспроизвести
> decl-time контекст + side-effect-эмиссию. ВЫСОКИЙ риск — делать атомарно, с свежим вниманием. Рекоменд.
> порядок P67: сначала spec_tests-gaps (named-priority spec/D, ниже риск), потом ФАЗА 1+3 атомарно в выделенной сессии.

## ФАЗА 2 (CHECKER, основной объём, P-фазы) — закрыть fall-through C (:36498)
CHECKER-gap: `resolved_types[id]` ОТСУТСТВУЕТ для Member/Index/Block/Match/If/Binary/Unary без аннотации.
Расширить checker-аннотацию resolved_types на все value-producing kinds из ФАЗА 0. Каждый kind: annotated=true →
channel :36427 ловит автоматически. Приоритет (doc:1266-1269):
(a) type-param tuple lowering · (b) Block trailing-let (checker-resolved вместо pattern_binding_overrides :36738) ·
(c) generic-Index @index-overload-return · (d) Match arm-bindings.

## ФАЗА 3 (RELOCATE side-effects, U.4.3-hazard guard) — ПЕРЕД удалением :36334
Channel `resolved_type_to_c` уже зеркалит register_mono_tuple (:2008)/worklist (:2042)/instance_info (:2045) по ТИПУ.
НЕ воспроизводит раннюю return-mono `register_generic_instances_in_typeref(ret_ty,subst)` (legacy :37692 method-ret,
:38176 free-fn, :37096-37104 turbofish-member — регистрирует адаптеры `obj.vmap(f).collect()` ДО path-5b).
→ Перенести на эмиссионные диспетчеры (частично :34461/:25250/:26689) BEFORE path-5b (emit_call ~22828).
TupleLit emit-сайт (:30094) регистрировать ВСЕГДА, не только annotated.

## ФАЗА 4 (RELIABLE-LOCALS, bounded, точечно, side-effect-free) — можно независимо
- **4B SelfAccess** (:36415-36418): `return legacy` → inline копия :37000+:37018-37022:
  `let raw=var_types["nova_self"].clone(); if raw.starts_with("NovaValue_")&&raw.ends_with('*'){raw.trim_end_matches('*').trim().into()}else{raw}` (гейт гарантирует contains_key).
- **4C Ident** (:36419-36424): хелпер `local_ident_c_type(&self,n)->Option<String>` =
  `closure_param_type_overrides.get(n)` → `pattern_binding_overrides.get(n)` → `var_types.get(n)` (точный порядок :36830/:36839/:36842).
  ⚠️ НЕЛЬЗЯ упростить до var_types.get(n) — SEGV (stale leaked var_types не-per-fn-scoped, коммент :36728-36730).

## ФАЗА 5 (Err-семантика) — сохранить usize/isize/ptr/Self-no-recv (resolved_named_to_c :2131-2189 Err)
как явный hard-error/диагност на сайтах потребления, НЕ через legacy fall-through. Не блокер.

## ФАЗА 6 (DELETE, только после tally=0)
Удалить 3 legacy-return (:36417/:36423/:36498) + вызов :36334 + тело :36501-39620; свести infer_expr_c_type к чистому
шиму `resolved_type_to_c(resolved_types[id])` с SelfAccess/Ident var_types-хелперами (ФАЗА 4). Удалить
equivalence-scaffolding (:36360-36366,:36389-36406) + GAP-tally (:36435-36497). grep-доказательство (acceptance doc:672): ноль второго re-derive.

## ВЕРИФИКАЦИЯ (§7)
После каждой фазы: detect172 pos+neg + звучность (nova_tests НЕ гейт корректности — byte-identical baseline только).
Перед ФАЗА 6: `NOVA_U45_GAP=0` на полном корпусе = объективный gate. Финал: kill-switch baseline на том же бинаре, CC-FAIL=0.

## РИСКИ
1. **U.4.3 typedef-drop**: удаление :36334 ДО ФАЗА 3 → пропадёт ранняя return-mono цепочек `obj.vmap(f).collect()` → NULL-stub/segfault/CC-FAIL. Mitigation: ФАЗА 3 строго ПЕРЕД ФАЗА 6.
2. **SEGV**: наивная замена Ident-legacy на var_types.get(n) → потеря override-precedence → stale var_types → SEGV. Mitigation: 3-уровневый хелпер 4C.
3. **SelfAccess mono-subst gap #2**: channel resolved_type_to_c(scope["@"]) ненадёжен в mono-теле (current_type_subst timing) — источник ОБЯЗАН остаться var_types["nova_self"]. Mitigation: ФАЗА 4B inline, не channel.
4. **fn_ret_by_span carrier-смена**: широкий blast-radius, риск регресса extern Gap-B (try_start_won→bool). Верифицировать NOVA_U45A_XCHECK.
5. **tally non-recursive**: может недосчитать вложенные un-annotated; gate=0 на полном корпусе.

## Порядок исполнения
ФАЗА 1 (carrier) → ФАЗА 2 (checker annotation, incremental по kind) → ФАЗА 3 (reloc) → ФАЗА 4 (reliable-locals, можно раньше) → ФАЗА 5 → ФАЗА 6 (delete после tally=0).

## ❄️ ЗАМОРОЗКА (2026-06-30): ФАЗЫ 1-6 ЗАМОРОЖЕНЫ

ФАЗЫ 1-6 (снижение tally через правки чекера/codegen) **заморожены** — не дали достаточного темпа
прогресса к удалению legacy. Активная работа переведена на **D-track** (раздел ниже).
Разморозить можно после закрытия D-track или по отдельному решению владельца.

## СТРАТЕГИЯ spec_tests/conformance — ПАРАЛЛЕЛЬНЫЙ ПУТЬ К ФИНАЛУ

**Ключевой инсайт:** вместо абстрактного счётчика tally→0, закрывать D-блоки в spec_tests/conformance
через новый канал. Когда ВСЕ D-блоки проходят через новый канал (resolved_types) и тесты зелёные —
это ДОКАЗАТЕЛЬСТВО что новый движок работает. Снос legacy строк становится техническим cleanup-ом.

**Архитектурный факт (ВАЖНО):**
`spec_tests/conformance` = **peer-модуль** (один CU). Все `.nv` файлы компилируются **за один раз**.
Тестировать ТОЛЬКО через команду (из nova-cli/):
```
$env:NOVA_GC_LIB_DIR="D:\Sources\nv-lang\nova\compiler-codegen\vcpkg_installed\x64-windows-static\lib"
$env:NOVA_INCLUDE_DIR="D:\Sources\nv-lang\nova\compiler-codegen\vcpkg_installed\x64-windows-static\include"
.\target\debug\nova.exe test ..\spec_tests\conformance
```
НЕ per-file — иначе маскируются cross-file баги (folder-module stale-var-leak, случай d85).

Для замера tally добавить `$env:NOVA_U45_GAP="1"` и агрегировать по kind:
```powershell
... 2>&1 | Select-String "U45GAP" | % { if ($_ -match "kind=(\w+)") { $matches[1] } } | Group-Object | Sort Count -Desc
```

**Статус 2026-06-30 (первый замер conformance CU, debug-build):**
- **333/333 PASS** — все тесты зелёные
- **6224 fall-through** (legacy используется для вывода типов этих выражений):

| Kind       | Count | Характер |
|------------|-------|----------|
| Call       | 2040  | generic method-returns; keystone = value-record instantiation (emit_c.rs:2237) |
| Member     | 1093  | field-access на generic receiver |
| Ident      | 664   | globals/consts (585 = nova_int, 79 = NovaOpt_*/ptr); deletion-only |
| TurboFish  | 585   | decoupled от checker (INERT) |
| Binary     | 539   | operator-overloads / non-channel operands |
| Index      | 357   | generic index-return |
| If         | 307   | else-if chains / non-primitive joins |
| Path       | 153   | path-exprs |
| Closure    | 124   | closure-body return |
| Other      | 103   | misc |
| Block      | 79    | block trailing expr |
| TupleLit   | 47    | tuple construction |
| RecordLit  | 37    | record construction |
| Match      | 32    | match-expr |
| Range      | 28    | range exprs |
| Coalesce   | 17    | ?? exprs |
| Unary      | 9     | unary ops |
| Try        | 6     | ? exprs |
| IfLet      | 2     | if let |
| ArrayLit   | 1     | array literal |
| **TOTAL**  | **6224** | |

**Ident detail (664 total):** 585 = `nova_int` (int-constants), 38 = `NovaOpt_nova_int`, 9 = `NovaOpt_nova_unit`,
7 = `NovaOpt_Tuple2_int_int`, 7 = `NovaOpt_nova_str`, 6 = `NovaOpt_Tuple2_str_str`, 3 = ptr-types, 2 = `nova_unit`, 2 = `nova_char`, 1 = `nova_char`, 1 = ptr.

**Рабочий процесс (per-kind):**
1. Выбрать kind по таблице выше (начинать с tractable: Path→Block→TupleLit→RecordLit→Match→If→Ident)
2. Добавить checker-аннотацию в `types/mod.rs` для этого kind
3. Замерить tally: должен упасть на N для этого kind
4. §7-верификация: 333/333 PASS + 0 регресс nova_tests
5. Коммит `feat(172.1 ФАЗА2 <Kind>): channel <Kind> exprs → −N fall-through`

**Рабочий процесс (per-D, если D FAIL):**
1. Найти D-файл в spec_tests/conformance/, понять что тестируется
2. Выяснить какой kind выражения не аннотирован (U45GAP лог)
3. Починить через checker (types/mod.rs), НЕ через legacy
4. §7: 333/333 PASS + 0 регресс
5. Коммит `spec(D<N>): ...`

**Связь с финалом:** когда tally→0 на conformance CU, ФАЗА 6 (удаление ~3119 строк legacy) разблокирована.

> Файлов в conformance: ~102 `.nv` + `neg/`, `target/`.
> Основные D: D15, D17, D19, D22-D27, D30, D32-D34, D36, D38-D40, D42, D44-D45, D52-D58, D66, D72,
> D74, D85-D86B, D109-D145, D175-D189, D200, D214-D232, D237-D292, D299-D310, D315, D327-D328, D403-D412.

---

## 🎯 D-TRACK — АКТИВНЫЙ ПЛАН (2026-06-30)

**Цель:** каждый D-блок из spec/decisions/ должен иметь свой файл в `spec_tests/conformance/`
и проходить через **новый канал** (resolved_types), а не через legacy. Когда все D зелёные
на новом канале — новый компилятор доказанно работает, legacy удаляем.

**Архитектурный инвариант:**
- `spec_tests/conformance/` = peer-модуль, все файлы = ОДИН CU
- Каждый D-файл: `d<N>_<name>.nv`, тесты через `EXPECT_*` маркеры
- Тестировать ТОЛЬКО через `nova test spec_tests/conformance` (весь CU за раз)
- НЕ добавлять новый legacy-путь при написании тестов — если D не проходит, чинить checker

**Рабочий процесс для каждого D:**
1. Проверить есть ли файл `spec_tests/conformance/d<N>_*.nv`
2. Если нет — написать тест по spec/decisions/
3. Если есть но FAIL — починить через checker (`types/mod.rs`), НЕ через legacy
4. Убедиться что тест проходит в составе всего CU: `nova test spec_tests/conformance` → 333+/333+ PASS
5. Коммит `spec(D<N>): <что именно тестируется>`

**Команда запуска:**
```powershell
# из d:\Sources\nv-lang\nova-p172\nova-cli\
$env:NOVA_GC_LIB_DIR="D:\Sources\nv-lang\nova\compiler-codegen\vcpkg_installed\x64-windows-static\lib"
$env:NOVA_INCLUDE_DIR="D:\Sources\nv-lang\nova\compiler-codegen\vcpkg_installed\x64-windows-static\include"
.\target\release\nova.exe test ..\spec_tests\conformance   # быстро (без tally)
# или debug для замера tally:
$env:NOVA_U45_GAP="1"; .\target\debug\nova.exe test ..\spec_tests\conformance
```

### Статус D-блоков (2026-06-30)

Источник D: `spec/decisions/` (01-core.md, 02-types.md, 03-effects.md, 04-effects.md).
Всего в conformance ~102 файла, охватывают подмножество D. Полный список D в spec не инвентаризирован — делать по мере продвижения.

| D | Файл в conformance | Статус | Примечание |
|---|---|---|---|
| D15 | d15_structural_compat.nv | ✅ PASS | структурная совместимость protocol |
| D17 | d17_type_decls_punning.nv | ✅ PASS | punning полей |
| D19 | d19_match_arrow.nv | ✅ PASS | match-arm expr/block форма |
| D22-D27 | (нет отдельных файлов) | ❓ проверить | |
| D30 | (нет файла) | ❓ проверить | |
| D32-D34 | (нет файлов) | ❓ проверить | |
| D36 | (нет файла) | ❓ проверить | |
| D38-D40 | d38_*.nv / d40_*.nv | ❓ проверить | |
| D42 | d42_*.nv | ✅ PASS | protocol-literal |
| D44-D45 | d44_*.nv / d45_*.nv | ✅ PASS | литералы / expression-body |
| D52 | d52_*.nv | ✅ PASS | sum/newtype |
| D54 | d54_*.nv | ✅ PASS | cast + сужение |
| D55 | d55_*.nv | ✅ PASS | literal-coercion (канал) |
| D58 | d58_*.nv | ✅ PASS | range |
| D66 | d66_*.nv | ✅ PASS | Self в static |
| D72 | d72_*.nv | ✅ PASS | protocol bound |
| D74 | d74_*.nv | ✅ PASS | int/f64 math-методы |
| D85 | d85_*.nv | ✅ PASS | ? оператор |
| D86 | d86_*.nv | ✅ PASS | ?? оператор |
| D109 | d109_*.nv | ✅ PASS | primitive builtin methods |
| D119 | (в d109 CU) | ✅ PASS | method-level U inferred |
| D122 | (в d109 CU) | ✅ PASS | mono bound methods |
| D123 | (в d109 CU) | ✅ PASS | concrete typed tuple |
| D125 | (в d109 CU) | ✅ PASS | u8 primitive |
| D126 | d126_no_int_collapse.nv | ✅ PASS | no int-collapse generic return |
| D128 | (в d109 CU) | ✅ PASS | char codepoint |
| D129 | d129_*.nv | ✅ PASS | int/i64/MAX/MIN |
| D130 | d130_*.nv | ✅ PASS | uint literal/arith/compare |
| D135 | (в d109 CU) | ✅ PASS | bare Result/Option |
| D142 | (в d109 CU) | ✅ PASS | protocol-literal vtable |
| D144 | d144_*.nv | ✅ PASS | slice views / Range |
| D145 | d145_*.nv | ✅ PASS | fn[T] prefix receiver |
| D175 | d175_*.nv | ✅ PASS | ro field |
| D176 | d176_*.nv | ✅ PASS | ro type modifier |
| D180 | (в d109 CU) | ✅ PASS | .new() / .with_capacity() |
| D181 | (в d109 CU) | ✅ PASS | fluent mut chain |
| D182 | (в d109 CU) | ✅ PASS | Self parametric static |
| D189 | (в d109 CU) | ✅ PASS | defer LIFO |
| D200 | d200_*.nv | ❓ проверить | associated const |
| D214 | d214_*.nv | ❓ проверить | opaque ptr handle |
| D215 | d215_*.nv | ❓ проверить | named tuple value |
| D216 | d216_*.nv | ❓ проверить | generic anon tuple mono |
| D226 | d226_*.nv | ❓ проверить | signed len arith |
| D237-D292 | (нет или частично) | ❓ инвентаризировать | |
| D299-D310 | d310_type_set_bound.nv | ✅ D310 PASS | type-set bounds |
| D315 | d315_*.nv | ✅ PASS | ResolvedType lossless canon |
| D327 | d327_*.nv | ✅ PASS | codepoint u32 |
| D328 | (нет файла) | ❓ проверить | |
| D403-D412 | d403-d412_*.nv | ✅ PASS | int-collapse серия (uint/u8/cast) |

**Следующие шаги (приоритет — незакрытые D):**
1. Инвентаризировать все D из spec/decisions/ которых НЕТ в conformance → написать файлы
2. Для каждого ❓ — запустить и проверить PASS/FAIL
3. FAIL → чинить через checker, НЕ через legacy
4. Итог: все D ✅ PASS на новом канале = новый компилятор работает = legacy удаляем
