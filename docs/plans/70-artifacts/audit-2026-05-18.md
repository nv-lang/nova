# Plan 70 Ф.0 — Audit baseline (2026-05-18)

## Total fallback inventory

154 places в codegen где `"nova_int"` появляется как target type.

| Категория | Pattern | Count | Действие |
|---|---|---|---|
| **A1** | `type_ref_to_c(...).unwrap_or_else(\|_\| "nova_int".into())` | **90** | → strict error (Result propagation) |
| **A2** | `_ => "nova_int"` wildcard без комментария | **27** | → strict error |
| **A total** | **silent miscompilation candidates** | **117** | **MIGRATION SCOPE** |
| **B** | `=> "nova_int", // erased T` (commented) | 2 | Document + assert + keep |
| **C** | `WithResultCategory::IntLike => "nova_int"` | 2 | Legit, keep |
| **D** | `IntLit/CharLit/"int"/"i8"/.../"len"/"capacity"/"byte_len" => "nova_int"` | 11 | Legit literal/method mappings, keep |

**Migration scope per Plan 70 = 117 sites** (Cat A).

## Cat A1 distribution per file

| File | Cat A1 count |
|---|---|
| `compiler-codegen/src/codegen/emit_c.rs` | 88 |
| `compiler-codegen/src/codegen/external_registry.rs` | 2 |

## Cat A2 (wildcard `_ => "nova_int"`) distribution

| File | Cat A2 count |
|---|---|
| `compiler-codegen/src/codegen/emit_c.rs` | 27 |

## Cat A line ranges in emit_c.rs (для phase-grouping)

| Line range | Approx function context | Cat A count |
|---|---|---|
| 1300-1500 | `emit_fn_decl` + `emit_handler_lit` | ~12 |
| 2400-3500 | `emit_with` / `emit_call` early dispatch | ~18 |
| 4200-4900 | `emit_record_lit` / `emit_handler_lit` continuation | ~14 |
| 5200-5800 | `emit_method_dispatch` / `monomorphize_*` | ~22 |
| 6800-9000 | `infer_expr_c_type` / `emit_for_loop` / `emit_match` | ~20 |
| 10000-13900 | `infer_expr_c_type` extended + emit_call dispatch | ~18 |
| 15000-18500 | type_ref_to_c callers + emit_array_lit + emit_tuple_lit | ~13 |

Cumulative ~117 (varies ±2 от exact function boundary).

## Baseline test count

Запустить `nova test` (release) перед миграцией ОБЯЗАТЕЛЬНО до Phase A1.
Текущая main HEAD = `dc560a35e03` (Plan 66 use case driver).

Last known baseline (main, до Plan 70 work):
- 717 PASS / 0 FAIL / 44 SKIP (после Plan 65 hardening merge, Plan 67 closure)
- Подтвердить заново перед migration phases (phase-by-phase verify).

## Infrastructure readiness

| Зависимость | Status |
|---|---|
| `verify::pipeline::Reason::Warning` | exists (для contracts verifier W2402 family) |
| Codegen warning channel | **MISSING** — Result<_, String> only для errors; warning не поддерживается |
| Diagnostic E5101 pattern (Plan 65) | exists, reuse для E7001-E7099 |
| `cargo clippy` infra | available; lint guard может быть custom rule |
| Plan 36 R7 structured diagnostic | exists для CLI tooling |
| Plan 48 mono pass | partially landed; some erasure remains |

## Phase plan re-validated

| Phase | LOC budget | Cat A sites |
|---|---|---|
| Ф.1 diagnostic infra | ~30 | 0 (helper only) |
| Ф.2 lint guard | ~20 | 0 (Rust-side script) |
| PhaseA1 `infer_expr_c_type` | ~80 | ~20 (worst cascade) |
| PhaseA2 `emit_call` dispatch | ~120 | ~25 |
| PhaseA3 `monomorphize_generic_fn` | ~70 | ~15 |
| PhaseA4 остальные | ~250 | ~57 |
| Ф.3 Cat B audit doc | ~40 | 2 (docs only) |
| Ф.4 spec D126 + sync | ~30 | 0 |
| Ф.5 final audit + perf | ~10 | 0 (verify) |

Total estimated LOC of changes ~650, plus error message strings.

## Decision items для Phase A1

- [ ] Confirm `infer_expr_c_type` signature change: `String → Result<String, String>` (cascades down — affects дозены callers immediately).
- [ ] Diagnostic format E7001: "cannot infer C type for `<expr>` at `<file>:<line>` — `<reason>`. Add explicit type annotation or ensure generic is monomorphized."
- [ ] Migration strategy для broken user code (если найдём при тестах): per-site decision — annotation suggest vs internal mono fix.
