# Plan 99.3 — Migrate 6 closure-applying methods (consumer)

> **Sub-plan Plan 99 (master).** Consumer: миграция 6 closure-applying
> Option/Result-методов на Nova-body. **Atomic per-method** shipping.
> **Статус:** ✅ **ЗАКРЫТ 2026-05-23** (worktree `nova-p99-impl`, ветка `plan-99-impl`).
> **Приоритет:** P3.
> **Оценка:** ~0.5 dev-day (с per-method регрессией).
> **Зависимости (HARD):** [Plan 99.1](99.1-method-level-generic-in-declared-body.md) ✅,
> [Plan 99.2](99.2-contextual-variant-constructors.md) ✅.
> **Master:** [Plan 99](99-option-result-closure-applying-methods-nova-body.md).

## Цель

Перенести 6 closure-applying методов из `emit_c.rs` inline emits в
`std/prelude/core.nv` Nova-body. После 99.3: 14/14 Option/Result-
методов на Nova (`unwrap` — Plan 61 lineage).

## Atomic shipping policy

**КРИТИЧНО:** каждый метод — **один коммит** с парой:
1. `std/prelude/core.nv` — `external fn` → Nova-body `=> match @ {...}`.
2. `emit_c.rs` — удалить соответствующий inline emit-блок.

**Иначе** — C-redefinition collision (Nova-body эмитится с тем же
именем `Nova_Option_method_<m>_<T>`; если inline-emit живёт — collision).

После каждого коммита — **regression run** на `plan62/89/95/95.bis`
+ stdlib consumers. Если non-trivial FAIL — STOP, document, don't ship
broken state.

## Декомпозиция (6 atomic коммитов)

### Ф.1 — `Option.map[U]` (commit 1)

**core.nv:**
```nova
fn Option[T] @map[U](f fn(T) -> U) -> Option[U]
  => match @ { Some(v) => Some(f(v)), None => None }
```

**emit_c.rs:** удалить inline emit `:15008-15034`.

**Тесты:**
- `plan62/option_methods_from_prelude.nv` (`map(|x| x*2)` if present)
- `plan89/...`
- `plan95/*`
- `nova_tests/plan99/option_map_migrated.nv` (новая):
  ```nova
  test "Option[int].map int→int" {
      ro o Option[int] = Some(21)
      ro m = o.map(|x| x * 2)
      assert(m.unwrap_or(0) == 42)
  }
  test "Option[int].map int→str (T≠U)" {
      ro o Option[int] = Some(7)
      ro s = o.map(|x| str.from(x))
      assert(s.unwrap_or("") == "7")
  }
  ```

**Regression gate:** если plan62/std не зелёный → revert + investigate.

### Ф.2 — `Option.unwrap_or_else(f fn()->T) -> T` (commit 2)

**core.nv:**
```nova
fn Option[T] @unwrap_or_else(f fn() -> T) -> T
  => match @ { Some(v) => v, None => f() }
```

**emit_c.rs:** удалить inline emit `:14883-14920` (Option.unwrap_or_else).

**Тесты:** `option_unwrap_or_else_migrated.nv` с lazy default
(side-effect counter в closure — verify lazy invocation).

### Ф.3 — `Option.ok_or[E](err E) -> Result[T, E]` (commit 3)

**core.nv:**
```nova
fn Option[T] @ok_or[E](err E) -> Result[T, E]
  => match @ { Some(v) => Ok(v), None => Err(err) }
```

**emit_c.rs:** удалить inline emit `:15036-15064` (Option.ok_or).

**Тесты:** `option_ok_or_migrated.nv` — Option→Result(T,E) с
разными E (str, int, User-record).

### Ф.4 — `Result.unwrap_or_else(f fn(E)->T) -> T` (commit 4)

**core.nv:**
```nova
fn Result[T, E] @unwrap_or_else(f fn(E) -> T) -> T
  => match @ { Ok(v) => v, Err(e) => f(e) }
```

**emit_c.rs:** удалить inline emit (Result.unwrap_or_else блок,
local search в `:15240+`).

**Тесты:** `result_unwrap_or_else_migrated.nv`.

### Ф.5 — `Result.map[U](f fn(T)->U) -> Result[U, E]` (commit 5)

**core.nv:**
```nova
fn Result[T, E] @map[U](f fn(T) -> U) -> Result[U, E]
  => match @ { Ok(v) => Ok(f(v)), Err(e) => Err(e) }
```

**emit_c.rs:** удалить inline emit `:15249+` (Result.map).

**Тесты:** `result_map_migrated.nv` — Ok-transform, Err-passthrough.

### Ф.6 — `Result.map_err[F](f fn(E)->F) -> Result[T, F]` (commit 6)

**core.nv:**
```nova
fn Result[T, E] @map_err[F](f fn(E) -> F) -> Result[T, F]
  => match @ { Ok(v) => Ok(v), Err(e) => Err(f(e)) }
```

**emit_c.rs:** удалить inline emit Result.map_err блок.

**Тесты:** `result_map_err_migrated.nv` — Err-transform, Ok-passthrough.

## Per-method commit template

```
feat(plan99.3): Option.<method> на Nova-body

core.nv: external fn → Nova-body `=> match @ { ... }`.
emit_c.rs: удалён inline emit-блок (line N-M); mono'd Nova_Option_
method_<m>_<T>[_<U>] эмитится через DeclaredBody-dispatch (Plan 99.1
+ Plan 99.2).

Регрессия: plan62/89/95/95.bis stdlib consumers — N/0.

Co-Authored-By: ...
```

## Acceptance criteria

- [ ] **Ф.1-Ф.6**: каждый метод atomic-shipped с тестом + regression.
- [ ] После всех 6: 14/14 Option/Result-методов на Nova (только
      `unwrap` C-routed).
- [ ] inline emits в `emit_c.rs` для 6 методов удалены.
- [ ] `init_hardcoded_baseline` cleanup (если есть `<inline>` sentinels
      для этих методов — убрать).

## Non-scope

- **Plan 99.4** — comprehensive tests + spec + закрытие маркера.
- **`unwrap`** — Plan 61 lineage.

## Risk + mitigation

**R1.** C-redefinition collision — strict atomic per-method (core.nv
+ inline-delete в одном коммите).

**R2.** stdlib consumers (`std/encoding/json.nv` etc.) могут использовать
эти методы — verify через targeted regression на каждом шаге.

**R3.** Closure-arg type-inference edge cases (Plan 98 не покрыл
`[]Option[T]` — но closure-applying methods обычно не вызываются на
массивах). Document если выявится.

**R4.** STOP-at-failure: если на любой Ф.N regression non-trivial —
revert commit + investigate + либо fix + re-ship либо document как
known limitation.
