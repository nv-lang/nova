<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 104.10 — LSP V2: production-grade gap closure (parity с 7 LSP-пирами)

> **Ред. 2 — 2026-07-03**: планка расширена до 7 LSP-пиров (+Zig zls, +Swift sourcekit-lsp; Kotlin/Java = kotlin-lsp/jdtls, не IntelliJ); фаза Ф.0.5 для 3 compiler.rs-багов (root красной диагностики); D304→D378-D380 (D304 занят); table-stakes фичи (documentHighlight/folding/selection); связь с lsp-conventions.md; DCO/sync; Q-104-4.

> **Статус:** ✅ **ВЫПОЛНЕН 2026-07-04** — все 24 фазы (BLOCK A+B+C) + Ф.14 close-out. Ветка `plan-104-10`. См. «## Статус выполнения» ниже.
> **Home:** этот файл — единственный source-of-truth для Plan 104.10.
> **Depends:** Plan 104 ✅ (104.0–104.9 закрыты). Reuse `compiler-codegen` как library (M-11).
> **Промпт для исполнения:** «выполни план 104.10» (вся информация — в этом файле).
> **Acceptance bar (обязателен на КАЖДОЙ фазе):** «без упрощений как для прода» — никаких
> заглушек, TODO-веток, текстовых эвристик там, где требуется семантика. Если упрощение
> неизбежно — оно фиксируется отдельным `[M-104.10-*]` маркером с обоснованием, НЕ молча.

---

## 0. Контекст и честный baseline (verified 2026-06-19)

Plan 104 (V1) дал базовый feature-set. Этот план закрывает разрыв до уровня **7 LSP-пиров** —
rust-analyzer (Rust) / gopls (Go) / tsserver+TS-LS (TS) / kotlin-lsp (Kotlin) / jdtls (Java) /
zls (Zig) / sourcekit-lsp (Swift). *(IntelliJ — не-LSP IDE, богаче любого LSP; используется как
референс идей, НЕ как обязательная планка — паритет меряется против LSP-серверов.)* Ниже — **факты
из кода** (не предположения), на которых строится дизайн. Каждый факт проверен (baseline verified
2026-06-19; line-refs — снимок, дрейфуют, сверять по символам).

### 0.1 Что уже есть (V1, проверено)

| Возможность | Файл | Состояние |
|---|---|---|
| Diagnostics + debounce | `compiler.rs`, `debouncer.rs` | ✅ prod |
| Hover (fn/type/local var + Variant B инференция) | `hover.rs`, `symbol.rs` | ✅ |
| Goto-definition | `goto_definition.rs` | 🟡 **single-file only** (`uri.clone()`, goto_definition.rs:30) |
| Completion (keywords/snippets/import/методы) | `completion.rs` | 🟡 методы только из текущего файла (`scan_module_methods`); таблицы/эвристики удалены (2026-06-19); stdlib-методы — после Ф.5 |
| Signature help | `signature_help.rs` | 🟡 overload по имени, не по типу |
| Document/Workspace symbols | `symbols.rs` | ✅ |
| Find references | `symbols.rs:189`, `server.rs:729` | ✅ **cross-file** (полный FS-скан, regex word-boundary) |
| Code actions (25+ fixes) | `code_actions.rs` | ✅ prod |
| Rename | `rename.rs` (1074 строки) | 🟡 **regex word-boundary** (false-positives в разных scope) |
| Format on save | `format.rs` | ✅ |
| Semantic tokens (delta) | `semantic_tokens_delta.rs`, `server.rs:1060` | 🟡 **узко**: только cached-`@field` reads (Plan 123.5.2), legend = `[PROPERTY]` + mod `cached`/`readonly` |
| Inlay hints | — | ❌ **отсутствует полностью** (grep пуст) |

### 0.1a Планка 7 LSP-пиров (per-feature; Ред.2 2026-07-03)

Легенда: **F** = полно, **P** = частично, **X** = нет.

| Feature | RA (Rust) | gopls | tsserver | kotlin-lsp | jdtls | zls (Zig) | sourcekit (Swift) | Nova-план |
|---|---|---|---|---|---|---|---|---|
| goto-def cross-file | F | F | F | F | F | F | F | Ф.3 = паритет |
| hover (+member) | F | F | F | F | F | F | F | Ф.4/Ф.6 = паритет |
| completion type-driven | F | F | F | F | F | F | F | Ф.5 = паритет |
| completion fuzzy/ranking | F | F | F | F | F | P | F | Ф.5-подпункт (ranking) |
| signature help по типу | F | F | F | F | F | P | F | Ф.8 = паритет |
| rename semantic | F | F | F | F | F | P | F | Ф.7 (scope-aware, маркер на full-resolve) |
| find refs | F | F | F | F | F | F | F | есть + Ф.12 |
| documentHighlight | F | F | F | F | F | F | F | **🔴 Ф.15 (NEW Ред.2)** — было не покрыто |
| foldingRange | F | F | F | F | F | F | F | **🔴 Ф.16 (NEW Ред.2)** |
| selectionRange | F | F | F | F | F | F | F | **🔴 Ф.17 (NEW Ред.2)** |
| inlay hints | F | F | F | F | F | F | F | Ф.9 (BLOCK C) |
| semantic tokens | F | F | F | F | F | F | F | Ф.10 (BLOCK C) |
| organize imports | F | F | F | F | F | X | P | Ф.11 (BLOCK C) — ≥ zls |
| incremental check/index | F | F | F | F | F | F | F | Ф.1/Ф.12 (+фон-индекс Ред.2) |
| call hierarchy | F | F | F | F | F | X | F | scope-out §6 (маркер; фундамент готов) |
| typeDefinition / implementation | F | F | F/P | F | F | P/X | F | **🔴 Ф.19 (NEW Ред.2)** — было не покрыто |
| codeLens (run-test/refs/impl) | F | F | P | P | F | X | P | **🔴 Ф.20 (NEW Ред.2)** — 🏆 run-test с `nova test` |
| file watching (didChangeWatchedFiles) | F | F | F | F | F | F | F | **🔴 Ф.18 (NEW Ред.2)** — must-have, было не покрыто |
| willRenameFiles → update imports | F | F | F | F | F | X | P | **Ф.18 (NEW Ред.2)** |
| progress ($/progress) | F | F | F | F | F | F | F | **Ф.18 (NEW Ред.2)** |
| type hierarchy | F | F | P | F | F | X | F | scope-out §6 (маркер) |
| code actions | F | F | F | F | F | P | F | есть ✅ |

**Итог планки:** после Ред.2 (Ф.0.5 red-fix + Ф.15/16/17 + фон-индекс) Nova достигает паритета по всем table-stakes; отстаёт сознательно (scope-out с маркером) только call/type-hierarchy (как zls). **🏆 differentiator:** diagnostic-корректность = byte-parity с `nova check` (Ф.0.5) — ни один пир не гарантирует LSP-diag ≡ CLI-diag формально.

**Уроки не-покрытых пиров (в дизайн):**
- **zls (Zig):** incremental build-on-save + инвалидация по *обратным зависимостям* (изменил A → re-check зависящих от A). → Ф.1 добавляет пункт про dependent-invalidation (или маркер, если пока только сам-URI).
- **sourcekit-lsp (Swift):** persistent on-disk cross-module index (indexstore-db) + **фоновая** индексация ВСЕГО проекта (не только открытых файлов) + build-system awareness. → Ф.12 расширен: фон-индекс всех `.nv` по module-model (не glob), решение по persistence — scope-in/out с обоснованием.
- **Swift macros** (expand-in-editor): N/A — в Nova макросов нет.

### 0.2 Ключевые факты, определяющие архитектуру

**F1. `Span` УЖЕ содержит `file_id: FileId`** (`diag.rs:27-33`):
```rust
pub struct Span { pub start: usize, pub end: usize, pub file_id: FileId }
```
`FileId` = u32, `MAIN_FILE_ID = 0`. Есть `SourceMap` (`diag.rs:78-136`): `FileId → (path, source)`,
методы `register()`, `get()`, `path_for()`.

**F2. `PeerFile` хранит провенанс** (`ast/mod.rs:71-104`): `path: PathBuf`, `file_id: FileId`,
`items_here: Vec<Item>`, `is_entry_module: bool`, `module_name: Vec<String>`. После
`resolve_imports_inline` поле `module.peer_files` **populated** — у каждого файла есть path+file_id.

**F3. НО nova-lsp теряет провенанс.** hover/goto используют `nova_codegen::parser::parse(src)`
(single-string), который зовёт `parse_with_file_id(src, MAIN_FILE_ID=0)` (`parser/mod.rs:10359`).
Все спаны входного файла → `file_id=0`. При `resolve_imports_inline` инлайненные items парсятся
с **distinct** `file_id` (`imports.rs:685`), но nova-lsp нигде не строит `SourceMap` (grep пуст)
→ не может отобразить `file_id → path`. `resolve_imports_inline` возвращает `Result<()>`, не SourceMap.

**F4. `ModuleEnv` НЕ содержит per-expression типов** (`types/mod.rs:62-84`): только `types`, `fns`
(с overload-Vec), `consts`, proven-сайты. Checker вычисляет типы выражений при обходе, но **не
сохраняет** карту `Span → Ty`. `Ty` enum — `types/mod.rs:18-53` (Int/Float/Str/Bool/Named/Array/Tuple/Func/…).

**F5. Нет workspace `Cargo.toml`** в корне. Сборка: `cd nova-lsp && cargo build --release`.
Тесты nova-lsp: `cargo test` (НЕ требует `NOVA_GC_LIB_DIR` — это только для nova_tests C-codegen).
Unit-тесты = прямой вызов функций; integration = spawn бинаря + JSON-RPC по stdio.

**F6. `references` УЖЕ cross-file** (`symbols.rs:189`, `collect_nv_files` `symbols.rs:761`). Gap не
в «single-file», а в (a) full-FS-скан per-request (O(n)) и (b) regex без semantic-фильтра.

### 0.3 Корневая зависимость → порядок фаз

Многие фичи (completion типов, hover на `r.start`, inlay hints, signature dispatch) упираются в
**одну** недостающую вещь: **карту типов выражений** (`expr_types: Map<Span, Ty>`). Поэтому план
делится так: сначала фундамент (provenance + expr_types), затем фичи поверх него.

---

### 0.5 Аудит соответствия compiler-conventions (3-агентный аудит 2026-07-03)

Прогон nova-lsp против [docs/dev/compiler-conventions.md](../dev/compiler-conventions.md), 26 находок. **Бóльшая часть уже запланирована** этим планом (Ф.2 `expr_types` закрывает re-derive типов; Ф.5 — method-dot из TypeMethodMap; Ф.7 — rename через symbol-table). **НО вскрыты НЕПОКРЫТЫЕ критические баги в check-pipeline `compiler.rs` — root причина «красной» диагностики в IDE** (conformance §4/§1):

- **🔴 [M-104.10-import-diag-swallowed]** (`compiler.rs:148`, §4 тихая дыра): `let _ = resolve_imports_inline(...)` — Result с import-ошибками (cycle/missing/unreadable peer) **молча отброшен** → юзер видит шум «unknown type» вместо реальной import-диагностики. Фикс: смаппить import-ошибки в LSP-диагностики.
- **🔴 [M-104.10-degraded-cu-red]** (`compiler.rs:137`): в degraded-путях (unsaved buffer `path=None`; файл вне репо `find_repo_root=None`; **любой Err резолва** → ранний return ДО `module.peer_files=…`) → `peer_files` пуст → `check_module` видит только entry-файл → **символы peer-файлов И prelude (`print`/`Vec`) краснеют ложно**. Подтверждённый root красной диагностики на folder-module conformance-тестах. Фикс: fallback-скан entry-dir на peers по module-имени независимо от repo-root; при Err — surface.
- **🔴 [M-104.10-lsp-cmd-check-drift]** (`compiler.rs:137`, §1 дрейф LSP↔компилятор): LSP зовёт `check_module`, а `nova check` — `number_exprs + collect_all_signatures + check_module_with_sig_table` (Plan 162.2 подавляет false-positive на транзитивно-импортированных символах). → LSP выдаёт ошибки, которых нет в `nova check`. Фикс: свести LSP-вход к тому же, что cmd_check — **дом = новая фаза Ф.0.5** (НЕ Ф.2: Ф.2 = expr_types, check-вход не меняет).
- **🟡 [M-104.10-diag-numeric-codes]** (`diagnostic_mapping.rs:93`, §6): `extract_error_code` берёт только symbolic `[E_...]`, теряет legacy числовые `[Ennnn]` (call-arg/type) → code-action dispatch по ним молча не срабатывает. Фикс: ветка `E`+цифры.
- **🟡 §3 хардкод-списки (частично закрыто Ф.5, остаток) → [M-104.10-hardcode-lists]:** `completion.rs` `prelude_items()`/`STD_MODULES` (последний со **СТЕЙЛ-НЕВЕРНЫМИ путями** — `std.collections.map`/`std.sync.mutex`/`std.io`/`std.math` не существуют, врут в import-completion); `code_actions.rs` `known_stdlib_type_module`/`known_stdlib_protocol_import`/`auto_derivable_protocols`; keyword-дубли (`rename.rs NOVA_KEYWORDS` со стейл `let`/`impl`/`blocking`/`suspend`). §3: «что в пакетах — никогда не хардкод» → резолвить из `resolve_std_path` + search-path.
- **🟢 мелочь:** `symbol.rs:588` тип `"float"` (не существует; реально `f64`); `Cargo.toml`/module-заголовки приколочены к закрытому Plan 104; `server.rs:313` голый TODO без маркера.

Зонт: **[M-spec-nova-lsp-conformance-audit]** (backlog).

---


**Ссылка на governance:** нарушения degraded-cu-red/import-diag-swallowed нарушают также [docs/dev/lsp-conventions.md](../dev/lsp-conventions.md) §2.2 (degraded-mode — «полезный результат на неполном коде, не молчать/не паниковать») и §4 (анти-паттерны). Дом всех фиксов §0.5-багов = **Ф.0.5** (BLOCK A, «сейчас»).

## 1. Структура фаз: BLOCK A (фундамент, сейчас) → BLOCK B (фичи) → BLOCK C (полировка, потом)

```
BLOCK A — ФУНДАМЕНТ (обязательно, блокирует почти всё):
  Ф.0  Source provenance в nova-lsp (file_id → path)         ← разблокирует cross-file
  Ф.0.5 🔴 Diagnostic pipeline correctness (root красноты)   ← СЕЙЧАС, до Ф.3 (нужен Ф.0)
  Ф.1  Symbol cache (parsed Module + ModuleEnv per URI)      ← перф, независим
  Ф.2  expr_types: Map<Span, Ty> в ModuleEnv (compiler)      ← разблокирует Ф.5/Ф.6/B.*

BLOCK B — ФИЧИ (после A):
  Ф.3  Cross-file goto-definition           (нужен Ф.0)
  Ф.4  Cross-file hover                      (нужен Ф.0)
  Ф.5  Type-driven completion методов        (нужен Ф.2 + Ф.0)
  Ф.6  Hover на member-access (r.start)       (нужен Ф.2)
  Ф.7  Rename через symbol-table              (нужен Ф.0 + references)
  Ф.8  Signature help по типу receiver        (нужен Ф.2)
  Ф.15 documentHighlight (7/7 пиров)          (нужен Ф.7-резолвер)

BLOCK C — ПОЛИРОВКА (можно отложить, отдельный milestone):
  Ф.9  Inlay hints (типы + имена параметров)  (нужен Ф.2)
  Ф.10 Full semantic tokens                   (нужен Ф.2)
  Ф.11 Organize imports                       (независим)
  Ф.12 Incremental references index           (независим)
  Ф.13 Completion resolve (lazy doc/detail)   (независим)
  Ф.16 foldingRange / Ф.17 selectionRange     (синтаксические, независимы)

BLOCK D — ЗАКРЫТИЕ:
  Ф.14 Спека/D/Q/доки + закрытие маркеров + close-out
```

**Минимум для «V2 production» релиза:** BLOCK A + BLOCK B. BLOCK C — отдельный milestone (V2.1),
но фазы спроектированы здесь полностью, чтобы не переоткрывать дизайн.

---

## BLOCK A — ФУНДАМЕНТ

### Ф.0 — Source provenance в nova-lsp (file_id → path map)

**Проблема (F3):** nova-lsp не знает, из какого файла пришёл инлайненный item → cross-file
goto/hover невозможны.

**Дизайн (production, без SourceMap-рефакторинга компилятора):**
Использовать УЖЕ заполняемое `module.peer_files` (F2). После `resolve_imports_inline` каждый
`PeerFile` несёт `(file_id, path)`. Построить в nova-lsp `FileIdMap = HashMap<FileId, PathBuf>`
обходом `module.peer_files`. Этого достаточно — НЕ требует менять компилятор.

**НО** есть тонкость: входной файл парсится через `parse(src)` (file_id=0) ДО инлайна, а
`resolve_imports_inline` ре-парсит peers с distinct id. Нужно убедиться, что spans
оригинальных items входного файла консистентны с тем, что в `peer_files` для entry-модуля.

**Шаги:**
1. **Новый модуль `nova-lsp/src/provenance.rs`:**
   ```rust
   pub struct ResolvedModule {
       pub module: Module,
       pub items_start: usize,
       pub file_map: HashMap<FileId, PathBuf>,  // из peer_files
       pub env: Option<ModuleEnv>,              // заполняется Ф.1/Ф.2
   }
   pub fn resolve_module_for(path: &Path, src: &str) -> ResolvedModule
   ```
   Внутри: parse → `resolve_imports_inline` (catch_unwind как сейчас) → собрать `file_map` из
   `module.peer_files` (`pf.file_id → pf.path`). Entry-file → его реальный path.
2. **Хелпер `span_to_location(span, file_map, fallback_uri) -> Location`:** если `span.file_id`
   есть в `file_map` → `Url::from_file_path(path)`; иначе `fallback_uri` (текущий файл).
3. **Проверить consistency entry-file spans.** Если `parse(src)` даёт file_id=0, а `peer_files`
   для entry даёт file_id=N≠0, добавить в `file_map` маппинг `0 → entry_path` И `N → entry_path`.
   (Оба указывают на тот же файл — безопасно.)
4. Рефактор `hover.rs` и `goto_definition.rs` на использование `resolve_module_for` вместо
   inline-парсинга (Ф.3/Ф.4 завершат переключение).

**Спека/доки:** D-блок **D378** (ex-D304 занят Test Category Selectors; см. Ф.14) — «LSP source provenance via peer_files».

**Тесты (`nova-lsp/src/provenance.rs` unit + integration):**
- POS: `resolve_module_for` на файле с `import` → `file_map` содержит ≥2 записи (entry + prelude).
- POS: `span_to_location` для спана из другого файла → Location с ДРУГИМ URI.
- POS: `span_to_location` для спана entry-файла → Location с тем же URI.
- NEG: спан с file_id, которого нет в map → fallback на текущий URI (не паника).
- NEG: файл без импортов → `file_map` ≥1 запись (только entry), goto работает single-file.
- EDGE: битый импорт (resolve паникует) → catch_unwind, `file_map` хотя бы с entry, не паника.

**Критерии приёмки Ф.0:**
1. `resolve_module_for` возвращает корректный `file_map` (verified обходом `peer_files`).
2. cross-file спан → правильный URI; entry-спан → текущий URI.
3. 0 паник на битых импортах (catch_unwind).
4. Все новые тесты PASS; 0 регрессий в существующих nova-lsp тестах.
5. **Без упрощений:** провенанс через реальные `peer_files`, НЕ через повторный текстовый поиск
   определения по имени в файлах.

---

### Ф.1 — Symbol cache (parsed Module + ModuleEnv per URI)

**Проблема:** каждый hover/goto/completion заново парсит + `resolve_imports_inline` (инлайнит
450+ prelude items) + (после Ф.2) `check_module`. На большом файле ~50–100ms.

**Дизайн:**
1. В `WorkspaceState` (`state.rs`) добавить:
   ```rust
   pub resolved_cache: DashMap<Url, CachedResolved>,
   ```
   где `CachedResolved { resolved: Arc<ResolvedModule>, version: i32 }`.
2. `get_or_build_resolved(uri, version, src) -> Arc<ResolvedModule>`: если cache hit по version →
   вернуть; иначе построить через `resolve_module_for` (+ `check_module` для env, Ф.2) и закешировать.
3. Инвалидация: `didChange` обновляет version → следующий запрос перестроит. `didClose` → удалить.
4. **Потокобезопасность:** `Arc` для shared-read без клонирования Module; DashMap для concurrent.
5. **Бюджет памяти:** кешировать только открытые документы (на `didClose` чистить). Не кешировать весь workspace.
6. **Инвалидация по зависимостям (урок zls, Ред.2):** при `didChange` файла A инвалидировать не только сам URI, но и кеши файлов, импортирующих A (обратные зависимости из module-graph). V2-минимум: инвалидировать сам URI + пометить `[M-104.10-dependent-invalidation]` для полной reverse-dep инвалидации, если не сделано.

**Тесты:**
- POS: два hover-запроса подряд на одной version → второй берёт из кеша (проверить через
  счётчик построений — добавить `AtomicU64 build_count` в тест-режиме или через timing).
- POS: после `didChange` (новая version) → перестроение (build_count++).
- POS: `didClose` → запись удалена из cache.
- NEG: запрос на незакешированный URI → построение без паники.
- EDGE: конкурентные запросы на один URI (spawn 2 потока) → без data-race (DashMap), оба
  получают валидный результат.
- PERF: hover на файле 1000 строк из кеша ≤10ms (vs ≥50ms холодный) — `perf.rs` measure.

**Критерии приёмки Ф.1:**
1. Cache hit по совпадающей version (не перестраивает).
2. Инвалидация на `didChange`/`didClose` корректна.
3. Concurrent-safe (DashMap + Arc), тест на 2 потока PASS.
4. Hover из кеша ≤10ms (perf-тест).
5. **Без упрощений:** кешируется полный `ResolvedModule` (Module+file_map+env), не частично.

---

### Ф.2 — `expr_types: HashMap<Span, Ty>` в ModuleEnv (COMPILER-CODEGEN)

**Это крупнейшая и самая фундаментальная фаза.** Корень для Ф.5/Ф.6/Ф.8/Ф.9/Ф.10.

**Проблема (F4):** checker вычисляет типы выражений, но выбрасывает. IDE не может узнать тип
произвольного выражения (`r.start`, `foo().bar`, receiver в `x.method()`).

**Дизайн (production, opt-in чтобы не замедлять обычный compile):**
1. **Добавить поле в `ModuleEnv`** (`types/mod.rs`):
   ```rust
   pub expr_types: HashMap<Span, Ty>,   // заполняется только при record_expr_types=true
   ```
2. **Флаг сбора.** Добавить в `TypeCheckCtx` булев `record_expr_types`. Обычный `check_module`
   → `false` (zero-overhead, как сейчас). Новый `check_module_with_expr_types(module) -> ModuleEnv`
   → `true`. Это критично: НЕ замедлять `nova check`/`build`/`test`.
3. **Точки записи.** В `infer_expr` (или эквивалент — найти центральную функцию вывода типа
   выражения в `types/`), после вычисления `ty` выражения `e`: если `record_expr_types`, то
   `env.expr_types.insert(e.span, ty.clone())`. Покрыть: Ident, Member, Index, Call, Binary,
   литералы, Range, As, TupleLit, ArrayLit, RecordLit. Для member-access — записать тип и
   объекта, и результата (нужно для Ф.6).
4. **Span как ключ.** `Span` уже `Hash+Eq` (`diag.rs:27`). Коллизии маловероятны (start/end/file_id
   уникальны на выражение). Если выражение синтезировано без span (span=default) — НЕ записывать.
5. **Граница ответственности.** Если тип невыводим (generic-параметр, ошибка) → не записывать
   (отсутствие в map = «неизвестно», IDE деградирует gracefully).

**Где искать центральную функцию инференции:** `compiler-codegen/src/types/` — grep `fn infer_expr`,
`fn check_expr`, `fn expr_type`, `Ty::` возвраты. Если инференции выражений как единой функции нет
(bootstrap может проверять структурно) — оценить объём: возможно потребуется добавить
expression-type walker. **Это исследовать ПЕРВЫМ шагом Ф.2** и зафиксировать реальный объём;
если walker отсутствует целиком — выделить под-фазу Ф.2a (построить минимальный expr-type walker
для IDE: literals + local vars + fn-call-return + member-on-record + index — покрывает 90% IDE-кейсов)
и Ф.2b (полное покрытие) с маркером на остаток.

**Спека:** **D379** (Ф.14) — «ModuleEnv.expr_types — opt-in per-expression type map для IDE».
Зафиксировать: opt-in (zero-cost для compile), semantics «отсутствие = неизвестно».

**Тесты (compiler-codegen, `nova_tests/plan104_10/` + Rust unit в types/):**
- POS (Rust unit): `check_module_with_expr_types` на `ro x = 5` → `expr_types` содержит span литерала `5` → `Ty::Int`.
- POS: `ro r = 0..=5` → span Range → `Ty::Named("Range")` (или как Range представлен).
- POS: `foo()` где `foo -> str` → span Call → `Ty::Str`.
- POS: `rec.field` где field: int → span Member → `Ty::Int`; span obj → `Ty::Named("Rec")`.
- POS: `arr[i]` где arr: []int → span Index → `Ty::Int`.
- POS: zero-overhead — `check_module` (флаг false) → `expr_types` пуст.
- NEG: невыводимое выражение (bare generic T) → НЕ в map (graceful).
- NEG: синтезированный span (default) → НЕ записан.
- EDGE: вложенные `a.b.c` → все три уровня в map с правильными типами.

**Критерии приёмки Ф.2:**
1. `check_module_with_expr_types` заполняет map; `check_module` оставляет пустым (zero-overhead доказан тестом).
2. Покрыты: литералы, Ident, Member, Index, Call, Binary, Range, As, Tuple/Array/Record lit.
3. Невыводимые/синтетические → не в map (без мусора).
4. 0 регрессий в полном compile-pipeline (`expr_types` не влияет на codegen/diagnostics).
5. **Без упрощений:** реальная инференция из checker, НЕ текстовая эвристика. Остаток покрытия
   (если Ф.2a/b split) — явный маркер `[M-104.10-expr-types-coverage]` с перечнем непокрытого.

---

### Ф.0.5 — Diagnostic pipeline correctness (🔴 root ложной красной диагностики) — СЕЙЧАС, приоритет ВЫШЕ Ф.3

**Проблема (аудит §0.5, 3 подтверждённых бага + 2 мелких — user-visible: ложные красные подчёркивания):**
LSP-диагностика расходится с `nova check` и краснит валидный код. Это ХУЖЕ всех 7 пиров (RA/gopls не дают false-red на валидном folder-модуле) и нарушает [lsp-conventions.md](../dev/lsp-conventions.md) §2.2 (degraded-mode) + compiler-conventions §4.

**Дизайн (5 фиксов):**
1. **[M-104.10-import-diag-swallowed]** (`compiler.rs`, символ `let _ = resolve_imports_inline`): НЕ отбрасывать `Result` — собрать import-ошибки (cycle/missing/unreadable peer) и смаппить в LSP-диагностики (§6-контракт compiler-conventions: либо диагностика, либо маркер). Юзер видит реальную причину, не шум «unknown type».
2. **[M-104.10-degraded-cu-red]** (`compiler.rs`, `check_source_inner`): при `path=None` (unsaved buffer) / `find_repo_root=None` (файл вне репо) / **любом Err резолва** — сейчас ранний return ДО заполнения `module.peer_files` → чекер видит только entry-файл → символы peer-файлов И prelude (`print`/`Vec`) краснеют ложно. Фикс: **fallback-скан entry-dir на peers по module-имени** (folder-module CU) независимо от repo-root; unsaved buffer → резолвить peers с диска по URI-директории; при Err резолва — surface (см. п.1), НЕ оставлять peer_files пустым.
3. **[M-104.10-lsp-cmd-check-drift]** (`compiler.rs`, `check_source_inner`): LSP зовёт голый `check_module`, а `nova check` (cmd_check) — `number_exprs + collect_all_signatures + check_module_with_sig_table` (Plan 162.2 подавляет false-positive на транзитивно-импортированных символах). Фикс: **свести LSP check-вход к тому же пайплайну, что cmd_check** (включая 162.2-suppression). Резолюция Q-104-4 (Ф.14).
4. **[M-104.10-diag-numeric-codes]** (`diagnostic_mapping.rs`, `extract_error_code`): берёт только symbolic `[E_...]`, теряет legacy числовые `[Ennnn]` → code-action dispatch по ним молча не срабатывает. Фикс: ветка `E`+цифры.
5. **[M-104.10-hardcode-lists]** (частично; остаток →Ф.5): `completion.rs STD_MODULES` содержит СТЕЙЛ-НЕВЕРНЫЕ пути (`std.collections.map`/`std.sync.mutex`/`std.io`/`std.math` — не существуют, врут в import-completion). Здесь — снять хотя бы стейл (полный резолв из search-path → Ф.5).

**Тесты (nova-lsp Rust unit + integration):**
- **POS:** import cycle/missing peer → **реальная** import-диагностика (не «unknown type»); unsaved buffer (`path=None`) с prelude-символами → **0 false-red** на `print`/`Vec`; folder-module без repo-root → peer-символы резолвятся, **0 false-red**; символ из 162.2-транзитивного импорта → нет ошибки в LSP (идентично `nova check`); `[E7301]`-числовой код → code-action lightbulb появляется.
- **NEG:** РЕАЛЬНАЯ type-ошибка всё ещё репортится (фикс не глушит валидные диагностики); битый peer-файл не глушит валидные диагностики entry; несуществующий `std.io` в import-completion НЕ предлагается.
- **PARITY:** LSP-диагностики == `nova check` на conformance-сэмпле (byte-parity набора кодов/спанов).

**Критерии приёмки Ф.0.5:**
1. Import-ошибки **surface** (не swallowed); юзер видит причину.
2. **0 ложных red** на valid folder-module + degraded-CU (unsaved / вне-repo) фикстурах.
3. LSP-diag ≡ `nova check` на conformance-сэмпле (включая 162.2-suppression).
4. Числовые `[Ennnn]` коды → code-actions срабатывают.
5. **Без упрощений:** surface реальной ошибки (НЕ подавление); fallback-скан peers по module-имени (НЕ текстовый grep); свести к cmd_check-пайплайну (НЕ параллельный check).
6. **Гейт корректности (Ред.2):** diagnostic pos+neg (detect-подобный набор фикстур), НЕ nova_tests byte-baseline ([[feedback-nova-tests-not-correctness-gate]]).

---

## BLOCK B — ФИЧИ

### Ф.3 — Cross-file goto-definition

**Нужен:** Ф.0.

**Дизайн:** `compute_goto_definition` использует `resolve_module_for` (кеш Ф.1) → `resolve_symbol_at`
→ `symbol.span()` → `span_to_location(span, file_map, uri)`. Если символ объявлен в другом файле →
Location с тем URI; range через byte_offset в ИСХОДНИКЕ того файла (прочитать с диска или из
открытых docs).

**Тонкость:** range вычисляется в координатах файла-цели, не входного. Нужен исходник цели для
UTF-16 конверсии. Брать из `WorkspaceState.docs` если открыт, иначе читать с диска.

**Тесты:**
- POS: goto на fn из другого модуля → Location с URI того модуля, корректный range.
- POS: goto на тип из импорта → правильный файл.
- POS: goto на локальную fn в том же файле → тот же URI (регресс single-file).
- POS: goto на prelude-символ (`assert`) → Location в `std/prelude/*.nv`.
- NEG: goto на неизвестный символ → None.
- NEG: goto на whitespace → None.
- EDGE: goto на символ из файла, который не открыт в редакторе → читается с диска, range корректен.
- Integration (spawn): полный JSON-RPC `textDocument/definition` cross-file.

**Критерии приёмки Ф.3:**
1. Cross-file goto возвращает правильный URI + range.
2. Single-file goto не сломан (регресс-тесты).
3. Range корректен в UTF-16 для файла-цели (multi-byte тест).
4. `definition_provider` capability уже `true` — теперь честно cross-file.
5. **Без упрощений:** резолв через span.file_id+file_map, НЕ повторный текстовый grep по имени.

---

### Ф.4 — Cross-file hover

**Нужен:** Ф.0 (+ Ф.1 для перфа).

**Дизайн:** hover уже резолвит prelude-символы по имени (lookup_decl_by_name). Ф.4 добавляет: для
символа из другого файла показывать в hover путь-источник (опционально) и гарантировать, что
doc-comment берётся из реального объявления. Переключить `compute_hover` на `resolve_module_for`.

**Тесты:**
- POS: hover на cross-file fn → сигнатура + doc из файла-источника.
- POS: hover на локальный символ → без регресса.
- NEG: hover на неизвестном → None.
- EDGE: hover на символ, чей doc — в другом файле → doc отображается.

**Критерии приёмки Ф.4:**
1. Hover на cross-file символ показывает корректную сигнатуру+doc.
2. 0 регрессий локального hover.
3. **Без упрощений:** doc из реального decl.

---

### Ф.5 — Type-driven completion методов

**Нужен:** Ф.2 (expr_types) + Ф.0.

**Предшествующая очистка (до Ф.5):** хардкоженные таблицы методов (`int_methods`, `str_methods`,
`vec_methods`, `f64_methods`, `bool_methods`, `option_methods`, `result_methods`) и текстовые
эвристики (`infer_type_of_expr`, `methods_for_type`) **удалены** из `completion.rs` (2026-06-19).
`method_items` теперь опирается только на `scan_module_methods` — AST-скан объявлений в текущем
файле. Stdlib-методы (`str`, `int`, …) предлагаться не будут, пока Ф.5 не реализует type-driven
путь — это ожидаемо: пользователь видит только то, что реально объявлено в его коде.

**Проблема:** без expr_types нельзя отличить, к какому типу принадлежит выражение-ресивер.

**Дизайн:**
1. При completion-контексте `MethodDot`: найти выражение-ресивер перед `.`, взять его span, найти
   тип в `expr_types` (Ф.2). Если найден `Ty::Named("Foo")` → искать все методы с receiver `Foo` в
   `module.items` (включая cross-file через inline) → CompletionItems с сигнатурами.
2. Для stdlib-типов (str/int/Vec/…): методы берутся из AST std/ через тот же механизм — **никаких
   встроенных таблиц**. Единственный источник истины — TypeMethodMap компилятора.
3. Fallback если expr_types не покрыл выражение: показать все методы всех типов текущего файла
   (уже реализовано в `scan_module_methods`).
4. **§3-хардкод-остаток (Ред.2, [M-104.10-hardcode-lists]):** удалить хардкод-списки имён пакетов/типов/протоколов —
   import-path-completion (`STD_MODULES` со стейл-путями), `code_actions.rs` (`known_stdlib_type_module`/
   `known_stdlib_protocol_import`/`auto_derivable_protocols`), `rename.rs NOVA_KEYWORDS` (стейл `let`/`impl`/`blocking`/`suspend`).
   Резолвить из `resolve_std_path` + search-path (обход stdlib-каталога/манифеста); keyword-множество — из лексера компилятора.
   POS: `import std.│` предлагает РЕАЛЬНО существующие модули (FS-скан stdlib-dir); NEG: несуществующий `std.io` НЕ предлагается.
   compiler-conventions §3: «что в пакетах — никогда не хардкод».

**Тесты:**
- POS: `ro u User = ...; u.` → методы User.
- POS: `ro s str = ""; s.` → методы str из std (через TypeMethodMap).
- POS: `foo().` где `foo -> Bar` → методы Bar.
- POS: cross-file тип → его методы предлагаются.
- NEG: `unknown_var.` → graceful (не паника, пустой список или все методы файла).
- EDGE: `a.b.` (chained) → методы типа `a.b`.

**Критерии приёмки Ф.5:**
1. Методы пользовательских типов предлагаются через expr_types + TypeMethodMap.
2. Stdlib-методы (str/int/Vec/…) предлагаются из TypeMethodMap — без хардкода.
3. Cross-file типы покрыты.
4. **Без упрощений:** никаких встроенных таблиц методов/типов/пакетов/keywords в LSP-коде — всё из реестра компилятора / search-path.

---

### Ф.6 — Hover на member-access (r.start)

**Нужен:** Ф.2.

**Проблема:** `ro r = 0..=5; r.start` — hover на `start` ничего не показывает.

**Дизайн:** в `find_ident_in_bodies_from` / resolver — если cursor на `obj.field`: взять тип `obj`
из `expr_types[obj.span]` → `Ty::Named("Range")` → найти поле/метод `start` в decl `Range` →
вернуть SymbolInfo с типом поля.

**Тесты:**
- POS: `r.start` где r: Range → hover показывает `start int` (или фактический тип поля).
- POS: `rec.field` пользовательского record → тип поля.
- POS: `obj.method` → сигнатура метода.
- NEG: `r.nonexistent` → None (поля нет).
- EDGE: `a.b.c` → hover на `c` → тип через цепочку.

**Критерии приёмки Ф.6:**
1. Hover на поле через тип объекта из expr_types работает.
2. Методы тоже резолвятся.
3. Несуществующее поле → None.
4. **Без упрощений:** тип объекта из expr_types, поле из реального decl.

---

### Ф.7 — Rename через symbol-table

**Нужен:** Ф.0 + references.

**Проблема:** rename = regex word-boundary → переименует `foo` во всех scope с тем же именем.

**Дизайн:**
1. `compute_rename`: сначала `resolve_symbol_at(cursor)` → `(name, decl_span, decl_file_id)`.
2. Для каждого кандидата-вхождения (из текущего regex-скана) — резолвить его символ и сравнивать
   `decl_span`. Переименовывать только совпадающие с тем же объявлением.
3. **Реализм bootstrap:** полный resolve каждого вхождения дорог и требует точного name-resolution,
   которого в bootstrap-checker может не быть. Поэтому V2-rename использует **scope-aware фильтр**:
   - резолвить символ под курсором → определить его kind (local var / fn / type / field).
   - для **local var**: ограничить rename телом объявляющей функции (scope известен из AST).
   - для **top-level (fn/type)**: cross-file rename как сейчас, но с проверкой, что вхождение не
     затенено локальным биндингом того же имени (shadow-check через AST scope-walk).
4. Сохранить атомарный post-rename type-check (D296) — уже есть.

**Тесты:**
- POS: rename local var → меняется только в её scope, не трогает одноимённую в другой функции.
- POS: rename top-level fn → все cross-file вхождения.
- POS: shadow — `fn a(){ ro x=1 } fn b(){ ro x=2 }` rename x в a → b.x не тронут.
- NEG: rename в conflict (имя занято) → ошибка через atomic check (D296).
- NEG: rename keyword/literal → отклонён prepareRename.
- EDGE: rename типа, используемого cross-file → все файлы.

**Критерии приёмки Ф.7:**
1. Local var rename ограничен scope (shadow-safe).
2. Top-level rename cross-file + shadow-check.
3. Atomic conflict-check (D296) сохранён.
4. **Без упрощений:** scope из AST, НЕ просто regex. Остаток (если полный per-occurrence resolve
   не сделан) → маркер `[M-104.10-rename-full-resolve]` с обоснованием.

---

### Ф.15 — documentHighlight (подсветка вхождений символа) — NEW Ред.2, все 7 пиров имеют

**Нужен:** Ф.7-резолвер (resolve_symbol_at). **Дельта ~0.5d.**

**Дизайн:** `textDocument/documentHighlight`: резолвить символ под курсором (тот же `resolve_symbol_at`, что Ф.7) → вернуть все вхождения ТОГО ЖЕ символа в текущем файле (read/write kind). **НЕ regex по слову** — иначе те же false-positives, что чинит Ф.7.

**Тесты:** POS: курсор на переменной → подсвечены все её вхождения в scope; POS: read vs write kind различены; NEG: одноимённая переменная в другом scope НЕ подсвечена (semantic, не regex); NEG: курсор на keyword → пусто.

**Критерии приёмки:** вхождения через семантический резолв (НЕ regex); scope-correct; read/write kind; **без упрощений**.

---

### Ф.8 — Signature help по типу receiver

**Нужен:** Ф.2.

**Проблема:** `x.foo(│)` берёт первый overload `foo` по имени, игнорируя тип `x`.

**Дизайн:** в `signature_help` — если вызов это `recv.method(...)`: взять тип `recv` из expr_types →
выбрать overload с matching receiver-типом. Для свободных fn с overload по аргументам — выбирать по
числу/типам уже введённых аргументов.

**Тесты:**
- POS: `vec.push(│)` → сигнатура push именно Vec, не другого типа с push.
- POS: overload по числу аргументов → правильный по позиции курсора.
- NEG: неизвестный receiver → fallback на первый (graceful).
- EDGE: вложенный вызов `f(g(│))` → сигнатура g.

**Критерии приёмки Ф.8:**
1. Overload выбирается по типу receiver (expr_types).
2. Fallback graceful при неизвестном типе.
3. **Без упрощений:** dispatch по реальному типу.

---

### Ф.18 — Workspace lifecycle: file watching + willRename + progress + refresh (🔴 must-have, все 7 пиров) — NEW Ред.2

**Проблема (completeness-critic):** без реакции на ВНЕШНИЕ изменения файлов (git checkout/pull, правка вне редактора, codegen) индекс/кеш (Ф.1/Ф.12) протухает молча. `server.rs` не регистрирует watchers, `initialized()` имеет голый TODO. Долгий первичный check/индекс выглядит «зависанием» (нет прогресса).

**Дизайн (4 под-фичи):**
1. **`workspace/didChangeWatchedFiles`** (+ dynamic `client/registerCapability` на `**/*.nv` + `nova.toml`): на внешнее create/change/delete — инвалидировать кеши Ф.1 и записи индекса Ф.12 для затронутых путей (+ обратные зависимости, урок zls Ф.1). Убрать TODO `server.rs`.
2. **`workspace/willRenameFiles` → WorkspaceEdit** (+ `didRenameFiles`): переименование `.nv`-файла в folder-module меняет module-состав/import-пути → вернуть edit, обновляющий `import`-пути в зависящих файлах. Стыкуется с Ф.7-резолвером и module-model.
3. **`window/workDoneProgress/create` + `$/progress`**: репорт прогресса фонового индекса Ф.12 и холодного первичного check (spinner в IDE вместо «завис»).
4. **`{semanticTokens|inlayHint|codeLens}/refresh` (server→client)**: после завершения фон-индекса / перестройки expr_types послать refresh, иначе Ф.9/Ф.10/Ф.12 остаются с холодными данными до следующего edit.

**Тесты:** POS: внешнее изменение peer-файла (симуляция didChangeWatchedFiles) → индекс/диагностика обновились без правки буфера; POS: rename `a.nv`→`b.nv` с импортёром → edit обновил import-путь; POS: долгий индекс шлёт `$/progress` begin/report/end; NEG: watch-событие на не-.nv → игнор; EDGE: удаление файла → записи индекса убраны, битых ссылок нет.

**Критерии приёмки Ф.18:** внешние изменения не оставляют stale-индекс; rename файла обновляет import'ы (folder-module); прогресс виден при долгих операциях; refresh синхронизирует hints; **без упрощений** (реальная инвалидация, не «перезапусти LSP»).

---

### Ф.19 — Navigation extras: typeDefinition + implementation — NEW Ред.2

**Нужен:** Ф.2 (expr_types) + Ф.0 (provenance) + Ф.7-резолвер. **Дельта ~1.5d.**

**Дизайн:**
- **`textDocument/typeDefinition`**: `expr_types[expr.span]` → `Ty::Named(T)` → span объявления типа `T` → `span_to_location`. (7/7 пиров имеют.)
- **`textDocument/implementation`**: у Nova ЕСТЬ протоколы/impl → для протокола под курсором найти все типы с его impl (скан `#impl`/method-таблицы); для метода — переопределения. (6/7 пиров; нет только zls — в Zig нет протоколов.)

**Тесты:** POS: typeDefinition на `ro u = User.new()` → decl `type User`; POS: implementation на протоколе → все реализующие типы (cross-file); NEG: typeDefinition на литерале примитива → decl примитива или None (graceful); EDGE: generic-тип → его decl.

**Критерии приёмки Ф.19:** typeDefinition через expr_types; implementation через реестр impl (НЕ хардкод); cross-file; **без упрощений**.

---

## BLOCK C — ПОЛИРОВКА (отдельный milestone V2.1, дизайн готов)

### Ф.9 — Inlay hints (типы + имена параметров)

**Нужен:** Ф.2. tower-lsp 0.20 поддерживает `InlayHint` (F5).

**Дизайн:** advertise `inlay_hint_provider`. Хендлер `textDocument/inlayHint(range)`:
- **type hints:** для `ro x = expr` без аннотации → hint `: T` из expr_types после имени.
- **parameter name hints:** в вызове `foo(1, 2)` → hint имени параметра перед каждым аргументом
  (как rust-analyzer/IntelliJ).
- Настройки: оба вида включаемы (capability), по умолчанию оба on.

**Тесты:** POS type-hint на `ro x = 5` → `: int`; POS param-hint на `foo(1)` → `a:`; NEG аннотированная
переменная → нет hint; EDGE multi-byte позиции; integration spawn.

**Критерии приёмки:** оба вида hints; off-by-config; UTF-16 корректность; без упрощений (типы из expr_types).

### Ф.10 — Full semantic tokens

**Нужен:** Ф.2. Расширить существующий узкий (Plan 123.5.2) до полного пасса.

**Дизайн:** расширить legend (function/type/variable/parameter/property/keyword/namespace/…). Полный
AST+expr_types walk → токен на каждый идентификатор с правильным типом/модификатором. Сохранить delta
(уже есть). НЕ ломать field-cache подсветку (она становится частным случаем модификатора `cached`).

**Тесты:** POS токены для fn/type/var/param/field различены; POS delta при малом edit; регресс
field-cache `cached` модификатор; EDGE большой файл перф.

**Критерии приёмки:** полный semantic pass; delta работает; field-cache сохранён; без упрощений.

### Ф.11 — Organize imports

**Независим.** `source.organizeImports`: собрать импорты, удалить неиспользуемые (символ не
встречается в body — через expr_types/name-scan), отсортировать, вернуть TextEdit.

**Тесты:** POS удаление неиспользуемого; POS сортировка; POS сохранение используемых; NEG пустой
список импортов → no-op; EDGE re-export `export import` не удаляется как «неиспользуемый».

**Критерии приёмки:** unused убраны, used сохранены, отсортированы; export-import не ломается; без упрощений.

### Ф.12 — Incremental references index

**Независим.** Заменить full-FS-скан (`collect_nv_files` per request) на инкрементальный индекс
имя→[(uri, span)], обновляемый на `didChange`/`didOpen`. Использовать существующий `WorkspaceIndex` как основу.

**Уроки zls/sourcekit-lsp (Ред.2):** (а) **фоновая индексация ВСЕХ `.nv` workspace** (не только открытых) по **module-model** (обход peer_files/repo-структуры, НЕ голый glob) — иначе первый workspace-symbol/refs на холодном проекте упрётся в скан (урок sourcekit indexstore-db); (б) **persistence on-disk индекса** — зафиксировать scope-in/out решением: V2 = in-memory фон-индекс (маркер `[M-104.10-persistent-index]` для on-disk как V2.1); (в) инвалидация по обратным зависимостям — см. Ф.1.

**Тесты:** POS references находятся без полного скана; POS индекс обновляется на didChange; PERF
references на 100 файлов быстрее baseline; EDGE удаление файла → записи убраны.

**Критерии приёмки:** инкрементальный, корректный, быстрее full-scan; без упрощений.

### Ф.13 — Completion resolve (lazy doc/detail)

**Независим.** `resolve_provider: true` + `completionItem/resolve` хендлер: тяжёлые doc/detail
грузятся лениво по запросу клиента, не в initial list (перф большого списка).

**Тесты:** POS resolve добавляет doc; POS initial list без тяжёлого doc; NEG resolve неизвестного
item → graceful.

**Критерии приёмки:** lazy resolve работает; initial list легче; без упрощений.

---

### Ф.16 — foldingRange (сворачивание блоков/импортов) — NEW Ред.2

**Независим** (чисто синтаксический AST-walk, НЕ нужен expr_types). **~0.5d.**

**Дизайн:** `textDocument/foldingRange`: обход AST → folding-регионы для fn-тел, type-тел, блоков `{}`, import-групп, многострочных комментариев. Из span-границ узлов.

**Тесты:** POS: fn-тело → foldingRange; POS: import-группа → один регион; POS: вложенные блоки → вложенные регионы; NEG: однострочный fn → нет региона; EDGE: multi-byte.

**Критерии приёмки:** регионы из AST (НЕ текст-эвристика по отступам); вложенность корректна; **без упрощений**.

### Ф.17 — selectionRange (smart-expand по AST) — NEW Ред.2

**Независим** (синтаксический). **~0.5d.**

**Дизайн:** `textDocument/selectionRange`: для позиции — цепочка расширяющихся диапазонов по AST-иерархии (ident → выражение → statement → блок → fn). Родитель каждого — следующий в цепочке.

**Тесты:** POS: expand с ident → выражение → stmt → fn; POS: несколько позиций за раз; NEG: позиция вне кода → минимальный range; EDGE: вложенные вызовы.

**Критерии приёмки:** иерархия из AST; каждый range ⊃ предыдущий; **без упрощений**.

---

### Ф.20 — codeLens (run-test / references / implementations) + executeCommand — NEW Ред.2, 🏆 differentiator

**Нужен:** Ф.12 (refs-индекс) + `workspace/executeCommand`. **~2d.** (V1 адвертайзит `code_lens_provider`, но ТОЛЬКО field-cache lens — Plan 123.5.1.)

**Дизайн:**
1. **`workspace/executeCommand`** — enabler: серверные команды (`nova.runTest`, code-actions с server-side edit).
2. **run-test lens** (🏆 в связке с `nova test`): над тест-функциями (`test "..."` / `#[test]`-эквивалент) — «▶ Run test | Debug» → `executeCommand` дёргает `nova test <path>`. Owner-named фокус.
3. **references lens**: «N references» над fn/type (из индекса Ф.12).
4. **implementations lens**: «N implementations» над протоколом (из Ф.19-скана).

**Тесты:** POS: lens над тест-функцией → команда запуска; POS: «N references» совпадает с find-references; POS: «N implementations» над протоколом; NEG: не-тест fn → нет run-lens; EDGE: 0 references → «0 references» (не скрывать).

**Критерии приёмки Ф.20:** run-test lens запускает `nova test`; refs/impl-счётчики точны (из индекса); executeCommand работает; **без упрощений** (счётчики из реального индекса, не заглушка).

---

## BLOCK D — ЗАКРЫТИЕ

### Ф.14 — Спека / D / Q / доки / маркеры / close-out

**Спека (`spec/decisions/09-tooling.md`):**
- **D378** (ex-план-D304, занят Test Category Selectors) — «LSP source provenance via peer_files» (Ф.0): как nova-lsp восстанавливает file_id→path
  из `module.peer_files`; cross-file goto/hover контракт.
- **D379** (ex-план-D305) — «ModuleEnv.expr_types — opt-in per-expression type map для IDE» (Ф.2): поле, флаг сбора,
  zero-overhead для compile, semantics «отсутствие=неизвестно», покрытие.
- **D380** (ex-план-D306) — «LSP V2 capability surface» (Ф.0.5/9/10/13/15/16/17): diagnostic-parity контракт, inlay hints, full semantic tokens, documentHighlight/folding/selection, completion resolve — что advertise, дефолты.
- Расширить **D303** (если нужно) ссылками на новые фазы.
- **Поправить стейл-примечание D307** (`02-types.md` + `spec/decisions/README.md`): «D305/D306 зарезервированы proposed-планом 104.10» → «104.10 в итоге получил D378-D380; D305/D306 свободны» (иначе будущий автор считает D305/D306 занятыми).

**Q (`spec/decisions/Q-*` или раздел открытых вопросов):**
- Закрыть **Q-104-1** (incremental compilation strategy) — резолюция: per-URI ResolvedModule cache
  (Ф.1) + expr_types opt-in (Ф.2); полный incremental re-check внутри модуля — отложен, маркер.
- Закрыть **Q-104-2** (task runner UI) — out of scope LSP V2, отдельный план (зафиксировать).
- Закрыть **Q-104-3** (AI-completion hook) — vanilla completion + resolve (Ф.13) достаточно;
  спец-метод не нужен (зафиксировать решение).
- Закрыть **Q-104-4 (NEW Ред.2)** «LSP check-вход ≡ cmd_check pipeline (162.2)?» — РЕЗОЛЮЦИЯ: **да, свести** (Ф.0.5 фикс #3): LSP зовёт тот же `number_exprs + collect_all_signatures + check_module_with_sig_table`, иначе вечный дрейф false-positive. POS-тест на транзитивно-импортированный символ (LSP == nova check).
- Закрыть **Q-104-5 (NEW Ред.2)** «entry-file file_id консистентность (Ф.0 шаг 3: parse даёт 0, peer_files даёт N)?» — оформить как проверяемый инвариант с NEG-тестом (оба id резолвятся в entry-path), либо явный маркер если consistency не гарантируется.

**Доки (`docs/`):**
- **Обновить `docs/dev/lsp-conventions.md` (NEW Ред.2):** зафиксировать LSP-ноту — degraded-mode НЕ даёт false-red (§2.2 усилить кейсом folder-module/unsaved-buffer); запрет хардкод-списков имён пакетов (§3-аналог: резолв из search-path). Это дополнение конвенции при вскрытой дыре (governance разрешает). План ссылается на lsp-conventions.md как на governance-документ (симметрично compiler-conventions).
- Обновить `docs/plans/104-ide-integration.md` — добавить строку про 104.10, обновить таблицу
  «production-grade acceptance bar» (inlay hints/semantic tokens из 🟡 V2 → ✅ если BLOCK C сделан).
- `docs/dev/simplifications.md` — записи по каждому оставшемуся `[M-104.10-*]` маркеру.
- `editors/INSTALL.md` — упомянуть inlay hints / semantic tokens настройки если добавлены.
- `nova-private/discussion-log.md` + `project-creation.txt` — итог (отдельный репо).

**Маркеры в `docs/plans/backlog-followups.md`:**
- CLOSE: `[M-104.2-cross-file-goto]` (Ф.3), `[M-104.2-symbol-cache]` (Ф.1),
  `[M-104.9-dynamic-method-completion]` (Ф.5), `[M-104.2-expr-types-member-hover]` Variant A (Ф.2+Ф.6),
  `[M-104.6-symbol-table-rename]` (Ф.7), `[M-104.2-signature-type-dispatch]` (Ф.8),
  `[M-104.5-organize-imports]` (Ф.11), `[M-104.4-refs-incremental-index]` (Ф.12),
  `[M-104.2-protocol-method-hover]` (Ф.4/Ф.6 если покрыт).
- **CLOSE (§0.5-баги, Ред.2):** `[M-104.10-import-diag-swallowed]`, `[M-104.10-degraded-cu-red]`, `[M-104.10-lsp-cmd-check-drift]`, `[M-104.10-diag-numeric-codes]`, `[M-104.10-hardcode-lists]` — закрываются Ф.0.5 + Ф.5-подпунктом. **Завести их в backlog при старте** (сейчас только зонт `[M-spec-nova-lsp-conformance-audit]`).
- **Выправить phase-refs + дубли CLOSE-маркеров в backlog:** `[M-104.9-dynamic-method-completion]` (backlog говорит Ф.3 → правильно **Ф.5**; дубль-запись схлопнуть), `[M-104.5-organize-imports]` (Ф.6 → **Ф.11**; дубль схлопнуть).
- OPEN (новые, если остаток): `[M-104.10-expr-types-coverage]`, `[M-104.10-completion-text-fallback]`, `[M-104.10-rename-full-resolve]`, `[M-104.10-inlay-config-granularity]`, `[M-104.10-call-hierarchy]`, `[M-104.10-type-hierarchy]`, `[M-104.10-dependent-invalidation]`, `[M-104.10-completion-ranking]`. `[M-104.10-persistent-index]` — ✅ RESOLVED ([Plan 215](215-lsp-index-cache.md), 2026-07-19; см. `backlog-followups.md`).
- **OPEN scope-out (completeness-критик Ред.2, §6):** `[M-104.10-file-rename-imports]` (Ф.18 остаток, если willRename не полон), `[M-104.10-pull-diagnostics]`, `[M-104.10-declaration-alias]`, `[M-104.10-document-link]`, `[M-104.10-lazy-resolve-variants]`, `[M-104.10-workspace-file-ops]`, `[M-104.10-multi-root]` — зарегистрировать при старте как явные scope-out (не молча).

**Критерии приёмки Ф.14:**
1. **D378/D379/D380** написаны и слинкованы из индекса спеки (D304/D305/D306 НЕ использовать — D304 занят, D305/D306 освобождены); D307-примечание поправлено.
2. Q-104-1/2/3 закрыты с явной резолюцией.
3. Все CLOSE-маркеры убраны из OPEN-view backlog; новые OPEN-маркеры добавлены с обоснованием.
4. Доки обновлены (104-ide-integration, simplifications).
5. **Без упрощений:** каждое оставшееся упрощение — отдельный маркер, не молчаливое.

---

## 2. Сводная таблица фаз

| Блок | Ф. | Что | Маркер(ы) | При. | Зав. | Оценка |
|---|---|---|---|---|---|---|
| A | 0 | Source provenance (file_id→path) | новый D378 | P1 | — | 2d |
| A | 0.5 | **Diagnostic pipeline correctness (root красноты)** | `[M-104.10-import-diag-swallowed/degraded-cu-red/lsp-cmd-check-drift/diag-numeric-codes]` | **P1** | Ф.0 | 2.5d |
| A | 1 | Symbol cache per URI | `[M-104.2-symbol-cache]` | P1 | Ф.0 | 1.5d |
| A | 2 | expr_types в ModuleEnv (compiler) | `[M-104.2-expr-types-member-hover]` | P1 | — | 4d |
| B | 3 | Cross-file goto-def | `[M-104.2-cross-file-goto]` | P1 | Ф.0,1 | 1.5d |
| B | 4 | Cross-file hover | `[M-104.2-protocol-method-hover]` | P1 | Ф.0,1 | 1d |
| B | 5 | Type-driven completion | `[M-104.9-dynamic-method-completion]` | P1 | Ф.2,0 | 2d |
| B | 6 | Hover member-access | `[M-104.2-expr-types-member-hover]` | P2 | Ф.2 | 1.5d |
| B | 7 | Rename symbol-table | `[M-104.6-symbol-table-rename]` | P2 | Ф.0 | 2d |
| B | 8 | Signature help по типу | `[M-104.2-signature-type-dispatch]` | P2 | Ф.2 | 1d |
| B | 18 | **Workspace lifecycle: watching/willRename/progress/refresh (must-have)** | `[M-104.10-file-rename-imports]` | **P1** | Ф.1,12 | 2d |
| B | 19 | typeDefinition + implementation | новый | P2 | Ф.2,7 | 1.5d |
| C | 20 | codeLens (run-test/refs/impl) + executeCommand | новый | P3 | Ф.12 | 2d |
| C | 9 | Inlay hints | новый | P3 | Ф.2 | 2d |
| C | 10 | Full semantic tokens | новый | P3 | Ф.2 | 2.5d |
| C | 11 | Organize imports | `[M-104.5-organize-imports]` | P3 | — | 1d |
| C | 12 | Incremental references | `[M-104.4-refs-incremental-index]` | P3 | — | 2d |
| C | 13 | Completion resolve | новый | P3 | — | 1d |
| D | 14 | Спека/D/Q/доки/close-out | — | P1 | все | 1.5d |

**BLOCK A+B+D (V2 production):** ~17.5 dev-day.
**BLOCK C (V2.1):** ~8.5 dev-day.

---

## 3. Порядок исполнения

```
СЕЙЧАС (V2 production):
  Ф.0 → Ф.0.5 (🔴 diagnostic correctness — root красноты; ДО фичей) → Ф.1 (cache)
  Ф.2 (параллельно с Ф.0/Ф.0.5/Ф.1 — другой crate, compiler-codegen)
        ↓ когда Ф.0+Ф.0.5+Ф.1+Ф.2 готовы
  Ф.3, Ф.4 (cross-file goto/hover — нужен Ф.0)
  Ф.5 (completion + §3-хардкод-остаток — нужен Ф.2)
  Ф.6, Ф.8 (member-hover, sig-dispatch — нужен Ф.2)
  Ф.7 → Ф.15 (rename → documentHighlight поверх резолвера — нужен Ф.0)
  Ф.14 (close-out)

ПОТОМ (V2.1 milestone):
  Ф.9, Ф.10 (нужен Ф.2) ; Ф.11, Ф.12 (фон-индекс), Ф.13, Ф.16 (folding), Ф.17 (selection) — независимы
```

---

## 4. Методология исполнения (фоновые агенты / workflow)

> Эти правила ОБЯЗАТЕЛЬНЫ при использовании фоновых агентов или Workflow.

- **НЕ `git stash`** — repo-global, конкурентные worktree делят `.git`. Для baseline-сравнения:
  temp-worktree ИЛИ commit-reset (закоммить → измерь → reset). Никогда не stash при параллельных агентах.
- **`git add` только конкретные файлы**, никогда `git add -A`/`.` — рядом могут работать другие агенты.
- **Перед commit:** `git diff --cached --stat` (в индексе могут быть чужие pre-staged изменения).
- **НЕ добавлять `Co-Authored-By: Claude`** в commit messages.
- **DCO:** каждый commit с `-s` (`git commit -s` → `Signed-off-by`) — конвенция проекта (CI-гейт).
- **Отдельный commit на каждую фазу** (Ф.0, Ф.0.5, Ф.1, … — отдельные коммиты).
- **Rate-limit фоновых агентов/workflow:** серверный rate-limit иногда роняет агента. Стратегия:
  (a) дробить на фазы, не запускать >3–4 агентов параллельно; (b) при падении агента —
  retry той же под-задачи (фазы атомарны и идемпотентны по дизайну); (c) Workflow поддерживает
  resume через `resumeFromRunId` — закешированные фазы не перевыполняются; (d) если фаза падает
  повторно из-за rate-limit — выполнить её inline (не в фоне).
- **Тесты nova-lsp:** `cd nova-lsp && cargo test --release` (НЕ нужен NOVA_GC_LIB_DIR).
- **Тесты compiler-codegen (Ф.2):** `cd compiler-codegen && cargo test --release` + при изменении
  поведения — sample `nova test` через nova-cli для регресс-проверки (НЕ полный — он 60–90 мин;
  дробить батчами <10 мин по `bash-timeout-10min-max`).
- **При изменении `.nv` в std/prelude** — пересобрать nova-cli (include_str!).
- **Baseline для perf (Ф.1):** kill-switch на ТОМ ЖЕ бинаре, не sibling.
- **Последовательная работа над многофазным планом** — в постоянном worktree `nova-p104-10`,
  не плодить sub-worktree (`feedback-sequential-no-sub-worktree`); naming `nova-pNN` (`feedback-worktree-naming`).
- **Завершение фазы/плана:** bidirectional sync с main (merge ветки → main); перед FF в общий main — проверить `HEAD` (`feedback-ff-into-shared-main-repo`), не попасть в чужую ветку.

---

## 5. Глобальные критерии приёмки Plan 104.10 (closure)

1. **BLOCK A+B+D полностью закрыты** (V2 production milestone): Ф.0–Ф.8 + **Ф.0.5** + **Ф.15** + **Ф.18** (workspace lifecycle/watching — must-have) + **Ф.19** (typeDefinition/implementation) + Ф.14. (Ф.20 codeLens — BLOCK C/V2.1.)
2. `cd nova-lsp && cargo test --release` → все тесты PASS (V1 268 + новые), 0 регрессий.
3. `cd compiler-codegen && cargo test --release` → PASS (Ф.2 expr_types), 0 регрессий в compile.
4. **Parity-чеклист vs 7 LSP-пиров** (RA/gopls/tsserver/kotlin-lsp/jdtls/zls/sourcekit; таблица §0.1a) заполнен в `104-ide-integration.md`:
   cross-file goto ✅, type-driven completion ✅, member hover ✅, scope-aware rename ✅, type-driven signature ✅,
   **documentHighlight ✅ (Ф.15)**, **0 false-red diagnostic-parity ✅ (Ф.0.5)**; (BLOCK C: inlay/semantic/organize/folding/selection/incremental-refs — ✅ если V2.1 сделан). IntelliJ — не-LSP референс, вне обязательной планки.
4a. **🔴 0 ложных red** на valid folder-module + degraded-CU (unsaved / вне-repo) фикстурах; LSP-диагностики ≡ `nova check` на conformance-сэмпле (Ф.0.5). Import-ошибки surface, не swallowed.
5. `nova_tests/plan104_10/` фикстуры (Ф.2 expr_types через compiler) PASS через release nova.
6. POS+NEG тесты на КАЖДОЙ фазе (перечислены выше) PASS.
7. D378/D379/D380 в спеке (D304 был занят); Q-104-1/2/3/4 закрыты; D307-примечание поправлено.
8. Все CLOSE-маркеры убраны из backlog OPEN-view; остаточные упрощения — явные `[M-104.10-*]`.
9. **«Без упрощений как для прода»** — обязательный, проверяется на каждой фазе: никакой текстовой
   эвристики там, где есть семантика (expr_types/file_map/symbol-resolve); никаких хардкод-списков имён
   типов/методов/пакетов/keywords в LSP; каждое неизбежное упрощение — отдельный документированный маркер, не молчаливое.
10. **Гейты РАЗВЕДЕНЫ:** Ф.2 (expr_types opt-in) = nova_tests byte-identical baseline (zero-overhead доказан);
    Ф.0.5 (diagnostic-фиксы) = diagnostic pos+neg (detect-подобный набор фикстур), НЕ byte-baseline
    ([[feedback-nova-tests-not-correctness-gate]]). baseline для perf/regress = temp-worktree/commit-reset, ТОТ ЖЕ бинарь.

---

## 6. Связь / lineage

- **Plan 104** — V1 (104.0–104.9), эта фаза — V2 поверх него.
- **Plan 35 Ф.0** — `Span.file_id` + `SourceMap` инфраструктура (используется Ф.0).
- **Plan 123.5.2/123.5.5** — узкие semantic tokens (расширяются Ф.10).
- **D296** — atomic rename contract (сохраняется в Ф.7).
- **D303** — items_start pattern + Variant B (Ф.2 = Variant A, надмножество).
- **call hierarchy** (`prepareCallHierarchy`/incoming/outgoing) — **scope-out по приоритету, НЕ по codegen** (Ред.2-фикс: это статический граф вызовов из индекса ссылок Ф.12 + expr_types Ф.2 — фундамент готов, дельта ~2d; 6/7 пиров имеют, нет только у zls). Маркер `[M-104.10-call-hierarchy]`.
- **type hierarchy** (super/sub-types по protocol/impl-модели) — **scope-out** (~5-6/7 пиров имеют; дельта поверх symbol-table ~1.5d). Маркер `[M-104.10-type-hierarchy]`.
- **DAP (отладчик)** — после native codegen / LLVM (Plan 38): требует рантайма. **JetBrains native plugin**; **extract-function/extract-type** рефакторинги — отдельные планы.
- **macros expand-in-editor** — N/A (в Nova нет макросов).

**LSP-methods scope-out с маркерами (completeness-критик Ред.2 — явно, не молча):**
- **`textDocument/diagnostic` (PULL-model 3.17)** — push `publishDiagnostics` (V1+Ф.0.5) даёт функциональный паритет; pull = оптимизация (клиент запрашивает для видимых файлов). Маркер `[M-104.10-pull-diagnostics]`.
- **`textDocument/declaration`** — в Nova declaration ≡ definition (нет .h/.c split) → алиас на Ф.3 одной строкой; маркер `[M-104.10-declaration-alias]`.
- **`textDocument/documentLink`** (кликабельные import-пути/doc-ссылки) — `[M-104.10-document-link]`.
- **`inlayHint/resolve`, `codeAction/resolve` (V1 `resolve_provider=false`), `workspaceSymbol/resolve`, `semanticTokens/range` (V1 range=false)** — ленивые/range-варианты (перф); маркеры `[M-104.10-lazy-resolve-variants]`.
- **`workspace/willCreateFiles`/`willDeleteFiles`/`didDeleteFiles`**, **`didChangeWorkspaceFolders`** (multi-root) — `[M-104.10-workspace-file-ops]`, `[M-104.10-multi-root]`.
- **`textDocument/linkedEditingRange`** (парные диапазоны) — низкая ценность для Nova (нет тегов); N/A/маркер.
- **`textDocument/inlineValue`** — tied to DAP (после native codegen, Plan 38); N/A-до-DAP.
- **`textDocument/moniker`** — LSIF cross-repo индексация; вне одиночного LSP; N/A.

**Verify-нота (V1 advertise-ит, план подтверждает не-заглушку в Ф.14):** `textDocument/rangeFormatting` (`server.rs` document_range_formatting_provider=true) и `onTypeFormatting` (триггеры `\n`/`}`) — проверить, что хендлеры реально форматируют (не молча whole-file/None); если заглушка → маркер.

---

## Статус выполнения (2026-07-04)

**✅ ВЫПОЛНЕН** — все 24 фазы + close-out. Ветка `plan-104-10` (worktree `nova-p104-10`).
Исполнение — 3 sequential Workflow (ultracode): Foundation → Features → Polish; **0 ошибок,
0 rate-limit падений** за всю сессию (24 агента, ~4.5M subagent-токенов). Каждая фаза —
отдельный DCO-подписанный коммит.

**Финальный гейт:** nova-lsp **408 lib pass / 0 fail / 0 ignored** (+150 к baseline 258) + все
integration-сьюты pass; compiler-codegen build green (7 pre-existing fail + 1 stack-overflow —
подтверждены на baseline через temp-worktree, НЕ регрессии). 2 Ф.5-pending completion-теста
**возвращены** из ignore и зелёные.

| Блок | Фаза | Что | Commit |
|---|---|---|---|
| — | baseline | AST-drift fix (TypeSet/defer) + gate Ф.5-тестов | `ac3b4241` |
| A | Ф.2 | `ModuleEnv.expr_types` opt-in (compiler) | `00e07e70` |
| A | Ф.0 | source provenance (file_id→path) | `007a5dea` |
| A | Ф.0.5 | diagnostic pipeline correctness (root красноты) | `1c4776b8` |
| A | Ф.1 | symbol cache per-URI | `394802fc` |
| B | Ф.3 | cross-file goto-definition | `5b5bb10d` |
| B | Ф.4 | cross-file hover | `3b6df9e7` |
| B | Ф.5 | type-driven completion + §3 hardcode-removal | `e2898a56` |
| B | Ф.6 | member-access hover (obj.field) | `927433ef` |
| B | Ф.8 | signature help dispatch по типу | `1f5b3409` |
| B | Ф.7 | scope-aware rename (symbol-table) | `bfc7a4a5` |
| B | Ф.15 | documentHighlight | `f55143f6` |
| B | Ф.18 | workspace lifecycle (watch/willRename/progress) | `dc205d23` |
| B | Ф.19 | typeDefinition + implementation | `b8e8ddfb` |
| C | Ф.16 | foldingRange | `3a6b75b1` |
| C | Ф.17 | selectionRange | `8c0509f3` |
| C | Ф.11 | organize imports | `d5d84884` |
| C | Ф.12 | incremental references index | `f6b9642a` |
| C | Ф.13 | completion resolve (lazy) | `58b41cf2` |
| C | Ф.9 | inlay hints (type + param-name) | `67563c45` |
| C | Ф.10 | full semantic tokens | `8de200c7` |
| C | Ф.20 | codeLens (run-test/refs/impl) + executeCommand | `3cc9329c` |
| D | Ф.14 | spec D378-380 + Q-104-1..5 + доки + логи + статус | (close-out коммиты) |

**Spec:** D378 (provenance) / D379 (expr_types) / D380 (capability surface + diag-parity) в
09-tooling.md; D307-примечание поправлено (D305/D306 свободны). **Q-104-1..5** закрыты (104-ide-integration.md).

**Convention-compliance (verified):** no-hardcode держится (0 method-таблиц в completion.rs;
known_stdlib_* делегируют в StdlibIndex/`builtin_protocol_names` — single source of truth);
degraded-mode graceful; diag-parity LSP≡`nova check`; тесты на реальном движке (2 метод-теста
un-ignored). Rust-тесты (не `spec_tests/.nv`) — легитимно для LSP-internal/opt-in кода,
задокументировано в D378/D379.

**Открытые `[M-104.10-*]` маркеры** (bounded, P3, plan-санкционированные — не молчаливые упрощения):
`expr-types-coverage`, `vfs-overlay-imports`, `folding-plain-comments`, `organize-imports-namescan`,
`persistent-index`, `dependent-invalidation`, `lsp-cwd-anchor`, `call-hierarchy`, `type-hierarchy`
+ scope-out'ы §6. Все в backlog-followups.md + simplifications.md.

**Остаток (V2.1 / отдельные планы):** call/type-hierarchy (фундамент готов), persistent on-disk
index (sourcekit-стиль), DAP (после native codegen), JetBrains native plugin, extract-refactorings.
