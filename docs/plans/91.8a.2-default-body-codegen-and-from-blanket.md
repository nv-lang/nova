# Plan 91.8a.2 — Default body codegen synthesis + protocols refactor + From identity blanket (D183 amendment)

> **Подплан Plan 91** ([91-stdlib-mvp-for-0.1.md](91-stdlib-mvp-for-0.1.md)).
> Статус: 🔴 OPEN.
> Зависимости: **Plan 91.8a part 1** закрыт (parser default body syntax + renames).

## Мотивация

Plan 91.8a part 1 закрыл renames + parser default body syntax. Остались
большие пункты:

1. **Структура protocols.nv subop'тимальна** — `equals` default body
   находится в Comparable (через `use Equatable` + local override). Чище
   перенести в Equatable.
2. **Codegen synthesis для default bodies не сделан** — implementer пишет
   `@equals` явно как boilerplate. Followup `[M-91.8a.2-default-codegen]`
   нужно закрыть для real value из default body feature.
3. **From[T] identity blanket** — отсутствует. Каждый тип, желающий
   `Self.from(self)`, пишет boilerplate identity (или прибегает к obscure
   workaround'ам).
4. **`Self` в param-type position не работает** (E7001) — followup
   `[M-91.8a-self-in-param]` из 91.8a part 1.

## Scope

### 1. Refactor protocols.nv

**Было (91.8a part 1):**
```nova
type Equatable protocol {
    equals(other Self) -> bool                  // abstract
}

type Comparable protocol {
    use Equatable
    compare(other Self) -> int
    equals(other Self) -> bool => @compare(other) == 0     // default (override of embedded)
}
```

**Будет (91.8a.2):**
```nova
type Equatable protocol {
    equals(other Self) -> bool => @compare(other) == 0   // default (uses @compare if available)
}

type Comparable protocol {
    compare(other Self) -> int                            // abstract
}
```

**Rationale (см. discussion):**
- Orthogonal protocols — каждый stand-alone, без embed-зависимости.
- Default body использует `@compare` который МОЖЕТ не быть у типа.
  Если у типа нет `@compare` — default не синтезируется, implementer
  пишет `@equals` явно. Это conditional default — аналогично Rust
  PartialEq.ne который использует eq.
- Type только Equatable (Vector3, Complex) — пишет `@equals` явно (default
  неприменим).
- Type Comparable+Equatable — получает `@equals` через default бесплатно.

### 2. Printable.fmt с default body

```nova
type Printable protocol {
    fmt(sb StringBuilder) {
        sb.append(str.from(@))
    }
}
```

Default использует `str.from(@)` — primitive C function для primitives,
overload для user types. Implementer выбирает:
- Path 1 (efficient): пишет `@fmt(sb)` явно (no intermediate string)
- Path 2 (короче): пишет `fn str.from(MyType) -> str` overload (default fmt works)

### 3. From[T] identity blanket

Generic identity method available для каждого типа:

```nova
/// Blanket identity: каждый тип T можно конвертировать из себя.
/// Аналог Rust `impl<T> From<T> for T`. Override запрещён — identity
/// is identity (D9 single canonical path).
#stable(since = "0.1")
export fn[T] T.from(t T) -> T => t
```

Use case'ы:
- `let m = Money.from(other_money)` — identity, работает для любого T
- Generic `fn copy[T](x T) -> T => T.from(x)` — works for any T
- Identity Into auto-derived из identity From (D77)

Strict override rule: попытка явно объявить `fn Money.from(m Money) -> Money`
даёт E_BLANKET_IDENTITY_OVERRIDE.

### 4. Codegen lazy synthesis at use-site

**Trigger points** (когда synthesis активна):

| Контекст | Что |
|---|---|
| `a == b` / `a != b` operator (Plan 91.8b) | Equatable required → synth default `equals` если type только @compare |
| `a < b` / `<=` / `>` / `>=` operator (Plan 91.8b) | Comparable required → no default for @compare, error |
| `[T Equatable]` bound | T satisfies Equatable если has @equals OR has @compare (для default) |
| `[T Comparable]` bound | T satisfies Comparable если has @compare |
| `[T Printable]` bound | T satisfies Printable если has @fmt OR has str.from(T) (для default) |
| `let x Equatable = m` coercion | same as bound check |
| `"${expr}"` string interpolation (Plan 91.10) | Printable required for expr type |
| `fn[T] T.from(t T)` blanket call site | always synth для concrete T |
| **NOT triggered:** `m.equals(other)` direct method call | bare lookup; не нашли — error |

**Synthesis mechanism (production-grade):**

1. **Cache:** per compilation unit, `HashMap<(TypeId, MethodName), SynthFnDecl>`. Synth раз — переиспользуем.
2. **Eligibility check:** для типа T и метода M из протокола P:
   - T уже declares @M → use explicit (no synth)
   - P has abstract M (no body) → T должен иметь @M, иначе satisfaction fails
   - P has default body for M:
     - Substitute Self → T in body
     - Check body's method calls (@compare, str.from(@), etc.) resolve для T
     - All resolve → synthesize Nova_T_method_M; bound satisfied
     - Not all resolve → satisfaction fails (clear error message)
3. **Emit:** synth function added к module's method_overloads + emit_c.rs
   emission queue. Called normally.
4. **Generic blanket (`fn[T] T.from(t T) -> T => t`):** mono per concrete T.
   Treated as существующее generic free function через Plan 101 mono pass;
   только с static-on-T receiver вместо instance.

**NOT in scope 91.8a.2:**
- Operator dispatch refactor (`==` → @equals) — Plan 91.8b
- String interpolation rewrite — Plan 91.10
- `#impl(P + Q)` annotation — Plan 91.9

### 5. `Self` в param-type position fix

`fn T @method(other Self)` сейчас даёт E7001 «Self type used outside receiver
context». Расширить `current_receiver_type` resolution на param-type:

```rust
// emit_c.rs::params_c - already uses current_receiver_type for return
// add Self handling в param ty resolution (mirror return path)
```

Tests workaround в 91.8a part 1 использовали concrete type вместо Self.
После 91.8a.2 — `Self` работает в обоих позициях.

## Затронутые файлы

**Компилятор (nova-p91 worktree):**
- `compiler-codegen/src/codegen/emit_c.rs`:
  - Self resolution в param-type (mirror return-type path)
  - Lazy synthesis: bound/coercion satisfaction check включает default body fallback
  - Synth emission queue + cache
  - Plan 101 mono extended на `fn[T] T.method` static на generic T
- `compiler-codegen/src/types/mod.rs`:
  - BoundCtx::check_satisfaction: учёт default bodies
  - Bound satisfaction reports clear error если default body не synth'able
  - Strict check для From identity blanket override → E_BLANKET_IDENTITY_OVERRIDE

**Stdlib (nova-p91 worktree):**
- `std/prelude/protocols.nv`:
  - Refactor Equatable/Comparable (move default from Comparable to Equatable)
  - Add Printable.fmt default body
  - Add `fn[T] T.from(t T) -> T => t` blanket

**Tests (nova-p91 worktree):**
- `nova_tests/plan91_8a_2/` — новый каталог

## Spec / D-block

### D183 amendment (Plan 91.8a.2)

**Файл:** `spec/decisions/02-types.md` — amend existing D183.

**Content updates:**

1. **Protocol structure refinement:**
   - Equatable.equals default body (not embedded в Comparable)
   - Comparable orthogonal (no `use Equatable`)
   - Rationale: conditional default из orthogonal protocol cleaner than embed override

2. **Printable.fmt default body:** `{ sb.append(str.from(@)) }`

3. **From identity blanket:** `fn[T] T.from(t T) -> T => t`. Override запрещён.

4. **Lazy synthesis at use-site:**
   - Synthesis ТОЛЬКО в protocol context (bounds, coercion, operators, interpolation)
   - НЕ при bare method call
   - Cache per compilation unit
   - Clear error при non-synth'able default

5. **`Self` в param-type position:** разрешён (extension of return-type rule).

### Q-block updates

**Файл:** `spec/open-questions.md`.

- `Q-default-methods` (если есть) — close by D183 amendment
- `Q-protocol-composition` — update про lazy synthesis
- `Q-blanket-impl` (если есть) — close by From identity decl

## Тесты

### Позитивные (новые в `nova_tests/plan91_8a_2/`)

| Файл | Что тестирует |
|---|---|
| `equatable_default_test.nv` | type `Money` only has `@compare`; `[T Equatable]` bound — synth default `equals`; works |
| `equatable_explicit_override_test.nv` | type with both `@compare` and explicit `@equals` — explicit wins |
| `equatable_only_no_compare_test.nv` | type `Vector3` only has `@equals` explicit (no @compare); satisfies Equatable directly; default неприменим |
| `printable_default_test.nv` | type with only `str.from(T)` overload; `[T Printable]` bound — synth default `fmt`; works |
| `printable_explicit_test.nv` | type with explicit `@fmt`; explicit wins (default not used) |
| `from_blanket_int_test.nv` | `int.from(42) == 42` через blanket |
| `from_blanket_user_test.nv` | `MyType.from(my_instance) == my_instance` через blanket |
| `from_explicit_overload_test.nv` | `Money.from(int)` explicit + `Money.from(Money)` blanket coexist; overload resolution by arg type |
| `self_in_param_test.nv` | `fn T @method(other Self) -> R` парсится + работает (D183 amendment) |
| `bound_satisfaction_synth_test.nv` | generic fn с `[T Equatable]` принимает type с только `@compare`; synth активна |
| `synth_cache_test.nv` | один тип используется через bound multi-call → synth раз, переиспользуется |

### Негативные

| Файл | Что должно дать compile error |
|---|---|
| `bound_no_synth_possible_err.nv` | type `Foo` без `@equals` И без `@compare`; `[T Equatable]` bound → clear error «no @equals; default body uses @compare which is also missing» |
| `printable_no_str_from_err.nv` | type `Bar` без `@fmt` и без `str.from(Bar)`; `[T Printable]` bound → error |
| `from_blanket_override_err.nv` | `fn Money.from(m Money) -> Money` explicit declaration → E_BLANKET_IDENTITY_OVERRIDE |
| `comparable_no_default_err.nv` | type без `@compare`; `[T Comparable]` bound → error (no default для compare) |
| `synth_recursion_err.nv` | default body использует `@equals` (which is itself default) — detect cycle → error |

### Регрессия

- `nova test plan91_8a` — 2/2 PASS (existing 91.8a part 1)
- `nova test plan62 plan72 plan87 plan91 plan91_7 protocols syntax` — без новых failures
- `nova test str_interp` (если есть) — without regression (interpolation не trogаем в 91.8a.2)
- Full `nova test` regression — baseline maintained

## Acceptance criteria

### Реализация
- [ ] `std/prelude/protocols.nv` refactored: Equatable holds equals default, Comparable orthogonal
- [ ] `std/prelude/protocols.nv` Printable.fmt default body
- [ ] `std/prelude/protocols.nv` From identity blanket declared
- [ ] `compiler-codegen/src/codegen/emit_c.rs`: Self в param-type position resolves
- [ ] `compiler-codegen/src/codegen/emit_c.rs`: lazy synthesis at use-site (bound/coercion contexts)
- [ ] `compiler-codegen/src/codegen/emit_c.rs`: synthesis cache per compilation unit
- [ ] `compiler-codegen/src/types/mod.rs`: bound satisfaction учитывает default body fallback
- [ ] `compiler-codegen/src/types/mod.rs`: clear error message если default не synth'able
- [ ] Plan 101 mono pass extended на `fn[T] T.method` static на generic T
- [ ] E_BLANKET_IDENTITY_OVERRIDE error для попытки override `T.from(T)`

### Spec / D / Q
- [ ] D183 amended в `02-types.md` (4 части: protocol structure, Printable default, From blanket, lazy synthesis)
- [ ] Q-blocks updated (Q-default-methods, Q-protocol-composition, Q-blanket-impl)

### Тесты
- [ ] 11 позитивных тестов в `nova_tests/plan91_8a_2/` — PASS
- [ ] 5 негативных тестов — expected compile errors с clear messages
- [ ] Регрессия: plan62/72/87/91/91_7/protocols без новых failures
- [ ] Full `nova test` baseline maintained

### Документация
- [ ] `docs/project-creation.txt` — секция «Plan 91.8a.2 closure»
- [ ] `docs/simplifications.md` — note: orthogonal protocols, lazy synthesis, blanket From
- [ ] `D:/Sources/nv-lang/nova-private/discussion-log.md` — секция
- [ ] parent `91-stdlib-mvp-for-0.1.md` — статус 91.8a.2 ✅
- [ ] **«Статус закрытия»** в этом файле — заполнить

### Коммиты
- [ ] Атомарные коммиты (stdlib refactor / codegen synthesis / blanket / tests / docs — отдельно)
- [ ] **НЕ push** в origin
- [ ] **НЕ merge** в локальный main без подтверждения

## Открытые вопросы

1. **Q1:** Cycle detection в default body resolution. Например `Equatable.equals
   default = @compare(other) == 0`; gипотетический `Comparable.compare default =
   if @equals(other) { 0 } else { ... }` → инфинитный recursion. **Default:**
   detect cycle при synthesis, error `E_SYNTH_CYCLE`. Сейчас нет такого
   паттерна в stdlib, но защита нужна.

2. **Q2:** Order of synthesis при multiple defaults. Type T satisfies both
   protocol P (with default M1) and protocol Q (with default M1 same name).
   Conflict? **Default:** error «ambiguous default body source for method M1
   на type T: protocols P and Q both provide»; implementer должен явно
   написать M1.

3. **Q3:** Generic blanket `fn[T] T.from(t T)` — как mono'd для `T = T` где
   T сам generic param? Recursive instantiation? **Default:** mono per
   concrete T только; generic T в bound context — synth deferred to call
   site с concrete T.

4. **Q4:** From identity blanket overrride strict vs lax? **Default:** strict
   (E_BLANKET_IDENTITY_OVERRIDE). Identity is identity; lax allows confusion.

5. **Q5:** Synthesis для `pure` метода protocol должна сохранять pure marker?
   **Default:** да; synthesized function inherits effects/pure marker из
   protocol method declaration.

## Принцип реализации — production-grade

> Implementation должна быть **production-grade**. Никаких MVP shortcut'ов
> вроде «cache disabled for simplicity», «error message TBD», «handle only
> Equatable, others later». Lazy synthesis работает **uniformly** для всех
> protocol contexts (bounds, coercion, operators ready for 91.8b, etc.).
>
> **Error messages должны включать:**
> - Имя type T и method M
> - Список протоколов с default body для M
> - Why default не synth'able (e.g., «@compare missing on T»)
> - Hint: «implement `@equals` explicitly OR provide `@compare`»
>
> **Cache discipline:** synth per (TypeId, MethodName) ОДИН раз per
> compilation unit. Verify duplicate-free emission.
>
> **Edge cases (все покрыты тестами):**
> - Cycles в default body method calls → E_SYNTH_CYCLE
> - Multiple protocol defaults same method → E_SYNTH_AMBIGUOUS
> - Generic param T в default body → mono per concrete T at synthesis time
> - From blanket override → E_BLANKET_IDENTITY_OVERRIDE
> - Self в param-type — резолвится в receiver type
>
> **Plan 101 mono pass extension** — production-quality reuse existing
> infrastructure для generic mono; don't bypass.
>
> Если упирается в архитектурную проблему — **поднять вопрос пользователю**,
> не обходить.

---

## Статус закрытия

> **Шаблон.** Заполнить при закрытии работ.

**Дата закрытия:** YYYY-MM-DD

**Коммиты:**
- nova-p91 (`plan-91-stdmvp`): `<hash>` — protocols refactor (Equatable/Comparable orthogonal + Printable default + From blanket)
- nova-p91 (`plan-91-stdmvp`): `<hash>` — Self в param-type fix
- nova-p91 (`plan-91-stdmvp`): `<hash>` — codegen lazy synthesis at use-site
- nova-p91 (`plan-91-stdmvp`): `<hash>` — tests
- nova-p91 (`plan-91-stdmvp`): `<hash>` — docs
- nova (`main`): `<hash>` — D183 amendment
- nova-private (`main`): `<hash>` — discussion-log

**Тесты:**
- plan91_8a_2 positive: X/Y PASS
- plan91_8a_2 negative: X/Y expected compile errors fired
- Регрессия: full `nova test` — baseline или лучше

**Что вошло:**
- Protocol refactor (Equatable holds equals default): ✅
- Printable.fmt default body: ✅
- From identity blanket: ✅
- Self в param-type: ✅
- Codegen lazy synthesis at use-site: ✅
- Synthesis cache: ✅
- D183 amendment в spec: ✅

**Что отложено / followup:**
- `[список заполнить]`

**Lessons learned:**
- `[если есть нетривиальные наблюдения]`

**Acceptance criteria check:** все пункты выше отмечены [x].
