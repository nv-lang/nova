<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 145.2: Codegen emission determinism

> **Создан:** 2026-06-15 (из находки Plan 145.1 — `[M-codegen-emission-nondeterminism]`).
> **Статус:** 🟢 **ЗАКРЫТ + СМЁРЖЕН в main** (`5c11bce8`, FF 2026-06-15) + разблокировал Plan 145.1 — ветка `plan-145.2`.
> **Приоритет:** P1 (блокировал Plan 145.1).
> **Маркер:** `[M-codegen-emission-nondeterminism]`.

## 1. Проблема

Эмиссия C-кода в `emit_c.rs` зависела от **порядка итерации Rust `HashMap`/`HashSet`**.
У `HashMap` порядок перебора рандомизирован (RandomState, seed per-process), поэтому одна
и та же Nova-программа давала **разный порядок деклараций в `.c` между сборками/прогонами**.

Обычно случайный порядок был безвреден, но иногда будил **два латентных order-зависимых
бага prelude**:

- **(A) init-order static-init OOB:** при «плохом» порядке некоторый prelude module-level
  init читал массив раньше его заполнения → `panic: array: index 0 out of bounds for length 0`
  на старте (НЕ в коде теста).
- **(B) `var_boxed` cross-function leak:** closure mut-capture heap-box `_box_<v>` объявлялся
  в одной top-level C-функции (test-блоке/free-fn) и использовался в другой → CC-FAIL
  `use of undeclared identifier`.

`main` был «стабилен» лишь потому, что его случайный порядок не триггерил баги. **Любое
codegen-изменение** (в частности portable-stmt-expr Plan 145.1) перебрасывало порядок в
«плохой» → flaky CC-FAIL/RUN-OOB. Это и блокировало Plan 145.1 (его корректные фиксы
будили латентные баги).

## 2. Диагностика (investigation-workflow, 4 агента)

Карта недетерминированных emission-итераций: критичны `method_overloads` (итерируется при
эмиссии embed-proxies ~9891 + registration ~3123) и `embed_fields` (~3118/9886) — их порядок
влияет на (а) порядок proxy-функций в `.c`, (б) порядок **enqueue в mono-worklist** (откуда
распространяется на порядок mono/generic-type эмиссии). Уже детерминированы: `per_e_fail_types`
(сорт перед emit ~1285), `type_id_registry` (sort_by_key ~3665), `mono_tuple_instances`
(topo-sort ~3568). Остаточный benign: порядок NovaOpt-typedef'ов (косметика, независимые
typedef'ы — не триггерит баги).

## 3. Фикс

`method_overloads` + `embed_fields`: `HashMap` → **`BTreeMap`** (ключи `Ord`:
`(String,String)` / `String`). Сортированная детерминированная итерация — стабилизирует
порядок эмиссии (и enqueue в worklist) ровно настолько, чтобы (A)+(B) перестали будиться.
API-совместимо (insert/get/iter/contains_key/entry) — drop-in.

> Почему BTreeMap, а не sort-на-каждом-сайте: смена типа поля детерминизирует ВСЕ итерации
> разом + enqueue-порядок, без риска пропустить отдельный сайт. Почему не fix самих (A)/(B):
> `emit_test`/`emit_lambda_body` УЖЕ изолируют `var_boxed` (save/restore) — баг проявлялся
> через ПОРЯДОК эмиссии lambda-bodies/mono-инстансов, а не через изоляцию; детерминизм —
> корневой фикс. (Robust per-fn `var_boxed`-flush — дополнительное упрочнение, см. §6.)

## 4. Валидация

- **overload_method_values 10/10 + bare-str-array read 10/10** (ранее flaky CC-FAIL/RUN-OOB).
- **clang 0 net-new FAIL** (set-diff `syntax`; basics/plan90/96/131/138/138_1/91_fe1/generics/
  152 зелёные; pre-existing plan138_2 3 / syntax 6 / plan62 7 / plan125 1 / plan61 1 = baseline).
- **Разблокировал Plan 145.1:** поверх 145.2 переприменены portable struct-write/heap-box/
  Option-get → **MSVC ВСЁ зелёное** (plan145 6/0, plan138 10/0, plan138_1 10/0, plan91_fe1 10/0,
  plan90/96/131/152_1/basics/generics), clang 0 net-new, overload/rd 8/8 стабильно.
- Регресс-guard: `nova_tests/plan145_2/` (closure_capture_cross_test, str_array_literal_rw).
- **Site-targeted фикстуры Plan 145.1 (добавлены 2026-06-15, аудит-добор):**
  `throw_primitive_expr_pos` (site #2 — heap-box примитива в throw-как-выражении; `.c`
  содержит `nova_box_value(&(nova_int){…})`) и `composite_get_option_pos` (site #3 —
  int64-erased composite `.get(i)` → NPO; `.c` содержит
  `nova_npo_from_tagged_int(nova_array_get_nova_int(…))`). Обе **PASS на clang+MSVC**;
  наличие портируемого хелпера в `.c` проверено grep'ом (не stmt-expr). Прежде эти два
  сайта покрывались только косвенно (plan91_fe1/composite_array_map_pos для #3; примитив
  в #2 — нигде: все throw-expr тесты бросали строки → nova_str comma-ветка).

## 5. Критерии приёмки

- **AC1 (ОБЯЗАТЕЛЬНЫЙ):** production-grade, без упрощений. ✅
- **AC2:** триггер-паттерны (закрытые ранее flaky) стабильны: overload 10/10, rd 10/10. ✅
- **AC3:** clang 0 net-new FAIL. ✅
- **AC4:** Plan 145.1 (struct-write/heap-box/Option-get) применяется чисто; MSVC зелёный. ✅
- **AC5:** регресс-фикстуры plan145_2 проходят (clang+MSVC). ✅
- **AC6:** doc/логи обновлены; маркер закрыт. ✅
- **AC7:** инвариант детерминизма зафиксирован в спеке — **D279** (spec/decisions/08-runtime.md;
  изначально D278, перенумерован из-за коллизии с editors-D278, смёрженным в main раньше). ✅
- **AC8:** site-targeted регресс-фикстуры для обоих закрытых Plan 145.1-сайтов
  (#2 примитив-throw-expr, #3 composite-get-NPO) добавлены, бьют по целевому хелперу
  в `.c`, PASS на clang+MSVC через **релизные** nova-codegen + nova CLI. ✅

## 6. Остаток (residual, benign / followup)

- **NovaOpt-typedef order** всё ещё non-det (косметика — независимые typedef'ы, не триггерит;
  overload/rd 10/10). Для полностью reproducible `.c` — сортировать `novaopt_typedefs_buf`
  emission (low-pri, `[M-codegen-emission-nondeterminism]` residual).
  - **ПОПРАВКА 2026-09-03: этот пункт УСТАРЕЛ, и он успел ввести в заблуждение.**
    Маркер `[M-codegen-emission-nondeterminism]` **полностью закрыт 2026-07-20** (пять недель
    спустя после этой строки), и остаток `NovaOpt` в той записи назван **снятым по
    опровержению**: ноль совпадений `NovaOpt` в диффах, включая цель, интенсивно гоняющую
    `Option` через `HashMap.get`/`Queue.pop`/`Deque.pop_back`. Доказательство закрытия —
    три цели по три сборки с `NOVA_CACHE=0`, SHA256 идентичен 3/3, тогда как ДО фикса флагман
    расходился на 68–86 строк между запусками. Запись целиком —
    [backlog-followups.md](backlog-followups.md), строка маркера.
    **Почему поправка приписана, а не заменила текст:** раздел — исторический, и он верен
    НА СВОЮ ДАТУ; убрать его значило бы стереть то, что тогда действительно было открыто.
    **Зачем вообще:** 2026-09-03 я планировал приёмку плана 280 по этой строке, ослабил
    побайтовое сравнение до «нормализованного» и откатил это, только дойдя до OPEN-view.
    Статус маркера решает `backlog-followups.md`, а не прозаический остаток старого плана.
- **var_boxed robust flush** (clear на входе каждой top-level fn, не только на выходе) —
  дополнительное упрочнение против будущих order-вариантов; детерминизм уже снял триггер.
- **value-record throw-как-выражение** — единственный остаточный codegen stmt-expr (редкий),
  Plan 145.1 §11.
