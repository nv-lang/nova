# REG-трек execution-план — единый реестр методов/stdlib (§0.6, ultracode recon 2026-06-28)

> Источник: workflow `wf_617a5479-a1f` (4 ридера + синтез). Полный — `tasks/wajn18h99.output`.
> §0 (один резолв питает чекер И codegen), §3 (никакого хардкода Nova-имён; stdlib по search-path).
> **Coupling с Session A: PARALLEL, лёгкое касание** (НЕ depends). REG-4 ЯВНО ОСТАВЛЯЕТ int-примитивы
> (int/i8..i64/uint/size) в language_builtins — удаляет ТОЛЬКО stdlib-имена. REG-0/1/2 ‖ A; REG-4/6/7
> синхронизировать с финальным int-каноном A (is_primitive_recv-список types/mod.rs:7966; type_ref_to_c —
> REG-6 наследует A's de-collapse БЕСПЛАТНО, синергия).

## Последовательность (§10 порядок-удаления — главный риск-класс: снабжение НЕ удалять до звучности инлайна)
- **REG-0 (KEYSTONE/ENABLER, first-executable, ADDITIVE low-risk):** расширить from_module-merge в codegen
  на `receiver_types` + `type_decls`. `emit_c.rs:2619-2622` вливает ТОЛЬКО `by_key`, хотя `from_module`
  (`external_registry.rs:200-268`) УЖЕ строит `receiver_types`(:232)+`type_decls`(:257-268) — молча
  выбрасываются. Добавить dedup-влив (паттерн `merge_from_module:180-194`). Ничего НЕ удалять (предусловие
  §10). Gate: detect-mode registry-diff ⊇ load_builtins-набора, 0 потерянных type_decls, byte-identical C.
  Fixture: `import std.ffi.cstr` → CStr type_decl через merge (прецедент U.1.3a ✅). Self-contained, не нужен U.4.
- **REG-1 (U.1.4-remainder):** declared-intrinsic table (namespace,method)→{типы из .nv extern-сигнатур,
  c-lowering tag} для gc/bench/fibers/runtime. type-inference `emit_c.rs:38393-38418` ЧИТАЕТ таблицу (не
  хардкод-match); C-lowering тела `:24912-25029` остаются §2-exception [M-172.1-U1-intrinsic-lowering]
  gated на table-hit. `is_intrinsic_namespace` (lib.rs:52) читает namespace-set таблицы. Reconciliation finite.
- **REG-2 (U.1.5 keystone):** `build_base` (`sig_registry.rs:159-187`) индексирует `Item::Type`+std-типы →
  `is_known_type` (`types/mod.rs:3660`) registry-aware (не хардкод BUILTIN_SIG_MODULES-merge :3316). Deps REG-0. Additive.
- **REG-3 (U.1.5):** 3 name-res сайта (`types/mod.rs:14170` in_builtins, `:14644` is_known, `:14585`
  is_variant_like) → единый predicate `is_resolvable_stdlib_name`. Сузить **permissive PascalCase-bail
  `:14674-14676`** (любой PascalCase→true = §3 soundness-hole) → undefined-type-call rule звучен. Deps REG-1+REG-2.
  Fail/Detach/CancelToken/fence provenance grep-prove ПЕРВЫМ (могут быть load-bearing только через хардкод).
- **REG-4 (U.1.6 removal A):** удалить **stdlib-имена** из `builtins:HashSet` (`types/mod.rs:13848-13887`:
  gc/bench/fibers/runtime/Fail/Detach/CancelToken/fence); ОСТАВИТЬ language_builtins (:13782-13796 примитивы/
  true-false/Self → переименовать поле, [M-172.1-U1-language-builtins]). Deps REG-3 (read-сайты на predicate ПЕРВЫМ).
  Отдельный revertable-коммит + FULL nova test.
- **REG-5 (U.1.6 removal B):** удалить `include_str!`-список (`external_registry.rs:103-125` 9 SRC) +
  `BUILTIN_SIG_MODULES`(:30-42) + `load_builtins`(:134-173) — схлопнуть параллельные реестры checker↔codegen.
  Per-package revertable. SB/WB/RB/raw_mem снимаемы; **sync/net GATED на U.4** [M-172.1-U1-lib-import-needs-U4]
  (звучность инлайна латентна). Строго ПОСЛЕ REG-0+2+3+4.
- **REG-6 (U.2.4 re-scoped, НЕ byte-identical):** codegen наполняет `method_overloads` ИЗ единого SigRegistry
  (`populate_codegen_views` sig_registry.rs:237). 5 локальных populators (ExternalRegistry/D84/receiver/D39/
  mono) → 1; ~90 read-consume (горячий `:23543`). Gate: detect172 pos+neg + звучность. c_name-drift = риск.
- **REG-7 (unblocks, GATED REG-2/3/6):** U.3.2 primitive-recv (`types/mod.rs:7965`+codegen `:25510`),
  net-slice (`std/net/ffi.nv:84` net_last_error transitive-inline), generic-Index (`emit_c.rs:11140`).

## Главные риски (§10 ordering)
1. REG-5 ДО REG-0 → потеря sync OnceCell/net opaque type_decls → Call-GAP (~14k StringBuilder). → REG-0 ПЕРВЫМ.
2. sync/net удалять ДО U.4 = регресс (инлайн-звучность латентна: `if condition must be bool got nova_int`). GATED.
3. REG-4 без REG-3 → undefined-identifier/потеря intrinsic. provenance grep-prove перед.
4. REG-6 c_name-drift на горячем dispatch → мис-диспатч. byte-identical INFEASIBLE → soundness-гейт.
Каждое soundness-removal = отдельный revertable-коммит + FULL nova test (НЕ сэмпл, §6.6).
