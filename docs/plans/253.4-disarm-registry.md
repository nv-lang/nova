<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 253.4 — Реестр дизарма: кодоген заново решает задачу резолва по именам

**Статус:** 🚧 Ф.0 и Ф.1 ИСПОЛНЕНЫ 2026-08-08 в ветке `p253-disarm` (окно
`p253-disarm`, коммиты `318334eea`, `e43d24b7a`) — **НЕ ВЛИТА**, ждёт зелёного
авторитетного гейта: хук `pre-merge-commit` запрещает слияние при красном.
**Реестров было 2 → стало 0.** Ф.2 (инвентарь остальных мест самодельного
резолва по имени) не начата; кандидаты названы окном: `hoisted_let_vars` (плоское
множество без областей), `var_types` (плоская карта имя→C-тип).

**Что окно нашло сверх брифа — перечень интегратора был НЕПОЛОН по ОБЕИМ осям.**
Заведение: точек эмиттера две, но `Stmt::ConsumeScope` порождают **ПЯТЬ**
синтаксисов из пяти мест парсера — это и есть источник №456. Дизарм: **СЕМЬ**
точек, из них две (вход в re-consume-блок на уже взведённом связывании и передача
файберу, №379) в брифе отсутствовали. Ради ровно этого случая в приёмке и стояло
«частичное покрытие приёмкой не считается».

**Ответ на вопрос про `defer`:** флаг и `defer` УЖЕ были одной операцией —
печатаются соседними строками. Разделены были ПЕРВЫЙ (сама переменная) и ТРЕТИЙ
(регистрация); выпадал третий. **«Двух механизмов вместо одного» нет:** drop-флаг
D432 §4/§5 И ЕСТЬ переменная `_active`, а реестр был не механизмом, а способом её
разыскать.

**Структурная проба выполнена:** убрать поле из одной точки конструирования →
`error[E0063]: missing fields` → **не собирается**, а не «тест краснеет». 800
прогонов стохастического носителя чистые при ожидании 6-13 отказов; все доступные
гейты совпадают с `main` дословно.

**Дыра, названная самим окном:** точка дизарма B7 (передача файберу, №379) НЕ
покрыта фикстурой — проба не покраснела ни на одной. Это дыра покрытия, а не
правки, но закрывать обязательно. Подплан зонтика
[253](253-invariant-reduction-umbrella.md), трек Д. Дефект-корень —
[№480](221.1-bug-sweep.md). Основание — разбор владельца 2026-08-08 по следам
закрытого [№456](221.1-bug-sweep.md): «дизарм происходит в текущем блоке, зачем
всё переусложнять? для данной consume-переменной автоматически заводишь
переменную для дизарма — она известна компилятору, алгоритм простой и полностью
детерминированный, какие проблемы?»

**Номер 253.3 пропущен намеренно:** он был занят планом размещения значений,
который в тот же день признан не относящимся к предмету зонтика и перенесён в
[172.15](172.15-value-placement-abi-audit.md). Переиспользовать номер нельзя —
он остался в истории коммитов (реестр [№464](221.1-bug-sweep.md): номер плана
обязан быть уникальным).

## Почему это предмет зонтика 253, а не отдельная тема

Различающий вопрос нормы: *можно ли выразить состояние, нарушающее утверждение?*

Утверждение здесь: **«каждый взведённый флаг авто-очистки зарегистрирован в
реестре дизарма»**. Нарушающее состояние выразимо — и именно оно случилось:
эмиссия `Stmt::ConsumeScope` для формы `spawn consume a, b { … }` взводила флаг
`_defer_<блок>_<индекс>_active` напрямую, не попадая ни в один из двух реестров.
Правило держалось договорённостью между тремя местами кода.

Мерило числами тоже есть: было 2 независимых реестра плюс прямое взведение мимо
обоих → должно стать 1 или 0 (см. Ф.2).

## Что установлено

### Взведение и снятие идут разными путями

```rust
fn disarm_var_for(&self, name: &str) -> Option<String> {
    if let Some((_, bid))      = self.reconsume_scopes.iter().rev().find(|(n, _)| n == name) { … }
    if let Some((_, bid, idx)) = self.auto_cleanup_active.iter().rev().find(|(n, _, _)| n == name) { … }
    None
}
```

* флаг в сгенерированном C зовётся `_defer_<блок>_<индекс>_active` — имя
  составлено из номера блока и порядкового номера, **не из имени переменной**;
* поэтому `disarm_var_for` — не «сними флаг», а «вычисли, как он называется»;
  вычислить из имени переменной нельзя, нужен реестр;
* реестры наполняются в двух местах (`emit_c.rs:31057`, `:33055`), каждое —
  под свою форму объявления;
* третья форма взводила флаг сама и не регистрировалась нигде.

### Корень глубже реестра: у кодогена нет связи «использование → объявление»

Узел переменной в дереве несёт только имя-строку:

```rust
Ident { name: String, span: Span, is_mut: bool, is_consume: bool }
```

Идентификатора объявления нет. Встретив `x.close()`, эмиттер не знает, какое
именно объявление `x` имеется в виду — одноимённых в разных областях может быть
несколько. Отсюда стек реестров и поиск `.rev().find()`: **это самодельная
имитация разрешения имён внутри эмиттера**. Кодоген заново решает задачу, уже
решённую чекером, и решает её по строке — а любая вторая реализация расходится с
первой. Она и разошлась.

**КЛАСС: одна операция (завести флаг и уметь его снять) размазана по трём
местам, и одно из них можно забыть.** Тот же класс, что корень Б в
[253.2](253.2-contract-doors.md) (хук на одной из пяти веток конструирования) и
что снятые хардкоды `__sret` в [172.15](172.15-value-placement-abi-audit.md)
(четыре проявления одного неверно выбранного места решения).

Цена этого конкретного случая измерена: зависание, ловившееся раз на тысячу
прогонов, с порчей кучи и невозвратом из `GC_gcollect()` — [№456](221.1-bug-sweep.md).

## Решение владельца 2026-08-08 — нормативно

Дословно: «когда появилась в блоке consume-переменная, сразу создаёшь переменную
для её дизарма **+ defer с обработкой дизарма**».

Одной операцией заводятся **ТРИ** вещи: сама переменная, её флаг, и `defer`,
читающий этот флаг. Так исчезает не только реестр, но и **второй способ
ошибиться** — «завёл флаг, но не поставил очистку» либо «поставил очистку,
но флаг заводится где-то ещё». Сегодня это разные шаги, и они могут разойтись
точно так же, как разошлись взведение и регистрация.

**Имя флага выводится из имени переменной, а не из номера блока.** Флаг
объявляется рядом с самой переменной, в том же C-блоке. Тогда:

* связь «переменная → её флаг» существует **по построению**, а не по записи в
  реестре — забыть зарегистрировать нечего, регистрации нет;
* дизарм пишет присваивание флагу по выводимому имени, ничего не разыскивая;
* **теневание разрешает сам C**: во вложенном блоке одноимённая переменная
  принесёт с собой свой флаг, объявленный там же, и он затенит внешний ровно
  так же, как затеняет сама переменная. Правила областей видимости C делают
  работу, ради которой эмиттер вёл собственный стек.

**Это снимает предпосылку типизированного IR.** Первая редакция плана
утверждала, что Ф.2 упирается в [172.12](172.12-typed-ir-mono.md), потому что
узел использования не несёт идентификатора объявления. Неверно: идентификатор
объявления и не нужен, если имя флага детерминировано именем переменной, а
разрешение отдано C. Ошибка интегратора, снята владельцем.

## Объём работы — решение владельца

Дословно: «делаешь одним окном для всех случаев с consume и дизармом».

**Одно окно закрывает ВСЕ формы разом, а не ту, на которой споткнулись.** Это
прямое следствие устройства дефекта: реестр наполнялся по одной форме на место,
и именно поэтому третья форма выпала. Чинить формы по одной значит
воспроизводить ту же ошибку, только медленнее.

В объём входит **всё, что заводит или снимает авто-очистку**, а не только
`Stmt::ConsumeScope`: bare consume-let (D432 §2), re-consume-скоуп
`consume X { … }`, форма `spawn`/`detach consume a, b { … }` (D415 §4),
потребление получателем (`X.method(...)`, `consume_receiver_methods`),
потребление аргументом на consume-позиции параметра
(`free_fn_consume_param_positions`), и любые формы, которые окно найдёт сверх
этого перечня. Перечень **не считается полным** — его пополнение есть часть
задачи, а не отклонение от неё.

### Алгоритм эмиссии — решение владельца целиком

Дословно: «когда появилась в блоке consume-переменная, сразу создаёшь переменную
для её дизарма + defer с обработкой дизарма — одно окно на все случаи жизни,
только проверить надо, реализован ли `@cleanup`, если нет, то не эмитишь
переменную дизарм и defer, если `@cleanup` есть, то делаешь в defer проверку на
дизарм и если не было дизарма вызываешь `@cleanup(outcome)`».

Целиком, без домысливания:

```
при объявлении consume-переменной X типа T:
    если у T НЕТ метода @cleanup:
        не эмитить ничего — ни флага, ни defer   // вызывать нечего
    иначе:
        эмитить рядом с X флаг  _defer_<c_имя_X>_active = 1
        эмитить defer(o ScopeOutcome):
            если _defer_<c_имя_X>_active:        // дизарма не было
                T.@cleanup(X, o)

при потреблении X (consume-метод, consume-позиция параметра, …):
    _defer_<c_имя_X>_active = 0
```

**Три уточнения к наброску владельца, установленные по коду** (владелец
набрасывал по памяти и просил уточнить):

1. **Полярность обратная.** В наброске флаг назывался `_disarm_` и ноль означал
   «дизарма не было». В коде и в сгенерированном C флаг зовётся `_active` и
   означает «очистка ВЗВЕДЕНА»: заводится единицей, дизарм ставит ноль, `defer`
   вызывает очистку при `if (_defer_N_M_active)`. Смысл тот же, но полярность
   надо сохранить существующую — иначе придётся переписывать все места чтения и
   легко получить перевёрнутое условие там, где его не заметят.
2. **Имя строится из C-идентификатора, а не из имени Nova.** Эмиттер уже
   отображает имена Nova в C; флаг обязан строиться из ТОГО ЖЕ отображённого
   имени, иначе разойдётся с переменной, которую сторожит. Сегодня в имени стоят
   номер блока и порядковый номер (`_defer_1_0_active`) — именно это и вынудило
   завести реестр.
3. **Передавать исход изобретать не надо — механизм есть.** `defer(o
   ScopeOutcome) { … }` реализован (D314, план 173 Ф.2.B2): имя outcome-параметра
   объявляется как `Nova_ScopeOutcome*` и виден телу `defer`. То есть пункт
   «вызываешь `@cleanup(outcome)`» опирается на существующую машинерию, а не
   требует новой.

**Что схема даёт сверх снятия реестра — условное потребление.** Если `X`
потребляется в одной ветке и не потребляется в другой, флаг решает это сам, без
анализа путей: присваивание нуля стоит там, где потребление, и `defer` читает
итог. Сегодня это же свойство обеспечивается реестром плюс drop-флаг механикой
D432 §4/§5 — то есть двумя механизмами вместо одного.

Свойства, ради которых это и делается:

* **связь по построению.** Флаг и `defer` рождаются вместе с переменной, одной
  операцией. Реестра нет — регистрировать нечего, забыть нечего.
* **теневание разрешает сам C.** Во вложенном блоке одноимённая переменная
  принесёт свой флаг, объявленный там же, и затенит внешний ровно так же, как
  затеняет сама переменная. Собственный стек областей видимости в эмиттере не
  нужен.
* **условие эмиссии одно** — «есть ли у типа `@cleanup`» — и задаётся в одной
  точке, вместо трёх мест, обязанных договориться.
* **мёртвых флагов нет.** Носителей `@cleanup` в `std` восемь (`File`,
  `BufWriter`, `UdpSocket`, `TcpListener`, `TcpStream` и др.), значит для
  подавляющего большинства consume-переменных тройка не порождается вовсе.

**Исход блока обязан дойти до очистки.** `@cleanup` принимает `ScopeOutcome`
(форма объявления: `fn T consume @cleanup(_outcome ScopeOutcome) -> ()`), то есть
очистка знает, вышли штатно или через раскрутку, и код в `std` на это различие
уже опирается. В псевдокоде выше это `outcome`, и он не может быть константой:
потерянный исход даст тихую регрессию, невидимую ни на позитивных фикстурах, ни
в conformance, где проверяется успешный путь.

## Фазы

* **Ф.0 — перечислить ВСЕ формы, заводящие или снимающие авто-очистку.**
  Без полного перечня Ф.1 повторит исходную ошибку: закроет известные формы и
  оставит ту, о которой не вспомнили. Каждая форма — с файлом, строкой и
  фикстурой, показывающей её поведение сегодня.
* **Ф.1 — флаг заводится вместе с переменной, имя выводится из имени — ДЛЯ ВСЕХ
  ФОРМ ИЗ Ф.0.** Один шаг вместо двух: объявление consume-переменной порождает и
  её флаг.
  Реестры `reconsume_scopes` и `auto_cleanup_active` снимаются целиком,
  `disarm_var_for` вырождается в построение имени. Мерило: реестров было 2 →
  стало 0; мест взведения было N → стало столько же, сколько объявлений.
  Единственное, что требует внимания: отображение имён Nova в C-идентификаторы
  (эмиттер его уже делает) — флаг обязан строиться из ТОГО ЖЕ отображённого
  имени, иначе разойдётся с переменной, которую сторожит.
* **Ф.2 — проверить, где ещё кодоген резолвит по имени.** Реестр дизарма найден
  по следам аварии. Вопрос: сколько ещё мест в эмиттере ведут собственный стек
  областей видимости и ищут в нём строкой. Инвентарь перечислением, не догадкой —
  каждое место с файлом и строкой.

## Приёмка

* **Фикс носителя приёмкой НЕ считается.** №456 уже закрыт тремя строками —
  третья форма подключена к существующему реестру. Предмет плана в том, что
  реестр вообще существует.
* Проба «подсунь негодное» на Ф.1: завести флаг в обход единой точки — сборка
  обязана стать невозможной (а не «тест краснеет»).
* Отдельно: **завести переменную без флага или без `defer` обязано стать
  невыразимым** — сборка не собирается, а не фикстура краснеет.
* **Счёт флагов в `.c` равен счёту consume-переменных, чей тип имеет
  `@cleanup`** — и ни одним больше. Машинная проверка, не суждение; ловит и
  обратную ошибку: флаг заведён там, где очистки нет.
* Фикстура на выход из блока **через ошибку**: очистка обязана получить
  соответствующий `ScopeOutcome`. Проба — подменить исход на штатный и показать,
  что фикстура краснеет.
* Мерило Ф.1 числами: реестров было 2 → стало 0; форм покрыто N из N по
  перечню Ф.0 — **частичное покрытие приёмкой не считается**, это и был исходный
  дефект.
* Ф.2: перечень мест с самодельным резолвом, каждое с файлом и строкой.

## Ф.0 — ПОЛНЫЙ перечень форм (окно `p253-disarm`, ветка `p253-disarm`)

Строки — по дереву ветки. Перечень строится по двум осям: **где авто-очистка
ЗАВОДИТСЯ** (флаг + `defer` + запись) и **где она СНИМАЕТСЯ** (дизарм).
Перечень плана оказался неполным по обеим осям — ниже добавленное помечено «+».

### A. Формы, ЗАВОДЯЩИЕ авто-очистку — их ровно ДВЕ точки на весь эмиттер

| # | Форма | Где заводится | Фикстура |
|---|---|---|---|
| A1 | bare consume-let `consume x = e` (D432 §2) | `emit_c.rs:29216` `enter_defer_scope` (пролог блока: флаг + `DeferEntry` + hoist переменной) + взведение `emit_c.rs:31110` `emit_auto_cleanup_arm` | `d432_auto_cleanup_hybrid_c.nv` |
| A2 | ЛЮБОЙ `Stmt::ConsumeScope` | `emit_c.rs:30401` `enter_consume_defer_scope` + взведение `emit_c.rs:33003` | см. A2a–A2e |

`Stmt::ConsumeScope` — это ОДИН узел, но **пять синтаксических форм**, и
породить его может пять разных мест парсера. Их и надо перечислять, потому что
именно расхождение между ними дало №456:

| # | Синтаксис | Парсер | `re_consume` | Фикстура |
|---|---|---|---|---|
| A2a | `consume X { … }` (Plan 201 re-consume) | `parser/mod.rs:5905` | `true` | `d188_reconsume_block.nv` |
| A2b | `consume A, B, C { … }` (Plan 174, сахар над вложением) | `parser/mod.rs:6071` | `true` | `d188_multivar_reconsume.nv` |
| A2c | `ro s = consume X { …; X }` (rvalue-форма с result-приёмником) | `parser/mod.rs:6144` | `true` | `d188_v3_view_tail.nv`, `d188_v3_expr_escape.nv` |
| A2d | `consume X = e { … }` (D188 binding-форма) | `parser/mod.rs:5991` | `false` | `d188_consumable_scope_block.nv`, `f5_consume_exactly_once.nv` |
| A2e | `spawn consume c { … }` / `detach consume c { … }` (D415 §4) и их мульти-var зеркало `spawn`/`detach consume a, b { … }` (№379) | `parser/mod.rs:10972` (spawn), `:11143` (detach), `:11243` (мульти-var) | `false` | `m456_consume_scope_explicit_close_disarm.nv`, `spawn_detach_consume_multivar_ok.nv` |

**+ Гейт эмиссии — один на обе точки:** `emit_c.rs:29190`
`auto_cleanup_qualifies` → `auto_cleanup_types` (`emit_c.rs:5335`, pre-pass,
зеркало `LinearityRegistry::build` чекера): у типа обязан быть `@cleanup`
формы `(outcome ScopeOutcome)`. Нет `@cleanup` — не заводится ничего.
Проба П10 подтвердила, что гейт несущий: снятие даёт `undefined symbol:
Nova_<T>_consume_cleanup` на линковке.

**+ Run-site'ы (где `defer` читает флаг и вызывает `@cleanup(outcome)`) — их
ЧЕТЫРЕ, и исход у каждого свой:** LEAVE `emit_c.rs:29666` (`Success`), FAIL
`emit_c.rs:29459`/`:30424` (`FromFrame` → `Failure`/`Panic`), INTERRUPT
`emit_c.rs:29548`/`:30460` (`Interrupt`), EARLY-EXIT `emit_c.rs:29819`
(`return`/`break`/`continue`). Все четыре идут через `emit_consume_entry_cleanup`
(`emit_c.rs:30277`) — единственное место, где вызывается сам `@cleanup`.

### B. Точки, СНИМАЮЩИЕ авто-очистку (дизарм) — их СЕМЬ

| # | Форма потребления | Где | Фикстура |
|---|---|---|---|
| B1 | consuming-вызов по получателю `X.close()` | `emit_c.rs:30960` `disarm_auto_cleanup_receiver_call`, ветка (a), гейт `consume_receiver_methods` | `d432_…`, `m456_…` |
| B2 | передача в consume-позицию параметра `pump(a, b)` | там же, ветка (b), гейт `free_fn_consume_param_positions` / `method_consume_param_positions` | `guard_cross_scope_transfer.nv`, `m456_…` (кейс моста 249) |
| B3 | `return X` голым идентификатором | `emit_c.rs:33003`-выше, ветка `Stmt::Return` | `d432_…` (кейс 2) |
| B4 | non-bare `return EXPR` / tail-EXPR с выносом в consume-позиции | `emit_c.rs:31016` `emit_expr_with_reconsume_disarm` | `d188_v3_expr_escape.nv` |
| B5 | tail-вынос `consume X { …; X }` при result-приёмнике | `emit_c.rs:33003`, ветка `tail_escape` | `d188_reconsume_block.nv` |
| B6 | **+** вход в re-consume-блок на уже взведённом биндинге (блок берёт очистку на себя) | `emit_c.rs:33003`, ветка `if *re_consume` в начале | `d188_reconsume_block.nv` |
| B7 | **+** передача биндинга файберу в `spawn`/`detach consume` (№379) | `emit_c/emit_detach.rs:43` `disarm_outer_auto_cleanup_for_fiber_body` | `spawn_detach_consume_multivar_ok.nv` |

**+ Восьмая точка, не являющаяся дизармом, но снимающая записи:**
`leave_defer_scope` (`emit_c.rs:29666`) — выход из блока. Прежде она вдобавок
чистила реестр (`auto_cleanup_active.retain(…)`); теперь запись уходит вместе
со своим скоупом, отдельной уборки нет.

**Итог по объёму: форм заведения 2 точки / 5 синтаксисов, точек дизарма 7.
Покрыто 7 из 7 и 5 из 5.**

## Ф.1 — сделано в ветке `p253-disarm` (окно `p253-disarm`, 2026-08-08)

**Реестров было 2 → стало 0.** `reconsume_scopes` и `auto_cleanup_active`
удалены как поля. Вместо них — обход `defer_scopes`, то есть ТОЙ САМОЙ
структуры, которая владеет флагом и его `defer`'ом:

* имя флага строится из C-идентификатора переменной:
  `_defer_<c-имя>_active` для A1 и `_defer__consume_<имя>_<id>_active` для A2
  (у A2 C-идентификатор биндинга и есть `_consume_<имя>_<id>`);
* `nova_binding` и `re_consume` — **обязательные поля конструктора**
  `ConsumePolicy`: завести consume-запись, не назвав её биндинг, не собирается
  (`error[E0063]: missing fields`);
* `disarm_var_for` (`emit_c.rs:30889`) ничего не вычисляет — берёт `active_var`
  у найденной записи; `reconsume_active_names` (`:30904`) и
  `drop_disarm_binding` (`:30924`) закрывают вторую и третью роли снятых
  реестров.

**Попутно сняты ещё три реконструкции одного и того же:**
`emit_auto_cleanup_arm` звал `pattern_binding` ВТОРОЙ раз (для
`Pattern::Wildcard` это свежий `fresh_tmp`, то есть ДРУГОЕ имя на ту же
переменную) и пересобирал имя флага строкой; `Stmt::ConsumeScope` пересобирал
`_defer_<bid>_0_active` в двух местах (взведение и tail-вынос).

**Про «два механизма вместо одного» (drop-флаг D432 §4/§5).** Проверено:
второго механизма нет и не было. Drop-флаг — это и есть C-переменная
`_active`; реестр был не вторым механизмом, а СПОСОБОМ ЕЁ РАЗЫСКАТЬ. Убрав
реестр, мы убрали поиск, а не механику. Условное потребление (потребил в одной
ветке, не потребил в другой) как работало флагом, так и работает — это
подтверждает кейс MaybeConsumed в `m456_…`.

**Про `defer`.** Проверено по коду: пункты 2 и 3 наброска владельца (флаг и
`defer`) УЖЕ были одной операцией — `enter_defer_scope` и
`enter_consume_defer_scope` создают `DeferEntry` и печатают `int …_active = 0;`
соседними строками. Разделён был ПЕРВЫЙ пункт (сама переменная; для A1 это
залатано hoist'ом в пролог) и ТРЕТИЙ — регистрация. Именно третий и выпадал.

**Спека не трогается.** Правка не меняет наблюдаемого поведения: те же формы,
те же run-site'ы, тот же `ScopeOutcome`, те же решения чекера о том, что
считать потреблением. Меняются только имена C-локалей и способ их резолва
внутри эмиттера. D-амендмент не требуется (в отличие от фикса №456, где D415 §4
молчала о судьбе cleanup'а на разрешённом move-out — там текст правился).

## Порядок относительно тега

* **До тега — ничего из Ф.1.** №456 закрыт, живых носителей нет, а правка
  горячего места кодогена перед релизом рискованнее дыры, у которой носителей не
  осталось. Оговорка: после снятия предпосылки IR работа стала дешёвой, и
  соблазн сделать её «заодно» велик — не поддаваться, цена ошибки здесь измерена
  зависанием раз на тысячу прогонов.
* **Ф.2 (инвентарь) — можно до тега**: знание, изменений кода нет.

## Связанные

Зонтик [253](253-invariant-reduction-umbrella.md) ·
[253.1](253.1-receiver-mode.md) (трек А) · [253.2](253.2-contract-doors.md)
(трек Б, корень Б — то же строение) · [№456](221.1-bug-sweep.md) (носитель,
закрыт) · [№480](221.1-bug-sweep.md) (класс) ·
[172.12](172.12-typed-ir-mono.md) (ошибочно записан предпосылкой, снято) ·
[172.15](172.15-value-placement-abi-audit.md) (тот же класс в другом месте) ·
`docs/dev/conventions-governance.md`, раздел «Инварианты».
