# План 152.7.2 — формат-контекст в Display (D419) + интерполяция прямо-в-sink

**Статус:** 🔨 В РАБОТЕ (2026-07-13, владелец: «делай»; волна запущена — ветка
`d419-format-fmt`, worktree nova-p173, sonnet).
**Родитель:** [152.7 — интерполяция и форматирование](152.7-interpolation-formatting.md)
(B1 mini-language ✅ 2026-06-14; 152.7.1 Write-sink/D374 ✅ 2026-06-16).
**Спека:** Q-format-spec-to-display → **D419** (в том же слиянии, что код).

**⚠️ SUPERSEDE-риск (2026-07-16):** [Plan 208](208-unified-formatter.md) РЕТРАКТИРУЕТ D419 (спека-ретракт
D419→D422 уже в main, ветка `plan208-spec`) — 208 сворачивает двух-методный `@display(Write)`+`@display_fmt(Fmt)`
в ОДИН `@display(mut f Fmt)`. Эта (двух-методная D419) версия и 208 — про ОДНУ поверхность. НЕ доводить
конфликтующий двух-методный слой до слияния, не сверившись с 208. Общее и нужное обоим — **interp-direct-to-sink**
(`[M-152.7.2]` / `[M-d419-interp-direct-primitives]`): его наработку 208 переиспользует. Координация — в шапке 208.

## Объём

1. **Протокол `Fmt`** поверх `Write` (аксессоры спека: `@alternate() -> bool`,
   `@precision() -> Option[int]`) + второй ОПЦИОНАЛЬНЫЙ метод `@display_fmt(mut f Fmt)`;
   типы без него живут по-старому (`@display(mut w Write)` + пост-обработка у вызывающего) —
   БЕЗ повторной ломки сигнатур после D374.
2. **`#` (alternate) = канонический pretty-флаг**: `"${v:#}"`; первый потребитель —
   `JsonValue @display_fmt` (эквивалент `to_str_pretty`).
3. **Оси типу:** alternate, precision; width/fill/align остаются пост-обработкой
   у вызывающего; radix — встроенные числовые пути.
4. **Движок интерполяции — прямо-в-sink:** `${x}` лоуэрится в `@display(mut sb)`
   без промежуточной str; `str.from` уходит из интерполяционного пути
   (остаётся конверсией по 174.1-канону). Примитивы — через рантайм-хелперы
   152.7-B (`nova_fmt_int_body` и семейство); в т.ч. переделка тел вида
   `fn int @display(mut w Write) => w.write(str.from(@))` (вопрос владельца
   2026-07-13 об аллокации). Если путь примитивов глубоко вплетён —
   маркер `[M-d419-interp-direct-primitives]`, хвост отдельной короткой волной.
5. `E_FORMAT_SPEC_UNKNOWN` смягчается ТОЛЬКО для типов с `@display_fmt`;
   протухший `[M-91.14-format-dsl-extensions]` закрывается ссылкой на D419;
   Q-блок помечается RESOLVED.

## Гейты
conformance полный без --jobs (δ0) + std/src/encoding (JsonValue) + std/src/collections;
тесты: pos (реакция на `:#`/`.N` у типа с display_fmt; прежнее поведение без него),
neg (неизвестный спек без display_fmt = ошибка как была).
