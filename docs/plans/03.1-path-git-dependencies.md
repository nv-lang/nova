// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 03.1: `path`- и `git`-зависимости (bootstrap, без registry)

> **Создан:** 2026-05-22 (декомпозиция [Plan 03](03-package-ecosystem-roadmap.md)).
> **Статус:** ✅ **ЗАКРЫТ** (2026-05-22) — Ф.1–Ф.6 целиком.
> - **Ф.1** — парсинг `[dependencies]` (`path`/`git`, `rev`/`tag`/`branch`).
> - **Ф.2** — git fetch + кэш (`git_cache.rs`: bare-клон + worktree-
>   checkout по commit'у, offline-режим, memo).
> - **Ф.3** — межпакетный резолв (`lookup_dependency` + `dep_root`;
>   ужесточение repo-root looseness §3.2; cross-package cycle detection;
>   rule H через границу пакета; `verify_case` dep-aware).
> - **Ф.4** — `nova.lock` (`lockfile.rs`: формат, транзитивный dep-граф,
>   package-cycle detection, `sync` из `nova build`, lock-таблица пинов).
> - **Ф.5** — CLI `nova add` / `nova update`.
> - **Ф.6** — spec D78 sync + `docs/nova-cli*`.
>
> Тесты: 11 фикстур `nova_tests/plan03_1/` + 18 Rust-тестов; **983 PASS /
> 0 FAIL** — 0 регрессий. Ветка `plan-3-deps`, worktree `nova-p3-deps`.
> **Приоритет:** P3 — сделан near-term (§1): не зависит ни от registry,
> ни от self-hosting.
> **Родитель:** [Plan 03](03-package-ecosystem-roadmap.md) (roadmap-индекс).
> **Spec:** [D78](../../spec/decisions/07-modules.md#d78).

---

## 1. Scope и почему «можно сейчас»

Первый исполнимый срез экосистемы пакетов: **внешние зависимости через
`path` и `git`** в bootstrap-Rust `nova` CLI + воспроизводимый
`nova.lock` для них. **Без** registry, **без** version-ranges, **без**
self-hosting.

Почему near-term (вопреки редакции 1 Plan 03, гейтившей всё на
self-host): `path`/`git`-резолв не требует ни registry-инфраструктуры,
ни version-resolution (источник пинится точно — путём или commit'ом),
ни Nova-компилятора (bootstrap-Rust справляется). Self-hosting — про
*язык инструмента*, не про *функциональность* (Plan 03 §7).

**Разблокирует:** multi-package разработку до v2.0 — напр. сайт
[Plan 64](64-nova-lang-website.md) сможет зависеть от `std` как от
пакета; прикладные проекты — делиться кодом через `git`.

**НЕ входит** (следующие под-планы Plan 03): version-ranges + PubGrub
(03.2), registry (03.3), `nova info`/effect-diff/`audit` + capability-
confined deps (03.4). 03.1 **резолюцию зависимостей доводит до конца**
(без упрощений) — но *ограничение* зависимостей и registry — отдельные
концерны.

## 2. Текущее состояние

- ✅ Workspace (`nova.toml` members), резолв модулей **внутри** дерева
  (Plan 81), `resolve_imports_inline` (`imports.rs`).
- ❌ Внешних зависимостей нет: `[dependencies]` не парсится, `nova.lock`
  не генерируется, межпакетный резолв за пределы workspace отсутствует.

## 3. Дизайн

### 3.1. `nova.toml` `[dependencies]`

```toml
[dependencies]
foo = { path = "../foo" }
bar = { git = "https://example.org/bar.nv", rev = "a1b2c3d" }
baz = { git = "https://example.org/baz.nv", tag = "v0.3.0" }
qux = { git = "https://example.org/qux.nv", branch = "main" }
```

- `path` — локальная директория (другой пакет на диске).
- `git` — репозиторий; пин через `rev` / `tag` / `branch`. Даже при
  `branch` **lockfile фиксирует точный commit** — ветка не «уедет».
- **Forward-compat поля резервируются** (для 03.4, без enforcement в
  03.1): `forbid = ["Net","Fs"]` (capability-confined dep, Plan 03 §4).
  Парсятся и сохраняются, но в 03.1 не enforce'ятся — чтобы 03.4 не
  ломал формат.

### 3.2. Межпакетный резолв

Первый сегмент import-пути — имя пакета. `import foo.mod.{X}` → `foo`
ищется среди **объявленных зависимостей** (`[dependencies]`); резолв — в
исходном дереве зависимости теми же правилами D29/Plan 81.

**Дизайн-решение: импорт между пакетами требует явной `[dependencies]`-
записи — даже для членов одного workspace.** Workspace-membership сам по
себе **не** делает пакет импортируемым (модель Cargo). Причины: explicit
dependency-граф (AI-first «видно откуда»); член workspace, от которого
ты не зависишь, не должен засорять твой namespace; единая точка для
версии / `forbid` / effect-surface. Текущий резолвер неявно находит
модули через repo-root — это looseness, 03.1 её **ужесточает**.

**`std` — исключение:** остаётся неявным standard-library-пакетом,
доступным без `[dependencies]`-записи (как Rust `std`). Машинерия 03.1 —
для **не-`std`** пакетов.

- Коллизия имени dependency и workspace-member → **config error**.
- Импорт из зависимости — всегда **полным путём** (относительные импорты
  [Plan 84](84-relative-imports.md) package-scoped, границу пакета не
  пересекают; 03.1 и 84 композируются чисто).
- `internal/` (rule H) зависимости — приватная граница соблюдается:
  потребитель не видит `dep.internal.*`.

### 3.3. `nova.lock`

Формат — D78. 03.1-подмножество (без registry/version-ranges):
- `git`-deps: имя + URL + исходный `pin` + **resolved commit hash**.
  Commit — **сам по себе** integrity-пин: git-commit криптографически
  адресует дерево исходников, подменить содержимое нельзя без смены
  commit'а (паритет с многолетним поведением `Cargo.lock`). Ветка не
  «уедет»: следующий резолв берёт зафиксированный commit.
  *Отдельный* sha256-хэш дерева + подписи + transparency-log —
  supply-chain hardening **Plan 03.4** (а не 03.1): для bootstrap-
  компилятора без сторонних crate-зависимостей собственная крипто-
  реализация — отдельный осознанный концерн.
- `path`-deps: имя + путь, **без hash-пина** — path-deps локальны и
  мутабельны, всегда берётся текущее содержимое (как в Cargo).
- Поле под effect-surface зависимости — **зарезервировано** (заполняет
  03.4), формат сразу с ним.

### 3.4. Кэш git

Fetched git-репозитории кэшируются (`~/.nova/git/<hash>/` либо
`target/`), пин по commit'у. Повторная сборка — без сети, если lock и
кэш на месте. Offline-режим: build из lock+cache без обращения к сети.

## 4. Фазы

### Ф.0 — Свериться/расширить D78 schema (~0.5 дня)

Проверить, что D78 покрывает `[dependencies]` с `path`/`git` и формат
`nova.lock`; дописать недостающее (резерв полей `forbid` и effect-
surface — §3.1/§3.3). Spec-only.

### Ф.1 — Парсинг `nova.toml` `[dependencies]` (~1 день)

`path`/`git` (+ `rev`/`tag`/`branch`); резерв `forbid`-поля. Валидация:
взаимоисключение `path`/`git`, требование пина для `git`, понятные
ошибки.

### Ф.2 — Git fetch + кэш ✅ (сделано)

Модуль `compiler-codegen/src/git_cache.rs`:

- `resolve_git_dep(url, pin, locked_commit) -> GitResolution` —
  материализует git-зависимость в кэше. Раскладка кэша
  (`$NOVA_HOME/git` либо `~/.nova/git`): `db/<repo-id>.git` — bare-клон,
  `co/<repo-id>/<commit>/` — worktree-checkout на точном commit'е.
- `<repo-id>` = читаемый stem URL + 64-битный хэш URL (без коллизий).
- Резолв пина: `rev`/`tag`/`branch`/без-пина → commit через
  `git rev-parse`. `rev`/`tag` (≈immutable) — fetch только при промахе;
  `branch` — fetch перед каждым резолвом (ветка движется).
- **Offline-режим** (`NOVA_OFFLINE=1`): сетевые операции запрещены —
  сборка только из готового кэша.
- `locked_commit` (для Ф.4): точный commit из `nova.lock` — пин
  игнорируется как селектор, берётся именно он → воспроизводимость.
- Process-memo: резолвер зовёт `resolve_git_dep` на каждый импорт —
  без memo `branch`-пин делал бы `git fetch` многократно.
- Интеграция (Ф.3): `lookup_dependency` для `git`-зависимости вызывает
  `resolve_git_dep`, дальше checkout резолвится как обычный пакет на
  диске (`finalize_dep_pkg` — общий код с `path`-deps).
- Тесты: 6 unit (`git_cache::tests` — clone/checkout по tag/rev/default,
  locked-commit, ошибки) + 1 e2e (`tests/git_dep_e2e.rs` — полный
  `resolve_imports_inline` на проекте с git-зависимостью). Все offline
  (локальный `git init`-репозиторий).

**Отложено:** sha256-хэш дерева исходников для tamper-evidence — в Ф.4
(часть `nova.lock`).

### Ф.3 — Межпакетный резолв ✅ (сделано)

Резолвер (`imports.rs`): первый сегмент import → объявленная
`[dependencies]`-зависимость (+ неявный `std`); candidate-root = source
root этой зависимости.

**Сделано:**
- `lookup_dependency(importer, dep_name) -> DepLookup` — ищет первый
  сегмент среди `[dependencies]` пакета импортирующего файла; для
  `path`-deps возвращает source root дерева зависимости, для `git`-deps
  материализует репозиторий через `git_cache` (Ф.2) и резолвит checkout.
  registry/invalid/missing-path/no-manifest/name-mismatch/git-error —
  диагностируемые ошибки.
- `dep_root` проброшен `resolve_one` → `resolve_module_paths`: при
  импорте из зависимости первый сегмент (имя пакета) отбрасывается,
  единственный root = source root зависимости.
- **Ужесточение repo-root looseness (§3.2):** non-`std`, non-relative,
  non-dep импорт, резолвящийся в файл ДРУГОГО пакета (иной
  `package_root_of`), отвергается — межпакетное обязано идти через
  `[dependencies]`.
- **Cross-package cycle detection** (A→B→A): переиспользует
  module-name-keyed `in_progress`-механизм — он cross-package по
  построению, отдельного кода не потребовал.
- **Rule H `internal/`** через границу пакета: `find_internal_owner_dir`
  работает по filesystem-containment — `dep.internal.*` снаружи
  недоступен без дополнительного кода.
- Валидация `[dependencies]`: имя `std` зарезервировано, дубли запрещены.
- `verify_case` (Plan 81 Ф.4) сделан dep-aware: логический первый
  сегмент (`std` / имя пакета) не сверяется с именем директории на диске.
- 11 фикстур `nova_tests/plan03_1/` (см. Ф.6); **983 PASS / 0 FAIL**.

**Отложено в Ф.2:** фактический git-резолв (сейчас — честная ошибка).
**Снято с 03.1:** коллизия имени dependency vs workspace-member — под
explicit-dependency-моделью (§3.2: членство ≠ импортируемость) коллизии
нет, пункт vestigial.

### Ф.4 — `nova.lock` ✅ (сделано)

Модуль `compiler-codegen/src/lockfile.rs`:

- Формат `nova.lock` (минимальный TOML, ручной парсер) — `version` +
  массив `[[package]]`: `path`-deps (имя + путь), `git`-deps (имя + URL
  + исходный `pin` + резолвнутый `commit`). Неизвестные ключи парсер
  игнорирует — формат forward-совместим (резерв под effect-surface 03.4).
- `LockFile::render` / `parse` — детерминированный вывод (сортировка по
  имени).
- `collect_dep_graph(entry_pkg_dir)` — транзитивный обход
  `[dependencies]`: `path`-deps → директория, `git`-deps →
  материализация через `git_cache`. Diamond-deps — один раз.
  **Package-level cycle detection** (A→B→A; стек засеян entry-пакетом).
- `sync(entry_pkg_dir)` — загружает существующий `nova.lock` в
  `git_cache`-таблицу пинов, собирает граф, пишет `nova.lock`.
  Запускается из `nova build` (до резолва импортов).
- **Воспроизводимость:** `git_cache` lock-таблица (`install_lock_entries`
  / `forget_lock_entry`); `resolve_git_dep` без явного пина сверяется с
  ней → `branch`-зависимость не «уезжает» после первого `sync`.
- Тесты: 6 unit (`lockfile::tests` — render/parse roundtrip, forward-
  compat, package-cycle, транзитивный граф) + 1 e2e
  (`tests/lockfile_repro.rs` — `branch`-dep пинится на commit, upstream-
  коммиты не сдвигают lock). `nova build` проекта с `path`-зависимостью
  проверен — `nova.lock` генерируется корректно.

### Ф.5 — Минимальный CLI: `nova add` / `nova update` ✅ (сделано)

- `nova add <name> --path <DIR>` — добавляет path-зависимость.
- `nova add <name> --git <URL> [--tag|--branch|--rev <X>]` — добавляет
  git-зависимость (пины взаимоисключающие — clap `conflicts_with`).
- Обе формы: правят секцию `[dependencies]` в `nova.toml` (создают её
  при отсутствии; дубль имени → ошибка; правка scoped к `[dependencies]`)
  и вызывают `lockfile::sync` → `nova.lock` обновляется, git-зависимость
  материализуется в кэше.
- `nova update [<name>]` — `lockfile::update`: снимает целевые git-пины
  из `nova.lock` и пере-резолвит их через `sync` (без имени — все
  git-зависимости). `path`-deps не пинятся → отвергаются с пояснением.
- `nova add`/`update` работают только внутри пакета (с `[package]`),
  не на голом workspace-манифесте.
- Тесты: 4 unit (`insert_dependency` — вставка в секцию / создание
  секции / дубль-ошибка / scope дубль-проверки); end-to-end проверка
  `nova add --path` + `nova add --git` + `nova update` на временном
  проекте с локальным git-репозиторием.
- (Полный `publish`/`search`/`info` — registry, под-план 03.4.)

### Ф.6 — Тесты + spec + docs ✅ (сделано)

- **Фикстуры** `nova_tests/plan03_1/` (11) — собирались по ходу Ф.3:
  `path_dep` (позитив), `internal_dep` (позитив + leak), cross-package
  cycle, name-mismatch, git-dep, missing-path, `std`-reserved, dup-dep,
  cross-package boundary. Git-позитив — Rust-тестами (`git_cache` unit +
  `git_dep_e2e` + `lockfile_repro`): локальный «git» через `git init`,
  без сети.
- **spec D78 sync:** добавлен раздел «Реализация: Plan 03.1»
  (фактический формат `nova.lock`, что работает, что отложено);
  `manifest.rs` приведён к D78 — source root = корень пакета (default
  `[lib] src` → `.`, 2026-05-22 src убран).
- **docs:** `docs/nova-cli.md` / `nova-cli.ru.md` — разделы `nova add` /
  `nova update`, env-vars `NOVA_HOME` / `NOVA_OFFLINE`.

## 5. Acceptance

- ✅ Проект с `path`- и `git`-зависимостями собирается; импорт имён из
  зависимости работает полным путём.
- ✅ `nova.lock` генерируется; `git`-deps пинятся commit'ом, lock-таблица
  даёт воспроизводимость; offline-режим (`NOVA_OFFLINE`).
- ✅ `git`-dep на `branch` — lock пинит commit; ветка не «уезжает»
  (тест `lockfile_repro`).
- ✅ Cross-package cycle (на уровне импортов и пакетного графа) /
  `std`-reserved / дубль имени → детерминированная ошибка.
- ✅ `internal/` зависимости снаружи недоступна (rule H через границу).
- ✅ Формат `nova.lock` forward-совместим (неизвестные ключи
  игнорируются) — резерв под 03.4 без format-break.
- ✅ 0 регрессий: **983 PASS / 0 FAIL** (1 FAIL — `mn_runtime_actual_workload`
  — известный флак M:N-планировщика, проходит при изоляции).

## 6. Честная рамка

- 03.1 **полностью** закрывает резолюцию `path`/`git`-зависимостей — это
  не «упрощённая версия пакетного менеджера», а законченный срез
  (resolution + lockfile + reproducibility).
- Версионные диапазоны (`^1.2`) и registry — **сознательно** не здесь:
  для `path`/`git` источник пинится точно, SAT-resolver не нужен; он —
  03.2. Это декомпозиция, не упрощение.
- `git`-deps тянут произвольный код — но lock пинит точный commit, а
  git-commit криптографически адресует дерево исходников → tamper-evident
  (паритет с `Cargo.lock`). Отдельный sha256 дерева, подписи,
  transparency log, `nova audit`, effect-diff — supply-chain модель
  03.3/03.4.

## 7. Связь

- [Plan 03](03-package-ecosystem-roadmap.md) — roadmap-индекс; 03.1 —
  первый под-план.
- [Plan 81](81-module-resolution-hardening.md) ✅ — резолв внутри пакета;
  03.1 надстраивает межпакетный.
- [Plan 84](84-relative-imports.md) — относительные импорты package-
  scoped; композируется с 03.1 (межпакетное — всегда полный путь).
- [D78](../../spec/decisions/07-modules.md#d78) — `nova.toml`/`nova.lock`.
- [Plan 64](64-nova-lang-website.md) — потребитель (зависимость от `std`).
- Ориентиры: Cargo (`path`/`git`-deps, `Cargo.lock`, path-deps unpinned).
