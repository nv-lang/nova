<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 144.0: may-GC effect analysis (Ф.0 prerequisite — closes H4/Q15)

> **Создан:** 2026-06-14.
> **Статус:** ✅ DONE (2026-06-14) — все фазы Ф.0–Ф.8 закрыты; emit-nothing верифицирован.
>   Spec [D273](../../spec/decisions/06-concurrency.md#d273), CLI `nova gc-effect-analyze`,
>   10 фикстур + 19 unit-тестов, adversarial-review закрыт. Результат — §7.
> **Родитель:** [Plan 144](144-precise-gc-implementation.md) §7.6 дыра **H4**, вопрос **Q15** (may-GC),
>   тир **O1** (§7.5 «условие жизнеспособности»).
> **Зависимости:** НЕТ. Анализ **ничего не эмитит в рантайм** → не попадает под гейт
>   «стартовать после стабилизации scheduler» (§8 Plan 144) и не может сломать ни один
>   существующий тест. Полностью изолирован от других планов.
> **Модель работы:** Opus + Thinking ON (soundness-критичный анализ).

---

## 1. Зачем это, и почему сейчас (вне очереди Ф.0)

Plan 144 §7.6 перечисляет soundness-дыры precise-GC. **H4** — самая опасная:

> **H4 (high) — may-GC: косвенные/FFI-callback рёбра.** First-class замыкания
> (`(*fn)(env,…)`) + C→Nova callbacks — fixpoint может пропустить ребро → ложный NoGC →
> **UAF**. Фикс (soundness): дефолт решётки = **MayGC (top)**; NoGC доказывать.

Если may-GC-вывод неверен, **весь** codegen Ф.2+ (shadow-frame elision, отказ от
write-back перед `#no_gc`/pure-вызовами — тир O1) строится на песке: пропущенный
safe-point = пропущенный корень = use-after-free. Поэтому доказать корректность
may-GC-вывода **до** того, как на него обопрётся эмиссия кадров — настоящий de-risk.

**Почему можно делать сейчас, в отрыве от остального Ф.0:**
- Анализ **чисто compile-time** и **ничего не эмитит** в генерируемый C → нулевой
  риск для рантайма, не зависит от стабильности scheduler.
- Переиспользует уже готовую инфраструктуру
  [`preempt_keep.rs`](../../compiler-codegen/src/codegen/preempt_keep.rs) (Plan 143.2):
  call-graph, `fn_key`, overload-резолюция, итеративный Tarjan SCC.
- Результат — верифицируемый артефакт (`nova gc-effect-analyze` CLI + фикстуры),
  который Ф.2 потом **потребляет напрямую** (driver тира O1).

## 2. Что это: решётка эффекта may-GC

Каждой функции (на уровне исходного шаблона, см. §3 про мономорфизацию)
присваивается эффект из двухточечной решётки:

```
        MayGC   (top, ⊤)   ← ДЕФОЛТ (soundness — H4)
          │
        NoGC    (bottom, ⊥) ← доказывается
```

- **NoGC** — функция **доказуемо** не запускает GC: не аллоцирует сама И каждый
  достижимый вызов доказуемо NoGC.
- **MayGC** — всё остальное. **Любое сомнение → MayGC** (H4: дефолт = top).

`is_no_gc(f)` истинно **только** когда весь статический конус вызовов из `f`
полностью разрешён по именам И не содержит ни одной аллокации.

## 3. Алгоритм (переиспользует preempt_keep.rs)

### 3.1. Узлы и рёбра (как в preempt_keep)
- **Узлы** — не-external `FnDecl`, ключ `fn_key` (receiver::name::param-sig).
- **Рёбра** — прямые разрешённые вызовы (с overload-резолюцией по
  approx-arg-signatures — переиспользовать `approx_arg_sig`/`add_candidate_edges`).

### 3.2. Seed: `self_may_gc(node)`
Узел сам по себе MayGC, если ИСТИННО любое из:
1. **Аллоцирует** — тело содержит аллоцирующее выражение (см. §3.3).
2. **Косвенный вызов** (`makes_indirect`) — closure/fn-ptr/метод-на-не-self/
   trailing-block/`with`/`spawn`/`select`/`parallel-for` и пр. (как в preempt_keep:
   цель неизвестна → не можем доказать NoGC цели → top).
3. **FFI/extern** (`makes_ffi`) — внешняя функция; её may-GC неизвестен → top.
4. **Неразрешённый callee** — bare-ident/Type.method/module.func, не сматченный ни
   к одному known `FnDecl` (cross-module / closure-local) → top.

> NB: `address_taken` сам по себе НЕ делает узел MayGC (быть взятым по адресу — не
> значит аллоцировать). Он влияет лишь на КОЛЛЕРА, который делает indirect-вызов
> (там сработает `makes_indirect`). Это отличие от preempt_keep, где address_taken
> входил в KEEP-набор. Учесть при переносе.

### 3.3. Детекция аллокации (`allocates(expr)`)
**Дизайн-принцип (soundness): allowlist provably-non-allocating, всё прочее → аллоцирует.**
Конструкции, которые в текущем codegen эмитят GC-аллокацию (черновик, Ф.0 уточняет
по `emit_c.rs`):
- `RecordLit` (heap-record), sum-конструкторы, `ArrayLit`, `MapLit`,
  `InterpolatedStr` / str-конкатенация (буфер), `Lambda`/`ClosureLight`/`ClosureFull`
  (env), `Spawn`/`Detach`/`Blocking`/`Supervised`/`ParallelFor` (fiber/runtime),
  boxing escaping-значений, vec/StringBuilder/Map конструкторы и `.clone()` на
  heap-типах, range-в-коллекцию.
- **Provably non-allocating** (allowlist): целочисленная/плавающая арифметика,
  сравнения, доступ к полю/индексу без копии, `as`-касты скаляров, literal-скаляры,
  return/break/continue, чтение локала. **str-литерал** — Ф.0 решает: интернированный
  `static const u8[]` НЕ аллоцирует, но при сомнении → аллоцирует (conservative).
- Любой **неизвестный** AST-узел → считать аллоцирующим (top).

### 3.4. Транзитивная пропагация (новое vs preempt_keep)
preempt_keep считал KEEP локально + цикл. may-GC требует, чтобы MayGC **текла вверх
по коллерам**: если `g` MayGC и `f` вызывает `g`, то `f` MayGC.

Реализация — **конденсация SCC + обратный топо-проход** (переиспользовать
`cycle_members`/итеративный Tarjan, расширив до полного SCC-разбиения):
1. Построить SCC-компоненты графа.
2. SCC помечается MayGC, если: **любой** его член имеет `self_may_gc=true`, ИЛИ
   **любое** исходящее ребро (в ДРУГУЮ SCC) ведёт в MayGC-SCC.
3. Обработать SCC в обратном топологическом порядке (callee → caller).
4. `NoGC = {узлы, чья SCC не помечена MayGC}`.

**Цикл с аллокацией:** рекурсивная SCC, где хоть один член аллоцирует → вся SCC
MayGC (член `self_may_gc` ⇒ компонент MayGC). Чистая рекурсия без аллокаций и без
внешних MayGC-рёбер → NoGC.

### 3.5. Соундность над мономорфизацией
Тот же аргумент, что в preempt_keep (§ комментарий в `preempt_keep.rs`):
свойства seed (аллокация / indirect / FFI / unresolved) — **исходные**; KEEP/MayGC,
вычисленный на шаблоне, наследуется КАЖДЫМ мономорфизированным/erased-инстансом.
Over-approximation рёбер ⇒ over-approximation MayGC ⇒ ни один реальный
may-GC-инстанс не пропущен. Spurious-рёбра лишь добавляют MayGC (теряем элизию,
остаёмся соундны).

### 3.6. Conservative-gating (как PreemptKeepSet)
`MayGcSet { may_gc: HashSet<FnKey>, populated: bool }`.
`is_no_gc(key) = populated && !may_gc.contains(key)`.
Если `populated == false` (анализ не прогонялся над непустой вселенной) — **никто не
NoGC** (всё MayGC). Зеркалит `PreemptKeepSet::populated`-контракт.

## 4. Deliverables
1. `compiler-codegen/src/codegen/may_gc.rs` (новый) — `compute_may_gc_set(all_fns)`,
   `MayGcSet`, `is_no_gc`, переиспользует helpers из `preempt_keep.rs` (вынести общие
   в общий модуль ЛИБО продублировать с явной ссылкой — Ф.2 решает, без дублирования
   логики SCC если разумно).
2. `compiler-codegen/src/codegen/mod.rs` — `pub mod may_gc;`.
3. `nova-cli`: подкоманда `nova gc-effect-analyze <path> [--format json|text]`
   (зеркало `consume-analyze`): парсит, строит набор, печатает классификацию каждой
   функции (NoGC/MayGC) + сводку. **Introspection-only, в бинарь ничего не эмитит.**
4. **Codegen НЕ потребляет набор в этом плане** — гарантия emit-nothing. Потребление
   (frame-elision / write-back-skip) — Ф.2 Plan 144, отдельно и под гейтом.

## 5. Критерии приёмки
1. ✅ **Дефолт решётки = MayGC (top).** NoGC доказывается только при полностью
   разрешённом + неаллоцирующем статическом конусе. (Закрывает H4 в этом слайсе —
   spec [D273](../../spec/decisions/06-concurrency.md#d273).)
2. ✅ **Соундность над мономорфизацией** — §3.5; seed-свойства source-level, вердикт
   шаблона наследуется инстансами; покрыто негативом.
3. ✅ **Транзитивная пропагация корректна** — SCC-конденсация, обратный топо; покрыто
   позитивом (`positive_leaf_forwarder` NoGC-форвардер) и негативом (`negative_calls_allocator`/
   `negative_recursive_alloc`: аллок в callee/SCC → caller MayGC).
4. ✅ **Emit-nothing** — нулевое изменение генерируемого C; `emit_c.rs` не зовёт модуль;
   release-сборка чистая (`Finished`), регрессионный сэмпл зелёный.
5. ✅ **CLI** `gc-effect-analyze` работает (json + text), корректные exit-коды.
6. ✅ **Позитивные + негативные фикстуры** проходят через **РЕЛИЗНЫЙ nova** —
   3 позитива + 7 негативов в `nova_tests/plan144_0/`.
7. ✅ **Adversarial review** (≥3 soundness-линзы) — закрыта находка first-class-method-value
   (`obj.@m`/`Type.@m` в value-позиции эмитит env+closure alloc → был ложный NoGC, ровно
   H4-форма): `@`-префиксный `Member` теперь seed'ит аллокацию (commit `b908acb2`).
8. ✅ **«Без упрощений как для прода» — ОБЯЗАТЕЛЬНЫЙ критерий ПОДТВЕРЖДЁН.** Нет stub'ов/
   TODO-срезов; allowlist provably-non-allocating (unknown → allocates); conservative-MayGC
   при любом сомнении; soundness > агрессивность элизии на всех путях.

## 6. Фазы (порядок исполнения workflow)
| Ф | Цель | Коммит |
|---|------|--------|
| **Ф.0** research/design (parallel, read-only) | survey may-GC/effect-inference (Go `nowritebarrierrec`, HotSpot safepoint-elision, Koka/MLton); survey allocation-sites в `emit_c.rs` → финальный allowlist/denylist аллокации + дизайн-заметки | (нет — read-only) |
| **Ф.1** plan doc | этот файл закоммичен | doc |
| **Ф.2** `may_gc.rs` | реализация + `mod.rs` + Rust unit-тесты; сборка release | feat |
| **Ф.3** CLI | `gc-effect-analyze` + dispatch; сборка release | feat |
| **Ф.4** фикстуры | `nova_tests/plan144_0/` позитив+негатив + CLI-ассерты; проверка через release nova | test |
| **Ф.5** adversarial review (parallel линзы) | находки | (нет) |
| **Ф.6** фиксы ревью | применить + перепроверить | fix |
| **Ф.7** доки/spec/D/Q | D-блок (D2xx) + open-questions Q + Plan 144 §7.6/Ф.0 статус (H4-слайс закрыт) + project-creation.txt + simplifications.md + backlog-followups.md + nova-private/discussion-log.md | docs (split) |
| **Ф.8** empirical gate | полная сборка + регрессионный сэмпл; подтвердить emit-nothing; финальный статус в §7 | (в составе Ф.7) |

**Per-phase commit** — обязателен (устойчивость к серверному rate-limit фоновых
агентов: прогресс сохраняется по фазам).

## 7. Статус
✅ **DONE (2026-06-14).** Все фазы Ф.0–Ф.8 закрыты; **«без упрощений как для прода» подтверждено.**

**Результат.**
- **Анализ:** `compiler-codegen/src/codegen/may_gc.rs` (новый) — `compute_may_gc_set` /
  `MayGcSet` / `is_no_gc`, двухточечная решётка (дефолт MayGC top), allowlist-аллокация,
  SCC-конденсация с обратной пропагацией MayGC по коллерам; переиспользует call-graph/
  `fn_key`/overload-резолюцию/Tarjan из `preempt_keep.rs` (`mod.rs` экспортирует `pub mod may_gc`).
- **CLI:** `nova gc-effect-analyze <path> [--format json|text]` — introspection-only, в бинарь
  ничего не эмитит; печатает NoGC/MayGC по функции + сводку, json для машин.
- **Фикстуры:** `nova_tests/plan144_0/` — **10** (.nv): 3 позитива (`positive_pure_leaf`,
  `positive_leaf_forwarder`, `positive_pure_recursion` → NoGC) + 7 негативов
  (`negative_allocates_record`, `negative_calls_allocator`, `negative_ffi_call`,
  `negative_indirect_call`, `negative_method_value`, `negative_recursive_alloc`,
  `negative_unknown_callee` → MayGC), все через релизный `nova gc-effect-analyze`.
- **Unit-тесты:** 19/19 may_gc PASS (включая bound/unbound method-value позитив + plain-field
  NoGC-guard).
- **Emit-nothing:** генерируемый C не изменён; `emit_c.rs` не вызывает модуль; release-сборка
  из `nova-cli/` чистая (`Finished`).
- **Review outcome:** adversarial-review (≥3 soundness-линзы: indirect/FFI-дыра, направление
  пропагации, SCC-с-аллокацией, default-top) — закрыта **1 реальная находка**: first-class
  method-value (`obj.@m`/`Type.@m` в value-позиции → 2× `nova_alloc` env+closure) был ложным
  NoGC — ровно H4-форма; `@`-префиксный `Member`-узел теперь seed'ит аллокацию. Соундность-only
  (только NoGC→MayGC).
- **Spec/Q:** [D273](../../spec/decisions/06-concurrency.md#d273) (06-concurrency.md);
  [Q-may-gc-precision] (open-questions.md) для residual-точности; Plan 144 §7.6 H4-параграф +
  Ф.0-строка отмечают ранний de-risk; marker [M-144.0-may-gc-effect-analysis] → done
  (O1-потребление = Plan 144 Ф.2).

**Commit shas (ветка `plan-144-may-gc-effect-analysis`).**
- `6276d729` — plan doc (Ф.1).
- `60590d21` — `may_gc.rs` анализ + `mod.rs` + unit-тесты (Ф.2).
- `13042ed8` — CLI `gc-effect-analyze` (Ф.3).
- `b95f3854` — фикстуры plan144_0 (Ф.4).
- `b908acb2` — review-фикс: method-value seeds MayGC (Ф.6).
- docs/spec/Q/status (Ф.7) — см. commit этого коммита (ниже в логе ветки).

**O1-потребление** набора (frame-elision / write-back-skip) — **вне этого плана**, остаётся за
Plan 144 Ф.2 ([M-144.0-may-gc-effect-analysis]), отдельно и под гейтом.

## 8. Worktree / окружение
- **Worktree:** `D:\Sources\nv-lang\nova-p144`, ветка `plan-144-may-gc-effect-analysis`.
- **Сборка:** из `nova-cli/` (нет workspace-root Cargo.toml): `cargo build --release`;
  проверять `${PIPESTATUS[0]}` / `Finished` (пайп в `tail` маскирует exit-код cargo).
  Анализ-CLI требует только compiler front-half — **libuv/Boehm НЕ нужны** для
  `gc-effect-analyze`/`nova check`. Полный `nova test` (если нужен) — env как в
  worktree-setup (NOVA_GC_LIB_DIR/INCLUDE_DIR → main vcpkg; libuv копировать/junction).
- **Git:** `git add` ТОЛЬКО конкретные файлы (никогда `-A`/`.`); `git diff --cached
  --stat` ОТДЕЛЬНЫМ шагом перед commit; **НИКОГДА `git stash`** (repo-global, общий
  `.git`, конкурентные worktree); без `Co-Authored-By: Claude`.
