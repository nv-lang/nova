<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# План 240 — `bigrat` (пакет nova-bigint): точные рациональные числа поверх BigInt

**Статус:** 📋 НА РЕВИЗИИ ВЛАДЕЛЬЦА 2026-08-02 (написан интегратором по слову владельца;
раздел «Открытые вопросы» — решения владельца до запуска). Реализация НЕ начата.
**Приоритет:** P3 (ниша; по слову владельца, не самозапуск).

**Зависимость:** BigInt V1 ✅ реализован (Plan 235, `nova-bigint` master), НО в
`src/bigint.nv` НЕТ `gcd` (проверено грепом 2026-08-02: только `@div_rem/@div/@rem`) —
нормализация BigRat без НОД невозможна → **Ф.1 этого плана добавляет `BigInt @gcd` в
`bigint.nv`** (дом по смыслу — bigint, не bigrat). Семья: 235 ✅ / 236 BigDecimal ✅
(2026-07-31) / 237 BigFloat ✅ (2026-08-01) — BigRat закрывает четвёрку.

## 0. Мотив

BigRat — точная дробь p/q: арифметика `+ − × ÷` **вообще без округления** (1/3 хранится
как 1/3). Ниша: геометрические предикаты (погрешность ломает логику, не цифры), точные
вероятности/доли, символьные вычисления. **Внутренний мотив семьи:** BigRat — точный
ЭТАЛОН для тестов округления BigDecimal/BigFloat («округлённый результат vs точный
рациональный») — тест-синергия, которой сейчас нет. Эталоны: Go `math/big.Rat`,
Python `fractions.Fraction`, Ruby `Rational`, Haskell `Rational`.

Строится **исключительно поверх BigInt** (как 236/237): собственных лимбов нет,
добавленная сложность — только пара (num, den) и её нормальная форма.

## 1. Объём V1 — корректность, читаемость, покрытие; никаких оптимизаций

Представление + нормальная форма; конверсии (int-веер, str, BigDecimal↔BigRat);
`@plus/@minus/@times/@div/@recip/@neg/@abs`; `@compare/@equal/@hash`; `@to_str/@to_int/
@to_i128`; предикаты. **НЕ входит:** continued fractions/Stern-Brocot (единственный
именованный V2-кандидат из этой области — `limit_denominator`, см. вопрос 6); `@to_f64`
(путь через `@to_bigdecimal`/`@to_bigrat`-связки); `BigRat @to_bigfloat` (вопрос 5);
литералы/неявные коэрсии (аллокация вне полос D429); ускорения gcd (binary-gcd, Lehmer);
mediant; pow.

## 2. Дизайн (V1)

- **Представление:** `type BigRat value { num BigInt, den BigInt }` — value-record над
  heap-BigInt, тот же приём, что BigDecimal (`{ mant BigInt, scale int }`); прецедент
  «value-record с BigInt-полем» доказан 236-м, с ДВУМЯ BigInt-полями — пин Ф.0.
- **Нормальная форма (инвариант КАЖДОЙ операции):** `den > 0` (знак живёт в num);
  `gcd(|num|, den) == 1`; канонический ноль = `0/1`. Следствие: `@equal` = покомпонентное
  сравнение, `@hash` тривиально корректен.
  **Осознанное отличие от BigDecimal:** 236 нормализует ЛЕНИВО (trailing zeros живут,
  каноничность только в `@equal/@hash/normalize()`); BigRat нормализует **в каждой
  операции** — для рациональных это не косметика, а защита от взрыва num/den (без
  сокращения растут даже у сократимых дробей; ленивость здесь = квадратичное распухание
  по цепочке операций).
- **Ошибки — канон семьи (Result, не panic):** `den == 0` при конструировании →
  `Err(DivError.DivisionByZero)` (тип УЖЕ есть в bigint.nv:29 — переиспользуем, свой не
  заводим); `@div/@recip` на ноль — так же. Паритет `BigInt @div_rem -> Result[_, DivError]`.
- **API V1** (все конверсии — `to_*`-канон, nv-coding-style §1/D410; формы сверены с
  реальным bigdecimal.nv):
  - `BigRat.new(num BigInt, den BigInt) -> Result[BigRat, DivError]` — нормализует
    (знак/НОД); `BigRat.zero()/one()`;
  - **ОДИН генерик-бланкет** `fn[T Ints] T @to_bigrat() -> BigRat` (n/1) + отдельная
    перегрузка `i128 @to_bigrat()` (i128 ∉ `Ints`) — по образцу `@to_bigint`
    (bigint.nv:408/423), НЕ по веерам 236/237: те 10-перегрузочные веера были обходом
    `[M-generic-body-calls-generic-mono-placeholder]`, дефект ЗАКРЫТ №170 (2026-07-31),
    generic-из-generic пробой подтверждён 2026-08-02. **Попутная заметка (вне объёма
    240):** свёртка вееров `@to_bigdecimal`/`@to_bigfloat` 10→1 разблокирована тем же
    фиксом — отдельная мелкая задача;
  - `str @to_bigrat() -> Result[BigRat, ParseBigRatError]` — формы V1: `"p/q"` и целое
    `"n"` (знак у p); реализация — ДЕЛЕГАЦИЕЙ: split по `/`, части парсит существующий
    `str @to_bigint()`, его ошибки маппятся; `ParseBigRatError enum Empty | OnlySign |
    InvalidCharacter | ZeroDenominator` — имена вариантов строго как у
    `ParseBigIntError`/`ParseBigDecimalError` (паритет семьи; НЕ `BadDigit`);
  - **связка семьи — ОБЕ сестры (мотив «точный эталон» требует обеих):**
    `BigDecimal @to_bigrat() -> BigRat` — ТОЧНАЯ (mant/10^scale, затем нормализация);
    `BigRat @to_bigdecimal(mc MathContext) -> BigDecimal` — деление num/den с
    округлением mc (первый носитель «BigRat как эталон» в тестах 236);
    `BigFloat @to_bigrat() -> BigRat` — ТОЧНАЯ (mant×2^exp: exp≥0 → `num = mant.shl(exp)`,
    exp<0 → `den = BigInt.one().shl(-exp)`; `@shl`/`@bit_length` уже в bigint.nv:702/714
    с волны 237). Обратная `BigRat @to_bigfloat(ctx)` — НЕ в V1 (округление деления в
    двоичной мантиссе — своя аккуратность; появится с первым носителем — открытый
    вопрос 5);
  - `@num()/@den()` — property-геттеры (D84/D409, образец `BigDecimal @scale()`);
  - `@plus/@minus/@times` (операторы `+ - *` через desugar — прецедент семьи);
    `@div(other) -> Result[BigRat, DivError]` и `@recip() -> Result[BigRat, DivError]`
    (ноль → Err; оператор `/` НЕ десугарится — Result-возврат, симметрия «`/` не
    оператор» у BigDecimal); `@neg/@abs`;
  - `@compare/@equal` — `@equal` покомпонентно (нормальная форма), `@compare`
    кросс-умножением `num_a*den_b <=> num_b*den_a` (den>0 ⇒ знаки корректны, float не
    участвует); `@hash() -> u64` (образец BigDecimal:377);
  - `@to_str()` — `"p/q"`; целые (den==1) — просто `"p"` (см. открытый вопрос 2);
    `@to_int()/@to_i128() -> Option[...]` — trunc-к-нулю (паритет int-деления), None
    при не-целом? НЕТ — trunc всегда определён: `num/den` trunc; Option только на
    переполнение приёмника (паритет `BigDecimal @to_int`);
  - `@is_zero()/@is_int()` (den==1) / `@sign() -> int`.
- **Ф.1-предусловие в bigint.nv:** `export fn BigInt @gcd(other BigInt) -> BigInt` —
  Евклид через готовый `@rem` (V1; binary-gcd — вне объёма), результат неотрицателен,
  `gcd(0,0)=0`; + тесты рядом.
- **Дом:** пакет `nova-bigint`, файлы `src/bigrat.nv` (`module bigint.bigrat` — по
  образцу `bigint.bigdecimal`) + `src/bigrat_test.nv`. НЕ std, НЕ новая репа.

## 3. Фазы (одна sonnet-волна с внутренним порядком; приёмка интегратора)

- **Ф.0 Пины (внутри той же волны, ДО ядра):** (а) value-record с двумя BigInt-полями —
  мини-фикстура компилябельности; (б) операторный desugar `+ - *` на новом типе (дёшево,
  прецедент семьи); (в) `DivError` из `bigint` виден в `bigint.bigrat` (кросс-модульный
  импорт внутри пакета); (г) бланкет `fn[T Ints] T @to_bigrat()`, зовущий бланкет
  `@to_bigint()` из своего generic-тела, — В ПАКЕТНОМ контексте (№170 закрыт, проба
  2026-08-02 в репе nova зелёная; пакетный CU подтвердить дёшево; красный → откат на
  веер 236-го образца с маркером, НЕ блокер волны).
- **Ф.1 gcd в bigint.nv:** `@gcd` (Евклид/@rem) + тесты (пары с известным НОД, 0-кейсы,
  большие взаимно-простые) — отдельный коммит.
- **Ф.2 Ядро bigrat:** представление, normalize (знак+НОД), new/zero/one, веер
  `@to_bigrat`, `@num/@den`, предикаты, `@neg/@abs`, `@compare/@equal/@hash`.
- **Ф.3 Арифметика:** `@plus` (a/b + c/d = (ad+cb)/bd → normalize; оптимизация НОД-ов
  Кнута — вне V1), `@minus/@times/@div/@recip`.
- **Ф.4 Строки и связка семьи:** `str @to_bigrat`/`@to_str`;
  `BigDecimal @to_bigrat`/`BigRat @to_bigdecimal(mc)`; `BigFloat @to_bigrat` (точная).
- **Ф.5 Тесты (рядом с модулем):** identities на малых значениях против int-эталона
  (seeded splitmix64 `.wrapping_*`): a+b-b==a, (a/b)*b==a (b≠0), compare-транзитивность;
  нормальная форма после каждой op (den>0, gcd==1 — прямой assert через @gcd);
  канонические вектора: 1/3+1/6==1/2, суммы гармонического ряда H_10 (точное значение),
  BigDecimal-roundtrip (1.25 ↔ 5/4); большие num/den (100! / 99! == 100). Большие
  вектора — в репо, не в дефолт-прогон. **Плюс первый эталон-кейс мотива (тест-синергия
  семьи):** `BigDecimal @div(mc)` под HalfEven против точного `BigRat`-частного с ручным
  округлением — расхождение = баг одного из двух; зеркальный кейс для `BigFloat @div`
  через точный `@to_bigrat` обеих сторон.
- **Ф.6 Закрытие:** doc-комменты (англ.), STATUS/README пакета — строка о четвёртом
  типе; приёмка интегратора: `nova check`/`nova test` пакета, пуш на 3 ремоута.

## 4. Гейты

Таргетно (агент): `nova check` + `nova test` пакета nova-bigint зелёные (все 4 модуля —
регресс 235/236/237 обязателен: gcd-коммит трогает bigint.nv); линт чистый. Приёмка
(интегратор): повтор гейтов + пуш. Мега-CU не касается (пакет вне conformance).

## 5. Риски

| Риск | Митигация |
|---|---|
| Взрыв num/den по цепочке операций | обязательная НОД-нормализация каждой op (§2) + perf-заметка честно: V1 корректность, не скорость |
| gcd Евклидом поверх O(n²)-деления медленный | приемлемо V1; binary-gcd — V2 по замеру |
| Кросс-умножение в @compare аллоцирует | честная цена точности; short-cut по знакам — микро-опт, можно в V1 если тривиален |
| Правки bigint.nv (gcd) заденут 235-регресс | Ф.1 отдельным коммитом + полный тест пакета в гейте |

## 6. Открытые вопросы владельцу (до запуска волны)

1. **Парсинг десятичной формы** `"1.25"` в `str @to_bigrat` (точно = 5/4)?
   Рекомендация: НЕ в V1 — пересекается с BigDecimal-парсингом; путь пользователя:
   `"1.25".to_bigdecimal()` → `.to_bigrat()` (точен, уже в объёме).
2. **`@to_str` целых:** `"5"` или `"5/1"`? Рекомендация: `"5"` (читаемость; roundtrip
   через парс целого сохраняется).
3. **floor/ceil-варианты `@to_int`** (сейчас только trunc): нужны в V1?
   Рекомендация: нет (появятся с первым носителем).
4. **Имя типа:** `BigRat` (семья Big*, Go-паритет) или `Rational` (Ruby/Haskell)?
   Рекомендация: `BigRat` — единообразие пакета.
5. **`BigRat @to_bigfloat(ctx)` (обратная связка):** в V1 или с первым носителем?
   Рекомендация: НЕ в V1 (двоичное округление деления — своя аккуратность; прямая
   точная `BigFloat @to_bigrat` уже даёт эталон-тесты 237-му без обратной).
6. **`limit_denominator` (Python `Fraction.limit_denominator`, «лучшая дробь с den ≤ N»):**
   зафиксировать как ЕДИНСТВЕННОГО кандидата V2 из исключённых continued fractions?
   Рекомендация: да, строкой в §1 — чтобы V2-заявки не размывали объём.

## Связи

[235](235-bigint.md) (фундамент; gcd-дыра закрывается здесь Ф.1) ·
[236](236-bigdecimal.md) (каркас-образец; связка to_bigrat/to_bigdecimal; КОНТРАСТ
нормализации — ленивая там, жадная здесь, §2) · [237](237-bigfloat.md) (семья) ·
nv-coding-style §1/D410 (to_*-канон) · D9 (одна дверь: `/` не оператор при
Result-возврате) · D429 (без коэрсий) · эталоны Go big.Rat / Python Fraction.
