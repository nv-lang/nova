<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 290 — смена интегратора 2026-09-16: десять поручений владельца

**Статус:** 🚧 В РАБОТЕ с 2026-09-16. **Ведёт:** интегратор. **Родитель:** нет —
план ведёт СМЕНУ, а не предмет; каждый пункт либо закрывается здесь, либо уезжает
в профильный план строкой «переехало в NNN».

**Зачем этот файл существует.** Владелец выдал десять поручений одним сообщением и
потребовал план с приёмкой по каждому. До него часть работы этой смены уже была
сделана по устным поручениям — она записана разделом «Уже сделано» ниже, чтобы
план не выглядел начинающимся с нуля и чтобы сделанное можно было проверить, а не
принять на слово.

**Правило этого файла:** приёмка каждого пункта — МАШИННАЯ, то есть названа
командой и строкой вердикта. Где приёмка глазами — так и написано, с причиной.

---

## Уже сделано этой сменой по устным поручениям (до появления плана)

Всё перечисленное влито в `main` и запушено на три зеркала; проверяется `git log`.

| что | коммит | чем доказано |
|---|---|---|
| слияние `p274-novac` (83 коммита Карины, 89 файлов) | `9e29ba857` | числа реестра ПЕРЕСЧИТАНЫ правилом стража на итоговом дереве |
| 274 §11а — что реестр 221.1 значит для Карины | `c1a895732` | три роли названы; сказано и чего он НЕ значит |
| шесть красных гейта, принесённых слиянием | `e00594096` | каждый закрыт у причины, не у числа |
| две охоты (guards ×6 находок, `emit_c`×К7 ×4) | `f03c86572` | пробы в дереве, отчёты с машинными строками |
| строки реестра №1123–№1133 | `bf564ebcb` | форма проверена стражем (343 ≤ 343) |
| переезд реестра расхождений в `274.12` | `875a979ed` | 49 ссылок в 18 файлах; переезд вскрыл дословный повтор |
| хвост переезда — 10 ссылок в `.nv`/`.allow` | `5edad122f` | проверка тем же скриптом имела слепоту правки |
| снятие трека охоты `oracle` + правило «кто платит долг» | `44da383c1` | ключ УДАЛЁН, а не поднят |
| 274.11 пункт 8 — стражи, снимаемые по СОБЫТИЮ | `a192025b8` | 10 к снятию, 9 рантаймовых переживают |
| №975 ЗАКРЫТА — раннер называет сигнал | `f7ed1c235` | проба в обе стороны на настоящем Linux (WSL) |
| №1134 ЗАКРЫТ — кэш libuv получил ключ среды | `6593debb4` | три среды — три корзины |
| гейт: ярус `push` судит дифф | `856167998` | 1371 → 358 с, три пробы |
| гейт: стражи в параллель по памятке | `020713f87` | 358 → 208 с; проба «заведомо красный страж» |
| №1136 + починка двух протухших указателей + передача | `700ee6828` | найдено вопросом владельца, не проверкой |
| №1135 ЗАКРЫТ — ключ архива видит компилятор | `54b81ccd9` | ДВЕ первые пробы оказались ложными |

**Что из этого НЕ закрыто и переезжает в пункты ниже:** гейт ускорен вшестеро, а
не вдесятеро (пункт 9); CI красен двумя независимыми (пункт 5).

---

## Пункт 0. Разбор №848/№1038 — фикстура снимается пределом под нагрузкой

**Слово владельца:** «разбирай — беру №848/№1038 в работу вместо пуша».

**Что известно ДО работы, чтобы не искать заново.** `presume_446_stress` —
стресс-репро гоночных строк №446/№447, будит тысячи запаркованных файберов.
Проявления РАЗНЫЕ в разных средах, и это главная зацепка:

- на CI (двухъядерный раннер Linux) — **SIGSEGV(11)**, строка №968;
- локально в пачке мега-CU — **снята ПРЕДЕЛОМ времени**, в одиночку зелёная;
- в WSL (8 ядер, 31ГБ, `overcommit_memory=1`) — зелёная и одна, и в пачке, и на
  двух ядрах через `taskset`.

**Гипотеза, названная ДО прогона (иначе это не причина, а история):** узкое место
— не память и не ядра, а ЗАМОК ПЛАНИРОВЩИКА при малом числе воркеров (так и
говорит №848). Ось, отделяющая её от соседней: время выполнения должно расти
СВЕРХЛИНЕЙНО при уменьшении числа воркеров при неизменной работе.

**Приёмка пункта, машинная:**
- (а) снят профиль: время фикстуры при `NOVA_MAXPROCS` = 16, 8, 4, 2, 1 на одном
  дереве и одном бинаре, каждая точка — три прогона, в таблицу; рост
  сверхлинейный ИЛИ гипотеза отвергнута явной строкой «ось не подтвердилась»;
- (б) если замок найден — названы два адреса `файл:строка` и вопрос, на который
  они отвечают по-разному;
- (в) строка №848 обновлена ЗАМЕРОМ (не рассуждением), №1038 — тем же слиянием;
- (г) если причина не найдена — это законный исход, и он записывается как
  НАЗВАННЫЙ ПРОБЕЛ с перечнем того, что проверено и чем именно.

**Не делаем:** не чиним планировщик в этом пункте — пункт про ДИАГНОЗ. Починка
уедет своим номером.

---

## Пункт 1. `/next` в чужом окне — годится ли

**Вопрос владельца:** подходит ли `/next` окну, которое ведёт свою задачу
(например, окну агента 274)?

**Ответ по факту — НЕТ, и это проверяется чтением файла.** `.claude/commands/next.md`
первым разделом объявляет ГЛАВНОЙ ЗАДАЧЕЙ вывод Карины в релиз и велит брать из
274.10/274.11 — то есть это команда ИНТЕГРАТОРА, а не всякого окна. Окно 274,
позвавшее `/next`, получит указание бросить свою волну.

**Приёмка:**
- (а) команды разделены по РОЛИ, и роль названа в первой строке описания каждой;
- (б) `grep -c` показывает, что ни одна команда не велит двум разным ролям разное
  под одним именем;
- (в) в каждой команде есть строка «кому эта команда» — проверяется грепом.

---

## Пункт 2. Команда запуска новой сессии интегратора

**Зачем:** если окно оборвётся, восстановление сегодня требует помнить три вещи
(handoff, `/next`, живые строки подпланов) — то есть держится на памяти.

**Приёмка:**
- (а) команда существует и её описание называет роль;
- (б) она печатает СОСТОЯНИЕ ИЗ ДЕРЕВА (ветка, непушенное, последний вердикт
  гейта, живые строки 274.x), а не пересказывает записку;
- (в) страж шапок слэш-команд зелёный.

---

## Пункт 3. Сохранение сессии командой + автоматически раз в час

**Замер, из-за которого пункт есть:** передача не обновлялась 17 часов работы;
правило было, механизма не было.

**Приёмка:**
- (а) команда сохранения существует и дописывает раздел с ВРЕМЕНЕМ из `date`;
- (б) автовызов раз в час настроен механизмом, а не памятью окна;
- (в) проба: вызвать дважды подряд — второй раз НЕ создаёт дубля раздела.

---

## Пункт 4. Ревизия `.claude` после отмены релиза оракула

**Приёмка:**
- (а) таблица: команда → жива/устарела/дублирует → решение (оставить, переориентировать,
  удалить, слить);
- (б) ни одна команда не ссылается на закрытые планы как на живые (греп по 221, 221.3);
- (в) число команд после ревизии названо и обосновано, а не «стало меньше».

---

### Пункт 4 — ВЕРДИКТ 2026-09-16 17:32: команд по-прежнему 22, и это решение

**(в) Число названо и обосновано: 22 → 22.** Одна команда заменена (не удалена и не
добавлена): `check-oracle-release` → `check-release`. **Сливать было нечего, и это
проверено, а не предположено:** четыре команды-перепроверки выглядят дублями и ими не
являются — они разделены ПО ПРЕДМЕТУ, и граница теперь написана в каждой: `/oracle-recheck`
судит `compiler-codegen`/`nova-cli`/`nova-lsp`/`std`, `/novac-recheck` — `novac/`,
`/task-recheck` — всё остальное (стражи, гейт, доки, пакеты), `/plan-recheck` — ДОКУМЕНТ,
а не работу по нему. Слить их значило бы получить одну команду, которая для каждого
случая права на четверть.

**Что сделано по (а):**

| команда | состояние | решение |
|---|---|---|
| `check-oracle-release` | МЁРТВАЯ ЦЕЛИКОМ: мерила расстояние до тега v0.1 оракула | переориентирована на лестницу Карины и переименована в `check-release`; устройство замера («число падает тремя способами, и только один — движение») перенесено дословно, потому что верной была механика, а неверным — предмет |
| `next` | указывал на план 221 как на живой источник приоритета | указатель заменён лестницей версий; это ТРЕТИЙ протухший указатель в этом файле за сутки, и все три записаны в нём летописью |
| `oracle-recheck` | **порча моей же утренней правки** | см. ниже — отдельный случай |
| `trim` | ссылалась на `controller.md`, которого нет (файл переименован в `push-controller.md`) | исправлено, 2 вхождения + `argument-hint` |
| 19 команд из 22 | не говорили, КОМУ они адресованы | дописана строка «КОМУ ЭТА КОМАНДА» каждой; форма взята у `integrator.md` |
| `delegate`, `defect-hunter` | трек охоты `oracle` закрыт | закрыто ранее этой же сменой |

**ОТДЕЛЬНЫЙ СЛУЧАЙ, И САМЫЙ ДОРОГОЙ УРОК ПУНКТА: `/oracle-recheck` полдня нёс ДВЕ цели
сразу.** Утренняя правка вписала новую цель («снимает ли работа налог оракула на
Карину») и оставила СТАРЫЙ абзац стоять ниже — с его указанием «первым вопросом: эта
работа приближает тег или нет?». То есть команда, которой перепроверяют работу по
оракулу, велела мерить событие, которого не будет. Единственным видимым следом было
оборванное `**` посреди фразы.

**Правило, которое это стоило:** замена цели — это УДАЛЕНИЕ прежней, а не дописывание
новой сверху. Прежний текст, если он ценен, остаётся ОДНОЙ строкой летописи, а не живым
абзацем с указаниями. Записано в самой команде, рядом с местом порчи.

**(б) Ссылок на закрытое как на живое — ноль.** Мерка точная: `221.3`, `221-release`,
«план 221», `v0.1`, «тег оракула» (слово «оракул» из мерки убрано намеренно — оракул ЖИВ
и правится там, где мешает Карине). Осталось 12 строк, и все двенадцать — ЛЕТОПИСЬ вида
«здесь стояло то-то, и оно протухло тогда-то». Летопись оставлена сознательно: без неё
следующее окно впишет тот же указатель заново — так уже было дважды в `next.md`.

## Пункт 5. CI зелёный?

**Известно:** нет, красен ДВУМЯ независимыми — №1061 (заголовки рантайма как C++)
и №968 (SIGSEGV фикстуры). Пуши идут с `NOVA_SKIP_CI_CHECK=1`, счёт обходов в
№968 — шесть.

**Приёмка:** `gh run list` на свежем коммите даёт `success` у **всех** job'ов
`nova-gate`, либо каждый оставшийся красный имеет строку реестра с ЗАМЕРОМ и
названной причиной, почему он не чинится сейчас.

---

### Пункт 5 — ВЕРДИКТ 2026-09-16 16:10: НЕТ, и красных ТРИ, а не два

Замер по прогону `nova-gate` 35087740107 на коммите `700ee6828`. Из семи
воркфлоу **шесть зелёные** (`crate-tests`, `nova-lint`, `nova-test-regression`,
`nova-doc`, `contracts-crosscheck`, `contracts-z3`); красен ровно `nova-gate`, и
внутри него три job'а из пяти:

| job | время | причина | адрес |
|---|---|---|---|
| `gate.sh tier loop` | 42с | `check-runtime-cxx-clean`: заголовки рантайма не компилируются как C++ | №1061 |
| `gate.sh tier push` | 28м28с | фикстура `presume_446_stress` | №968 |
| `novac-gate` | 45м17с | **отменён по пределу времени job'а (45 мин)** | №1138, заведена сегодня |

**Третий красный найден этим вопросом и не был заведён нигде.** Он отличается от
двух других по природе: те — ОТКАЗЫ проверок, а этот — ОТМЕНА, то есть проверки
не досчитались вовсе и о состоянии Карины на CI не известно НИЧЕГО. В списке
job'ов оба вида выглядят одинаковым крестом.

**Почему пункт не закрывается починкой прямо сейчас, и это решение, а не
отговорка.** №1061 — 93 ошибки в пяти заголовках рантайма, работа отдельной волны
(и открытый вопрос 2 этого плана). №968 — сегодня разобран до диагноза (пункт 0),
но причина не найдена, а лечить симптом пределом запрещено оговоркой строки.
№1138 лечится ускорением `gate-novac.sh`, то есть пунктом 9 этого же плана.
**Приёмка пункта выполнена во второй её половине:** каждый оставшийся красный
имеет строку реестра с ЗАМЕРОМ и названной причиной, почему не чинится сейчас.

## Пункт 6. Время и профиль в выводе

**Что просит владелец:** локальное время в любом сообщении любого окна; при старте
гейта — время, имя, ОЖИДАЕМАЯ длительность; по завершении — время, полное время,
имя и профиль.

**Приёмка:**
- (а) гейт печатает при старте: местное время, имя яруса, ожидаемую длительность
  (из базы профиля, а не из головы);
- (б) по завершении: местное время, полное время, имя, и путь к профилю шагов;
- (в) ожидаемая длительность берётся из ФАЙЛА и обновляется прогоном, иначе она
  протухнет как протух профиль 2026-08-21;
- (г) правило «время в каждом сообщении» записано там, откуда приезжает в каждое
  окно, а не в одной команде.

---

## Пункт 7. Критерии приёмки во всех планах семьи 274

**Приёмка:** для 274 и каждого 274.x названо: есть ли раздел приёмки, машинна ли
она, есть ли «Не делаем» и «Открытые вопросы». Результат — таблица; у кого нет —
дописано или названо, почему не нужно.

---

### Пункт 7 — ВЕРДИКТ 2026-09-16 17:04: приёмка есть у всех, кроме реестра; дыры в границах у двух

Мерено скриптом по тринадцати файлам `docs/plans/274*.md`: упоминания приёмки в
заголовках и жирных зачинах, и есть ли в тридцати строках под каждым КОМАНДА (грубый
признак машинности — он ловит «сделать X» и пропускает красивую формулировку без
запуска; так и назван).

| файл | упоминаний приёмки | из них с командой | «Не делаем» | «Открытые вопросы» |
|---|---|---|---|---|
| 274 | 56 | 35 | да | да |
| 274.1 | 8 | 1 | **НЕТ** | **НЕТ** |
| 274.2 | 1 | 1 | да | да |
| 274.3 | 1 | 1 | **НЕТ** | **НЕТ** |
| 274.4 | 9 | 5 | да | да |
| 274.5 | 25 | 12 | да | да |
| 274.6 | 17 | 11 | да | да |
| 274.7 | 22 | 21 | да | да |
| 274.8 | 63 | 24 | да | да |
| 274.9 | 10 | 2 | да | да |
| 274.10 | 2 | 1 | да | да |
| 274.11 | 2 | 2 | да | да |
| 274.12 | **0** | 0 | да | нет |

**ПЕРВАЯ МЕРКА БЫЛА НЕВЕРНА, И ЭТО ГЛАВНОЕ, ЧТО ДАЛ ПУНКТ.** Она искала слово
«Критерии» во множественном числе — а 274.9 пишет «**Критерий:**» в единственном, у
каждой из семи волн. План с критерием у каждой волны числился «без приёмки вовсе», и
я собирался дописывать ему то, что там уже есть. Это тот же класс, который в тот же
день чинился в `check-plan-wave-boundaries.py` (там оказалось ПЯТЬ написаний), —
значит класс не закрылся починкой стража: он живёт в каждой новой мерке, которую
пишут по памяти о форме, а не по корпусу.

**Что реально открыто:**

1. **274.12 — приёмки нет, и это законно.** Причина дописана в сам файл: реестр не
   делается, а ведётся, его условие конца — исчезновение предмета (уже в статус-строке).
   Тот же случай у 221.1 и 251.1.
2. **274.1 и 274.3 — нет ни «Не делаем», ни «Открытых вопросов».** Это НАЗВАННЫЙ
   ПРОБЕЛ, а не сделанная работа: границы архитектурного плана («чего архитектура
   Карины НЕ покрывает») и плана классов ревью — суждение окна 274, которое их ведёт,
   а не интегратора, который их принимает. **Адресат: окно 274. Умолчание, если
   молчит:** при следующем слиянии любого из двух интегратор не принимает волну без
   этих разделов — у стража `check-plan-wave-boundaries` они и так обязательны для
   волн после 2026-08-29, так что умолчание уже механизировано.
3. **Машинность приёмки нигде не нулевая**, но у 274.1 она 1 из 8, у 274.9 — 2 из 10.
   Это не дефект формы: у 274.9 критерий волны формулируется через долю корпуса и
   вердикт оракула, и команда там — `novac-diff-corpus.sh`, названный в другом абзаце.
   Признак «команда в тридцати строках» такое не видит, и это его предел, а не их.

**Приёмка пункта выполнена:** по каждому файлу семьи названо, есть ли приёмка, машинна
ли, есть ли границы и вопросы; у единственного без приёмки причина дописана в файл; у
двух без границ назван адресат и умолчание.

## Пункт 8. Ревизия планов от 289 до 240

**Приёмка:** таблица по каждому плану: статус-строка есть · актуален ли · есть ли
приёмка · решение (оставить/закрыть/переориентировать). Планы, потерявшие предмет
с отменой релиза оракула, закрываются с причиной.

**Граница:** судится ТОЛЬКО диапазон 240–289; более ранние — отдельным пунктом,
если владелец скажет.

---

### Пункт 8 — ВЕРДИКТ 2026-09-16 17:06: закрывать нечего, устарели СРОКИ, а не планы

Ревизия сделана агентом (opus, 14 минут, 55 файлов `docs/plans/240-*` … `289-*`,
включая подпланы), числа пересчитаны мной скриптом — и разошлись, см. ниже.

**Кандидатов на закрытие — НОЛЬ, и это главный ответ пункта.** Ни один план
диапазона не построен на «довести оракул до тега» как на единственном предмете: у
каждого свой предмет — язык, спека, std, рантайм, пакет, инструмент, — и он переживает
отмену релиза оракула. Устарело у них другое: СРОК. Он звучал «до тега» и указывал на
событие, которого не будет.

**Поэтому работа пункта 8 оказалась не «закрыть устаревшее», а «дать сроку новый
адрес»** — и это сделано лестницей версий (план 274) и стражем
`check-plans-tag-bound.py`, который не даёт числу планов без вердикта расти.

**Замер агента я не принял на веру, и правильно сделал.** Он назвал «минимум 26
файлов» с тег-фразировкой; мой счёт той же фразы даёт **49 файлов, из них 42
кандидата** (семь — сам предмет). Разница не в аккуратности, а в ГРАНИЦЕ: агент судил
диапазон 240–289, как и было велено, а тег держит ещё шесть файлов вне его — 129,
172.15, зонтик 222 с тремя подпланами и 238. Задание, ограниченное диапазоном, не может
найти то, что лежит за диапазоном; граница была моя, и промах тоже мой.

**Строка статуса есть у всех 55 файлов** — ни одного пропуска.

**Без раздела приёмки — десять планов:** 243, 246, 248, 249, 250, 260, 267, 268, 269,
270. (Ещё три — 251.1, 253, 274.12 — без приёмки ЗАКОННО: сырой инвентарь, зонтик с
приёмкой в подпланах, живой реестр.)

**Решение по этим десяти — не дописывать приёмку сейчас, и вот почему это не
откладывание.** Все десять стоят под запретом лестницы (не делаются до 0.3), а
критерии пишутся ТЕМ, КТО БЕРЁТ ПЛАН, до первой строки кода — таково правило `/next`.
Приёмка, написанная сегодня интегратором для плана, который возьмут через месяц и не
он, — это догадка о чужой работе, и переписана она будет первой же волной. Момент
взятия плана и есть момент написания критериев, и он механизирован:
`check-plan-wave-boundaries` судит каждую волну, датированную после 2026-08-29.

**Отдельного внимания владельца требуют три места, где срок был не просто фразой:**

* **286 (драйвер БД), §L1022** — пункт помечен СРОЧНЫМ и привязан к тегу дословно:
  «снять `#stable` до тега либо переезжать до тега». Триггера больше нет, а пункт жив;
* **172.15** — решение владельца 2026-08-08 «`__sret` реализовать ДО ТЕГА» отменило
  прежний порядок интегратора; к какой ступени лестницы оно теперь относится, не сказано;
* **268 и 273** — «отложено в v0.2». Раз релиза оракула не будет, неясно, существует ли
  «v0.2 оракула» как адресат вовсе, или эта работа не нужна никому. (Не путать с
  Carina 0.2: совпадение номеров случайное и опасное — я сам споткнулся о него.)

**Приёмка пункта выполнена:** таблица по каждому плану есть (в отчёте ревизии),
статус-строка проверена у всех, кандидатов на закрытие ноль с названной причиной,
планы без приёмки перечислены поимённо с решением по каждому.

## Пункт 9. Гейт при пересборке — в десять раз

**Замер:** прогон с правкой `compiler-codegen` — **2388 с**; без правки — 208 с.
Разница вся в пересборке и в том, что задета область `compiler`, включающая ВСЕ
поведенческие шаги.

**Приёмка:** прогон, включающий пересборку, укладывается в 240 с, ИЛИ названо
числом, какая часть неустранима и почему.

---

## Не делаем (границы всего плана)

- **Карину не чиним** (слово владельца 2026-09-16) — строки №1130–№1133 заведены
  и НЕ взяты в работу до отмены этого запрета.
- **Стражей оракула не снимаем** до события «Карина собрала себя сама» (274.11 п.8).
- **Планы до 240 не трогаем** — граница пункта 8.

## Открытые вопросы

1. **Кому:** владелец. **Вопрос:** пункт 9 требует 240 с при пересборке — если
   неустранимая часть окажется больше, что режем: параллельность сборки или состав
   поведенческих шагов? **Умолчание до ответа:** ничего не режу, называю число.
2. **Кому:** владелец. **Вопрос:** пункт 5 — чинить №1061 (заголовки как C++, 93
   ошибки в пяти заголовках) в этой смене? **Умолчание:** не чиню, оставляю запись.
