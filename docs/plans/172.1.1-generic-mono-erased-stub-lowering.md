# Plan 172.1.1 — Generic-mono / erased-stub lowering reconciliation (U.4.5 substrate)

**Статус:** 📋 proposed 2026-06-27 (выделено из 172.1 U.4.5 — решение владельца)
**Родитель:** [172.1 §9](172.1-unified-type-engine.md) U.4.5; маркер `[M-172.1-U4-typedir-substrate]`.
**Конвенции:** §0 (один лоуэринг `resolved_type_to_c`, `type_ref_to_c`/`erased_type_ref_c` ретайрятся),
§7 (де-риск: detect → full-.c-diff baseline), §1 (нет авто-fallback / `nova_int`).

## 0. Зачем отдельный суб-план

U.4.5 (удалить codegen-re-derive `infer_expr_c_type_legacy`) требует, чтобы `resolved_type_to_c`
(единый лоуэринг) был **byte-identical** с текущим codegen-выводом ДЛЯ ВСЕХ видов выражений
(приёмка U.4.5: «full .c diff vs clean = пусто»). §7-измерение (`NOVA_U45_GAP`, 2026-06-27) показало:
доминирующие невыфлипнутые виды — **Ident 34% + SelfAccess 28% + Call 12% + Binary 9% + Member 8% =
~90%**. Проба-flip SelfAccess (28%) дала **2327 дивергенций** — корень ОДИН: **generic-mono / erased-stub
рассинхрон лоуэринга**. Это keystone-блокер U.4.5 (62% gap = Ident+SelfAccess) — отдельный фокус-атом.

## 0.12. P0 ЧИСТЫЙ REMEASURE + GO/NO-GO (2026-06-28) — multi-agent design+review, затем не-инвазивный замер

**Контекст.** Владелец дал sign-off на 172.3 + greenlight 172.4/5 + ultracode. Прогнан multi-agent
workflow (12 агентов: research+design+adversarial-review по 172.3/172.4/172.5 + keystone). Review
keystone-дизайна (v/agg) дал критические правки: (1) D312 — **НОВЫЙ** D-блок (не «обновление»),
писать при impl; (2) Call-GAP §0.11 ретрактнул только recv=-разбивку — реальный профиль БЫЛ НЕИЗВЕСТЕН
→ P0 = **жёсткий go/no-go гейт** ПЕРЕД утверждением «bounded»; (3) Ident **unit-variant-as-value**
(36637-36652, None/Ok/Err) — настоящий re-derive value-leaf, классифицировать отдельно; (4) parent
172.1 U.4.5 приёмка всё ещё требует **byte-identical** (:438/:672) — рассинхрон с §0.5 soundness-гейтом,
реконсилировать (§5); (5) Index poisoned side-table `array_element_types` (last-wins across folder-peers)
— P2 обязан брать element-тип из СХЕМЫ чекера, не side-table; (6) P7-grep нужен whitelist разрешённых
helper'ов (var_types/overrides), запрет reachable re-derive (find_variant_compat / compute_array_elem_type).

**P0-инструмент.** `NOVA_U45_GAP` дополнен НЕ-инвазивным синтаксическим `recv=` тегом для Call (чисто
AST-match `func.kind`/`Member.obj.kind`, БЕЗ type-inference — урок §0.11). Чистый remeasure (debug-бинарь,
корпус generics/plan11_followup/strings/basics/plan108/plan153_1/plan152_7 = **47950 fallback-exprs**):

| kind | count | примечание |
|------|-------|------------|
| **Ident** | 19975 (42%) | из них **nova_int-leaf 6259** (§1-риск, аудит «never-final»); остальное — **var_types-reads** (Vec/StringBuilder/str/char-локали = ЛЕГИТИМНЫЙ consumer-lowering, НЕ re-derive); реальный variant-as-value (NovaOpt_) ~131 — мал |
| **Call** | 9253 | rc=false **6993 (76%)**, rc=true 2260 |
| **Binary** | 5650 | ВСЕ annotated=false (чекер не аннотирует результат) |
| **Member** | 4889 | ВСЕ annotated=false (field-тип выбрасывается, §1 validation-discard) |
| If 2473 · Index 1674 · TurboFish 1336 · Path 830 · Block 455 · Match 382 · TupleLit 192 · RecordLit 140 | | хвост |

**Call rc=false (6993) по recv — ГЛАВНЫЙ go/no-go дискриминатор:**

| recv-shape | count | класс |
|------------|-------|-------|
| method.chain `x.a().b()` | **2315** (33%) | **DEEP** — receiver = Call → нужен полный receiver-type-inference (§0.9 hard) |
| free `f()` | 1708 (24%) | BOUNDED — чекер пишет callee дёшево |
| method.var `v.m()` | 1610 (23%) | BOUNDED-ish — receiver = простой Ident-локаль |
| path `M.f()` | 485 | BOUNDED |
| turbofish | 421 | BOUNDED-ish |
| method.field/other/selffield/index | 654 | DEEP/moderate |

**ВЕРДИКТ — PARTIAL-GO:** keystone удаляем **стадийно**, но P4(Call) расщепляется:
- **Bounded ~60%** (free+var+path+turbofish ≈ 4224) → чекер записывает callee (механически, в scope U.3).
- **DEEP ~40%** (method.chain 2315 + field/other ≈ 2769) → **полный receiver-type-inference** = реальный
  §0.9 precondition, **кандидат в под-план 172.1.2** (receiver-inference depth). Review-предупреждение
  подтвердилось эмпирически: Call НЕ полностью bounded.

**Следствие для критпути:** legacy-удаление НЕ блокируется целиком на 172.1.1-substrate. Стадии P1–P3,
P5 (generator-self-typing, Member/Index-канал, Binary-аннотация, Ident-consumer-lock) — bounded, идут
сразу. P4 разбит: P4a bounded-Call (сразу), **P4b chain-Call → 172.1.2 receiver-inference** (после).
P6 substrate — только для container-БЕЗ-var_types остатка. Финальное удаление legacy (P7) — после того
как ВСЕ виды либо в канале, либо var_types-consumer-state, либо доказанно-неиспользуемый-leaf.

**Staged-план (каждый шаг гейт = SOUNDNESS, §0.5: detect172 pos+neg + 0-CC-FAIL + nova test green +
baseline-DELTA vs ЧИСТЫЙ бинарь, НЕ byte-identical, НЕ git stash):**
- **P1** generator self-typing (auto-derive Debug/Display + str-interp StringBuilder-десугар эмитят C-тип
  напрямую; перенести TurboFish-member instance/worklist-регистрацию 36886-36895 в генератор, СОХРАНИВ
  before-ordering — §0 phase-correctness).
- **P2** материализовать field/element-тип в канал (U.4.4: f3_check_member пишет field.ty, не выбрасывает;
  Index element-тип из СХЕМЫ чекера, НЕ из poisoned `array_element_types`) → флип Member+Index.
- **P3** аннотировать результат Binary (+ptr-arith U.4.2) → флип Binary.
- **P4a** чекер пишет resolved_callees для bounded-Call (free/var/path/turbofish).
- **P4b** [→172.1.2] receiver-type-inference для method.chain → флип chain-Call.
- **P5** закрепить var_types/override-READ как авторитетный consumer-lowering Ident/SelfAccess; **channel
  variant-as-value Ident** (36637-36652); аудит nova_int-leaf (6259) «never-final» НЕ-инвазивно.
- **P6** (усл.) substrate для container-БЕЗ-var_types.
- **P7** финальное удаление `infer_expr_c_type_legacy` (36291-36915) + вызов 36146 + хвост 36243-36288;
  **author D312** (новый); whitelist-grep (запрет reachable re-derive helper); реконсиль 172.1 U.4.5
  приёмки byte-identical→soundness (§0.5).

### Прогресс реализации (2026-06-28)
Consumer `infer_expr_c_type` УЖЕ читает канал (36237 resolved_type_to_c) когда `resolved_types[id]`
есть — flip готов; работа = **наращивать аннотацию в чекере** (НЕ codegen-flip). Каждый шаг
soundness-gated против ЧИСТОГО main-бинаря (§7.5: pre-existing fail-set priority_queue/net_v2/
plan159-f2f3/doc-f23/map/f10 — те же без правок → не регрессии).
- **P3 Binary ✅** (c8e6b602): `infer_expr_type` не имел Binary-арма → ВСЕ 5650 в legacy. Inline-
  аннотация в f1_expr (comparison→bool byte-identical; arith→операнд-тип, сохраняет type-param T).
  **Binary GAP 5650 → 1594 (−72%)**, 0-new-FAIL. Остаток 1594 = арифметика над un-inferrable
  generic-операндами.
- **P2-partial Member ✅** (866f1a78): гейт расширен primitive → concrete non-generic declared
  value-type (record/sum/newtype/named-tuple). Low-impact: доминирующий Member-gap = **method-access**
  `obj.m` (не field) — нужен отдельный подход (глубже). 0-new-FAIL.
- **Актуальный GAP-профиль (P0-корпус):** Ident 19975 (var_types-consumer-state — НЕ deletion-blocker,
  §0.12 P5 keep), Call 9261, Member 4889 (method-access доминирует), Index 1674, Binary 1594.
- **Остаточные deletion-blockers — DEEP (→ 172.1.2 / P7):** Call receiver-inference (method.chain),
  Member method-access typing, Index element-type из схемы, Binary-arith generic-операнды. Все требуют
  полного receiver/operand type-inference в чекере (фундаментальная способность) ИЛИ codegen-time
  annotation с mono-контекстом — это и есть 172.1.2-субстрат + финальный P7.

### 172.1.2 STEP 1 ✅ ЗАЛЭНДЖЕН (2026-06-28) — receiver-inference субстрат (звучен, 0-NEW-FAIL)
`resolve_instance_method_return` (instance method-call return) + `infer_method_call_channel_type`
(канальная инференция). Метрика: Call-GAP generics 833→813 (**−20, скромно**). 4 регрессии de-risk'а
(types/alias_tagged, self_nested, plan153_5/flatten, plan154_1) найдены multi-agent-workflow'ом
(root-cause) и **устранены 3 guard'ами**:
- **DECOUPLE** (`infer_method_call_channel_type` отдельно от `infer_expr_type`) — richer inline-receiver-
  resolution вскрывает (a) false-pos `[E_EXTENSION_METHOD_NEEDS_IMPORT]` (self_nested: f3 резолвит
  chain-receiver → policy), (b) `[]T`/`Vec[T]` spelling-flip (plan154: inline-ripple → ранний Vec-mono).
- **container-return guard** (out = Array/Tuple/Named-with-generics → bail) — контейнер-mono перетряхивает
  layout → **conservative-GC root-finding segfault** (plan154 sibling test_9); + ловит nested-receiver
  мис-subst (flatten `Vec[Vec[str]]`).
- **fully-concrete guard** (out упоминает любой recv-carrier/method generic → bail) — unbound (Pair6→`Nova_B*`)
  И вакуумно-связанный T→T (into_raw→`Nova_T**`).
- **gs-gate** в аннотации (in-scope type-param → не каналить).

**КЛЮЧЕВОЙ FINDING (roadmap-affecting):** канальное покрытие CONTAINER-возвратов **гейтнуто на
conservative-GC layout-hardening** (plan154-класс: смена mono-set → layout → root-finding segfault).
Поэтому субстрат остаётся **fully-concrete-консервативным** (отсюда скромный −20). Полное покрытие
(включая контейнеры/generics — основная масса GAP) требует ЛИБО hardening GC root-finding (Plan 144),
ЛИБО codegen-time annotation с гарантированным mono-subst. DECOUPLE — помеченный tactical-measure (§0
«целевая форма vs интерим»): цель = unified inference, гейтнута на робастность inline-проверок
(extension-policy, `[]T`/Vec spelling) к richer receiver-resolution (§7.7 precondition).

**STEP 2/3/4 mapping (multi-agent, high-confidence) — для следующих заходов:**
- **STEP 2 (multi-overload):** снять single-overload bail, переиспользовать `overload_applicability` +
  unique-compatible (идентично `check_instance_overload` → тот же overload что в `resolved_callees`).
- **STEP 3 (Index):** `resolve_index_element` — `@index` scalar-read selector (Instance, non-mut,
  integer-key; Vec имеет 3 `@index`, исключить Range/write). Registry-driven. str[i]→char остаётся
  codegen-intrinsic.
- **STEP 4 (Binary):** остаток — **downstream** типизации операндов: closure-параметры (не в scope) +
  Member-field (`@field`). НЕ method-call. ⇒ закрывается **Member-field армом + closure-param injection**.

## 0.7. DETECT Call-GAP (§7.2, 2026-06-27) — scope U.1-выигрыша

Инструмент `NOVA_U45_GAP` расширен (Call: callee-shape + receiver C-type + `rc=` membership в
resolved_callees). Замер (modules/generics/plan153_1/strings/plan108):
- **rc=false (28523) — CHECKER-gap** (чекер НЕ записал callee → method_table его не имеет → U.1).
- rc=true (5916) — CODEGEN-gap (записал, но нет `fn_ret_by_span` → generic-mono method, span не в индексе).

Топ по receiver: **StringBuilder 13985 (ВСЕ rc=false)** — registry-only concrete-тип (грузится
`load_builtins`/ExternalRegistry, codegen-only; чекер его НЕ видит). Vec/контейнеры — generic
(rc=true codegen-gap ИЛИ resolved). free-fn 6056 (5885 rc=false).

**Вывод:** крупнейший Call-GAP (~14k StringBuilder + др. registry-only concrete) = ЧИСТО U.1
(дать чекеру registry-only сиги). Lifetime: `SigRegistry<'a>` (`'a`=module) — external .nv (include_str!
`'static`) парсятся в `'static` OnceLock → `'static: 'a` → merge в sig OK (без 'a-конфликта). Это
конкретный, scoped U.1-шаг (НЕ generic-mono codegen-gap, тот отдельно). Гейт: soundness (§10 — чекер
теперь резолвит StringBuilder; новые ошибки/мис-резолв ловит 0-new-FAIL + clean-DELTA).

## 0.11. КОРРЕКЦИЯ §0.7/§0.9/§0.10 (2026-06-27): измерение было замутнено инструментом

§0.10 «Call-GAP = codegen-генерируемые» — **НЕВЕРНО, отозвано.** Причина: recv=-инструмент
(`NOVA_U45_GAP`, добавил `recv = self.infer_expr_c_type(obj)` per Call-лог) **рекурсивно** инферит
receiver → на цепочках (`a.b().c()`) каскадит доп-инферы+лог-строки → СЧЁТЧИК ИНФЛИРОВАН. «StringBuilder
13985» — артефакт (инфляция + re-emission), не реальное число. Проверка: `str-interp` эмитит
`Nova_StringBuilder_method_append` C-строкой НАПРЯМУЮ (emit_c.rs:28921), НЕ AST → НЕ в
infer_expr_c_type-GAP; StringBuilder-usage реально живёт в **stdlib-source** (std/ грепом, не codegen-gen).

**Что НАДЁЖНО (не зависит от recv=):** (1) U.1-merge sound (baseline-DELTA=0); (2) receiver-inference
swap = no-op (богатый infer тоже не резолвит эти receiver'ы); (3) приоритет = удалить legacy (§0).
**Что НЕнадёжно (отозвано):** recv=-разбивка Call-GAP (StringBuilder-доминирование, codegen-gen
вывод). Реальный источник Call-GAP надо мерить ЧИСТО (unique по span, без рекурсивного recv=).

**Урок:** инструмент с рекурсивным self-вызовом в hot-инференс-пути искажает замер (§7.2 «детект» —
инструмент должен быть НЕ-инвазивным). recv= оставлен (env-gated, debug) но его агрегаты читать с
поправкой на инфляцию.

**Доп. коррекция (grep StringBuilder std/ = 3254 — тоже вводит в заблуждение):** 3136 в `.c`-файлах
(C-рантайм реализация, не Nova-source) + 299 `_experimental` (не компилится) → реальное usage в
компилируемых `.nv` = **~40 строк** (vec/core/mutate/protocols, encoding/utf16/base64, prelude/*,
runtime/string/transform). StringBuilder в source — СКРОМНО. ⇒ «StringBuilder доминирует Call-GAP» —
окончательно артефакт. **Вывод: recv=-разбивка Call-GAP ПОЛНОСТЬЮ отозвана; реальный профиль Call-GAP
неизвестен и требует чистого замера (unique-span, без рекурсии).** Надёжно лишь: landed-коммиты sound
(baseline-DELTA=0, независимо от GAP), приоритет = удалить legacy (§0).

## 0.10. (ОТОЗВАНО — см. §0.11) ОПРЕДЕЛЯЮЩИЙ finding (2026-06-27): большой Call-GAP = CODEGEN-ГЕНЕРИРУЕМЫЕ вызовы

Гипотеза «receiver-inference глубина» (§0.9) — ОПРОВЕРГНУТА. Swap `infer_arg_ty`→`infer_expr_type`
(богатый inference) = **NO-OP** (Call-GAP rc=false 28523→28522). Верификация: `modules/*.nv`
содержат StringBuilder **1 раз** (grep), но GAP = **11542 StringBuilder-вызовов в modules**. ⇒ эти
вызовы **codegen-ГЕНЕРИРУЕМЫЕ** (auto-derive `Debug`/`Display` контейнеров + str-interpolation
десугар), НЕ в исходном AST. **Чекер их НИКОГДА не видит** → не может записать `resolved_callees` →
ни U.1-sigs, ни receiver-inference (оба checker-side) их НЕ закроют (red herring).

**ОПРЕДЕЛЯЮЩЕЕ следствие для удаления legacy (U.4.5):** GAP — НЕ проблема полноты чекерского канала
(U.1/U.3). `infer_expr_c_type_legacy` зовётся codegen'ом для ДВУХ классов: (а) source-exprs (покрыты
каналом — армы+merge) и (б) **codegen-генерируемые exprs** (Debug/Display auto-derive, str-interp
StringBuilder) — их чекер не видит. Удаление legacy требует, чтобы (б) эмитили типы **напрямую при
генерации** (codegen знает их при генерации типизированного кода), не через `infer_expr_c_type`. Это
**bounded codegen-рефактор ГЕНЕРАТОРОВ** (Debug/Display/interp), а НЕ checker-completeness и НЕ
receiver-inference.

**Пересмотренный roadmap U.4.5-удаления:** (1) source-exprs → канал ✅ (армы+Call-channel+merge, в
работе); (2) **codegen-генераторы (auto-derive Debug/Display + str-interp) → эмитят C-типы напрямую**,
не legacy-infer — bounded; (3) тогда legacy удаляем. Спина U.1/U.2 (единый реестр) — для (1)
source-резолва, НЕ для (2). U.4.5-удаление НЕ гейтнуто на полный U.1 — гейтнуто на (2) генераторы.

## 0.9. U.1 type+method merge ✅ ЗАЛЭНДЖЕН (2026-06-27) + СТРАТЕГ-finding

type+method merge РЕАЛИЗОВАН (external_registry::builtin_sig_modules 'static OnceLock парс
StringBuilder/WriteBuffer/ReadBuffer; SigRegistry::merge_module_fns; TypeCheckCtx::build merge
Item::Type в self.types; check_module merge sig). **§7.2 DETECT: blast-radius = 0** (PASS 94 FAIL 1,
baseline-DELTA=0 vs чистый бинарь — §10-hazard НЕ материализовался, merge ЗВУЧЕН). Чекер теперь
знает StringBuilder/buffers как ТИПЫ + их method-сиги (§3 «один реестр», целевая форма).

**СТРАТЕГ-finding (меняет roadmap удаления):** GAP StringBuilder 13985→13582 — **лишь ~400 (~3%)**.
97% StringBuilder-вызовов ОСТАЛИСЬ rc=false ДАЖЕ с типом+сигами в чекере. Корень: `check_instance_
overload` берёт receiver через `infer_arg_ty(obj)` — резолвит ТОЛЬКО let-bound простые var
(`mut sb = StringBuilder.new()`). 97% receiver'ов — **method-chains** (`sb.append(x).append(y)`),
**self-fields** (`@buf.append` в типе с StringBuilder-полем), сложные exprs → `infer_arg_ty` их НЕ
резолвит. ⇒ **большой Call-GAP блокирован ГЛУБИНОЙ receiver-type-inference, НЕ отсутствием sigs.**
U.1-sigs (этот merge) = ~3% + фундамент; **настоящий рычаг = полный receiver-inference** (chains/
fields/complex) в чекере — фундаментальная checker-способность, гораздо глубже. (Альтернатива: эти
97% не баги — legacy/codegen их резолвит через ExternalRegistry+type-info; они блокеры ТОЛЬКО для
DELETION U.4.5, не корректности.)

**Вывод для roadmap U.4.5-удаления:** не «дать чекеру sigs» (сделано, ~3%), а «полный receiver-type-
inference» (chains/fields) ИЛИ codegen-side канал для temp-receiver вызовов. Это пересматривает спину
(§0.6): корень удаления — receiver-inference depth, не единый-реестр-sigs. Merge — фундамент под это.

## 0.8. U.1 method-merge ПРОБА (2026-06-27) — недостаточна, type-merge = high-risk

Попытка: `external_registry::builtin_sig_modules()` ('static OnceLock парс string_builder/write_buffer/
read_buffer.nv) + `SigRegistry::merge_module_fns` (merge method-сигов, skip-known-type guard) +
wire в check_module ПОСЛЕ Bound/Cap, ПЕРЕД TypeCheckCtx. Собралось, sound (94/1), но **GAP
StringBuilder НЕ сдвинулся (13986 rc=false)** → merge методов в `method_table` НЕ помог.

**Корень глубже (локализован):** `check_instance_overload` (types/mod.rs:7700) берёт receiver через
`BoundCtx::infer_arg_ty(obj)` → для `sb.append()` нужен `sb: StringBuilder`, а `sb` инферится из
`mut sb = StringBuilder.new()` → `infer_expr_type(StringBuilder.new())` требует StringBuilder в
**`self.types`** (TypeCheckCtx), не только в `method_table`. ⇒ цепочка: **type-decl в self.types +
static-ctor резолв + receiver-inference**, не просто method-сиги.

**Почему НЕ session-end push:** type-merge StringBuilder в self.types = чекер НАЧНЁТ полностью
type-check'ить StringBuilder-usage (поля, арги, статика) — **blast-radius 13985 вызовов** → массовые
потенциальные новые ошибки при ЛЮБОМ checker↔codegen mismatch (§10-hazard в обратную сторону:
добавление знания вскрывает рассинхрон). Требует §7 **detect-mode** (прогон с type-merge в
non-fatal-режиме, замер новых ошибок, калибровка) ПЕРЕД enable. Это фокус-многосессионная де-риск-
работа, не одна правка. Откатил no-effect method-merge (чистый стейт).

**Вывод для следующей попытки U.1 (де-рискнуто):** (1) merge external TYPE-decls в `self.types`
(не только методы); (2) detect-mode: посчитать новые E_* на корпусе ПЕРЕД enable; (3) калибровать
(вероятно нужен per-type opt-in, начать с 1 типа); (4) только потом — Call-channel выигрыш. Метод-
merge инфраструктура (builtin_sig_modules + merge_module_fns) — переиспользуема, но одна не даёт эффекта.

## 0.6. КРИТПУТЬ к удалению legacy (определён 2026-06-27 — спина 172.1)

Per-arm аннотация (SelfAccess/Ident/Index/Member/Block/control-flow/Binary/Unary — все ✅ залэнджены,
baseline-DELTA=0) **упёрлась в убывающую отдачу**: GAP остаётся **Ident 47k** (имена fn/type/module —
нет value-типа, НЕ аннотируемы), **Call 28k**, **Binary 21k** (сложные операнды), **Member 18k**.
Полное удаление legacy НЕ закрывается аннотацией листьев. Реальная цепочка зависимостей (спина):

1. **U.1/U.2 — ЕДИНЫЙ реестр сигнатур (корень).** Сейчас чекер (`sig.method_table`, TypeRef-уровень)
   и codegen (`ExternalRegistry` с `return_c_type`/`c_name`/span, построен из `.nv`) — **ДВА
   параллельных реестра** (§3-анти-паттерн). Чекер НЕ резолвит primitive/builtin-method callees
   (`is_primitive_recv_name → return`, types/mod.rs:7708; «external overloads не в method_table») →
   28k Call не попадают в `resolved_callees`. **Фикс:** один реестр (с FnDecl.span) питает ОБА слоя.
2. **U.3.2/U.3.3 — резолв вызовов в чекере** (свободные fn + instance/static + перегрузки): снять
   gate 7708, чекер пишет `resolved_callees` для ВСЕХ Call (через единый реестр). Закрывает Call-GAP.
3. **U.4.5 — Call-канал полон + удаление legacy.** Когда `resolved_callees` покрывает все Call →
   `fn_ret_by_span` канал полон → legacy для Call не нужен. Non-value-Ident (47k) — отдельный
   вопрос (имя fn/type не несёт value-тип; codegen-потребление их `infer_expr_c_type` надо
   аудитить — возможно результат не используется). typedef-side-effects → §0-фаза-корректность
   (forward-decl рано/define поздно), не lazy-в-inference.
4. **U.6.1 — `type_ref_to_c` ретайрится** → единый `resolved_type_to_c` для binding И literal
   (снимает dual-lowering #23-класса by construction).

**Вывод:** fastest-whole = спина **U.1/U.2 (единый реестр) → U.3 → U.4.5 → U.6.1**, НЕ добивание
листьев. Корень — единый реестр (U.2.2 build включая external-сиги со spans + U.2.3 consume в
чекере). Это фокус-многосессионная работа с §10-осторожностью (не удалять registry-only снабжение
до звучности инлайн-пути). Independent-атомы (U.1.4 intrinsic-table, U.6.4, U.7.2/3, U.8) —
параллельно, не гейтнуты на спину.

## 0.5. Состояние 2026-06-27 (ВЕРИФИЦИРОВАНО по коду, не по докам)

### Разбор бага #23 (plan-169.2, пустой `[]fn`→SEGV) — это «ДВА ОКНА ПРАВДЫ», не просто nova_int

Владелец указал docs/plan-169.2-compiler-fixes.md (баги легаси). #23 разобран до корня (2026-06-27):
`mut arr []fn()->int = []` даёт в .c **РАССОГЛАСОВАНИЕ ЛОУЭРИНГА**:
- **binding** `arr` → `NovaArray_void_p*` (через `type_ref_to_c(decl.ty)` — declared-тип).
- **literal** `[]` → `Nova_Vec____nova_int* _nv_tmp = ..._static_new()` (через codegen element-derive
  пустого литерала → fallback `nova_int`, + другой container-KIND: Vec, не NovaArray).
⇒ `nova_array_push_void_p(arr, closure)` на физическом `Nova_Vec____nova_int` → layout-mismatch на
realloc → SEGV. Это **§0/D315 «два окна правды»**: `[]fn` лоуэрится ДВУМЯ путями (`type_ref_to_c`
binding vs literal-emit element-derive). **Point-fix невозможен заплаткой** (пробовал hint void_p
перед value-emit — container всё равно Vec____nova_int из другого emit-пути; KIND-mismatch Vec↔Array
не снимается хинтом element-типа). Подтверждает doc «point-fix продублирует U.4». **Настоящий фикс =
единый лоуэринг `resolved_type_to_c` для ОБОИХ (binding И literal)** — U.4.5 (literal через канал) +
U.6.1 (`type_ref_to_c` ретайрится). Т.е. #23 — частный случай keystone, не отдельный баг.

Док-статусы устарели — **код опережает**. Реальное состояние `infer_expr_c_type` (consumer):

**Армы УЖЕ флипнуты на канал** (возвращают channel, не legacy re-derive):
- **Call** — `resolved_callees[id]`→`fn_ret_by_span[span]` (U.4.5a, было сделано ранее; Explore-агент
  ошибочно счёл U.4.3 «многонедельным неначатым» — он СДЕЛАН для resolved-callee-вызовов).
- **leaf/number_exprs** (literals/arith/As/ctor/bool-ops) — byte-identical (U.4.1/4.2).
- **RecordLit / TupleLit** — срезы (byte-identical).
- **SelfAccess** ✅ `985035ff` (var_types-gated), **Ident** ✅ `5c73f22e` (var_types-gated, non-primitive),
  **Index/Member/Block/If/IfLet/Match/Path** ✅ (структурные+control-flow, аннотированы).

**КЛЮЧЕВОЙ ИНСАЙТ (исправляет прежнюю гипотезу):** gap #2 (bare-`@`→erased-схема→nova_int element,
CC-FAIL lru) был **ТОЛЬКО в SelfAccess-flip-каскаде** (flip типизировал `@` → downstream field/index
брали erased-схему). **Прямая аннотация Index/Member НЕ триггерит gap #2** — канал берёт element/field-тип
ПРЯМО из чекера (`infer_expr_type`), корректно. Доказано: Index+Member+control-flow = **0-new-FAIL/34 дир**
(baseline-DELTA=0). Значит структурные армы НЕ блокированы subst-reliability.

**ДВА оставшихся блокера удаления `infer_expr_c_type_legacy`:**
1. **Un-annotated surface** (GAP-замер, fallback-в-legacy по KIND): Call-без-`resolved_callees` (~30k),
   Binary (~23k), non-value Ident (~53k — имена fn/type/module, у них нет value-типа → `infer_expr_type`
   None), Member-без-field-type (~20k), TurboFish/Closure/ArrayLit/MapLit. Каждый нужно либо покрыть
   каналом, либо доказать, что legacy для него = надёжный codegen-state (как Ident/SelfAccess через
   var_types), не re-derive.
2. **Side-effect side-channel (typedef-emit + mono-registration в `legacy`).** `legacy` крутится ВСЕГДА
   (emit_c.rs:36135) РАДИ side-effects. Два теста 2026-06-27 + ПЕРЕОСМЫСЛЕНИЕ (владелец 2026-06-27:
   «в легаси были ошибки — не делай legacy эталоном»):
   - **NOLEG-тест** (`NOVA_U45_NOLEG`: пропуск legacy для resolved_types-аннотированных не-gated армов):
     FAIL-set ИДЕНТИЧЕН (121/1, **компилируется И тесты проходят**), .c-diff = 31 файл. **Изначально
     счёл это «side-effects влияют → нельзя удалять». ОШИБКА интерпретации:** раз channel-only вывод
     КОМПИЛИРУЕТСЯ и поведение корректно (121/1), то .c-различия — это НЕ дропнутые нужные typedef
     (иначе CC-FAIL), а ИЗБЫТОЧНЫЕ typedef, которые legacy эмитил, а channel-only корректно опускает
     (или benign reorder). ⇒ channel-only side-effects **ЗВУЧНЫ** (sound).
   - **Order-тест** (lazy-legacy после resolved_type_to_c): .c-diff = 28/28, но тоже компилируется+проходит
     → benign reorder, тоже sound.
   **ВЫВОД (исправленный): byte-identical — НЕВЕРНЫЙ гейт.** Он (а) сохранял бы баги легаси, (б) over-
   reject'ил ЗВУЧНЫЕ channel-выводы. Правильный гейт (= [feedback-nova-tests-not-correctness-gate],
   conventions §1): **SOUNDNESS — detect172 pos+neg + 0 CC-FAIL + тесты проходят**, НЕ byte-identical.
   Этот гейт ЛОВИТ настоящие регрессии (gap #2 lru дал CC-FAIL = unsound → отвергнут корректно) и
   ПРИНИМАЕТ benign-различия (NOLEG/order компилируются+проходят). ⇒ **удаление legacy ФИЗИБЕЛЬНО**
   под soundness-гейтом; «byte-identical для всех армов» — отброшено как ошибочная установка.

⇒ U.4.5-удаление = многосессионное: покрыть un-annotated surface + relocate side-effects. Армы-покрытие
(этот раунд) — необходимая подготовка, sound (baseline-DELTA=0), но НЕ достаточно для удаления.

## 1. Корень (локализован 2026-06-27)

**U.4.8-тождество `type_ref_to_c(T) ≡ resolved_type_to_c(from_type_ref(T))` НАРУШЕНО для generic-
контейнеров в erased-template-теле:**
- **`erased_type_ref_c` (emit_c.rs:~12089) + `receiver_c_type` (:11803)** — для generic с type-param
  args дают **BARE** `Nova_Deque*` (erased-stub имя без мэнглинга). Это то, что codegen реально эмитит
  в erased generic-method-теле (`emit_generic_fn_erased`, recv_c :12273 → var_types["nova_self"]).
- **`resolved_named_to_c` (:2227)** — мэнглит ВСЕ args, включая erased type-param → **MANGLED**
  `Nova_Deque____Nova_T_p*`.
- ⇒ два naming-scheme одного erased-контейнера. resolved_type_to_c(scope["@"]=Deque[T]) даёт mangled,
  а erased-тело объявило receiver как bare → flip SelfAccess/Ident рушится.

**Доп. внутренняя несогласованность legacy** (которую U.4.5 и устраняет): `erased_type_ref_c` даёт
bare для Box[T] (param), а `infer_expr_c_type` RecordLit-арм (:36426) даёт mangled для того же Box[T] —
т.е. сам legacy непоследователен; цель — ОДНА форма из `resolved_type_to_c`.

## 2. Проба (A) 2026-06-27 — частичный результат

Зеркало `erased_type_ref_c` в `resolved_named_to_c` через **прокси** `is_generic_stub_c(arg)` (erased
type-param лоуэрится в stub C-тип `Nova_T*`; concrete `nova_int` — нет; контекст берётся из
`current_type_subst`: erased→stub→bare, mono→concrete→mangled):
- ✅ **container-erasure класс почти ушёл** (`Nova_Deque____Nova_T_p*` vs `Nova_Deque*`: 233 → ~4).
- ✅ byte-identical для текущих consumer'ов (RecordLit/Tuple detect = 0 div).
- ✅ FAIL'ы SelfAccess-пробы 5 → 2.
- ⚠️ НЕ чисто: остаток `Nova_Lru*` ⟷ `Nova_Lru____…*` REVERSED (3+3) — **прокси `is_generic_stub_c`
  ≠ настоящий «erased-body context»** (в некоторых mono-контекстах arg лоуэрится в stub ошибочно).
- ⚠️ Доминирующий остаток SelfAccess (~2100) — ОТДЕЛЬНЫЙ класс `ir=<sized-int> legacy=nova_int`
  (legacy Cat-B13 nova_int-fallback :36729) = §1-ФИКСЫ, не container-substrate.

Проба откатана (интерим-прокси, §0 «не патчь дубль») — чистый фикс ниже.

**ВТОРАЯ переплетённая проблема (выявлена reversed-кейсами `Nova_Lru*` ⟷ `Nova_Lru____nova_str__nova_int*`):
доступность `current_type_subst` в точке потребления канала.** В некоторых MONO-телах на момент вызова
`resolved_type_to_c` (consume канала) `current_type_subst` НЕ populated → args лоуэрятся в stub
(`Nova_K_p`), тогда как legacy на том же сайте имеет concrete (`nova_str`) через иной путь. ⇒ даже
mangled-форма выходит неверной (stub-args вместо concrete). Значит substrate = НЕ только bare/mangled-
выбор, но и **гарантия populated mono-subst к моменту consume** (фаза-корректность §0: факт=тип-аргументы
готов до потребления). in_erased_body-флаг решает bare/mangled; subst-availability — отдельная под-задача.

**Проба (B) 2026-06-27 — `in_erased_body`-флаг в 2 эмиттерах НЕДОСТАТОЧЕН.** Добавил флаг
`CEmitter.in_erased_body` (save/restore вокруг body-emit в `emit_generic_method_erased`:12379 +
`emit_generic_fn_erased`:16536) + gate в `resolved_named_to_c` (`in_erased_body && is_generic_stub_c
→ bare`). Результат: 2163 дивергенции (≈прокси), **container-класс НЕ убран чисто** —
`Nova_Set____Nova_T_p*` vs `Nova_Set*` (12), `Nova_Stack…` (5) остались + 5 FAIL. Вывод: SelfAccess
для Set/Stack потребляется при `in_erased_body=FALSE` → **есть ДРУГИЕ erased-emit/consume контексты
помимо 2 эмиттеров** (или consume в иное время, чем body-emit этих функций). ⇒ substrate
**многоконтекстный**; чистый фикс требует ЛИБО (i) полного покрытия всех контекстов, где
`resolved_type_to_c` потребляется для erased-body-выражений (энумерация), ЛИБО (ii) более
фундаментального подхода — напр. канал несёт уже-erasure-разрешённую форму, либо `resolved_type_to_c`
выводит erasure из СОСТОЯНИЯ типа (а не из emit-контекста). Обе пробы (A прокси / B флаг-2-эмиттера)
откатаны (§0 «не интерим-патч»). Это и есть focused-work суб-плана: §7 — энумерировать consume-контексты
(NOVA_U45_RLCHECK по kind=self + трассировка in_erased_body) ПЕРЕД фиксом.

**УНИФИЦИРУЮЩИЙ КОРЕНЬ обеих проб (главный инсайт 2026-06-27):** и прокси (`is_generic_stub_c`), и
флаг (`in_erased_body`) падают по ОДНОЙ причине — **`current_type_subst` не синхронен с emit-контекстом**:
- erased-тело с CONCRETE-arg (`Nova_Deque____nova_int*` residual): subst populated там, где контекст
  erased → arg concrete → proxy не сработал, но legacy = bare;
- mono-тело без populated subst (`Nova_Lru` reversed): arg лоуэрится в stub → proxy сработал ложно,
  legacy = mangled-mono.
⇒ инвариант «stub-arg ⟺ erased-контекст», на который опирался прокси, **НАРУШЕН рассинхроном
subst↔контекст**. ⇒ **целевой фикс (§0, фаза-корректность):** привести `current_type_subst` к
инварианту «populated РОВНО в mono-эмиссии, пусто в erased» (фаза-корректность §0 — тип-аргументы
готовы/отсутствуют согласно контексту), ТОГДА `resolved_type_to_c` выводит bare/mangled из СОСТОЯНИЯ
типа (концерт→mangled, stub→bare) консистентно — без emit-context-флага и без энумерации. Это и есть
«resolved_type_to_c выводит erasure из состояния типа» (вариант ii). Под-задача 172.1.1: аудит всех
set/clear `current_type_subst` (emit_monomorphized_fn + erased-эмиттеры) → привести к инварианту →
proxy-лоуэринг становится корректным → flip SelfAccess/Ident.

**Проба (C) 2026-06-27 — subst-clear (в 2 erased-эмиттерах) + proxy: container-класс 233→12.** Добавил
`std::mem::take(&mut current_type_subst)` вокруг body-emit в `emit_generic_method_erased`:12378 +
`emit_generic_fn_erased`:16522 (erased-тело видит пусто→stub) + proxy (`is_generic_stub_c→bare`).
Результат: **container-erasure класс 233→12 residual** (subst-clear решает БОЛЬШИНСТВО!). БЕЗ
SelfAccess-flip — **byte-identical** (RecordLit/Tuple detect=0, 0 новых FAIL, priority_queue
pre-existing). С flip: 12 container-residual + 2100 §1-fix-класс. Откатано (инфра без flip
преждевременна, §0). **12 residual = ~3 erased-emit контекста ВНЕ 2 эмиттеров** (подтверждает пробу B:
есть ещё erased-emit пути) — их найти (трассировкой) и тоже subst-clear'ить.

**ЧИСТЫЙ ПУТЬ 172.1.1 (после 3 проб):**
1. **subst-clear во ВСЕХ erased-emit контекстах** (2 известных + ~3 для 12 residual — энумерировать
   трассировкой in_erased/subst-state) → container-класс → 0.
2. **proxy** `is_generic_stub_c→bare` в `resolved_named_to_c` (корректен при инварианте subst↔context).
3. **flip SelfAccess + Ident** на канал.
4. **§1-fix класс** (`<sized-int> vs nova_int`, ~2100, legacy Cat-B13 `var_types[nova_self]` fallback):
   это НЕ container-substrate — channel корректнее legacy → принять как U.4.4b-style behavior-change
   (verify: soundness-аргумент channel-корректности + 0-new-FAIL + detect172), НЕ требовать ==legacy.
   Бар: container-класс byte-identical (full-.c-diff) + §1-fix задокументирован.

**Проба (D) 2026-06-27 — cohesive (subst-clear ×2 + proxy + flip): ломает `modules/lru`.** Полная сборка:
container 233→**12 residual** (4 Deque + 6 Lru-reversed + 2 PQ) + §1-fix класс ~2134 + **NEW FAIL
`modules/lru`** (`lru.c: passing 'nova_int' to parameter`). Корень lru-слома = **mono-side gap**: в
mono-теле lru `current_type_subst` НЕ populated к моменту consume SelfAccess → proxy ложно даёт bare
`Nova_Lru*` (вместо mono `Nova_Lru____nova_str__nova_int*`) → type mismatch. subst-clear чинит ТОЛЬКО
erased-сторону. **Mono-сторона (гарантировать populated subst к consume) — отдельный реальный блокер,
ломающий компиляцию.** Откатано (регрессия, §7). **infra-слой (subst-clear+proxy БЕЗ flip) — sound**
(0 RecordLit-div + 0-new-FAIL над 16 дир), но без flip бесполезен.

**ИТОГ де-риска (4 пробы A/B/C/D) — точные оставшиеся блокеры:**
1. **mono-side subst-population** (ломает lru) — найти, где mono-emit НЕ ставит `current_type_subst`
   к consume SelfAccess generic-receiver-метода, и поставить (фаза-корректность §0). Это ГЛАВНЫЙ блокер.
2. **§1-fix класс** (~2134 `<sized-int> vs nova_int`, Cat-B13) — подтвердить soundness (channel=верный
   mono-receiver) + принять как U.4.4b behavior-change (detect172 + 0-new-FAIL).
3. ~3 extra erased-контекста (12 container-residual) + subst-clear там.
Это careful focused-реализация (не probe-churn): сперва (1) — он и ломает компиляцию, и разблокирует proxy.

**Mono-side gap #1 ✅ ЗАЛЭНДЖЕН `bab296cf` (2026-06-27)** — sound, 0-new-FAIL/17 дир (modules/lru,
plan153*, plan140, generics, …). Первый конкретный substrate-код. Детали ниже.

**SelfAccess flip ✅ ЗАЛЭНДЖЕН `985035ff` (2026-06-27, var_types-gated)** — второй substrate-код.
**КЛЮЧЕВОЙ ИНСАЙТ (после 6 проб):** `var_types["nova_self"]` — НАДЁЖНОЕ codegen-state (тип
receiver-параметра, выставлен при эмиссии сигнатуры), **НЕ re-derive под-выражения**. Consumer
использует channel для SelfAccess ТОЛЬКО когда var_types["nova_self"] ОТСУТСТВУЕТ (Cat-B13:
closures/generic-bodies — где legacy ложно nova_int, §1-fix); иначе var_types (надёжно, incl. mono).
Это ОБХОДИТ subst-timing (gap #2: mono SelfAccess→var_types, не channel→resolved_type_to_c). Verify:
**baseline-DELTA=0** (чистый бинарь и flip → ИДЕНТИЧНЫЙ FAIL-set 131/10 над 34 дир; 10 pre-existing).
**Это ЦЕЛЕВАЯ форма, не stopgap:** var_types-чтение для Ident/SelfAccess — легитимный consumer-лоуэринг
(не re-derive), переносимый в consumer по-армно → путь к удалению legacy (legacy для Ident/SelfAccess
в ОСНОВНОМ корректен через var_types; §1-fix subset мал). Следствие: **gap #2 (subst-timing) НЕ
блокирует SelfAccess** (var_types-gate его обходит); subst-reliability нужна лишь для полного
channel-flip generic-container типов, не для var_types-несомых.

**Mono-side gap #1 ЛОКАЛИЗОВАН (2026-06-27):** `try_emit_default_body_candidates` (emit_c.rs:6061 —
синтез default-protocol-методов для типа T) ставит `current_type_subst["Self"]=t_c_ty` (:6085), но
**НЕ generic-params** (K/V). Default-метод на generic-типе (Lru[K,V]) → K/V отсутствуют в subst →
`resolved_type_to_c(K)`→stub→proxy bare→type-mismatch (lru-слом). **Фикс:** распарсить mangled
`t_c_ty` (`Nova_Lru____nova_str__nova_int*` → [nova_str,nova_int]) + zip с template-generics [K,V] →
populate `current_type_subst[K]=nova_str` и т.д. (как `emit_monomorphized_method`:15082 делает из
`type_subst`). NB: могут быть ДРУГИЕ mono-side gaps (этот — первый найденный; энумерировать остальные
трассировкой proxy-bare в mono-контексте). emit_monomorphized_method (:15082) subst СТАВИТ — там ок.

**Проба (E) 2026-06-27 — gap #1 (залэнджен) НЕ чинит lru.** Re-cohesive (gap#1 + subst-clear + proxy +
flip) на gap#1-базе: lru ВСЁ ЕЩЁ CC-FAIL + reversed `Nova_Lru*` (3+3) остался. ⇒ lru-reversed из
**ДРУГОГО mono-пути (gap #2)**, НЕ `try_emit_default_body_candidates`. **Substrate = МНОЖЕСТВО mono-side
subst-gaps** — gap #1 лишь один (default-method-synthesis). Откатано (flip ломает lru через gap #2).
**Остаток:** энумерировать ВСЕ mono-subst-gaps (трассировкой: где SelfAccess-consume даёт proxy-bare в
mono-контексте = var_types[nova_self]=mono, current_type_subst пуст) → gap #2 (lru) + возможно ещё →
пофиксить каждый (как gap #1) → ТОГДА flip + §1-fix verification. Multi-gap focused-эффорт.

## 3. Подход (целевая форма, §0)

1. **Erased-context-aware `resolved_type_to_c`** (не прокси): ввести в CEmitter флаг/состояние
   «in erased generic body» (ставится в `emit_generic_fn_erased`), и в `resolved_named_to_c` для
   generic-контейнера с type-param args лоуэрить в **bare erased-stub** под этим контекстом, в
   **mangled mono** иначе. Это делает `resolved_type_to_c` способным **субсумировать**
   `erased_type_ref_c`/`receiver_c_type` (§0 — один лоуэринг; шаг к ретайру `type_ref_to_c`).
2. **Flip SelfAccess + Ident** (62% gap) на канал — теперь byte-identical для container-типов.
3. **Класс `nova_int`-fallback (Cat-B13)** — отдельно: это §1-ФИКСЫ (legacy выдал `nova_int`, канал
   корректен), принимаются как documented behavior-change (прецедент U.4.4b), НЕ требуют ==legacy;
   верификация = soundness-аргумент + 0-new-FAIL + detect172, а не байт-идентичность.

## 4. Приёмка

- `resolved_type_to_c` byte-identical с codegen-выводом для generic-контейнеров в erased И mono телах
  (detect `NOVA_U45_RLCHECK` = 0 для container-класса; full-.c-diff vs clean для не-§1-fix набора = пусто).
- SelfAccess + Ident зафлиплены на канал; §1-fix набор (nova_int→корректный) задокументирован, 0 нов. FAIL.
- Разблокирует U.4.5 (после Call/Binary/Member — отдельные срезы): удаление `infer_expr_c_type_legacy`.

## 5. Deps / Risk

- **Deps:** U.4.4 (аннотация field/element ✅), U.4.6 (`resolved_type_to_c` state-aware ✅).
- **Risk:** High — `resolved_named_to_c`/`erased_type_ref_c` — ядро type→C, потребляется повсеместно.
  Де-риск по §7: detect-режим → полный регресс → **full emitted-.c diff vs clean = пусто** (бар U.4.5);
  per-arm, asserts ON до удаления.
- **Инструменты:** `NOVA_U45_GAP` (re-measure gap), `NOVA_U45_RLCHECK` (detect divergence).
