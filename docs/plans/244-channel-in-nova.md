# План 244 — `Channel` НА NOVA (не обёртка над C), снос ad-hoc веток кодогенератора

> **Статус:** ⏸ ЖДЁТ АПРУВА ВЛАДЕЛЬЦА — не начинать без его слова.
> Приоритет — К2 плана [221](221-release-v0-1.md). Решение владельца
> 2026-08-02: делать ДО тега, после окна рантайм-гонок Vela.

> **Редакция 2 (2026-08-03).** Первая редакция была НЕВЕРНОЙ: она описывала
> тонкую `.nv`-обёртку над теми же сишными функциями — вся логика канала
> оставалась в C. Владелец: «когда я говорил реализовать на Nova, это
> означает, что нет `extern "C" fn chan_new` и др.» Эта редакция — настоящая
> реализация на Nova.

## Зачем

`Channel`/`ChanWriter`/`ChanReader` сегодня — **compiler-intrinsic без единого
`.nv`-объявления**: логика в C-рантайме, а сборка пары `(tx, rx)` держится на
**трёх написанных вручную ветках кодогенератора**
(`[M-59.1-channel-new-cleanup]`). Нарушение нормы «одна дверь» и конвенции
«компилятор берёт типы и функции из `.nv` по максимуму, в Rust — только
непортируемое».

## Ключевое наблюдение: всё нужное в Nova уже есть

| Нужно каналу | Есть в Nova | Где |
|---|---|---|
| взаимное исключение | `Mutex` (`#share`) | `std/src/runtime/sync.nv` |
| ожидание условия | `Condvar` — `@wait(mut m Mutex)`, `@wait_for`, `@wait_until`, `@notify_one`, `@notify_all` | там же |
| атомарный розыгрыш | `AtomicInt` + `@compare_exchange` | там же |
| буфер элементов | `[]T` — обычный `Vec[T]` | prelude |

`Mutex`/`Condvar`/`Atomic*` остаются `extern "nova"` — и это ПРАВИЛЬНО: они
часть планировщика, парковка файбера непортируема по определению. Канал же
поверх них — обычная структура данных, ей место в `.nv`.

## Что ИСЧЕЗАЕТ вместе с обёрточным подходом

Первая редакция тащила цепочку вопросов — все снимаются реализацией на Nova,
потому что буфер становится обычным `[]T`:

- `elem_size` / `size_of[T]()` при создании — **не нужно**;
- выравнивание области данных — **не вопрос** (`Vec[T]` сам о себе заботится);
- GC-безопасность байтового буфера — **не вопрос** (`Vec[T]` под сборщиком как
  любой другой);
- `mut slot uninit T` для приёма — **не нужно**, `recv` читает элемент буфера;
- `*mut u8`, приведения, `unsafe` — **ни одного**;
- типизированные FFI-ручки (№288) — здесь неприменимо, канал больше не ходит
  через FFI. Сам №288 остаётся, план [245](245-ffi-typed-handles.md).

## Представление

```nova
module std.concurrency.channel

/// Разделяемое состояние канала. #share — внутренняя синхронизация (D415):
/// тип по назначению живёт сразу в нескольких файберах.
#share
type Chan[T] {
    lock      Mutex
    not_empty Condvar
    not_full  Condvar
    buf       []T          // кольцевой буфер ёмкости cap
    head      int          // индекс головы
    count     int          // сколько элементов внутри
    closed    bool
    tx_refs   AtomicInt    // счётчик писателей: канал закрывается на нуле
    waiters   []SelectWaiter   // регистрации select-ветвей, см. ниже
}

export type ChanWriter[T] value { priv ch Chan[T] }
export type ChanReader[T] value { priv ch Chan[T] }
```

Разделение типов даёт безопасность следствием системы типов, а не особым
случаем в компиляторе: у писателя физически нет метода `recv`, у читателя —
`send`.

## Создание — обычная функция, кортеж собирается на месте

```nova
#stable(since = "0.1")
export fn Channel[T].new(cap int) -> (ChanWriter[T], ChanReader[T]) {
    ro ch = Chan[T] {
        lock: Mutex.new(),
        not_empty: Condvar.new(),
        not_full: Condvar.new(),
        buf: []T.with_capacity(cap),
        head: 0, count: 0, closed: false,
        tx_refs: AtomicInt.new(1),
        waiters: []SelectWaiter.new(),
    }
    (ChanWriter[T] { ch }, ChanReader[T] { ch })
}
```

Ни кортежного FFI, ни плана [115](115-ptr-type-and-tuple-ffi.md) — зависимость
снята полностью.

## Отправка и приём — учебный ограниченный буфер

```nova
export fn ChanWriter[T] @send(consume v T) -> bool {   // consume: владение уходит (D131/№144)
    mut g = @ch.lock.lock()
    while @ch.count == @ch.buf.cap() && !@ch.closed {
        @ch.not_full.wait(@ch.lock)
    }
    if @ch.closed { return false }
    @ch.buf_put(@ch.head + @ch.count, v)
    @ch.count += 1
    @ch.not_empty.notify_one()
    @ch.wake_select_waiters()        // см. раздел select
    true
}

export fn ChanReader[T] @recv() -> Option[T] {
    mut g = @ch.lock.lock()
    while @ch.count == 0 && !@ch.closed {
        @ch.not_empty.wait(@ch.lock)
    }
    if @ch.count == 0 { return None }     // закрыт и пуст
    ro v = @ch.buf_take(@ch.head)
    @ch.head = (@ch.head + 1) % @ch.buf.cap()
    @ch.count -= 1
    @ch.not_full.notify_one()
    @ch.wake_select_waiters()
    Some(v)
}
```

`try_send`/`try_recv` — те же тела без `wait`: не готово → сразу `false`/`None`.
`close` у писателя уменьшает `tx_refs` и на нуле ставит `closed` плюс
`notify_all` на обоих условиях; `close` у читателя ставит `closed` и будит
ждущих отправителей.

**Кольцевое обращение** (`buf_put`/`buf_take` выше — рабочее имя) в Ф.1
выбирается между приватным помощником и обычной индексацией с `% cap()` на
месте; второе проще и не расширяет `Vec`. Обосновать в отчёте.

## Полный список деклараций

Поверхность сохраняется в точности той, что описана в `docs/guide/channels.md`
сегодня — план меняет реализацию, а не API.

```nova
module std.concurrency.channel

// ── создание ─────────────────────────────────────────────────────────────
#stable(since = "0.1")
export fn Channel[T].new(cap int) -> (ChanWriter[T], ChanReader[T])

// ── сторона записи ───────────────────────────────────────────────────────
/// Блокирующая отправка. `true` — принято; `false` — канал закрыт (не panic).
/// `consume` — владение уходит в канал (D131, №144): использование `v`
/// после отправки отклоняется линейной проверкой.
#stable(since = "0.1")
export fn ChanWriter[T] @send(consume v T) -> bool

/// Неблокирующая. `false` — буфер полон либо канал закрыт.
#stable(since = "0.1")
export fn ChanWriter[T] @try_send(consume v T) -> bool

/// Закрывает ЭТУ возможность записи. Идемпотентно. При нескольких писателях
/// (`share`) — по счётчику: канал закрывается, когда закрылся последний.
#stable(since = "0.1")
export fn ChanWriter[T] @close() -> ()

#stable(since = "0.1")
export fn ChanWriter[T] @is_closed() -> bool

/// Ещё один писатель поверх того же буфера; счётчик писателей +1.
#stable(since = "0.1")
export fn ChanWriter[T] @share() -> ChanWriter[T]

// ── сторона чтения ───────────────────────────────────────────────────────
/// Блокирующий приём. `None` — канал закрыт И буфер пуст.
#stable(since = "0.1")
export fn ChanReader[T] @recv() -> Option[T]

/// Неблокирующий. `None` — буфер пуст (это НЕ значит «закрыт», см. is_closed).
#stable(since = "0.1")
export fn ChanReader[T] @try_recv() -> Option[T]

#stable(since = "0.1")
export fn ChanReader[T] @len() -> int

#stable(since = "0.1")
export fn ChanReader[T] @capacity() -> int

#stable(since = "0.1")
export fn ChanReader[T] @is_closed() -> bool

/// Таймерный канал для ветви `select`: срабатывает по истечении срока.
/// Статическая форма — не метод существующего читателя.
#stable(since = "0.1")
export fn ChanReader.close_after(d Duration) -> ChanReader[()]

// ── внутреннее (НЕ export) ───────────────────────────────────────────────
fn Chan[T] mut @try_handoff_to_recv_waiter(v T) -> bool
fn Chan[T] mut @try_take_from_send_waiter() -> Option[T]
fn ChanReader[T] @register_recv(w RecvWaiter[T]) -> ()
fn ChanReader[T] @unregister_recv(w RecvWaiter[T]) -> ()
fn ChanWriter[T] @register_send(w SendWaiter[T]) -> ()
fn ChanWriter[T] @unregister_send(w SendWaiter[T]) -> ()
```

## Как должен быть реализован `select`

### Типы

```nova
/// Общий арбитр на ВЕСЬ select: решает, какая ветвь победила.
#share
type SelectSlot {
    lock  Mutex
    cv    Condvar
    fired AtomicInt      // -1 — не решено; >= 0 — индекс ветви; -2 — отказ (ветвь `_`)
}

/// Ожидатель принимающей ветви. Типизирован — несёт слот прямой передачи.
#share
type RecvWaiter[T] {
    sel  SelectSlot      // общий на все ветви одного select
    arm  int             // индекс ЭТОЙ ветви
    slot Option[T]       // отправитель кладёт значение СЮДА (паритет с C)
}

/// Ожидатель отправляющей ветви.
#share
type SendWaiter[T] {
    sel  SelectSlot
    arm  int
    val  Option[T]       // значение, которое ветвь хочет отдать
}
```

У ветвей разные `T`, поэтому ожидатели типизированы отдельно, а общий у них
только `SelectSlot` — он и есть единственный арбитр.

### Единственный инвариант, из которого следует корректность

> **Ветвь получает (или отдаёт) значение ТОЛЬКО после победы в
> `sel.fired.compare_exchange(-1, arm)`. Другого пути нет — ни у быстрого
> прохода, ни у прямой передачи.**

Отсюда сразу три свойства:

1. **Победитель ровно один** — обмен удаётся единожды.
2. **Проигравшие каналы значений не теряют**: у них никто не выиграл обмен,
   значит из них ничего не забирали, значения так и лежат в их буферах.
3. **Двойного вручения не бывает**: отправитель, проигравший обмен, оставляет
   значение себе (кладёт в буфер или продолжает ждать).

### Регистрация

`register_recv(w)` под замком канала добавляет `w` в список ожидателей и
СРАЗУ ВОЗВРАЩАЕТСЯ. Ни одного элемента не извлекается — это ответ на
опасение «а вдруг заберём из всех каналов».

### Отправка со стороны канала (та же логика, что вне select)

```
send(v):
  взять замок канала
  если закрыт → false
  # 1. прямая передача: есть ли ждущая принимающая ветвь?
  для каждого w в recv_waiters:
      если w.sel.fired.compare_exchange(-1, w.arm) удался:
          w.slot = Some(v)          # значение уходит МИМО буфера
          w.sel.cv.notify_one()
          отпустить замок; вернуть true
  # 2. обычный путь
  если в буфере есть место: положить; not_empty.notify_one(); true
  иначе: ждать not_full; повторить
```

`recv` симметричен: сначала `try_take_from_send_waiter` (забрать напрямую у
ждущей отправляющей ветви, выиграв её обмен), затем буфер, затем ожидание.

### Разворачивание формы

```nova
select {
    Some(v) = rx     => handle(v)
    tx.send(payload) => sent()
    _                => busy()          // ветвь по умолчанию
}
```

компилятор разворачивает в:

```nova
ro sel = SelectSlot.new()
ro w0 = RecvWaiter[T0].new(sel, 0)
ro w1 = SendWaiter[T1].new(sel, 1, payload)

rx.register_recv(w0)
tx.register_send(w1)

// быстрый проход по ветвям СО СЛУЧАЙНОГО СМЕЩЕНИЯ (защита от голодания)
mut which = try_arms_random(sel, w0, w1)

if which < 0 {
    if has_default {
        // ВАЖНО: перед уходом в ветвь `_` закрыть арбитра, иначе опоздавший
        // отправитель вручит значение ветви, которую уже никто не прочтёт.
        if sel.fired.compare_exchange(-1, ABANDONED) {
            which = -2                     // честная ветвь `_`
        } else {
            which = sel.fired.load()       // не успели: значение уже вручено,
        }                                  // обязаны обработать эту ветвь
    } else {
        sel.wait()                         // спим до победы любой ветви
        which = sel.fired.load()
    }
}

rx.unregister_recv(w0)      // снятие — под замком канала
tx.unregister_send(w1)

if which == 0 {
    ro v = w0.slot!!        // значение УЖЕ здесь — ни повторного взятия
    handle(v)               // замка, ни повторной попытки (паритет с C)
} else if which == 1 {
    sent()
} else {
    busy()
}
```

### Тонкости, которые обязаны быть реализованы именно так

1. **Отказ по ветви `_` — тоже через обмен.** Если просто уйти в `busy()`,
   опоздавший отправитель успеет выиграть обмен и положить значение в
   `w0.slot`, который никто не прочитает — потеря значения. Поэтому уход по
   уход по ветви `_` закрывает арбитра значением `ABANDONED`; если обмен не удался,
   значит нас уже выбрали, и ветвь ОБЯЗАНА отработать.
2. **Снятие регистрации — под замком канала.** Иначе отправитель может
   выбрать ожидателя, которого мы в этот момент удаляем.
3. **Случайное смещение быстрого прохода.** Сегодняшний рантайм перемешивает
   ветви (Фишера-Йетса, `nova_select_try_immediate`) — семантика Go, защита от
   голодания: без неё первая ветвь всегда съедала бы вторую. Свойство
   обязано сохраниться; фикстура на голодание — в Ф.4.
4. **Таймерная ветвь.** `ChanReader.close_after(d)` — обычный читатель
   `ChanReader[()]`, который получает значение по истечении срока. Никакого
   особого пути в `select` для неё не нужно: это такая же принимающая ветвь.

### Что остаётся в компиляторе

Только разворачивание формы в код выше: разбор ветвей, создание ожидателей,
связывание переменных, ветвление по индексу победителя. Знания об устройстве
канала — ноль; всё через публичные и внутренние методы `.nv`-слоя, ровно как
`for x in v` разворачивается в вызовы протокола итератора.

## Как достичь паритета с C-версией (вопрос владельца 2026-08-03)

Паритет достижим — но только если воспроизвести приёмы C-версии, а не
надеяться на оптимизатор. Разбор по источникам скорости.

### 1. Прямая передача значения — ГЛАВНОЕ

C-версия при парковке получателя НЕ кладёт значение в кольцевой буфер:
отправитель пишет его прямо в слот ожидателя и будит его
(`recv_val` в `channels.h`: «wake recv helper direct-writes value сюда»,
план 44.1 R1 B1). Экономится копирование и целый круг
«проснуться → взять замок → перепроверить → забрать».

На Nova это выражается один в один: ожидатель несёт типизированный слот.

```nova
#share
type RecvWaiter[T] {
    lock  Mutex
    cv    Condvar
    slot  Option[T]      // отправитель кладёт значение СЮДА
    ready AtomicInt
}
```

`send` под замком канала: если есть припаркованный получатель — положить
значение в его `slot`, отметить `ready`, разбудить, и в буфер НЕ писать.
Проснувшийся получатель читает свой слот, замок канала ему не нужен.

**Следствие для select:** «пробуждение с повтором» из раздела выше — это
УПРОЩЕНИЕ, которое стоит одного лишнего круга. Для паритета ветви тоже
получают прямую передачу: каждая ветвь регистрирует СВОЙ типизированный
`RecvWaiter[T_i]` (у ветвей разные `T` — общий нетипизированный ожидатель
слот нести не может), а «кто победил» решает ОДИН общий `AtomicInt`,
переданный всем ветвям. Тогда розыгрыш остаётся атомарным, а значение
приходит сразу в слот победившей ветви.

Порядок работ: Ф.2 делает простой вариант (пробуждение с повтором) —
он корректен и его легко проверить; **Ф.5 замеряет, и если разрыв
существенный, прямая передача включается и для select**.

### 2. Быстрый путь без сна

Незанятый `Mutex` обязан стоить одну атомарную операцию, без обращения к
планировщику. Если замер покажет иное — это дефект `Mutex`, а не канала,
и чинится он в своём месте (записать номером, не обходить внутри канала).

### 3. Встраивание малых методов — ЧЕСТНЫЙ ПРОБЕЛ

C-версия целиком `static inline` в заголовке: тела втягиваются в место
вызова. В Nova атрибута `#inline` НЕТ (проверено грепом по `std` — ноль
вхождений), и втягивание зависит от того, что сделает компилятор и затем
сишный оптимизатор.

Это единственный источник разрыва, который нельзя закрыть кодом самого
канала. Порядок действий: Ф.5 замеряет; если разрыв объясняется именно
вызовами — вопрос выносится владельцу отдельно (введение `#inline` —
язык-меняющее решение, не принимается внутри этого плана).

### 4. Проверки границ буфера

Индекс в кольце уже приведён по модулю ёмкости, значит проверка границ
при обращении к `buf` избыточна. Трогать это **только после замера**: если
вклад в разрыв мал — не трогать вовсе; преждевременное снятие проверок в
конкурентном коде — плохой размен.

### Правило приёмки по скорости

Паритет считается достигнутым при просадке **не более 5%** на каждом из
трёх сценариев Ф.5. Хуже — либо включается прямая передача для select
(п.1), либо вопрос идёт владельцу с числами и вариантами. Утверждать
паритет без чисел запрещено.

## Фазы

- **Ф.1 — `Chan[T]` на Nova:** структура, `send`/`recv`/`try_*`/`close`/
  `is_closed`/`len`/`cap`, без select. Существующие channel-фикстуры зелёные
  на НОВОЙ реализации (кроме требующих select).
- **Ф.2 — `select`:** `SelectWaiter`, регистрация и розыгрыш, новое
  разворачивание формы в кодогенераторе на вызовы `.nv`-слоя.
- **Ф.3 — снос:** три ad-hoc ветки `Channel.new`, отработавшая часть
  `channels.h`, ограждение `E_CHANNEL_UNSOUND_ELEM_TYPE` (беспредметно —
  буфер типизирован). **Ратчет обязан пойти ВНИЗ** — цель фазы, а не побочный
  эффект; считать ИТОГ (минус ветки `Channel.new`, минус старое опускание
  select, плюс новое).
- **Ф.4 — расширение и фикстуры:** `Channel[str]`, `Channel[f64]`, канал
  многополевых записей — всё, что запрещало ограждение; select над такими
  каналами; фикстура «проигравшие ветви не теряют значения» (два готовых
  канала, два прохода select обязаны выдать оба значения); фикстура на
  голодание (случайный порядок ветвей).
- **Ф.5 — замер производительности (ОБЯЗАТЕЛЬНАЯ, см. риск 1).**
- **Ф.6 — дока и спека:** `docs/guide/channels.md` (сейчас описывает
  ограничение word-safe), D91/D94-соседство — тем же слиянием.

## Риски

1. **Производительность — главный риск.** Сегодняшний канал — вылизанный C с
   прямой передачей значения ожидателю. Версия на Nova ходит через `Mutex` и
   `Condvar`, а они сами паркуют файбер — цепочка длиннее. Канал использует
   флагман и Полярис, регресс будет заметен. **Ф.5 обязана ПОДТВЕРДИТЬ ПАРИТЕТ числами** (рецепт — раздел «Как достичь паритета»): замер до/после
   на одинаковом сценарии: пропускная способность при разной ёмкости буфера,
   задержка на пустом канале, select с несколькими ветвями. Если просадка
   существенная — решение владельца: принять, оптимизировать (прямая передача
   значения ожидателю, как в C-версии) или откатить план. **Без замера план не
   закрывается.**
2. **`Condvar.wait` под `Mutex` в M:N.** Проверить корректность парковки при
   миграции файбера между потоками: механизм тот же, что у `Mutex`, но канал
   нагружает его иначе.
3. **`#share` и D415.** `Chan[T]` помечен `#share` — под энфорсом S1a это
   разрешает захват сторон канала в файбер. Проверить, что `consume`-семантика
   `send` (№144) не сломана: использование после отправки обязано отклоняться.
4. **Рантайм-гонки.** Текущая C-реализация вылизана годами, новая может
   принести свои. Приёмка статистическая (как в окне p-vela): нагрузочная
   фикстура прогоняется **30 раз**, проходной результат 30/30.

## Как читать этот план (урок его же редактирования)

Первая редакция правилась множеством точечных замен, и трижды в ней оставались
следы прежних версий (`addr_of`, `@0`, `.null()`), а сама она описывала не то,
что просил владелец. Отсюда два правила. **Исполнителю:** прочитать план
ЦЕЛИКОМ перед стартом; при противоречии внутри плана верным считать раздел
решений владельца и сказать интегратору, а не догадываться. **Правящему:**
после блока замен грепнуть СНЯТУЮ форму (ожидание — ноль вхождений) и
перечитать файл целиком.

## Приёмка

Мега-CU (канон на момент старта — 629/0/68), флагман `--strict-effects`,
`nova check std/src` 147/26/60, polaris 37/0/18; **ratchet ВНИЗ** по `lines`;
три пробы интегратора из №286 остаются зелёными; фикстуры Ф.4; нагрузочная
30/30; **замер Ф.5 в отчёте с числами**; `nova lint` 0.
