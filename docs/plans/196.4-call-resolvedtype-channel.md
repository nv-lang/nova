<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 196.4 — Канал Call-expr `ResolvedType`: фундамент Tier-2

**Родитель:** [196-one-truth-closeout.md](196-one-truth-closeout.md) (матрица одного окна + целевая
архитектура + rustc-эталон). **Триггер:** «★★ СТРУКТУРНАЯ НАХОДКА (Q5)» в
[196.3-wave2-d-driven.md](196.3-wave2-d-driven.md) — чекер НЕ аннотирует `ExprKind::Call` в
`resolved_types` → codegen перевыводит тип результата вызова → весь Tier-2 инвентаря волны-2 заблокирован.
**Статус:** 🔄 ДИЗАЙН (opus-разведка, worktree `nova-spike`, ветка `spike-196`). **Это ДИЗАЙН, НЕ реализация —
код канала не пишется в этом заходе.**

> Развилка (A) из Q5: построить канал Call-expr `ResolvedType` (фаза коллектора, rustc-aligned) — крупно, но
> разблокирует БОЛЬШИНСТВО ядра волны-2 и является целевой архитектурой. Этот документ = дизайн развилки (A).

---

## 0. TL;DR (резюме дизайна)

_(заполняется последним — однострочное резюме точки внедрения + канала + потребления + Tier-2)_

## 1. Проблема (из Q5, доказано по коду)

_(что именно отсутствует: Call не ключуется в resolved_types_buf; codegen перевыводит через infer_call_ret_c
и семейство; Tier-2 структурно заблокирован)_

## 2. Как чекер СЕЙЧАС заполняет `resolved_types_buf`

_(recon-1: f1_expr_inner, какие ExprKind, ключ, какой ResolvedType; где Call пропускается)_

## 3. Где чекер УЖЕ вычисляет подставленный тип результата вызова

_(recon-2: constraint-solver / method-resolve / call-typecheck — где на чек-тайме уже есть subst-тип
результата generic-вызова, даже если не кладётся в канал)_

## 4. Задел typed-ir-p1 / коллектор — переиспользуем ли

_(recon-3: git log/show ветки typed-ir-p1; что там за typed-IR/collector; переиспользуемые куски)_

## 5. rustc-эталон: mono collector + Instance-subst

_(recon-4: rustc_monomorphize collector + Instance/subst per-call-site; наш Stage-1 аналог без MIR)_

## 6. ДИЗАЙН канала Call-expr ResolvedType

### 6.1 Точка внедрения в чекере
_(где хукнуть, чтобы для КАЖДОГО Call положить подставленный ResolvedType в resolved_types_buf[call.id])_

### 6.2 Данные (subst-мапа, generic-space каллиси → space вызывающего)
_(откуда на чек-тайме берётся subst T→конкрет; как переносится generic-пространство)_

### 6.3 Потребление codegen (channel-first)
_(resolved_type_to_c вместо infer_call_ret_c / легаси; порядок чтения канала)_

## 7. Минимальность — границы Stage-1

_(что НЕ делаем: не полный MIR, не отдельный IR-узел; что именно входит в Stage-1)_

## 8. Разблокируемый Tier-2 (инвентарь + порядок закрытия)

_(список функций Tier-2 + порядок их закрытия следом за каналом)_

## 9. Связь с терминальным кластером волны-1

_(46293-48883 infer_call_ret_c удаляется, когда ВСЕ Call идут через канал)_

## 10. Оценка объёма / рисков / поэтапность

_(объём, риски, этапы Stage-1a/1b/…, гейты)_
