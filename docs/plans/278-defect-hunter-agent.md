<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 278. Агент-охотник за дефектами

**Статус:** ✅ СДЕЛАН 2026-08-30 — все четыре фазы закрыты; тем же днём принят и РЕАЛИЗОВАН регламент владельца (Ф.5 долг-триггер, Ф.6 треки и свёртка — см. фазы ниже, четыре стража в гейте). Ф.1 файл агента (`.claude/agents/defect-hunter.md`, увязан стражем связности); Ф.2 мера (`check-hunter-mark.sh` + самотест, в гейте); Ф.3 сетка из дерева (`check-hunter-coverage.py` + самотест + храповик, в гейте); Ф.4 — ПЕРВАЯ ОХОТА проведена по трём клеткам (parse×К3, lex×К2, check×К1), дала 7 находок и 6 строк реестра (№807-812), все с меткой «НАЙДЕНО ОХОТНИКОМ» и маршрутом в план 274. Мера считается грепом: 3 охоты / 7 находок / 6 в реестре; покрытие 98→95 клеток. Отчёты — `docs/dev/hunts/novac/2026-08-30-*.md` (Ф.6: у каждого трека свой каталог). Прежний статус: 📋 ЗАВЕДЁН 2026-08-29 интегратором по предложению
[docs/dev/research/2026-08-29-defect-hunter-agent.md](../dev/research/2026-08-29-defect-hunter-agent.md)
(ревизия того же дня запуском; предложение доказало себя на себе — проверка его
центрального утверждения дала два новых дефекта с репро, №799 и №800, которых
не видел ни один из 153 стражей — **правило счёта:** файлов `scripts/guards/check-*.{sh,py}`, замер 2026-08-29; в тот же день их стало 154, потому что этот счёт и породил 154-го — `check-oracle-nesting-depth.sh`). **🔴 БЛОКЕР ТЕГА — решение владельца
2026-08-29: «включи до тега в релиз».** Пункт в плане релиза —
[221](221-release-v0-1.md) A-V17.

**ГЛАВНОЕ В ПЛАНЕ — ОХОТА ПО `novac`** (владелец 2026-08-29, поправка к моей
первой редакции: «охота по novac — самое главное»). Я сузил объём до «построить
охотника, а охотиться после тега» — это было неверно и противоречило §5 самого
предложения: постоянная ценность охотника именно в `novac`, потому что он
строится СЕЙЧАС, а дефект, найденный в строящемся компиляторе, стоит дешевле
всего; оракулу же положен один проход ярусом гейта, и он уже сделан (№800).
Значит Ф.4 — это охота по `novac`, и она входит в тег наравне с постройкой.

**Один результат предложения уже В ТЕГЕ — и вот чем именно, чтобы никто не
построил это дважды:** №800 (оракул умирал на глубокой вложенности без
диагностики) закрыт 2026-08-29 по прямому решению владельца. Коммиты:
**`d2ed84578`** (предел 512 в парсере, `E_NESTING_TOO_DEEP`, страж
`check-oracle-nesting-depth.sh` с самотестом, шаг гейта на ярусе `push`) и
**`8d6c828d7`** (обе фикстуры переписаны с вложенных скобок на `if`: скобки
глубже предела — это буквально `W_REDUNDANT_PAREN`, и фикстура не может
показывать одно правило, нарушая другое).

## Что решено и не пересматривается

Разбор предложения и ответы автора — в самом документе (§12–§15). Здесь только
то, что связывает исполнителя:

- **вердикт по диффу запрещён**, гипотеза с воспроизведением — нужна;
- **цель назначает окно, всегда**; сетка клеток (модуль × класс К1–К7) — КАРТА
  ПОКРЫТИЯ, не очередь, и выводится из дерева, а не пишется руками (прецедент
  `check-no-handwritten-plan-index.sh`);
- **мера §8 считается ПО КЛЕТКЕ**; порог снятия — три прогона по трём РАЗНЫМ
  клеткам без единой дошедшей находки;
- **находка приезжает файлом-пробой в своём пустом каталоге** (§14.2), потому
  что `nova check <файл>` тянет папку как модуль: пробы в одном каталоге меряют
  соседей — на этом ошиблась первая ревизия;
- **строка реестра по находке охотника несёт метку `НАЙДЕНО ОХОТНИКОМ <дата> (novac|oracle)`** (§14.1 + Ф.6: трек ПОСЛЕ даты, чтобы не сломать регэксп даты) — иначе числитель меры не посчитать грепом, и мера держится на памяти.

## Регламент охоты — решения владельца 2026-08-30, реализованы тем же днём

Три требования владельца («Срочно доработать план 278, реализовать и отдать
274»), их механизмы и где каждый живёт:

1. **Частота — на автомате, не по чуйке окна.** Триггер — ДОЛГ ДЕРЕВА:
   `check-hunter-debt.sh` (ярус `loop`) меряет added-строки поверхности трека
   с коммита, который последним ДОБАВИЛ отчёт охоты (`git log --diff-filter=A`
   — свёртка-удаление часы не двигает, рукописных дат нет вовсе); долг выше
   бюджета из `hunter-debt.baseline` — гейт красный, пока охота не проведена
   и отчёт не закоммичен. Календарь отвергнут проектным разбором (панель трёх
   дизайнов + три адверсариальных критика, 2026-08-30): часы не принадлежат
   дереву, а строки принадлежат; окно, не растящее поверхность, охотиться не
   обязано. **Барьер бумажной охоты** (общая дыра всех трёх критиков: отчёт
   можно написать не охотясь): отчёт обязан привозить ≥3 файлов-проб в
   `docs/dev/hunts/<трек>/probes/<имя-отчёта>/`, каждая находка цитирует
   существующую пробу — судит `check-hunter-mark.sh`; подделка стоит как
   настоящая охота.

2. **Треки раздельны, куча ограничена.** У оракула и `novac` свои охоты:
   каталоги `docs/dev/hunts/novac/` и `docs/dev/hunts/oracle/`, метка несёт
   трек, мера Ф.2 считается ПО ТРЕКАМ, бюджеты долга — свои. Реестр
   `221.1-bug-sweep.md` остаётся ОДНИМ домом всех дефектов (разделение — про
   кучи охоты, не про реестр; вторая копия реестра разошлась бы). Кучи не
   копятся бесконечно: `check-hunter-fold.sh` держит предел открытых отчётов
   (`hunter-fold.baseline`, `max_open` на трек); разобранный отчёт
   СВОРАЧИВАЕТСЯ в строку `LEDGER.md` трека (формат — шапка fold-стража,
   единственный дом), файл и пробы удаляются — история остаётся в git,
   клетки покрытия держит леджер, свёрнутые находки остаются в знаменателе
   меры. Сетка-храповик покрытия — только у `novac`; у оракула оси в доке
   нет, это граница, названная в его леджере.

3. **Страж пишет ЧИНЯЩИЙ, не находящий.** Охотник только находит; окно
   заводит строку реестра с меткой; тот, кто чинит дефект, обязан чинить
   КЛАСС и назвать страж/фикстуру, которые класс удержат, — для находок по
   `novac` это окно 274. Механическая опора — действующие правила: новый
   `E_*`/`W_*` без негативной фикстуры не проходит, строка реестра
   закрывается только по замеру. Записано в бриф охоты (`delegate.md`,
   раздел «Агент-охотник»).

## Не делаем (границы)

- Не даём агенту выносить вердикт «баг или норма» — это работа окна.
- Не заводим очередь целей; РАСПИСАНИЯ охоты по-прежнему нет — есть долг
  дерева (Ф.5): триггер механический, а КЛЕТКУ и момент внутри бюджета
  по-прежнему выбирает окно.
- Не переносим объёмные ревью-инструкции, исполняемые моделью: у нас проверки —
  детерминированные стражи с самотестами.
- Не даём агенту коммитить и пушить чужую работу; своя ветка — можно.

## Открытые вопросы (адресат, умолчание, срок)

1. **ОТВЕЧЕН владельцем 2026-08-29, переспрашивать не нужно:** охота по `novac`
   идёт ДО тега и является главным содержанием плана. Вопрос стоял как «пускать
   ли охотника по novac до тега», умолчание было «нет» — и умолчание оказалось
   неверным. Записано ответом, а не вопросом, чтобы не стоить владельцу второго
   ответа.
2. **Интегратору, с умолчанием и сроком:** модель охотника. Умолчание — opus
   (это разведка и суждение о формах, а `delegate` запрещает отдавать такое
   дешёвым моделям); пробный прогон — sonnet, чтобы измерить разницу, а не
   предположить её. **Срок: до первого прогона Ф.4** — позже вопрос теряет
   смысл, потому что сравнивать будет уже не с чем.
3. **Владельцу, с умолчанием:** числа регламента — `budget_novac=1500`,
   `budget_oracle=3000` (added-строк до обязательной охоты),
   `max_open=6` (открытых отчётов на трек до обязательной свёртки).
   Засеяны интегратором 2026-08-30 по замеру темпа (novac ~1200 строк/день →
   охота примерно раз в день-полтора активной работы); базы помечены
   «ОЖИДАЕТ слова владельца». **Умолчание — засеянные числа действуют.**
   Срок: до первой красноты долга — она покажет, жмёт порог или провисает.

## Фазы и приёмка

### Ф.1. Файл агента — ✅ СДЕЛАНА 2026-08-30

`.claude/agents/defect-hunter.md` написан: цель-клетка от окна, форма выдачи
§3 (строка на находку + три обязательных раздела), три запрета дословно,
правка §14.2 (находка = файл-проба в своём пустом каталоге), дисциплина проб
(чекпоинты, молчание — не успех, №770), эталоны для оракула и `novac` (П25/П27:
оракул не донор). Модель — opus, по умолчанию вопроса 2.

**Машинная половина прошла обе стороны ВЖИВУЮ:** сразу после создания файла
`check-agent-definitions-wired.sh` покраснел («определение не названо ни одной
командой») — имя проведено в `delegate.md` (раздел «Агент-охотник»), страж
зелёный: «определений 2, безымянных 0, висячих 0». Приёмка глазами содержимого
брифа — за владельцем, когда посмотрит; первый живой прогон агента — Ф.4.

**Урок, купленный этой фазой:** файл создавался под ИДУЩИМ гейтом, и шаг
связности гейта мог попасть в окно «определение есть, имени нет» — не попал
только потому, что успел пройти раньше. Правки дерева под идущим гейтом —
нельзя, даже текстовые.

Прежний текст фазы:

`.claude/agents/defect-hunter.md` — роль, запреты, ФОРМА ВЫДАЧИ (§3 предложения,
включая правку §14.2 про файл-пробу).

**Приёмка ГЛАЗАМИ, потому что** содержимое брифа машиной не проверить.

**Машинная половина у неё всё же есть, и страж для неё УЖЕ СУЩЕСТВУЕТ —**
`check-agent-definitions-wired.sh` (проверено 2026-08-29 запуском: «определений
1, ссылок из команд и скилов 1, безымянных 0, висячих 0»). Он судит связность в
обе стороны: каждое `.claude/agents/X.md` названо хотя бы одной командой или
скиллом, и каждый `subagent_type` ведёт в существующий файл. Значит новый агент
обязан быть НАЗВАН там, куда окно смотрит, иначе страж покраснеет — и заводить
для этого второго стража не нужно.
**Первая редакция этого плана назвала здесь «стража полноты страницы правил»** —
это другой страж (`check-rules-page-complete.sh`), он судит
`docs/dev/rules-for-agents.md` и к файлам агентов отношения не имеет. Опора была
названа неверно; исправлено второй перепроверкой.

### Ф.2. Метка находки и её счёт

Метка `НАЙДЕНО ОХОТНИКОМ <дата> (novac|oracle)` в строках реестра + страж, считающий числитель
и знаменатель грепом. Приёмка: страж печатает оба числа и краснеет, если метка
стоит в строке без даты либо дата не разбирается. Самотест обязателен.

### Ф.3. Сетка клеток, выведенная из дерева

Скрипт, строящий карту (модуль × класс) из таблицы рёбер архитектуры и списка
классов; число «клеток, где никогда не охотились» — храповик вниз.

**База храповика засевается СЕГОДНЯШНИМ числом, а не нулём** — иначе страж
покраснеет на всей истории в первый же прогон и будет отключён первым окном,
которое его встретит. Судит он только движение вверх от засеянной базы.

**Приёмка:** рукописной сетки в дереве нет (страж), число печатается, храповик
работает. **Краснота доказывается** подсовыванием сетки с бо́льшим числом
неохваченных клеток — самотест обязателен.

**Столкновений домов нет — проверено 2026-08-29:** стражей со словом `hunter` в
`scripts/guards/` ноль; девять файлов с `grid|coverage` в имени — про фикстуры
диагностик, покрытие std и грамматику novac, к сетке клеток отношения не имеют.

### Ф.4. Охота по `novac` — главная фаза

Три прогона по трём РАЗНЫМ клеткам сетки Ф.3, цель каждого назначает окно
(§15: живое подозрение окна — самая точная наводка, стоячей очереди нет).
Каждая находка приезжает файлом-пробой в своём пустом каталоге (§14.2) и
попадает в реестр строкой с меткой `НАЙДЕНО ОХОТНИКОМ <дата> (novac)` (§14.1, трек — Ф.6).

**Что запускается:** страж Ф.2 (`bash scripts/guards/<имя>.sh .`), и его строка
с двумя числами — числителем и знаменателем — есть доказательство.
**Приёмка:** доля находок, дошедших до реестра, посчитана этим стражем — числом,
а не впечатлением; решение «оставляем охотника / снимаем» принимается по этому
числу. Порог снятия — три прогона по трём разным клеткам без единой дошедшей
находки (по разным, иначе три неудачи в одной пустой клетке снимут работающего
агента).

**Почему это главная фаза, а не хвост:** `novac` строится сейчас, и дефект,
найденный в строящемся компиляторе, стоит дешевле всего — переделывать по нему
нужно меньше кода, чем потом. У оракула ценность обратная: там окно закрывается
тегом, и потому ему положен ОДИН проход ярусом гейта, который уже сделан (№800).

### Ф.5. Долг дерева — автоматический триггер охоты — ✅ СДЕЛАНА 2026-08-30

Регламент §1. `check-hunter-debt.sh` + `hunter-debt.baseline`
(budget_novac/budget_oracle/anchor — якорь нужен треку без единого отчёта) +
самотест `test-check-hunter-debt.sh`; шаг гейта на ярусе `loop`.

**Приёмка (машинная, пройдена):** самотест — три подделки краснеют (долг над
бюджетом; свёртка-удаление отчёта НЕ гасит долг — дыра критиков закрыта
`--diff-filter=A`; якорь-не-коммит), гашение долга коммитом нового отчёта
зелёное, живая половина зелёная. Вердикт живого прогона:
`check-hunter-debt ok: novac: долг 0/1500 …; oracle: долг 0/3000 …`.

### Ф.6. Треки и свёртка — куча с пределом — ✅ СДЕЛАНА 2026-08-30

Регламент §2 (+§3 в брифе). Отчёты переехали в `hunts/novac/`; заведён
`hunts/oracle/`; в каждом треке `LEDGER.md`; пробы трёх охот Ф.4 перенесены
в дерево (`probes/<имя-отчёта>/`, 9 каталогов — цитируемые находками плюс
законная граница на каждый отчёт); метки №807–812 получили трек `(novac)`;
`check-hunter-mark.sh` переписан (треки + барьер бумажной охоты),
`check-hunter-coverage.py` — только `novac`, многоклеточный findall + клетки
из леджера; новый `check-hunter-fold.sh` + `hunter-fold.baseline` + самотест.

**Приёмка (машинная, пройдена):** самотесты mark (шесть подделок), coverage
(четыре + многоклеточность + леджер), fold (четыре: переполнение кучи,
свёртка-потеряшка, нечитаемая строка леджера, пробы-сироты) — все зелёные;
четыре живых прогона на настоящем дереве зелёные, счёт не изменился
(3 охоты / 7 находок / 6 меток / покрытие 95).

## За чей счёт время

Период отдан плану [274](274-novac-self-hosted-compiler.md) (`novac`) — и этот
план работает НА него, а не против: главная фаза (Ф.4) охотится именно по
`novac`, где ценность вложения постоянная (§5 предложения). Постройка Ф.1–Ф.3
время у 274 отнимает, и это названо прямо; окупается она первой же охотой,
потому что без метки и сетки результат охоты нечем измерить.

Окна: постройка (Ф.1–Ф.3) — любое окно, компилятор не трогается. Охота (Ф.4) —
окно, которое ведёт работу по `novac`: цель назначает оно, и никто за него.
