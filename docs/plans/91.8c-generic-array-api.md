# Plan 91.8c — Generic array API: sort/min/max/binary_search + _by (D185)

> **Подплан Plan 91** ([91-stdlib-mvp-for-0.1.md](91-stdlib-mvp-for-0.1.md)).
> Статус: 🔴 OPEN.
> Зависимости: **Plan 91.8a** (Comparable.compare -> int), желательно 91.8b закрыт но не блокер.

## Мотивация

Plan 91.3 даёт `[]int @sort()` (concrete). Plan 91.7 закрепил `-> @` mut chain
для array builtins. Остаётся **generic API** для `[]T` где `T Comparable`:

- Сейчас `vs.sort()` работает только для `[]int`. Для `[]str`, `[]u64`, user types — compile error.
- В Plan 91.7 followup `[M-91.7-sort-generic]` зафиксирован — теперь делаем.
- После 91.8a `str.compare() -> int` consistent с Comparable; sort на `[]str` должен работать без особых обёрток.

## Scope

### Generic methods (Comparable bound)

```nova
fn[T Comparable] []T mut @sort() -> @
fn[T Comparable] []T @binary_search(target T) -> Option[int]
fn[T Comparable] []T @min() -> Option[T]
fn[T Comparable] []T @max() -> Option[T]
```

### Callback methods (без bound на T)

```nova
fn[T] []T mut @sort_by(cmp fn(T, T) -> int) -> @
fn[T] []T @min_by(cmp fn(T, T) -> int) -> Option[T]
fn[T] []T @max_by(cmp fn(T, T) -> int) -> Option[T]
fn[T] []T @binary_search_by(cmp fn(T) -> int) -> Option[int]
```

`binary_search_by` принимает 1-arg cmp возвращающий int сравнивая target с element (как Rust's `binary_search_by`).

### Algorithm

- **sort:** stable insertion sort для MVP (Plan 91.3 уже это для int). Для generic — тот же подход, но через `@compare`. Followup `[M-91.8c-pdq-sort]` для pdq-sort на больших данных.
- **binary_search:** classic O(log n) с `@compare` для сравнения с target.
- **min/max:** linear scan, O(n).
- **min_by/max_by:** linear scan с callback.

### Backward compat

Existing `fn []int mut @sort() -> @` в `std/sort.nv` (Plan 91.3) — оставляем как **concrete fast-path**. Generic версия добавляется параллельно; overload resolution выбирает concrete для `[]int`.

После 91.8c можно concrete удалить (D183 fast-path не нужен — generic mono per T даёт identical C code). Решение: **оставить пока** для ясности.

## Затронутые файлы

**Компилятор (nova-p91 worktree):**
- `compiler-codegen/src/codegen/emit_c.rs` — generic method mono для `fn[T Comparable]` на builtin `[]T`. Plan 101 mono pass уже работает для user types; нужно расширить на builtin array receiver.
- `compiler-codegen/src/types/mod.rs` — type-checker для `[T Comparable]` bound на builtin `[]T` receiver.

**Stdlib (nova-p91 worktree):**
- `std/sort.nv` — добавить generic `fn[T Comparable] []T @sort()` и т.д.; existing `[]int @sort` оставить как fast-path или удалить (решить).
- `std/collections/vec.nv` — добавить `@min/@max/@min_by/@max_by`? Или в `sort.nv`? **Default:** в `sort.nv` (semantically related).

**Tests (nova-p91 worktree):**
- `nova_tests/plan91_8c/` — новый каталог

## Spec / D-block

### D185 — Generic array methods bound на Comparable

**Файл:** `spec/decisions/02-types.md`.

**Title:** `D185. Generic array API: sort/min/max/binary_search + _by callbacks`

**Content:**

1. Generic surface API:
   - `fn[T Comparable]` для bound-based methods
   - `fn[T]` + cmp callback для `_by` variants
2. Stable sort guarantee (insertion sort для MVP).
3. binary_search behavior: returns `Option[int]` с found-index; `None` если absent.
4. min/max: `Option[T]` — `None` для empty.
5. Algorithm complexity: O(n²) sort, O(log n) search, O(n) min/max. Followup `[M-91.8c-pdq-sort]`.
6. Связь: D183 (Comparable), D181 (array mut `-> @`), D178 (str.compare).

### Q-block updates

- `Q-sort-generic` (если есть) — closed by D185.
- `Q-array-api` — третий update note.

## Тесты

### Позитивные (новые в `nova_tests/plan91_8c/`)

| Файл | Что тестирует |
|---|---|
| `sort_generic_int_test.nv` | `[3,1,4].sort() == [1,3,4]` — concrete unchanged |
| `sort_generic_str_test.nv` | `["c","a","b"].sort() == ["a","b","c"]` — через `str.compare` |
| `sort_generic_u64_test.nv` | `[]u64.sort()` PASS |
| `sort_generic_user_type_test.nv` | type Point с `@compare(other) -> int`; `[Point...]` sortable |
| `sort_by_callback_test.nv` | `xs.sort_by(\|a,b\| b - a)` — descending; `xs.sort_by(\|a,b\| a.field - b.field)` — by field |
| `binary_search_test.nv` | sorted array; `find target`, `not found`, `empty array` |
| `binary_search_by_test.nv` | search в sorted-by-field user types |
| `min_max_test.nv` | `[3,1,4].min() == Some(1)`, `.max() == Some(4)`, `[].min() == None` |
| `min_by_max_by_test.nv` | callback-based; works for any T |
| `sort_stable_test.nv` | stable property: equal elements сохраняют relative order |
| `sort_chain_test.nv` | `xs.sort().binary_search(target)` — chain after `-> @` |

### Негативные

| Файл | Что должно дать compile error |
|---|---|
| `sort_non_comparable_err.nv` | type Vector3 без `@compare` — `[Vector3].sort()` → error «T does not satisfy Comparable» |
| `binary_search_unsorted_err.nv` | runtime warning или debug_assert, не compile error — skip или mark known |
| `min_empty_err.nv` | НЕ error — `[].min() == None` (это позитивный тест выше) |

### Регрессия

- `nova test plan91/sort_basic` — 15/15 PASS (Plan 91.3 not broken)
- `nova test plan91_7/sort_chain_test` — PASS (concrete-path)
- `nova test plan73 plan77 plan91 plan91_7 plan91_8a plan91_8b plan100 plan108 syntax`
- Full `nova test` — baseline maintained

## Acceptance criteria

### Реализация
- [ ] `fn[T Comparable] []T mut @sort() -> @` — generic, работает для int/str/u64/user types
- [ ] `fn[T Comparable] []T @binary_search(target T) -> Option[int]`
- [ ] `fn[T Comparable] []T @min() -> Option[T]`
- [ ] `fn[T Comparable] []T @max() -> Option[T]`
- [ ] `fn[T] []T mut @sort_by(cmp fn(T, T) -> int) -> @`
- [ ] `fn[T] []T @min_by(cmp fn(T, T) -> int) -> Option[T]`
- [ ] `fn[T] []T @max_by(cmp fn(T, T) -> int) -> Option[T]`
- [ ] `fn[T] []T @binary_search_by(cmp fn(T) -> int) -> Option[int]`
- [ ] codegen: Plan 101 mono pass для `fn[T Comparable] []T @method` — emit per-T concrete instance
- [ ] type-checker: `[T Comparable]` bound enforced на use-site

### Spec
- [ ] D185 в `spec/decisions/02-types.md`
- [ ] Index обновлён
- [ ] Q-block updates

### Тесты
- [ ] 10+ позитивных тестов в `nova_tests/plan91_8c/` — PASS
- [ ] 1+ негативный тест (sort_non_comparable) — expected compile error
- [ ] Регрессия: Plan 91.3 sort_basic 15/15 PASS, plan91_7 sort_chain PASS
- [ ] Full `nova test` baseline maintained

### Документация
- [ ] `docs/project-creation.txt` — секция «Plan 91.8c closure»
- [ ] `docs/simplifications.md` — note про generic sort dispatch
- [ ] `nova-private/discussion-log.md` — секция
- [ ] parent `91-stdlib-mvp-for-0.1.md` — статус 91.8c ✅
- [ ] **Финальный «Статус закрытия»** в этом файле

### Коммиты
- [ ] Атомарные коммиты (codegen / stdlib / tests / docs)
- [ ] **НЕ push** в origin
- [ ] **НЕ merge** в локальный main без подтверждения

## Открытые вопросы

1. **Q1:** Удалять concrete `[]int @sort()` после generic? **Default:** оставить как documentation/canonical example (не дублирует C-emission).
2. **Q2:** Sort algorithm — insertion sort O(n²) достаточен? Когда переходить на pdq? **Default:** MVP — insertion; followup `[M-91.8c-pdq-sort]`.
3. **Q3:** `min/max` для empty: `None` или panic? **Default:** `None` (Option[T] design).

## Effort + risk

**Estimated:** 1-2 dev-days. Generic mono для builtin `[]T` — нетривиально (Plan 101 покрывает user types; builtin требует additional emit_c.rs paths). Plus 11+ tests, spec, docs.
**Risk:** medium-high. Generic mono per T для array builtin может зацепить existing array-method dispatch. Type-checker `[T Comparable]` bound enforcement через protocol satisfaction — fragile.

## Принцип реализации — production-grade

> Implementation должна быть **production-grade**. Никаких MVP shortcut'ов
> вроде «hardcoded для []int, []str и потом extend». Generic dispatch
> должен работать **uniformly** для любого T satisfying Comparable
> (int/str/u64/user records). Insertion sort алгоритм — для MVP OK,
> но не «temporary» — это canonical implementation для small/medium arrays.
>
> Все edge cases (empty, single-element, all-equal, reversed, with NaN/INF
> для float) — должны быть covered тестами. Stable property — формально
> проверена через property test.
>
> Generic mono per T — concrete C code для каждого T, **NOT** type erasure
> через void*. Performance должна быть равна или лучше manual implementation.
>
> Если упирается в архитектурную проблему — **поднять вопрос пользователю**,
> не обходить.

---

## Статус закрытия

> **Шаблон.** Заполнить при закрытии работ.

**Дата закрытия:** YYYY-MM-DD

**Коммиты:**
- nova-p91 (`plan-91-stdmvp`): `<hash>` — codegen generic mono для array
- nova-p91 (`plan-91-stdmvp`): `<hash>` — stdlib sort.nv generic methods
- nova-p91 (`plan-91-stdmvp`): `<hash>` — tests
- nova-p91 (`plan-91-stdmvp`): `<hash>` — docs
- nova (`main`): `<hash>` — D185 spec
- nova-private (`main`): `<hash>` — discussion-log

**Тесты:** plan91_8c positive X/Y, negative X/Y, regression status.

**Что вошло / что отложено / lessons learned:** заполнить.

**Acceptance criteria check:** проставить [x] на каждом пункте.
