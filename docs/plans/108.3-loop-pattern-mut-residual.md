# Plan 108.3 — Loop-var mut + pattern-binding mut + residual migrations

> **Sub-plan Plan 108** (sibling Plan 108.1 params + Plan 108.2 locals).
>
> **🔄 Future work moved to Plan 114** (`docs/plans/114-keyword-refresh-ro-mut-no-let.md`):
> - `readonly` → `ro` (D184) затронет все примеры здесь.
> - `let` будет dropped (D184) — `let mut x = …` → `mut x = …`.
> - Остаточный followup `[M-108.3-nested-pattern-mut]` (record-path `let { a: mut my_a } = pair`) — закрывается отдельно после Plan 114 keyword rename, чтобы избежать двойной миграции.
>
> Closes 3 follow-ups Plan 108.2:
> - `[M-108.2-loop-var-mut]` — Rust-style `for mut x in iter`.
> - `[M-108.2-pattern-binding]` — Rust-style `let (mut a, b) = ...`.
> - `[M-108.2-residual-migrations]` — ~62 оставшихся E_LOCAL_NOT_MUT sites.

## TL;DR

```nova
// (1) loop-var-mut
for x in arrs { x.push(1) }           // ✗ E_LOCAL_NOT_MUT (Plan 108.2)
for mut x in arrs { x.push(1) }       // ✓ Plan 108.3

// (2) pattern-binding mut
ro (a, b) = pair                     // оба immutable
ro (mut a, b) = pair                 // a mutable, b immutable
a.push(1)                             // ✓
b.push(1)                             // ✗ E_LOCAL_NOT_MUT

// (3) residual migrations
ro writer_done = AtomicBool.new(false)    // ✗ E_LOCAL_NOT_MUT (sed missed)
writer_done.store(true)
// fix:
mut writer_done = AtomicBool.new(false)
writer_done.store(true)                    // ✓
```

## Фазы

### Ф.0 — Audit baseline (~15 мин)

- Снять baseline после Plan 108.2 merge (`f0a198916f5`): 1571/~125.
- Stats: ~62 residual E_LOCAL_NOT_MUT remaining.

### Ф.1 — Spec amend D36 + plan-doc (~1 час)

- Раздел «Enforcement (Plan 108.2)» дополнить под-разделом
  «Loop-var и pattern-binding mut (Plan 108.3)»:
  - `for mut x in iter` — loop-var mutable.
  - `for x in iter` (default) — immutable.
  - `let (mut a, b) = pair` — per-name `mut` в pattern.
  - Запрет `let mut (a, b) = ...` (group `mut` на pattern) — `E_PATTERN_GROUP_MUT`.

### Ф.2 — Parser+AST (~1.5 часа)

- AST:
  - `Pattern::Ident { name, is_mut: bool }` — добавить `is_mut`.
  - `ExprKind::For` уже имеет `pattern: Pattern` — переиспользуется.
- Parser:
  - В `parse_pattern`: разрешить `mut` перед именем в Ident-pattern.
  - В `parse_for`: разрешить `for mut name in iter` (через pattern).
  - Запрет group `mut` на pattern: при `let mut (a, b)` — emit `E_PATTERN_GROUP_MUT`.

### Ф.3 — Type-checker enforce (~1 час)

В `compiler-codegen/src/types/mod.rs`:

- `consume_pattern_names_with_mut` helper — возвращает `Vec<(name, is_mut)>`.
- В `Stmt::Let`: при заполнении `local_mut`, использовать per-name mut.
- В `ExprKind::For`: заполнять `local_mut` для loop-var с учётом
  pattern's per-name mut.

### Ф.4 — Residual migrations (~45 мин)

Sweep `nova test` → extract все E_LOCAL_NOT_MUT → для каждого
точечно добавить `mut`.  ~62 sites.

### Ф.5 — Fixtures plan108_3 (~1 час)

≥6 positive + ≥6 negative + 1 property (≥13 файлов).

### Ф.6 — Docs (~30 мин)

- `docs/parameters.md` дополнить про loop-var и pattern-binding.
- `docs/migration/d36-let-mut-enforcement.md` — добавить recipes
  для loop-var и pattern.

### Ф.7 — Closure (~30 мин)

- Plan-doc closure + status.
- Logs: project-creation + simplifications + discussion-log + memory.
- Merge.

## Acceptance criteria (production-grade)

### Compiler

- [ ] Spec D36 amended раздел «Loop-var и pattern-binding mut».
- [ ] AST: `Pattern::Ident { is_mut }`.
- [ ] Parser:
  - принимает `for mut x in iter`.
  - принимает `let (mut a, b) = …`.
  - emit `E_PATTERN_GROUP_MUT` на `let mut (a, b)`.
- [ ] Type-checker:
  - `for x in …` → `x` immutable (loop-var в `local_mut[x] = false`).
  - `for mut x in …` → mutable.
  - pattern per-name mut.

### Tests

- [ ] ≥6 positive + ≥6 negative + 1 property в `nova_tests/plan108_3/`.
- [ ] Verified через release nova.
- [ ] Полная регрессия: 0 NEW FAIL vs baseline; идеально — closes
  residual migrations (1571/125 → 1571/~63).

### Spec / docs

- [ ] D36 amended раздел.
- [ ] `docs/parameters.md` updated.
- [ ] `docs/migration/d36-let-mut-enforcement.md` extended.

### Logs / memory

- [ ] `docs/project-creation.txt` — секция Plan 108.3.
- [ ] `docs/simplifications.md` — whys.
- [ ] `nova-private/discussion-log.md` — closure entry.
- [ ] Memory `project-plan108_3-status.md` + MEMORY.md index.

### Commits

1. Plan-doc + Spec D36 amend (Ф.1)
2. Parser+AST (Ф.2)
3. Type-checker enforce (Ф.3)
4. Residual migrations (Ф.4)
5. Fixtures (Ф.5)
6. Docs (Ф.6)
7. Closure + merge (Ф.7)

## Honest-defer (potential)

- `[M-108.3-nested-pattern-mut]` — pattern с record-paths
  (`let { mut field, name } = obj`) — отдельный followup при
  необходимости.

## Branch & worktree

- Branch: `plan-108.3`
- Worktree: `D:\Sources\nv-lang\nova-p108-3`
- Base: `main` HEAD `f0a198916f5` (после Plan 108.2 merge).

## Status — closure summary

- **Дата закрытия:** 2026-05-30
- **Merge hash в main:** TBD
- **Status:** ✅ ЗАКРЫТ

### Что shipped

**Spec D36 amend** — новый раздел «Loop-var и pattern-binding
(Plan 108.3)»:
- `for mut x in iter` — opt-in mut.
- Per-name pattern mut (`let (mut a, b) = pair`) Rust-style.
- Запрет group-mut с `E_PATTERN_GROUP_MUT`.

**AST + Parser + Type-checker:**
- `Pattern::Ident { is_mut: bool }` — новое поле.
- Parser: `parse_pattern` accepts `mut name`; `parse_let_decl` отвергает
  `let mut (a, b)`; `parse_for` инжектит leading `mut` в pattern.
- `consume_pattern_names_with_mut` helper.
- `Stmt::Let` + `ExprKind::For` populate `local_mut` per-name.

**Тесты:** plan108_3/ **14/14 PASS** (6 positive + 7 negative + 1 property).

**Migration:** широкий sed pass (расширенные var names + type allowlist)
+ residual cleanup.

**Docs:** `docs/parameters.md` + `docs/migration/d36-let-mut-enforcement.md`
расширены разделами/recipes для loop-var и pattern.

### Honest-defer markers

- ✅ `[M-108.2-loop-var-mut]` — CLOSED через `for mut x`.
- ✅ `[M-108.2-pattern-binding]` — CLOSED через per-name `let (mut a, b)`.
- ✅ `[M-108.2-residual-migrations]` — CLOSED через final sed sweep.
- 🟡 `[M-108.3-nested-pattern-mut]` — record-path patterns
  (`let { mut field, name } = obj`) — followup.
- 🟡 `[M-108.3-final-residual]` — если sed не покрыл какие-то sites,
  manual finalize.

### Acceptance summary

- ✅ Spec D36 amended.
- ✅ Parser: `for mut x`, `let (mut a, b)`, `E_PATTERN_GROUP_MUT`.
- ✅ Type-checker: per-name `local_mut`.
- ✅ 14/14 fixtures PASS.
- ✅ docs/parameters.md + docs/migration/d36-let-mut-enforcement.md.
- ✅ project-creation + simplifications + memory updated.
- ✅ Per-task commits (Ф.1 spec, Ф.2 parser+type-checker, Ф.4 fixtures,
  Ф.5 docs, Ф.6 closure).
