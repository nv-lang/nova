<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# План 274.7 — снятие подмножества: спека на 100%, без упрощений

**Статус:** 🔨 В РАБОТЕ (заведён 2026-09-05 по решению владельца — 274 §1.3а). **СДЕЛАНО:** В1а (интерполяция — все примитивные семьи, 2026-09-05). **ОСТАЛОСЬ:** В1б, В1в, порядок дальнейших волн по замеру частоты (§2); инвентарь А 116 → 116 констант (В1а сузила область одной, не сняла), Б 15 → 13. **Родитель:** [274](274-novac-self-hosted-compiler.md). **Соседи:** [274.5](274.5-read-own-source.md) (самосборка — главный носитель отказов), [274.6](274.6-fix-tuple-to-record-door.md) (кортеж → запись).

## 0. Слово владельца и что оно меняет

Дословно (2026-09-05, по трём местам эмиттера из отчёта охоты emit_c × К3): «похоже на
хардкод, это не в правил же?», «не похоже, что это по спеке», «У тебя в плане должно быть
написано, что делать без упрощений как для прода, все должно соответствовать спеке
полностью на 100% — все переделать, если были упущения».

До этого дня отказ «outside the subset» был законным промежуточным состоянием с датой
(`check-novac-subset-debt-dated`). С этого дня он — **отклонение от спеки, подлежащее
переделке**; честный отказ остаётся единственно допустимой формой НЕДОДЕЛКИ, но не
конечным состоянием. Полная формулировка решения — 274 §1.3а; здесь — работа.

## 1. Мера

Инвентарь снят с дерева агентом 2026-09-05 (раздел И): **116 констант отказа, 159 мест
вызова; 15 хардкодов ширины/каста в эмиттере.** Мера плана — оба числа вниз до нуля,
храповиком: `check-novac-subset-debt-dated` получает базу `refusals=116` и красит РОСТ;
второй счётчик — хардкоды Б — держит новый страж (заводится первой волной, потому что
без него число Б живёт только в этом файле).

## 2. Порядок волн — по числу носителей, не по алфавиту

Порядок определяется ЗАМЕРОМ, а не инвентарём: сколько раз каждый отказ срабатывает
на (а) пачечной самопроверке novac (`scripts/tools/novac-batch-selfcheck.sh` → счётчик
по тексту отказа), (б) корпусе `examples/` и `novac/fixtures/`. Первая волна — тот
отказ, что срабатывает чаще всего; хардкоды Б идут вместе с волной, чьи типы они
касаются (интерполяция → `emit_interp.nv:66` и `exprs.nv:304` в одной волне; диапазон
`for` → `emit_c.nv:705` с волной ширин у циклов).

Три носителя, названные владельцем, идут первыми волнами независимо от счёта — это
его слово:
- В1 «интерполяция по спеке» — делится на две, потому что у них разные механизмы:
  - **В1а — примитивы всех семей дверью по ряду универсума.** Сейчас `check/exprs.nv:304`
    держит слот множеством `{str_id, int_id}` по ИДЕНТИЧНОСТИ id, а `emit_interp.nv:66`
    кастит всё не-`str` в `(nova_int)` и зовёт `int_display_spec`. Рантайм
    (`std/src/runtime/string_builder.nv`, семья `*_display_spec`) уже умеет `int`, `f64`,
    `f32`, `bool`, `str`, `char` — спека требует ровно их через `Into[str]`. Устройство:
    одна дверь `display_call_of(row)` в `emit_interp.nv` — по семье ряда (`PfInt`, `PfFloat`
    с шириной, `PfBool`, `PfChar`, `PfStr`) имя функции и C-тип аргумента, как `printer_of`
    делает для `println` (274.5 §3-пред59); чекер принимает слот, если `prim_row_of(t)`
    отвечает строкой (все ширины `Ints`, обе `Floats`, `bool`, `char`, `str`), newtype — через
    носитель (`carrier_of`). Носитель волны: `examples/basics/interp_families.nv` со слотами
    всех семей и ширин; держатель — на точный C (`f64_display_spec`, `bool_display_spec`,
    каст к `nova_f32`, а не `nova_int`); обратная проба — дверь снята, `${1.5}` печатает `1`.
  - **В1а СДЕЛАНА 2026-09-05, все пять пунктов приёмки:** (1) носитель `examples/basics/interp_families.nv` — слоты `int`, `f64`, `bool`, `char`, `str`, `i8`, `u16`, `i64`, `f32`, выражения — байт-в-байт с оракулом; (2) обратная проба: дверь `display_call_of` подменена на «всё через int» — держатель `subset_test.nv` RUN-FAIL, дым носителя падает на компиляции C; восстановлено — оба зелёные, остатков подмены ноль; (3) держатель на ТОЧНЫЙ C: `17bool_display_spec(`, `16f64_display_spec(` с кастом `(double)`, `17char_display_spec(`, `16f32_display_spec(`, и отрицательный — нет `int_display_spec(_novac_isb_t`; (4) корпусный дифференциал — зелёный тем же коммитом; (5) счётчик: константа `INTERP_TYPE_MSG` НЕ удалена — её область сузилась до не-примитивов (В1б), и это сказано в её тексте со сроком `(274.7 B1b)`; новый датированный долг `INTERP_U64_MSG` `(274.7 B1c)`. **Поправка по дороге:** первая редакция принимала newtype «через носитель» — оракул отказал `E_INTERP_NO_DISPLAY` на моём же носителе: спека (D73) требует у отдельного типа СВОЙ `Display`; судья сужен до самого примитивного ряда, newtype — ground В1б. Инвентарь Б: строки `emit_interp.nv:66` и `exprs.nv:304` закрыты.
  - **В1в — `uint_display_spec` в std** (открытый вопрос выше, решение владельца): пока его нет, `u64`/`uint` в слоте — датированный отказ `(274.7 B1c)`, не усечение.
  - **В1б — пользовательские типы и `Option[T]` в слоте: три пути спеки, один судья.**
    По спеке (02-types, амендмент `E_INTERP_NO_DISPLAY`; D422; D410) объявленный тип
    отображаем в голом `${x}`, если у него есть ОДНО из трёх: (а) явный метод `@display(mut f
    Fmt)` — и сверяется ПАРАМЕТР, а не имя (оракул, №634: семь типов пережили миграцию формы
    молча, потому что проверка искала по имени — и падали SEGV); (б) `#impl(Display)` на
    объявлении — авто-вывод по гейту; (в) `T.to_str()` (правка 2026-09-05: здесь стояло ещё
    «`str.from(T)` перегрузка» — амендмент D54 того же дня, `03-syntax.md:3618`, говорит, что
    `str.from(v)` НЕ существует; противоречие с примерами `02-types.md:8537/8544/8685` — в §2а,
    выбор не мой). Иначе —
    диагностика по спеке (у оракула `E_INTERP_NO_DISPLAY`), не отказ подмножества. `Option[T]`
    отображаем, когда `T` отображаем (D73). rustc-прецедент, записанный в спеке: `Display` не
    выводится сам, отсутствие — ошибка компиляции, не фолбэк.
    **Чего у novac нет и что даёт волна:** novac не читает `#impl(...)`-атрибуты объявлений
    вовсе (грепом по `sem/`, `check/` — ноль кодовых строк); `ProtoTable` есть, `Display` как
    протокол прелюдии — есть в `std/prelude/protocols.nv`. Устройство: (1) `harvest`/`collect`
    читают `#impl(P)` объявления в строку типа (`TypeDef` получает список реализованных
    протоколов — ОДНА таблица-владелец, спрашивающий — чекер; урок §3-пред62); (2) судья слота
    в `check/exprs.nv` спрашивает одну дверь `displayable(t)`: примитивный ряд → да; `Option[T]`
    → `displayable(T)`; объявленный тип → метод `display` с параметром типа `Fmt` в `fns`, или
    `#impl(Display)`, или `to_str` (не `str.from` — D54, §2а); иначе диагностика спеки; (3) эмиттер: для
    объявленного типа — вызов его `display` через `Fmt`-контекст рантайма (интероп, назвать
    как интероп с условием конца), для `Option[T]` — `None`/`Some(x)` по правилу прелюдии.
    **Носитель:** запись с `#impl(Display)`, запись с явным `@display(mut f Fmt)`, `Option[int]`
    в слотах; **негативный держатель** — запись без всего трёх → точный текст диагностики;
    **обратная проба** — снята проверка параметра `Fmt` → тип с `@display(mut w Write)` проходит
    (класс №634 в форме novac) → держатель краснеет. Дифференциал корпуса — обязателен.
    **Граница:** `${x:?}` (Debug) и спецификаторы формата — не эта волна; их отказ датируется
    `(274.7 B1d)`.
    **Решение по устройству (2026-09-05, до кода):** атрибут `#impl(P)` читается ТОЙ ЖЕ дверью,
    что `#coerce` сегодня — `attr_names(c, ATTR)` (`sem/coerce.nv:120`, `check/check.nv:548`), через
    константу `IMPL_ATTR` в `builtins.nv` (П5: имя атрибута в одном месте). Реализации протоколов
    — отдельная таблица `ImplTable` в `Ctx` (строка `type_decl → proto`), а не поле `TypeDef`: у
    строки типа девять мест конструирования (collect 6, harvest 2, sem 1), и это ДРУГОЙ предмет со
    своим наполнителем (collect) и одним потребителем (дверь `displayable` в check) — как `ProtoTable`
    рядом; строка §10.3б заводится тем же слиянием (страж `check-novac-ctx-tables`). Волна идёт
    ЦЕЛИКОМ: судья + таблица + эмиттер + probe оболочки; судья без эмиттера — половина, а половина
    запрещена решением владельца (§0). Старт — когда машина свободна для пробы C-формы оракула.
    **Разделение В1б по механизму (2026-09-05, после пробы оракула `target/b1b-probe/disp`):**
    - **В1б-1 — явный `@display(mut f Fmt)` у объявленного типа.** C-форма снята: слот →
      `Nova_FmtCtx* ctx = Nova_FmtCtx_static_bare(<sb>, 0, false); Nova_<T>_method_display(v, ctx);`,
      где `<sb>` — `Nova_StringBuilder*` цепочки, принимаемый как `Write` напрямую (сигнатура в
      оболочке: `static_bare(Nova_StringBuilder* sink, nova_int mark, nova_bool is_debug)`); метод
      пользователя лоурится с `Nova_FmtCtx* f`, `f.write(b)` → `Nova_FmtCtx_method_write(f, b)`,
      `"lit".bytes()` → `Nova_str_method_bytes(lit)`. **Интероп, названный интеропом:** протокол `Fmt`
      привязан к единственному реализатору `FmtCtx`, которого кодоген синтезирует руками (комментарий
      std/prelude/protocols.nv у `FmtCtx`) — одна пара `Fmt → FmtCtx` в builtins, уходит, когда novac
      получит настоящую диспетчеризацию протоколов. Судья: у типа слота есть метод `display`, ПЕРВЫЙ
      параметр которого типа `Fmt` (по типу, не по имени — №634). Что даёт волна: вызов метода на
      получателе-протоколе (`f.write`) через сигнатуры `ProtoTable` и пару реализатора; `c_type(Fmt)`
      → `Nova_FmtCtx*`; слот-дверь в `emit_interp`. Носитель — запись с явным `@display`, тело —
      `f.write("…".bytes())`; держатель на точный C (`static_bare(`, `method_display(`); обратная
      проба — судья снят с проверки типа параметра → `@display(mut w Write)` проходит и краснеет.
      **В1б-1 СДЕЛАНА 2026-09-05, все пять пунктов приёмки:** (1) носитель
      `examples/basics/interp_display.nv` (запись `Pt` с `@display(mut f Fmt)`, слоты `p ${p} end`
      и `${p}${p}`) — байт-в-байт с оракулом (`novac-e1-smoke ok`), и `interp_families.nv` В1а
      остался зелёным; (2) обратная проба — у `display_row_of` снят ТИП параметра (ключ с
      `no_ty()`, скрипт `scratch/b1b1_reverse_probe.py`): держатель «`@display(mut w Write)`
      отвергается» покраснел (`interp_test.nv`, assert `contains("non-primitive type")`),
      носитель остался зелёным; восстановлено, остатка `.b1b1-backup` нет; (3) держатели — все
      17 файлов `novac/src/*/*_test.nv` PASS (интерполяционный блок уехал из `subset_test.nv` в
      `pipeline/interp_test.nv`: файл перешёл 1000 строк — режем по смыслу, не по счёту);
      (4) дифференциал корпуса: фикстур 16/16, корпус 109 файлов, contract 60, behavior 52 из 54;
      (5) база `novac-corpus.baseline` 58/50 → 60/52 тем же коммитом, с летописью двух файлов.
      **Где устройство отошло от записанного до кода — и почему это конструкция, а не заплата:**
      - **Второй стол канала `implicit`.** Первая проба записала строку `display` в `res` узла
        слота, а узел слота — голое имя `p`, и эмиттер имени прочитал запись как
        «вариант-значение» (ICE «a bare name resolved to a non-variant callee»). Узел слота сам
        может быть вызовом (`${f()}`), а `res` держит ОДИН ответ на узел по конструкции — значит
        неявным вызовам нужен свой стол, а не арм `Resolution`. Донор — rustc
        `TypeckResults::adjustments`. `record_implicit`/`implicit_of`/`has_implicit`; mono
        обходит оба стола ОДНОЙ функцией `mark_ref` (иначе тело, достижимое только через
        `display`, не эмитится, и об этом сказал бы линкер). Строка §10.3б-канал плана 274 —
        тем же коммитом (страж `check-novac-ctx-tables`).
      - **Поданный метод ПЛОСКОГО типа, тело которого несёт оболочка, — законный интероп-вызов**
        (через одну дверь `c_method_symbol`: плоское или инстансное написание), а не отказ
        «compiles no body». Правило то же, что путь инстансов применял к `v.len()`; до этого дня
        отказ был правдой о novac и неправдой о программе. Побочный плод: `examples/basics/strings.nv`
        принят (`byte_len`/`to_ascii_upper`/`starts_with`), держатель pipeline_test, утверждавший
        три отказа, переписан в обратную сторону — с одним отказом ПО ИМЕНИ метода, которого в
        оболочке нет.
      - **`harvest` читает тип возврата ПОСЛЕ ведущего режима** (`-> ro []u8`, как std пишет
        `str @bytes`); до этого подпись молча отбрасывалась, и `"Pt".bytes()` было «no such method».
      - **Обобщённый инстанс в слоте (`Box[int]` с `@display`) — отказ с адресом `(274.7 B1b)`**,
        не эмиссия `c_callable` с пустой подстановкой (это была бы неверная программа); через дверь
        `record_instance_method` судья слота пойдёт в В1б-2.
      - **`display_row_of` — дверь без аллокации** (`#realtime nogc`, П18), ключ строит
        `display_key_of`; `impl_of_proto` возвращает через `same_ty()` на всех путях
        (`[LEGACY-#760-newtype-ro-launder]` — оракул красит голый `return t` у newtype в
        `nova check` и в test-CU, а `nova build` пропустил: расхождение драйверов, строка реестра
        №TBD ниже).
      **Найдено по дороге (реестр 221.1, №TBD):** (а) оракул: `nova build` собирает файл, который
      `nova check` и сборка test-CU отвергают по `E_READONLY_COERCE` — три драйвера, два ответа
      (класс №669); (б) novac: поданная декларация `fn … -> ()` БЕЗ тела — мусор восстановления,
      глотающий СЛЕДУЮЩУЮ декларацию (`type Fmt protocol` исчезала молча, проба s0), и пользователь
      видит «no such method» вместо причины.
    - **В1б-2 — `#impl(Display)` авто-вывод, `T.to_str()` (не `str.from` — D54 амендмент, §2а), `Option[T]`.** Требует `ImplTable`
      и чтения `#impl` (решение выше) и идёт после В1б-1; до неё эти формы — датированный отказ
      `(274.7 B1b)`.
    **Факты эмиссии (чтение std, 2026-09-05; C-форма — пробой оракула, когда машина свободна):**
    `std/prelude/protocols.nv` — `export type Display protocol { @display(mut f Fmt) -> () }`;
    `Fmt protocol { use Write; @width/@precision/@align/@fill/@sign/@alternate/@kind; mut @pad }`;
    конкретный контекст — `FmtCtx { sink Write, mark int, width/precision/align Option, fill,
    sign, alternate, kind FmtKind, mut pad_consumed, mut prec_consumed }`, строится кодогеном на
    каждом `${x}` через `FmtCtx.bare(sink Write, mark int, is_debug bool)` (все оси пусты, `kind` —
    `Display`/`Debug`); приёмник `sink` — `Write` поверх `StringBuilder` цепочки. Значит эмиссия
    пользовательского слота — ТРИ интероп-вызова оболочки: обёртка билдера в `Write`, `FmtCtx.bare`,
    затем `Nova_<T>_method_display(&value, ctx)`; каждый обязан существовать в `shell.tpl.c`, иначе
    probe растёт той же волной (правило регенерации оболочки). Символы оболочки на день записи:
    Nova_FmtCtx (+ method_align/alternate/fill/kind/pad/precision/sign/width/write), Nova_FmtCtx_static_bare, Nova_FmtCtx_static_bare__sret; method_display: int, bool, char, Vec (from the probe); NOT yet in the shell: the Write wrapper over StringBuilder and a user method_display.
  - **Открытый вопрос В1 (владельцу · умолчание · срок):** рантайм не имеет
    `uint_display_spec`/`u64`-варианта — оракул печатает беззнаковые 64 бита через
    `int_display_spec` с усечением (это отклонение ОРАКУЛА от спеки, не novac). Умолчание:
    В1а печатает `u64`/`uint` через `nova_print_uint`-подобный путь только там, где рантайм
    его даёт, а недостающую функцию заводит в `std` отдельной малой волной с амендментом
    (`std` — язык поставки); до её появления слот `u64`/`uint` получает ЧЕСТНЫЙ отказ с
    именем недостающей функции рантайма, а не усечение. Срок — до начала В1а.
- **В2 СНЯТА поправкой 2026-09-05, и это поправка к моему ответу владельцу, не к спеке.** Я назвал `nova_int` у переменной `for lo..hi` хардкодом «не по D58». Спека говорит обратное: 03-syntax, блок D58 — `fn RangeIter mut @next() -> Option[int]`, `fn Range @iter() -> RangeIter`, тип `Range` НЕ обобщён; элемент диапазона — `int`, границы любой целой ширины расширяются до него (проба охоты `p07-range-u8`: `idu8(250)..idu8(255)` печатает 250…254 у обоих). Эмиттер (`emit_c.nv:705`) здесь по спеке; отклонением была бы сама В2. Место остаётся в инвентаре Б как **не-дефект** с этой причиной, чтобы следующий читатель не завёл волну заново. Урок: порядок старшинства из `AGENTS.md` — спека → конвенции → компилятор → память; я ответил владельцу с уровня «память» и был поправлен уровнем «спека».
- В3 — далее по замеру (2).

## 2а. Поправки спеки 2026-09-03…09-05 — сверка с novac (заведено 2026-09-05 по вопросу владельца)

Вопрос владельца 2026-09-05: «там много корректировок спеки пришли, видел? в план включил?» —
ответ по факту: НЕ включил. Инвентарь А/Б строился от отказов и хардкодов, а не проходом по
поправкам; D58 и D422 читались по ходу волн, остальное системно не сверялось. Это дыра плана,
и закрывается она здесь: 13 спековых коммитов `main` с 2026-09-03 (`git log origin/main
--since=2026-09-03 -- spec/`) сопоставляются с обязанностями novac строкой на поправку. Строки
приезжают выдачей агента-читателя (sonnet, форма `/delegate`), сверяются по дереву и вписываются
тем же днём; первая — та, что бьёт по текущей волне, — записана сразу:

| Поправка | Что требует | Для novac | В волне |
|---|---|---|---|
| D129/D130 амендмент 2026-09-05 (`int` ≠ `i64`, `uint` ≠ `u64` — РАЗНЫЕ типы по ИМЕНИ, разные mangle-имена; чекер оракула сравнивает ширины — дефект по букве D129) | одинаковый числовой тип = одинаковый по имени; `int + i64` — ошибка | судья слота В1а/В1в отказывает `u64`/`uint` по ШИРИНЕ (`family == PfInt && !signed && bits == 64`, `check/exprs.nv`) — это тот же дефект в форме novac: отказ и функция рантайма обязаны различаться ПО ИМЕНИ типа (`uint_display_spec` ≠ `u64`-вариант); universe novac держит `int` и `i64` отдельными строками — проверить, что ни одна дверь (`prim_row_of`, `c_type`, `subst`) не склеивает их по ширине | В1в (u64/uint), плюс отдельная проверка дверей — строка добавляется по выдаче читателя |
| D405 амендмент 2026-09-04 + `spec/conversions.md` (новая страница): никакого автоматического расширения — оба операнда любого числового оператора одного ИМЕНОВАННОГО типа; послабления только для ЛИТЕРАЛОВ; «A value of type `int` next to a value of type `f64` is still an error, literal or not» (conversions.md:69) | `int + f64` с именованными операндами — ошибка компиляции | **novac НАРУШАЕТ:** `check/type_of.nv:173-179` `int_f64_mix` пропускает пару `int`/`f64` в обе стороны и расширяет до `f64` — по замеру оракула 2026-09-04, а спека того же дня объявляет ровно этот случай ошибкой. Спека > оракул (AGENTS, порядок старшинства): дверь снимается, литеральное послабление остаётся (`literal_rules.nv`); держатель — отказ на `a + b` при `a int`, `b f64`, приём на `b + 1`. Оракул — сторона интегратора (№926, `docs/dev/research/2026-09-04-numeric-widening.md`) | **В4 (новая, малая): «смешанные числа — по спеке, не по оракулу»**; после В1в |
| Унарный минус определён только для знаковых типов (`spec/conversions.md`, R2 амендмент 04-effects 2026-09-04) | `-x` при `x` беззнаковом — ошибка компиляции | **novac НАРУШАЕТ в чекере:** `check/operators.nv:250` `Negate => { if @is_numeric(t) { return false } }` пропускает минус на любом числовом; эмиттер (`emit_expr.nv`, `neg_signed`) знает «только знаковые» — чекер не знает. Отказ добавляется в чекер, держатель на `-u` при `u u8` | В4 (та же волна: одна дверь «числовой оператор — сверка операндов») |
| D129/D130 (см. первую строку) — дополнение по сверке дерева | `int` ≠ `i64`, `uint` ≠ `u64` по имени | universe novac (`builtins.nv:296-308`) держит четыре РАЗНЫЕ строки с разными `TyId` и C-именами (`nova_int`/`int64_t`, `nova_uint`/`uint64_t`) — идентичность операндов по id не склеивает их; единственная дверь по ШИРИНЕ — судья слота (первая строка). `as` (bit-pattern, Q2 снят) — целиком в инвентаре А (`CAST_MSG`) | В1в + инвентарь А (`as`) |
| D102 амендмент 2026-09-04: параметр без дефолта — только позиционно, с дефолтом — только по имени | симметричное правило связывания | **соответствует:** `check/calls.nv:165-167` формулирует ровно это правило с ссылкой на D102 | — |
| D54 амендмент / №931(а) 2026-09-05 (`03-syntax.md:3618-3639`): `str.from(v)` НЕ существует (`E_UNKNOWN_STATIC_METHOD`), канон — `v.to_str()`; `char.try_from(n)` не существует, проверяемой int→char конверсии сегодня нет | таблица сужений без мёртвых отсылок | **бьёт по тексту этого плана:** В1б называла `str.from(T)` третьим путём Display — снято этой правкой (см. В1б-2). **Противоречие ВНУТРИ спеки, выбор не мой:** `03-syntax.md:3618` «`str.from(v)` не существует» против `02-types.md:8537` «`sb.append(str.from(@))`», `:8544` «`fn str.from(MyType) -> str` overload», `:8685` «`fn str.from(c Coin) -> str => ...`» — старший D-блок про Display/fmt всё ещё пишет `str.from`. Владельцу/интегратору с обеими цитатами | В1б-2 идёт по `T.to_str()`; `str.from` — только после решения по противоречию |
| D469 (2026-09-04): анонимный тип с ИМЕНОВАННЫМИ компонентами `(file str, line int)` — структурное тождество по (имя, тип), позиционное конструирование, доступ по имени | вторая грамматическая ветка кортежа | novac: `parse/type_ref.nv:114` знает только позиционную форму, и ЛЮБАЯ кортеж-форма как тип отказана (`TYPE_FORM_MSG`, инвентарь А, 4 места); `sem/sem.nv:103-111` уже несёт `RecordShape.AnonNamedTuple` без читателя | волна `TYPE_FORM_MSG` инвентаря А получает ОБЕ формы, не одну |
| D430 R6 (checked float→int `@try_to_<T>()` на `S Floats`), D310 амендмент (set-blanket метод считается на каждом члене и для конформанса), множество `Floats` | type-set-bounds и их конформанс | novac не имеет понятия type-set вовсе; generic-bound отказан оптом (`GENERIC_BOUND_MSG`, `rules.nv:804`) | волна `GENERIC_BOUND_MSG` инвентаря А — с D310/D430 в критериях приёмки |
| D52 амендмент (2026-09-04): операторы над числовым newtype — только внутри одного newtype (+ литерал) | newtype не стирается оператором | сегодня любой оператор на newtype отказан (`operators.nv:198`, инвентарь А) — норма станет обязательной с диспетчем `@plus`/`@compare` | волна операторов-протоколов инвентаря А, критерий приёмки — D52 |
| D468 (`CallerLoc`, `caller_loc()`): место вызова — параметр с умолчанием, подставляемым НА МЕСТЕ ВЫЗОВА; `requires`/`assert`/`panic`/`throw` берут его сами | дефолт-значение параметра — ВЫЗОВ | novac: ноль строк на `CallerLoc`/`caller_loc`; упирается в `DEFAULT_EXPR_MSG` (`rules.nv:459`: дефолт — только число или булев) | волна `DEFAULT_EXPR_MSG` инвентаря А расширяется до вызова `caller_loc()`; D468 — в её критериях |
| Эффекты и параллелизм: D455 (`Outcome[T]` cancel-scope), D456 (форма effect-операции), D453 (эффект запуска процесса); англ. `syntax.md` подтянул D78 rev-6, D447, D457, D464 | — | вне подмножества novac целиком: эффекты не читаются (`ATTR_MSG`, `rules.nv:394`), модули отказаны (`IMPORT_MSG`/`EXPORT_MSG`), bound'ы отказаны | волны эффектов/модулей инвентаря А; номер по замеру частоты |

**Чего в документах нет (по выдаче читателя, сверено):** устройство «одна дверь сверки операндов для всех числовых операторов» описано как требование (D405 п.4), не как конструкция novac — решается в В4; имя проверяемой int→char конверсии спека сама называет нерешённым (`03-syntax.md:3636-3639`); как `caller_loc()` ложится на harvest (builtin без объявления в `.nv`) — не описано; порядок этих волн относительно В1–В3 — по замеру частоты (`novac-batch-selfcheck.sh`), замер не проводился.

Модели агентов на этот раздел: читатель — sonnet (spec-reader, форма `/delegate`), 1505 строк диффа за один заход; сверка фактов дерева — моя, до записи.

## 3. Приёмка каждой волны — форма §3 плана 274 ЦЕЛИКОМ, все пять

(1) носитель в `examples/basics/` — дым байт-в-байт с оракулом; (2) обратная проба —
правило снято, носитель или держатель краснеет; (3) держатель в модульном тесте на
ТОЧНЫЙ текст (не на факт отказа); (4) `check-novac-differential` — корпус целиком, не
«позже» (поправка к 274.5 §3-пред61: волна без дифференциала не закрыта, коммит ждёт
машину); (5) счётчик отказов/хардкодов опущен ТЕМ ЖЕ коммитом, константа отказа
удалена из `rules.nv` (мёртвая константа — ложь о покрытии). **Эталон — спека, не
оракул:** где оракул сам отклоняется от спеки (реестр 221.1 это знает), расхождение
поведения фиксируется в `novac-divergences.md` с номером строки, а novac делает по
спеке.

## 4. Не делаем (границы)

- Не меняем архитектуру §3 и правило «одна дверь на класс»: они и есть способ дойти
  до 100% без второй копии; волна, требующая второй двери, останавливается и идёт к
  владельцу.
- Не трогаем отказы, которые спека ТРЕБУЕТ (E_-диагностики языка: неисчерпывающий
  `match`, дубль объявления, самоссылка инициализатора) — они меняют текст с «outside
  the subset» на диагностику по спеке и уходят из инвентаря как переименование, не как
  реализация; это отдельная малая волна «слова отказов».
- Не берём модель единицы компиляции (CU) — решение владельца ожидается (274.5
  §3-пред62); классы «сигнатура без тела» в инвентарь не входят.

## 5. Открытые вопросы (адресат · умолчание · срок)

- Владельцу: порядок после В1/В2 — по замеру частоты (умолчание) или по его слову;
  умолчание действует, пока не сказано иное.
- Интегратору: номер строки реестра под класс «отказ подмножества как конечное
  состояние» (для ссылок из строк волн); умолчание — `№TBD`.
- Владельцу: отказы, требуемые спекой (граница 2), — считать ли их закрытыми
  переименованием текста; умолчание — да, с держателем на новый текст.

## И. Инвентарь (фотография дерева 2026-09-05)

## И.1. Инвентарь А — отказы подмножества (снят агентом sonnet 2026-09-05, грепом по дереву)

Инвентарь А -- отказы подмножества (`outside the subset`) в `novac/src/**/*.nv`
(без `*_test.nv`, без комментариев). Отсортировано по числу мест вызова, убывание.

| file:line | const name | refusal text (first 90 chars) | call sites |
|---|---|---|---|
| novac/src/check/rules.nv:334 | TYPE_FORM_MSG | outside the subset: this type form is not compiled yet (slices, tuples, fn types and quali | 4 |
| novac/src/check/rules.nv:253 | NESTED_INTERP_MSG | outside the subset: an interpolation nested inside another slot is not compiled yet (E2-b3 | 3 |
| novac/src/check/rules.nv:288 | VARIANT_VALUE_NEEDS_ARGS_MSG | outside the subset: this variant carries a payload -- construct it with arguments, `Name(. | 3 |
| novac/src/check/rules.nv:297 | IFLET_VARIANT_MSG | outside the subset: `if <pattern> = ...` takes only `Some(name)` here -- other variants ar | 3 |
| novac/src/check/rules.nv:421 | VALUE_METHOD_MSG | outside the subset: a method on a `value` record needs the receiver taken by POINTER (the  | 3 |
| novac/src/check/rules.nv:531 | CAST_MSG | outside the subset: a cast `expr as T` is not compiled yet (E2-b, with the numeric family) | 3 |
| novac/src/check/binds.nv:484 | SELF_INIT_MSG | outside the subset: a binding's initializer refers to the name it binds | 2 |
| novac/src/check/binds.nv:520 | NOT_EXHAUSTIVE_MSG | outside the subset: match on a sum leaves a variant uncovered (oracle: E_MATCH_NON_EXHAUST | 2 |
| novac/src/check/binds.nv:524 | ARMS_DISAGREE_MSG | outside the subset: match arms produce different types (no arm coercion before E2-b3) | 2 |
| novac/src/check/literal_rules.nv:382 | DECL_POSITIONAL_PROJ_MSG | outside the subset: projection by position on a DECLARED positional record is not compiled | 2 |
| novac/src/check/literal_rules.nv:386 | CHAR_UNICODE_MSG | outside the subset: the unicode char escape (backslash-u) is not compiled yet (E2-b3) -- w | 2 |
| novac/src/check/literal_rules.nv:394 | COALESCE_NONOPT_MSG | outside the subset: `??` unwraps an `Option`, and this left side is not one | 2 |
| novac/src/check/rules.nv:239 | INT_TOO_LARGE_MSG | outside the subset: this integer literal does not fit in `int` -- the emitted C would wrap | 2 |
| novac/src/check/rules.nv:243 | NESTING_TOO_DEEP_MSG | outside the subset: nesting is deeper than 512 levels -- the parser stops before the stack | 2 |
| novac/src/check/rules.nv:245 | DUP_DECL_MSG | outside the subset: this top-level name is declared twice in one file -- the second body i | 2 |
| novac/src/check/rules.nv:261 | INTERP_TYPE_MSG | outside the subset: an interpolation slot of this type is not compiled yet (E2-b3) -- I1 s | 2 |
| novac/src/check/rules.nv:265 | INTERP_CONST_MSG | outside the subset: a constant whose initializer interpolates is not compiled yet (E2-b3) | 2 |
| novac/src/check/rules.nv:278 | INTERP_CONTRACT_MSG_MSG | outside the subset: an interpolated contract message is not compiled yet (E2) -- spell the | 2 |
| novac/src/check/rules.nv:283 | PRINTLN_ARG_FORM_MSG | outside the subset: this form is not compiled in an argument position yet (E2-b) -- bind i | 2 |
| novac/src/check/rules.nv:293 | DISJ_BINDER_MSG | outside the subset: a `\|` pattern takes `_` binders only -- name the value in a separate a | 2 |
| novac/src/check/rules.nv:311 | TUPLE_PARAM_MSG | outside the subset: a tuple as a PARAMETER is not compiled yet (B15 bounds) -- return one, | 2 |
| novac/src/check/rules.nv:321 | SHADOW_FOREIGN_MSG | outside the subset: the interop shell already declares a struct under this name's C spelli | 2 |
| novac/src/check/rules.nv:341 | IF_EXPR_MSG | outside the subset: an `if` in value position is not compiled yet | 2 |
| novac/src/check/rules.nv:355 | TYPED_LOCAL_MSG | outside the subset: a declared local type is not compiled yet (E2-b3) | 2 |
| novac/src/check/rules.nv:363 | RECORD_VARIANT_MSG | outside the subset: a record-form variant is not compiled yet | 2 |
| novac/src/check/rules.nv:382 | IMPORT_ALIAS_MSG | outside the subset: an `import ... as ...` alias arrives with cross-file resolution -- a C | 2 |
| novac/src/check/rules.nv:394 | ATTR_MSG | outside the subset: an effect attribute is read but not compiled yet | 2 |
| novac/src/check/rules.nv:397 | UNSAFE_MSG | outside the subset: an `unsafe` block is read but not compiled yet | 2 |
| novac/src/check/rules.nv:429 | VALUE_MUT_PARAM_MSG | outside the subset: a `mut` parameter of a `value` record needs the V* lowering (the fixed | 2 |
| novac/src/check/rules.nv:436 | VARIADIC_MSG | outside the subset: a variadic parameter `...args []T` arrives with generics (E2-b) | 2 |
| novac/src/check/rules.nv:450 | DEFAULT_EXPR_MSG | outside the subset: a default parameter value is a number or a boolean here (D102 evaluate | 2 |
| novac/src/check/rules.nv:566 | RANGE_MSG | outside the subset: a range is a `for` head here, not a value (E2-b) | 2 |
| novac/src/check/rules.nv:581 | FOR_HEAD_MSG | outside the subset: the head of a `for` is a range `lo..hi` here; a collection head arrive | 2 |
| novac/src/check/rules.nv:804 | GENERIC_BOUND_MSG | outside the subset: a bound on a type parameter dispatches through a protocol (D122), and  | 2 |
| novac/src/check/rules.nv:807 | GENERIC_CONSUME_MSG | outside the subset: a generic over a must-consume type (`[T consume]`) needs linearity, an | 2 |
| novac/src/check/strings.nv:95 | ESCAPE_MSG | outside the subset: this string escape is not compiled yet (the subset knows only \\\" \\\ | 2 |
| novac/src/check/binds.nv:46 | <inline> | outside the subset: a binding needs a name and an initializer | 1 |
| novac/src/check/binds.nv:312 | <inline> | outside the subset: a type applied to arguments is read but not compiled yet | 1 |
| novac/src/check/binds.nv:346 | <inline> | outside the subset: indexing is compiled for `[]T` only -- this receiver is not an applied | 1 |
| novac/src/check/binds.nv:352 | <inline> | outside the subset: indexing is compiled for a ONE-argument application (`[]T`), and this  | 1 |
| novac/src/check/binds.nv:369 | <inline> | outside the subset: only an `int` index is compiled yet -- the language allows other integ | 1 |
| novac/src/check/binds.nv:389 | <inline> | outside the subset: indexing is read but not compiled yet (E2-b) -- the interop shell carr | 1 |
| novac/src/check/binds.nv:408 | <inline> | outside the subset: a typed array literal is written `[]T.${LITERAL_CTOR}(...)` -- this co | 1 |
| novac/src/check/binds.nv:417 | <inline> | outside the subset: an empty array literal -- the oracle panics on it at run time (`${LITE | 1 |
| novac/src/check/binds.nv:424 | <inline> | outside the subset: a spread `...xs` inside an array literal is not compiled yet | 1 |
| novac/src/check/binds.nv:427 | <inline> | outside the subset: an array literal takes positional elements only | 1 |
| novac/src/check/binds.nv:438 | <inline> | outside the subset: novac does not know this element type yet | 1 |
| novac/src/check/binds.nv:455 | <inline> | outside the subset: element ${i + 1} is ${ty_source_name(@ctx, et)}, but the literal's ele | 1 |
| novac/src/check/binds.nv:464 | <inline> | outside the subset: the slice head `${SLICE_HEAD}` is not among the declarations novac was | 1 |
| novac/src/check/binds.nv:475 | <inline> | outside the subset: the shell novac links into carries no `${SLICE_HEAD}` instance for ${t | 1 |
| novac/src/check/binds.nv:516 | METHOD_DECL_MSG | outside the subset: novac does not declare methods yet (the form is read from std declarat | 1 |
| novac/src/check/calls.nv:58 | <inline> | outside the subset: this name is not a callable novac knows | 1 |
| novac/src/check/calls.nv:90 | <inline> | outside the subset: no function of this name takes this many arguments | 1 |
| novac/src/check/calls.nv:95 | <inline> | outside the subset: no overload of this name accepts these argument types | 1 |
| novac/src/check/calls.nv:135 | <inline> | outside the subset: this argument coerces through a `#coerce` method whose body novac does | 1 |
| novac/src/check/calls.nv:139 | <inline> | outside the subset: this argument reaches the parameter only through a declared `#coerce`  | 1 |
| novac/src/check/calls.nv:142 | <inline> | outside the subset: this argument's type is not the one this function declares here | 1 |
| novac/src/check/calls.nv:321 | <inline> | outside the subset: this name is not a callable novac knows | 1 |
| novac/src/check/calls.nv:356 | <inline> | outside the subset: a type in call position constructs a value, and novac compiles that on | 1 |
| novac/src/check/calls.nv:364 | <inline> | outside the subset: constructing a newtype takes exactly one argument -- its representatio | 1 |
| novac/src/check/calls.nv:371 | <inline> | outside the subset: this argument is not the newtype's representation | 1 |
| novac/src/check/calls.nv:500 | <inline> | outside the subset: argument ${clash_at + 1} is ${ty_source_name(@ctx, key[clash_at].type_ | 1 |
| novac/src/check/calls.nv:505 | <inline> | outside the subset: no argument fixes this type parameter -- novac deduces from arguments  | 1 |
| novac/src/check/calls.nv:528 | <inline> | outside the subset: type arguments on a function that declares none | 1 |
| novac/src/check/calls.nv:555 | <inline> | outside the subset: the call gives a different number of type arguments than the declarati | 1 |
| novac/src/check/calls.nv:575 | <inline> | outside the subset: a type argument inside a generic body is not resolved -- novac compose | 1 |
| novac/src/check/calls.nv:577 | <inline> | outside the subset: a type argument names a type novac does not know | 1 |
| novac/src/check/calls.nv:597 | <inline> | outside the subset: ${ty_source_name(@ctx, ids[slot])} does not satisfy `${bound}` -- miss | 1 |
| novac/src/check/calls.nv:612 | <inline> | outside the subset: this argument's type is not the one the instance declares under these  | 1 |
| novac/src/check/casts.nv:55 | <inline> | outside the subset: a cast to a type novac does not know | 1 |
| novac/src/check/exprs.nv:164 | <inline> | outside the subset: a call on a TYPE (`Type.method(...)`) is compiled only as a binding in | 1 |
| novac/src/check/exprs.nv:198 | <inline> | outside the subset: a type name takes a variant after the dot, and this is not one of its  | 1 |
| novac/src/check/exprs.nv:475 | <inline> | outside the subset: a field access needs a name after the dot | 1 |
| novac/src/check/exprs.nv:562 | <inline> | outside the subset: a method call needs a name after the dot | 1 |
| novac/src/check/exprs.nv:631 | <inline> | outside the subset: this name is not a callable novac knows in an expression (println is a | 1 |
| novac/src/check/exprs.nv:691 | <inline> | outside the subset: an argument's type is not decided yet, so this overloaded function is  | 1 |
| novac/src/check/exprs.nv:713 | <inline> | outside the subset: a constant is a value, and a value is not callable | 1 |
| novac/src/check/match_arms.nv:39 | <inline> | outside the subset: a match needs a scrutinee | 1 |
| novac/src/check/match_arms.nv:62 | <inline> | outside the subset: a match arm needs a pattern and a value | 1 |
| novac/src/check/match_arms.nv:86 | <inline> | outside the subset: a match needs a scrutinee | 1 |
| novac/src/check/match_arms.nv:265 | <inline> | outside the subset: a match arm needs a pattern | 1 |
| novac/src/check/match_arms.nv:355 | <inline> | outside the subset: a match arm needs a pattern and a value | 1 |
| novac/src/check/methods.nv:128 | <inline> | outside the subset: this type has no such method in the declarations novac was handed | 1 |
| novac/src/check/methods.nv:133 | <inline> | outside the subset: no overload of this method accepts these argument types | 1 |
| novac/src/check/methods.nv:172 | <inline> | outside the subset: an argument's type is not decided yet, so this overloaded method is no | 1 |
| novac/src/check/methods.nv:189 | <inline> | outside the subset: novac read this method's signature but compiles no body for it yet | 1 |
| novac/src/check/methods.nv:219 | <inline> | outside the subset: a generic method declared in this file -- mono instantiates functions, | 1 |
| novac/src/check/methods.nv:224 | <inline> | outside the subset: the shell novac links into carries no `${t.text}` for ${ty_source_name | 1 |
| novac/src/check/methods.nv:237 | <inline> | outside the subset: this method returns `${ty_source_name(@ctx, ret)}` -- an instance of a | 1 |
| novac/src/check/operators.nv:67 | <inline> | outside the subset: `==` on `str` takes a `str` right-hand side, and this operand's type i | 1 |
| novac/src/check/operators.nv:78 | <inline> | outside the subset: an operand's type is not decided yet, so the `${EQUAL_METHOD}` overloa | 1 |
| novac/src/check/operators.nv:86 | <inline> | outside the subset: `==` on `str` needs the `${EQUAL_METHOD}` method, and it is not among  | 1 |
| novac/src/check/operators.nv:100 | <inline> | outside the subset: `==` on `${ty_source_name(@ctx, lhs_t)}` needs `${sym}`, and the shell | 1 |
| novac/src/check/operators.nv:198 | <inline> | outside the subset: this operator on these operands dispatches through a protocol (D46: @p | 1 |
| novac/src/check/operators.nv:254 | <inline> | outside the subset: this prefix operator on this operand dispatches through a protocol (D4 | 1 |
| novac/src/check/rules.nv:350 | SELF_FIELD_MSG | outside the subset: `@field` arrives with methods | 1 |
| novac/src/check/rules.nv:359 | INTERP_MSG | outside the subset: only a bare name is compiled inside an interpolation yet (E2-b3) -- ch | 1 |
| novac/src/check/rules.nv:367 | IMPORT_MSG | outside the subset: `import`/`use` arrive with modules (E3) | 1 |
| novac/src/check/rules.nv:373 | EXPORT_MSG | outside the subset: `export` arrives with modules (E3) | 1 |
| novac/src/check/rules.nv:400 | TEST_MSG | outside the subset: a `test` block arrives with the test runner | 1 |
| novac/src/check/rules.nv:615 | <inline> | outside the subset: the head of a for must be a range `lo..hi`, not another binary operato | 1 |
| novac/src/check/rules.nv:754 | <inline> | outside the subset: an `extern` declaration is read, but novac emits no FFI call yet | 1 |
| novac/src/check/rules.nv:895 | <inline> | outside the subset: no such method in the declarations handed to novac | 1 |
| novac/src/check/typing.nv:156 | <inline> | outside the subset: a fn declaration needs a name | 1 |
| novac/src/check/typing.nv:199 | <inline> | outside the subset: a fn declaration needs a name | 1 |
| novac/src/check/typing.nv:399 | <inline> | outside the subset: a tail `if` needs an `else` to be a value (E2-b) | 1 |
| novac/src/check/typing.nv:415 | <inline> | outside the subset: the branches of a tail `if` must agree on a type (E2-b) | 1 |
| novac/src/check/typing.nv:434 | <inline> | outside the subset: an `else if` chain is not compiled as a value yet (E2-b) | 1 |
| novac/src/check/typing.nv:452 | <inline> | outside the subset: a branch of a tail `if` ends without a value (E2-b) | 1 |
| novac/src/check/typing.nv:590 | <inline> | outside the subset: this type is generic -- write its type arguments (for `Vec` the langua | 1 |
| novac/src/check/typing.nv:592 | <inline> | outside the subset: novac does not know this type yet (its universe is int, str, bool, f64 | 1 |
| novac/src/check/typing.nv:682 | <inline> | outside the subset: `return` must carry a value | 1 |
| novac/src/check/typing.nv:890 | <inline> | outside the subset: println covers int, str, bool and f64 today | 1 |
| novac/src/emit_c/emit_c.nv:731 | <inline> | emit: statement kind outside the subset | 1 |
| novac/src/emit_c/emit_expr.nv:348 | <inline> | emit_c: literal kind outside the subset | 1 |
| novac/src/emit_c/emit_expr.nv:733 | <inline> | emit: expression kind outside the subset | 1 |

Примечания к А:
- «мест вызова» для именованных констант -- число вхождений имени константы по
  `novac/src/**/*.nv` (grep `\bNAME\b`), включая саму строку определения.
- «мест вызова» для `<inline>` -- 1 (литерал написан прямо в месте отказа, имени
  для повторного грепа нет).
- Исключены комментарии (`check/check.nv:545`, `check/typing.nv:783`,
  `check/calls.nv:157`, `check/rules.nv:620`, `check/literal_rules.nv:396`) и файлы
  `*_test.nv` (`diag/diag_test.nv:14`, `pipeline/subset_test.nv:191,220`,
  `pipeline/handed_sum_test.nv:48`).
- Шесть именованных констант дают только 1 совпадение (только определение, без
  видимого места вызова по имени в `.nv`-исходниках сейчас): `SELF_FIELD_MSG`,
  `INTERP_MSG`, `IMPORT_MSG`, `EXPORT_MSG`, `TEST_MSG`, `METHOD_DECL_MSG`.

## И.2. Инвентарь Б — хардкоды ширины и каста в эмиттере (тем же снимком)

Инвентарь Б -- хардкоды ширины/каста в `novac/src/emit_c/*.nv` (без `*_test.nv`,
без комментариев). Отсортировано по файлу и номеру строки.

| file:line | matched substring(s) | line (truncated to 110 chars) |
|---|---|---|
| novac/src/emit_c/emit_c.nv:384 | prims.int_id | `        if !has_expr_body && ret == @ctx.prims.int_id {` |
| novac/src/emit_c/emit_c.nv:385 | (nova_int) | `            @body.append("    return (nova_int)0LL;\n")` |
| novac/src/emit_c/emit_c.nv:705 | nova_int  | `        @body.append("    for (nova_int ${v} = ")` |
| novac/src/emit_c/emit_expr.nv:134 | prims.str_id | `    if t == @ctx.prims.str_id { return "nova_print_str" }` |
| novac/src/emit_c/emit_expr.nv:135 | prims.bool_id | `    if t == @ctx.prims.bool_id { return "nova_print_bool" }` |
| novac/src/emit_c/emit_expr.nv:144 | nova_print_int | `            return "nova_print_int"` |
| novac/src/emit_c/emit_expr.nv:305 | prims.int_id; prims.f64_id | `            ro own_ty = if lk == TokenKind.FloatLit { @ctx.prims.f64_id } else { @ctx.prims.int_id }` |
| novac/src/emit_c/emit_expr.nv:680 | (nova_int) | `            @body.append("((nova_int)(")` |
| novac/src/emit_c/emit_expr.nv:707 | (nova_int) | `            @body.append("((nova_int)((")` |
| novac/src/emit_c/emit_for.nv:54 | nova_int  | `    @body.append("    for (nova_int ${itmp} = 0; ${itmp} < ${len_c}(${vtmp}); ${itmp}++) {\n")` |
| novac/src/emit_c/emit_interp.nv:60 | prims.str_id | `            if st == @ctx.prims.str_id {` |
| novac/src/emit_c/emit_interp.nv:63 | (nova_int); LL) | `                @body.append("), ((nova_int)0LL), false, ((nova_int)0LL), nova_make_Align_Left(), (nova_char)(` |
| novac/src/emit_c/emit_interp.nv:66 | (nova_int); int_display_spec | `                @body.append("    nova_fn_7runtime14string_builder16int_display_spec(${sb}, (nova_int)(")` |
| novac/src/emit_c/emit_interp.nv:68 | (nova_int); LL) | `                @body.append("), ((nova_int)0LL), ((nova_int)10LL), false, false, false, false, nova_make_Alig` |
| novac/src/emit_c/emit_match.nv:128 | LL) | `        @body.append("LL)")` |

Примечания к Б:
- Файлы `emit_c/shell.nv`, `emit_c/emit_decls.nv`, `emit_c/emit_destructure.nv`,
  `emit_c/emit_iflet.nv`, `emit_c/emit_requires.nv` дали ноль совпадений по
  заданным подстрокам.
- Подстрока `ULL)` нигде не встретилась вне комментария (`emit_expr.nv:302`,
  исключён).
- Две строки (`emit_interp.nv:63,68`) обрезаны до 110 знаков по правилу формы.

ITOGO A: 116 konstant, 159 mest
ITOGO B: 15
