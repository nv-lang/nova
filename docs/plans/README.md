# Планы Nova

В этой директории — только **планы** (что и когда делаем). Справочные
материалы (таблицы сравнений, research-заметки, бенчмарки) живут в
[docs/research/](../research/).

## Схема нумерации

- `01-…`, `02-…` — главные планы по порядку создания.

## Текущие планы

| # | Файл | О чём | Статус |
|---|---|---|---|
| 01 | [01-roadmap-v0.1.md](01-roadmap-v0.1.md) | Roadmap разработки компилятора v0.1–v1.0+ | активный |
| 02 | [02-codegen-c-backend.md](02-codegen-c-backend.md) | C backend: компиляция Nova в нативный бинарь | активный |
| 03 | [03-package-ecosystem-roadmap.md](03-package-ecosystem-roadmap.md) | **Package ecosystem — roadmap-индекс.** Редакция 2: честное сравнение Cargo/npm/Go-mod/Deno/pip, Nova-уникальный угол (effect-aware зависимости — effect-surface dep видна, effect-diff как supply-chain сигнал, capability-confined deps), production security-модель (content-addressed, подпись + transparency log, `nova audit`). Декомпозирован на 03.1–03.6. Коррекция: self-hosting НЕ блокер экосистемы. | roadmap |
| 03.1 | [03.1-path-git-dependencies.md](03.1-path-git-dependencies.md) | `path`/`git`-зависимости в bootstrap-компиляторе + `nova.lock` (git commit + sha256, path-deps unpinned). Без registry/version-ranges/self-host — **можно делать сейчас**. Межпакетный резолв + cross-package cycle detection, минимальный `nova add`/`update`. Разблокирует multi-package разработку до v2.0. | 📋 proposed 2026-05-22, не начат, **P3** (near-term) |
| 04 | [04-buffer-split-and-external.md](04-buffer-split-and-external.md) | Buffer → StringBuilder/WriteBuffer/ReadBuffer + `external` keyword | ✅ выполнено (Buffer удалён из языка) |
| 05 | [05-as-cast-codegen.md](05-as-cast-codegen.md) | `as`-cast — реализация narrowing в codegen (D54 compliance) | ✅ выполнено |
| 06 | [06-iter-protocol-codegen.md](06-iter-protocol-codegen.md) | `Iter[T]` protocol в codegen — общий for-in (D58 compliance) | ✅ выполнено |
| 07 | [07-as-cast-saturation.md](07-as-cast-saturation.md) | `as`-cast saturation для float→int + spec D54 narrowing semantics | ✅ выполнено |
| 08 | [08-from-into-conversions.md](08-from-into-conversions.md) | `From`/`Into` framework + char/byte/bool + strict if-cond + conversions.md | ✅ почти выполнено, Ф.6 отложен |
| 09 | [09-clang-migration.md](09-clang-migration.md) | Миграция Windows-сборки с MSVC на Clang/LLVM (10-15% perf) | ✅ Ф.1-Ф.5 закрыты 2026-05-11 (Clang — default toolchain: `Auto` = Clang > MSVC > GCC); Ф.6 бенчмарки отложены |
| 10 | [10-pgo-integration.md](10-pgo-integration.md) | PGO integration (stub, после плана 09) — 15-30% perf на hot path | stub / future |
| 11 | [11-method-values-and-overload.md](11-method-values-and-overload.md) | Method values + overload по типу аргумента (закрывает Q-overloading вариант 1) | ✅ ЗАКРЫТ (Ф.8 sweep std — optional, не блокер) |
| 12 | [12-builtins-driven-codegen.md](12-builtins-driven-codegen.md) | builtins.nv-driven external dispatch (Q-codegen-builtins-cleanup) | ✅ ЗАКРЫТ (Ф.6 type-checker gate отложен; Ф.4.5 auto-derive ❌ ОТМЕНЕНО Plan 13 Ф.9.5) |
| 13 | [13-runtime-stdlib-and-autogen.md](13-runtime-stdlib-and-autogen.md) | Runtime stdlib (str/math) + auto-gen std/runtime/*.nv (read-only projection реестра компилятора) | ✅ ЗАКРЫТ (включая Ф.9.2 `+` через `@plus` body + Ф.9.6 bag-fix `StringBuilder.@len` codepoints); user-defined `@plus` routing отложен до method_overloads expansion |
| 14 | [14-stdlib-codegen-gaps.md](14-stdlib-codegen-gaps.md) | Закрыть codegen-gap'ы блокирующие std/* (Iter[T] element-type, const non-trivial, free-fn-as-value, fn-в-record, D69 variadic, `int as char` literal) | ✅ **ЗАКРЫТ 2026-05-12** (Ф.1/2/3/4/6/6-bis/7); Ф.5 → [Plan 35](35-cross-file-resolve.md); type-check регресс → [Plan 34](34-stdlib-typecheck-and-compile-fix.md) |
| 15 | [15-generic-bounds-enforcement.md](15-generic-bounds-enforcement.md) | D72 generic bounds `[T Protocol]` enforcement в type-checker'е | ✅ ЗАКРЫТ (Ф.1-Ф.5; Ф.4 закрыта 2026-05-11 — 3 negative + forward-dep positive; D53 anonymous-protocol literals — отдельная задача) |
| 16 | [16-capability-enforcement.md](16-capability-enforcement.md) | D63 `forbid` + D64 `realtime` compile-time capability checks (закрытие spec-vs-impl drift) | ✅ ЗАКРЫТ (Ф.1-Ф.9 ✅; 97/97 PASS включая 5 negative-tests) |
| 17 | [17-q-resolutions.md](17-q-resolutions.md) | Закрыть полу-открытые Q (string interpolation, clone semantics, array API, и др.) | ✅ ЗАКРЫТ (включая Ф.4 — string interpolation полная реализация: lexer/parser/AST/codegen StringBuilder/interp; 13 regression-тестов) |
| 18 | [18-stdlib-roadmap.md](18-stdlib-roadmap.md) | Stdlib gap-анализ Rust/Go → Nova: P0/P1/P2 приоритизация под backend/CLI нишу + зафиксированные дизайн-решения (libuv, rustls, M:N-aware sync) | proposal, не начат |
| 19 | [19-closure-and-error-ops.md](19-closure-and-error-ops.md) | Closure-rev + D85 error-ops (`expr!!` / `expr??` унифицированное поведение для Result/Option, throw-стиль). | ✅ ЗАКРЫТ 2026-05-10 |
| 20 | [20-defer-implementation.md](20-defer-implementation.md) | D90 implementation: `defer` и `errdefer` scope-level cleanup statements | ✅ ЗАКРЫТ (лексер/парсер/AST/codegen LIFO defer-scope; ~27 тестов: syntax + expected_runtime panic-interaction + negative; все в полном прогоне) |
| 21 | [21-channel-revision-implementation.md](21-channel-revision-implementation.md) | D91 implementation: Channel revision на capability-split (Rust mpsc-style); зависит от Plan 20 | ✅ ЗАКРЫТ (capability-split `ChanWriter`/`ChanReader`, negative-тесты sender-no-recv / receiver-no-send проходят) |
| 22 | [22-sleep-libuv-integration.md](22-sleep-libuv-integration.md) | `Time.sleep` через libuv `uv_timer_t` + унифицированный event-loop в scheduler'е; D92 implicit main-scope + D93 park/wake API. Открывает дорогу для Plan 18 P0 stdlib и Plan 21 Channel revision. | ✅ ЗАКРЫТ (Ф.1-Ф.6 ✅; 134/134 PASS + 5 sleep + 2 bench) |
| 24 | [24-cross-platform-test-runner.md](24-cross-platform-test-runner.md) | `nova-codegen test-build`/`test-all` subcommands — единая логика runner'а в Rust, run_tests.ps1/.sh thin wrappers | ✅ ЗАКРЫТ (Ф.1-Ф.3 ✅; Linux smoke — отдельно) |
| 25 | [25-production-readiness-roadmap.md](25-production-readiness-roadmap.md) | Honest gap analysis vs Go/Rust: gaps (M:N, growable stacks, **G3a default malloc-only**, G3b GC pauses, Linux smoke, preemption, cancel propagation) с blockers и acceptance criteria. G7 ✅ closed (Ф.8). | roadmap, не начат |
| 26 | [26-test-runner-hardening.md](26-test-runner-hardening.md) | Test-runner hardening до cargo/go-test уровня: per-test timeout, parallel execution (`--jobs`), structured output (JSON/TAP/JUnit), UTF-8 codepage, verbose/quiet, `--rerun-failed`, wrapper cleanup. Plan 24 был 80% prod-grade, Plan 26 — 95%. Ф.5 caching отложен (не блокирует CI). | ✅ ЗАКРЫТ 2026-05-11 (10/11 фаз) |
| 27 | [27-gc-switch.md](27-gc-switch.md) | **GC switch: Boehm как default** (Plan 25 G3a closer). vcpkg gc.lib уже vendored. Ф.1 add --gc flag → Ф.4 switch к Boehm после bench. **Production blocker для long-running workloads.** | ✅ почти закрыт (Ф.1/1.5/2/3/4/6 ✅ Б.2–Б.8 ✅ Ф.D/G audit-fixes ✅ — detect_boehm graceful fallback с Linux/macOS hints, pause measurement в spec/overview.md; Ф.5 Linux smoke-test ждёт CI) |
| 28 | [28-nova-cli.md](28-nova-cli.md) | `nova` CLI binary — единая точка входа для пользователя (`nova test/build/run/check/regen-runtime`). Заменяет run_tests.ps1/.sh и regen_runtime.ps1. nova-codegen остаётся внутренним инструментом. | ✅ ЗАКРЫТ (Ф.0-7 ✅; nova-cli/ crate, все субкоманды, скрипты удалены) |
| 29 | [29-repo-layout.md](29-repo-layout.md) | Реорганизация корня: `compiler-codegen/` → `compiler/`, `nova-cli/` → `cli/`. Nova-пакеты (nova_tests/, std/) не переименовываются — D78 риск. | план, не начат, низкий приоритет |
| 30 | [30-channel-improvements.md](30-channel-improvements.md) | Channel improvements: `send`→`bool` (no panic on closed) + multi-writer (`tx.clone()` + `writer_count` ref-count). Зависит от Plan 21. | ✅ ЗАКРЫТ (2026-05-11; статус в самом плане-доке) |
| 31 | [31-channel-select.md](31-channel-select.md) | `select` — multiplexed channel receive: ожидание на N каналах одновременно, пробуждение по первому готовому. Парсер + runtime SelectWaiter + codegen. | ✅ ЗАКРЫТ (Ф.1-6 ✅; all-closed panic реализован; 179/179 PASS после D97 fix memory_footprint) |
| 32 | [32-gc-introspection.md](32-gc-introspection.md) | `std.runtime.gc` introspection API (`heap_size`, `live_count`, `alloc_count`, `collect`, `reset_stats`) для differential GC-тестов и production memory pressure observability. | ✅ ЗАКРЫТ (Ф.1-Ф.5 ✅; 181/181 PASS malloc+boehm; differential: heap bounded 348 KB под Boehm на 500k allocs; no-leak: max_growth=0 на 20 циклов × 10k allocs; pause < 16ms на all workloads) |
| 33 | [33-contracts-implementation.md](33-contracts-implementation.md) | D24 контракты — **roadmap-индекс**. Разбит на 33.1 / 33.2 / 33.3 с decision point после каждого. Цель — паритет с Dafny/Creusot, превосходство над Rust+kani/prusti (где tool отдельный, не часть языка). Закрывает Q-contract-dsl и Q-pure-view. | roadmap |
| 33.1 | [33.1-contracts-core.md](33.1-contracts-core.md) | **Core:** `requires`/`ensures`/`old`/`result` + `ContractResult[R,E]` + Z3 SMT + debug runtime fallback + zero-cost release CI gate. | ✅ **V1 ЗАКРЫТ 2026-05-13**: Ф.1-Ф.4 + TrivialBackend SMT + Z3 backend (Cargo feature `z3-backend`, vcpkg libz3 x64-windows-static, env `NOVA_SMT_BACKEND=z3`, own FFI bindings) + `// REQUIRES_SMT_BACKEND` test-runner gate + zero-cost release. Strings/FP/quantifiers — Plan 33.3 advanced milestone. |
| 33.2 | [33.2-contracts-imperative.md](33.2-contracts-imperative.md) | **Imperative:** `reads`/`modifies` frame + loop invariants + `decreases`/termination + composition `#pure` функций + record `invariant` + arrays в SMT + `assert_static`. | ✅ **ЗАКРЫТ bootstrap 2026-05-12**: Ф.5 (frame parser+type-check) + Ф.6 (loop invariants parser) + Ф.7 (decreases+composition+invariant) + Ф.8 (assert_static). SMT verify loops/frame — после Z3. |
| 33.3 | [33.3-contracts-advanced.md](33.3-contracts-advanced.md) | **Advanced (Dafny-parity):** `pure_view`+`axiom`+`#verify_handler` + ghost state + `assume` + bounded quantifiers + IEEE 754 FP + strings/sets/maps теории + incremental cache + parallel + Z3↔CVC5 cross-check + `#must_verify_module` + `#trusted` external + Dafny-tutorial port (20 примеров). | ✅ **ЗАКРЫТ 2026-05-16**: Ф.10 (ghost full codegen+assume+quantifiers) + Ф.11 (IEEE 754 FP+Seq strings в Z3) + Ф.12 (incremental FNV1a cache) + Ф.13 (#must_verify_module + #trusted + nova contracts JSON CLI) + Ф.14 (20 Dafny-parity тестов). 115 PASS 0 FAIL. **Аудит Plan 33.8 (2026-05-21):** parallel verification и Z3↔CVC5 cross-check из исходного scope 33.3 НЕ доставлены — перенесены в роадмап-слоты 33.14/33.15 (никогда не написаны как планы). |
| 33.4 | [33.4-contracts-parity-gaps.md](33.4-contracts-parity-gaps.md) | **Parity gaps closure:** D.0.2 composition + D.0.3 loop invariants AST + D.0.4 decreases WF + D.1.2 frame axiom + D.1.3 quantifiers + D.1.4 trigger stub + D.1.5 ensures_fail + P0-1 V1 handler verify (static axioms) + P1-5 BinderType + P1-6 spec sync. P1-4 Liskov blocked (→ Plan 33.5). | ✅ **ЗАКРЫТ 2026-05-15** (autonomous session; 72/72 contracts/ PASS, 9 SKIP Z3-only) |
| 33.5 | [33.5-contracts-verifier-production.md](33.5-contracts-verifier-production.md) | **Verifier production hardening:** ghost erasure + trigger patterns proper + loop decreases SMT + loop havoc/preservation + purity SCC inference + lemma functions + calc proofs + Liskov V1 + post(Action)(view) symbolic exec V2. Закрывает все оставшиеся V1-упрощения; добавляет lemma/calc для Dafny-parity и symbolic-handler-exec для уникальной Nova-фичи. | ✅ **ЗАКРЫТ** (аудит 2026-05-20): Ф.1.2 triggers + Ф.1.3 loop decreases + Ф.2 havoc + Ф.3 SCC + Ф.4.1 lemma + Ф.4.2 calc + Ф.5.1 EffectMethod contracts + Ф.5.2 Liskov + Ф.6 post(Action) V2 ✅; Ф.1.1 ghost erasure закрыт в Plan 33.6 (compile-error «ghost variable cannot be read in non-ghost code», soundness guard ghost_runtime_use_fail). Аудит: contracts/ suite 239 PASS trivial / 275 PASS z3 / 0 FAIL. |
| 33.6 | [33.6-contracts-production-hardening.md](33.6-contracts-production-hardening.md) | **Production hardening:** soundness honesty (ghost erasure V5 closure + non-silent Unsupported + handler Unknown = error + soundness regression guards) + handler_exec.rs split + AI-first diagnostics (counterexample minimisation + witness extraction + suggest fixes + `#proof_budget` + apply auto-inference + calc pinpoint + trigger ranking + variance inference) + `#must_verify_module` + `#trusted external fn` + subsumption cache + Z3 CI matrix + entry-snapshot для `old()` + V2/V3 positive tests. Big initiatives (bit-vectors / refinement types / opaque-reveal / sets-maps / FP / strings / cross-check / parallel / LSP / AI-loop / distributed / Dafny-port / HOF) — Plan 33.7-33.20. | ✅ **ЗАКРЫТ** (аудит 2026-05-20): Ф.1.1-Ф.1.5 + Ф.2.1 + Ф.3.1-Ф.3.8 + Ф.4.1-Ф.4.2 + Ф.5.1-Ф.5.4 + Ф.6 (25 спринтов TrivialBackend bound coverage, 24 pipeline lints, soundness fix Ф.46 E2408). Последний пункт Ф.3.8 variance inference закрыт аудитом (handler_exec.rs `infer_param_variance` + liskov_variance_diag_fail.nv). **239 PASS trivial / 276 PASS z3 / 0 FAIL**. |
| 33.7 | [33.7-bitvector-overflow.md](33.7-bitvector-overflow.md) | **Bit-vectors + integer overflow theory.** Sized integer типы (`u8`/`u16`/`u32`/`u64`/`i8`/`i16`/`i32`) → `BitVec N` Z3-sort (точная wrap-around семантика); битовые операции `&`/`|`/`^`/`<<`/`>>` → SMT bitvector-теория; атрибут `#nooverflow` генерирует overflow VC для каждой арифм. операции (недоказуемый → compile error). `int` остаётся unbounded Z3 Int. V1: Ф.1 SMT-IR, Ф.2 Z3 FFI, Ф.3 encode+pipeline, Ф.4 #nooverflow, Ф.5 тесты. V2: точная знаковость (bvsdiv/bvslt для i*, bvudiv/bvult для u*), BV cast resize (zero/sign-extend/extract), overflow VC для блочных тел. | ✅ **ЗАКРЫТ полностью (V1 + V2)** (V1 — 2026-05-20, V2 — 2026-05-21): V1 — SortRef::BitVec/BitVecLit + Z3 FFI (bvadd/bvsub/bvmul/bitwise/comparisons/overflow predicates) + encode BV dispatch + `#nooverflow`; V2 — `SortRef::BitVec { width, signed }` (i8/i16/i32 signed), `as`-cast resize через zero_extend/sign_extend/extract, overflow VC для let-bindings/блоков через BvScope subst. 9 фикстур f60_*/f61_* (7 positive + 2 negative). **285 PASS / 0 FAIL** (z3 backend). |
| 33.8 | [33.8-verifier-soundness.md](33.8-verifier-soundness.md) | **Verifier soundness hardening.** Создан по результатам аудита «с чистого листа» при закрытии 33.7 — найдены 3 SOUNDNESS-CRITICAL дыры (верификатор объявлял контракт «доказан», хотя в рантайме он мог быть ложным). Ф.1 — переполнение `int` (codegen эмитил голый C `+` = UB/wrap, верификатор кодировал безграничным Int → `result==a+b` ложно-доказывался): теперь `int` overflow → `panic` (`__builtin_*_overflow` → `nv_panic`), что делает безграничную кодировку sound; `nat ≥ 0` аксиома. Ф.2 — havoc-модель сохранения инварианта цикла не покрывала составные/вложенные присваивания → ложный `Proven`: fail-safe `Warning`. Ф.3 — `assume` lint `trust-introduced`. **Слот 33.8 переиспользован** под soundness (роадмап 33.6 предлагал «refinement types» — спекулятивно, не написан). | ✅ **soundness ЗАКРЫТ (2026-05-21)**: Ф.1 (int panic + nat≥0) ✅, Ф.2 (loop fail-safe) ✅, Ф.3 (assume lint) ✅. Полный `nova_tests`: 931 PASS / 0 FAIL; contracts: 291 PASS / 0 FAIL. V2-остаток (НЕ soundness): Ф.1.3 overflow-VC оптимизация, Ф.2.2 моделирование условных присваиваний, `assume` SMT-интеграция. |
| 33.9 | [33.9-opaque-reveal-fuel.md](33.9-opaque-reveal-fuel.md) | **Opaque/reveal/fuel** для matching-loops control при scaling до 50+ контрактов. `#opaque #pure fn` (body как UF в SMT), `reveal name` statement (раскрывает body в caller scope), `#fuel(n)` (controlled unfolding depth для recursive). Standard Dafny/Verus pattern. Phased: V1 parser+AST+TrivialBackend gate+lints (2 дня), V2 Z3 axiomatic encoding+fuel chain (2 дня research), V3 spec sync (0.5 дня). 5 pipeline lints (reveal-on-non-opaque, dead-opaque, dup-reveal, fuel-without-opaque, reveal-undef). | ✅ **ЗАКРЫТ 2026-05-20** (все фазы): Ф.1+Ф.2+Ф.3 parser/AST ✅ (`#opaque`, `reveal X`, `#fuel(N)`), Ф.4 ✅ TrivialBackend opaque-aware W2402 (term_references_opaque_uf + NotAttempted→UnsupportedTheory upgrade), Ф.5 ✅ Z3 axiomatic encoding + fuel chain, Ф.6 ✅ 5 pipeline lints (E2411 + 4× W2402/W2403), Ф.7 ✅ spec D130 + idiom doc. **239 PASS / 0 FAIL**. |
| 33.14 | [33.14-z3-cvc5-crosscheck.md](33.14-z3-cvc5-crosscheck.md) | **Z3↔CVC5 cross-check** — вторая линия защиты soundness (в дополнение к 33.8 Ф.7 regression-suite). Каждая VC прогоняется через два независимых пути — Z3 (FFI) и CVC5 (текстовый SMT-LIB подпроцесс); расхождение определённых ответов (`Proven` vs `Disproved`) → CI-fail. Ловит баги кодирования Nova→SMT (например 33.8 Ф.6.2 — Z3-FFI молча терял формулу) и баги решателя. **CI-only** — обычная компиляция не замедляется. Реанимирует обещание Plan 33.3 (было в scope, не доставлено). Ф.1 SMT-LIB эмиттер, Ф.2 CVC5 runner, Ф.3 cross-check режим, Ф.4 сравнение+diff, Ф.5 CI-gate, Ф.6 тесты. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.6; CVC5 через текстовый SMT-LIB, не FFI — второй независимый кодировщик; 47 unit-тестов; корпус 228 файлов — 0 расхождений; gate NOVA_CROSSCHECK=1 + workflow contracts-crosscheck) |
| 34 | [34-stdlib-typecheck-and-compile-fix.md](34-stdlib-typecheck-and-compile-fix.md) | stdlib type-check + compile fix. **Ф.1-3** type-check: 33/44 → 45/45 + `std.testing.handlers` + 4 parser-фикса. **Ф.5.1** `--skip` флаг (агент). **Ф.5.2** `int as char` refactor через `char.try_from` в 5 файлах. **Ф.7** production-grade: LCG → **xoshiro256++** + splitmix64 init + `seed u64` + bytes(n) 8 байт/advance + **`mut_clock(start_ms)`** (mutable virtual clock). **Followup #1**: `std/runtime/gc.nv` заполнен 5 external fn declarations. **Ф.8 followup #2**: spec sync (04-effects.md mut_clock + 08-runtime.md D26 signatures) + 8 focused tests в `nova_tests/plan34/` (4 positive: gc namespace, xoshiro/mut_clock inline reproducers; 4 negative EXPECT_COMPILE_ERROR markers для Ф.2 parser fixes). | ✅ **ЗАКРЫТ 2026-05-12** (Ф.1-8 ✅; 45/45 std type-check, 8/8 plan34 tests, 249/256 nova_tests — 7 fail'ов агента из Plan 44.1 ABI change) |
| 35 | [35-cross-file-resolve.md](35-cross-file-resolve.md) | Cross-file resolve для multi-file stdlib через codegen. **Bootstrap MVP закрыт 2026-05-12**: Ф.1 R1-R21 реализован, **R31** ✅ unified pipeline (shared `resolve_imports_inline` для nova check/build/test), **R3 cycle detection** ✅ (BFS → DFS с in_progress/visited множествами), **Sub-plan 35.A R26+R27** ✅ (selective `import X.{A, B}` + `export import X` + `std/prelude.nv` auto-import; R25 wildcard и R28 pub granularity ❌ spec-rejected D29/D5), **Sub-plan 35.B sync** ✅ (workspace-aware find_repo_root). Open: 35.B on-disk cache + incremental, 35.C cross-file generic bounds, 35.D stable mangling v0, 35.E cfg/editions, R26 type-checker visibility enforcement (post-bootstrap). | ✅ **bootstrap MVP ЗАКРЫТ 2026-05-12**; advanced sub-plans (35.B cache/35.C/D/E) — open → перенесены в [Plan 81](81-module-resolution-hardening.md) |
| 36 | [36-cli-production-hardening.md](36-cli-production-hardening.md) | Production hardening `nova check` / `nova test` — **R1-R30 + AD1-AD12** после **4-way audit** (160 gaps closed). MVP реализован (commits 0b9ec6ec7f + 62c04378fa): `nova check <path>` + `nova test <path>` polymorphic; exit codes 0/1/2/101 (1=diagnostic, 2=usage, 101=panic через std::panic::set_hook); Ф.0 correctness fix (cmd_check теперь дёргает infer_effects + lint_module); --color auto/always/never + NO_COLOR/CI/CLICOLOR/TERM=dumb auto-detect; parallel check = num_cpus с deterministic output. **Sub-plans 36.A-E** (post-MVP): outputs (JSON/SARIF/JUnit), caching (blake3), CI (pre-commit/GHA), ergonomics (--explain/completions), workspace. `--tests-dir` clean break. | **MVP ✅ ЗАКРЫТ** (Ф.0+Ф.1+R7+R10) |
| 37 | [37-typecheck-semantic-parity.md](37-typecheck-semantic-parity.md) | Type-checker semantic parity с codegen: перенести D54-проверки (запрещённые `as`-касты — 24 пары; strict bool в `if`/`while`) из `emit_c.rs` в type-checker. Сейчас `nova check` пропускает, `nova test`/`nova build` падает в codegen — нарушение контракта `nova check` (Plan 36 / D95) «полная type+lint валидация». Triggered by `std/encoding/hex.nv` (`int as char`). | план, не начат, средний приоритет |
| 38 | [38-numeric-type-constants.md](38-numeric-type-constants.md) | `int.MAX` / `f64.NAN` / `u8.MAX` / etc. — codegen mapping для D26-prelude numeric type constants. ~30 mappings (signed/unsigned int, char, f32/f64). `numeric_type_constant_mapping` static helper в emit_c.rs. `<float.h>` include в nova_rt.h. | ✅ ЗАКРЫТ (commit 64d1a41c1a, 18 sub-тестов в numeric_constants.nv PASS, 210/210 regression) |
| 39 | [39-range-stdlib-fixes.md](39-range-stdlib-fixes.md) | `std/collections/range.nv` stdlib fixes. **All 4 issues closed**: Issue D (b516bba9ae) Iter[T] D58 diagnostic + mut-receiver. Issue B/C already implemented. **Issue A** (handler-flow inference D61 §10): runtime NovaInterruptFrame.value_ptr + codegen category-routing (IntLike/Pointer/ValueStruct) + auto-gen Option-helpers. range.nv + handler_flow_inference.nv PASS. | ✅ **ЗАКРЫТ 2026-05-12** (все Issue A/B/C/D; 225/225 regression) |
| 42 | [42-folder-modules.md](42-folder-modules.md) | Folder-modules (Go-style peers): module = file `X.nv` ИЛИ folder `X/` с ≥1 peer-файлами. Все файлы folder объявляют одинаковый `module <path>` и share namespace. Закрывает «internal helpers» + scaling больших std модулей. Реализует **D29 rev-2 + rev-3** (parent.X declarations). MVP ✅ ЗАКРЫТ 2026-05-12. Sub-plans с числовой нумерацией (leading zeros для sort order): **42.01-42.16 ВСЕ закрыты** (42.02 → Plan 45; 42.03, 42.07 ❌ rejected). 42.15 — per-peer import isolation (Rule C strict); 42.16 — module-attrs перед `module` + `#cfg` операторы `\|\| && !`. | ✅ **ВСЕ sub-plans закрыты** (2026-05-14, 338 PASS) |
| 42.04 | [42.04-per-file-imports-scope.md](42.04-per-file-imports-scope.md) | Per-file imports scope refactor (Plan 42 правило C). `PeerFile` AST struct + FileId propagation через lexer/parser/imports + type-checker per-peer name resolution (`peer_module_names: HashMap<FileId, HashSet<String>>`). Позитивный тест `folder_per_file_imports/`. Rule C частично enforced (ограничение [M10] в simplifications.md). 278 PASS. | ✅ **ЗАКРЫТ 2026-05-14** (Шаги 1-3 + тесты) |
| 42.08 | [42.08-audit-closing.md](42.08-audit-closing.md) | Closing audit gaps Plan 42 (2026-05-14). Ф.1: 2 negative tests (folder_file_vs_folder_conflict, folder_cycle_between_modules; 3-й Test inconsistent_decl N/A — manifest rev-3 catches per-peer). Ф.2: ResolveErr enum + diagnostic для ambiguous X.nv+X/. Ф.3: Rule H comment terminology fix + [M11] simplifications entry для Rule A canonical PathBuf keying. 284 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.09 | [42.09-re-export.md](42.09-re-export.md) | `export import X.{A as B}` — re-export через `is_export` flag + item rename (Plan 42.09). Bootstrap MVP работает через inline expansion + selective list filtering. 2 tests (reexport_facade_use + rename_import_use). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.10 | [42.10-module-level-forbid.md](42.10-module-level-forbid.md) | Module-level `#forbid` через `_module.nv` convention. `_module.nv` attrs пропагируются всеми peers через `inherited_attrs` в resolve_one. CapabilityCtx применяет к compiled module. 2 tests (positive folder_module_forbid_use + negative module_forbid_violation). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.11 | [42.11-inline-module-doc.md](42.11-inline-module-doc.md) | `#doc "..."` attribute для module-level documentation. AST: `ModuleAttrKind::Doc(String)`. Multi-peer merge через Plan 42.10 inherited_attrs. Consumer — Plan 45 (nova doc). 1 test (folder_with_doc_use). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.13 | [42.13-internal-naming.md](42.13-internal-naming.md) | `internal/` extended naming (D29 rev-3.1). Single-file/folder-module внутри `internal/` → `owner.internal.target` (3 segments) вместо `internal.target` (collision). Edge case: `internal/` сама folder-module → `owner.internal` (2 segments). manifest.rs special-case ~35 LOC. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.12 | [42.12-cfg-conditional-compilation.md](42.12-cfg-conditional-compilation.md) | Conditional compilation (D99): filename suffix (`_windows.nv`, `_linux.nv`, `_macos.nv`, `_unix.nv`, `_posix.nv`) + module-level `#cfg(feature = "X")` / `#cfg(target_os = "Y")`. NOVA_TARGET_OS / NOVA_FEATURES env override. **Plan 18 P0 unblocked**. Predicates `any/all/not` + item-level `#cfg` добавлены в 42.14. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.14 | [42.14-backlog-closure.md](42.14-backlog-closure.md) | Backlog closure (6 deferred items). Ф.1 `#cfg(any/all/not)` recursive predicates. Ф.2 item-level `#cfg`. Ф.3 [M11] Rule A cycle detection по module-name (symlink-safe `read_module_decl`). Ф.5 Rule H fix через rev-3.1 consistency. Ф.4+Ф.6 → 42.15 (общий NameResCtx refactor). 328 PASS. | ✅ **ЗАКРЫТ 2026-05-14** (4/6; Ф.4+Ф.6 re-defer) |
| 42.15 | [42.15-per-peer-import-isolation.md](42.15-per-peer-import-isolation.md) | Per-peer / per-import isolation (NameResCtx refactor). Закрывает [M10] Rule C Path-form + [M12] selective import visible-scope. `PeerFile.imported_item_names` + `is_entry_module` + NameResCtx `group_decls` (per module-group) + `peer_imported_names` (per-peer imports). `export import` re-export propagation. 2 tests. 331 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.16 | [42.16-module-attr-syntax.md](42.16-module-attr-syntax.md) | Module-attr syntax: `#forbid`/`#cfg`/`#doc` ПЕРЕД `module` (консистентно с item-level) + `#cfg` операторы `\|\| && !` вместо `any/all/not` (precedence parser). Fix: module-scan helpers пропускают `#`-атрибуты. 13 файлов мигрированы. spec D99/D100/D101 update. 338 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.17 | [42.17-audit-closure.md](42.17-audit-closure.md) | Post-42.16 production-readiness audit для Plan 42 folder-modules: doc drift fix, dead code cleanup, Rule H implementation, module-attr handling. Ф.8 entry-folder-module isolation — honest-defer с полным дизайном в simplifications.md (not a bug). Закрывает spec/impl drift в Plan 42.04/42.09/42.14, `[M10]`/`[M12]` resolved для imported folder-modules. | ✅ ЗАКРЫТ 2026-05-14 (9 фаз; Ф.8 designed-defer) |
| 44 | [44-mn-runtime-roadmap.md](44-mn-runtime-roadmap.md) | M:N runtime — **umbrella roadmap** (thread pool + work-stealing + concurrent GC + TLS migration). Sub-plans 44.1–44.7 ниже. Был Plan 23 до 2026-05-13. | roadmap; sub-plans: 44.1/44.2/44.4/44.5/44.7 ✅, 44.3 🔒 blocked, concurrent GC — open (v1.0+) |
| 44.1 | [44.1-channel-hardening.md](44.1-channel-hardening.md) | M:N channels hardening (was Plan 40). production-parity с Go/Rust mpsc: atomics + mutex + doubly-linked + cache padding + 8 audit rounds. D91/D94 spec. | ✅ **ЗАКРЫТ** (Windows + Linux Docker validated) |
| 44.2 | [44.2-fiber-arena-posix.md](44.2-fiber-arena-posix.md) | M:N fiber arena POSIX (was Plan 41). Per-thread mmap + lazy commit + single Boehm GC root. Снимает MAX_ROOT_SETS=128. D97 spec. | ✅ **ЗАКРЫТ** (Этапы 1-5 + R8 audit + P41-3/P41-6) |
| 44.3 | [44.3-fiber-arena-windows.md](44.3-fiber-arena-windows.md) | M:N fiber arena Windows (was Plan 43). 4 неудачные попытки — root cause: Windows TIB tracks thread stack, minicoro MCO_USE_ASM не обновляет → SEH hangs. D97 disclosure. | 🔒 **fundamentally blocked** (Windows kernel constraint) |
| 44.4 | [44.4-mn-runtime-stage0.md](44.4-mn-runtime-stage0.md) | M:N runtime Этап 0 — infrastructure proof-of-concept: NovaWorker struct, runtime.init/shutdown, std.runtime.runtime API. (Был Plan 23.1, потом Plan 44.1, переименован для chronological order.) | ✅ **Этап 0 ЗАКРЫТ 2026-05-13** (266 PASS / 0 FAIL Windows; Linux Docker smoke PASS) |
| 44.5 | [44.5-work-stealing-scheduler.md](44.5-work-stealing-scheduler.md) | M:N work-stealing scheduler: Chase-Lev deque (L1) + worker integration (L2) + per-worker libuv loop (L3 — TLS) + Boehm GC_THREADS (L4) + actual workload distribution через codegen routing (L5) + D98 spec (L6). | ✅ **ЗАКРЫТ 2026-05-14** (L1–L6; Layer 5 park/wake worker integration — commit `8fcbc67fddb`, GC_THREADS на всех платформах) |
| 44.7 | [44.7-preemption.md](44.7-preemption.md) | Signal-free preemption: sysmon-thread + codegen safepoints (пролог функции + backedge цикла). CPU-bound fiber не монополизирует worker — кооперативный yield по флагу. Вариант B (не SIGURG — `mco_yield` не async-signal-safe). | ✅ **ЗАКРЫТ 2026-05-14** (sysmon + yielded-FIFO + uv_run-per-iter; `mn_runtime_preemption.nv` 4/4; D103 spec) |
| 45 | [45-nova-doc.md](45-nova-doc.md) | `nova doc` — production-grade documentation tooling. **MVP (Ф.0-Ф.20)**: D104-D107 spec + lexer `///`/`//!` + parser attach + DocModel + markdown/JSON renderer + intra-doc links + doc-tests + `--check`/`--watch` CLI + CI + 14 golden snapshots. **Production hardening (Ф.21-Ф.29)**: workspace mode + `--strict` + must_verify SMT wiring + handler matrix + capability badges + 25-pt Nova-unique audit + JSON Schema v1.0.0 STABLE + mutation testing. **HTML+search (Ф.31)**: single/multi-page + dark mode + sitemap + syntax highlighting (без external deps). **MCP/CLI (Ф.32-Ф.34)**: `nova doc-query` DSL + JSON-RPC stdio/HTTP MCP server + incremental mtime cache + nova.toml `[doc]` config. **stdlib doc-pass (Ф.35-Ф.36)**: ~258 items documented across duration/vec/json/path + 6 batches checksums/concurrency/identifiers/math/data/crypto/testing/bench/prelude + **batch 7** (collections×9 + snowflake + runtime×4, 14 файлов, коммит `743dcc709ab`). Породил Plan 60 / 61 / 62 как follow-ups. | ✅ **ЗАКРЫТ 2026-05-16** (Ф.0-Ф.36 + batch 7; branch `plan-45-doc` merged; 100+ golden tests PASS; JSON Schema v1.0.0 STABLE) |
| 46 | [46-named-parameters.md](46-named-parameters.md) | Именованные аргументы `f(name: val)` + значения параметров по умолчанию `fn f(x int = 0)` (D102). Парсер + type-checker binding-pass + codegen call-site reorder. Блокирует Plan 47. | ✅ ЗАКРЫТ 2026-05-14 (Ф.0-Ф.5; argbind.rs + callnorm.rs; 11 тестов) |
| 47 | [47-supervised-cancel.md](47-supervised-cancel.md) | `supervised(cancel: tok)` — удаление keyword `cancel_scope` (ревизия D75 2026-05-14). Caller-owned `CancelToken` + runtime bind-check. Зависит от Plan 46. | ✅ закрыт 2026-05-14 (Ф.0–Ф.4+Ф.6; Ф.5 stdlib `within`/`race` → Plan 48) |
| 48 | [48-closures-in-generics.md](48-closures-in-generics.md) | **Monomorphization** — generic-функции/методы/типы без type-erasure (Rust-grade, zero-cost). Сейчас codegen 100% erasure (всё `void*`) → хуже Go/Rust/TS. Instantiation worklist + мангление + fixpoint. Замыкания в generic'ах выпадают тривиально (исходная цель). Разблокирует Plan 47 Ф.5 + `retry.nv`. Переписан 2026-05-15 (был erasure-adapter подход). Ф.9 method-param mono (`Wrapper[T] @map[U]`) — Plan 63 Fix C followup, branch `plan-48-mpm`. | 🟡 **partial 2026-05-17**: Ф.0-Ф.9 + Ф.4 + Ф.5 + Ф.6 + Ф.7.1-7.7 + Ф.8 ВСЕ ✅ DONE; остаток — partial acceptance: generic `[]T` return-path (→ Plan 54), retry.nv codegen (→ Plan 54), erased-emitter V1 fallback (unit-variant usage-context); 668 PASS / 2 FAIL (Windows UAC os 740 baseline, identical to main) |
| 49 | [49-cancel-throw-routing.md](49-cancel-throw-routing.md) | Cancellation semantics — kinded throws (`USER`/`CANCEL`) + **typed** cancel reason `CancelToken[T]` (лучше Go `error` / TS `any`) + USER-precedence (лучше Go errgroup) + defer-on-cancel (Rust Drop паритет) + cross-type cascade через `From`-конвертацию (D73/D77) + `tok.merge(other)` композиция (beyond Go/TS/Rust). Отмена ≠ ошибка, не убегает из `supervised(cancel:)`. Закрывает `[M-cancel-throw-routing]` + `[M-within-error-conflation]`. | ✅ **ЗАКРЫТ 2026-05-16** (Ф.0-Ф.7 + audit sprint EOD: per-T un-box reason (P0 silent UB fix), cross-type cascade with From-protocol, tok.merge композиция; 6/6 cancel_*_test.nv PASS на release nova; runtime infra в effects.h/fibers.h: NovaThrowKind, nova_throw_cancel, first_error_kind atomic, linked_converters parallel array). Deferred → Plan 50: `cancel_defer { ... }` keyword (parser changes). |
| 50 | [50-default-keyword-only.md](50-default-keyword-only.md) | Параметр с дефолтом — keyword-only на месте вызова (ревизия D102 2026-05-15). Ограничение поверх Plan 46 + production-grade диагностика + миграция кодовой базы. | ✅ ЗАКРЫТ 2026-05-15 (Ф.0-Ф.5; structured suggestions в Diagnostic; [M-interp-named] RESOLVED; 12 тестов) |
| 51 | [51-d55-record-literal-unification.md](51-d55-record-literal-unification.md) | Record-literal: тип пишется ровно один раз — запретить дублирование в `=>` и `let` аннотациях. Узкий scope ~24 sites `-> T => T{}`. Investigation: полная D55 не оправдана. Ф.1 codegen (4 LOC), Ф.2 parser (40 LOC), Ф.3 миграция, Ф.4 тесты, Ф.5 spec. Spec D55 honesty-fix. | ✅ ЗАКРЫТ — Ф.1-Ф.5 в main (`658954c`..`a1c7459`) |
| 52 | [52-hashmap-literals.md](52-hashmap-literals.md) | HashMap-литералы: `{field: v}` map-coercion (ревизия D55) + `[k: v]` map-литерал (D108). Десугаринг сразу в `with_capacity`+`@insert`, без промежуточных объектов. | ✅ ЗАКРЫТ 2026-05-16 (Ф.0–Ф.8; AST/parser/type-checker/desugar с turbofish via annotate_module pass; 2 negative + 3 positive PASS; 0 регрессий) |
| 52.1 | [52.1-hashmap-literals-advanced.md](52.1-hashmap-literals-advanced.md) | HashMap-литерал advanced features: spread `[...defaults, k: v]`, const map-literal, MIR-lowering. Зависит от Plan 52 ✅ + Plan 48 Ф.7.8+ (mono-instance tracking). Ф.1 spread ~80 LOC (после Plan 48 fix), Ф.2 const ~300 LOC (заблокирована const-ordering bug). Deferred от Plan 52 из-за real-complexity vs bootstrap priorities. | 📋 план, не начат, **P3** |
| 52.2 | [52.2-bootstrap-codegen-fixes.md](52.2-bootstrap-codegen-fixes.md) | Codegen fixes: `emit_record_lit` fallback для match-body context (anonymous record literal в while-body), const-decl ordering (forward-declare mono'd types — const HashMap type unknown). Два фикса (50-100 LOC + 80 LOC). Разблокирует Plan 52.1 Ф.1+Ф.2. Depends: Plan 48 mono-pass. | ✅ ЗАКРЫТ — Ф.1+Ф.2 done; mono-блокеры сняты Plan 55/56, spread работает (2026-05-22) |
| 52.3 | [52.3-remaining-map-features.md](52.3-remaining-map-features.md) | Закрытие оставшихся `[M-52-*]` маркеров: Ф.1 пустой `[]` в map-position ✅ (17 LOC annotate); Ф.2 mono-pass quirk, Ф.3 spread, Ф.4 type-inference edges — deferred (зависят от Plan 48 mono-pass fix). | ✅ ЗАКРЫТ — Ф.1 + Ф.2/Ф.3/Ф.4 сняты Plan 55.4/55.5/56 (2026-05-22) |
| 53 | [53-let-destructuring.md](53-let-destructuring.md) | Record-destructuring в `let` — `let { tx, rx } = ch` вместо `let tx = ch.tx; let rx = ch.rx`. AST/parser/match-codegen уже было; добавил emit_record_destructure + refutability-check в type-checker. | ✅ ЗАКРЫТ 2026-05-15 (codegen + interp + refutability; 7 positive + 4 negative тестов) |
| 54 | [54-codegen-followups-from-48-49-audit.md](54-codegen-followups-from-48-49-audit.md) | Codegen followup items из Plan 48/49 audit: pattern-var leak (emit_test snapshot/restore), int-extension record field dispatch, unit-variant inference (accepted-as-is — паритет с industry), generic array return mono, nested call inference (Source 2d), polymorphic recursion test. Unblock `retry_test.nv` (Plan 48). 11 new tests, 0 регрессий. | ✅ ЗАКРЫТ 2026-05-16 (7/8 фаз; 537 PASS preserved) |
| 55 | [55-codegen-followups-from-plan-54.md](55-codegen-followups-from-plan-54.md) | Codegen follow-ups от Plan 54 + Plan 52.x mono-pass blockers + baseline cleanup + spread + spec sync. 8 фаз prod-grade (P3→P1) + followups: array-of-func-mono, match-arm pattern_inner_type, infer-return-type-from-body, mono-pass corruption (save/restore + protocol whitelist + NOVA_DEBUG_MONO), name-collision + multi-instance (Pattern::Record), baseline NEG-* cleanup (ASCII anchors + decreases bug fix), fixture directories convention, map-spread infrastructure, spec sync D45+D108. | ✅ **ОКОНЧАТЕЛЬНО ЗАКРЫТ 2026-05-16 EOD** (Ф.0-Ф.8 + 4 followups; **557 PASS / 0 FAIL / 40 SKIP** real; **10+ M-маркеров closed**; deferred items → Plan 56 (vtable) / 57 (perf bench) / 58 (cross-toolchain)) |
| 56 | [56-vtable-dispatch-erased-generics.md](56-vtable-dispatch-erased-generics.md) | **Hybrid dispatch** для bound-K methods + real field array element type propagation. Phases: Ф.1 vtable runtime infra (3 protocols + 5 primitive K vtables) ✅; Ф.2 widened stub + placeholder mono skip + effect-free enforcement в bound methods (D122) + AI-first diagnostics ✅; Ф.3 stdlib unlock — `HashMap.@clone()` + `@merge_from()` + `@filter()` через direct field access + GC stress ✅; Ф.4 spec D122 + perf-conventions.md + stdlib-bound-dispatch.md ✅. **Real fix**: `compute_array_elem_type_for_obj` для произвольной глубины `obj.f1.f2.field[i]`. Закрывает `[M-erased-generic-method-dispatch]`. | ✅ **ЗАКРЫТ как production-grade для bootstrap 2026-05-16** (7 tests / ~30 sub-tests; vtable codegen integration для truly erased dispatch deferred — не нужен в single-crate bootstrap, Plan 03 future) |
| 57 | [57-perf-benchmark-infrastructure.md](57-perf-benchmark-infrastructure.md) | Performance benchmark infrastructure — **production-grade, лучше Criterion (Rust) / `testing.B`+benchstat (Go) / tinybench (TS) по нескольким axes**. **ВСЕ 8 phases закрыты 2026-05-17** (worktree `nova-lang-p57`, branch `plan-57`, 50+ commits). **MVP**: contextual `bench "..." { measure { ... } }` DSL + `bench.*` namespace; pure-Rust stats (median/MAD/Tukey/Welch); `nova bench run/diff/gate`; JSON v1; bench.toml. **57.A** (5): history-add, dashboard echarts, calibrate noise-floor, thermal/load detection, --profile samply. **57.B** (5): 10-file corpus + Criterion-compat JSON + `(n in [...])` sweeps + CPU instr FFI + group/case. **57.C** (8): PerfTimer hooks, gc.last_pause_ns, heap sampler, CPU instr per-sample, recursive discovery, history-squash, lints, corpus subcommand. **57.D** (5): PerfTimer aggregation, sleep-lint Path-form, aggregated JSON, multi-runner baselines + CI matrix, HTML compiler-perf dashboard. **57.E** (3 impl + 3 sketch): drill-down dashboard, PELT anomaly detection, e2e shell tests. **57.F** (4, sketches → impl): SSH distributed bench coord (`bench remote list/ping/run`), AI regression interpretation (`bench diff --explain` через system curl, opt-in), memory bandwidth measurement Linux (`bench membw-check` — LLC-miss + Intel MBM + AMD DF probe), extended e2e tests. **57.G** (5, audit-driven small batch): drift slope+R² в JSON, defensive unwrap fixes, errno decoder для perf_event_open paths, ASCII histogram (`--histogram` flag), `bench.metric(name, value, unit)` custom metrics DSL (closes Go `b.ReportMetric` gap). **57.H** (3, cross-platform): multi-group geomean (benchstat-style per-group lines), `nova bench hyperfine` cross-binary timing, `nova bench callgrind` cross-platform CPU instructions via valgrind subprocess. **Test coverage**: 113+ unit tests + 11 `.nv` tests + 110 e2e asserts ALL PASS. **Zero new Rust crate deps**: AI/distributed/callgrind use system binaries (curl/ssh/valgrind); membw/cpu_instr raw libc syscall FFI. См. `docs/bench-conventions.md` + spec D121 в `spec/decisions/09-tooling.md`. | ✅ **MVP+A+B+C+D+E+F+G+H ЗАКРЫТЫ 2026-05-17** (worktree p57, 50+ commits) |
| 57.E.2 | [57.E.2-distributed-bench-sketch.md](57.E.2-distributed-bench-sketch.md) | Distributed bench coordination — design-sketch (SSH-координация bench-прогонов на нескольких машинах). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 57.E.3 | [57.E.3-ai-regression-interpretation-sketch.md](57.E.3-ai-regression-interpretation-sketch.md) | AI regression interpretation — design-sketch (`bench diff --explain` через LLM). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 57.E.4 | [57.E.4-memory-bandwidth-sketch.md](57.E.4-memory-bandwidth-sketch.md) | Memory bandwidth measurement — design-sketch (`bench membw-check`, Linux LLC-miss/MBM/AMD DF). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 58 | [58-cross-toolchain-msvc-verification.md](58-cross-toolchain-msvc-verification.md) | Cross-toolchain MSVC + GCC verification matrix. CI gate на 3 OS × 2-3 toolchains, `docs/toolchain-matrix.md`. Closes Plan 55 acceptance "Cross-toolchain MSVC verify". ~2 dev-days. | план, не начат, **P2** |
| 59 | [59-tuple-monomorphization.md](59-tuple-monomorphization.md) | **Tuple monomorphization** — mono'd `_NovaTuple____<T1>__<T2>` structures (вместо generic `_NovaTupleN` с int slots). Phase 1 infra (registry + emit marker), Phase 2 codegen (apply_type_subst/type_ref_to_c/TupleLit/emit_for tuple destructure all mono'd когда concrete), Phase 3 unlock idiomatic `for (k, v) in hashmap`, Phase 4 spec D123. Параллель Rust/Swift mono tuples. Закрывает `[M-mono-tuple-element-types]`. | ✅ **ЗАКРЫТ 2026-05-17** (idiomatic `for (k, v) in coll` works; legacy `_NovaTupleN` сохранён как fallback для erased contexts) |
| 60 | [60-len-access-uniformity.md](60-len-access-uniformity.md) | **Size-accessor uniformity** — `.len()` / `.is_empty()` / `.cap()` исключительно через method-вызов для всех container'ов (built-in `[]T`, `str`, и user-types: HashMap/Lru/Set/Range/Deque/Queue/Vec). Запретить field-access `c.len` без скобок (compile-error + machine-applicable fix-it). Закрывает Java-патологию «массивы field, коллекции method» (Nova сейчас — точное соответствие). Rust-парадигма; TS-style property отвергнут (D-блок «no computed property с эффектами / O(n) скрытой стоимостью»). Migration scope ~518 occurrences. Породил Plan 45 Ф.36. D117 (новый). | ✅ **ЗАКРЫТ 2026-05-17** (Ф.0-Ф.6: registry additions + auto-migration tool ~150 LOC + atomic field-access switch + method-value disambiguation + spec D117 + cross-ref cleanup; branch `plan-60`; **562 PASS / 0 FAIL**; cross-toolchain Clang/MSVC/GCC) |
| 61 | [61-typed-error-effect-codegen.md](61-typed-error-effect-codegen.md) | Typed `Fail[E]` codegen. Закрывает 3 silent UB в effect-codegen (throw type-pun, `nova_throw_value` placeholder, handler-arm wrong C-type). TypeId runtime infra (`typeid.h/.c`), typed payload в fail-frame (`error_user_payload` + `NovaTypeId`), per-E dispatch (`_nova_throw_typed_<E>` + per-E vtable/TLS), `expr!!` typed throw, D65 spec amend. | ✅ **ЗАКРЫТ 2026-05-17** (impl `75b3da4682b` + merge `657630e39ae` + closure `4cd6b7f650c` «all 4 deferred resolved, 0 simplifications»; маркеры `[M-plan-61-*]` ×4 RESOLVED; реализовано через payload-in-fail-frame + per-E dispatch followup#4). Шапка обновлена 2026-05-21 (была устаревшая «proposed/не начат») |
| 62 | [62-prelude-hardcode-migration.md](62-prelude-hardcode-migration.md) | Migrate hardcoded prelude → `std/prelude.nv`: D26 compliance, splittable (`partial_prelude`), edition-versionable. 50+ items (Option/Result, print/println, RuntimeError, protocols, effects). 6 sub-plans (62.A core types / 62.B runtime fns / 62.C error types / 62.D collections / 62.E protocols / 62.F effects + `no_prelude` enforcement + edition-versioning). Verified drift: ~50 items vs ~13 claimed; `no_prelude` — silent bug, `PRELUDE_VERSION` — только маркер. | ✅ **ЗАКРЫТ 2026-05-18** (P0 scope: 62.A/B/C/D non-opaque/E/F все ✅; **62.D non-opaque bis-1 ЗАКРЫТ 2026-05-18** — Range/RangeIter prelude facade migration + 4 latent codegen bugs fixed + D29 W_PRELUDE_SHADOW basic; **62.F.bis ЗАКРЫТ 2026-05-18** — edition versioning + structured W_PRELUDE_SHADOW + Time/Mem formal decls + D124/D125; **62.D.bis ЗАКРЫТ 2026-05-18** — opaque types D126; **62.B.bis ЗАКРЫТ 2026-05-18** — print/println D69 variadic + `[]any` + Plan 67 hotfix absorbed; deferred sub-plans 62.C.bis RuntimeNoneError / 62.E.bis TryFrom/TryInto — остаются blocked externally; **62.A.bis ЗАКРЫТ 2026-05-20**). PRELUDE_VERSION bumped 2 → 3 → 4 → 5 → 6 → 7. **713 PASS / 0 FAIL / 44 SKIP** + 16/16 sum_schema unit tests. |
| 63 | [63-cross-module-mono-dispatch-correctness.md](63-cross-module-mono-dispatch-correctness.md) | **Cross-module + mono dispatch correctness** — family из 6 compiler bugs обнаруженных в Sprint F.36 regression (`nova_tests/plan11_followup/`). Все из-за того что compiler tracks N (signature/typearg/overload) во-внутри-module, но теряет N при пересечении module boundary. **Fix A**: return-type inference для method registered через external_registry. **Fix B**: variadic signature через alias-import `import X as p`. **Fix C**: generic anonymous record `Box[T]` (`Nova_T_p` placeholder leak) — resolved через Plan 59 mono pipeline. **Fix D**: typeargs-aware overload dispatch (`HashMap[str,int].from`). **Fix E**: `for (k,v)` в generic method body. **Fix F**: `Result[(T,U), E]` Ok-payload destructure. | ✅ **ЗАКРЫТ 2026-05-17** (Fixes A/B/C/D/E/F all resolved; 16/18 PASS в `plan11_followup/` — 2 Windows AV os 740 deferred baseline) |
| 62.A.bis | [62.A.bis-sum-schema-registry.md](62.A.bis-sum-schema-registry.md) | **Generic schema registry для sum-types** — sub-task Plan 62 (см. §«`Result[T,E]` mono compromise» + Open Question #1). Layered registry поверх hardcoded `sum_schemas`: file-based декларации в `std/prelude/core.nv` (Option/Result/Error) + `std/prelude/errors.nv` (RuntimeError) получают приоритет, hardcoded baseline остаётся как fallback для ABI compat с `nova_rt/array.h` value-type helpers. 4 incremental phases (3-4 dev-days). **Unblocks**: 17 deferred Option/Result methods из Plan 62.A + Plan 62.C RuntimeError migration. **NOT в scope**: полная sum-type монoморфизация (Plan 59 Phase 7.5). | ✅ **ЗАКРЫТ 2026-05-20** (Phases 1-3 closed: registry infra + variant unification + method routing; Plan 62.A follow-up: 17 `external fn` деклараций в `std/prelude/core.nv` + 29 регрессионных тестов в `nova_tests/plan62/`; **855 PASS / 0 FAIL** / 47 SKIP; Phase 4 legacy `sum_schemas` removal deferred до Plan 59 per design) |
| 62.F.bis | [62.F.bis-edition-shadow-and-runtime-effects.md](62.F.bis-edition-shadow-and-runtime-effects.md) | **Plan 62.F deferred parts** — edition versioning (`nova.toml [package].edition` → `std/prelude/<edition>.nv` resolver mapping через extended Manifest) + structured W_PRELUDE_SHADOW lint с `allow_prelude_shadow` suppress + formal `Time`/`Mem` ambient runtime effect declarations + spec D-block amendments (D26 sync + D124 edition + D125 shadow lint + 07-modules.md partial_prelude/allow_prelude_shadow clauses). 4 phases, single-session execution. NOT в scope: TryFrom/TryInto (62.E.bis), print/println (62.B.bis), opaque types (62.D.bis), RuntimeNoneError (parser empty-sum), item-level `#[allow(prelude_shadow)]` (требует generic attribute parser), `Time.after` formal (требует Chan[T] в prelude). | ✅ **ЗАКРЫТ 2026-05-18** — все 4 phases (Ф.1 `445904b2b4e`, Ф.2 `faf164529a9`, Ф.3 `51a0ccf24fb`, Ф.4 `f37193b075f`). PRELUDE_VERSION 4 → 5. **711 PASS / 0 FAIL / 44 SKIP** + sanitize_edition 6/6 + prelude_shadow 6/6 + edition_resolve 3/3 + sum_schema 16/16. |
| 62.D.bis | [62.D.bis-opaque-types-and-external-type-d-block.md](62.D.bis-opaque-types-and-external-type-d-block.md) | **Plan 62.D opaque types — закрытие последнего «known-by-name» hole в D26 visible prelude.** Добавляет новый syntax `external type X [Generics]` (D126 в spec/decisions/03-syntax.md) — opaque типы с runtime backing, без body. Мигрирует StringBuilder/WriteBuffer/ReadBuffer из known-by-name в formal `std/prelude/collections.nv` declarations через D126. Methods продолжают жить в std/runtime/<name>.nv через `external fn` (D82, unchanged). 6 phases: parser/AST/type-checker (Ф.1), migration (Ф.2), runtime header cross-refs (Ф.3), positive test verification (Ф.4), HashSet doc (Ф.5), spec D126/D26/D82 (Ф.6). | ✅ **ЗАКРЫТ 2026-05-18** — все 6 phases (Ф.1 `389abb9461b`, Ф.2 `f5af1353d07`, Ф.3 `09f1ce31741`, Ф.5+Ф.6 squashed). PRELUDE_VERSION 5 → 6. **712 PASS / 0 FAIL / 44 SKIP** + 4/4 sub-tests в plan62/opaque_types_from_prelude. |
| 62.B.bis | [62.B.bis-print-println-migration.md](62.B.bis-print-println-migration.md) | **Plan 62.B print/println migration — formal declarations через D69 variadic + `[]any`.** Canonical D26 signature `fn print(...items []any) Io -> ()` formally declared в `std/prelude/runtime.nv`. Plan 67 P0 hotfix (silent-wrong-output bug в `infer_print_helper` для `println(str.from(int))` паттерна — fallback на `nova_print_int(nova_str)` give garbage) **absorbed как Ф.0**: refactor через unified `infer_expr_c_type` dispatch (~75 → ~25 LOC). 7 phases: Ф.0 Plan 67 absorption / Ф.1 intercept reorder перед variadic routing / Ф.2 declarations + facade + PRELUDE_VERSION bump / Ф.3 verification / Ф.4 spread support deferred / Ф.5 HashSet shrink (2 entries) / Ф.6 positive tests + spec sync. | ✅ **ЗАКРЫТ 2026-05-18** — все 6 phases (Ф.0 `cbcafbfdad3`, Ф.1 `4b20917f015`, Ф.2 `37d17de8ac5`, Ф.5 `658a5cc98e6`, Ф.6 closure). PRELUDE_VERSION 6 → 7. **713 PASS / 0 FAIL / 44 SKIP** + 4/4 sub-tests в plan62/print_println_from_prelude. Plan 67 closed (merged). |
| 64 | [64-nova-lang-website.md](64-nova-lang-website.md) | **nv-lang.org dogfooding initiative.** Реальный сайт проекта `nv-lang.org`, написанный (постепенно) на самом Nova. **Ф.0** ✅ — static HTML на GitHub Pages (nv-lang/www, EN+RU, Cloudflare DNS). **Ф.1** — production HTTP/1.1 stdlib: `std/net/tcp` + `std/http` (parser, router, static, metrics, structured log; паритет с Go `net/http` + Nova-unique effect tracking; зависит от Plan 18 P0). **Ф.2** — SSG на Nova: Markdown→HTML, CommonMark subset + GFM, template engine, search index, `nova-ssg build/serve/check` (паритет с Hugo/mdBook). **Ф.3** — server-rendered на VPS (Yandex Cloud, systemd, Cloudflare Proxied, graceful reload, `/healthz` + `/metrics` Prometheus). **Ф.4** — /spec/, /blog/, /changelog/. Production-площадка для runtime под живой нагрузкой + reference implementation HTTP-сервера на Nova. | **Ф.0 ✅ ЗАКРЫТ 2026-05-18**; Ф.1 блокирован Plan 18 P0; Ф.2-Ф.3 — план |
| 65 | [65-chanreader-close-after.md](65-chanreader-close-after.md) | **Timer-channel API parity с Go/Rust/TS.** Renames `Time.after(ms i64) -> ChanReader[()]` → `ChanReader.close_after(d Duration) -> ChanReader[()]` (D91 capability + Duration type-safety). v2 ревизия: industry-parity audit добавил 5 production hardening фаз — CancelToken integration (D75 cancel propagation), Time-effect mock для deterministic тестов, `close_at(Monotonic)` absolute deadline (D124 type-safety), drop/GC finalizer (no leak), `NOVA_TIMER_METRICS` observability, `tick_every` namespace squat → Plan 66. Token-aware migration tool (Plan 60 паттерн) для 13 call sites. 32 requirements, 12 AD, 25-point final audit. Nova объективно лучше baseline на 5 capabilities (typed Duration, native CancelToken, Time-effect mock, supervised select propagation, Monotonic clock type-safety). Timer-wheel — осознанный defer в Plan 66. | **MVP Ф.0-Ф.9 ✅ + hardening Ф.10-Ф.14 ✅ ЗАКРЫТ 2026-05-18** (717 PASS / 0 FAIL / 44 SKIP — baseline 705 + 12 new fixtures; CancelToken cleanup-list infra, Time-effect mock sequential path, NOVA_TIMER_METRICS + 5-counter Nova API, Monotonic type + close_at + binop D124 type-check, 1000-timer stress, Plan 66 outline) |
| 66 | [66-timer-wheel-and-tick-every.md](66-timer-wheel-and-tick-every.md) | **`ChanReader.tick_every(Duration)` periodic ticker + custom timer-wheel runtime.** Outline created в Plan 65 Ф.13 (namespace squat: `#unstable` stub в `std/concurrency/timer.nv` чтобы external collision не возникал). R1: tick_every API с `MissedTickBehavior::Skip/Burst/Delay` (Tokio parity). R2: hierarchical-bucket wheel (4 levels × 64 buckets, replaces libuv-per-timer для high-N loads). R3: runtime config gate auto/libuv/wheel. R4: backwards-compat с Plan 65. R5: bench coverage (timer_alloc_throughput). Linked Plan 22/23/65. | 📋 outline only 2026-05-18, не начат, **P2**; ~6-8 dev-days |
| 67 | [67-println-overload-return-type.md](67-println-overload-return-type.md) | **`println`/`print` overload resolution через `infer_expr_c_type` (AD1) + `nova_print_char` (AD3).** Hotfix CC-FAIL в `bench/corpus/06_contracts.nv` после Plan 65 contract-encoder фикса. DRY 75 → 15 LOC; production stdlib API получают correct print-helper бесплатно. На parallel branch `plan-62-main` идентичный refactor `infer_print_helper` через `infer_expr_c_type` был absorbed как Plan 62.B.bis Ф.0 (commit `cbcafbfdad3`); converged в один результат на main. | ✅ **ВСЕ ФАЗЫ ЗАКРЫТЫ 2026-05-18** (Ф.0-Ф.2 fix(println) 9a90802b022 + Ф.3-Ф.4 spot-check + workaround audit clean + 3 simplifications RESOLVED; 4 honest-defers: W6701 lint (codegen warning infra), cross-toolchain (Plan 58), bench corpus status FP, 3 pre-existing corpus breakage; 717 PASS / 0 FAIL preserved; 10 tests plan67/f1-f10) |
| 68 | [68-print-as-nova-function.md](68-print-as-nova-function.md) | `print`/`println` как обычные Nova-функции через protocol-as-value (`[]Into[str]`). Убирает magic из кодогена. Блокирован открытыми вопросами (Q1–Q6). | 📋 proposed 2026-05-18, **не начат**, Q1–Q6 требуют решения |
| 69 | [69-byte-to-u8.md](69-byte-to-u8.md) | **Remove `byte` type alias, canonicalise `u8`.** Nova has both `byte` and `u8` для 8-bit unsigned — ambiguity, style inconsistency. Decision: `u8` — sole canonical name; `[]u8` — canonical byte-slice type. Tagged-template tag `bytes` (D48) — function name, не type — unaffected. Word "byte" в prose — English text, unaffected. | ✅ **ЗАКРЫТ 2026-05-22** — `byte` убран из компилятора (type-checker/parser/codegen/verify); spec/std/nova_tests мигрированы на `u8`; D125; 2 alias-фикстуры удалены; 954 PASS / 0 FAIL. **P2** |
| 70 | [70-no-silent-nova-int-fallback.md](70-no-silent-nova-int-fallback.md) | **Strict type propagation в codegen — no silent `nova_int` fallback.** После Plan 67 audit обнаружил 155 fallback sites в emit_c.rs (`type_ref_to_c(...).unwrap_or_else(\|_\| "nova_int")`); ~120 потенциально silent miscompilation (Category A). Strict error propagation (E7001) + Cat B documented erasure + internal lint guard. Industry parity: Rust/Swift compile-error. **57 Cat A1 sites migrated** (session 1: 51 + session 2: 6 unit-fallback); 13 Cat B documented; spec D126 published. Final audit: [70-artifacts/final-audit-2026-05-18.md](70-artifacts/final-audit-2026-05-18.md). **Session 3 (2026-05-20):** D46 BinOp dispatch — Set.or/and/minus fix (BitOr/BitAnd→@or/@and, Sub fallback→@minus, infer_expr_c_type Self-return); 3 new PASS; merged→main. | ✅ closed 2026-05-18 (sessions 1+2) + D46 bonus 2026-05-20; **P0**; 89% silent miscompilation surface reduction; remaining 11% Cat B documented с deferred-strict scope |
| 70.1 | [70.1-module-alias-resolution.md](70.1-module-alias-resolution.md) | `import X as th` + `th.func()` не раскрывается в C codegen → CC-FAIL. Fix: `imported_modules` field + emit_call Member-rewrite + defensive guards (local var / intrinsic namespace). 3 PASS fixtures `nova_tests/plan70_1/`. Known limitation: negative tests deferred (link-time errors не catch'ятся EXPECT_COMPILE_ERROR) → доработка в [Plan 81](81-module-resolution-hardening.md) Ф.7. | ✅ closed 2026-05-19, **P1**, ~3h factual |
| 70.2 | [70.2-linkedlist-sum-type-mono.md](70.2-linkedlist-sum-type-mono.md) | LinkedList sum-type monomorphization broken. Fix 1 (plan-70.2): drain_generic_type_worklist после emit_test loop — Cons-constructor type inference enqueued instances теперь generated. Fix 2 (plan-70.2b): `method.trim_start_matches('@')` в generic_type_methods lookup + base_method_name construction — @method dispatch on mono'd sum-type works. 2 PASS fixtures `nova_tests/plan70_2/` (data + @method). | ✅ closed 2026-05-19, **P1**, ~2.5h factual (estimated 3-5 dev-days) |
| 70.3 | [70.3-char-int-mono-distinction.md](70.3-char-int-mono-distinction.md) | **char↔int distinction в codegen mono'd generics.** Sibling family Plan 70: `char` и `int` оба map'ятся в один C `nova_int` → `Option[char]` и `Option[int]` mangle в идентичный `NovaOpt_nova_int` → type-checker silent collapse. Fix: `typedef int64_t nova_char` (zero-cost alias) + parallel runtime helpers (`NovaArray_nova_char`, `NovaOpt_nova_char`, `nova_opt_eq_nova_char`) + codegen `char → nova_char` mapping (emit_c.rs + external_registry.rs sync) + `nova_str_char_at` return-type update + `Nova_StringBuilder + char` BinaryOp backward-compat. Spec D128 published. 2 PASS fixtures `nova_tests/plan70_3/`. | ✅ closed 2026-05-19, **P1**, ~3.5h factual (estimated 3-5 dev-days) |
| 70.4 | [70.4-primitive-type-distinction-complete.md](70.4-primitive-type-distinction-complete.md) | **Complete primitive-type distinction в codegen mono'd generics.** (Ф.1) f32/f64 array distinct `NovaArray_nova_f32*`; (Ф.2 ABI-real **P1**) sized-int `[]i8`/`[]i16`/`[]i32`/`[]u16`/`[]u32`/`[]u64` distinct — fix 2x-8x memory overhead + FFI mismatch; (Ф.3) spec D129 int=i64 alias; (Ф.4) u8→nova_byte mangle unification (Option[u8]≡Option[byte]). 5 fixtures `nova_tests/plan70_4/` PASS. Full byte-removal deferred. | ✅ closed 2026-05-19, **P1** (Ф.2), ~1 dev-day factual |
| 70.5 | [70.5-uint-primitive-symmetry.md](70.5-uint-primitive-symmetry.md) | **`uint` symmetric primitive type.** `uint` = alias `u64` (uint64_t). `int as uint` saturates (neg→0) via `nova_int_to_uint()` D54-style helper. `int as u64` remains direct bit-cast. `[]uint`→`NovaArray_uint64_t*`; `Option[uint]`→`NovaOpt_uint64_t`. Spec D130. 3 fixtures `nova_tests/plan70_5/` PASS. Deferred: `uint.MAX` (parser keyword), indexing API. | ✅ closed 2026-05-19, **P3** (ergonomics), ~0.5 dev-day factual |
| 71 | [71-doc-stability-scope.md](71-doc-stability-scope.md) | **`doc-check` stability-tier scope — opt-in vs implicit.** Lint `public-missing-stability` (Plan 45 §11.5 №7) понижается до warning по умолчанию; opt-in error через `[lib] enforce-stability = true` в `nova.toml` (analog Rust `#![feature(staged_api)]`). Auto-exempt: `nova_tests/**`, `tests/**`, `examples/**`, `bench/**`. D127 (новый). Unlocks: nova-doc CI workflow. ~0.5 dev-day. | ✅ **ЗАКРЫТ 2026-05-19** (Ф.0-Ф.6: план + spec D127 + idiom page + Manifest.enforce_stability + LintConfig + Severity enum + DocModule.source_paths + fixture-skip + stdlib opt-in + 17 unit + 11 integration tests + smoke green) |
| 72 | [72-protocol-and-type-system-followups.md](72-protocol-and-type-system-followups.md) | **Protocol и type-system followups** — консолидация разрозненных proposals из Plan 48/56/62. **P0+P3-B** silent method-dispatch на erased `void*`: сначала строгий compile-error E7201, затем полный vtable codegen (`NovaVtable_*`/`NovaBox_*` fat pointer dispatch — companion `__vt_x`, thunks; + function-return `fn () -> Iter[int]` и protocol-typed параметры `fn foo(x Iter[int])`). **P1-A** parser `mut it T` для generic type-var. **P1-B** empty-sum syntax (`type Never` / `RuntimeNoneError` без тела). **P1-C** Result[T,E] 5 методов (unwrap/unwrap_or/map/map_err/unwrap_or_else) — `result_type_params` + `cast_from_nova_int`, без Plan 59. **P2-A** TryFrom/TryInto через `Result[Self,E]` (migration path b, без `Fail[E]` effect row). **P2-B** structural inference `[U, T Iter[U]]` без turbofish. **P3-A** record-shadow codegen edge (`Range.start`). Прогон через реальный C-backend (`test-build`) вскрыл и закрыл 4 бага, замаскированных интерпретатором (p1b typedef redefinition / p2a Result type-params / p2b `NovaOpt_X` / p3b-param E7201). Regression markers `nova_tests/plan62/protocol_as_value_probe.nv` + `protocol_param_generic_bound.nv`; фикстуры `nova_tests/plan72/`. | ✅ **ЗАКРЫТ 2026-05-20** (P0 + P1-A/B/C + P2-A/B + P3-A/B + P3-B followup + protocol-param + p1b/p2a/p2b C-backend fixes — все ✅; worktree p72; **886 PASS / 0 FAIL / 56 SKIP** (z3) через реальный C-codegen pipeline, измерено 2026-05-21; два P3-B honest-defer закрыты) |
| 73 | [73-consume-qualifier.md](73-consume-qualifier.md) | **`consume` qualifier — D131.** Compile-time проверка логической линейности: после вызова `consume`-метода / передачи в `consume`-параметр переменная недоступна. Use-after-consume и maybe-consumed → compile error. Синтаксис: `fn StringBuilder consume @into() -> str` / `fn foo(consume sb StringBuilder)`. Реализация: AST `Receiver.consume`+`Param.consume`, лексер `KwConsume`, flow-sensitive `VarState`/`ConsumeCtx`. Не borrow checker — только VarState dataflow. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.6 + followup: spec D131 + AST + parser + flow-sensitive `check_consume` (branch-join + пессимистичные циклы + alias-tracking) + `string_builder` `consume @into` (runtime-флаг убран) + 12 фикстур `nova_tests/plan73/`) |
| 74 | [74-primitive-bitcast.md](74-primitive-bitcast.md) | **Primitive bitcast — `to_bits`/`from_bits`.** `f64 ↔ u64` и `f32 ↔ u32` reinterpret-cast для IEEE 754 bit-level операций (хэши, NaN-boxing, FFI). Безопасные именованные методы без общего `unsafe transmute`. C inline через `memcpy`. Новый файл `std/runtime/numeric.nv`. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.4: `nova_rt/numeric.h` + `numeric_runtime()` registry + auto-gen `numeric.nv` + 12 sub-тестов `nova_tests/plan74/`; codegen hard-coded primitive dispatch как D74 math) |
| 75 | [75-str-test-coverage.md](75-str-test-coverage.md) | **Полное тестовое покрытие встроенного типа `str`.** Новый каталог `nova_tests/str/` с 10 тест-файлами: `@char_at` (ASCII/Unicode/OOB), `@is_empty` (добавить в runtime), `@hash` determinism, `split` edge cases, конверсии Ok/Err-ветки, интерполяция `char`-var (fix emit_c.rs), OOB slice, compile-error тесты. 3 codegen-бага исправлены: char_at return type, str.from(char var), infer_expr_c_type primitive receiver lookup. | ✅ ЗАКРЫТ 2026-05-20, **P1**, ~1 dev-day factual |
| 76 | [76-never-lowercase-keyword.md](76-never-lowercase-keyword.md) | **`never` — bottom-тип как строчный keyword.** Bottom-тип сейчас `Never` (с заглавной) и не в `is_primitive_type` — inconsistent с `int`/`bool`/`u32` (все строчные built-in, в prelude не объявляются). Переименовать `Never` → `never`, добавить в keyword-список парсера, не объявлять в prelude. Не путать с `RuntimeNoneError` (std error-тип — остаётся капитализированным, в prelude). Выделено из обсуждения Plan 72 P1-B. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.5: parser `is_primitive_type` + Rust string-rename + `type_ref_to_c` ABI placeholder + 3 `.nv` миграции + spec + `nova_tests/plan76/`; внутренний `Ty::Never` оставлен — корректный Rust PascalCase; **888 PASS / 0 FAIL**) |
| 77 | [77-fluent-return.md](77-fluent-return.md) | **Fluent-return — точное «метод возвращает receiver».** `-> Self` гарантирует «тот же тип», не «тот же объект» — builder-методам (`append` и т.п.) нужно второе. Выбран **вариант B — синтаксис `-> @`**. Разблокирует sound alias-tracking в consume-checker (Plan 73 builder-chain) + самодокументируемые fluent-API. Выделено из обсуждения Plan 73. | ✅ **ЗАКРЫТ 2026-05-21** (вариант B: parser `-> @` + `FnDecl.returns_receiver` + check-pass `check_fluent_return` + consume-checker alias + ~22 stdlib builder'а → `-> @` + spec D132 + 5 фикстур `nova_tests/plan77/`) |
| 78 | [78-prelude-codegen-single-source.md](78-prelude-codegen-single-source.md) | **Prelude codegen single-source — устранить hardcoded зеркала.** Follow-up Plan 62. `std/prelude/*.nv` — source of truth для type-check/resolve/docs, но codegen имеет параллельные хардкод-зеркала: `init_hardcoded_baseline` (method routing Option/Result/RuntimeError), `sum_schemas` pre-populate (Option/NovaOpt/RuntimeError), print/println special-cases. Дублирование = drift-риск. Ф.1 routing из деклараций, Ф.2 убрать sum_schemas pre-populate, Ф.3 print/println → Plan 68, Ф.4 panic/assert аудит, Ф.5 spec+verify. НЕ про автоген (prelude — наоборот, codegen должен читать `.nv`). | ✅ **ЗАКРЫТ 2026-05-22** — Ф.1 (method_routing переклассифицирован как легитимный реестр C-реализации, вариант 2), Ф.2 (хардкод-зеркало `sum_schemas` для Option/RuntimeError устранено — variants из `.nv`), Ф.3 (print/println → Plan 68), Ф.4 (panic/exit/assert — legitimate codegen), Ф.5 (verify). 954 PASS / 0 FAIL. **P2** |
| 79 | [79-typecheck-hardening-no-silent-fallback.md](79-typecheck-hardening-no-silent-fallback.md) | **Type-checker hardening — «no silent fallback» на уровне типов.** Перепроверка 2026-05-21 показала: `types/mod.rs` молча принимает arg-type mismatch (`want_bool(42)`), annotation↔RHS mismatch (`let x int = true`), wrong type-arity (`Result[int]`) — компилируются И запускаются неверно (silent miscompilation); type-as-value и bad-field ловятся только C-компилятором (CC-FAIL). Go/Rust/TS ловят всё на compile-time. Аналог Plan 70, но для type-checker'а: Ф.1 assignability arg↔param + annotation↔RHS, Ф.2 type-arity, Ф.3 field/variant existence, Ф.4 type-vs-value, Ф.5 закрытие untyped-escape (строже TS — без `any`-дыры), Ф.6 негативные тесты (разблокирует p1b/p2a), Ф.7 spec. Главный риск — fallout в std/nova_tests. Выделено из `[M-typecheck-missing-type-compat-checks]` (Plan 72). | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1 E7301 + Ф.2 E7310 + Ф.3 E7320 + Ф.4 E7330 + Ф.5 audit + Ф.6 27 фикстур + Ф.7 D135 + Ф.8; `TypeCheckCtx` проход; **946 PASS / 1 FAIL** — plan81/lib pre-existing) |
| 81 | [81-module-resolution-hardening.md](81-module-resolution-hardening.md) | **Module-resolution hardening — production-grade резолв модулей.** Консолидирует открытые пункты Plan 35 (35.B-E + R26), Plan 70.1 (known-limitation), маркеры simplifications — доводит каждый аспект до уровня ≥ Go/Rust/TS. Переработан с чистого листа после сверки с кодом (структурная конформность → без orphan-rule; `SourceMap`/`FileId` уже есть; field-visibility spec-rejected; `nova_fn_<name>` без пути модуля — реальная коллизия). Ф.1 visibility enforcement (P1 — приватные элементы импорт. модуля доступны, нарушение D5), Ф.2 alias/member-call type-check (P1 — compile- вместо link-error), Ф.3 cross-file generic bounds (структурно, без orphan-rule), Ф.4 strictness (unused imports + case-sensitive пути), Ф.5 peer mutual recursion 2-pass, Ф.6 symbol mangling v0 (D134), Ф.7 DCE, Ф.8 диагностика (multi-error + did-you-mean + FileId audit), Ф.9 content-addressed build cache, Ф.10 entry-folder-module isolation, Ф.11 spec/simplifications/README sync. НЕ входит (spec-rejected): относительные пути, wildcard, pub-гранулярность. | ✅ **ЗАКРЫТ 2026-05-21** — Ф.1–Ф.11 ✅ (visibility enforcement, module-call E7401, cross-file bounds, case-sensitive пути + unused-import lint, peer recursion, symbol mangling v0 D134, linker+compiler DCE свободных функций, диагностика multi-error/did-you-mean/FileId, content-addressed build cache, entry-folder-module isolation, spec/README sync; ветка `plan-81-hardening`, **956 PASS / 0 FAIL**). Декомпозировано в отдельные инкременты: Ф.6.3 `nova demangle` (stretch), Ф.7.2-methods (method-level compiler-DCE — нужна protocol-dispatch консервативность; бинарь уже чистит линкер). **P1** |
| 82 | [82-windows-fiber-arena.md](82-windows-fiber-arena.md) | **Windows fiber stack arena — re-diagnosis + production-реализация.** Перепроверка с чистого листа опровергла документированный root cause Plan 44.3: текущий vendored `minicoro.h:665-768` — Windows x64 asm-backend **уже свопает** `NT_TIB.StackBase/StackLimit/DeallocationStack` (уровень corosensei/Boost.Context). TIB — не блокер. Реальная работа: arena-аллокация (`VirtualAlloc` lazy commit + `PAGE_NOACCESS` guard + VEH), Boehm GC vs Windows commit-charge (active-range root), cross-thread migration (work-stealing). Ф.0 re-diagnosis (standalone-спайк — установить настоящий блокер 44.3), Ф.1 arena, Ф.2 GC, Ф.3 migration, Ф.4 prod test-matrix (SEH/overflow/`/GS`, clang-cl+MSVC), Ф.5 M:N + bench, Ф.6 spec D97. Коррекция: «growable stacks» несовместимы с conservative GC → цель = lazy-commit large-reserve. Сравнение с Go/Rust(tokio+corosensei)/TS. | 📋 proposed 2026-05-21, не начат, **P2** |
| 83 | [83-mn-default-on-gomaxprocs.md](83-mn-default-on-gomaxprocs.md) | **M:N по умолчанию + GOMAXPROCS-style конфигурация — roadmap-индекс.** Сейчас M:N — opt-in (`runtime.init(n)` обязателен) — противоположно Go (`GOMAXPROCS`=NumCPU) и tokio. Цель: M:N вкл по умолчанию для compiled-бинарей (`nova run` — однопоточный интерпретатор, не затронут), worker'ов = NumCPU, override ENV `NOVA_MAXPROCS` + API. **Редакция 3: декомпозирован** на 83.1/83.2/83.3 по линии гейта Plan 82. | roadmap |
| 83.1 | [83.1-mn-infrastructure.md](83.1-mn-infrastructure.md) | M:N-инфраструктура (pre-flip): worker-count resolution (`uv_available_parallelism`, cgroup-aware), `NOVA_MAXPROCS`, `runtime.init` → опциональный тюнер + `runtime.maxprocs()` getter, lazy worker-пул + auto-shutdown, thread-budget для `nova test`/`bench` (против `NumCPU²`-oversubscription). Дефолт **остаётся opt-in** — безопасно, закрывается независимо от Plan 82. | 🟡 Ф.1-Ф.3 ✅ (2026-05-22, в main); Ф.4-Ф.5 open, **P2** |
| 83.2 | [83.2-mn-default-flip.md](83.2-mn-default-flip.md) | Флип дефолта (M:N вкл) + миграция + escape hatch `NOVA_MAXPROCS=1` + spec + speedup-бенчмарк. Ф.0 readiness-gate (decision point). **GATED:** Plan 82 ✅ + 83.1 ✅ + GC-safety под multi-worker; желательно 83.3 ✅. | 📋 proposed 2026-05-22, не начат, **P2** (GATED) |
| 83.3 | [83.3-blocking-effect-threadpool.md](83.3-blocking-effect-threadpool.md) | `Blocking`-эффект (D50) → libuv threadpool (`uv_queue_work` + park/wake D93). Без этого worker залипает в блокирующем FFI и `NOVA_MAXPROCS=N` не даёт N CPU throughput. Companion-паритет с Go (`P`-handoff) / tokio (`spawn_blocking`). Ортогонален Plan 82. | 📋 proposed 2026-05-22, не начат, **P2** |
| 84 | [84-relative-imports.md](84-relative-imports.md) | **Относительные импорты `./` / `../` — package-scoped (D29 rev-4).** `./X`/`../X` резолвятся относительно директории файла, но строго в пределах своего пакета (Rust-стиль: относительно — внутри, полный путь — наружу). Префикс `./`/`../` — синтаксический дискриминатор → неоднозначности нет by construction. Граница пакета жёсткая (`../` не выходит за корень). Bare-путь остаётся абсолютным. Между Go (нет относительных) и TS (безграничные). Фазы Ф.1-Ф.6: лексер/парсер, резолвер + package-boundary, диагностика, аудит взаимодействий, тесты, spec D29 rev-4. | ✅ ЗАКРЫТ 2026-05-22 (Ф.1-Ф.6; в main; 14 фикстур; nova test --filter plan84 = 12 PASS / 0 FAIL) |
| 85 | [85-builtin-types-test-coverage.md](85-builtin-types-test-coverage.md) | **Полное тестовое покрытие встроенных типов и протоколов.** Umbrella-план для 5 sub-plans: StringBuilder (17 методов), ReadBuffer+WriteBuffer (44+31 методов), протоколы конверсии From/Into/TryFrom/TryInto, протоколы сравнения Hashable/Equatable/Comparable/Display, протокол Iter[T]. Каждый метод — ≥1 позитивный + ≥1 негативный тест. Особый фокус — поиск codegen-багов (аналог Plan 75). | 📋 proposed 2026-05-22, не начат, **P1** |
| 85.1 | [85.1-stringbuilder-coverage.md](85.1-stringbuilder-coverage.md) | **StringBuilder — полное покрытие.** 17 методов: конструкторы (`new`, `with_capacity`, `from(str)`, `from(char)`), append/plus (str+char, Unicode), метрики (`len`, `byte_len`, `capacity`), `clone` isolation, inspection (`starts_with`, `ends_with`, `is_empty`, `peek`), consume (`into` + use-after-consume panic). | 📋 proposed 2026-05-22, не начат, **P1** |
| 85.2 | [85.2-buffers-coverage.md](85.2-buffers-coverage.md) | **ReadBuffer + WriteBuffer — полное покрытие.** ReadBuffer: 44 метода (навигация, байты, u8..u64/i8..i64 LE+BE, f32/f64 LE+BE, char/str, OOB). WriteBuffer: 31 метод (write_*, LE+BE, char, zero, clone). Ключевой тест — round-trip write_X → into → ReadBuffer.from → read_X для всех типов. | 📋 proposed 2026-05-22, не начат, **P1** |
| 85.3 | [85.3-conversion-protocols.md](85.3-conversion-protocols.md) | **From / Into / TryFrom / TryInto — полное покрытие.** User-defined impl, auto-derive Into из From, TryFrom Ok+Err пути, TryInto auto-derive, generic bounds `[T From[str]]`, compile-error при несоответствии. Known limitation: record Ok-payload field-access (pre-existing bug, Plan 59). | 📋 proposed 2026-05-22, не начат, **P1** |
| 85.4 | [85.4-comparison-protocols.md](85.4-comparison-protocols.md) | **Hashable / Equatable / Comparable / Display — полное покрытие.** User impl для всех 4. Hashable: детерминизм + различие + встроенные int/bool/char. Equatable: рефлексивность, симметрия, транзитивность. Comparable: Less/Equal/Greater все три. Display: fmt через StringBuilder + вложенный Display. | 📋 proposed 2026-05-22, не начат, **P1** |
| 85.5 | [85.5-iter-protocol.md](85.5-iter-protocol.md) | **Iter[T] — полное покрытие.** User-defined impl с состоянием, exhaustion (next→None + повторный безопасен), пустой Iter, for-in, early break, вложенные Iter, `str.chars()`/`str.bytes()` как Iter, generic bound `[T Iter[int]]`, compile-error для non-Iter в for-in. | 📋 proposed 2026-05-22, не начат, **P1** |

> Plan 19 — see `19-closure-and-error-ops.md` (closure-rev + D85 error-ops).
> Plan 20 и 21 — последовательные (Plan 21 зависит от Plan 20).
> Plan 22 — самостоятельный, не блокирует Plan 20/21.
> Plan 25 — gap analysis vs Go/Rust state-of-the-art; не план-исполнения, а honest assessment.

## Связанные директории

- [docs/research/](../research/) — справочные материалы и сравнения
