# Планы Nova

В этой директории — только **планы** (что и когда делаем). Справочные
материалы (таблицы сравнений, research-заметки, бенчмарки) живут в
[docs/research/](../research/).

## Схема нумерации

- `01-…`, `02-…` — главные планы по порядку создания.

## Текущие планы

| # | Файл | О чём | Статус |
|---|---|---|---|
| 01 | [01-roadmap-v0.1.md](01-roadmap-v0.1.md) | Roadmap разработки компилятора v0.1–v1.0+ | активный |
| 02 | [02-codegen-c-backend.md](02-codegen-c-backend.md) | C backend: компиляция Nova в нативный бинарь | активный |
| 03 | [03-package-ecosystem-roadmap.md](03-package-ecosystem-roadmap.md) | **Package ecosystem — roadmap-индекс.** Редакция 2: честное сравнение Cargo/npm/Go-mod/Deno/pip, Nova-уникальный угол (effect-aware зависимости — effect-surface dep видна, effect-diff как supply-chain сигнал, capability-confined deps), production security-модель (content-addressed, подпись + transparency log, `nova audit`). Декомпозирован на 03.1–03.6. Коррекция: self-hosting НЕ блокер экосистемы. | roadmap |
| 03.1 | [03.1-path-git-dependencies.md](03.1-path-git-dependencies.md) | `path`/`git`-зависимости в bootstrap-компиляторе + `nova.lock` (git commit как integrity-пин, path-deps unpinned). Без registry/version-ranges/self-host. Межпакетный резолв + cross-package cycle detection, `nova add`/`update`. Разблокирует multi-package разработку до v2.0. | ✅ ЗАКРЫТ 2026-05-22 (Ф.1–Ф.6, 983 PASS / 0 FAIL) |
| 03.2 | [03.2-version-resolution.md](03.2-version-resolution.md) | Version resolution: semver-диапазоны (`^1.2`/`~1.2.3`/`>=1.0,<2.0`), git-теги как источник версий, backtracking-резолвер (транзитивный, диагностика конфликта), `nova update [--precise]`. Без registry — вселенная версий из git-тегов. | ✅ ЗАКРЫТ 2026-05-22 (Ф.1–Ф.5, 1038 PASS / 0 FAIL) |
| 03.3 | [03.3-registry-protocol.md](03.3-registry-protocol.md) | HTTP registry-протокол + content-addressing + подпись + transparency log. Задел из 03.1/03.2/03.4 зафиксирован (`DependencyProvider`, `nova.lock` forward-compat, effect-surface в метаданных). | 📋 proposed, отложен (нужна серверная инфраструктура — 03.5) |
| 03.4 | [03.4-effect-aware-tooling.md](03.4-effect-aware-tooling.md) | Effect-aware tooling (Nova-уникальное, Plan 03 §4): `nova info` + effect-surface (агрегированные эффекты публичного API зависимости), effect-diff (supply-chain сигнал), capability-confined deps (`forbid` на границе). `publish`/`search`/`audit` — registry-gated. | ✅ ЗАКРЫТ 2026-05-22 (Ф.1–Ф.4, 1058 PASS / 0 FAIL) |
| 04 | [04-buffer-split-and-external.md](04-buffer-split-and-external.md) | Buffer → StringBuilder/WriteBuffer/ReadBuffer + `external` keyword | ✅ выполнено (Buffer удалён из языка) |
| 05 | [05-as-cast-codegen.md](05-as-cast-codegen.md) | `as`-cast — реализация narrowing в codegen (D54 compliance) | ✅ выполнено |
| 06 | [06-iter-protocol-codegen.md](06-iter-protocol-codegen.md) | `Iter[T]` protocol в codegen — общий for-in (D58 compliance) | ✅ выполнено |
| 07 | [07-as-cast-saturation.md](07-as-cast-saturation.md) | `as`-cast saturation для float→int + spec D54 narrowing semantics | ✅ выполнено |
| 08 | [08-from-into-conversions.md](08-from-into-conversions.md) | `From`/`Into` framework + char/byte/bool + strict if-cond + conversions.md | ✅ почти выполнено, Ф.6 отложен |
| 09 | [09-clang-migration.md](09-clang-migration.md) | Миграция Windows-сборки с MSVC на Clang/LLVM (10-15% perf) | ✅ Ф.1-Ф.5 закрыты 2026-05-11 (Clang — default toolchain: `Auto` = Clang > MSVC > GCC); Ф.6 бенчмарки отложены |
| 10 | [10-pgo-integration.md](10-pgo-integration.md) | PGO integration (stub, после плана 09) — 15-30% perf на hot path | stub / future |
| 11 | [11-method-values-and-overload.md](11-method-values-and-overload.md) | Method values + overload по типу аргумента (закрывает Q-overloading вариант 1) | ✅ ЗАКРЫТ (Ф.8 sweep std — optional, не блокер) |
| 12 | [12-builtins-driven-codegen.md](12-builtins-driven-codegen.md) | builtins.nv-driven external dispatch (Q-codegen-builtins-cleanup) | ✅ ЗАКРЫТ (Ф.6 type-checker gate отложен; Ф.4.5 auto-derive ❌ ОТМЕНЕНО Plan 13 Ф.9.5) |
| 13 | [13-runtime-stdlib-and-autogen.md](13-runtime-stdlib-and-autogen.md) | Runtime stdlib (str/math) + auto-gen std/runtime/*.nv (read-only projection реестра компилятора) | ✅ ЗАКРЫТ (включая Ф.9.2 `+` через `@plus` body + Ф.9.6 bag-fix `StringBuilder.@len` codepoints); user-defined `@plus` routing отложен до method_overloads expansion |
| 14 | [14-stdlib-codegen-gaps.md](14-stdlib-codegen-gaps.md) | Закрыть codegen-gap'ы блокирующие std/* (Iter[T] element-type, const non-trivial, free-fn-as-value, fn-в-record, D69 variadic, `int as char` literal) | ✅ **ЗАКРЫТ 2026-05-12** (Ф.1/2/3/4/6/6-bis/7); Ф.5 → [Plan 35](35-cross-file-resolve.md); type-check регресс → [Plan 34](34-stdlib-typecheck-and-compile-fix.md) |
| 15 | [15-generic-bounds-enforcement.md](15-generic-bounds-enforcement.md) | D72 generic bounds `[T Protocol]` enforcement в type-checker'е | ✅ ЗАКРЫТ (Ф.1-Ф.5; Ф.4 закрыта 2026-05-11 — 3 negative + forward-dep positive; D53 anonymous-protocol literals — отдельная задача) |
| 16 | [16-capability-enforcement.md](16-capability-enforcement.md) | D63 `forbid` + D64 `realtime` compile-time capability checks (закрытие spec-vs-impl drift) | ✅ ЗАКРЫТ (Ф.1-Ф.9 ✅; 97/97 PASS включая 5 negative-tests) |
| 17 | [17-q-resolutions.md](17-q-resolutions.md) | Закрыть полу-открытые Q (string interpolation, clone semantics, array API, и др.) | ✅ ЗАКРЫТ (включая Ф.4 — string interpolation полная реализация: lexer/parser/AST/codegen StringBuilder/interp; 13 regression-тестов) |
| 18 | [18-stdlib-roadmap.md](18-stdlib-roadmap.md) | Stdlib gap-анализ Rust/Go → Nova: P0/P1/P2 приоритизация под backend/CLI нишу + зафиксированные дизайн-решения (libuv, rustls, M:N-aware sync) | 🟡 **PARTIALLY ACTIVE** (2026-05-25): Шаг 1 (std.sync) ✅ зашиплен — формализация [Plan 103](103-sync-primitives-spec-formalization.md); Шаги 2-4 (fs/os/net) → [Plan 91](91-stdlib-mvp-for-0.1.md) std MVP (релизы 0.2+); Шаг 5 (RwLock/Semaphore) deferred |
| 19 | [19-closure-and-error-ops.md](19-closure-and-error-ops.md) | Closure-rev + D85 error-ops (`expr!!` / `expr??` унифицированное поведение для Result/Option, throw-стиль). | ✅ ЗАКРЫТ 2026-05-10 |
| 20 | [20-defer-implementation.md](20-defer-implementation.md) | D90 implementation: `defer` и `errdefer` scope-level cleanup statements | ✅ ЗАКРЫТ (лексер/парсер/AST/codegen LIFO defer-scope; ~27 тестов: syntax + expected_runtime panic-interaction + negative; все в полном прогоне) |
| 21 | [21-channel-revision-implementation.md](21-channel-revision-implementation.md) | D91 implementation: Channel revision на capability-split (Rust mpsc-style); зависит от Plan 20 | ✅ ЗАКРЫТ (capability-split `ChanWriter`/`ChanReader`, negative-тесты sender-no-recv / receiver-no-send проходят) |
| 22 | [22-sleep-libuv-integration.md](22-sleep-libuv-integration.md) | `Time.sleep` через libuv `uv_timer_t` + унифицированный event-loop в scheduler'е; D92 implicit main-scope + D93 park/wake API. Открывает дорогу для Plan 18 P0 stdlib и Plan 21 Channel revision. | ✅ ЗАКРЫТ (Ф.1-Ф.6 ✅; 134/134 PASS + 5 sleep + 2 bench) |
| 24 | [24-cross-platform-test-runner.md](24-cross-platform-test-runner.md) | `nova-codegen test-build`/`test-all` subcommands — единая логика runner'а в Rust, run_tests.ps1/.sh thin wrappers | ✅ ЗАКРЫТ (Ф.1-Ф.3 ✅; Linux smoke — отдельно) |
| 25 | [25-production-readiness-roadmap.md](25-production-readiness-roadmap.md) | Honest gap analysis vs Go/Rust: gaps (M:N, growable stacks, **G3a default malloc-only**, G3b GC pauses, Linux smoke, preemption, cancel propagation) с blockers и acceptance criteria. G7 ✅ closed (Ф.8). | roadmap, не начат |
| 26 | [26-test-runner-hardening.md](26-test-runner-hardening.md) | Test-runner hardening до cargo/go-test уровня: per-test timeout, parallel execution (`--jobs`), structured output (JSON/TAP/JUnit), UTF-8 codepage, verbose/quiet, `--rerun-failed`, wrapper cleanup. Plan 24 был 80% prod-grade, Plan 26 — 95%. Ф.5 caching отложен (не блокирует CI). | ✅ ЗАКРЫТ 2026-05-11 (10/11 фаз) |
| 27 | [27-gc-switch.md](27-gc-switch.md) | **GC switch: Boehm как default** (Plan 25 G3a closer). vcpkg gc.lib уже vendored. Ф.1 add --gc flag → Ф.4 switch к Boehm после bench. **Production blocker для long-running workloads.** | ✅ почти закрыт (Ф.1/1.5/2/3/4/6 ✅ Б.2–Б.8 ✅ Ф.D/G audit-fixes ✅ — detect_boehm graceful fallback с Linux/macOS hints, pause measurement в spec/overview.md; Ф.5 Linux smoke-test ждёт CI) |
| 28 | [28-nova-cli.md](28-nova-cli.md) | `nova` CLI binary — единая точка входа для пользователя (`nova test/build/run/check/regen-runtime`). Заменяет run_tests.ps1/.sh и regen_runtime.ps1. nova-codegen остаётся внутренним инструментом. | ✅ ЗАКРЫТ (Ф.0-7 ✅; nova-cli/ crate, все субкоманды, скрипты удалены) |
| 29 | [29-repo-layout.md](29-repo-layout.md) | Реорганизация корня: `compiler-codegen/` → `compiler/`, `nova-cli/` → `cli/`. Nova-пакеты (nova_tests/, std/) не переименовываются — D78 риск. | план, не начат, низкий приоритет |
| 30 | [30-channel-improvements.md](30-channel-improvements.md) | Channel improvements: `send`→`bool` (no panic on closed) + multi-writer (`tx.clone()` + `writer_count` ref-count). Зависит от Plan 21. | ✅ ЗАКРЫТ (2026-05-11; статус в самом плане-доке) |
| 31 | [31-channel-select.md](31-channel-select.md) | `select` — multiplexed channel receive: ожидание на N каналах одновременно, пробуждение по первому готовому. Парсер + runtime SelectWaiter + codegen. | ✅ ЗАКРЫТ (Ф.1-6 ✅; all-closed panic реализован; 179/179 PASS после D97 fix memory_footprint) |
| 32 | [32-gc-introspection.md](32-gc-introspection.md) | `std.runtime.gc` introspection API (`heap_size`, `live_count`, `alloc_count`, `collect`, `reset_stats`) для differential GC-тестов и production memory pressure observability. | ✅ ЗАКРЫТ (Ф.1-Ф.5 ✅; 181/181 PASS malloc+boehm; differential: heap bounded 348 KB под Boehm на 500k allocs; no-leak: max_growth=0 на 20 циклов × 10k allocs; pause < 16ms на all workloads) |
| 33 | [33-contracts-implementation.md](33-contracts-implementation.md) | D24 контракты — **roadmap-индекс**. Разбит на 33.1 / 33.2 / 33.3 с decision point после каждого. Цель — паритет с Dafny/Creusot, превосходство над Rust+kani/prusti (где tool отдельный, не часть языка). Закрывает Q-contract-dsl и Q-pure-view. | roadmap |
| 33.1 | [33.1-contracts-core.md](33.1-contracts-core.md) | **Core:** `requires`/`ensures`/`old`/`result` + `ContractResult[R,E]` + Z3 SMT + debug runtime fallback + zero-cost release CI gate. | ✅ **V1 ЗАКРЫТ 2026-05-13**: Ф.1-Ф.4 + TrivialBackend SMT + Z3 backend (Cargo feature `z3-backend`, vcpkg libz3 x64-windows-static, env `NOVA_SMT_BACKEND=z3`, own FFI bindings) + `// REQUIRES_SMT_BACKEND` test-runner gate + zero-cost release. Strings/FP/quantifiers — Plan 33.3 advanced milestone. |
| 33.2 | [33.2-contracts-imperative.md](33.2-contracts-imperative.md) | **Imperative:** `reads`/`modifies` frame + loop invariants + `decreases`/termination + composition `#pure` функций + record `invariant` + arrays в SMT + `assert_static`. | ✅ **ЗАКРЫТ bootstrap 2026-05-12**: Ф.5 (frame parser+type-check) + Ф.6 (loop invariants parser) + Ф.7 (decreases+composition+invariant) + Ф.8 (assert_static). SMT verify loops/frame — после Z3. |
| 33.3 | [33.3-contracts-advanced.md](33.3-contracts-advanced.md) | **Advanced (Dafny-parity):** `pure_view`+`axiom`+`#verify_handler` + ghost state + `assume` + bounded quantifiers + IEEE 754 FP + strings/sets/maps теории + incremental cache + parallel + Z3↔CVC5 cross-check + `#must_verify_module` + `#trusted` external + Dafny-tutorial port (20 примеров). | ✅ **ЗАКРЫТ 2026-05-16**: Ф.10 (ghost full codegen+assume+quantifiers) + Ф.11 (IEEE 754 FP+Seq strings в Z3) + Ф.12 (incremental FNV1a cache) + Ф.13 (#must_verify_module + #trusted + nova contracts JSON CLI) + Ф.14 (20 Dafny-parity тестов). 115 PASS 0 FAIL. **Аудит Plan 33.8 (2026-05-21):** parallel verification и Z3↔CVC5 cross-check из исходного scope 33.3 НЕ доставлены — перенесены в роадмап-слоты 33.14/33.15 (никогда не написаны как планы). |
| 33.4 | [33.4-contracts-parity-gaps.md](33.4-contracts-parity-gaps.md) | **Parity gaps closure:** D.0.2 composition + D.0.3 loop invariants AST + D.0.4 decreases WF + D.1.2 frame axiom + D.1.3 quantifiers + D.1.4 trigger stub + D.1.5 ensures_fail + P0-1 V1 handler verify (static axioms) + P1-5 BinderType + P1-6 spec sync. P1-4 Liskov blocked (→ Plan 33.5). | ✅ **ЗАКРЫТ 2026-05-15** (autonomous session; 72/72 contracts/ PASS, 9 SKIP Z3-only) |
| 33.5 | [33.5-contracts-verifier-production.md](33.5-contracts-verifier-production.md) | **Verifier production hardening:** ghost erasure + trigger patterns proper + loop decreases SMT + loop havoc/preservation + purity SCC inference + lemma functions + calc proofs + Liskov V1 + post(Action)(view) symbolic exec V2. Закрывает все оставшиеся V1-упрощения; добавляет lemma/calc для Dafny-parity и symbolic-handler-exec для уникальной Nova-фичи. | ✅ **ЗАКРЫТ** (аудит 2026-05-20): Ф.1.2 triggers + Ф.1.3 loop decreases + Ф.2 havoc + Ф.3 SCC + Ф.4.1 lemma + Ф.4.2 calc + Ф.5.1 EffectMethod contracts + Ф.5.2 Liskov + Ф.6 post(Action) V2 ✅; Ф.1.1 ghost erasure закрыт в Plan 33.6 (compile-error «ghost variable cannot be read in non-ghost code», soundness guard ghost_runtime_use_fail). Аудит: contracts/ suite 239 PASS trivial / 275 PASS z3 / 0 FAIL. |
| 33.6 | [33.6-contracts-production-hardening.md](33.6-contracts-production-hardening.md) | **Production hardening:** soundness honesty (ghost erasure V5 closure + non-silent Unsupported + handler Unknown = error + soundness regression guards) + handler_exec.rs split + AI-first diagnostics (counterexample minimisation + witness extraction + suggest fixes + `#proof_budget` + apply auto-inference + calc pinpoint + trigger ranking + variance inference) + `#must_verify_module` + `#trusted external fn` + subsumption cache + Z3 CI matrix + entry-snapshot для `old()` + V2/V3 positive tests. Big initiatives (bit-vectors / refinement types / opaque-reveal / sets-maps / FP / strings / cross-check / parallel / LSP / AI-loop / distributed / Dafny-port / HOF) — Plan 33.7-33.20. | ✅ **ЗАКРЫТ** (аудит 2026-05-20): Ф.1.1-Ф.1.5 + Ф.2.1 + Ф.3.1-Ф.3.8 + Ф.4.1-Ф.4.2 + Ф.5.1-Ф.5.4 + Ф.6 (25 спринтов TrivialBackend bound coverage, 24 pipeline lints, soundness fix Ф.46 E2408). Последний пункт Ф.3.8 variance inference закрыт аудитом (handler_exec.rs `infer_param_variance` + liskov_variance_diag_fail.nv). **239 PASS trivial / 276 PASS z3 / 0 FAIL**. |
| 33.7 | [33.7-bitvector-overflow.md](33.7-bitvector-overflow.md) | **Bit-vectors + integer overflow theory.** Sized integer типы (`u8`/`u16`/`u32`/`u64`/`i8`/`i16`/`i32`) → `BitVec N` Z3-sort (точная wrap-around семантика); битовые операции `&`/`|`/`^`/`<<`/`>>` → SMT bitvector-теория; атрибут `#nooverflow` генерирует overflow VC для каждой арифм. операции (недоказуемый → compile error). `int` остаётся unbounded Z3 Int. V1: Ф.1 SMT-IR, Ф.2 Z3 FFI, Ф.3 encode+pipeline, Ф.4 #nooverflow, Ф.5 тесты. V2: точная знаковость (bvsdiv/bvslt для i*, bvudiv/bvult для u*), BV cast resize (zero/sign-extend/extract), overflow VC для блочных тел. | ✅ **ЗАКРЫТ полностью (V1 + V2)** (V1 — 2026-05-20, V2 — 2026-05-21): V1 — SortRef::BitVec/BitVecLit + Z3 FFI (bvadd/bvsub/bvmul/bitwise/comparisons/overflow predicates) + encode BV dispatch + `#nooverflow`; V2 — `SortRef::BitVec { width, signed }` (i8/i16/i32 signed), `as`-cast resize через zero_extend/sign_extend/extract, overflow VC для let-bindings/блоков через BvScope subst. 9 фикстур f60_*/f61_* (7 positive + 2 negative). **285 PASS / 0 FAIL** (z3 backend). |
| 33.8 | [33.8-verifier-soundness.md](33.8-verifier-soundness.md) | **Verifier soundness hardening.** Создан по результатам аудита «с чистого листа» при закрытии 33.7 — найдены 3 SOUNDNESS-CRITICAL дыры (верификатор объявлял контракт «доказан», хотя в рантайме он мог быть ложным). Ф.1 — переполнение `int` (codegen эмитил голый C `+` = UB/wrap, верификатор кодировал безграничным Int → `result==a+b` ложно-доказывался): теперь `int` overflow → `panic` (`__builtin_*_overflow` → `nv_panic`), что делает безграничную кодировку sound; `nat ≥ 0` аксиома. Ф.2 — havoc-модель сохранения инварианта цикла не покрывала составные/вложенные присваивания → ложный `Proven`: fail-safe `Warning`. Ф.3 — `assume` lint `trust-introduced`. **Слот 33.8 переиспользован** под soundness (роадмап 33.6 предлагал «refinement types» — спекулятивно, не написан). | ✅ **soundness ЗАКРЫТ (2026-05-21)**: Ф.1 (int panic + nat≥0) ✅, Ф.2 (loop fail-safe) ✅, Ф.3 (assume lint) ✅. Полный `nova_tests`: 931 PASS / 0 FAIL; contracts: 291 PASS / 0 FAIL. V2-остаток (НЕ soundness): Ф.1.3 overflow-VC оптимизация, Ф.2.2 моделирование условных присваиваний, `assume` SMT-интеграция. |
| 33.9 | [33.9-opaque-reveal-fuel.md](33.9-opaque-reveal-fuel.md) | **Opaque/reveal/fuel** для matching-loops control при scaling до 50+ контрактов. `#opaque #pure fn` (body как UF в SMT), `reveal name` statement (раскрывает body в caller scope), `#fuel(n)` (controlled unfolding depth для recursive). Standard Dafny/Verus pattern. Phased: V1 parser+AST+TrivialBackend gate+lints (2 дня), V2 Z3 axiomatic encoding+fuel chain (2 дня research), V3 spec sync (0.5 дня). 5 pipeline lints (reveal-on-non-opaque, dead-opaque, dup-reveal, fuel-without-opaque, reveal-undef). | ✅ **ЗАКРЫТ 2026-05-20** (все фазы): Ф.1+Ф.2+Ф.3 parser/AST ✅ (`#opaque`, `reveal X`, `#fuel(N)`), Ф.4 ✅ TrivialBackend opaque-aware W2402 (term_references_opaque_uf + NotAttempted→UnsupportedTheory upgrade), Ф.5 ✅ Z3 axiomatic encoding + fuel chain, Ф.6 ✅ 5 pipeline lints (E2411 + 4× W2402/W2403), Ф.7 ✅ spec D130 + idiom doc. **239 PASS / 0 FAIL**. |
| 33.14 | [33.14-z3-cvc5-crosscheck.md](33.14-z3-cvc5-crosscheck.md) | **Z3↔CVC5 cross-check** — вторая линия защиты soundness (в дополнение к 33.8 Ф.7 regression-suite). Каждая VC прогоняется через два независимых пути — Z3 (FFI) и CVC5 (текстовый SMT-LIB подпроцесс); расхождение определённых ответов (`Proven` vs `Disproved`) → CI-fail. Ловит баги кодирования Nova→SMT (например 33.8 Ф.6.2 — Z3-FFI молча терял формулу) и баги решателя. **CI-only** — обычная компиляция не замедляется. Реанимирует обещание Plan 33.3 (было в scope, не доставлено). Ф.1 SMT-LIB эмиттер, Ф.2 CVC5 runner, Ф.3 cross-check режим, Ф.4 сравнение+diff, Ф.5 CI-gate, Ф.6 тесты. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.6; CVC5 через текстовый SMT-LIB, не FFI — второй независимый кодировщик; 47 unit-тестов; корпус 228 файлов — 0 расхождений; gate NOVA_CROSSCHECK=1 + workflow contracts-crosscheck) |
| 34 | [34-stdlib-typecheck-and-compile-fix.md](34-stdlib-typecheck-and-compile-fix.md) | stdlib type-check + compile fix. **Ф.1-3** type-check: 33/44 → 45/45 + `std.testing.handlers` + 4 parser-фикса. **Ф.5.1** `--skip` флаг (агент). **Ф.5.2** `int as char` refactor через `char.try_from` в 5 файлах. **Ф.7** production-grade: LCG → **xoshiro256++** + splitmix64 init + `seed u64` + bytes(n) 8 байт/advance + **`mut_clock(start_ms)`** (mutable virtual clock). **Followup #1**: `std/runtime/gc.nv` заполнен 5 external fn declarations. **Ф.8 followup #2**: spec sync (04-effects.md mut_clock + 08-runtime.md D26 signatures) + 8 focused tests в `nova_tests/plan34/` (4 positive: gc namespace, xoshiro/mut_clock inline reproducers; 4 negative EXPECT_COMPILE_ERROR markers для Ф.2 parser fixes). | ✅ **ЗАКРЫТ 2026-05-12** (Ф.1-8 ✅; 45/45 std type-check, 8/8 plan34 tests, 249/256 nova_tests — 7 fail'ов агента из Plan 44.1 ABI change) |
| 35 | [35-cross-file-resolve.md](35-cross-file-resolve.md) | Cross-file resolve для multi-file stdlib через codegen. **Bootstrap MVP закрыт 2026-05-12**: Ф.1 R1-R21 реализован, **R31** ✅ unified pipeline (shared `resolve_imports_inline` для nova check/build/test), **R3 cycle detection** ✅ (BFS → DFS с in_progress/visited множествами), **Sub-plan 35.A R26+R27** ✅ (selective `import X.{A, B}` + `export import X` + `std/prelude.nv` auto-import; R25 wildcard и R28 pub granularity ❌ spec-rejected D29/D5), **Sub-plan 35.B sync** ✅ (workspace-aware find_repo_root). Open: 35.B on-disk cache + incremental, 35.C cross-file generic bounds, 35.D stable mangling v0, 35.E cfg/editions, R26 type-checker visibility enforcement (post-bootstrap). | ✅ **bootstrap MVP ЗАКРЫТ 2026-05-12**; advanced sub-plans (35.B cache/35.C/D/E) — open → перенесены в [Plan 81](81-module-resolution-hardening.md) |
| 36 | [36-cli-production-hardening.md](36-cli-production-hardening.md) | Production hardening `nova check` / `nova test` — **R1-R30 + AD1-AD12** после **4-way audit** (160 gaps closed). MVP реализован (commits 0b9ec6ec7f + 62c04378fa): `nova check <path>` + `nova test <path>` polymorphic; exit codes 0/1/2/101 (1=diagnostic, 2=usage, 101=panic через std::panic::set_hook); Ф.0 correctness fix (cmd_check теперь дёргает infer_effects + lint_module); --color auto/always/never + NO_COLOR/CI/CLICOLOR/TERM=dumb auto-detect; parallel check = num_cpus с deterministic output. **Sub-plans 36.A-E** (post-MVP): outputs (JSON/SARIF/JUnit), caching (blake3), CI (pre-commit/GHA), ergonomics (--explain/completions), workspace. `--tests-dir` clean break. | **MVP ✅ ЗАКРЫТ** (Ф.0+Ф.1+R7+R10) |
| 37 | [37-typecheck-semantic-parity.md](37-typecheck-semantic-parity.md) | Type-checker semantic parity с codegen: перенести D54-проверки (запрещённые `as`-касты — 24 пары; strict bool в `if`/`while`) из `emit_c.rs` в type-checker. Сейчас `nova check` пропускает, `nova test`/`nova build` падает в codegen — нарушение контракта `nova check` (Plan 36 / D95) «полная type+lint валидация». Triggered by `std/encoding/hex.nv` (`int as char`). | план, не начат, средний приоритет |
| 38 | [38-numeric-type-constants.md](38-numeric-type-constants.md) | `int.MAX` / `f64.NAN` / `u8.MAX` / etc. — codegen mapping для D26-prelude numeric type constants. ~30 mappings (signed/unsigned int, char, f32/f64). `numeric_type_constant_mapping` static helper в emit_c.rs. `<float.h>` include в nova_rt.h. | ✅ ЗАКРЫТ (commit 64d1a41c1a, 18 sub-тестов в numeric_constants.nv PASS, 210/210 regression) |
| 39 | [39-range-stdlib-fixes.md](39-range-stdlib-fixes.md) | `std/collections/range.nv` stdlib fixes. **All 4 issues closed**: Issue D (b516bba9ae) Iter[T] D58 diagnostic + mut-receiver. Issue B/C already implemented. **Issue A** (handler-flow inference D61 §10): runtime NovaInterruptFrame.value_ptr + codegen category-routing (IntLike/Pointer/ValueStruct) + auto-gen Option-helpers. range.nv + handler_flow_inference.nv PASS. | ✅ **ЗАКРЫТ 2026-05-12** (все Issue A/B/C/D; 225/225 regression) |
| 42 | [42-folder-modules.md](42-folder-modules.md) | Folder-modules (Go-style peers): module = file `X.nv` ИЛИ folder `X/` с ≥1 peer-файлами. Все файлы folder объявляют одинаковый `module <path>` и share namespace. Закрывает «internal helpers» + scaling больших std модулей. Реализует **D29 rev-2 + rev-3** (parent.X declarations). MVP ✅ ЗАКРЫТ 2026-05-12. Sub-plans с числовой нумерацией (leading zeros для sort order): **42.01-42.16 ВСЕ закрыты** (42.02 → Plan 45; 42.03, 42.07 ❌ rejected). 42.15 — per-peer import isolation (Rule C strict); 42.16 — module-attrs перед `module` + `#cfg` операторы `\|\| && !`. | ✅ **ВСЕ sub-plans закрыты** (2026-05-14, 338 PASS) |
| 42.04 | [42.04-per-file-imports-scope.md](42.04-per-file-imports-scope.md) | Per-file imports scope refactor (Plan 42 правило C). `PeerFile` AST struct + FileId propagation через lexer/parser/imports + type-checker per-peer name resolution (`peer_module_names: HashMap<FileId, HashSet<String>>`). Позитивный тест `folder_per_file_imports/`. Rule C частично enforced (ограничение [M10] в simplifications.md). 278 PASS. | ✅ **ЗАКРЫТ 2026-05-14** (Шаги 1-3 + тесты) |
| 42.08 | [42.08-audit-closing.md](42.08-audit-closing.md) | Closing audit gaps Plan 42 (2026-05-14). Ф.1: 2 negative tests (folder_file_vs_folder_conflict, folder_cycle_between_modules; 3-й Test inconsistent_decl N/A — manifest rev-3 catches per-peer). Ф.2: ResolveErr enum + diagnostic для ambiguous X.nv+X/. Ф.3: Rule H comment terminology fix + [M11] simplifications entry для Rule A canonical PathBuf keying. 284 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.09 | [42.09-re-export.md](42.09-re-export.md) | `export import X.{A as B}` — re-export через `is_export` flag + item rename (Plan 42.09). Bootstrap MVP работает через inline expansion + selective list filtering. 2 tests (reexport_facade_use + rename_import_use). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.10 | [42.10-module-level-forbid.md](42.10-module-level-forbid.md) | Module-level `#forbid` через `_module.nv` convention. `_module.nv` attrs пропагируются всеми peers через `inherited_attrs` в resolve_one. CapabilityCtx применяет к compiled module. 2 tests (positive folder_module_forbid_use + negative module_forbid_violation). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.11 | [42.11-inline-module-doc.md](42.11-inline-module-doc.md) | `#doc "..."` attribute для module-level documentation. AST: `ModuleAttrKind::Doc(String)`. Multi-peer merge через Plan 42.10 inherited_attrs. Consumer — Plan 45 (nova doc). 1 test (folder_with_doc_use). | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.13 | [42.13-internal-naming.md](42.13-internal-naming.md) | `internal/` extended naming (D29 rev-3.1). Single-file/folder-module внутри `internal/` → `owner.internal.target` (3 segments) вместо `internal.target` (collision). Edge case: `internal/` сама folder-module → `owner.internal` (2 segments). manifest.rs special-case ~35 LOC. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.12 | [42.12-cfg-conditional-compilation.md](42.12-cfg-conditional-compilation.md) | Conditional compilation (D99): filename suffix (`_windows.nv`, `_linux.nv`, `_macos.nv`, `_unix.nv`, `_posix.nv`) + module-level `#cfg(feature = "X")` / `#cfg(target_os = "Y")`. NOVA_TARGET_OS / NOVA_FEATURES env override. **Plan 18 P0 unblocked**. Predicates `any/all/not` + item-level `#cfg` добавлены в 42.14. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.14 | [42.14-backlog-closure.md](42.14-backlog-closure.md) | Backlog closure (6 deferred items). Ф.1 `#cfg(any/all/not)` recursive predicates. Ф.2 item-level `#cfg`. Ф.3 [M11] Rule A cycle detection по module-name (symlink-safe `read_module_decl`). Ф.5 Rule H fix через rev-3.1 consistency. Ф.4+Ф.6 → 42.15 (общий NameResCtx refactor). 328 PASS. | ✅ **ЗАКРЫТ 2026-05-14** (4/6; Ф.4+Ф.6 re-defer) |
| 42.15 | [42.15-per-peer-import-isolation.md](42.15-per-peer-import-isolation.md) | Per-peer / per-import isolation (NameResCtx refactor). Закрывает [M10] Rule C Path-form + [M12] selective import visible-scope. `PeerFile.imported_item_names` + `is_entry_module` + NameResCtx `group_decls` (per module-group) + `peer_imported_names` (per-peer imports). `export import` re-export propagation. 2 tests. 331 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.16 | [42.16-module-attr-syntax.md](42.16-module-attr-syntax.md) | Module-attr syntax: `#forbid`/`#cfg`/`#doc` ПЕРЕД `module` (консистентно с item-level) + `#cfg` операторы `\|\| && !` вместо `any/all/not` (precedence parser). Fix: module-scan helpers пропускают `#`-атрибуты. 13 файлов мигрированы. spec D99/D100/D101 update. 338 PASS. | ✅ **ЗАКРЫТ 2026-05-14** |
| 42.17 | [42.17-audit-closure.md](42.17-audit-closure.md) | Post-42.16 production-readiness audit для Plan 42 folder-modules: doc drift fix, dead code cleanup, Rule H implementation, module-attr handling. Ф.8 entry-folder-module isolation — honest-defer с полным дизайном в simplifications.md (not a bug). Закрывает spec/impl drift в Plan 42.04/42.09/42.14, `[M10]`/`[M12]` resolved для imported folder-modules. | ✅ ЗАКРЫТ 2026-05-14 (9 фаз; Ф.8 designed-defer) |
| 44 | [44-mn-runtime-roadmap.md](44-mn-runtime-roadmap.md) | M:N runtime — **umbrella roadmap** (thread pool + work-stealing + concurrent GC + TLS migration). Sub-plans 44.1–44.7 ниже. Был Plan 23 до 2026-05-13. | roadmap; sub-plans: 44.1/44.2/44.4/44.5/44.7 ✅, 44.3 ⬛ superseded by Plan 82, concurrent GC — open (v1.0+) |
| 44.1 | [44.1-channel-hardening.md](44.1-channel-hardening.md) | M:N channels hardening (was Plan 40). production-parity с Go/Rust mpsc: atomics + mutex + doubly-linked + cache padding + 8 audit rounds. D91/D94 spec. | ✅ **ЗАКРЫТ** (Windows + Linux Docker validated) |
| 44.2 | [44.2-fiber-arena-posix.md](44.2-fiber-arena-posix.md) | M:N fiber arena POSIX (was Plan 41). Per-thread mmap + lazy commit + single Boehm GC root. Снимает MAX_ROOT_SETS=128. D97 spec. | ✅ **ЗАКРЫТ** (Этапы 1-5 + R8 audit + P41-3/P41-6) |
| 44.3 | [44.3-fiber-arena-windows.md](44.3-fiber-arena-windows.md) | M:N fiber arena Windows (was Plan 43). 4 неудачные попытки 2026-05-13/14; документированный диагноз «TIB не обновляется» **опровергнут** (Plan 82 §1.1–1.2 — minicoro asm свопает TIB всё время). Исторический документ. | ⬛ **SUPERSEDED BY [Plan 82](82-windows-fiber-arena.md)** (2026-05-22) |
| 44.4 | [44.4-mn-runtime-stage0.md](44.4-mn-runtime-stage0.md) | M:N runtime Этап 0 — infrastructure proof-of-concept: NovaWorker struct, runtime.init/shutdown, std.runtime.runtime API. (Был Plan 23.1, потом Plan 44.1, переименован для chronological order.) | ✅ **Этап 0 ЗАКРЫТ 2026-05-13** (266 PASS / 0 FAIL Windows; Linux Docker smoke PASS) |
| 44.5 | [44.5-work-stealing-scheduler.md](44.5-work-stealing-scheduler.md) | M:N work-stealing scheduler: Chase-Lev deque (L1) + worker integration (L2) + per-worker libuv loop (L3 — TLS) + Boehm GC_THREADS (L4) + actual workload distribution через codegen routing (L5) + D98 spec (L6). | ✅ **ЗАКРЫТ 2026-05-14** (L1–L6; Layer 5 park/wake worker integration — commit `8fcbc67fddb`, GC_THREADS на всех платформах) |
| 44.7 | [44.7-preemption.md](44.7-preemption.md) | Signal-free preemption: sysmon-thread + codegen safepoints (пролог функции + backedge цикла). CPU-bound fiber не монополизирует worker — кооперативный yield по флагу. Вариант B (не SIGURG — `mco_yield` не async-signal-safe). | ✅ **ЗАКРЫТ 2026-05-14** (sysmon + yielded-FIFO + uv_run-per-iter; `mn_runtime_preemption.nv` 4/4; D103 spec) |
| 45 | [45-nova-doc.md](45-nova-doc.md) | `nova doc` — production-grade documentation tooling. **MVP (Ф.0-Ф.20)**: D104-D107 spec + lexer `///`/`//!` + parser attach + DocModel + markdown/JSON renderer + intra-doc links + doc-tests + `--check`/`--watch` CLI + CI + 14 golden snapshots. **Production hardening (Ф.21-Ф.29)**: workspace mode + `--strict` + must_verify SMT wiring + handler matrix + capability badges + 25-pt Nova-unique audit + JSON Schema v1.0.0 STABLE + mutation testing. **HTML+search (Ф.31)**: single/multi-page + dark mode + sitemap + syntax highlighting (без external deps). **MCP/CLI (Ф.32-Ф.34)**: `nova doc-query` DSL + JSON-RPC stdio/HTTP MCP server + incremental mtime cache + nova.toml `[doc]` config. **stdlib doc-pass (Ф.35-Ф.36)**: ~258 items documented across duration/vec/json/path + 6 batches checksums/concurrency/identifiers/math/data/crypto/testing/bench/prelude + **batch 7** (collections×9 + snowflake + runtime×4, 14 файлов, коммит `743dcc709ab`). Породил Plan 60 / 61 / 62 как follow-ups. | ✅ **ЗАКРЫТ 2026-05-16** (Ф.0-Ф.36 + batch 7; branch `plan-45-doc` merged; 100+ golden tests PASS; JSON Schema v1.0.0 STABLE) |
| 46 | [46-named-parameters.md](46-named-parameters.md) | Именованные аргументы `f(name: val)` + значения параметров по умолчанию `fn f(x int = 0)` (D102). Парсер + type-checker binding-pass + codegen call-site reorder. Блокирует Plan 47. | ✅ ЗАКРЫТ 2026-05-14 (Ф.0-Ф.5; argbind.rs + callnorm.rs; 11 тестов) |
| 47 | [47-supervised-cancel.md](47-supervised-cancel.md) | `supervised(cancel: tok)` — удаление keyword `cancel_scope` (ревизия D75 2026-05-14). Caller-owned `CancelToken` + runtime bind-check. Зависит от Plan 46. | ✅ закрыт 2026-05-14 (Ф.0–Ф.4+Ф.6; Ф.5 stdlib `within`/`race` → Plan 48) |
| 48 | [48-closures-in-generics.md](48-closures-in-generics.md) | **Monomorphization** — generic-функции/методы/типы без type-erasure (Rust-grade, zero-cost). Сейчас codegen 100% erasure (всё `void*`) → хуже Go/Rust/TS. Instantiation worklist + мангление + fixpoint. Замыкания в generic'ах выпадают тривиально (исходная цель). Разблокирует Plan 47 Ф.5 + `retry.nv`. Переписан 2026-05-15 (был erasure-adapter подход). Ф.9 method-param mono (`Wrapper[T] @map[U]`) — Plan 63 Fix C followup, branch `plan-48-mpm`. | 🟡 **partial 2026-05-17**: Ф.0-Ф.9 + Ф.4 + Ф.5 + Ф.6 + Ф.7.1-7.7 + Ф.8 ВСЕ ✅ DONE; остаток — partial acceptance: generic `[]T` return-path (→ Plan 54), retry.nv codegen (→ Plan 54), erased-emitter V1 fallback (unit-variant usage-context); 668 PASS / 2 FAIL (Windows UAC os 740 baseline, identical to main) |
| 49 | [49-cancel-throw-routing.md](49-cancel-throw-routing.md) | Cancellation semantics — kinded throws (`USER`/`CANCEL`) + **typed** cancel reason `CancelToken[T]` (лучше Go `error` / TS `any`) + USER-precedence (лучше Go errgroup) + defer-on-cancel (Rust Drop паритет) + cross-type cascade через `From`-конвертацию (D73/D77) + `tok.merge(other)` композиция (beyond Go/TS/Rust). Отмена ≠ ошибка, не убегает из `supervised(cancel:)`. Закрывает `[M-cancel-throw-routing]` + `[M-within-error-conflation]`. | ✅ **ЗАКРЫТ 2026-05-16** (Ф.0-Ф.7 + audit sprint EOD: per-T un-box reason (P0 silent UB fix), cross-type cascade with From-protocol, tok.merge композиция; 6/6 cancel_*_test.nv PASS на release nova; runtime infra в effects.h/fibers.h: NovaThrowKind, nova_throw_cancel, first_error_kind atomic, linked_converters parallel array). Deferred → Plan 50: `cancel_defer { ... }` keyword (parser changes). |
| 50 | [50-default-keyword-only.md](50-default-keyword-only.md) | Параметр с дефолтом — keyword-only на месте вызова (ревизия D102 2026-05-15). Ограничение поверх Plan 46 + production-grade диагностика + миграция кодовой базы. | ✅ ЗАКРЫТ 2026-05-15 (Ф.0-Ф.5; structured suggestions в Diagnostic; [M-interp-named] RESOLVED; 12 тестов) |
| 51 | [51-d55-record-literal-unification.md](51-d55-record-literal-unification.md) | Record-literal: тип пишется ровно один раз — запретить дублирование в `=>` и `let` аннотациях. Узкий scope ~24 sites `-> T => T{}`. Investigation: полная D55 не оправдана. Ф.1 codegen (4 LOC), Ф.2 parser (40 LOC), Ф.3 миграция, Ф.4 тесты, Ф.5 spec. Spec D55 honesty-fix. | ✅ ЗАКРЫТ — Ф.1-Ф.5 в main (`658954c`..`a1c7459`) |
| 52 | [52-hashmap-literals.md](52-hashmap-literals.md) | HashMap-литералы: `{field: v}` map-coercion (ревизия D55) + `[k: v]` map-литерал (D108). Десугаринг сразу в `with_capacity`+`@insert`, без промежуточных объектов. | ✅ ЗАКРЫТ 2026-05-16 (Ф.0–Ф.8; AST/parser/type-checker/desugar с turbofish via annotate_module pass; 2 negative + 3 positive PASS; 0 регрессий) |
| 52.1 | [52.1-hashmap-literals-advanced.md](52.1-hashmap-literals-advanced.md) | HashMap-литерал advanced features: spread `[...defaults, k: v]`, const map-literal, MIR-lowering. Зависит от Plan 52 ✅ + Plan 48 Ф.7.8+ (mono-instance tracking). Ф.1 spread ~80 LOC (после Plan 48 fix), Ф.2 const ~300 LOC (заблокирована const-ordering bug). Deferred от Plan 52 из-за real-complexity vs bootstrap priorities. | 📋 план, не начат, **P3** |
| 52.2 | [52.2-bootstrap-codegen-fixes.md](52.2-bootstrap-codegen-fixes.md) | Codegen fixes: `emit_record_lit` fallback для match-body context (anonymous record literal в while-body), const-decl ordering (forward-declare mono'd types — const HashMap type unknown). Два фикса (50-100 LOC + 80 LOC). Разблокирует Plan 52.1 Ф.1+Ф.2. Depends: Plan 48 mono-pass. | ✅ ЗАКРЫТ — Ф.1+Ф.2 done; mono-блокеры сняты Plan 55/56, spread работает (2026-05-22) |
| 52.3 | [52.3-remaining-map-features.md](52.3-remaining-map-features.md) | Закрытие оставшихся `[M-52-*]` маркеров: Ф.1 пустой `[]` в map-position ✅ (17 LOC annotate); Ф.2 mono-pass quirk, Ф.3 spread, Ф.4 type-inference edges — deferred (зависят от Plan 48 mono-pass fix). | ✅ ЗАКРЫТ — Ф.1 + Ф.2/Ф.3/Ф.4 сняты Plan 55.4/55.5/56 (2026-05-22) |
| 53 | [53-let-destructuring.md](53-let-destructuring.md) | Record-destructuring в `let` — `let { tx, rx } = ch` вместо `let tx = ch.tx; let rx = ch.rx`. AST/parser/match-codegen уже было; добавил emit_record_destructure + refutability-check в type-checker. | ✅ ЗАКРЫТ 2026-05-15 (codegen + interp + refutability; 7 positive + 4 negative тестов) |
| 54 | [54-codegen-followups-from-48-49-audit.md](54-codegen-followups-from-48-49-audit.md) | Codegen followup items из Plan 48/49 audit: pattern-var leak (emit_test snapshot/restore), int-extension record field dispatch, unit-variant inference (accepted-as-is — паритет с industry), generic array return mono, nested call inference (Source 2d), polymorphic recursion test. Unblock `retry_test.nv` (Plan 48). 11 new tests, 0 регрессий. | ✅ ЗАКРЫТ 2026-05-16 (7/8 фаз; 537 PASS preserved) |
| 55 | [55-codegen-followups-from-plan-54.md](55-codegen-followups-from-plan-54.md) | Codegen follow-ups от Plan 54 + Plan 52.x mono-pass blockers + baseline cleanup + spread + spec sync. 8 фаз prod-grade (P3→P1) + followups: array-of-func-mono, match-arm pattern_inner_type, infer-return-type-from-body, mono-pass corruption (save/restore + protocol whitelist + NOVA_DEBUG_MONO), name-collision + multi-instance (Pattern::Record), baseline NEG-* cleanup (ASCII anchors + decreases bug fix), fixture directories convention, map-spread infrastructure, spec sync D45+D108. | ✅ **ОКОНЧАТЕЛЬНО ЗАКРЫТ 2026-05-16 EOD** (Ф.0-Ф.8 + 4 followups; **557 PASS / 0 FAIL / 40 SKIP** real; **10+ M-маркеров closed**; deferred items → Plan 56 (vtable) / 57 (perf bench) / 58 (cross-toolchain)) |
| 56 | [56-vtable-dispatch-erased-generics.md](56-vtable-dispatch-erased-generics.md) | **Hybrid dispatch** для bound-K methods + real field array element type propagation. Phases: Ф.1 vtable runtime infra (3 protocols + 5 primitive K vtables) ✅; Ф.2 widened stub + placeholder mono skip + effect-free enforcement в bound methods (D122) + AI-first diagnostics ✅; Ф.3 stdlib unlock — `HashMap.@clone()` + `@merge_from()` + `@filter()` через direct field access + GC stress ✅; Ф.4 spec D122 + perf-conventions.md + stdlib-bound-dispatch.md ✅. **Real fix**: `compute_array_elem_type_for_obj` для произвольной глубины `obj.f1.f2.field[i]`. Закрывает `[M-erased-generic-method-dispatch]`. | ✅ **ЗАКРЫТ как production-grade для bootstrap 2026-05-16** (7 tests / ~30 sub-tests; vtable codegen integration для truly erased dispatch deferred — не нужен в single-crate bootstrap, Plan 03 future) |
| 57 | [57-perf-benchmark-infrastructure.md](57-perf-benchmark-infrastructure.md) | Performance benchmark infrastructure — **production-grade, лучше Criterion (Rust) / `testing.B`+benchstat (Go) / tinybench (TS) по нескольким axes**. **ВСЕ 8 phases закрыты 2026-05-17** (worktree `nova-lang-p57`, branch `plan-57`, 50+ commits). **MVP**: contextual `bench "..." { measure { ... } }` DSL + `bench.*` namespace; pure-Rust stats (median/MAD/Tukey/Welch); `nova bench run/diff/gate`; JSON v1; bench.toml. **57.A** (5): history-add, dashboard echarts, calibrate noise-floor, thermal/load detection, --profile samply. **57.B** (5): 10-file corpus + Criterion-compat JSON + `(n in [...])` sweeps + CPU instr FFI + group/case. **57.C** (8): PerfTimer hooks, gc.last_pause_ns, heap sampler, CPU instr per-sample, recursive discovery, history-squash, lints, corpus subcommand. **57.D** (5): PerfTimer aggregation, sleep-lint Path-form, aggregated JSON, multi-runner baselines + CI matrix, HTML compiler-perf dashboard. **57.E** (3 impl + 3 sketch): drill-down dashboard, PELT anomaly detection, e2e shell tests. **57.F** (4, sketches → impl): SSH distributed bench coord (`bench remote list/ping/run`), AI regression interpretation (`bench diff --explain` через system curl, opt-in), memory bandwidth measurement Linux (`bench membw-check` — LLC-miss + Intel MBM + AMD DF probe), extended e2e tests. **57.G** (5, audit-driven small batch): drift slope+R² в JSON, defensive unwrap fixes, errno decoder для perf_event_open paths, ASCII histogram (`--histogram` flag), `bench.metric(name, value, unit)` custom metrics DSL (closes Go `b.ReportMetric` gap). **57.H** (3, cross-platform): multi-group geomean (benchstat-style per-group lines), `nova bench hyperfine` cross-binary timing, `nova bench callgrind` cross-platform CPU instructions via valgrind subprocess. **Test coverage**: 113+ unit tests + 11 `.nv` tests + 110 e2e asserts ALL PASS. **Zero new Rust crate deps**: AI/distributed/callgrind use system binaries (curl/ssh/valgrind); membw/cpu_instr raw libc syscall FFI. См. `docs/bench-conventions.md` + spec D121 в `spec/decisions/09-tooling.md`. | ✅ **MVP+A+B+C+D+E+F+G+H ЗАКРЫТЫ 2026-05-17** (worktree p57, 50+ commits) |
| 57.E.2 | [57.E.2-distributed-bench-sketch.md](57.E.2-distributed-bench-sketch.md) | Distributed bench coordination — design-sketch (SSH-координация bench-прогонов на нескольких машинах). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 57.E.3 | [57.E.3-ai-regression-interpretation-sketch.md](57.E.3-ai-regression-interpretation-sketch.md) | AI regression interpretation — design-sketch (`bench diff --explain` через LLM). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 57.E.4 | [57.E.4-memory-bandwidth-sketch.md](57.E.4-memory-bandwidth-sketch.md) | Memory bandwidth measurement — design-sketch (`bench membw-check`, Linux LLC-miss/MBM/AMD DF). Поглощено реализацией Plan 57.F. | design-sketch (исходный эскиз; реализовано в 57.F) |
| 58 | [58-cross-toolchain-msvc-verification.md](58-cross-toolchain-msvc-verification.md) | Cross-toolchain MSVC + GCC verification matrix. CI gate на 3 OS × 2-3 toolchains, `docs/toolchain-matrix.md`. Closes Plan 55 acceptance "Cross-toolchain MSVC verify". ~2 dev-days. | план, не начат, **P2** |
| 59 | [59-tuple-monomorphization.md](59-tuple-monomorphization.md) | **Tuple monomorphization** — mono'd `_NovaTuple____<T1>__<T2>` structures (вместо generic `_NovaTupleN` с int slots). Phase 1 infra (registry + emit marker), Phase 2 codegen (apply_type_subst/type_ref_to_c/TupleLit/emit_for tuple destructure all mono'd когда concrete), Phase 3 unlock idiomatic `for (k, v) in hashmap`, Phase 4 spec D123. Параллель Rust/Swift mono tuples. Закрывает `[M-mono-tuple-element-types]`. | ✅ **ЗАКРЫТ 2026-05-17** (idiomatic `for (k, v) in coll` works; legacy `_NovaTupleN` сохранён как fallback для erased contexts) |
| 60 | [60-len-access-uniformity.md](60-len-access-uniformity.md) | **Size-accessor uniformity** — `.len()` / `.is_empty()` / `.cap()` исключительно через method-вызов для всех container'ов (built-in `[]T`, `str`, и user-types: HashMap/Lru/Set/Range/Deque/Queue/Vec). Запретить field-access `c.len` без скобок (compile-error + machine-applicable fix-it). Закрывает Java-патологию «массивы field, коллекции method» (Nova сейчас — точное соответствие). Rust-парадигма; TS-style property отвергнут (D-блок «no computed property с эффектами / O(n) скрытой стоимостью»). Migration scope ~518 occurrences. Породил Plan 45 Ф.36. D117 (новый). | ✅ **ЗАКРЫТ 2026-05-17** (Ф.0-Ф.6: registry additions + auto-migration tool ~150 LOC + atomic field-access switch + method-value disambiguation + spec D117 + cross-ref cleanup; branch `plan-60`; **562 PASS / 0 FAIL**; cross-toolchain Clang/MSVC/GCC) |
| 61 | [61-typed-error-effect-codegen.md](61-typed-error-effect-codegen.md) | Typed `Fail[E]` codegen. Закрывает 3 silent UB в effect-codegen (throw type-pun, `nova_throw_value` placeholder, handler-arm wrong C-type). TypeId runtime infra (`typeid.h/.c`), typed payload в fail-frame (`error_user_payload` + `NovaTypeId`), per-E dispatch (`_nova_throw_typed_<E>` + per-E vtable/TLS), `expr!!` typed throw, D65 spec amend. | ✅ **ЗАКРЫТ 2026-05-17** (impl `75b3da4682b` + merge `657630e39ae` + closure `4cd6b7f650c` «all 4 deferred resolved, 0 simplifications»; маркеры `[M-plan-61-*]` ×4 RESOLVED; реализовано через payload-in-fail-frame + per-E dispatch followup#4). Шапка обновлена 2026-05-21 (была устаревшая «proposed/не начат») |
| 62 | [62-prelude-hardcode-migration.md](62-prelude-hardcode-migration.md) | Migrate hardcoded prelude → `std/prelude.nv`: D26 compliance, splittable (`partial_prelude`), edition-versionable. 50+ items (Option/Result, print/println, RuntimeError, protocols, effects). 6 sub-plans (62.A core types / 62.B runtime fns / 62.C error types / 62.D collections / 62.E protocols / 62.F effects + `no_prelude` enforcement + edition-versioning). Verified drift: ~50 items vs ~13 claimed; `no_prelude` — silent bug, `PRELUDE_VERSION` — только маркер. | ✅ **ЗАКРЫТ 2026-05-18** (P0 scope: 62.A/B/C/D non-opaque/E/F все ✅; **62.D non-opaque bis-1 ЗАКРЫТ 2026-05-18** — Range/RangeIter prelude facade migration + 4 latent codegen bugs fixed + D29 W_PRELUDE_SHADOW basic; **62.F.bis ЗАКРЫТ 2026-05-18** — edition versioning + structured W_PRELUDE_SHADOW + Time/Mem formal decls + D124/D125; **62.D.bis ЗАКРЫТ 2026-05-18** — opaque types D126; **62.B.bis ЗАКРЫТ 2026-05-18** — print/println D69 variadic + `[]any` + Plan 67 hotfix absorbed; deferred sub-plans 62.C.bis RuntimeNoneError / 62.E.bis TryFrom/TryInto — остаются blocked externally; **62.A.bis ЗАКРЫТ 2026-05-20**). PRELUDE_VERSION bumped 2 → 3 → 4 → 5 → 6 → 7. **713 PASS / 0 FAIL / 44 SKIP** + 16/16 sum_schema unit tests. |
| 63 | [63-cross-module-mono-dispatch-correctness.md](63-cross-module-mono-dispatch-correctness.md) | **Cross-module + mono dispatch correctness** — family из 6 compiler bugs обнаруженных в Sprint F.36 regression (`nova_tests/plan11_followup/`). Все из-за того что compiler tracks N (signature/typearg/overload) во-внутри-module, но теряет N при пересечении module boundary. **Fix A**: return-type inference для method registered через external_registry. **Fix B**: variadic signature через alias-import `import X as p`. **Fix C**: generic anonymous record `Box[T]` (`Nova_T_p` placeholder leak) — resolved через Plan 59 mono pipeline. **Fix D**: typeargs-aware overload dispatch (`HashMap[str,int].from`). **Fix E**: `for (k,v)` в generic method body. **Fix F**: `Result[(T,U), E]` Ok-payload destructure. | ✅ **ЗАКРЫТ 2026-05-17** (Fixes A/B/C/D/E/F all resolved; 16/18 PASS в `plan11_followup/` — 2 Windows AV os 740 deferred baseline) |
| 62.A.bis | [62.A.bis-sum-schema-registry.md](62.A.bis-sum-schema-registry.md) | **Generic schema registry для sum-types** — sub-task Plan 62 (см. §«`Result[T,E]` mono compromise» + Open Question #1). Layered registry поверх hardcoded `sum_schemas`: file-based декларации в `std/prelude/core.nv` (Option/Result/Error) + `std/prelude/errors.nv` (RuntimeError) получают приоритет, hardcoded baseline остаётся как fallback для ABI compat с `nova_rt/array.h` value-type helpers. 4 incremental phases (3-4 dev-days). **Unblocks**: 17 deferred Option/Result methods из Plan 62.A + Plan 62.C RuntimeError migration. **NOT в scope**: полная sum-type монoморфизация (Plan 59 Phase 7.5). | ✅ **ЗАКРЫТ 2026-05-20** (Phases 1-3 closed: registry infra + variant unification + method routing; Plan 62.A follow-up: 17 `external fn` деклараций в `std/prelude/core.nv` + 29 регрессионных тестов в `nova_tests/plan62/`; **855 PASS / 0 FAIL** / 47 SKIP; Phase 4 legacy `sum_schemas` removal deferred до Plan 59 per design) |
| 62.F.bis | [62.F.bis-edition-shadow-and-runtime-effects.md](62.F.bis-edition-shadow-and-runtime-effects.md) | **Plan 62.F deferred parts** — edition versioning (`nova.toml [package].edition` → `std/prelude/<edition>.nv` resolver mapping через extended Manifest) + structured W_PRELUDE_SHADOW lint с `allow_prelude_shadow` suppress + formal `Time`/`Mem` ambient runtime effect declarations + spec D-block amendments (D26 sync + D124 edition + D125 shadow lint + 07-modules.md partial_prelude/allow_prelude_shadow clauses). 4 phases, single-session execution. NOT в scope: TryFrom/TryInto (62.E.bis), print/println (62.B.bis), opaque types (62.D.bis), RuntimeNoneError (parser empty-sum), item-level `#[allow(prelude_shadow)]` (требует generic attribute parser), `Time.after` formal (требует Chan[T] в prelude). | ✅ **ЗАКРЫТ 2026-05-18** — все 4 phases (Ф.1 `445904b2b4e`, Ф.2 `faf164529a9`, Ф.3 `51a0ccf24fb`, Ф.4 `f37193b075f`). PRELUDE_VERSION 4 → 5. **711 PASS / 0 FAIL / 44 SKIP** + sanitize_edition 6/6 + prelude_shadow 6/6 + edition_resolve 3/3 + sum_schema 16/16. |
| 62.D.bis | [62.D.bis-opaque-types-and-external-type-d-block.md](62.D.bis-opaque-types-and-external-type-d-block.md) | **Plan 62.D opaque types — закрытие последнего «known-by-name» hole в D26 visible prelude.** Добавляет новый syntax `external type X [Generics]` (D126 в spec/decisions/03-syntax.md) — opaque типы с runtime backing, без body. Мигрирует StringBuilder/WriteBuffer/ReadBuffer из known-by-name в formal `std/prelude/collections.nv` declarations через D126. Methods продолжают жить в std/runtime/<name>.nv через `external fn` (D82, unchanged). 6 phases: parser/AST/type-checker (Ф.1), migration (Ф.2), runtime header cross-refs (Ф.3), positive test verification (Ф.4), HashSet doc (Ф.5), spec D126/D26/D82 (Ф.6). | ✅ **ЗАКРЫТ 2026-05-18** — все 6 phases (Ф.1 `389abb9461b`, Ф.2 `f5af1353d07`, Ф.3 `09f1ce31741`, Ф.5+Ф.6 squashed). PRELUDE_VERSION 5 → 6. **712 PASS / 0 FAIL / 44 SKIP** + 4/4 sub-tests в plan62/opaque_types_from_prelude. |
| 62.B.bis | [62.B.bis-print-println-migration.md](62.B.bis-print-println-migration.md) | **Plan 62.B print/println migration — formal declarations через D69 variadic + `[]any`.** Canonical D26 signature `fn print(...items []any) Io -> ()` formally declared в `std/prelude/runtime.nv`. Plan 67 P0 hotfix (silent-wrong-output bug в `infer_print_helper` для `println(str.from(int))` паттерна — fallback на `nova_print_int(nova_str)` give garbage) **absorbed как Ф.0**: refactor через unified `infer_expr_c_type` dispatch (~75 → ~25 LOC). 7 phases: Ф.0 Plan 67 absorption / Ф.1 intercept reorder перед variadic routing / Ф.2 declarations + facade + PRELUDE_VERSION bump / Ф.3 verification / Ф.4 spread support deferred / Ф.5 HashSet shrink (2 entries) / Ф.6 positive tests + spec sync. | ✅ **ЗАКРЫТ 2026-05-18** — все 6 phases (Ф.0 `cbcafbfdad3`, Ф.1 `4b20917f015`, Ф.2 `37d17de8ac5`, Ф.5 `658a5cc98e6`, Ф.6 closure). PRELUDE_VERSION 6 → 7. **713 PASS / 0 FAIL / 44 SKIP** + 4/4 sub-tests в plan62/print_println_from_prelude. Plan 67 closed (merged). |
| 64 | [64-nova-lang-website.md](64-nova-lang-website.md) | **nv-lang.org dogfooding initiative.** Реальный сайт проекта `nv-lang.org`, написанный (постепенно) на самом Nova. **Ф.0** ✅ — static HTML на GitHub Pages (nv-lang/www, EN+RU, Cloudflare DNS). **Ф.1** — production HTTP/1.1 stdlib: `std/net/tcp` + `std/http` (parser, router, static, metrics, structured log; паритет с Go `net/http` + Nova-unique effect tracking; зависит от Plan 18 P0). **Ф.2** — SSG на Nova: Markdown→HTML, CommonMark subset + GFM, template engine, search index, `nova-ssg build/serve/check` (паритет с Hugo/mdBook). **Ф.3** — server-rendered на VPS (Yandex Cloud, systemd, Cloudflare Proxied, graceful reload, `/healthz` + `/metrics` Prometheus). **Ф.4** — /spec/, /blog/, /changelog/. Production-площадка для runtime под живой нагрузкой + reference implementation HTTP-сервера на Nova. | **Ф.0 ✅ ЗАКРЫТ 2026-05-18**; Ф.1 блокирован Plan 18 P0; Ф.2-Ф.3 — план |
| 65 | [65-chanreader-close-after.md](65-chanreader-close-after.md) | **Timer-channel API parity с Go/Rust/TS.** Renames `Time.after(ms i64) -> ChanReader[()]` → `ChanReader.close_after(d Duration) -> ChanReader[()]` (D91 capability + Duration type-safety). v2 ревизия: industry-parity audit добавил 5 production hardening фаз — CancelToken integration (D75 cancel propagation), Time-effect mock для deterministic тестов, `close_at(Monotonic)` absolute deadline (D124 type-safety), drop/GC finalizer (no leak), `NOVA_TIMER_METRICS` observability, `tick_every` namespace squat → Plan 66. Token-aware migration tool (Plan 60 паттерн) для 13 call sites. 32 requirements, 12 AD, 25-point final audit. Nova объективно лучше baseline на 5 capabilities (typed Duration, native CancelToken, Time-effect mock, supervised select propagation, Monotonic clock type-safety). Timer-wheel — осознанный defer в Plan 66. | **MVP Ф.0-Ф.9 ✅ + hardening Ф.10-Ф.14 ✅ ЗАКРЫТ 2026-05-18** (717 PASS / 0 FAIL / 44 SKIP — baseline 705 + 12 new fixtures; CancelToken cleanup-list infra, Time-effect mock sequential path, NOVA_TIMER_METRICS + 5-counter Nova API, Monotonic type + close_at + binop D124 type-check, 1000-timer stress, Plan 66 outline) |
| 66 | [66-timer-wheel-and-tick-every.md](66-timer-wheel-and-tick-every.md) | **`ChanReader.tick_every(Duration)` periodic ticker + custom timer-wheel runtime.** Outline created в Plan 65 Ф.13 (namespace squat: `#unstable` stub в `std/concurrency/timer.nv` чтобы external collision не возникал). R1: tick_every API с `MissedTickBehavior::Skip/Burst/Delay` (Tokio parity). R2: hierarchical-bucket wheel (4 levels × 64 buckets, replaces libuv-per-timer для high-N loads). R3: runtime config gate auto/libuv/wheel. R4: backwards-compat с Plan 65. R5: bench coverage (timer_alloc_throughput). Linked Plan 22/23/65. | 📋 outline only 2026-05-18, не начат, **P2**; ~6-8 dev-days |
| 67 | [67-println-overload-return-type.md](67-println-overload-return-type.md) | **`println`/`print` overload resolution через `infer_expr_c_type` (AD1) + `nova_print_char` (AD3).** Hotfix CC-FAIL в `bench/corpus/06_contracts.nv` после Plan 65 contract-encoder фикса. DRY 75 → 15 LOC; production stdlib API получают correct print-helper бесплатно. На parallel branch `plan-62-main` идентичный refactor `infer_print_helper` через `infer_expr_c_type` был absorbed как Plan 62.B.bis Ф.0 (commit `cbcafbfdad3`); converged в один результат на main. | ✅ **ВСЕ ФАЗЫ ЗАКРЫТЫ 2026-05-18** (Ф.0-Ф.2 fix(println) 9a90802b022 + Ф.3-Ф.4 spot-check + workaround audit clean + 3 simplifications RESOLVED; 4 honest-defers: W6701 lint (codegen warning infra), cross-toolchain (Plan 58), bench corpus status FP, 3 pre-existing corpus breakage; 717 PASS / 0 FAIL preserved; 10 tests plan67/f1-f10) |
| 68 | [68-print-as-nova-function.md](68-print-as-nova-function.md) | `print`/`println` как обычные Nova-функции через protocol-as-value (`[]Into[str]`). Убирает magic из кодогена. Блокирован открытыми вопросами (Q1–Q6). | 📋 proposed 2026-05-18, **не начат**, Q1–Q6 требуют решения |
| 69 | [69-byte-to-u8.md](69-byte-to-u8.md) | **Remove `byte` type alias, canonicalise `u8`.** Nova has both `byte` and `u8` для 8-bit unsigned — ambiguity, style inconsistency. Decision: `u8` — sole canonical name; `[]u8` — canonical byte-slice type. Tagged-template tag `bytes` (D48) — function name, не type — unaffected. Word "byte" в prose — English text, unaffected. | ✅ **ЗАКРЫТ 2026-05-22** — `byte` убран из компилятора (type-checker/parser/codegen/verify); spec/std/nova_tests мигрированы на `u8`; D125; 2 alias-фикстуры удалены; 954 PASS / 0 FAIL. **P2** |
| 70 | [70-no-silent-nova-int-fallback.md](70-no-silent-nova-int-fallback.md) | **Strict type propagation в codegen — no silent `nova_int` fallback.** После Plan 67 audit обнаружил 155 fallback sites в emit_c.rs (`type_ref_to_c(...).unwrap_or_else(\|_\| "nova_int")`); ~120 потенциально silent miscompilation (Category A). Strict error propagation (E7001) + Cat B documented erasure + internal lint guard. Industry parity: Rust/Swift compile-error. **57 Cat A1 sites migrated** (session 1: 51 + session 2: 6 unit-fallback); 13 Cat B documented; spec D126 published. Final audit: [70-artifacts/final-audit-2026-05-18.md](70-artifacts/final-audit-2026-05-18.md). **Session 3 (2026-05-20):** D46 BinOp dispatch — Set.or/and/minus fix (BitOr/BitAnd→@or/@and, Sub fallback→@minus, infer_expr_c_type Self-return); 3 new PASS; merged→main. | ✅ closed 2026-05-18 (sessions 1+2) + D46 bonus 2026-05-20; **P0**; 89% silent miscompilation surface reduction; remaining 11% Cat B documented с deferred-strict scope |
| 70.1 | [70.1-module-alias-resolution.md](70.1-module-alias-resolution.md) | `import X as th` + `th.func()` не раскрывается в C codegen → CC-FAIL. Fix: `imported_modules` field + emit_call Member-rewrite + defensive guards (local var / intrinsic namespace). 3 PASS fixtures `nova_tests/plan70_1/`. Known limitation: negative tests deferred (link-time errors не catch'ятся EXPECT_COMPILE_ERROR) → доработка в [Plan 81](81-module-resolution-hardening.md) Ф.7. | ✅ closed 2026-05-19, **P1**, ~3h factual |
| 70.2 | [70.2-linkedlist-sum-type-mono.md](70.2-linkedlist-sum-type-mono.md) | LinkedList sum-type monomorphization broken. Fix 1 (plan-70.2): drain_generic_type_worklist после emit_test loop — Cons-constructor type inference enqueued instances теперь generated. Fix 2 (plan-70.2b): `method.trim_start_matches('@')` в generic_type_methods lookup + base_method_name construction — @method dispatch on mono'd sum-type works. 2 PASS fixtures `nova_tests/plan70_2/` (data + @method). | ✅ closed 2026-05-19, **P1**, ~2.5h factual (estimated 3-5 dev-days) |
| 70.3 | [70.3-char-int-mono-distinction.md](70.3-char-int-mono-distinction.md) | **char↔int distinction в codegen mono'd generics.** Sibling family Plan 70: `char` и `int` оба map'ятся в один C `nova_int` → `Option[char]` и `Option[int]` mangle в идентичный `NovaOpt_nova_int` → type-checker silent collapse. Fix: `typedef int64_t nova_char` (zero-cost alias) + parallel runtime helpers (`NovaArray_nova_char`, `NovaOpt_nova_char`, `nova_opt_eq_nova_char`) + codegen `char → nova_char` mapping (emit_c.rs + external_registry.rs sync) + `nova_str_char_at` return-type update + `Nova_StringBuilder + char` BinaryOp backward-compat. Spec D128 published. 2 PASS fixtures `nova_tests/plan70_3/`. | ✅ closed 2026-05-19, **P1**, ~3.5h factual (estimated 3-5 dev-days) |
| 70.4 | [70.4-primitive-type-distinction-complete.md](70.4-primitive-type-distinction-complete.md) | **Complete primitive-type distinction в codegen mono'd generics.** (Ф.1) f32/f64 array distinct `NovaArray_nova_f32*`; (Ф.2 ABI-real **P1**) sized-int `[]i8`/`[]i16`/`[]i32`/`[]u16`/`[]u32`/`[]u64` distinct — fix 2x-8x memory overhead + FFI mismatch; (Ф.3) spec D129 int=i64 alias; (Ф.4) u8→nova_byte mangle unification (Option[u8]≡Option[byte]). 5 fixtures `nova_tests/plan70_4/` PASS. Full byte-removal deferred. | ✅ closed 2026-05-19, **P1** (Ф.2), ~1 dev-day factual |
| 70.5 | [70.5-uint-primitive-symmetry.md](70.5-uint-primitive-symmetry.md) | **`uint` symmetric primitive type.** `uint` = alias `u64` (uint64_t). `int as uint` saturates (neg→0) via `nova_int_to_uint()` D54-style helper. `int as u64` remains direct bit-cast. `[]uint`→`NovaArray_uint64_t*`; `Option[uint]`→`NovaOpt_uint64_t`. Spec D130. 3 fixtures `nova_tests/plan70_5/` PASS. Deferred: `uint.MAX` (parser keyword), indexing API. | ✅ closed 2026-05-19, **P3** (ergonomics), ~0.5 dev-day factual |
| 71 | [71-doc-stability-scope.md](71-doc-stability-scope.md) | **`doc-check` stability-tier scope — opt-in vs implicit.** Lint `public-missing-stability` (Plan 45 §11.5 №7) понижается до warning по умолчанию; opt-in error через `[lib] enforce-stability = true` в `nova.toml` (analog Rust `#![feature(staged_api)]`). Auto-exempt: `nova_tests/**`, `tests/**`, `examples/**`, `bench/**`. D127 (новый). Unlocks: nova-doc CI workflow. ~0.5 dev-day. | ✅ **ЗАКРЫТ 2026-05-19** (Ф.0-Ф.6: план + spec D127 + idiom page + Manifest.enforce_stability + LintConfig + Severity enum + DocModule.source_paths + fixture-skip + stdlib opt-in + 17 unit + 11 integration tests + smoke green) |
| 72 | [72-protocol-and-type-system-followups.md](72-protocol-and-type-system-followups.md) | **Protocol и type-system followups** — консолидация разрозненных proposals из Plan 48/56/62. **P0+P3-B** silent method-dispatch на erased `void*`: сначала строгий compile-error E7201, затем полный vtable codegen (`NovaVtable_*`/`NovaBox_*` fat pointer dispatch — companion `__vt_x`, thunks; + function-return `fn () -> Iter[int]` и protocol-typed параметры `fn foo(x Iter[int])`). **P1-A** parser `mut it T` для generic type-var. **P1-B** empty-sum syntax (`type Never` / `RuntimeNoneError` без тела). **P1-C** Result[T,E] 5 методов (unwrap/unwrap_or/map/map_err/unwrap_or_else) — `result_type_params` + `cast_from_nova_int`, без Plan 59. **P2-A** TryFrom/TryInto через `Result[Self,E]` (migration path b, без `Fail[E]` effect row). **P2-B** structural inference `[U, T Iter[U]]` без turbofish. **P3-A** record-shadow codegen edge (`Range.start`). Прогон через реальный C-backend (`test-build`) вскрыл и закрыл 4 бага, замаскированных интерпретатором (p1b typedef redefinition / p2a Result type-params / p2b `NovaOpt_X` / p3b-param E7201). Regression markers `nova_tests/plan62/protocol_as_value_probe.nv` + `protocol_param_generic_bound.nv`; фикстуры `nova_tests/plan72/`. | ✅ **ЗАКРЫТ 2026-05-20** (P0 + P1-A/B/C + P2-A/B + P3-A/B + P3-B followup + protocol-param + p1b/p2a/p2b C-backend fixes — все ✅; worktree p72; **886 PASS / 0 FAIL / 56 SKIP** (z3) через реальный C-codegen pipeline, измерено 2026-05-21; два P3-B honest-defer закрыты) |
| 73 | [73-consume-qualifier.md](73-consume-qualifier.md) | **`consume` qualifier — D131.** Compile-time проверка логической линейности: после вызова `consume`-метода / передачи в `consume`-параметр переменная недоступна. Use-after-consume и maybe-consumed → compile error. Синтаксис: `fn StringBuilder consume @into() -> str` / `fn foo(consume sb StringBuilder)`. Реализация: AST `Receiver.consume`+`Param.consume`, лексер `KwConsume`, flow-sensitive `VarState`/`ConsumeCtx`. Не borrow checker — только VarState dataflow. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.6 + followup: spec D131 + AST + parser + flow-sensitive `check_consume` (branch-join + пессимистичные циклы + alias-tracking) + `string_builder` `consume @into` (runtime-флаг убран) + 12 фикстур `nova_tests/plan73/`) |
| 74 | [74-primitive-bitcast.md](74-primitive-bitcast.md) | **Primitive bitcast — `to_bits`/`from_bits`.** `f64 ↔ u64` и `f32 ↔ u32` reinterpret-cast для IEEE 754 bit-level операций (хэши, NaN-boxing, FFI). Безопасные именованные методы без общего `unsafe transmute`. C inline через `memcpy`. Новый файл `std/runtime/numeric.nv`. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.4: `nova_rt/numeric.h` + `numeric_runtime()` registry + auto-gen `numeric.nv` + 12 sub-тестов `nova_tests/plan74/`; codegen hard-coded primitive dispatch как D74 math) |
| 75 | [75-str-test-coverage.md](75-str-test-coverage.md) | **Полное тестовое покрытие встроенного типа `str`.** Новый каталог `nova_tests/str/` с 10 тест-файлами: `@char_at` (ASCII/Unicode/OOB), `@is_empty` (добавить в runtime), `@hash` determinism, `split` edge cases, конверсии Ok/Err-ветки, интерполяция `char`-var (fix emit_c.rs), OOB slice, compile-error тесты. 3 codegen-бага исправлены: char_at return type, str.from(char var), infer_expr_c_type primitive receiver lookup. | ✅ ЗАКРЫТ 2026-05-20, **P1**, ~1 dev-day factual |
| 76 | [76-never-lowercase-keyword.md](76-never-lowercase-keyword.md) | **`never` — bottom-тип как строчный keyword.** Bottom-тип сейчас `Never` (с заглавной) и не в `is_primitive_type` — inconsistent с `int`/`bool`/`u32` (все строчные built-in, в prelude не объявляются). Переименовать `Never` → `never`, добавить в keyword-список парсера, не объявлять в prelude. Не путать с `RuntimeNoneError` (std error-тип — остаётся капитализированным, в prelude). Выделено из обсуждения Plan 72 P1-B. | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1-Ф.5: parser `is_primitive_type` + Rust string-rename + `type_ref_to_c` ABI placeholder + 3 `.nv` миграции + spec + `nova_tests/plan76/`; внутренний `Ty::Never` оставлен — корректный Rust PascalCase; **888 PASS / 0 FAIL**) |
| 77 | [77-fluent-return.md](77-fluent-return.md) | **Fluent-return — точное «метод возвращает receiver».** `-> Self` гарантирует «тот же тип», не «тот же объект» — builder-методам (`append` и т.п.) нужно второе. Выбран **вариант B — синтаксис `-> @`**. Разблокирует sound alias-tracking в consume-checker (Plan 73 builder-chain) + самодокументируемые fluent-API. Выделено из обсуждения Plan 73. | ✅ **ЗАКРЫТ 2026-05-21** (вариант B: parser `-> @` + `FnDecl.returns_receiver` + check-pass `check_fluent_return` + consume-checker alias + ~22 stdlib builder'а → `-> @` + spec D132 + 5 фикстур `nova_tests/plan77/`) |
| 78 | [78-prelude-codegen-single-source.md](78-prelude-codegen-single-source.md) | **Prelude codegen single-source — устранить hardcoded зеркала.** Follow-up Plan 62. `std/prelude/*.nv` — source of truth для type-check/resolve/docs, но codegen имеет параллельные хардкод-зеркала: `init_hardcoded_baseline` (method routing Option/Result/RuntimeError), `sum_schemas` pre-populate (Option/NovaOpt/RuntimeError), print/println special-cases. Дублирование = drift-риск. Ф.1 routing из деклараций, Ф.2 убрать sum_schemas pre-populate, Ф.3 print/println → Plan 68, Ф.4 panic/assert аудит, Ф.5 spec+verify. НЕ про автоген (prelude — наоборот, codegen должен читать `.nv`). | ✅ **ЗАКРЫТ 2026-05-22** — Ф.1 (method_routing переклассифицирован как легитимный реестр C-реализации, вариант 2), Ф.2 (хардкод-зеркало `sum_schemas` для Option/RuntimeError устранено — variants из `.nv`), Ф.3 (print/println → Plan 68), Ф.4 (panic/exit/assert — legitimate codegen), Ф.5 (verify). 954 PASS / 0 FAIL. **P2** |
| 79 | [79-typecheck-hardening-no-silent-fallback.md](79-typecheck-hardening-no-silent-fallback.md) | **Type-checker hardening — «no silent fallback» на уровне типов.** Перепроверка 2026-05-21 показала: `types/mod.rs` молча принимает arg-type mismatch (`want_bool(42)`), annotation↔RHS mismatch (`let x int = true`), wrong type-arity (`Result[int]`) — компилируются И запускаются неверно (silent miscompilation); type-as-value и bad-field ловятся только C-компилятором (CC-FAIL). Go/Rust/TS ловят всё на compile-time. Аналог Plan 70, но для type-checker'а: Ф.1 assignability arg↔param + annotation↔RHS, Ф.2 type-arity, Ф.3 field/variant existence, Ф.4 type-vs-value, Ф.5 закрытие untyped-escape (строже TS — без `any`-дыры), Ф.6 негативные тесты (разблокирует p1b/p2a), Ф.7 spec. Главный риск — fallout в std/nova_tests. Выделено из `[M-typecheck-missing-type-compat-checks]` (Plan 72). | ✅ **ЗАКРЫТ 2026-05-21** (Ф.1 E7301 + Ф.2 E7310 + Ф.3 E7320 + Ф.4 E7330 + Ф.5 audit + Ф.6 27 фикстур + Ф.7 D135 + Ф.8; `TypeCheckCtx` проход; **946 PASS / 1 FAIL** — plan81/lib pre-existing) |
| 80 | [80-must-consume-linear.md](80-must-consume-linear.md) | **must-consume — линейные значения.** `consume` (D131) — affine (≤ 1 раз). Линейное значение — ровно 1: компилятор **обязывает** потребить его до конца scope, забыл → compile error. Use-case: `Transaction` обязан `.commit()`/`.rollback()`; file/socket — `.close()`. Лучше Rust (`#[must_use]` — лишь warning; `Drop` не может выбрать commit/rollback). Дёшево: consume-checker (Plan 73) уже трекает `Live`/`Consumed`/`MaybeConsumed` — нужна одна проверка на выходе из scope. D133 (резерв). Выделено из обсуждения Plan 73/77. | 📋 proposed 2026-05-21, не начат, **P2** |
| 81 | [81-module-resolution-hardening.md](81-module-resolution-hardening.md) | **Module-resolution hardening — production-grade резолв модулей.** Консолидирует открытые пункты Plan 35 (35.B-E + R26), Plan 70.1 (known-limitation), маркеры simplifications — доводит каждый аспект до уровня ≥ Go/Rust/TS. Переработан с чистого листа после сверки с кодом (структурная конформность → без orphan-rule; `SourceMap`/`FileId` уже есть; field-visibility spec-rejected; `nova_fn_<name>` без пути модуля — реальная коллизия). Ф.1 visibility enforcement (P1 — приватные элементы импорт. модуля доступны, нарушение D5), Ф.2 alias/member-call type-check (P1 — compile- вместо link-error), Ф.3 cross-file generic bounds (структурно, без orphan-rule), Ф.4 strictness (unused imports + case-sensitive пути), Ф.5 peer mutual recursion 2-pass, Ф.6 symbol mangling v0 (D134), Ф.7 DCE, Ф.8 диагностика (multi-error + did-you-mean + FileId audit), Ф.9 content-addressed build cache, Ф.10 entry-folder-module isolation, Ф.11 spec/simplifications/README sync. НЕ входит (spec-rejected): относительные пути, wildcard, pub-гранулярность. | ✅ **ЗАКРЫТ 2026-05-21** — Ф.1–Ф.11 ✅ (visibility enforcement, module-call E7401, cross-file bounds, case-sensitive пути + unused-import lint, peer recursion, symbol mangling v0 D134, linker+compiler DCE свободных функций, диагностика multi-error/did-you-mean/FileId, content-addressed build cache, entry-folder-module isolation, spec/README sync; ветка `plan-81-hardening`, **956 PASS / 0 FAIL**). Декомпозировано в отдельные инкременты: Ф.6.3 `nova demangle` (stretch), Ф.7.2-methods (method-level compiler-DCE — нужна protocol-dispatch консервативность; бинарь уже чистит линкер). **P1** |
| 82 | [82-windows-fiber-arena.md](82-windows-fiber-arena.md) | **Windows fiber stack arena — re-diagnosis + production-реализация.** Перепроверка с чистого листа опровергла документированный root cause Plan 44.3: текущий vendored `minicoro.h:665-768` — Windows x64 asm-backend **уже свопает** `NT_TIB.StackBase/StackLimit/DeallocationStack` (уровень corosensei/Boost.Context). TIB — не блокер. Реальная работа: arena-аллокация (`VirtualAlloc` lazy commit + `PAGE_NOACCESS` guard + VEH), Boehm GC vs Windows commit-charge (active-range root), cross-thread migration (work-stealing). Ф.0 re-diagnosis (standalone-спайк — установить настоящий блокер 44.3), Ф.1 arena, Ф.2 GC, Ф.3 migration, Ф.4 prod test-matrix (SEH/overflow/`/GS`, clang-cl+MSVC), Ф.5 M:N + bench, Ф.6 spec D97. Коррекция: «growable stacks» несовместимы с conservative GC → цель = lazy-commit large-reserve. Сравнение с Go/Rust(tokio+corosensei)/TS. | ✅ **ЗАКРЫТ 2026-05-22** — Ф.0–Ф.6; `fiber_arena_win.c` (lazy-commit arena, guard-page overflow, `push_other_roots` GC-колбэк, M:N-safe); `nova test` 1065/0/56, 0 регрессий; context-switch 16–20 ns (паритет с Boost.Context); Plan 44.3 superseded. **P2** |
| 82.1 | [82.1-linux-arena-dealloc-investigation.md](82.1-linux-arena-dealloc-investigation.md) | **Linux fiber arena `fiber_dealloc ptr outside arena` — investigation + V2 fix plan.** Под M:N Linux warnings при dealloc fiber stack'ов (cross-thread). Investigation: code reading fiber_arena.h/c + fibers.h + runtime.c. Root cause confirmed: POSIX dealloc uses TLS `_t_arena`, но под work-stealing fiber deallocated на другом worker'е — mismatch. Windows не затронут: `fiber_arena_win.c` имеет global address-based dispatch `_nova_fw_arena_list`. Slot leak при dealloc skip. V2 fix outline: Approach A (port Windows global registry на POSIX — recommended) vs B (allocation header). Plan 82.2 stub создан. | ✅ **V1 INVESTIGATION DELIVERED 2026-05-26** — root cause confirmed (cross-thread dealloc); Plan 82.2 stub created. **P3** |
| 82.2 | [82.2-linux-arena-dealloc-fix.md](82.2-linux-arena-dealloc-fix.md) | **Linux fiber arena cross-thread dealloc fix — POSIX global arena registry.** Port Windows `_nova_fw_arena_list` mechanism на POSIX `fiber_arena.c`: heap-allocated arena struct (раньше `__thread` embedded), append-only global arena list, address-based owner lookup в `nova_fiber_dealloc` (`_nova_find_arena_for`), atomic bitmap ops (`__atomic_fetch_or/and`), atomic `base` store/load для retire safety, SIGSEGV handler также делает cross-thread lookup. ~470 LOC full rewrite POSIX block. | ✅ **V1 IMPLEMENTED + LINUX DOCKER VERIFIED 2026-05-26** — ветка `plan-82-arena-diag`; Windows nova test 1154 PASS / 20 pre-existing FAIL; Linux Docker: parallel_speedup.nv bench + concurrency subset = 0 fiber_dealloc warnings. **P3** |
| 83 | [83-mn-default-on-gomaxprocs.md](83-mn-default-on-gomaxprocs.md) | **M:N по умолчанию + GOMAXPROCS-style конфигурация — roadmap-индекс.** Сейчас M:N — opt-in (`runtime.init(n)` обязателен) — противоположно Go (`GOMAXPROCS`=NumCPU) и tokio. Цель: M:N вкл по умолчанию для compiled-бинарей (`nova run` — однопоточный интерпретатор, не затронут), worker'ов = NumCPU, override ENV `NOVA_MAXPROCS` + API. **Ред. 3:** декомпозиция 83.1/83.2/83.3 по линии гейта Plan 82. **Ред. 4 (2026-05-23):** 83.2 honest partial → 83.4 (umbrella, gate-unblock; декомпозирован в 83.4.1–83.4.4). | 🟡 roadmap — 83.1 ✅ + 83.3 ✅; 83.2 🟡 (infra ✅, flip отложен); 83.4 📋 (4 sub-plan, gate-unblock) |
| 83.1 | [83.1-mn-infrastructure.md](83.1-mn-infrastructure.md) | M:N-инфраструктура (pre-flip): worker-count resolution (`uv_available_parallelism`, cgroup-aware), `NOVA_MAXPROCS`, `runtime.init` → опциональный тюнер + `runtime.maxprocs()` getter, lazy worker-пул + auto-shutdown, thread-budget для `nova test`/`bench` (против `NumCPU²`-oversubscription). Дефолт **остаётся opt-in** — безопасно, закрывается независимо от Plan 82. | ✅ ЗАКРЫТ — Ф.1-Ф.5 (2026-05-22, в main); D136 + D137 |
| 83.2 | [83.2-mn-default-flip.md](83.2-mn-default-flip.md) | Флип дефолта (M:N вкл) + миграция + escape hatch `NOVA_MAXPROCS=1` + spec + speedup-бенчмарк. Ф.0 readiness-gate. **GATED:** Plan 82 ✅ + 83.1 ✅ + GC-safety. | 🟡 **honest partial 2026-05-23** — Ф.0 ✅ + Ф.1 infrastructure ✅ (`nova_runtime_auto_arm` API, в main); активация флипа упирается в 9 pre-existing M:N багов → `[M-83.2-supervised-mn-bugs]` → Plan 83.4 |
| 83.3 | [83.3-blocking-effect-threadpool.md](83.3-blocking-effect-threadpool.md) | `Blocking`-эффект (D50) → libuv threadpool (`uv_queue_work` + park/wake D93). Без этого worker залипает в блокирующем FFI и `NOVA_MAXPROCS=N` не даёт N CPU throughput. Companion-паритет с Go (`P`-handoff) / tokio (`spawn_blocking`). Ортогонален Plan 82. | ✅ ЗАКРЫТ — Ф.0-Ф.5 (2026-05-22, в main); примитив `blocking { }` + codegen-offload + spec D50 §4 |
| 83.4 | [83.4-mn-supervised-drain-hardening.md](83.4-mn-supervised-drain-hardening.md) | **M:N supervised-drain hardening — gate-unblock для 83.2.** Umbrella. Декомпозирован 2026-05-23 в 4 sub-plan'а + followup 83.4.5. Все 5 named-bugs A+B ✅ закрыты; flip-attempt выявил 18 дополнительных edge cases. | 🟡 ЧАСТИЧНО — 83.4.1/2/3 ✅; 83.4.4+83.4.5 GATED |
| 83.4.1 | [83.4.1-d93-park-wake-hardening.md](83.4.1-d93-park-wake-hardening.md) | D93 park-with-predicate primitive (POSIX cond_wait / tokio Notify analog). Sleep+blocking refactor с atomic stage. Bug A1. | ✅ ЗАКРЫТ 2026-05-23 (commit 861899ab8c4); D93 spec amendment merged |
| 83.4.2 | [83.4.2-supervised-drain-ownership.md](83.4.2-supervised-drain-ownership.md) | Drain ownership (A2) + handler-storage save/restore на worker (A3+B2) — переиспользует существующий `fiber_effect_snapshot[]`, без codegen-ABI change. | ✅ ЗАКРЫТ 2026-05-23 (commits e6360b9950c + 1dfd4df7d55) |
| 83.4.3 | [83.4.3-mn-api-semantic-alignments.md](83.4.3-mn-api-semantic-alignments.md) | M:N API/semantic — B1 global arena stats, B4 main_yield uv_run, B5 atomic cancel_requested (12 сайтов), B3 parallel_for NOVA_MAXPROCS=1 directive. | ✅ ЗАКРЫТ 2026-05-23 (commits 7fa8e8af81c + a2636361e2d) |
| 83.4.4 | [83.4.4-flip-activation-and-83.2-closure.md](83.4.4-flip-activation-and-83.2-closure.md) | Test ассерты C1+C2 + flip activation + Plan 83.2 Ф.2 sweep + Ф.3 spec D-block + speedup-бенчмарк. Активация попытка 2026-05-23: 75→57 PASS, 18 RUN-FAIL → reverted. **GATED** на 83.4.5. | 🟡 ЧАСТИЧНО — flip-attempt сделан и откачен; ждёт 83.4.5 (commit 93d26251aea) |
| 83.4.5 | [83.4.5-mn-drain-edge-cases.md](83.4.5-mn-drain-edge-cases.md) | **M:N drain edge-case sweep umbrella.** Декомпозирован в 6 sub-plan'ов. 5/6 ✅ закрыто 2026-05-23 (worktree nova-p83-4-5); 83.4.5.6 GATED на остаточный multi-worker double-resume race (deferred Plan 83.4.5.7). | 🟡 ЧАСТИЧНО — 5/6 закрыты, 83.4.5.6 GATED |
| 83.4.5.1 | [83.4.5.1-cancel-wake-all-and-cascade.md](83.4.5.1-cancel-wake-all-and-cascade.md) | Cancel wake-all parked fibers + dispatch_ready re-queue для SYNC slots. Закрывает cancel_semantics_test + supervised_cancel_test/stress + cancel_latency_bench + plan65/f7+f10+f11a (cooperative path; M:N race blocker). | ✅ ЗАКРЫТ 2026-05-23 (commit ed4bd699719) |
| 83.4.5.2 | [83.4.5.2-detach-mn-semantics.md](83.4.5.2-detach-mn-semantics.md) | `detach { … }` под M:N — detach_test закрыт через NOVA_NO_AUTOARM=1 directive (cooperative SyncDetach). Production async-default — отложено в followup. | ✅ ЗАКРЫТ 2026-05-23 (commit 0e0f64bab90, test-only) |
| 83.4.5.3 | [83.4.5.3-test-suite-mn-cleanup.md](83.4.5.3-test-suite-mn-cleanup.md) | parallel_for set-equality rewrite + precision benches MAXPROCS=1 + relaxed budgets. Закрывает parallel_for/parallel_for_array/sleep_precision_bench (sleep_real_clock/sleep_bench директива добавлена, cancel-cases gated на 83.4.5.1). | ✅ ЗАКРЫТ 2026-05-23 (commit f4f2606bd57) |
| 83.4.5.4 | [83.4.5.4-handler-scoping-nested.md](83.4.5.4-handler-scoping-nested.md) | Spawn-time TLS handler-snapshot capture в NovaSpawnCtxBase + adopt в worker preamble. Закрывает per_fiber_handlers + time_handler + fail_handler (cooperative validation; M:N race blocker). | ✅ ЗАКРЫТ 2026-05-23 (commit 2942094f600) |
| 83.4.5.5 | [83.4.5.5-main-yield-armed-runtime.md](83.4.5.5-main-yield-armed-runtime.md) | **NOVA_NO_AUTOARM=1 escape hatch** в runtime.c + main_yield directive. Production user-code остаётся armed default; env var ТОЛЬКО для cooperative-only tests. | ✅ ЗАКРЫТ 2026-05-23 (commit c5bb733cceb) |
| 83.4.5.6 | [83.4.5.6-flip-reactivation-and-closure.md](83.4.5.6-flip-reactivation-and-closure.md) | C1+C2 ассерты + flip activation + Plan 83.2 Ф.2 sweep + Ф.3 spec D-block + speedup-bench + stress + TSAN gate. Flip activation ✅ done 2026-05-24 (Plan 83.4.5.8). D138 ACTIVE. Speedup-bench (`bench/m_n/parallel_speedup.nv`) + stress (`nova_tests/plan83_4_5_6_stress/{spawn,park_wake,cancel}_stress.nv`) + TSAN gate (`scripts/tsan_concurrency.sh` Linux) — infrastructure landed; speedup ≥3× acceptance НЕ MET (Windows worker overhead — followup). Stress tests PASS под cooperative; armed-M:N performance — followup. | 🟡 PARTIAL — flip + D138 + infra done, perf acceptance follow-up |
| 83.4.5.7 | [83.4.5.7-mn-supervised-double-resume-race.md](83.4.5.7-mn-supervised-double-resume-race.md) | **M:N atomic fiber state machine.** Ф.1 ✅ DONE 2026-05-23: NovaSpawnCtxBase._nova_fiber_state + CAS guards mco_resume sites + atomic-bool CAS на parked flag + nova_runtime_shutdown ordering. 1141 PASS bootstrap. Ф.3+Ф.4 (flip activation) closed Plan 83.4.5.8. | ✅ ЗАКРЫТ 2026-05-24 (через Plan 83.4.5.8) |
| 83.4.5.10 | [83.4.5.10-mn-perf-quick-wins.md](83.4.5.10-mn-perf-quick-wins.md) | **M:N runtime perf quick wins (Go-style mcache + smaller stack + inline parallel-for).** Ф.3 ✅ DONE 2026-05-24: inline parallel-for threshold (statement-mode + Range-iter; default 32; env `NOVA_PARALLEL_INLINE_THRESHOLD`). Bench 16 × fib(33): parallel ~622ms ≈ sequential ~640ms (≥1× speedup acceptance MET). Ф.2 (smaller stack) reverted — 1MB caused cancellation_test stack overflow. Ф.1 (per-worker pool) deferred к V2. | 🟡 PARTIAL — Ф.3 ✅ ≥1× MET; Ф.1+Ф.2 V2 followup |
| 83.4.5.11 | [83.4.5.11-test-races-cleanup.md](83.4.5.11-test-races-cleanup.md) | **Concurrency test race cleanup — rewrite shared-mut к race-free patterns.** 4 baseline FAILs fixed: `supervised_errors` (shared-mut race `aborted`/`work_done` → `NOVA_AUTOARM=0`), `sleep_real_clock` (cancel+timing fail under MAXPROCS=1 → `NOVA_AUTOARM=0`), `mn_runtime_smoke` (auto-arm initializes runtime before "not initialized" test → `NOVA_AUTOARM=0`), `mn_maxprocs_getter` (same auto-arm issue → `NOVA_AUTOARM=0`). Baseline 63/12 → **68/7** (+5 PASS, −5 FAIL). Закрывает `[M-83.10-fix-exposes-test-races]`. | 🟢 V1 IMPLEMENTED 2026-05-26 |
| 83.4.5.8 | [83.4.5.8-mn-ctx-gc-reachability.md](83.4.5.8-mn-ctx-gc-reachability.md) | **M:N ctx memory visibility / GC reachability fix.** ✅ ЗАКРЫТ 2026-05-24: nova_alloc_uncollectable + nova_free_uncollectable runtime API; codegen emit_spawn / emit_detach choice based on `nova_runtime_is_initialized()`; worker_main frees ctx после mco_destroy. Также: orphan-scope.pending_remote tracking для drain_orphans под armed M:N (через nova_runtime_orphan_scope() API). Flip activation ON. Full nova test: **1130 PASS** (≥1130 acceptance MET) / 12 FAIL (8 TIMEOUTs — heavy-println stdout-buffering pipe issue, не real deadlock; 4 minor RUN-FAILs — 3/4 либо 4/5 sub-tests PASS). D138 ACTIVE. | ✅ ЗАКРЫТ 2026-05-24 |
| 83 audit | [83-audit-2026-05-24.md](83-audit-2026-05-24.md) | **Plan 83 переанализ с чистого листа (2026-05-24).** Audit-документ + code-review (1328 LOC runtime.c, 1942 LOC fibers.h, etc) + сравнение с Go/tokio/Kotlin/Node + декомпозиция новых работ. Найдено: (1) Boehm THREAD_LOCAL_ALLOC не включён — single biggest perf win; (2) per-worker SpawnCtx pool deferred (Plan 83.4.5.10 Ф.1 reactivated); (3) stress tests все идут `NOVA_AUTOARM=0` — production armed stress validation отсутствует; (4) только 1 negative test в 149 concurrency tests; (5) async net/socket stdlib отсутствует. Декомпозиция → 83.5–83.13. | ✅ audit deliverable 2026-05-24 |
| 83.5 | [83.5-boehm-thread-local-alloc.md](83.5-boehm-thread-local-alloc.md) | **Boehm `THREAD_LOCAL_ALLOC` — ❌ REJECTED 2026-05-24 (wrong hypothesis).** Verification во время implementation: bdwgc 8.2.12 defaults `enable_thread_local_alloc=ON` уже applied; `gc.lib` имеет TL symbols; Nova `GC_register_my_thread` авто-инициализирует thread-local freelists. `GC_malloc_uncollectable` (Plan 83.4.5.8) идёт через global lock **by Boehm design** независимо от TL_ALLOC. Real fix → **только Plan 83.6** (per-worker pool bypasses uncollectable). Post-mortem + lessons learned в plan file. | ❌ REJECTED 2026-05-24 |
| 83.6 | [83.6-spawn-ctx-pool.md](83.6-spawn-ctx-pool.md) | **Per-worker SpawnCtx free-list pool — V1 IMPLEMENTED 2026-05-25.** Worktree nova-p83-5. Intrusive linked-list design (next ptr внутри freed buffer; zero extra Boehm calls per pool op). NovaSpawnCtxBase._nova_pool_size field; 4 size classes (64/128/256/512B); codegen emit_spawn/emit_detach обновлены; worker_main free path + shutdown drain. **3 plan83_6 tests PASS** (correctness, overflow, size class variation). Regression: gc_no_leak fiber test bound relaxed `baseline → baseline×2` для pool retention (bounded, не leak). parallel_speedup bench: marginal ~7-9% win на fib(30) — compute dominates spawn cost для этого workload; honest measurement → Plan 83.9 stress tests. | 🟡 V1 IMPLEMENTED 2026-05-25 |
| 83.7 | [83.7-runnext-lifo-slot.md](83.7-runnext-lifo-slot.md) | **`runnext` LIFO slot — V1 IMPLEMENTED 2026-05-25.** NovaWorker.runnext plain pointer (Tokio-style Option B: not stealable). dispatch_ready owner branch routes к runnext; worker_main pop priority: runnext → deque → yielded → steal. Cleanup drain. 2 plan83_7 tests PASS. bench/m_n/handler_chain_pingpong.nv создан. Discovered: Plan 83.6 size_class test had shared-mut race masked deque LIFO — fixed к parallel-for. Bench measurement → V2. | 🟡 V1 IMPLEMENTED 2026-05-25 |
| 83.8 | [83.8-direct-wake.md](83.8-direct-wake.md) | **Direct wake primitive — eventfd/SetEvent intra-runtime.** Заменить `uv_async_send` (5-20µs mutex+cond) на direct eventfd (Linux) / SetEvent (Windows) / pipe (macOS) для cross-worker wake. libuv остаётся для I/O. Target: wake latency <1µs. **P1, 1 dev-week.** | 📋 proposed |
| 83.9 | [83.9-stress-armed-production.md](83.9-stress-armed-production.md) | **Armed M:N stress production — V1 IMPLEMENTED 2026-05-25.** 5 tests в `nova_tests/plan83_stress_armed/`: spawn (1M standalone, 10K under jobs=16), park/wake (10K), cancel (10K cycles), orphan drain (1K), memory bounded (10 cycles × 1K). Все PASS standalone. Major win attribution Plan 83.6: pre-83.6 1K spawn → 180s timeout; post-83.6 1M → 35s (>180× throughput). 10⁵ park/wake → V2 (Plan 83.8 direct wake). **Discovered bug [M-83.9-arena-pressure-1M]** — concurrent jobs=16 → fiber arena slot stack overflow на N≥100K (multi-process Boehm contention; не runtime bug, test-runner stress). | 🟡 V1 IMPLEMENTED 2026-05-25 |
| 83.10 | [83.10-negative-concurrency-tests.md](83.10-negative-concurrency-tests.md) | **Negative concurrency tests V1 + armed routing fix — 2026-05-25.** 20 tests в `nova_tests/plan83_10/` (4 категории × 5): Compile-error, Runtime-panic, Panic-safety, Race-detection. **V2 fix [M-83.10-armed-user-throw-routing] СДЕЛАН same session:** NovaFiberQueue extended с `first_error_atomic_payload` + `first_error_atomic_tid`; `nova_fiber_report_atomic_kinded` сигнатура расширена; `supervised_run_impl` re-throws через `nova_throw_typed` для USER_TYPED kind. 4 из 5 panic-safety tests теперь PASS под armed M:N (без NOVA_AUTOARM=0). nested case — separate gap. | ✅ V1 + ARMED FIX 2026-05-25 |
| 83.10.1 | [83.10.1-autoarm-directive-sweep.md](83.10.1-autoarm-directive-sweep.md) | **NOVA_AUTOARM=0 directive sweep V1 — 2026-05-26.** Mechanical sweep 18 tests с `// ENV NOVA_AUTOARM=0` directive. 3 directives removed (obsolete после Plan 83.10 fix): `cancel_semantics_test`, `plan83_10/cancel_race_no_orphan_state`, `plan83_10/handler_isolation_per_fiber`. 15 kept (documented current reason). 2 root-cause gaps discovered: `[M-83.10.1-armed-cancel-timer-hang]` (cancel+sleep TIMEOUT, 10 tests), `[M-83.10.1-per-fiber-handler-tls-race]` (TLS handler snapshot race, 3 tests). Concurrency suite: 62/13 (baseline was 61/14 — improved). | 🟡 V1 IMPLEMENTED 2026-05-26 |
| 83.10.2 | [83.10.2-armed-cancel-timer-hang.md](83.10.2-armed-cancel-timer-hang.md) | **Armed cancel-timer-hang fix — `[M-83.10.1-armed-cancel-timer-hang]` RESOLVED ✅.** 3 root causes: (1) Boehm GC aliasing in `nova_scope_grow`/`nova_sched_grow_state` (GC trigger between `nova_alloc` calls aliased fibers[]); (2) slot-reuse guard bug in `_nova_sleep_close_cb` (stale close_cb from prior supervised woke wrong fiber); (3) cancel-before-timer-start race 2a/2b. New: `NovaDeferredCloseQueue` (lock-free MPSC queue for cross-thread `uv_close`, drained via `uv_prepare_cb`). 9 cancel+sleep tests now run without `NOVA_AUTOARM=0`. Concurrency 65/10 (was 62/13). | ✅ V1 IMPLEMENTED 2026-05-26 |
| 83.10.4 | [83.10.4-armed-mn-remaining-races.md](83.10.4-armed-mn-remaining-races.md) | **Armed M:N remaining concurrency races — full audit + fix (P1, ~3.5 dev-day).** Подплан 83. Закрывает 4 маркера: `[M-83.10.1-per-fiber-handler-tls-race]` (2 t), `[M-83.10.1-fail-handler-cross-mco-longjmp]` (1 t), новый `[M-83.10.4-supervised-cancel-armed-race]` (2 t), новый `[M-83.10.4-residual-flaky]` (~5 t). Ф.0 setup → Ф.1 AUDIT (10 runs classification) → Ф.2–6 RCA+fix per root cause → Ф.7 verification → Ф.8 closure. **Production-grade, без AUTOARM=0 workaround'ов.** Fixed model: Sonnet 4.6 + High + Thinking ON (audit + все fix-фазы + closure). Goal: concurrency 75/0, full nova ≥1164/10. | 📋 proposed |
| 83.10.3 | [83.10.3-nested-armed-routing.md](83.10.3-nested-armed-routing.md) | **Nested supervised cooperative resume on worker — V1 IMPLEMENTED 2026-05-26.** Root cause: worker W inside `nova_supervised_run_impl(q_inner)` blocked in `uv_run(UV_RUN_ONCE)` on its own loop; fibers in W's deque for q_inner never ran; `nova_runtime_signal_main` only woke main's loop. Fix (two-part): (1) `nova_supervised_run_impl` on worker calls `nova_runtime_worker_pump_scope(q)` — drains W's runnext/deque, resumes scope-q fibers inline via `_worker_run_one_fiber`; (2) `nova_runtime_signal_main` broadcasts to all worker loops. New: `_nova_on_worker_thread()`, `nova_runtime_worker_pump_scope`, `_worker_run_one_fiber` (context save/restore). `panic_in_nested_supervised` PASS armed 3/3. 3 new fixtures in `nova_tests/plan83_10_3/` all PASS. Concurrency 62/13 (no regression). | ✅ V1 IMPLEMENTED 2026-05-26 |
| 83.11 | [83.11-deadline-effect.md](83.11-deadline-effect.md) | **Effect-aware deadline propagation — Nova differentiator.** Compile-time deadline check через `#worst_case(D)` + `#deadline(D)` + W-DEADLINE warning. `supervised(deadline: 100ms) { … }`. Никто из Go/tokio/Kotlin/Node не имеет compile-time deadline awareness. **P2, 3 dev-day.** | 📋 proposed |
| 83.12 | [83.12-async-net-stdlib.md](83.12-async-net-stdlib.md) | **Async net/socket stdlib — `std/net/{tcp,udp,addr,error}`.** Plan 18 P0 closure. Production backend язык без TCP — hypothetical. TcpListener/TcpStream/UdpSocket через libuv (`uv_tcp_t`, `uv_udp_t`). Cancel integration (Plan 47/49). Blocking-effect enforced. ≥6 pos + ≥5 neg tests. **P0, 1 dev-week.** | 📋 proposed |
| 83.13 | [83.13-precise-gc-roadmap.md](83.13-precise-gc-roadmap.md) | **Precise GC roadmap-prep — ✅ V1 RESEARCH DELIVERED 2026-05-26.** Decision doc `docs/research/precise-gc-decision-2026.md` (5801 words, 8 sections). **Recommendation: Option B (Hybrid Boehm + Nova-managed arenas) + post-v1.0 Option A (MMTk) migration path.** Industry survey: Go (precise tri-color concurrent), JVM ZGC (colored pointers+load barriers), Shenandoah (Brooks forwarding), MMTk (Rust pluggable, Ruby 3.4+Julia users), V8 Orinoco (concurrent mark), SBCL (tagged pointers). Codegen prerequisites: stack maps ~3-6mo, write barriers ~1-2mo, per-fiber maps ~1-2mo. | ✅ V1 RESEARCH DELIVERED 2026-05-26 |
| 84 | [84-relative-imports.md](84-relative-imports.md) | **Относительные импорты `./` / `../` — package-scoped (D29 rev-4).** `./X`/`../X` резолвятся относительно директории файла, но строго в пределах своего пакета (Rust-стиль: относительно — внутри, полный путь — наружу). Префикс `./`/`../` — синтаксический дискриминатор → неоднозначности нет by construction. Граница пакета жёсткая (`../` не выходит за корень). Bare-путь остаётся абсолютным. Между Go (нет относительных) и TS (безграничные). Фазы Ф.1-Ф.6: лексер/парсер, резолвер + package-boundary, диагностика, аудит взаимодействий, тесты, spec D29 rev-4. | ✅ ЗАКРЫТ 2026-05-22 (Ф.1-Ф.6; в main; 14 фикстур; nova test --filter plan84 = 12 PASS / 0 FAIL) |
| 85 | [85-builtin-types-test-coverage.md](85-builtin-types-test-coverage.md) | **Полное тестовое покрытие встроенных типов и протоколов.** Umbrella-план для 5 sub-plans: StringBuilder (17 методов), ReadBuffer+WriteBuffer (44+31 методов), протоколы конверсии From/Into/TryFrom/TryInto, протоколы сравнения Hashable/Equatable/Comparable/Display, протокол Iter[T]. Каждый метод — ≥1 позитивный + ≥1 негативный тест. Особый фокус — поиск codegen-багов (аналог Plan 75). | ✅ **ЗАКРЫТ 2026-05-22** — все 5 sub-plans (85.1–85.5); 47 новых тест-файлов; **4 codegen-бага найдены и исправлены**; **P1** |
| 85.1 | [85.1-stringbuilder-coverage.md](85.1-stringbuilder-coverage.md) | **StringBuilder — полное покрытие.** 17 методов: конструкторы (`new`, `with_capacity`, `from(str)`, `from(char)`), append/plus (str+char, Unicode), метрики (`len`, `byte_len`, `capacity`), `clone` isolation, inspection (`starts_with`, `ends_with`, `is_empty`, `peek`), consume (`into` + use-after-consume D131). | ✅ **ЗАКРЫТ 2026-05-22** — `nova_tests/str_builder/` 9 файлов (7 pos + 2 neg), все 17 методов; 0 codegen-багов; **P1** |
| 85.2 | [85.2-buffers-coverage.md](85.2-buffers-coverage.md) | **ReadBuffer + WriteBuffer — полное покрытие.** ReadBuffer: 44 метода (навигация, байты, u8..u64/i8..i64 LE+BE, f32/f64 LE+BE, char/str, OOB). WriteBuffer: 31 метод (write_*, LE+BE, char, zero, clone). Ключевой тест — round-trip write_X → into → ReadBuffer.from → read_X для всех типов. | ✅ **ЗАКРЫТ 2026-05-22** — `nova_tests/buffers/` 11 файлов (10 pos + 1 neg), все 44+31 методов; 0 codegen-багов; **P1** |
| 85.3 | [85.3-conversion-protocols.md](85.3-conversion-protocols.md) | **From / Into / TryFrom / TryInto — полное покрытие.** User-defined impl, auto-derive Into из From, TryFrom Ok+Err пути, TryInto, generic bounds `[T From[str]]`, compile-error при несоответствии. | ✅ **ЗАКРЫТ 2026-05-22** — `nova_tests/protocols/conversion/` 9 файлов (8 pos + 1 neg); **исправлен codegen-баг** overload `.into()` auto-derive; ограничение `T.from()` на typevar — `[M-generic-static-method-on-typevar]`; **P1** |
| 85.4 | [85.4-comparison-protocols.md](85.4-comparison-protocols.md) | **Hashable / Equatable / Comparable / Display — полное покрытие.** User impl для всех 4. Hashable: детерминизм + различие + встроенные int/bool/char. Equatable: рефлексивность, симметрия, транзитивность. Comparable: Less/Equal/Greater все три. Display: fmt через StringBuilder + вложенный Display. | ✅ **ЗАКРЫТ 2026-05-22** — `nova_tests/protocols/comparison/` 7 файлов (6 pos + 1 neg); **3 codegen-бага исправлены** (char.hash segfault, ==/@eq dispatch, concrete generic return-type); **P1** |
| 85.5 | [85.5-iter-protocol.md](85.5-iter-protocol.md) | **Iter[T] — полное покрытие.** User-defined impl с состоянием, exhaustion (next→None + повторный безопасен), пустой Iter, for-in, early break, вложенные Iter, `str.chars()`/`str.bytes()`, generic bound `[T Iter[int]]`, compile-error для non-Iter в for-in. | ✅ **ЗАКРЫТ 2026-05-22** — `nova_tests/protocols/iter/` 11 файлов (9 pos + 2 neg); 0 codegen-багов в Iter-пути; **P1** |
| 87 | [87-for-in-explicit-elem-type.md](87-for-in-explicit-elem-type.md) | **for-in с явным типом элемента — `for x TYPE in iter`.** `spec/syntax.md` документирует аннотацию типа loop-переменной (`for id u64 in ids`), но парсер не поддерживает — spec/impl drift + дыра в дизайн-консистентности (Nova аннотирует тип везде, кроме for-in). Ревизия 2 (production-grade): аннотация **обязана проверяться** против типа элемента (Go/Rust/TS аннотацию loop-переменной не дают вовсе — Nova получает её как checked superset). Ф.1 AST+парсер, Ф.2 match-сайты, Ф.3 type-check аннотации (ядро), Ф.4 тесты (+негатив mismatch), Ф.5 spec. Источник — Plan 85.5 `[M-for-in-explicit-elem-type]`. | ✅ **ЗАКРЫТ 2026-05-22** (Ф.1-Ф.5: `elem_type: Option<TypeRef>` в AST `For`/`ParallelFor`; `parse_for`/`parse_parallel_for` парсят TypeRef + `mut`; type-check `E7340` сверяет аннотацию с типом элемента итератора; codegen — checked assertion, не потребляется → поведение 1:1; 6 фикстур `nova_tests/plan87/`; обнаружен pre-existing codegen-баг `[M-iflet-match-boxed-sum-ptr]`). **P3** |
| 88 | [88-generic-static-method-on-typevar.md](88-generic-static-method-on-typevar.md) | **generic static-method dispatch на type-параметре.** `fn wrap[T From[str]](s str) -> T => T.from(s)` не мономорфизируется — codegen эмитит литеральный `nova_fn_T_from` → undefined symbol; `Result[T,E]` в return generic-функции → unsubstituted `Nova_Result_...`. Instance-методы на typevar (`it.next()`) работают — дыра в static-вызовах. Это **spec-долг D72** (канонический пример `fn func[K, T From[K]](v K) -> T => T.from(v)`). Планка — **Rust-паритет** (Go/TS структурно не умеют). Линия mono-followup Plan 48→54→63→88. Ф.0 аудит кластера (GATE, +soundness-check), Ф.1 static-call на typevar, Ф.2 type-параметр в return (Result[T,E] безусловно), Ф.3 тесты, Ф.4 spec. Источник — Plan 85.3 `[M-generic-static-method-on-typevar]`. | ✅ **ЗАКРЫТ 2026-05-22** (Ф.0 аудит: 8 probe-фикстур → 2 дефекта D1/D2, все отказы loud — silent-wrong нет; Ф.1: Path-обработчик `recv_seg` резолвит typevar через `current_type_subst` → static-dispatch `Nova_<T>_static_<m>`; Ф.2: `apply_type_subst_to_ref` спец-ветка `Result[T,E]` → `NovaRes_<ok>_<err>`; `Option[T]`/`[]T`/`Box[T]` return уже работали; D72-канонический пример работает — Rust-паритет; 5 фикстур `nova_tests/plan88/` + 2 conversion-теста переписаны с probe-формы). **P2** |
| 89 | [89-iflet-match-boxed-sum-ptr.md](89-iflet-match-boxed-sum-ptr.md) | **деструктуризация боксированного sum-элемента — `for o in []Option[T]`.** `for o in opts { if let Some(v) = o }` для `opts []Option[int]` → CC-FAIL: элемент массива боксируется в `NovaOpt_T*`, а pattern-codegen (`if let`/`match`/`while let`) обращается к `.tag`/`.value` как к значению (рассинхрон value↔pointer). Nova здесь **строго хуже** Go/Rust/TS — деструктуризация optional/sum-элемента коллекции есть везде. Чисто codegen-дефект (тип/парсер не трогаются). Ф.0 аудит value/pointer-боксинга (GATE, decision point A: разбокс в `emit_for` / B: deref в pattern-codegen), Ф.1 реализация, Ф.2 тесты, Ф.3 spec/docs. Источник — Plan 87 Ф.4.2 `[M-iflet-match-boxed-sum-ptr]`. | ✅ **ЗАКРЫТ 2026-05-22** (Ф.0 аудит: 8 probe-фикстур → дефект только у `Option` — единственного value-семантика sum'а; `Result`/user-sum/records работали; все отказы loud. Подход A: `emit_for` деболксит value-семантику sum-элемента (`NovaOpt_T o = *ptr`) — чинит все 5 контекстов (if-let/match/`==`/fn-arg/method) одной точкой; критерий — `SumSchemaRegistry` ABI. 7 фикстур `nova_tests/plan89/`). **P2** |
| 90 | [90-memory-access-primitives.md](90-memory-access-primitives.md) | **Примитивы доступа к памяти (`byte_at` + bulk slice-операции) + аудит FFI / `unsafe`.** Нет примитивов доступа к байтам/срезам: байт `str` по индексу — только через codepoint-`char_at` (O(i)); копирование среза — поэлементный цикл либо лишние аллокации (`slice`/`bytes`); сравнение — поэлементный `eq`→`bool`; `fill` нет. Триггер — миграция str-методов и `WriteBuffer`/`ReadBuffer` на Nova без аллокаций и `external fn`. Три неустранимых примитива: `byte_at` (O(1) байт, для data-dependent сканов — lexer/find/trim), **один** `compare` (memcmp-класс; `==`/`lt..ge` — частные случаи, equality = zero-case `compare==0`), `copy_from`/`copy_within`/`fill`. Корректировка премиссы: self-hosting компилятора **не требует** ни `memcpy`, ни `unsafe`, ни указателей (компилятор = трансформация деревьев). Паритет Go `copy()`/`bytes` / Rust `slice::copy_*`/`[u8]::cmp` / TS typed arrays — **без** сырых указателей и `unsafe`-keyword (консистентно с D6). Ф.0 аудит (GATE) → решение **A safe-only** (`unsafe`/указатели не вводятся), Ф.1 spec D141, Ф.2 рантайм C (`array.h`/`nova_rt.h`), Ф.3 codegen, Ф.4 тесты, Ф.5 docs. Источник — обсуждение self-hosting. | ✅ **ЗАКРЫТ 2026-05-22** (Ф.0–Ф.5; worktree `nova-p90`; вариант A; D141; 6 фикстур `nova_tests/plan90/` — 6 PASS / 0 FAIL). **P2** |
| 91 | [91-stdlib-mvp-for-0.1.md](91-stdlib-mvp-for-0.1.md) | **std MVP для релиза 0.1.** std не компилируется в exe (~3/50 — type-check мягче codegen). Без рабочей std нельзя написать нетривиальную программу → релиз 0.1 бессмыслен, адопшен языка закрыт. Plan 91 — минимально достаточный набор для 0.1: алгоритмическое ядро (`Option`/`Result`, `Vec`/`HashMap`/`HashSet`, `text`, `sort`, `json`, `time`, `math`) — код **уже написан** в `std/`, нужно не новое, а снятие codegen-блокеров. `fs`/`net`/`http`/`sync` (libuv) явно отложены в Plan 18. Ф.0 re-baseline (GATE — STATUS.md от 2026-05-09 устарел, ~50 планов назад), Ф.1 коллекции, Ф.2 текст, Ф.3 json, Ф.4 время/math/sort, Ф.5 conformance-тесты, Ф.6 getting-started, Ф.7 карантин не-MVP + release-checklist. Источник — `std/STATUS.md` + roadmap 0.1 (Plan 01). | 📋 proposed 2026-05-22, не начат, **P0 — блокер релиза 0.1** |
| 92 | [92-flaky-mn-test-stabilization.md](92-flaky-mn-test-stabilization.md) | **стабилизация флаки M:N-тестов.** `concurrency/mn_runtime_actual_workload` периодически падает в полном `nova test` под нагрузкой (16 job'ов), но 3/3 PASS изолированно. Флаки-тест роняет «зелёный» CI и **маскирует настоящие регрессии** (нельзя отличить флаки от реального FAIL без ручного разбора). Корень — либо timing-assumption в test-harness, либо реальная гонка M:N work-stealing runtime (тогда это soundness, эскалация). Ф.0 воспроизведение+диагностика (GATE, decision point A harness / B runtime-гонка), Ф.1 стабилизация по итогам, Ф.2 аудит прочих concurrency-тестов, Ф.3 docs (политика флаки в test-conventions.md). Источник — наблюдение при закрытии Plan 89. | ✅ **ЗАКРЫТ 2026-05-22** (Ф.0 GATE: воспроизведено под устойчивой нагрузкой — 66/100 FAIL, все на `assert(on_w0 < 16)`, `total_ran` 0/352 → **Case A harness-flakiness**: тест проверял work-stealing-распределение как контракт, но это эвристика планировщика; рантайм корректен. Ф.1: тест переписан — контракт `total_ran == 16` детерминированно, распределение статистически (32 batch'а, false-fail ≈ 2.6e-6); standalone под 24-burner нагрузкой → PASS. Ф.2: source-review 75 concurrency-тестов — другого hard-assert эвристики нет. Ф.3: политика флаки-тестов в `test-conventions.md`). **P2** |
| 93 | [93-option-predicates-nova-body.md](93-option-predicates-nova-body.md) | **`Option.is_some`/`is_none` как Nova-методы (`DeclaredBody` routing).** Сейчас `is_some`/`is_none` — компилятор-магия: `external fn` в `std/prelude/core.nv` + C-трамплины `Nova_Option_method_is_some_<T>` в `nova_rt/array.h`. Тривиальные предикаты (`match @ { Some(_) => true, None => false }`), выразимые на Nova одной строкой — паритет с Rust (`Option::is_some` — обычный код в `core`, не интринсик). Узкий осознанный пересмотр Plan 78 Ф.1 (только чистые предикаты; `unwrap`/`map`/`ok_or` остаются C-routed — Fail-dispatch / closure-codegen). Оживляет незавершённый `MethodRouting::DeclaredBody` (огрызок Plan 62.A.bis.4 — сейчас scaffold-only). Ф.0 аудит+decision point (GATE — per-T mono Nova-тела + граница что переносится), Ф.1 `DeclaredBody`-routing + снятие `NovaOpt_`-перехвата, Ф.2 перенос тел + удаление трамплинов, Ф.3 тесты, Ф.4 spec/docs. Выигрыш — de-magic / single-source, не производительность. Источник — обсуждение 2026-05-22. | 🟣 **SUPERSEDED by Plan 95.** Ф.0 ВЫПОЛНЕН — GATE-STOP 2026-05-22: аудит доказал, что `is_some`-Nova-телом требует инфраструктуры «Option/Result в generic-method-mono» (`emit_c.rs:1526` исключает их из generic-механизма; `[M-option-methods-not-mono-able]`). Эта инфраструктура декомпозирована в **Plan 95**; цель Plan 93 (перенос предикатов) = Ф.4 Plan 95. Plan 93 не возобновляется отдельно. **P3** |
| 94 | [94-str-methods-on-nova.md](94-str-methods-on-nova.md) | **Перенос алгоритмов `str` в Nova (`.nv`).** str-методы (`starts_with`/`find`/`to_lower`/`len`/…) сейчас — C-магия (`nova_str_*` в `nova_rt.h` + `external fn`-заглушки + хардкод `str_method_to_rt`). После Plan 90 (`byte_at`) алгоритмы выразимы на Nova поверх `byte_at`/`byte_len`/`StringBuilder` — паритет с Rust (`core::str`) / Go (`strings`), где строковые алгоритмы написаны на самом языке. В C остаются лишь неустранимые примитивы (`byte_at`, `byte_len`, `from(char)`, ranged-compare). Ф.0 аудит+decision points (GATE: примитив ranged-compare для memcmp-скорости; slice view-vs-copy; где живут многострочные тела), Ф.1 инфра (многострочные тела + примитивы), Ф.2 сравнение/поиск, Ф.3 codepoint (`len`/`char_at`), Ф.4 конструкторы (`slice`/`to_lower`/`concat`), Ф.5 коллекции+`hash`, Ф.6 закрытие. Выигрыш — de-magic / self-hosting stdlib. Зависит от Plan 90 ✅. Источник — обсуждение 2026-05-22. | 📋 proposed 2026-05-22, не начат, **P3** |
| 95 | [95-builtin-sum-method-mono.md](95-builtin-sum-method-mono.md) | **`Option`/`Result` как generic-method-able типы (мономорфизация методов builtin sum-типов).** Реализует вывод Plan 93 Ф.0: `Option`/`Result` намеренно исключены из generic-method-mono (`emit_c.rs:1526`) — их представление `NovaOpt_<T>`/`NovaRes_<…>` не связано с mono-механизмом, поэтому Nova-тельный метод на них → `incomplete type`. Plan 95 строит канал **«method-only mono»** (подход B): представление типа не трогается, добавляется только мономорфизация *методов* — `Option`/`Result` в `generic_type_methods` (не в `generic_type_templates`), `receiver_c_type` спец-кейсит их в value-тип, диспетчеризация локализована в существующих перехватах #6/#7 через ветку `MethodRouting::DeclaredBody`. Ф.0 аудит+архитектурное решение A/B (GATE, рекоменд. B), Ф.1 method-collection+routing-scaffold, Ф.2 receiver/mono-name резолвинг, Ф.3 `DeclaredBody`-dispatch, Ф.4 перенос предикатов `Option` (= цель Plan 93), Ф.5 перенос предикатов `Result`, Ф.6 тесты, Ф.7 spec/docs. Выигрыш — de-magic / single-source. Supersedes Plan 93. Источник — Plan 93 «Итог Ф.0» + обсуждение 2026-05-23. | ✅ **ЗАКРЫТ 2026-05-23** (Ф.0–Ф.7 все выполнены; worktree `nova-p95`, ветка `plan-95`, 7 коммитов). Полный `nova test`: **1067 PASS / 0 реальных FAIL / 56 SKIP** (3 CC-FAIL `contracts/f30_*_strict_positive` — Windows lld-link file-lock infra noise под нагрузкой; isolated re-run все 3 → PASS). Подход B (method-only mono): `builtin_sum_type_params` + `generic_type_methods` для Option/Result, `receiver_c_type` спец-кейс → `NovaOpt_<T>` / `NovaRes_<ok>_<err>*`, drain mono-worklist fallback, новый buffer `builtin_sum_method_fwd_decls` (post-typedef ordering), `DeclaredBody`-ветки в перехватах #6/#7, skip регулярного emit. Перенесены: `Option.is_some`/`is_none` + `Result.is_ok`/`is_err` (`=> match @ { ... }` в core.nv); удалены 14 C-трамплинов из array.h + lazy-emit + baseline. Маркер `[M-option-methods-not-mono-able]` закрыт. Plan 93 superseded. Discovery в Ф.6 → Plan 98 (turbofish-gap). **P3** |
| 96 | [96-array-slices.md](96-array-slices.md) | **Sub-slice views для `[]T` — production-grade, паритет/лучше Go/Rust/TS** (rev. 2 после аудита 2026-05-23). У `[]T` нет под-диапазонов; срез `arr[a..b]` — вью (ptr+offset, без копии). Модель: single-type `[]T` с **`cap == len`** для view → push реаллокает и детачится → Go-`append`-footgun устранён без borrow checker'а. Все **11 решений приняты ДО старта** (не GATE-defer): single-type, cap==len (проверено — не нарушает doubling-invariant, deque.h не затронут), 5 форм Range (`a..b`/`a..=b`/`a..`/`..b`/`..` — Rust `RangeBounds` parity), multiple mut-views разрешены, M:N inherits D79, for-in snapshot len, push-detach + lint `W_VIEW_PUSH_DETACH`, neg-index panic, empty slices OK, header 24 байта (тот же что owner), **bracket-форма `s[a..b]` для `str`** (codepoint-indexed, panic на OOB — унифицирует idiom `arr[a..b]` ≡ `str[a..b]`; старый `s.slice` сохраняется clamp для backwards-compat, align в Plan 94). **Бандлит pre-existing fix**: codegen `arr[i]` сейчас НЕ эмитит bounds-check (D27 §1632 drift) — Ф.1 чинит до slice. Закрывает: `Q-array-slicing`, `Q-array-api.5`, D27 §1663 («Слайсинг отложен»), D27 §1632 bounds-check drift. Новый **D144** + amend D27/D58/D6. Production comparison table с byte-layout/aliasing/iter-invalidation/multi-fiber по Go/Rust/TS/Swift/Python + footgun examples. Real-world use cases: network parsers, HMAC, two-pointer quicksort, parser stacks. Ф.1 raw-index bounds-check fix, Ф.2 open-ended Range AST migration, Ф.3 type-checker dispatch, Ф.4 runtime+codegen `nova_array_slice_<T>` + HashMap propagation, Ф.5 interop+push-detach+lint, Ф.6 ~35 тестов pos/neg+GC stress, Ф.7 spec. ~7–8 dev-day. Источник — обсуждение 2026-05-23 + аудит. | ✅ **ЗАКРЫТ 2026-05-23** (Ф.1-Ф.7, 23 теста plan96, regression 1087 PASS / 0 FAIL после rebase на main с Plan 97); lint `W_VIEW_PUSH_DETACH` отложен ([P-plan96-lint-deferred]) — закрывается **[Plan 96.1](96.1-array-slices-followup.md)** |
| 96.1 | [96.1-array-slices-followup.md](96.1-array-slices-followup.md) | **Followup Plan 96 — закрытие 2 deferred-маркеров.** (а) `W_VIEW_PUSH_DETACH` lint в type-checker'е (warning при `let mut view = arr[r]; view.push(...)` — silent detach surprise). (б) Удаление `str.@slice` метода — bracket-only `s[a..b]` (D9 «один очевидный путь»; convergence Rust/Go/Swift/Python). Миграция ~55 std/test call-sites + удаление `nova_str_slice` (clamp) + `external fn @slice` + str_method_to_rt mapping. Закрывает `[P-plan96-lint-deferred]` + `[P-str-slice-clamp-vs-panic]`. ~1.5 dev-day. Источник — обсуждение 2026-05-23 после закрытия Plan 96. | ✅ **ЗАКРЫТ 2026-05-23** (Ф.1-Ф.6, lint + 3 теста plan96_1 + ~55 миграций; оба deferred-маркера → RESOLVED) |
| 97 | [97-protocol-effect-syntax-symmetry.md](97-protocol-effect-syntax-symmetry.md) | **protocol/effect syntax — единый sweep: `.method` static + анон-литералы + `handler` → `effect` rename.** Один план закрывает два связанных пробела одной согласованной итерацией (раздельные Plan 97 + 98 объединены 2026-05-23). **(а) Static-dot в `protocol {}` body** — резолв `Q-static-method-protocol`: `.from(t T) -> Self` для static, bare-имена остаются instance (backwards-compat). Корректирует `From`/`TryFrom` декларации; destale comment 101-108 + `@from` doc-comment. **(б) Type-position анон-protocol** `c protocol { close() -> () }` — D53 §628 bootstrap; закрывает Plan 15 `[P-15-anon-protocol-bound]`. **(в) Clean-break rename** `handler` keyword → `effect` literal + builtin `Handler[E, IRT]` → `Effect[E, IRT]`; миграция ~30 `.nv` + ~10 spec. **(г) Expression-position литерал** `let l = protocol Locker { lock() => state.lock() }` (instance-only) — разблокирует capability-split factory pattern для stdlib Plan 18. Новые **D142** (symmetry) + **D143** (static-dot). Ф.0 design+D-блоки GATE, Ф.1 static-dot, Ф.2 type-anon, Ф.3 rename sweep, Ф.4 protocol-literal, Ф.5 18 pos/neg тестов, Ф.6 close. Источник — обсуждение 2026-05-23. | ✅ **ЗАКРЫТ 2026-05-23** (worktree `nova-p97`, ветка `plan-97`, 5 коммитов, merge `3ef21279795`). Ф.0 D142+D143+amendments; Ф.1 static-dot+`.from`/`.try_from`; Ф.2 anon-protocol type-position+`check_satisfaction_against_methods` (снят `[P-15-anon-protocol-bound]`); Ф.3 clean-break handler→effect+sweep 11 .nv; Ф.4 parser+AST+type-check protocol-литерала (codegen → `[M-protocol-literal-codegen-deferred]` → **подплан [Plan 97.1](97.1-protocol-literal-codegen.md)**); Ф.6 spec sweep 9 .md. **Регресс nova test-all (на main после merge): PASS 1083 / FAIL 0 / SKIP 56.** Q-keyword-symmetry + Q-static-method-protocol ✅ закрыты. **P2** |
| 97.1 | [97.1-protocol-literal-codegen.md](97.1-protocol-literal-codegen.md) | **подплан Plan 97 Ф.4** — codegen vtable struct + dispatch для protocol-литерала. Закрывает `[M-protocol-literal-codegen-deferred]`: до этого `protocol Name {...}` парсился и type-check'ался, но при компиляции в C → `unknown type 'NovaVtable_<Proto>'`. План вводит: (1) эмиссию `NovaVtable_<Proto>` для всех protocol-типов в `emit_type_decl`; (2) `emit_protocol_lit` с synthetic ctx struct + free-fn methods + vtable allocation (analog `emit_handler_lit` через Plan 56 D122 `emit_protocol_vtable_companion`); (3) method dispatch на protocol-typed value через vtable companion. Подход A (synthetic concrete + reuse Plan 72 P3-B инфры). Ф.0 аудит+design GATE, Ф.1 базовая vtable infra, Ф.2 ProtocolLit codegen, Ф.3 dispatch, Ф.4 фикстуры pos/neg, Ф.5 закрытие. ~2-3 dev-day. Разблокирует capability-split factory для stdlib Plan 18. | ✅ **ЗАКРЫТ 2026-05-23** (6 этапов на ветках `plan-97-1[-fu/-hd/-final/-prod/-bench]`, 13 коммитов + 6 merges): **main** Ф.0-Ф.5 — emit_protocol_box_typedef/companion на non-generic + emit_protocol_lit (synthetic ctx + free-fn methods) + method dispatch + capability-split factory + overload-mangling/tuple typedef ordering/closure_capture; **fu** — NovaBox-aware infer (closes `[M-protocol-method-name-shadowing]`) + Ф.4.4 anon-param фикстура; **hd** — Nova-side typed-method-call enforcement + by-value capture для factory + GC stress 1000-цикл; **final** — `scan_expr_fwd` recurse HandlerLit/ProtocolLit (тихий bug fix в pre-emit scan) + multi-method (5 сигнатур) + nested literals + spec D142 canonical example; **prod** — canonical 3-way `Cell.split() -> (Reader, Writer, Resetter)` фикстура; **bench** — micro-bench protocol_lit (direct fn vs vtable dispatch vs literal creation overhead, 10k×). **Регресс на main: 1129 PASS / 1 FAIL pre-existing (plan99_probe intentional gap) / 56 SKIP** (+46 vs baseline 1083 post-Plan 97). 13 фикстур plan97/ (9 positive + 4 negative) + 1 bench. 2 закрытых маркера (`[M-protocol-literal-codegen-deferred]` + `[M-protocol-method-name-shadowing]`). **M** |
| 98 | [98-free-fn-generic-type-param-inference.md](98-free-fn-generic-type-param-inference.md) | **Free-fn generic type-param inference на generic-типах (Option/Result/user-generics).** `infer_type_param_binding` (`emit_c.rs:8480`) умеет выводить `T` только из голого `T` и `[]T` — `Option[T]`/`Result[T,E]`/пользовательские `Box[T]`/`HashMap[K,V]` молча игнорируются → каждый generic-helper, принимающий generic-тип, **требует turbofish** (`check[int](a)` вместо естественного `check(a)`). Хуже Rust/Go/TS, где это базовая unification. Обнаружено в Plan 95 Ф.6.1 (тест `option_in_generic_fn.nv` не компилировался без turbofish'а). Ф.0 аудит+решение A/B (GATE: подход A — `infer_type_param_binding` → метод `&self` + резолв через `novaopt_value_types`/`novares_ok_err`/`generic_type_instance_info` — рекомендуется; B — sanitized-форма, частичная заплатка с silent miscompile-риском), Ф.1 реализация (Option/Result/user-generics в один pass; 16 call-sites правок), Ф.2 тесты позитив+негатив (включая loud-error на конфликтующих bindings), Ф.3 spec/docs. Lineage Plan 88 (static-method-on-typevar). Источник — Plan 95 Ф.6 discovery. | ✅ ЗАКРЫТ 2026-05-23 (merge `c4f02e31be2`); реализация в `emit_c.rs:9076-9151` — Option[T]/Result[T,E]/user-generics через `novaopt_value_types`/`novares_ok_err`/`generic_type_instance_info`; тесты `nova_tests/plan98/` 5/0 PASS |
| 95.bis | [95.bis-option-result-pure-methods-nova-body.md](95.bis-option-result-pure-methods-nova-body.md) | **Перенос «чистых» Option/Result-методов на Nova-body (расширение Plan 95).** После Plan 95 (4 предиката `is_some`/`is_none`/`is_ok`/`is_err`) — ещё 5 методов выразимы на Nova-body инфрой Plan 95 без новых блокеров: `Option.unwrap_or(default_v) => match @ { Some(v) => v, None => default_v }`, `Option.or(other) => match @ { Some(v) => Some(v), None => other }`, `Result.unwrap_or(default_v)` (зеркально), `Result.ok() => match @ { Ok(v) => Some(v), Err(_) => None }`, `Result.err()` (зеркально). Удалены C-трамплины из `array.h` (включая `NOVA_ARRAY_IMPL`-macro entry `Nova_Option_method_or_<T>` + explicit `_nova_str` специализация, `Nova_Result_method_unwrap_or_<n>`/`_ok_<n>` + back-compat `#define`-алиасы) и lazy-emit в `register_novaopt_decl`/`register_novares_decl`. Также удалён inline emit `Result.err()` (`emit_c.rs:14528+` Plan 59 Ф.7.5 D3). Result `DeclaredBody`-dispatch доработан: mono-имя **всегда суффиксированный** (даже для legacy `Nova_Result*`) — иначе C-redefinition с `#define`-алиасами. Не входит (требуют доп. инфры): `unwrap` (Fail-handler — Plan 61 lineage); `unwrap_or_else`/`map`/`map_err`/`ok_or` (closure-applying + method-level generic + Plan 98 inference). ~0.5 dev-day. Источник — обсуждение 2026-05-23 после закрытия Plan 95. | ✅ **ЗАКРЫТ 2026-05-23** (worktree `nova-p95bis`, ветка `plan-95.bis`, 3 коммита). Полный `nova test --jobs 4`: **1063 PASS / 0 реальных FAIL / 56 SKIP** (12 CC-FAIL — все `contracts/f30-f41/trivial_*` + `plan11_followup/f14-f18` с одинаковой `lld-link cannot open output file` infra-noise; sample re-run 4 из 12 в isolation → PASS все 4). **P3** |
| 99 | [99-option-result-closure-applying-methods-nova-body.md](99-option-result-closure-applying-methods-nova-body.md) | **Closure-applying Option/Result методы на Nova-body (master, расширение Plan 95+95.bis).** Оставшиеся 6 методов из 14: `Option.unwrap_or_else`/`map[U]`/`ok_or[E]`, `Result.unwrap_or_else`/`map[U]`/`map_err[F]`. После Plan 99: **14/14** на Nova (только `unwrap` C-routed — Plan 61). **Clean-slate re-audit 2026-05-23:** ключевые инфры (closure invoke arbitrary `fn(T)->U` через `NovaClosBase`+cast, `fn_param_sigs` registration в mono'd method, `method_extra_subst` inference для user-generic, contextual `current_fn_return_ty`, `register_novaopt_decl` для arbitrary T) **УЖЕ работают**. Real gaps = integration: DeclaredBody dispatch не запускает method_extra_subst для builtin Option/Result; mono_name без U-suffix; bare variant ctors (`Some(v)`/`Ok(v)`/`Err(e)`) не используют contextual type; 6 inline emit-блоков. Декомпозирован на **4 sub-plan'а** для атомарной verifiability: 99.1 (infra), 99.2 (ctors), 99.3 (atomic per-method migration), 99.4 (tests+spec+close). Паритет Rust full mono. ~3 dev-day total. Источник — Plan 95.bis closing + re-audit. | ✅ ЗАКРЫТ ЦЕЛИКОМ 2026-05-23 (merge `c48b85c4859`, 12 коммитов). Все 4 sub-plan'а ✅ + 2 hardening fix'а (param-rename `f`→`map_fn`/`default_fn`/`err_fn`, bare-Some arg-type приоритет). **15 / 17 Option/Result методов на Nova-body** (7 Option + 8 Result); только `Option.unwrap`/`Result.unwrap` C-routed (Plan 61). Полный nova test: 1141 PASS / 0 FAIL / 56 SKIP. **P3** |
| 99.1 | [99.1-method-level-generic-in-declared-body.md](99.1-method-level-generic-in-declared-body.md) | **Sub-plan 99: method-level generic в DeclaredBody (foundation).** Extract `method_extra_subst` (`:16386-16494`) в reusable helper, integrate в Option/Result DeclaredBody dispatch с расширенным mono_name `Nova_<Sum>_method_<m>_<T>_<U>...`, trigger `register_novaopt_decl(U)`/`register_novares_decl(U,E)` для return-types, fix `infer_expr_c_type` для method-level. Probe `my_map[U]` на Option/Result. Ф.1 helper extract, Ф.2 Option integration, Ф.3 Result integration, Ф.4 probes, Ф.5 regression. ~1.5 dev-day. | ✅ ЗАКРЫТ 2026-05-23 (commit `bb04949bb32`, merge `c48b85c4859`); `resolve_method_level_subst` extracted в `emit_c.rs:9178+`, integrated в Option/Result DeclaredBody dispatch |
| 99.2 | [99.2-contextual-variant-constructors.md](99.2-contextual-variant-constructors.md) | **Sub-plan 99: contextual variant constructors.** Bare `Some(v)`/`None`/`Ok(v)`/`Err(e)` (Ident-form) в expr-position используют `current_fn_return_ty` для typed compound literal вместо hardcoded `nova_make_<X>_<V>` (который всегда `NovaOpt_nova_int`/`Nova_Result*`). Параллель existing path-form `Option.Some(v)` на `:17251` + `Option.None` на `:17264`. Независим от 99.1. ~0.5 dev-day. | ✅ ЗАКРЫТ 2026-05-23 (commit `444511fdea4` + finalization fix `db200b8539b`, merge `c48b85c4859`); bare `None` rt-based, `Ok(v)`/`Err(e)` rt-based для (T,E), bare `Some(v)` ARG-type-based (fix sub-expr cases — plan11_followup f13/f16/f17) |
| 99.3 | [99.3-migrate-6-closure-methods.md](99.3-migrate-6-closure-methods.md) | **Sub-plan 99: миграция 6 closure-applying методов (consumer).** Atomic per-method shipping (core.nv migration + inline emit delete в одном коммите — C-redefinition risk иначе). 6 commits с regression-gate. Зависит от 99.1+99.2. ~0.5 dev-day. | ✅ ЗАКРЫТ 2026-05-23 (6 commits `c583978e80a` → `335676cdfb5` + ClosureFull fixup `059d0ab1595` + param-rename `a87e3c45058`, merge `c48b85c4859`); `Option.map[U]`/`unwrap_or_else`/`ok_or[E]` + `Result.map[U]`/`map_err[F]`/`unwrap_or_else` на Nova-body |
| 99.4 | [99.4-tests-spec-docs-close.md](99.4-tests-spec-docs-close.md) | **Sub-plan 99: comprehensive tests + spec + docs + close.** 6 positive + 3 negative test fixtures, полный `nova test`, spec `08-runtime.md` Plan 99 block, Plan 78 final amend (15/17 на Nova-body — 14/14 пересмотрен), закрытие маркера `[M-option-result-closure-methods-deferred]`, logs. Зависит от 99.3. ~0.5 dev-day. | ✅ ЗАКРЫТ 2026-05-23 (commit `569a745ccdd` + docs final `351e2916440` + merge-hash fill `42e83340c3e`, merge `c48b85c4859`); 8 фикстур `nova_tests/plan99/` (6 pos + 2 neg), spec D-block, маркер закрыт |
| 100 | [100-linear-must-consume.md](100-linear-must-consume.md) | **consume-типы (production-grade «must-be-consumed») — umbrella roadmap.** Расширение D131 affine `consume` (Plan 73 ✅): `type Transaction consume { ... }` — instance обязан быть consumed на каждом code-path'е. **Ред. 3 (2026-05-23):** production-grade rewrite + закрытие cross-cutting gap'ов — bootstrap имел 5 honest defer'ов + 4 cross-cutting gap'а (FFI / cross-module / migration / IDE-tooling). Декомпозирован на **8 sub-plan'ов**: **core** — 100.1 (D133 foundation), 100.2 generic propagation `[T consume]` + collection-aware (D156), 100.3 borrow `view T` без lifetime (D157), 100.4 cleanup-on-failure sub-umbrella (D158-D162 через 5 sub-sub-plan'ов). **Cross-cutting (Ред. 3)** — 100.5 FFI/external integration (D163), 100.6 cross-module visibility + mangling (D164), 100.7 stdlib migration playbook + 4 pilot migrations (D165), 100.8 performance + LSP + tooling (D166). Сравнение с Go/Rust/TS/Kotlin: matches Rust на 16 capabilities, **превосходит** на 4 (non-suppressable, distinct commit/rollback methods, effect-typed cleanup-fail, нет lifetime overhead). Outline-источник — [Plan 77](77-fluent-return.md) §«Потенциал лучше Rust». **~43 dev-day total** (5+4+4+14+4+3+3+3+3). | 📋 roadmap 2026-05-23, **P3** (polish, не блокер 0.1); 8 sub-plan'ов |
| 100.1 | [100.1-core-must-consume.md](100.1-core-must-consume.md) | **Core static analysis — D133.** Foundation Plan 100: type-level `consume` на type-decl + field + binding + flow-analysis. 10 design-decisions D1–D10 + D5.1 + D5.2 (nested field paths) locked 2026-05-23. Generic-заразность Option/Result/Box[T] auto-consume без хардкода (D6); field-aware flow в методах record'а с reopen-pattern (D5); D5.1 assign в Live consume-поле запрещён; D5.2 multi-level paths `@.inner.tx.commit()`. 17 фикстур (8 pos + 9 neg). Ф.0 GATE / Ф.1 spec / Ф.2 parser / Ф.3 LinearityRegistry / Ф.4 check_consume extension / Ф.5 pilot mock Transaction / Ф.6 tests / Ф.7 close. ~5 dev-day. **Blocks 100.2/3/4.** | ✅ ЗАКРЫТ 2026-05-25 (Ф.0–Ф.7; 23/0 plan100_1; 0 regressions; merge `ab60167f3e5`) |
| 100.2 | [100.2-generic-propagation.md](100.2-generic-propagation.md) | **Generic strict-mode `[T consume]` + collection-aware — D156.** Закрывает silent-leak в generic-коде (`fn first[T](pair (T,T)) -> T => pair.0` тихо теряет pair.1). `[T consume]` bound — opt-in strict mode (внутри generic-body T трактуется как possibly-consume, силент-forget → error). Backward-compat: generic-functions без bound сохраняют silent-ignore. `for consume tx in []Transaction { ... }` consume-aware iteration; `[T consume]` multi-param, tuple param detection. stdlib migration audit. 17 фикстур (11 pos + 6 neg). Ф.0-Ф.7. ~4 dev-day. **GATED на 100.1.** | ✅ ЗАКРЫТ 2026-05-26 (Ф.0–Ф.7; 17/0 plan100_2; 0 regressions; merge f79d4f28b5b) |
| 100.3 | [100.3-borrow-and-view.md](100.3-borrow-and-view.md) | **Implicit view default + closure capture + `match consume` — D157 (Ред. 2 2026-05-24).** `view T` keyword **отвергнут** — view это absence of `consume`/`mut` qualifier (унифицировано с D133). Default `match @file` peek'ает Option[File] без consume; `match consume @file` explicit ownership. Automatic closure capture-mode detection (view / mut-view / consume — FnMut/Fn/FnOnce analogs). **Превосходит Rust** — нет lifetime annotations + нет explicit `view T` keyword. 12 фикстур (7 pos + 5 neg). ~3 dev-day (упростилось после Ред. 2). **GATED на 100.1.** | 📋 proposed 2026-05-23 (Ред. 2 2026-05-24), **P3**; ~3 dev-day, GATED на 100.1 |
| 100.4 | [100.4-cleanup-on-failure.md](100.4-cleanup-on-failure.md) | **Cleanup-on-failure — umbrella production-grade defer/errdefer rework.** Bootstrap-вариант столкнулся с 5 ограничениями D90 (infallible body, no-suspend, interrupt не триггерит errdefer, undefined panic-in-defer, undefined multi-defer partial-failure). **Ред. 2 (2026-05-23):** rewrite через декомпозицию на **5 sub-sub-plan'ов** — amend D90 системно через 100.4.1 (failable body + Plan 49 composition, D158), 100.4.2 (async/suspend body + cancel-safe, D159), 100.4.3 (okdefer + reason-aware `defer \|result\|`, D160), 100.4.4 (multi-defer LIFO error accumulation + panic composition, D161), 100.4.5 (consume-integration final, D162). **Matches Rust на 6 capabilities; превосходит на 5** (scope-level vs Go fn-level; errdefer/okdefer family unique; Plan 49 composition vs Rust double-panic-abort; effect-typed cleanup-fail видно из сигнатуры; supervised-cancel native). ~14 dev-day total. | 📋 roadmap 2026-05-23, **P3**; 5 sub-sub-plan'ов, GATED на 100.1 |
| 100.4.1 | [100.4.1-failable-cleanup-body.md](100.4.1-failable-cleanup-body.md) | **Failable cleanup body — D158 + amend D90 §4.** Снимает ограничение «defer body INFALLIBLE» — `defer { tx.commit() }` где commit имеет Fail[E] становится валиден. Cleanup-fail composes с propagating error через Plan 49 multi-error (primary + suppressed chain). **Matches Java/TS на composition; превосходит Rust** (нет double-panic-abort). 18 фикстур (12 pos + 6 neg). Ф.0-Ф.7. ~3 dev-day. | ✅ ЗАКРЫТ 2026-05-26 (Opus 4.7); 18/18 PASS; followup markers [M-100.4.1-emit-defer-wrap], [M-100.4.1-inline-handler-in-defer] |
| 100.4.2 | [100.4.2-async-suspend-cleanup.md](100.4.2-async-suspend-cleanup.md) | **Async/suspend в cleanup body — D159 + amend D90 §5.** Снимает «no-suspend» — graceful socket close / DB drain / Time.sleep в defer body разрешены. spawn / parallel for / supervised / detach / blocking в defer запрещены (leak supervised hierarchy). 11 фикстур (7 pos + 4 neg) PASS. ~3 dev-day. | ✅ ЗАКРЫТ 2026-05-26 (Opus 4.7); 11/11 PASS; checker change (Option B); followup markers [M-100.4.2-cancel-shielding], [M-100.4.2-time-timeout-integration], [M-100.4.2-await-preservation] |
| 100.4.3 | [100.4.3-okdefer-reason-aware.md](100.4.3-okdefer-reason-aware.md) | **`okdefer` + reason-aware `defer \|result\|` — D160.** Новый keyword `okdefer` (complement к errdefer — выполняется только на success-path). Симметризует defer-family: `errdefer { rollback } + okdefer { commit }` exhaustive cover. Optional `defer \|result\| { match result { Ok(_) => ..., Err(e) => ... } }` reason-aware форма. **Unique среди GC-языков** (никто не имеет success-only cleanup distinction). 12 фикстур (8 pos + 4 neg). ~2 dev-day. **GATED на 100.1.** | 📋 proposed 2026-05-23, **P3**; ~2 dev-day, GATED на 100.1 |
| 100.4.4 | [100.4.4-multi-defer-error-accumulation.md](100.4.4-multi-defer-error-accumulation.md) | **Multi-defer LIFO error accumulation + panic-in-defer composition — D161.** Закрывает 2 production-safety hole'а: (1) Multi-defer LIFO **продолжает** после partial fail (все N defers attempted; errors accumulate в Plan 49 multi-error chain). (2) Panic в defer body **композируется** с propagating через Plan 49 — **нет Rust-style double-panic-abort**. MultiError API (`.primary()` / `.suppressed()` / `.fmt_chain()`). Matches TS SuppressedError + Java addSuppressed; **превосходит Rust уверенно** (no abort + all cleanups attempted). 17 фикстур (11 pos + 6 neg) PASS. ~3 dev-day. | ✅ ЗАКРЫТ 2026-05-26 (Opus 4.7); 17/17 PASS; closes [M-100.4.1-emit-defer-wrap]; followup markers [M-100.4.4-early-exit-cleanup-wrap], [M-100.4.4-interrupt-path-wrap], [M-100.4.4-fmt-chain-rich] |
| 100.4.5 | [100.4.5-consume-integration.md](100.4.5-consume-integration.md) | **Consume-integration final — D162 + amend D90 §7.** Финальный sub-sub-plan: `check_consume` распознаёт defer/errdefer/okdefer как покрывающие consume-vars. **ФИНАЛ Plan 100.4 umbrella.** 5 фикстур (1 POS + 4 NEG) PASS. | ✅ ЗАКРЫТ 2026-05-26 (Opus 4.7, bootstrap MVP); 5/5 PASS + 88 regression tests; leverage Plan 100.8 D166; followup [M-100.4.5-d90-§7-interrupt-errdefer] P2 BREAKING |
| 100.5 | [100.5-ffi-external-integration.md](100.5-ffi-external-integration.md) | **FFI consume integration (type-driven, без keyword'а) — D163 (Ред. 2 2026-05-24).** `external consume fn` keyword **отвергнут** — унифицировано с regular fn: return-type carries consume-ness; `consume` keyword только на params/receivers (D131). Opaque types (Plan 62.D.bis D126) + capability tracking (Plan 16 D63/D64) integrated. C runtime defensive helpers (`nv_consume_validate`, zero-on-consume). Pilot stdlib: File / Mutex / Socket. Generic-заразность через FFI (`Result[File, IoErr]` auto-consume). **Превосходит Rust** — нет `unsafe` keyword (D6) + унифицировано с Nova-side functions. 18 фикстур (12 pos + 6 neg). ~3 dev-day (упростилось после Ред. 2). **GATED на 100.1 + Plan 16/62.D.bis ✅.** | 📋 proposed 2026-05-23 (Ред. 2 2026-05-24), **P3**; ~3 dev-day, GATED на 100.1 |
| 100.6 | [100.6-cross-module-integration.md](100.6-cross-module-integration.md) | **Cross-module + visibility + mangling — D164.** consume-маркер part of exported type signature; Plan 35 visibility (R26) propagates marker. Symbol mangling (extends Plan 81 D134) включает **consume-bit** — ловит cross-version ABI break. Folder-modules (Plan 42), relative imports (Plan 84), re-export (Plan 42.09) — consume preserved. `nova.toml` `[exports.consume_types]` schema; `nova audit` (Plan 03.4) verifies cross-package contracts. Cross-module diagnostics показывают source-package + version. **Matches Rust на всех осях; превосходит** на consume-bit-in-mangling (ловит silent ABI mismatch которого Rust не видит). 15 фикстур (10 pos + 5 neg). ~3 dev-day. **GATED на 100.1.** | 📋 proposed 2026-05-23, **P3**; ~3 dev-day, GATED на 100.1 |
| 100.7 | [100.7-stdlib-migration-playbook.md](100.7-stdlib-migration-playbook.md) | **Stdlib migration playbook — D165.** Финальный sub-plan для production deployment. Full stdlib audit (inventory + rationale per type). `nova consume-migrate` CLI subcommand для assisted migration. Edition-versioning (Plan 62.F.bis) preserves backward-compat. **4 pilot migrations end-to-end**: File (std/io), Mutex+Lock-guard (std/sync), TcpSocket (std/net), Transaction (mock — real DB в Plan 18). Каждый pilot — 5 probes (open-use-close / parallel / error / cancel / cross-fiber). Migration ordering + dependency graph. Deprecation cycle (`#[deprecated_since]` + edition). 20 + 5 фикстур. ~3 dev-day. **GATED на ALL 100.1-6. ФИНАЛ Plan 100 umbrella.** | 📋 proposed 2026-05-23, **P3**; ~3 dev-day, GATED на ALL 100.1-6 |
| 100.8 | [100.8-performance-ide-tooling.md](100.8-performance-ide-tooling.md) | **Performance + IDE / tooling — D166.** ✅ ЗАКРЫТ 2026-05-26. D133/D162 machine-applicable suggestions; `nova doc` [consume] badge + Resource lifecycle section; bench/plan100/ 5 fixtures; `nova consume-analyze` CLI (human + JSON + --fail-on-uncovered); 15/15 nova_tests/plan100_8 PASS; 0 regressions plan100_1/plan100_4_3. C codegen fix: pre-hoist Let bindings referenced in errdefer bodies (hoisted_let_vars). | ✅ ЗАКРЫТ 2026-05-26; 15/15 tests |
| 101 | [101-receiver-generic-prefix.md](101-receiver-generic-prefix.md) | **`fn[T]` receiver-generic prefix + bounds + protocol composition — roadmap (D145).** Закрывает grammar gap: parser silently принимает `fn []T @method` (T = именованный тип, не дженерик), codegen падает. `std/collections/vec.nv` написан с этим bug'ом (7 методов!) → blocker Plan 91 std MVP. Финальный design (Ред. 3 после 3 iterations): **никакого implicit T**, `fn[T]` prefix обязателен везде где receiver не имеет carrier-brackets (`[]T`, bare T, tuple). Reuses [D72](../../spec/decisions/02-types.md#d72) bound syntax (`fn[T Hashable]`). Декомпозирован на **5 sub-plan'ов**: 101.1 (core + vec.nv migration, P1), 101.2 (bound integration), 101.3 (multi-bound `[T A + B]` — closes Q-multi-bound), 101.4 (protocol composition `use Foo` — closes D53 open question), 101.5 (audit + LSP + close, P1). **Nova edge** vs Rust/Go/TS/Kotlin/Scala/Java: cleanest receiver syntax (`fn[T] []T @map[U]` vs Rust `impl<T> Vec<T> { fn map<U> }`), bound без `:`, multi-bound `+` familiar Rust, protocol composition `use` parallel D39, loud disambiguation. ~6 dev-day total. Future: [Q-representation-bound](../../spec/open-questions.md#q-representation-bound) → Plan 102 (concrete-type bounds). | 🟡 roadmap 2026-05-24 (Ред. 3), **P1** (101.1 + 101.5 blocker Plan 91); 5 sub-plan'ов |
| 101.1 | [101.1-fn-prefix-core.md](101.1-fn-prefix-core.md) | **Core `fn[T]` grammar + codegen + vec.nv migration.** PARTIAL ✅ (2026-05-24, merge `0b41ab0db75`): parser `fn[T] ReceiverType @method` работает; vec.nv 7 методов мигрированы (int-array — единственный case с корректным mono); 7 positive фикстуры в `nova_tests/plan101_1/` ALL PASS; spec D72/D145 amend. **LIMITATIONS (deferred):** codegen mono per-T для non-int element types (`[]str`/`[]User`) — marker `[M-fn-prefix-int-only-mono]` (~4-6h codegen работы); type-check 4 error codes (Ф.2) — pending; bare-T receiver codegen (Ф.3 ext) — pending. Regression: 1133/15 vs baseline 1131/13. | 🟡 PARTIAL 2026-05-24 (parser+int-mono shipped); Ф.2 + non-int mono deferred |
| 101.2 | [101.2-bound-integration.md](101.2-bound-integration.md) | **Bound integration `fn[T Hashable]` — reuse D72.** Existing D72 bound syntax (`[T Bound]`) применим в новой позиции `fn[T Bound]` prefix. Не требует новых грамматических конструкций — только integration. 6 positive (single-bound на разных protocol-shapes: Hashable, Ord, parametric From[K], tuple, bare, с method-level gen) + 4 negative (bound violation, unknown protocol, concrete-type bound (refers Q-representation-bound), carrier conflict). Ф.0-Ф.4. ~0.5 dev-day. **GATED на 101.1.** | 📋 proposed 2026-05-24, **P2**; ~0.5 dev-day, GATED на 101.1 |
| 101.3 | [101.3-multi-bound.md](101.3-multi-bound.md) | **Multi-bound `[T A + B]` — closes Q-multi-bound.** Intersection-multi-bound через `+`, параллель Rust `<T: A + B>`. Применим везде где D72 bound допустим (free fn, type-decl, fn[T] prefix). `+` выбран vs `&` (TS) для Rust audience familiarity + no grammar conflicts в pure type-context `[…]`. 5 positive (2-bound, 3-bound, в fn[T] prefix, в type-decl, parametric From[K]+Default) + 4 negative (violation, trailing-`+`, concrete-type, parse errors). Ф.0-Ф.5. ~1 dev-day. **GATED на 101.2.** | 📋 proposed 2026-05-24, **P3**; ~1 dev-day, GATED на 101.2 |
| 101.4 | [101.4-protocol-composition.md](101.4-protocol-composition.md) | **Protocol composition `use A, B` — closes D53 open question (Ред. 5).** `use Type[, Type]*` keyword **внутри protocol body** (parallel D39 record-embed, разная семантика). Single-composition `use Reader` естественна без ambiguity. Multi compact: `use Reader, Writer`. Multiple `use` statements аккумулируются. Mix own + composition в любом порядке в block. Anonymous-composition в type-pos: `fn f(x protocol { use Reader, Writer })`. **Literal-composition (`use` в literal body) ОТВЕРГНУТА** — E_LITERAL_COMPOSITION_NOT_ALLOWED. Industry-aligned (Go interface embed). Asymmetry с multi-bound `+` (101.3) оправдана — разные scopes (use-site bound vs decl-time composition). Cycle detection + duplicate-method handling (E_CONFLICTING_COMPOSITION). 7 positive (single, multi-comma, pure, mix order, anon type-pos, satisfaction, как bound) + 5 negative (self, cycle, duplicate, record-in-protocol, literal-reject). Ф.0-Ф.5. ~1 dev-day. **Independent** (orthogonal к 101.1/2/3). | 📋 proposed 2026-05-24 (Ред. 5), **P2**; ~1 dev-day, independent |
| 101.5 | [101.5-stdlib-audit-close.md](101.5-stdlib-audit-close.md) | **Stdlib audit + LSP quick-fixes + Plan 101 close.** Full sweep std/ (audit на похожие patterns, unskip XFAIL тесты, vec.nv tests в `nova test` baseline). LSP quick-fixes для 4 error codes из 101.1 (Plan 50 D102 format). Plan 91 §«collections» update. 3 pos LSP smoke + 2 neg diagnostic format. Ф.0 audit / Ф.1 migration (если найдено) / Ф.2 vec test integration / Ф.3 LSP quick-fixes / Ф.4 tests / Ф.5 docs + master close. ~1 dev-day. **GATED на ALL 101.1+101.2+101.3+101.4. ФИНАЛ Plan 101.** | 📋 proposed 2026-05-24, **P1** (closing); ~1 dev-day, GATED на ALL 101.1-4 |
| 103 | [103-sync-primitives-spec-formalization.md](103-sync-primitives-spec-formalization.md) | **`std.runtime.sync` production-grade spec + API expansion — roadmap.** Закрытие spec-drift + достижение паритета (или превосходства) с Go/Rust/TS/Kotlin/Java. Декомпозирован на **9 sub-plans (V1 = 103.1-103.8 + V2 = 103.9 gated на Plan 100.7).** Capability matrix vs 4 эталона; **16 design-решений** (M1-M16): fair FIFO default + unfair opt-in, no poisoning, no `Mutex<T>` data-carrying, full Ordering API с default-SeqCst (M1), sized atomics (M2), `ReentrantMutex` opt-in (M3), writer-priority RwLock (M7), closure-form `Once.call_once` primary (M8), distinct `OnceCell`/`Lazy` (M9), `Condvar` tied to Mutex (M10), bounded Semaphore (M11), **type-checker `realtime { }` ban (M12, Nova-unique)**, module `runtime.sync` (M13), backward compat AtomicInt → AtomicI64 alias (M14), V1 без consume + V2 sub-plan 103.9 (M15), atomics никогда не consume (M16). Decision tree «когда channel vs mutex vs atomic vs once» в D173 (AI-first guidance). ~12-16 dev-day V1. Номер 102 зарезервирован Plan 101 под Q-representation-bound. | 🟡 roadmap 2026-05-25, **P1** (spec drift + capability parity); 9 sub-plans |
| 103.1 | [103.1-memory-ordering-api.md](103.1-memory-ordering-api.md) | **Memory ordering API foundation.** `MemOrdering` enum (Relaxed/Acquire/Release/AcqRel/SeqCst — переименован из `Ordering` чтобы избежать коллизии с prelude `Ordering` Less\|Equal\|Greater), `fence(MemOrdering)` module fn, D167 draft в spec (full Q-memory-model closure отложен в 103.7). Foundation для 103.2-103.5. Backward compat: existing API без ordering — preserved. **7/7 тестов PASS** (5 positive + 2 negative в `nova_tests/plan103_1/`). | ✅ **ЗАКРЫТ 2026-05-25** (merge `6be2519b0e2`; commit `9f6b0f902c2`) |
| 103.2 | [103.2-atomics-full-suite.md](103.2-atomics-full-suite.md) | **Atomics full suite (12 sized types × 13 ops).** AtomicI8/I16/I32/I64, AtomicU8/U16/U32/U64, AtomicIsize/Usize, AtomicBool, AtomicPtr[T] (GC-managed). Each: load/store/swap/CAS/CAS_weak/fetch_{add,sub,or,and,xor,max,min,nand} с `MemOrdering` overload + default-SeqCst (M1). AtomicInt deprecated alias на AtomicI64 (M14). Wraparound semantics docs. **17/17 тестов PASS** (11 positive + 6 negative в `nova_tests/plan103_2/`). D168 в spec. | ✅ **ЗАКРЫТ 2026-05-25** (merge `69d7605cc1c`; commit `348b205f47f`) |
| 103.3 | [103.3-mutex-family.md](103.3-mutex-family.md) | **Mutex+RwLock+ReentrantMutex family.** Mutex hardening: `try_lock_for(Duration)`, `is_locked()`, `with_lock[R](fn)` helper (M15 V1 pattern), `new_unfair()` opt-in (M6). RwLock: read/write/try_*/try_*_for/with_*, **writer-priority default (M7)** + reader-priority opt-in. ReentrantMutex opt-in (M3): owner-fiber tracking + lock_count. ≥12 positive + ≥8 negative + ≥3 property + ≥2 stress. ~2 dev-day. **GATED на 103.1+103.2.** | 📋 proposed 2026-05-25, **P1**; ~2 dev-day, GATED на 103.1+103.2 |
| 103.4 | [103.4-coordination-primitives.md](103.4-coordination-primitives.md) | **Semaphore+Barrier+CountDownLatch+Condvar.** Semaphore bounded permits (M11) + try_acquire_for + acquire_n/release_n + with_permit. Barrier (CyclicBarrier reusable) + wait_with_action + reset. CountDownLatch immutable count (safer than WaitGroup). Condvar tied to Mutex (M10): wait/wait_for/wait_until(predicate)/notify_one/notify_all + spurious wakeup contract + ReentrantMutex warning hook. ≥14 positive + ≥8 negative + ≥3 property + ≥2 stress. ~2 dev-day. **GATED на 103.3.** | 📋 proposed 2026-05-25, **P1**; ~2 dev-day, GATED на 103.3 |
| 103.5 | [103.5-once-lazy-oncecell.md](103.5-once-lazy-oncecell.md) | **Once+Lazy+OnceCell.** Once hardening: `call_once(fn)` closure-form primary (M8), legacy run/done deprecated alias, Poisoned state на panic. OnceCell[T]: get/set/get_or_init/take/is_initialized — value-capturing, init panic allows retry. Lazy[T]: auto-init wrapper, force(), init panic poisons (M9 distinct semantics vs OnceCell). **20/20 тестов PASS** (11 positive + 3 negative + 2 property + 1 stress в `nova_tests/plan103_5/`). | ✅ **ЗАКРЫТ 2026-05-26** (merge `c7f9bca1026`) |
| 103.6 | [103.6-realtime-blocking-integration.md](103.6-realtime-blocking-integration.md) | **Realtime/Blocking type-checker enforcement (M12, Nova edge).** Annotations `#realtime_safe`/`#parks`/`#wakes` sweep на ~85 sync.nv methods. Type-checker rejects `realtime { mutex.lock() }` / `blocking { cv.wait() }` etc. **6 error codes** (E_REALTIME_SYNC_PARK/WAKE, E_BLOCKING_SYNC_PARK, E_REALTIME_NESTED_SYNC_VIA_FN, W_REALTIME_TRY_LOCK_FOR_TIMER, W_BLOCKING_NOTIFY_RISK) Plan 50 D102 format. D172 full matrix realtime/blocking × method. ≥10 positive + ≥15 negative. ~1.5 dev-day. **GATED на 103.2+103.3+103.4+103.5.** | 📋 proposed 2026-05-25, **P1**; ~1.5 dev-day, GATED на 103.2-5 |
| 103.7 | [103.7-spec-d-blocks.md](103.7-spec-d-blocks.md) | **Spec D-blocks (D167-D173) final + AI-first guidance + open-questions cleanup.** Promote draft D167-D172 → final, internally consistent. **D173 NEW: AI-first guidance** — decision tree «когда channel vs mutex vs atomic vs once» + 5 canonical patterns с anti-patterns. §1531-1597 rewrite («Mutex/Atomic — НЕ в spec» → «stdlib, не prelude»). Q9/Q12.4/Q-memory-model → ✅ closed/partial-closed. 3 spec consistency tests. ~1.5 dev-day. **GATED на 103.1-103.6.** | 📋 proposed 2026-05-25, **P1** (spec closure); ~1.5 dev-day, GATED на 103.1-6 |
| 103.8 | [103.8-conformance-and-close.md](103.8-conformance-and-close.md) | **Conformance + stress + litmus + audit + V1 close.** Cross-cutting tests: 5 stress (mixed sync + M:N), 4 memory-ordering litmus (IRIW, Store Buffer, Message Passing, refcount-drop), 5 property (CAS convergence, semaphore bounded, barrier all-or-none, condvar no-lost-wakeup, once exactly-once). Backward compat: existing sync_*.nv PASS unchanged. Three-way audit (sync.nv ↔ sync_primitives.h ↔ D-блоки) → audit-report. Cross-platform Windows + Linux clang. Plan 18 + README + logs close. Master Plan 103 V1 → ✅. ~1.5 dev-day. **GATED на 103.1-103.7.** | 📋 proposed 2026-05-25, **P1** (V1 closure); ~1.5 dev-day, GATED на 103.1-7 |
| 103.9 | [103.9-consume-guards-migration.md](103.9-consume-guards-migration.md) | **V2 consume guards migration (GATED on Plan 100.7).** `Mutex.lock() → MutexGuard consume` (RAII drop=unlock), `RwLock.read/write → ReadGuard/WriteGuard consume`, `Semaphore.acquire → Permit consume`, `Once.try_start → OnceGuard consume` (с distinct commit/abort — Plan 100 P-преимущество vs single drop). D174 spec. Bare unlock/release/done → deprecated edition-gated (Plan 62.F.bis). `with_lock(fn)` helpers preserved as thin wrappers → V1→V2 non-breaking. 4 pilot migrations в nova_tests/concurrency/. ≥8 positive + ≥10 negative + ≥2 property. ~2-2.5 dev-day. **GATED на Plan 100.7** (Mutex pilot). Atomics/WaitGroup/Barrier/CountDownLatch/OnceCell/Lazy НЕ мигрируются (M16). | 📋 proposed 2026-05-25, **P2** (V2 enhancement); ~2-2.5 dev-day, **HARD GATE на Plan 100.7** |
| 104 | [104-ide-integration.md](104-ide-integration.md) | **Production-grade IDE integration — roadmap.** Полноценный LSP-сервер + tree-sitter grammar + editor distributions для VSCode/Cursor/VSCodium/Neovim/Helix/Zed. Закрывает Plan 01-roadmap §165 «LSP v0.5» + Plan 100.8 Ф.2 (LSP layer) + Plan 101 LSP V2 marker. Декомпозирован на **10 sub-plans (104.0-104.9)** — foundation, diagnostics, hover/goto/signature, completion, symbols/references, code actions/quick-fixes (≥25 fixes parity с gopls), rename + format, tree-sitter, editor packaging, close-out. **13 design-решений** (M1-M13): separate `nova-lsp` crate (M1), tower-lsp+tokio+ropey stack (M2), debounced full-recheck V1 (M3), V1 target editors VSCode+Cursor+Neovim+Helix+Zed (M5), tree-sitter как sub-plan (M6), JetBrains/DAP/inlay-hints/refactorings deferred V2 (M7/M8/M12/M13), reuse compiler-codegen (M11). ~33 dev-day total. **GATED на Plan 91 (std MVP stabilization) + Plan 100 (consume implementation landing).** Без gate — compiler-codegen API меняется слишком быстро, LSP постоянно ломается. | 🟡 in progress 2026-05-26 (**104.0 ✅, 104.1 ✅, 104.7 ✅**); 10 sub-plans, ~33 dev-day, GATED на Plan 91 + 100 |
| 104.0 | [104-ide-integration.md](104-ide-integration.md) | **LSP Foundation skeleton.** `nova-lsp/` standalone Rust crate: tower-lsp ^0.20 + tokio + dashmap + ropey + tracing. Backend{client,state,shutdown_requested}, initialize/initialized/shutdown lifecycle, did_open/did_change/did_close (Full sync). WorkspaceState{DashMap<Url,ParsedFile{Rope,version}>}. 4 test modules (build_smoke + lifecycle + document_cache + integration), 22 tests total. AsyncLspProcess helper for e2e JSON-RPC tests. | ✅ **ЗАКРЫТ 2026-05-25** (104.0.1–104.0.4; 22/22 PASS) |
| 104.1 | [104.1-lsp-diagnostics.md](104.1-lsp-diagnostics.md) | **LSP Diagnostics — production-grade.** `check_file`/`check_workspace` adapter (catch_unwind + run_with_large_stack 64 MiB), `diagnostic_mapping` (UTF-16 positions, RelatedInformation), incremental rope edits (TextDocumentSyncKind::INCREMENTAL), 200ms debouncer per-URI with CancellationToken, `publishDiagnostics` on open/change/save/close. PerfTimer + `measure!` macro. 9 phases (Ф.1-Ф.9); 5 new test modules (compiler_adapter × 12, publish_workflow × 7, workspace × 7, perf × 4, lifecycle_memory × 5); 91 tests total. | ✅ **ЗАКРЫТ 2026-05-26** (Ф.1-Ф.9; 91/91 PASS) |
| 104.7 | [104.7-tree-sitter-grammar.md](104.7-tree-sitter-grammar.md) | **Tree-sitter grammar для Nova (`nv-lang/tree-sitter-nova`).** Единственный sub-plan 104.x **НЕ gated** на Plan 91/100 — стартует немедленно (syntax-only, не зависит от type-checker/codegen). Полная `grammar.js` (mirror lexer 47 keywords + parser productions), 5 queries (`highlights/folds/indents/injections/locals.scm`), test corpus 84/84 fixtures, distribution (Helix runtime PR draft + Zed extension config + nvim-treesitter snippet + GitHub v0.1.0 release). 10 design-решений (M-7-104.7-1..10): separate repo `nv-lang/tree-sitter-nova` (M1), direct work в нём (M2), manual grammar mirror parser (M3), Node tree-sitter-cli toolchain (M4), Helix-strictest query format (M6), TextMate-compatible scope-names (M7), no external scanner V1 (M8), MIT license (M9). | ✅ **ЗАКРЫТ 2026-05-25** (Ф.0-Ф.7; v0.1.0; 84/84 fixtures; 5 query files; Helix/Zed/Neovim dist) |
| 104.8 | [104.8-editor-packaging.md](104.8-editor-packaging.md) | **Editor packaging & distribution (production-grade).** Все 4 редактора: VSCode TypeScript LanguageClient (auto-restart, configurable binary path, graceful error), Neovim nvim-lspconfig snippet (lspconfig.lua + ftdetect.lua + upstream PR draft), Helix languages.toml (auto-pairs '/' и backtick per D104.8-5, grammar ref tree-sitter-nova v0.1.0 per D104.8-6), Zed extension.toml + config.toml + highlights.scm (side-load). Unified editors/INSTALL.md. ELECTRON_RUN_AS_NODE fix for test runner. V1 simplifications: marketplace submission deferred, bundled binary V2. | ✅ **ЗАКРЫТ 2026-05-26** (Ф.1-Ф.7; VSCode 7/7 PASS; Helix/Zed TOMLs validated; [M-104.8-tool-nvim-unavailable] [M-104.8-tool-hx-unavailable]) |

> Plan 19 — see `19-closure-and-error-ops.md` (closure-rev + D85 error-ops).
> Plan 20 и 21 — последовательные (Plan 21 зависит от Plan 20).
> Plan 22 — самостоятельный, не блокирует Plan 20/21.
> Plan 25 — gap analysis vs Go/Rust state-of-the-art; не план-исполнения, а honest assessment.

## Связанные директории

- [docs/research/](../research/) — справочные материалы и сравнения
