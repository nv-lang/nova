<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 196.2 (ВОЛНА-1) — релокация class-C резолюции из `infer_call_ret_c` в чекер

**Родитель:** [196-one-truth-closeout.md](196-one-truth-closeout.md) (Ф.S1b). **Грунтовка:**
разведка 2026-07-12 (карта `infer_call_ret_c`). **Статус:** 🔄 = **ВОЛНА-1** (bottom-up), АКТИВНА
[opus, nova-p196]. P0-P3 пред-W1 ✓; механизм class-C подтверждён; **первое gate-1 снятие −17 (B11w
`a462a9fc0`)** + vec_lazy починен + `of`; идут дальнейшие снятия. Пара: волна-2 = [196.3](196.3-wave2-d-driven.md).

## Цель

Удалить `infer_call_ret_c` (`emit_c.rs:46293-48883`, **2591 стр**, class-C generic-method-return
mono — крупнейший кусок второго окна) переносом его резолюции в чекер + материализацией в
`resolved_types`. `infer_call_ret_c` — **fallback** (срабатывает только когда Channel-1/2 пусты),
кончается тремя `panic!("[P67-LEGACY]")` (не тихий `nova_int`).

## Метод — ПОРТ, не переизобретение (директива владельца 2026-07-12)

`infer_call_ret_c` уже умеет ВСЁ в том или ином виде. **Переносим готовые алгоритмы на новые
рельсы, НЕ пишем резолв заново:**
1. Взять логику под-случая из `infer_call_ret_c` (как он резолвит этот вид возврата).
2. **Адаптировать фазу:** codegen работает POST-mono и выдаёт C-строку через `type_ref_to_c`;
   чекер работает PRE-mono → выдаёт `ResolvedType` (с `TypeParam`, mono подставит позже в
   `resolved_type_to_c:3203`). То есть меняется НЕ алгоритм, а носитель (C-строка → ResolvedType)
   и место (codegen → `f1_expr_inner`).
3. **Материализовать** результат в `resolved_types_buf.insert(Call.expr.id, rt)` (§1 «материализуй,
   не выбрасывай»).
4. **УДАЛИТЬ** соответствующий под-случай из `infer_call_ret_c`. Сжимающийся fallback держит
   непере­несённое рабочим (strangler-fig).

De-risk: алгоритмы проверены (компилятор работает). Единый C-лоуеринг `resolved_type_to_c:3184`
уже есть — цель, чтобы Call лоуэрился через него, а не через `type_ref_to_c`+string-склейку.

## ⚑ ПРЕД-W1 гейтинг-атомы (блокеры ревью 2026-07-12 — ОБЯЗАТЕЛЬНЫ до первой миграции)

Ревью вскрыло: методика построена на инструменте/координатах НЕслитой ветки `cb44c941e` + 3 тяжёлых куска
не спроектированы. Прежде W1 — эти пререквизиты (каждый со своей приёмкой; W1 не начинается пока P0-P3 не зелёные):

- **P0 — порт `icr_trace` на main + перегенерация координат (блокеры 1,2).** `icr_trace` живёт только на
  `cb944c941e` (grep=0 на main); координаты 114-реестра/порядка W1 — тоже cb-координаты и НЕ совпадают с main
  (`B11c:47885` на main = `read_volatile`, чужой арм). → Портировать `icr_trace` на current-main как
  **env-gated счётчик** (`NOVA_ICR_TRACE`, живёт за флагом СКВОЗЬ правки веток); прогнать корпус; выдать
  (а) remap строк на current-main, (б) стартовую hit-count таблицу 114 веток; закоммитить durable-ледер
  **`196.2-progress.md`**. **`icr_trace-id` = ЕДИНСТВЕННЫЙ стабильный ключ атома; номера строк = snapshot cb,
  устаревают после первого удаления; локатор приёмки = `grep icr_trace` по id, НЕ строка.** «Прогнать трейс»
  = повторяемая команда приёмки каждого атома.
- **P1 — ExprId-спайк (блокер 5, дешёвый).** Подтвердить: `number_unset_exprs` гоняется в nova build/emit-пути
  И присваивает id **post-desugar Call-узлам** (иначе `resolved_types_buf.insert(Call.id)` → мусор → Channel-2
  промах → материализация не работает; синтетические Call от `?`/оператор-desugar — большой класс). Функция
  проверка дешёвая. FAIL → цель материализации меняется ДО W1.
  **★ РЕЗУЛЬТАТ P1 (разведка 2026-07-12, read-only) — ЧАСТИЧНЫЙ FAIL, фикс дешёвый:** `number_unset_exprs`
  (пост-desugar UNSET-нумерация, offset 2^30 — даёт id синтетическим Call от desugar/callnorm/chain_norm;
  `number_exprs.rs:47`) зовётся **ТОЛЬКО в `test_runner.rs:3079`**, НЕ в build-пути. `nova build`/codegen-main
  (`nova-cli/main.rs:1497/1614/2272/4597`, `compiler-codegen/src/main.rs:266/400`) зовут лишь `number_exprs`
  (пре-desugar full) → **синтетические пост-desugar Call в build-пути НЕ пронумерованы (id UNSET)** →
  `resolved_types_buf.insert(синтет-Call.id)` промахнётся в `nova build`. **ФИКС ДО W1 (дешёвый):** добавить
  `number_unset_exprs` в build+codegen-main пути (зеркалить test_runner:3079). Пре-desugar Call (большинство)
  НЕ затронуты — на них материализация работает; риск только у desugar-синтетики (`?`/оператор-цепочки).
- **P2 — mono-registration emit-пасс: ДИЗАЙН + scaffold-атом (блокер 3).** `register_generic_instances_in_typeref`
  (side-effect в резолве, 11 сайтов) знает КОНКРЕТНЫЙ инстанс → при переносе резолва в чекер (pre-mono)
  регистрацию нельзя в чекер. → Спроектировать отдельный POST-mono emit-пасс, регистрирующий mono-инстансы ДО
  их эмиссии; scaffold-атом секвенирован **ПЕРЕД B07r**; приёмка: все mono-инстансы эмитятся, 0 CC-FAIL на
  conformance+std, НЕЗАВИСИМО от удаления любой ветки.
  **★ РЕЗУЛЬТАТ P2 (разведка 2026-07-12, read-only):** `register_generic_instances_in_typeref` (emit_c.rs:17966,
  10 сайтов) при резолве типа enqueue'ит его mono-инстанс в `generic_type_worklist` + `generic_type_instance_info`
  (нужен `subst` → строго POST-mono). **Дизайн emit-пасса — СЛОЖИТЬ регистрацию в `resolved_type_to_c`
  (emit_c:3184):** это УЖЕ единая точка mono-подстановки (TypeParam→конкретика, 3203-3237, POST-mono) → добавить
  туда enqueue конкретного инстанса. Тогда любой лоуэренный из канала generic-ResolvedType авто-регистрирует свой
  mono-инстанс — тот же side-effect, но driven каналом+лоуэрингом, а не `infer_call_ret_c`. Scaffold-атом =
  вынести регистрацию в `resolved_type_to_c` ДО снятия первой side-effect-ветки (B07r/B11c); приёмка: 0 CC-FAIL
  независимо от удалений.
- **P3 — class-C ResolvedType-native: ДИЗАЙН + B07 = ОБЯЗАТЕЛЬНЫЙ спайк-на-авторитет (блокер 4, §7.14).**
  Несущая способность (чекер резолвит generic-method-chain returns в generic `ResolvedType`, неся его ПО
  ЦЕПОЧКЕ в обход §4-лосса) — НОВАЯ, неизвестной осуществимости. Резолверы `resolve_instance_method_return_arity`
  (mod.rs:13518) и `resolve_method_return_with_closure_args` (14059) возвращают lossy `Option<TypeRef>`. →
  Дизайн: новая сигнатура, возвращающая `ResolvedType`; шимы для `Option<TypeRef>`-вызовов; механизм протяжки
  по receiver-цепочке. **B07 = первый class-C атом = МАНДАТНЫЙ §7.14-спайк с жёстким СТОП-условием:** чекер
  производит generic ResolvedType для B07-формы → legacy-арм B07 РЕАЛЬНО удаляется при зелёных гейтах → ИНАЧЕ
  премиса опровергнута, СТОП+переоценка ДО стройки CAP-A/B/C. **Гейт-2 переформулирован: спайк НЕ нужен для
  ДОКАЗАННОГО class-A/B (мех-ка Ф.1/2, 21 арм), ОБЯЗАТЕЛЕН для НОВОГО class-C.**
  **★ РЕЗУЛЬТАТ P3 (разведка 2026-07-12, read-only) — фундамент ЕСТЬ, B07-спайк выглядит осуществимым:** в
  `resolve_instance_method_return_arity` уже живёт механизм TypeParam-протяжки (Шаг 3.2, mod.rs:13705-13708):
  receiver-carrier residual (`-> T`, `-> *mut T` при vacuous `T→T`) БОЛЬШЕ НЕ bail'ится — имя ∈ generics
  объемлющего тела → Call-арм помечает `mark_type_params` → `TypeParam(T)` → лоуэринг через
  receiver-instance-map/subst. Это ровно ResolvedType-native носитель. ОСТАЮТСЯ два bail'а под снятие в B07:
  (a) **13711** — method-level generic `[U]` (d119-класс; предпосылка = тег `[U]`→TypeParam ДО снятия, CAP-B);
  (b) **13714** — mono-container/slice/tuple return (side-effect mono-регистрация → нужен P2 emit-пасс).
  B07-спайк = доказать generic-`ResolvedType` для (a)+(b) под защитой tag + emit-пасс.
  **★★ РЕЗУЛЬТАТ B07-СПАЙКА (исполнение 2026-07-12, opus) — МЕХАНИЗМ ПОДТВЕРЖДЁН, B07 ПЕРЕ-СКОУПЛЕН:**
  инструментация B07-арма на conformance: **CH2-LOWER-ERR = 0** (лоуэринг generic-типа НИКОГДА не падал);
  CH2-OK=14, CH2-ABSENT=2226, id-UNSET=66. Чекер УЖЕ производит generic `ResolvedType` (VecIter[T]/BoxIter[U]/
  Option[T]/tuple-Self); покрытые B07-вызовы УЖЕ обходят legacy через Channel-2 в проде (conformance 95/0 + std
  новым путём). **Механизм class-C доказанно жив и корректен — план НЕ порочен.** НО B07-арм НЕ снимается как
  один −154-атом: оставшиеся 2226+66 ЖИВЫХ кейса = **iterator-adapter chained-receiver ядро** (lazy/iter/map/
  filter/enumerate; эрейзнутые generic-тела std-итераторов) — **материализационный** провал
  (`infer_method_call_channel_type` mod.rs:13970 → None на chained/erased receiver PRE-mono: `?`-bail 14008 +
  method-generic bail 13752 + §4-лосс `[]T↔Vec`), НЕ лоуэринг. Это ровно **CAP-A/B/C (carrier-chain протяжка)** —
  несущее ядро, ещё не построенное. **ПЕРЕ-СЕКВЕНИРОВАНИЕ W1: B07 — НЕ первый атом (он consumer); несущая
  способность = chained-receiver ResolvedType-протяжка строится ПЕРВОЙ, B07/B05/B11c снимаются КАК СЛЕДСТВИЕ.**
  Настоящий authority-спайк = протяжка на ОДНОЙ 2-звенной цепочке (`v.iter().map(f)`); успех → строить CAP-A/B/C
  (подтвердить с владельцем); провал протяжки PRE-mono → РЕАЛЬНОЕ опровержение → owner + Стадия-2/MIR-развилка.
  SHA: P1 `961090743`, спайк-ледер `53040ad25`.
  **★★★ РЕЗУЛЬТАТ CAP-A СУБ-СПАЙКА (2026-07-12, opus) — ПРОТЯЖКА УЖЕ ПОСТРОЕНА, §4-лосс НЕ непреодолим:**
  `infer_method_call_channel_type` (mod.rs:13970) УЖЕ рекурсивно резолвит цепочку, неся generic `ResolvedType`:
  `Vec[T].iter()`→`VecIter[T]`, `.map(f)`→`MapIter[VecIter[T],T,T]` (T протянут). Poison-тест: `iter`/`map` НЕ
  трогают B07-арм (Channel-2 покрывает сегодня). End-to-end inline `v.iter().map(|x|x).next()` компилится+
  исполняется. **СТОП НЕ сработал — «тяжёлое ядро» оказалось УЖЕ построенным.** 2226 CH2-ABSENT (B07-спайк) =
  НЕ отсутствие протяжки, а DOWNSTREAM-gaps:
  — **(a) let-binding усечение:** `let m = chain; m.op()` — let-инференс (`infer_expr_type`) усекает вложенный
    generic → fix bounded 1-локус: RHS-chain let-binding роутить через `infer_method_call_channel_type`.
  — **(b) adapter-internal terminator:** внутри mono'd `MapIter.next()` тело `(@src).next()` падает в B07 →
    P2 mono-регистрация вложенных адаптеров (fold в `resolved_type_to_c`).
  **W1-ИСПОЛНЕНИЕ (пере-скоуп финальный): (a)+(b) = enabler'ы; арм-снятие B07/B11c для итератор-адаптеров =
  СЛЕДСТВИЕ (gate-1 win); затем (c) механически filter/enumerate/take. 196.2 сильно де-рискован — carrier
  готов, PRE-mono-блокеров нет.** SHA суб-спайк-ледер `8b5683d46`.
  **★ РЕЗУЛЬТАТ W1-ЦИКЛА iter/map (2026-07-12, opus) — (a) ГОТОВ, (c) БЛОКЕР = ExprId-across-mono:**
  (a) let-binding scope propagation — ✅ коммит `839c3cf8e` (RHS-chain let-binding в scope через
  `infer_method_call_channel_type`, `types/mod.rs Stmt::Let`; `let m=v.iter().map(f); m.next()` компилится+
  исполняется, conformance 95/0, byte-parity). (b) mono-registration УЖЕ есть (`resolved_type_to_c→
  resolved_named_to_c` emit_c:3782). (c) **арм-снятие iter/map БЛОКИРОВАНО, Δкод-строк = 0.** Root-cause
  (NOVA_MAT_DUMP+NOVA_B07_DUMP): чекер материализует iter **1759×**/map **935×** на корпусе (протяжка РАБОТАЕТ),
  НО B07-хитящие iter/map = **mono-клонированные adapter-INTERNAL узлы, чьи ExprId чекер НЕ материализует**
  (0/10 codegen-B07-id ∈ 468 materialized; id 0=UNSET). **Блокер = ExprId-стабильность сквозь desugar→mono→emit**
  (mono клонирует тела адаптеров с id вне `resolved_types`). Отдельно от §4-лосса (threading подтверждён 3×).
  User-facing цепочки покрыты; adapter-INTERNAL mono-клоны — нет. Арм монолитный (46817-47021), iter/map
  хирургически не отделяются. **РАЗВИЛКА (owner-gated):** (A) спайк bounded-фикса (mono-клон СОХРАНЯЕТ
  template-ExprId → resolved_types покрывает; codegen substs per-instance); (B) пере-order W1 на
  не-адаптерные/leaf-армы без mono-клон-зависимости (gate-1 wins на removable); (C) ExprId-across-mono =
  Стадия-2/MIR-территория → adapter-INTERNAL fallback остаётся до Стадии-2, Stage-1 снимает только removable.
  SHA (a) `839c3cf8e`, findings-ледер `1e08c5574`.
  **★ ПОПРАВКА (A-спайк 2026-07-12, opus): «ExprId-across-mono» — МИСДИАГНОЗ. MIR НЕ нужен.** mono УЖЕ
  сохраняет template-ExprId (`body.clone()`, ExprId=Copy, renumber нет). Настоящий root iter/map-residual:
  **`[]T.of(...)` НЕ в списке резолва slice-конструкторов** (`mod.rs:13077` = `new|with_capacity|from|default|filled`,
  БЕЗ `of`) → `[]int.of`-локал не резолвится → receiver None → B07 (доказано: `Vec[int].of`→0 промахов,
  `[]int.of`→4). 1-словный фикс (`of` в список) даёт conformance iter-промахи 36→0, НО обнажает латентный
  downstream-баг **`NovaOpt from int`** (vec_lazy/testing-property CC-FAIL — тот же класс, что блокирует B11w).
  **Путь (всё Stage-1): (1) фикс `NovaOpt from int` consumption (терминатор читает канал, не перевыводит);
  (2) `of` шипится чисто; (3) реструктурировать МОНОЛИТНЫЙ B07-арм (46817-47021, Channel-2-first dispatch) →
  снятие iter/map = первый gate-1.** SHA findings `ea632b620`.

**Только после P0-P3 зелёных — W1.** (Ранняя формулировка «первый под-шаг W1 = трейс» устарела: трейс = P0,
пред-W1 пререквизит, не под-шаг.)

## ★ Фазовая дисциплина — PRE-mono резолв (чекер) vs POST-mono подстановка (codegen). НЕ СБИТЬСЯ.

> **Чекер резолвит ГЕНЕРИК-шаблон (PRE-mono, с `TypeParam`). Codegen подставляет mono (POST-mono).
> Эту границу НЕ двигать. §0 = ПОЛНОТА generic-`resolved_types`, а НЕ перенос mono в чекер.**

Строго по фазам (по коду, арх-карта §3/§5):
- **Чекер (PRE-mono):** резолвит тип возврата Call в **`ResolvedType` с `TypeParam`-носителями**
  (шаблон; generics ещё НЕ подставлены). Материализует в `resolved_types[Call.id]`. Это template-правда.
- **Codegen (POST-mono, лениво):** `resolved_type_to_c` (emit_c:3184) читает generic `ResolvedType`
  + `current_type_subst` → **подставляет `TypeParam`→конкретика** (3203-3237) → C-тип. Mono-подстановка
  живёт ЗДЕСЬ, НЕ в чекере.
- **Mono-registration side-effect** (`register_generic_instances_in_typeref`, 46995/43205) знает
  КОНКРЕТНЫЙ инстанс → остаётся в codegen (emit-пасс), НЕ переезжает в чекер (чекер generic, инстанса
  не знает). При порте — вынести регистрацию в отдельный emit-пасс ДО удаления арма (§1-риск W1).

**Правило порта (чтоб не сбиться):** `infer_call_ret_c` делает резолв+подстановку+регистрацию ВМЕСТЕ
(post-mono, C-строка). В чекер переносим **ТОЛЬКО РЕЗОЛВ → generic `ResolvedType` (с `TypeParam`)**.
Подстановку НЕ переносим (она уже в `resolved_type_to_c`). Регистрацию НЕ переносим в чекер (в emit-пасс).

**Anti-стрелка (стоп-сигнал):** если чекер начал производить КОНКРЕТНЫЙ (mono'd) тип вместо
generic-с-`TypeParam` — СТОП: ты тащишь mono в чекер. Перенос mono в чекер (post-mono типизированный
IR) — это **Стадия 2 (MIR, в-full), НЕ §0.** Здесь чекер резолвит ШАБЛОН, codegen подставляет.

## Правило: баг/улучшение → в НОВЫЙ путь, не в legacy (директива владельца, §10)

> **Любой баг или улучшение в зоне резолюции Call-возврата чинится/вносится в ЧЕКЕР+`resolved_types`
> (новый путь), а НЕ в `infer_call_ret_c` (legacy). Той же правкой удаляется legacy-эквивалент.**

Патч legacy = углубление долга, который мы убираем (§10 анти-паттерн «band-aid старого пути»).
Следствие: **починка бага = прогресс миграции** (фикс в новом пути + снятие legacy одной правкой).

**Конкретный мотивирующий дефект: `[M-vec-new-static-arity-overload]`** (`std/collections/vec/core.nv:130`,
директива владельца 2026-07-12) — generic-static-ctor **arity-overload cross-wiring**: 0-арг `Vec[T].new()` ↔
3-арг `Vec[T].new(ptr,len,cap)` перепутываются в codegen при сборке ВСЕГО vec-folder-модуля (второе окно
перевыводит НЕ тот overload → в C уходит неверный тип/арность: `Nova_Vec____nova_byte*` там, где `nova_str`;
0-арг функция зовётся с 3 аргументами). Это ровно class-C second-window баг (codegen сам резолвит
static-overload+return). **Один-в-чекере резолв overload+return снимает класс по построению → снятие маркера
= приёмочная веха W2** (выполняется ПОПУТНО с фиксом — складывание `from_raw_parts(ptr *T, len, cap) -> Self` в `Vec[T].new(ptr *mut T, len int, cap int) -> Self require cap >= len` (3-арг overload + `*mut T` вместо reinterpret-cast + контракт `cap >= len`); `new(cap int = 0)` через default-arg —
**ИСПРАВЛЕНИЕ 2026-07-12 (plan-200 агент [sonnet]):** первая эмпирика (то же 2026-07-12) ошибочно
классифицировала `[M-vec-new-cap-default-arg-backfill]` как ТОТ ЖЕ generic-static-ctor-codegen класс, что
`[M-vec-new-static-arity-overload]`, и отложила default-arg путь до W2. Повторная диагностика показала:
default-arg backfill — ОТДЕЛЬНЫЙ, чисто callnorm-side дефект (`try_normalize_call` classify-match не узнавал
turbofish/`__array`-Path static-receiver формы `Type[Args].method(...)`/`[]T.method(...)`, падая в
instance-lookup), НЕ в `infer_call_ret_c`/mono-clone (196-зона) — закрыт В РЕЗОЛВЕ callnorm.rs (не заплатка,
расширение ТОГО ЖЕ classify-match-арма) + 3 hand-formatted ctor-call сайта в emit_c.rs, вне зоны 196. `new(cap
int = 0)` уже В КОДЕ (см. план 200 п.1). **`[M-vec-new-static-arity-overload]`
(overload cross-wiring) остаётся ОТКРЫТЫМ, всё ещё ждёт W2** — это мотивирующий дефект НИЖЕ по-прежнему в
силе для class-C-миграции.).
Исключение — только если legacy-правка временно разблокирует срочное И помечена `[M-*]` с планом
переноса (§4а), но по умолчанию — в новый путь.

## Консумеры (249) и фазовый мост pre→post-mono (директива владельца 2026-07-12)

**Как сейчас:** 249 сайтов зовут `infer_expr_c_type` в **POST-mono** контексте (активен `current_type_subst`).
Внутри: **Channel-2 читает `resolved_types[id]`** (PRE-mono generic с `TypeParam`) → **`resolved_type_to_c`
ПОДСТАВЛЯЕТ** `TypeParam`→конкретика через `current_type_subst` (3203-3237) → **конкретный C-тип**. Пусто →
6z/`infer_call_ret_c` перевыводит concrete напрямую. **`resolved_types` получает управление ПЕРВЫМ**
(Channel-2 до 6z); **мост pre→post = `resolved_type_to_c`** — единственная точка подстановки. Обе стороны
mono сходятся ТАМ.

**Влияние на ДЕКОМПОЗИЦИЮ:** 249 консумеров — **НЕ единица декомпозиции.** Не трогаются: зовут
`infer_expr_c_type`, чьё тело ужимается до Channel-2 по мере снятия веток. Финал = тонкий wrapper
`resolved_type_to_c(resolved_types[id])`, консумеры untouched (либо оптом инлайн — косметика). **Миграция
= только ПРОДЮСЕР (114 веток + армы), консумеры — побочно/финал.**

**Влияние на РЕАЛИЗАЦИЮ (фазовый split):** Тип — прозрачно (чекер кладёт generic, `resolved_type_to_c`
подставляет на сайте post-mono). **Mono-регистрация** (`register_generic_instances_in_typeref`) сейчас в
`infer_call_ret_c` (post-mono, ЗНАЕТ конкретный инстанс). Чекер (pre-mono) инстанса НЕ знает → регистрацию
**нельзя в чекер, только в POST-mono emit-пасс.** Это корень требования «вынести регистрацию ДО снятия ветки».

**Влияние на ПРИЁМКУ:** byte-parity **ПРОЗРАЧЕН к pre/post** (оба пути кончаются конкретным C:
Channel-2→подстановка == 6z→re-infer; split невидим). **Ветки с §1 side-effect — ДОП. критерий приёмки:**
регистрация перенесена в emit-пасс + **0 CC-FAIL + mono-инстансы эмитятся** (иначе снятие ветки ломает mono).

## Правило: удалить ИЛИ паника — НИКОГДА тихий legacy-fallback (директива владельца 2026-07-12)

> **Перенос фичи в `resolved_types` обязан ПОЛНОСТЬЮ брать исполнение в объёме перенесённой фичи.
> В `infer_call_ret_c` для этой фичи код либо УДАЛЁН, либо (если удалить чисто нельзя — переплетено с
> соседними ветками) ЗАМЕНЁН на `panic!("[MIGRATED-<фича>] должно резолвиться через resolved_types")`.
> Тихий legacy-fallback для «перенесённой» фичи ЗАПРЕЩЁН — это ловушка co-authority (два окна сосуществуют).**

- **Паника = enforcement полного перехвата** (§4 нет тихого fallback, §7.13 инвариант = enforced код):
  сработала на корпусе (conformance/std) → новый путь НЕ полон, дыра → чинить в чекере. Не сработала →
  фича целиком на новом пути → panic'd мёртвый код удаляется (тогда строки убывают).
- **Метрика при панике:** ветка «нейтрализована» (удалена ИЛИ заменена паникой) = фича перенесена
  (счётчик веток / ~114-130). Финальное убывание строк — по мере удаления доказанно-мёртвого panic-кода
  (панику НИКОГДА не пробило по корпусу).
- Это нативный паттерн: `infer_call_ret_c` УЖЕ кончается `panic!("[P67-LEGACY]")` для непокрытого — тут
  то же, но для ПЕРЕНЕСЁННОГО (upstream обязан был обработать). Предпочтение — прямое удаление; паника —
  когда переплетение мешает чистому удалению одним атомом.

## Число фич (счётчик миграции)

**~27-30 логических под-каналов** в **7 группах** поверх **~114 return-веток** (`infer_call_ret_c`):
A (turbofish static-ctor) · B (fn-field call) · C (protocol/synth/NovaBox) · D1-D5 (multi-overload
method, 807 стр) · E1-E7 (Ident: free-fn/variant/closure/generic-fn, 400 стр) · F0-F13 (instance-
method-on-typed-recv, 926 стр) · G (Path static, 250 стр). **2 уже сняты** (ProtocolLit/HandlerLit,
Ф.S1a `e93fafd07`/`d45b564d6`). Счётчик закрытия = снятые под-каналы + Δстрок → 0.

## Гранулярность — АТОМЫ по 1-2 часа (директива владельца 2026-07-12)

> **Каждая волна МАКСИМАЛЬНО декомпозируется на атомы. Атом закрывается за 1-2 часа, один
> коммит, и приносит ИЗМЕРИМЫЙ эффект приближения к цели (гейт-1: Δстрок `infer_call_ret_c` < 0).**

- **Атом = ОДНА алгоритмическая ФОРМА резолюции (общая способность чекера), НЕ имя метода.**
  ⚠️ Дробить по ФОРМЕ, а НЕ по имени: `map`/`filter`/`collect` отдельно = per-method хардкод
  (§3-нарушение, перенос хардкода вместо общего резолва). Правильные атомы-по-форме:
  method-generic bare `-> U` из closure-return (покрывает map И любой `@m[U](f)->U`); non-bare
  `-> []U`/`-> Option[U]` (снять bail 13711); container-element из receiver-`T` (get/pop/first
  единообразно); generic-instance с receiver-generic-подстановкой; multi-overload-generic disambig.
  Каждый атом покрывает КЛАСС методов одним путём (резолв `Vec.map` == user-`Stack.map`, §3).
- Атом = порт одной ФОРМЫ → материализация в `resolved_types` → удаление legacy-обработки этого
  класса → замер гейт-1 → коммит. Если ловишь `if method=="map"` — неверная гранулярность.
- **Атом без измеримого убывания строк = НЕ атом** — либо дробить мельче, либо это не перенос.
- Прогресс к цели виден каждый час-два (снятые строки), а не «через месяц». Это анти-«месяц co-authority».

## Волны — HARDEST-FIRST (директива владельца: «наоборот»)

Тяжёлое class-C ядро ПЕРВЫМ — де-риск главного шеста + крупнейшее убывание строк раньше.
Волна = ГРУППА атомов (см. гранулярность). **Приёмка КАЖДОГО атома включает удаление legacy-строк (гейт-1).**

- **W1 — generic-mono INSTANCE (ядро, самое тяжёлое, ResolvedType-native).** D1+D3+F2+F9
  (`v.map(f).filter(g)`, `xs.iter().collect()`, generic-instance-method, container-method elem-mono).
  Портировать: расширить `resolve_instance_method_return_arity`/`resolve_method_return_with_closure_args`
  — снять bail на не-bare `-> []U`/`-> Option[U]` (mod.rs:13711), multi-stmt closure-body (14127),
  multi-overload-generic; нести `ResolvedType` ПО ЦЕПОЧКЕ (обойти §4-лосс `[]T↔Vec` в chained-receiver).
  **Δ ≈ −490 стр в-функции** (helper-detach −200 — позже, см. поправку ниже). Идентично lite/full.
  Взятие W1 (после B07-спайка P3) = доказательство осуществимости всего плана.
- **W2 — generic-mono FREE-FN + static-mono.** E6 (остаток после чекерского U4-inference),
  A+F0+G-static (снять bail `recv.kind!=Instance` 13674, static-return-резолвер в чекер;
  **mono-instance-регистрацию side-effect перенести в emit-пасс, НЕ в inference** — §1/риск). **Снимает
  `[M-vec-new-static-arity-overload]`** (static-ctor overload+return в одном чекер-окне — см. §«баг→прогресс»;
  приёмка W2 = попутный фолд `from_raw_parts` → `Vec[T].new(ptr *mut T, len int, cap int) require cap >= len`).
  **Δ ≈ −500 стр.**
- **W3 — blanket / Vtable / recursive-mono / serde / D-хвосты.** D2/D4/D5 остаток, F11 (Vtable),
  F13 (serialize/opaque/recursive-mono Plan 186), G-serde, C (protocol-shadowing/NovaBox). **Δ ≈ −400+ стр.**
- **W4 — variant-ctor + closure-value + newtype.** E2 (Ok/Err/user-sum-mono), E3/E7/B (closure-value
  calls), E5 (newtype/external-free-fn), F6 (Opt/Result method-return). **Δ ≈ −300 стр.**
- **W5 — builtin-интринсики фикс-возврат (лёгкий mop-up, wire-аннотация).** F3 (ptr-ops), F4/F5
  (Channel/Sender/StringBuilder/…), F7 (from_bytes), F8 (effect-name), F10 (math/str-int), G-builtins.
  Round-trip чист (§4). Материализация declared-return из `external_registry`/`primitive_instance_method_known`.
  **⚠ §3-precheck:** брать ДЕКЛАРИРОВАННЫЙ возврат (extern-декларация / prelude-сигнатура — авторитет), НЕ
  зеркалить хардкод `primitive_instance_method_known` в чекер (иначе перенос хардкода = §3-нарушение); таблица
  = только диспетчер, ТИП берётся из декларации. **Δ ≈ −500 стр.**
- **W6 — тривиальное + ФИНАЛ.** E1 (`println`→Unit), F1 (`.clone()`→receiver), B (fn-field). Затем:
  `infer_call_ret_c` пуст (три P67-panic'а недостижимы) → **удалить функцию + оба call-site**
  (49447, 50652); Channel-6q схлопывается в `resolved_type_to_c(resolved_types[id])`. **Δ → остаток до −2591.**

## W1 — атом-чеклист (пред-декомпозиция, грунтована `cb44c941e` + main, готово к исполнению)

**Знаменатель = 114 веток** (id `self.icr_trace("<id>")` из коммита `cb44c941e`, ветка typed-ir-p1).
**id = стабильный ключ атома. ★ P0 (2026-07-12) ПОДТВЕРДИЛ: `infer_call_ret_c` НЕ разошлась cb↔current-main
(region-diff = ТОЛЬКО trace-строки), `main-строка == cb-строка` для ВСЕХ 114 → номера строк ниже ВАЛИДНЫ на
current-main (remap НЕ нужен). Локатор атома всё равно = `grep icr_trace` по id (после первого удаления строки поедут).** W1 = **11 веток-атомов + 1 scaffold**, hardest-first.

**Способности-портеры W1 (одна способность → N веток, ОБЩИЙ код, НЕ per-method):**
- **CAP-A (generic-instance-method-return):** расширить `resolve_instance_method_return_arity`
  (mod.rs:13518; container-арм 13733-13744 уже открыт, снять vacuous-carrier residual). Несёт Self/tuple-subst.
- **CAP-B (method-generic из closure/args `-> U`/`-> []U`/`-> Option[U]`/`-> Map[K,V]`):** расширить
  `resolve_method_return_with_closure_args` (mod.rs:14059) — снять бейлы 14127/14143/14083, расширить
  bind 14136-14140 (сейчас не ловит `[]U`/`Option[U]`); снять residual-бейл 13711 в arity-резолвере.
  **⚠ Bail 13711 ИНТЕНЦИОНАЛЕН** (код-коммент 13702-13713; слепое снятие = d119-баг). Предпосылка снятия:
  сначала TypeParam-тегирование, чтобы non-bare `-> []U` резолвился в generic `ResolvedType`, а не в
  конкретику; снимать ТОЛЬКО под этой защитой (иначе B07-спайк P3 провалится ложно).
- **CAP-C (container-element `-> T`/`-> Option[T]` из receiver-generic-T, builtin+user единообразно):**
  container-арм 13733-13744 на raw-receivers `NovaArray_`/`Nova_Vec____` (get/pop/first/last).

| # | ветка (id) | main / резолвит | способность (fn+bail) | Δстр | round-trip | side-effect | deps |
|---|---|---|---|---|---|---|---|
| 1 | B07_generic_type_instance_method | 46807-46960 instance-метод на generic-TYPE receiver | CAP-A | −154 | ДА | нет | несущий |
| 2 | B07r_generic_instance_method_return | 46961-47010 финал subst + Self→mono | CAP-A | −50 | ДА | **★ДА 46995** | #1 |
| 3 | B11c_infer_mono_method_ret (F2) | 47845 → helper 43021-43220 | CAP-A+B | −3(+−200 helper) | ДА | **★ДА 43205** | #1,#5 |
| 4 | B05_array_ext_slice_sentinel_mono (D1) | 46602-46709 `[]T @map[U]` sentinel+closure-ret | CAP-B | −107 | ДА | нет | #5 |
| 5 | B06a_method_overload_sentinel_mono | 46718-46749 sentinel→resolve_mono_type_args | CAP-B | −32 | ДА | нет | базовый #3,#4 |
| 6 | B11x_novaarray get/pop/first (F9b) | 48314-48324 `NovaArray_.get`→`NovaOpt` | CAP-C | −~30 | ДА | нет | CAP-C |
| 7 | B11x user `[]T`-ext | 48346-48392 user array-метод, T→elem | CAP-B/C | −~46 | ДА | нет | #5 |
| 8 | B11w nova_vec_bridge (F9a) | 48286-48302 C-runtime bridge | CAP-C | −17 | нет | нет | независим |
| 9 | B06b single-candidate overload | 46758-46762 конкретный return_c_type | CAP-DECL-RET (single-overload) | −5 | нет | нет | shadow(§3) |
| 10 | B06c recv-mut tiebreak | 46791-46798 ro/mut disambig | CAP-D (13640-13668) | −8 | нет | нет | recv-mut в резолвер |
| 11 | B06d pool.first | 46799-46803 после arg-фильтра | CAP-D | −5 | нет | нет | #10 |
| (s) | B06 candidates scaffold | 46710-46717/46804-46806 обвязка | схлоп с #9-11 | −~12 | нет | нет | последним |

**Отложено из W1 (по природе W3):** B11ak (self-recursive-generic, main 48603-48615 → W3 Plan 186),
B07ext (external-registry, 47011-47088 → W3/W5). НЕ трогать в W1.

**Порядок W1 (hardest-first, carrier+base-first — ИСПРАВЛЕН carrier-цикл: B06a-база ПЕРЕД зависимыми
B05/B11c):** B07 → B07r → B06a → B05 → B11c → B11x(get/pop) → B11x(userext) → B11w → B06b → B06c → B06d →
[B06 scaffold]. **ΣΔ W1 ≈ −490 стр в-функции** (helper-detach −200 — позже, см. поправку); B07 = мандатный
спайк (P3).

**★ Достижимость ПЕРЕД удалением (§5, анти-Ф.4c-грабли):** ветка ЖИВА только когда чекер СЕЙЧАС бейлит
на этой форме (простой `v.map(expr)` уже каналится через mod.rs:7809 → бакет для него МЁРТВ). **Правило
атома:** снял бейл в чекере → материализация в `resolved_types` → **перезапуск `icr_trace`** → целевой id
ПОТУХ → удалил арм → `wc -l infer_call_ret_c` строго убыл (гейт-1) → byte-parity/conformance → коммит.
Удалять ТОЛЬКО армы с горящим счётчиком ПОСЛЕ включения нового покрытия — иначе «удаляешь» уже-мёртвое.
High-shadow-риск: B05/B06a/B06b/B07/B07r/B11c (простой случай чекер уже ловит; жив только бейл-резидуал).

**Side-effect (§1):** #2(B07r:46995), #3(B11c→43205) — `register_generic_instances_in_typeref` → перенести
в emit-пасс ДО удаления, иначе mono-инстанс не эмитится → CC-FAIL. **Round-trip-native** обязателен #1-#7.
**Checker-функции W1:** `resolve_instance_method_return_arity` (mod.rs:13518) + `resolve_method_return_with_closure_args`
(mod.rs:14059); диспетчер `infer_method_call_channel_type` (mod.rs:13929, recv-chain 13951-13967).

## Полный реестр 114 задач-веток по волнам (id:строка из `cb44c941e`, Σ=114 ✓)

Каждая строка = ОДНА задача-атом (снять ветку через способность-портер → гейт-1). W1 детально в таблице выше.

**Порядок ИСПОЛНЕНИЯ = hard→easy:** (1) волны W1→W6 — макро-сложность (generic-mono ядро → тривиал);
(2) ВНУТРИ волны — **carrier/dependency-first** (несущий/крупнейший первым, предпосылки раньше зависимых).
Для W1 порядок исполнения явный (ниже). **Для W2-W6 внутренний порядок исполнения фиксируется при входе
в волну** (детальный тайлинг + carrier-сортировка, как получил W1 от разведки) — сейчас там id:строка =
code-order (индекс-справочник, НЕ порядок исполнения).

**W1 (11) — generic-mono INSTANCE, ПОРЯДОК ИСПОЛНЕНИЯ (carrier+base-first; id-ключи, строки cb == current-main,
подтверждено P0):** B07 → B07r → B06a → B05 → B11c → B11x(get/pop) → B11x(userext) → B11w → B06b →
B06c → B06d → B06(scaffold, последним). *(B06a-база ПЕРЕД B05/B11c — исправлен carrier-цикл. Ниже W2-W6 —
code-order индекс из cb, НЕ порядок и НЕ current-main.)*

**W2 (21) — static-ctor + free-fn-mono:** B01:46311 · B10e:47441 · B10f:47456 · B10g_nongen:47481 ·
B10g_stash:47485 · B10j_resolve:47519 · B10j_valaware:47680 · B10j_concrete:47710 · B10j_stash:47719 ·
B10k:47728 · B11a:47747 · B11l_sb:48092 · B11l_wb:48099 · B11l_rb:48106 · B11l_bits:48120 · B11t:48291 ·
B12a:48746 · B12e_sb:48776 · B12e_wb:48783 · B12e_rb:48790 · B12f:48803.

**W3 (17) — protocol/blanket/external/serde-recursive:** B03:46443 · B04:46466 · B07ext:47049 · B08:47089 ·
B08r:47284 · B09:47303 · B10i:47509 · B11l_ext:48127 · B11p:48197 · B11ac:48542 · B11ag:48622 · B11ai:48655 ·
B11aj:48666 · B11ak:48695 · B12m:48892 · B12n_overload:48934 · B12n_deser:48953.

**W4 (13) — variant-ctor + closure + newtype:** B02:46406 · B10b_variant:47328 · B10b_opt:47333 ·
B10b_result:47358 · B10b_usermono:47382 · B10b_novafb:47385 · B10c:47410 · B10d:47424 · B10h:47494 ·
B10l:47734 · B11q:48205 · B11r:48236 · B12p:48980.

**W5 (43) — builtin-intrinsic fixed-return + effect (много веток ОДНОЙ CAP-DECL-RET способностью):** B11d:47901 ·
B11e:47968 · B11f:47976 · B11g:47990 · B11h_chnew:47997 · B11h_crclose:48005 · B11h_crat:48010 · B11h_ctnew:48018 ·
B11h_qual:48030 · B11i:48039 · B11j:48065 · B11k:48078 · B11m:48135 · B11n:48147 · B11o:48163 · B11s_str:48268 ·
B11s_user:48274 · B11u_recv:48299 · B11u_selfrec:48305 · B11u_static:48324 · B11v:48340 · B11y:48476 · B11z:48485 ·
B11aa:48496 · B11ab:48502 · B11ad_eq:48552 · B11ad_pred:48555 · B11ad_hash:48557 · B11ae:48572 · B11af:48579 ·
B12b:48751 · B12c_after:48757 · B12c_at:48762 · B12d:48769 · B12g:48808 · B12h:48814 · B12i:48847 · B12j_1:48854 ·
B12j_2:48859 · B12j_bytes:48865 · B12k:48876 · B12l:48882 · B12o:48961.

**W6 (9) — тривиальное + терминалы + ФИНАЛ:** B10a:47322(→unit) · B10m:47738 · B11b:47871(→receiver) ·
B11u_giveup:48328 · B11ah:48637 · B11al:48714(panic) · B12q:48983(panic) · B12r:48986(panic) · B12s:48990(panic).
**Финал:** удалить `infer_call_ret_c` + call-sites 49447/50652 + 4 эксклюзивных helper (−211 стр).

**Σ = 11+21+17+13+43+9 = 114 ✓** (совпадает с ground-truth `grep icr_trace = 114`, сирот 0).

**Ветка ≠ атом (сплит под-форм):** часть trace-id дробится на >1 атом (B11x → get/pop + userext;
B06 → scaffold + candidates) → ATOM-count ≥ 114 (в W1 «11 атомов» при меньшем числе уникальных id).
Приёмка — per-АТОМ, но каждый атом несёт родительский trace-id; id «закрыт», когда сняты ВСЕ его
под-атомы. Так снимается кажущийся Σ-разнобой 114-vs-число-атомов.

## Контрольная сумма покрытия (верификация a9252e, 2026-07-12 — сирот НЕТ)

Полное чтение всех 2591 строк `infer_call_ret_c`: **сирот 0.** Функция = чистый resolve-and-return
dispatch, каждый регион потребляется return в СВОЁМ блоке (нет накопления/хвостовой логики). После
снятия всех веток + обвязки + helpers + terminals функция **пуста → удаляема** (W6). **Знаменатель
полон** (114 веток-атомов ≙ ~140 raw-return-точек, план крупнее из-за match-арм-коллапса).

**Строки 2591 = 938 comment-only (36%!) + 1653 код + 0 blank.** Реальная кода-масса для гейта-1 ≈ 1653.

**Тайлинг волн → line-range (Σ = 2591, чек-сумма; W1 расписан поштучно выше, W2-W6 — по блокам):**
| Волна | line-range блоков | способность |
|---|---|---|
| **W1** | D-core 46496-47301 + F2 47836-47846 + F9 48281-48395 | CAP-A/B/C (11 веток, см. чеклист) |
| **W2** | A 46307-46390 + E6 47487-47686 + G-static (часть 48632-48879) | CAP-STATIC-CTOR / FREE-FN-MONO |
| **W3** | C 46417-46471 + F11/F13 (часть 47705-48631) + B07ext 47011-47088 + G-serde | CAP-PROTO/BLANKET/SERDE/EXTERNAL |
| **W4** | E-variant/closure (часть 47304-47704) + F6 48142-48201 | CAP-VARIANT-CTOR / CLOSURE-VALUE |
| **W5** | F-builtins (часть 47705-48631) + G-builtins (часть 48632-48879) | CAP-DECL-RET / BUILTIN-INTRINSIC |
| **W6** | E1 47305 + F1 47820 + B 46401-46409 + terminals + **финал: удалить fn + call-sites 49447/50652** | тривиал+финал |

Пер-строчный тайлинг веток W2-W6 (как у W1) — оформляется при входе в волну (та же форма); Σ line-ranges
= 2591 подтверждена структурно.

**Helper-функции (важно для гейта-1):**
- **4 ЭКСКЛЮЗИВНЫХ** (все вызовы внутри `infer_call_ret_c` → удаляемы вместе, **+211 стр экономии**):
  `fn_field_call_ret_c` (46279-46292, W6), `resolve_result_option_ret` (17739-17806, W1/W2),
  `infer_method_level_return_for_sum` (43551-43639, W4), `infer_static_method_ret` (44710-44749, W2).
  Удалять когда снята ПОСЛЕДНЯЯ кормящая ветка.
- **13 SHARED** (зовутся ещё из emit-пути → только ОТВЯЗАТЬ, строки не убывают): `infer_mono_method_ret_with_args`,
  `recv_c_type_materialized`, `register_generic_instances_in_typeref` (§1 side-effect, 10 сайтов),
  `value_aware_subst_to_ref`, `resolve_mono_type_args`, `try_infer_variant_mono_args`, ... .

**★ ПОПРАВКА к W1-чеклисту:** «#3 B11c → −200 helper» — ОПТИМИСТИЧНА. `infer_mono_method_ret_with_args`
(43021) **SHARED** (зовётся из emit-пути 34880 + wrapper 43012) → снятие ветки его лишь ОТВЯЗЫВАЕТ, −200
строк недостижимо, пока не мигрирован emit-путь. **Переклассифицировать: detach, не delete.** Реальный
W1-Δ в-функции ≈ −490 (не −600); helper-строки — позже.

## Приёмка (КАЖДОГО пункта/волны)

1. ★ **Удаление legacy (гейт-1, обязателен per-АТОМ) — метрика:**
   - **ГЛАВНОЕ число: `wc -l` тела `infer_call_ret_c`** (46293→конец) — считать **КОД-строки** (`rg -v` comment+blank; база 1653 из 2591, 36% — комментарии), НЕ сырой построчный счёт. Строго убыло каждый CAP-атом.
     0 снято = КРАСНЫЙ CAP-атом, стоп. **Конечная точка = ~0 строк → функция удалена = §0-цель.**
     - **★ Карве-аут panic/terminal/scaffold:** panic-нейтрализация, терминальные panic-армы (B11al/B12q/r/s)
       и scaffold (B06) НЕ обязаны убывать per-атом — их метрика = закрытые trace-id + отложенное убывание
       при финальном удалении функции. КРАСНЫЙ для них = 0 закрытых id (не 0 строк).
   - **Спутник (структурный): trace-id закрыто, X из ~108 CAP-атомов** (терминалы/обвязка ~6 = чек-лист
     финала, не прогресс к §0). Прогресс = закрытые CAP-id / ~108.
   - **Сквозной: `wc -l infer_expr_c_type`** (общая §0-цель). **NB: `infer_expr_c_type`@48885 — СИБЛИНГ `infer_call_ret_c`@46293, а НЕ его 90%; 196.2 снимает строки из `infer_call_ret_c`, сиблинг почти неподвижен до W6. Родит. гейт-1 в 196 на время 196.2 = `infer_call_ret_c`.**
   - hardest-first выравнивает строки с трудностью (ядро D1/D3/F9/E6/F0 ≈ 1900/2591 строк), поэтому
     крупное убывание строк рано = тяжёлое сделано рано — метрика не обманывает.
2. **Полнота канала:** для каждого снятого под-случая чекер кладёт `ResolvedType` на `Call.expr.id`,
   т.ч. `resolved_type_to_c` даёт тот же C, что удаляемый legacy-арм.
3. **Byte-parity (встроенная страховка) — ПРОТОКОЛ:** baseline = дважды эмитнуть `.c` на ТОМ ЖЕ бинаре ДО
   правки; два прогона расходятся → есть эмиссионный недетерминизм → зафиксировать/замаскировать сид ПЕРЕД
   использованием как эталон (иначе byte-parity ложно-красный). Затем — **порядок атома СТРОГО:** (1)
   материализовать `ResolvedType` в чекере → (2) byte-parity ПОКА legacy-fallback ещё ЖИВ (доказать: новый
   путь даёт тот же C) → (3) ТОЛЬКО потом нейтрализовать legacy-арм (удалить/panic) → (4) re-parity +
   conformance. Расхождение на шаге (2) → чинишь материализацию (fallback ловит, не паникует).
   **Спайк-на-авторитет: для class-A/B НЕ нужен (byte-parity встроен); для class-C (B07, НОВАЯ несущая
   способность) — ОБЯЗАТЕЛЕН (P3/§7.14) ДО стройки CAP-A/B/C.**
4. **Конформанс:** conformance 95/0 · `nova test std` без новых фейлов · nova-build смоук без P67.
5. **Реальная достижимость + ТЕСТ-ПОКРЫТИЕ ПЕРЕД удалением (двойная роль трейса):** трейс-счётчик на
   ветку по ШИРОКОМУ корпусу (conformance 95 + `nova test std` + nova_tests + examples).
   - **Достижимость:** не «удалять» уже-мёртвое (грабли Ф.4c) — снимаем ЖИВЫЕ ветки (hit>0 после включения
     нового чекер-покрытия).
   - **Тест-покрытие:** hit>0 = ветку ЭКЗЕРСИРУЕТ какой-то тест → byte-parity на нём верифицирует миграцию.
     **Ни одна ветка не снимается без экзерсирующего теста.**
   - **0-hit ветка:** либо структурно доказать мёртвость (снять безопасно), либо reachable-но-НЕтестируемая →
     **добавить регресс-фикстуру (§8, позитив+негатив) ПЕРЕД снятием**, иначе миграция неверифицируема.
   - **Это P0 (пред-W1), НЕ под-шаг W1:** трейс (портированный на current-main) по корпусу → **таблица
     hit-count на 114 веток** + стабильные id-ключи = карта «тестируемо / мёртво / нужен тест». База порядка
     и тест-плана; коммитится в `196.2-progress.md`.

**Финал 196.2:** `infer_call_ret_c` удалён (0 недостижимых строк); `resolved_types` полно покрывает
Call; `[M-172.1-lifted-legacy-arms]` (Call-часть) закрыт.

## Размещение регресс-тестов веток (по test-conventions.md)

**ВАЖНО (уточнение владельца):** `spec_tests/conformance/` — **D-block-keyed** (`d<NNN>_<кратко>.nv`, один
файл на D-блок, язык+прелюдия). Произвольные `icr_*`-файлы туда НЕ кладём — нарушило бы конвенцию.

**Безопасность миграции = byte-parity против СУЩЕСТВУЮЩЕГО корпуса, НЕ 114 новых файлов:**
- ветка **hit>0** → её экзерсирует существующий тест (conformance/std/nova_tests) → byte-parity на нём =
  гарантия (новый файл НЕ нужен);
- ветка **hit=0 структурно-мёртвая** → удаление byte-parity-тривиально;
- ветка **hit=0-НО-reachable** → нет экзерсирующего теста → **надо ДОБАВИТЬ покрытие** (few штук, из трейса),
  иначе миграция неверифицируема.

**Куда идёт НОВОЕ покрытие (только untested-reachable) — ПО КОНВЕНЦИИ, не `icr_*`:**
- поведение = **language/prelude-норма с D-блоком** → расширить/добавить `d<NNN>_<кратко>.nv` в
  `spec_tests/conformance/` (spec-first: норма без D-блока → сперва D-блок). Пиннинг (§pinning-test): assert
  ИМЕННО тип/значение, что резолвит ветка (не round-trip) — ловит регрессию миграции.
- поведение = **специфика std-типа** (метод Vec/Channel/StringBuilder/…) → пир-файл `std/<модуль>/<имя>_test.nv`
  рядом с модулем (folder-module = один CU per модуль).
- негатив (если есть) → `neg/` standalone-CU `module neg.<имя>`.

**★ РЕШЕНИЕ владельца (2026-07-12) — для экзотики (external-registry / blanket / serde-reconstruct / opaque)
без существующего D-блока/модуля:**
- **По умолчанию — ЗАВЕСТИ D-блок (spec-first)**, если поведение = USER-VISIBLE **языковая норма**
  (protocol-dispatch возврат, extern-fn возврат по декларации, derive-возврат) → новый/расширенный `d<NNN>` +
  conformance-тест. **Миграция ТАК улучшает spec-покрытие** — вскрывает и заполняет spec-дыры (spec-first §5).
- **module-тест** `std/<мод>/*_test.nv` — если поведение специфично конкретному std-модулю (extern-API модуля).
- **byte-parity-only** — лишь для чисто-внутреннего механизма БЕЗ user-visible отличия (одно наблюдаемое ⇒
  разные пути); редко.

Число таких — из трейса (первый W1-подшаг); D-блок-vs-модуль решаем по-веточно. Экзотика: B07ext/B09/B10i/B11p/
B11ag/B12m (external-registry), B03/B04/B08/B08r (protocol/blanket → D-блок протоколов), B11ai/B11aj/B12n
(serde/opaque → D-блок derive/Plan 180).

## Параллелизация

**Ядро миграции (114 веток) — ПОСЛЕДОВАТЕЛЬНО.** Все ветки правят ОДНИ hot-файлы (чекер-резолверы
`resolve_instance_method_return_arity` mod.rs:13518 + `resolve_method_return_with_closure_args` mod.rs:14059
+ удаление из `infer_call_ret_c` emit_c.rs) → параллельная правка = merge-ад (§9) + замутнённый byte-parity
(какая правка дала diff?). Плюс carrier-first зависимости. **Атом = один коммит, последовательно.**

**Что МОЖНО параллелить (file-disjoint дорожки):**
1. **★ Регресс-тесты** (untested-reachable покрытие — ПО «Размещению» выше: `d<NNN>_*.nv` в conformance ИЛИ
   `std/<модуль>/*_test.nv`, НЕ `icr_*`) — независимые файлы, **компилятор НЕ трогают.**
   Параллельный агент пишет ветки-фикстуры, опережая migration. Track-A (тесты) ∥ Track-B (migration) —
   разные файлы, без конфликтов. Крупнейшая безопасная параллель.
2. **Read-only prep:** coverage-трейс (one-shot, кормит все волны); пер-волновой line-range тайлинг W2-W6
   (анализ, код не правит).
3. **Side-effect emit-пасс extraction** (вынос `register_generic_instances_in_typeref`) — distinct prep, НО
   трогает emit_c.rs → согласовать регион с migration-агентом (§9), не параллелить слепо.

**Поток:** трейс (one-shot) → ДВЕ дорожки: **(A) тест-фикстуры** [независимо, опережая] ∥ **(B) migration**
[последовательно, атом=коммит, использует тесты из A]. Внутри B — строго последовательно (hot-файлы).

## Риски (из карты)

- **★ Side-effects в inference (§1).** `infer_call_ret_c` при резолве РЕГИСТРИРУЕТ mono-инстансы/worklist
  (A:46356, F0/Array/Tuple в `resolved_type_to_c`). При переносе резолва в чекер — регистрация ДОЛЖНА
  переехать в отдельный emit-пасс, иначе mono-инстанс не эмитится → CC-FAIL. Ключевой риск W1/W2.
- **Дублирующий C-лоуеринг** `type_ref_to_c` (legacy) vs `resolved_type_to_c` (цель). Per-case доказать
  `resolved_type_to_c(from_type_ref(tr)) == type_ref_to_c(tr)` (точка дивергенции — `Ref`/`Mut`-стрип §4).
- **Round-trip лосс (§4)** — только chained-receiver (W1: D1/D3/F9, W2: F0/A). ResolvedType-native обязателен.
- **Ф.4c-обвязка** (мёртвый `resolve_return_channel`+`debug_assert`+`let _=channel`, mod.rs:13761-13768)
  — снести или доразвить в W1; примитивы `constraint_solver.rs` переиспользуемы ВНУТРИ (ResolvedType-native).
- **ExprId staleness / desugar** — синтетические Call после desugar без id → канал промахивается;
  подтвердить `number_unset_exprs` в compile-пути (арх-карта §7 — не подтверждён).
- **Гейт-1 ≠ «трудное сделано»** — W5 (интринсики) снимает много строк дёшево; W1 (ядро) меньше строк
  на больший труд. Не спутать. Ядро W1 — реальный шест.
