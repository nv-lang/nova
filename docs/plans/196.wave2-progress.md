<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 196 — Волна-2: ледер прогресса (D-driven миграция сиблингов второго окна)

**Под-план:** [196.3-wave2-d-driven.md](196.3-wave2-d-driven.md). **Родитель:** [196-one-truth-closeout.md](196-one-truth-closeout.md).
**Worktree:** `D:/Sources/nv-lang/nova-wave2` (ветка `wave2-d-driven`). **Модель:** opus.

Ледер пополняется per-D: `(D · функция удалена/panic'd · тесты red/green · Δ · SHA · синк)`.

---

## Окружение сборки/теста в worktree (воспроизводимо)

```
NOVA_GC_LIB_DIR=D:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/lib
NOVA_GC_INCLUDE_DIR=D:/Sources/nv-lang/nova/compiler-codegen/vcpkg_installed/x64-windows-static/include
```
- libuv-подмодуль скопирован из главного репо в `compiler-codegen/nova_rt/libuv` (robocopy `/XD .git`) — обязателен даже для тривиального `nova build`.
- Сборка: `cargo build --release --manifest-path nova-cli/Cargo.toml` → `nova-cli/target/release/nova.exe`.
- Гейт conformance: `nova.exe test --positive --compile-error spec_tests/conformance` (один CU, ~3-4 мин). **Базлайн 2026-07-12: PASS 95 / FAIL 0.** ✅

---

## Архитектура «нового пути» (проверено по коду — куда мигрируем)

Чекер наполняет `resolved_types_buf` (в `type_check_ctx`, `types/mod.rs`) → лифтится в `ModuleEnv.resolved_types`
(`types/mod.rs:1895`). Дополнительно `resolved_callees: ExprId → Span` (`emit_c.rs:876`, ставит `main.rs:512`).

Диспетчер `infer_expr_c_type` (`emit_c.rs:48903`) читает каналы в порядке, legacy — LAZY-fallback:
1. **Пред-канальные спец-кейсы** (ДО каналов): `try_as[T]` (48911); **`infer_generic_static_ctor_ret`** (48958) —
   stub-wrapper mono ctor.
2. **Канал 1** (48963): `resolved_callees[id]` → `fn_ret_by_span[span]` — тип возврата Call от выбранного чекером callee.
3. **Канал 2** (48987): `resolved_types[id]` → `resolved_type_to_c` — тип любого выражения (вкл. closure-Func 49001).
4. **Legacy-fallback:** `infer_call_ret_c` (46311, зона волны-1) + сиблинги — срабатывают, когда каналы пусты.

**Миграция сиблинга = заставить чекер записать `resolved_callees`/`resolved_types` для соответствующей формы →
канал выигрывает → сиблинг недостижим → удалить (или panic для shared с волной-1).** Проверка: byte-parity + 95/0.

---

## ★ Инвентарь → D-фича → test-status → ОТДЕЛЯЕМОСТЬ (карта на 2026-07-12)

Отделяемость по вызовам сиблинга относительно зоны волны-1 `infer_call_ret_c` = **46293-48883**:
- **SEP** = ни одного вызова в зоне волны-1 → можно удалить, не задев волну-1.
- **SHARED** = есть вызов в зоне → только detach/мигрировать логику + `panic!("[MIGRATED-<D>]")`, НЕ удалять пока зовёт волна-1.
- **MIXED** = вызовы и внутри, и вне.

| Сиблинг (стр.) | D-фича | Conformance-тесты (есть) | Отделяемость (вызовы) | Судьба (арх.) |
|---|---|---|---|---|
| `infer_expr_c_type_str` (42837) | — (мёртвая обёртка) | — | **DEAD** (0 вызовов) | 🗑 удалить немедленно |
| `infer_static_method_ret` (44728) | static-методы D182/D372 | d182_self_return_parametric_static, d143_protocol_static_method, static_ctor_instance_method_named_tuple | **SHARED** (28058 вне; 48244/48274/48678 внутри) | ➡ чекер + panic (волна-1 зовёт) |
| `infer_generic_static_ctor_ret` (18272) | generic-static ctor D372/D239 | d372_canonical_new_defaults, types_generic_static_ctor, slice_static_generic_method, d239_slice_vec_alias | **SEP** (18179 вне; 48958 в диспетчере, вне зоны W1) | ➡ чекер (пред-канальный кейс → канал) |
| `infer_method_level_return_for_sum` (43569) | методы sum (Option/Result/enum) D52/D85/D407 | d52_sumint, d407_enum_payload_width, d406_enum_kind_token, d85_result_payload_width | **SHARED** (48170/48174/48199 все внутри) | ➡ чекер + panic |
| `infer_lambda_return_type_with_params` (45907) | замыкания D48/D402 | d22_closures, d402_closure_return_width, d157_view_default_closures, d417_closure_scalar_return_neg | **SEP** (42274/42278 — emit-lowering closure) | ➡ чекер (closure-Func канал уже есть 49001) |
| `infer_trailing_block_sig` (29709) | trailing-block D43 | d43_trailing_block_and_fn | **SEP** (29509/29522 — emit closure-sig) | ➡ чекер (sig из resolved_types) |
| `resolve_result_option_ret` (17743) | Result/Option D325/D85 | d325_result_everywhere, d85_question_return, d30_try_op_unwrap_pair, d408_option_chain_sized_width | **SHARED** (46753/47623 внутри) | ➡ чекер + panic |
| `resolve_result_te` (44203) | Result T/E D325 | d325_result_everywhere, d85_result_payload_width | **MIXED** (4716/28186/28298/44219 вне; 48193 внутри) | ➡ чекер + panic |
| `resolve_result_te_strict` (44216) | Result T/E строгий D325 | d325_result_everywhere | **SEP** (31782/32029) | ➡ чекер |
| `infer_type_param_binding` (19156) + `_from_ref` (18962) + `_rt` (2527) | generics/type-param D53/D119 | d53_type_protocol_kind_token, d119_method_level_type_params, d135_bare_generic_inferred | **MIXED** (десятки вызовов; `_rt` SEP) | ➡ чекер (решение в resolved_types) |
| `infer_protocol_structural_binding` (19022) | протоколы D42/D53/D355 | d42_structural_protocol, d355_blanket_protocol, d72_generic_bound_protocol | **SEP** (567/18194/18310/18328/18686/18700/19284) | ➡ чекер |
| `infer_result_type_params` (16687) | Result-типпараметры D325/D53 | d325_result_everywhere | **SEP** (16690/16704/16712/44202/44207/51020/51107) | ➡ чекер |
| `resolve_method_level_subst` (19350) | method-generics subst D119/D122 | d119_method_level_type_params, d122_bound_method_mono_dispatch, d122_generic_bound_forwarding | **SEP** от W1 (все < 46293) | 🔒 **[Q10 2026-07-12]** Tier-2 ЗАБЛОКИРОВАНО — направление «➡ чекер» верное, но канала (per-call node_substs-аналог) НЕТ; постройка трогает forbidden `f1_check_call` (types/mod.rs ~10242/10452). Doc-комментарий в коде добавлен, легаси не тронут. См. 196.3-wave2-d-driven.md §«Q10-подтверждение» |
| `resolve_mono_type_args` (18416) | mono type-args D122/D277 | d122_*, d277_generic_value_record_mono, d123_tuple_mono, d354_generic_anon_tuple_mono | **MIXED** (46831 внутри) | 🔒 **[Q10 2026-07-12] ИСПРАВЛЕНО:** НЕ «lowering-subst» — прежний вердикт ошибочен (rustc-аналогия неприменима без `node_substs`-канала). Tier-2 ЗАБЛОКИРОВАНО, тот же блокер что у twin-функции выше. Doc-комментарий в коде добавлен, легаси не тронут |
| `compute_array_elem_type_for_obj` (19703) | элемент массива/слайса D239/D373 | d239_slice_vec_alias, d373_generic_array_api, d403_vec_sized_element_read, d411_array_sized_elem_width | **MIXED** (48334 внутри) | ➡ чекер (elem = часть ResolvedType receiver) |
| `infer_mono_method_ret(_with_args)` (43029/43039) | generic-method возврат D119/D122 | d119_*, d122_*, d368_generic_return_width | **MIXED/SHARED** (47154/47255/47863/48506 внутри) | ➡ чекер + panic |
| `primitive_instance_method_known` (42954) | методы примитивов D109/D74 (§3 хардкод) | d109_primitive_builtin_methods, d74_math_methods | зовётся из ЧЕКЕРА (`types/mod.rs:9946`) + codegen | 🗑 §3: возвраты из .nv-деклараций |
| `type_ref_to_c` | тип→C D315 (ретайр, дублирует `resolved_type_to_c`) | d315_resolvedtype_lossless_canon | пронизывает всё | 🗑 D315 → `resolved_type_to_c` |

**Вывод по стратегии волны-2:** истинно-SEP и без побочек = `infer_expr_c_type_str` (мёртвый) → снят первым.
Далее SEP-возвраты, где чекер уже почти пишет канал (closure/trailing/protocol/result-te-strict/result-type-params).
SHARED (static_method_ret, method_level_return_for_sum, result_option_ret, mono_method_ret) — только detach+panic,
удалит их волна-1 при опустошении `infer_call_ret_c`. `compute_mono_name`/`compute_generic_type_c_name`/
`register_generic_instances` (мех. мангл+регистрация по УЖЕ решённому subst) ОСТАЮТСЯ — законный lowering, per
rustc-эталон mono=подстановка. **[Q10 2026-07-12, ИСПРАВЛЕНО]** `resolve_mono_type_args`/`resolve_method_level_subst`
(вычисление САМОЙ подстановки T/U из param-TypeRef ⋂ arg-C-type) — это НЕ тот же lowering: rustc-аналогия «mono =
подстановка, не инференс» держится там ТОЛЬКО потому, что `typeck` уже пишет `node_substs`; у Nova
per-call node_substs-канала нет (чекер `f1_check_call` явно skip'ает generic-возврат, `typeref_mentions_any`
guard ~10478) → обе функции — Tier-2 ВТОРОЕ ОКНО, СТРУКТУРНО ЗАБЛОКИРОВАНО (не «остаётся законно» — блокировано
до постройки канала, вне scope одного окна). Подробности: `196.3-wave2-d-driven.md` §«Q10-подтверждение».

---

## Первый заход — инвентаризация тест-покрытия (2026-07-12, БЕЗ фиксов/прогонов/миграции)

Директива владельца: ПЕРВЫЙ ШАГ = инвентаризация недостающих тестов per D (матрица форм×аспектов), НЕ миграция.
Компилятор не трогался (dead-`infer_expr_c_type_str`-снос откачен — отложен до захода миграции).

- **Трекер 307→308 файлов (196.3): ВСЕ строки помечены 🔍** (покрытие проверено). Инвентарь-релевантные D —
  матрично-проанализированы (таблица «Пробелы тест-покрытия по D-фичам» в 196.3, 12 строк-фич); прочие ~160 D —
  coverage-ok батчем (не-инвентарные фичи, существующие проходящие тесты, вне аспектов волны-2).
- **Найденный пробел (единственный actionable):** USER-type **static / generic-static** методы × `{default, named}`
  на **call-site** не упражнялись. `d102_*` покрывал только free-fn; `types_generic_static_ctor` ДЕКЛАРИРУЕТ
  `make(cap=42)` но **без test-блоков** → default-backfill не вызывался в runtime. Cross-module generic-static
  default (`Vec[int].new()`/`new(cap:8)`) уже покрыт d372.
- **Добавлен тест-файл (без прогона):** `spec_tests/conformance/d102_static_generic_static_defaults.nv` — 5
  test-блоков (static all-default / static named-override / static both-named-out-of-order / generic-static
  default-per-элемент-тип / generic-static named-override). Near-copy проверенного синтаксиса d102/d182/d88 →
  low-risk; НЕ верифицирован (директива «без прогонов») — прогон при заходе миграции.
- **Структурное ограничение:** USER-type кросс-модуль static/generic-static (отдельный ПАКЕТ) не выразим в
  single-file conformance; кросс-модуль-форма = импорт std (покрыта d372 Vec). Задокументировано как пробел, файл
  не добавлялся (нельзя безопасно собрать multi-file инфру без прогонов).

## Закрытые D (ледер)

_(пополняется — заход МИГРАЦИИ отдельно, по команде владельца)_

## Заблокированные D — Tier-2 (Q10, 2026-07-12)

- **D119/D122 mono / method-generics** (`resolve_mono_type_args` emit_c.rs ~18421,
  `resolve_method_level_subst` ~19355): классифицированы ВТОРОЕ ОКНО (unification-движки, не lowering),
  но migration СТРУКТУРНО ЗАБЛОКИРОВАНА — нет per-call node_substs-аналога канала; постройка требует правки
  forbidden `f1_check_call` (types/mod.rs ~10242-10475, маркер 10452 внутри). Подробный разбор + кодовые
  доказательства: `196.3-wave2-d-driven.md` §«Q10-подтверждение». Действие этого захода: doc-комментарии в
  коде (behavior-preserving, легаси не тронут) + исправлена ранняя карта (строки 61-62 выше). Гейты:
  conformance 95/0 (`c_keyword_ident_mangling`/`d13_no_prelude_panic_assert_intrinsic` timeout — подтверждённый
  флак, PASS в изоляции), targeted `nova test std/collections` 13/13 PASS. Тест-покрытие D119/D122
  перепроверено — без пробелов (6 файлов существуют). Ждёт владельца: развилка (A)/(B) из Q5 (построить канал
  vs отложить фундамент).
