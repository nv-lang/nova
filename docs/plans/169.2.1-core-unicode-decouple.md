# Plan 169.2.1 — Развязать `prelude.core` от `std.unicode`

> **Под-план [Plan 169.2](169.2-nova-tests-fix-sweep.md)** (ось «корректность»).
> **Создан:** 2026-06-19. **Статус:** ✅ CLOSED 2026-06-19 (branch
> `plan-169.2.1-core-unicode`, НЕ merged).
> **Приоритет:** P1 — чинит plan107 `#prelude(partial)` (корень
> `[M-169.2-prelude-partial-stdlib-graph]`).
>
> **Что сделано (✅ без упрощений, как для прода):**
> - 10 char-@методов перенесены из `std/prelude/core.nv` обратно в
>   `std/unicode/category.nv`; `import std.unicode` убран из `core.nv`
>   (цикл `core→unicode→collections→core` разорван).
> - Доступность char-@методов «без import» обеспечена **resolver-инъекцией**
>   `std.unicode` на method-call-селектор (`@method:`-тег, восстановлен механизм
>   Plan 159 Ф.4 — Plan 162 Ф.4 его удалил), а **НЕ** facade-реэкспортом:
>   реэкспорт сломал бы opt-in-границу свободных функций (см. §«Корректировка»).
> - `Option/Result @debug` (`#impl(Debug)`) вынесены из `core.nv` в
>   `protocols.nv` — иначе частичный `#prelude(core, runtime)` падал
>   `E_IMPL_UNKNOWN_PROTOCOL` (Debug/Write неизвестны), как только маска D133 ушла.
> - Spec: **[D303](../../spec/decisions/07-modules.md#d303)** (новый) +
>   amend D253 (03-syntax.md) + регистрация в таблице решений (README).
>
> **Acceptance-чеклист:**
> - [x] **«Без упрощений как для прода»** — production-grade декомпозиция; нет
>   хардкодов/обходов; механизм Plan 159 Ф.4 восстановлен полноценно.
> - [x] plan107 D133 (`[D133-not-consumed] sb`) — **устранён** (была корневая цель).
> - [x] plan159 — 0 новых FAIL (`f4_no_import_char_methods` PASS; 4 CC-FAIL
>   `nova_str`-init — до-существующие, не связаны).
> - [x] plan152_3 — 16/16 PASS (вкл. char-@методы под import).
> - [x] Opt-in negative boundary цел: `n_char_unicode_opt_in` +
>   plan152_4 (case/words/sentences/unicode) — 6/0 PASS.
> - [x] Option/Result Debug (plan91_14 + plan91_15) — 27/0 PASS.
> - [x] Spec D-блок + таблица решений обновлены.
>
> **Остаточный plan107 FAIL (вне scope, followup
> `[M-169.2.1-runtime-stringbuilder-partial]`):** `prelude_core_attr`/
> `prelude_multi_attr` теперь проходят type-check, но дают CC-FAIL
> `must use 'struct' tag … Nova_StringBuilder` — `runtime.unreachable`
> использует interp-string `"unreachable: ${reason}"`, лоуерится в
> `Nova_StringBuilder`, чей typedef частичный `#prelude(core, runtime)` не тянет
> (ось `runtime → collections`, ортогональна `core → unicode`). `no_prelude_*` 2
> FAIL — до-существующие, отдельные. Итог plan107: было 4 FAIL → осталось 4
> FAIL, но природа сменилась (type-check падения D133/E_IMPL_UNKNOWN_PROTOCOL
> устранены; остались codegen/link gaps), что соответствует «или меньше FAIL»
> в §Проверка и снимает корень `[M-169.2-prelude-partial-stdlib-graph]`.

## Проблема (корень plan107)

`#prelude(core, runtime)` падает: `[D133-not-consumed] sb`. Цепочка:
1. `core.nv:78 import std.unicode` тянет ВЕСЬ unicode folder-module (14 файлов).
2. `std/unicode/normalize.nv::cps_to_str` использует `consume sb =
   StringBuilder…; sb.as_str()`.
3. D133 (type-check, ДО codegen/DCE) проверяет инлайненный `cps_to_str`.
4. StringBuilder consume-методы — в `collections`, которого при partial
   `#prelude(core, runtime)` НЕТ → объявление не подтянуто → D133 видит `sb`
   не-consumed → падение.

`core` тянет весь unicode ради **8 char-методов** (`'a'.is_alphabetic()` и т.д.,
`core.nv:466-497`), которые делегируют в unicode-предикаты. Это намеренный цикл
`prelude.core → std.unicode → std.collections → prelude.core` (`core.nv:74`).

NB: НЕ cross-module-резолв и НЕ DCE — это неполнота транзитивных зависимостей
при partial prelude + полный type-check инлайненного кода. (DCE работает на
codegen, ПОСЛЕ падения в type-check.)

## Решение (утверждено 2026-06-19)

char-unicode-методы → из `core` в `unicode`; «доступность без import» обеспечить
через **полный prelude** (реэкспорт), а не через core. **`core` становится чистым.**

1. Перенести 8 char-методов из `std/prelude/core.nv` (строки ~466–497:
   `@is_alphabetic`/`@is_numeric`/`@is_alphanumeric`/`@is_whitespace`/
   `@is_uppercase`/`@is_lowercase`/`@to_uppercase`/`@to_lowercase`) в
   `std/unicode/` (откуда их перенёс Plan 162 Ф.4 — обратный ход).
2. Убрать `import std.unicode` из `core.nv` → core не зависит от unicode.
3. В `std/prelude.nv` (rolling facade) добавить реэкспорт этих char-методов из
   unicode → обычный код (полный prelude) видит `'a'.is_alphabetic()` без import.

## Корректировка при реализации (2026-06-19) — реэкспорт ОТКЛОНЁН в пользу resolver-инъекции

Шаг 3 (facade-реэкспорт) **не выполним как есть** и заменён на восстановление
механизма Plan 159 Ф.4 (resolver-инъекция). Причина: имена char-@методов
**совпадают** с одноимёнными **свободными функциями** в `category.nv`
(`is_alphabetic(cp int)`, `general_category(cp int)`, `to_uppercase(s str)`), а
селектор реэкспорта `export import std.unicode.{is_alphabetic, …}` матчится по
`FnDecl.name` без различения receiver → реэкспорт вытащил бы **свободные функции**
в глобальный namespace prelude и **сломал бы opt-in-границу**, закреплённую
негативным тестом `plan152_3/neg/n_char_unicode_opt_in.nv` (требует
`general_category(0x41)` = `undefined identifier` без `import std.unicode`).

Вместо facade-реэкспорта восстановлен механизм **Plan 159 Ф.4** (его удалил
Plan 162 Ф.4 при хосте в core): резолвер (`imports.rs`) на *method-call*-селектор
char-Unicode (`expr.is_alphabetic()`, помечен `@method:` в `lints::collect_expr`)
инжектит `import std.unicode` в **пользовательскую entry-группу** (cycle-free
путь), а Plan 159 Ф.1 DCE срезает неиспользуемые таблицы. Это различает
method-call от free-call → opt-in-граница свободных функций сохранена. `std/prelude.nv`
получил только **комментарий** (почему реэкспорт не годится), без реэкспорт-строки.

**Доп. фикс (вне исходного текста плана, для прода):** `Option/Result @debug`
(`#impl(Debug)`, Plan 91.15 Ф.2) перенесены из `core.nv` в `protocols.nv` —
они зависят от протокола `Debug` и sink-типа `Write` (оба в `protocols.nv`), и
после снятия маски D133 частичный `#prelude(core, runtime)` падал на них
`E_IMPL_UNKNOWN_PROTOCOL`. Это восстанавливает «ZERO imports» для `core`.

## Результат

- **Обычный код** (полный prelude): char-методы доступны без import (реэкспорт) —
  `plan159/f4_no_import_char_methods` цел.
- **`core` сам**: не тянет unicode → `#prelude(core)` не тянет normalize/
  StringBuilder → plan107 D133 **не падает**.
- **partial `#prelude(core)`**: char-методы недоступны (unicode не подтянут),
  но код unicode туда не инлайнится → ошибки нет (ожидаемо для урезанного prelude).

## Потребители char-методов (миграция, всего 3) — ИТОГ: миграция НЕ потребовалась

- `nova_tests/plan159/f4_no_import_char_methods.nv` — «без import». ✅ Цел: работает
  через resolver-инъекцию (Plan 159 Ф.4, восстановлена), а не через реэкспорт.
  PASS.
- `nova_tests/plan152_3/char_unicode.nv`, `nova_tests/plan152_8/t5_category_ok.nv`
  — уже имели `import std.unicode` (правка не нужна). ✅ PASS (plan152_3 16/16,
  plan152_8 category — PASS).

## Edition snapshot

`std/prelude/e2026_05.nv` НЕ трогать — edition-pin сейчас никем не используется
(ни один `nova.toml` не задаёт `edition`); snapshot замораживается намеренно.

## Проверка

- `nova test nova_tests/plan107` → было 4 FAIL, ожидается PASS (или меньше FAIL).
- `nova test nova_tests/plan159` / `plan152_3` / `plan152_8` → 0 новых FAIL.
- Регресс: обычный `nova test` (char-методы через полный prelude) — без регрессий.

## Замечания / альтернативы (если перенос окажется дорогим)

- Если category-предикаты сами тянут collections (проверить) — возможно
  достаточно развязать только category от collections (вариант 3 из 169.2),
  не перенося методы. Но утверждённый путь — перенос в unicode + реэкспорт.
- **ИТОГ (реализация):** перенос дешёвый; реэкспорт заменён на resolver-инъекцию
  (см. §«Корректировка»). Вариант 3 не понадобился.

## Followup-маркер

`[M-169.2.1-core-unicode-decouple]` ✅ **CLOSED** (2026-06-19) — закрывает
`[M-169.2-prelude-partial-stdlib-graph]` (ось `core → unicode` развязана,
D133-падение устранено).

**Новый followup (открыт 169.2.1):** `[M-169.2.1-runtime-stringbuilder-partial]`
— частичный `#prelude(core, runtime)` даёт CC-FAIL `Nova_StringBuilder` без
typedef: `runtime.unreachable` лоуерит interp-string в `Nova_StringBuilder`, чей
typedef живёт в `collections` и не подтягивается урезанным prelude. Ось
`runtime → collections`, ортогональна 169.2.1. Варианты фикса: (a) переписать
`unreachable` без interp-string, (b) включить StringBuilder-typedef в
`#prelude(…, runtime)`, (c) DCE неиспользуемого `unreachable` до codegen.
