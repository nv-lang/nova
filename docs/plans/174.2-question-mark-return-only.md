# Plan 174.2 — `?` строго return-only (Rust-стиль) + чистка spec

> **Top-level план.** Создан 2026-06-20. **Статус:** ✅ **CODEGEN-ЧАСТЬ РЕАЛИЗОВАНА** (Plan 173 Ф.1 #3,
> ea55bee7, 2026-07-04): чекер `[E_TRY_IN_FAIL_FN]` (Часть A) + amend D85 сделаны. **Переопределение
> vs исходный план:** де-риск 173 показал, что throw-mode `?` РЕАЛЬНО используется — D196 form 2
> (`consume X = expr? {}`) + defer-body (D158). Поэтому codegen `in_fail_ctx`-ветку **НЕ удалили**
> (Option A: EXEMPT consume-init + defer-body; чекер режет только FREE `?` в non-Result/Option fn).
> Часть B (авто-From) остаётся ОТКЛОНЕНА. **Маркер:** `[M-174.2-question-return-only]`.
> **Решение (2026-06-20):** `?` = **return-only** (только `Result`/`Option`, проброс
> значением, как Rust); throw-режим D165 убрать. Конверсия типа ошибки — **руками
> `.map_err`** (авто-`From` рассмотрен и ОТКЛОНЁН, см. Часть B).

## Часть A — `?` строго return-only (ДЕЛАЕМ)

`?` работает **только** в функциях, возвращающих `Result[T,E]` / `Option[T]`, и пробрасывает
**значением** (`return Err/None`). В Fail-эффект-функциях `?` **запрещён** — там ошибка
пробрасывается прямым `throw` или оператором `!!`. Один оператор — одна семантика.

- **Обоснование:** `never` — это тип операции эффекта `fail(value E) -> never`
  ([04-effects.md:922](../../spec/decisions/04-effects.md#L922)), а не функции (у неё свой
  `-> T`); смешивать value-propagation (`?`) с effect-throw (`fail`/`!!`) незачем. Это
  каноническая [D85](../../spec/decisions/04-effects.md#d85) («`?` не задействует Fail»).
- **Fallout минимальный:** в компилируемом корпусе `?` уже используется только на
  Result/Option-функциях (`nova_tests/effects/throws.nv`) — throw-режим D165 не задействован,
  «нечего откатывать». Мигрировать лишь: aspirational examples (`http.nv`, `oxsar_port.nv`)
  + doc-comment-примеры on_exit в spec/stdlib (`@commit()?` → `@commit()!!`).

### Изменения по слоям

1. **Checker** (`compiler-codegen/src/types/mod.rs`): `?` в функции на Fail-эффекте
   (return-тип не Result/Option) → `[E_TRY_IN_FAIL_FN]` с подсказкой «используй `!!` или `throw`».
2. **Codegen** (`compiler-codegen/src/codegen/emit_c.rs:21236-21340`): убрать ветку
   Fail-context throw для `?` (`in_fail_ctx`) — станет недостижимой (checker отверг раньше);
   оставить только return-Err/None.
3. **Спека:** amend [D85](../../spec/decisions/04-effects.md#d85) — `?` return-only, запрещён
   в Fail-fn. Починить stale `## D4` (04-effects.md:290) + дубль `####` (:950) (до-D85,
   противоречат канону). Поправить doc-comment on_exit-примеры (`@commit()?` → `!!`) в
   spec D188 + `std/prelude/{core,protocols,errors}.nv`.
4. **Тесты** `nova_tests/q_return_only/`: (a) `?` на Result → return Err; (b) `?` на Option →
   return None; (c) neg `?` в Fail-fn → `[E_TRY_IN_FAIL_FN]`.

## Часть B — авто-`From` конверсия ошибки: ОТКЛОНЕНО (2026-06-20)

Рассматривалось: `?` сам конвертит тип ошибки через `From` (как Rust:
`Err(e) => return Err(From::from(e))`). **Отклонено.** Причины:

1. **Противоречит принципу Nova** «не магия / single canonical path» ([D85](../../spec/decisions/04-effects.md#d85)) —
   авто-From это спец-правило (компилятор ищет `From`-impl за тебя).
2. **Боль меньше, чем в Rust.** У Rust нет эффектов → ошибки текут только значениями, конверсия
   на каждом слое задалбывает. У Nova Fail-эффект пробрасывает сам, ремаппинг типов — явно в
   `with Fail`-хендлере; авто-From помог бы только value-style `?` (ýже, не самый идиоматичный путь).
3. **Скрывает конверсию** в точке вызова (`parse(s)?` не показывает смену типа ошибки; может
   незаметно терять поля).
4. **Асимметрия обратимости:** добавить авто-From позже (если explicit задолбает) — легко и
   неломающе; убрать магию, когда на неё завязан код, — больно. Молодому языку — начать строго.

**Замена:** при `E ≠ E'` — явный `expr.map_err(|e| E'.from(e))?`. Видно на месте, не теряет данные молча.

> Если на практике boilerplate станет реальной болью — пересмотреть (маркер сохранён). Тогда
> учесть **ловушку identity-blanket**: `From[T] for T` есть для всех `T`, поэтому конверсия
> допустима только при `E ≠ E'` И наличии **не-identity** `From[E]` на `E'`.

## Гейт / зависимости

- Реализация компиляторная (checker + codegen) → зона **172**/владельца.
- Пересекается с 172.1 U.4 (typed IR): после U.4 типы ошибок в точке `?` известны чище.

## §7.7-оценка остатка (2026-07-06) + закрытие

**Что РЕАЛЬНО осталось живым (grep-факты на входе):**
- Ядро (`[E_TRY_IN_FAIL_FN]`, exempt consume-init/defer, amend-скелет D85) — **реализовано 173 Ф.1**
  (`types/mod.rs` `check_fn` → `check_try_return_only_*`, span-точный walker). НЕ переделывалось.
- Баннеры на stale `## D4` (04-effects.md:290) и дубль `#### `?` — сахар над match + throw`` (:983) —
  **уже проставлены** (retraction→D85 при enforcement 173 Ф.1) → противоречие снято ранее.
- **Живой остаток = Ф.A spec-closure + Ф.B cross-carrier диагностики + миграция aspirational-сайтов + conformance.**

**Сделано (spec-first, 2026-07-06):**
- **Ф.A spec-closure (D85):** тело D85 дочищено — (a) авто-`From` отклонение вынесено в
  **полноценный блок** в «### Что отвергнуто» (не только NB-врезка); (b) убрана ссылка на «D165 как
  D-блок» (D165 = error-code семейства Plan 100, не decision) → «прежний throw-режим D67-эры»;
  (c) NB-врезка про `## D4`/`####` обновлена (баннеры проставлены → противоречие снято, не «чинится»).
- **Миграция `?`→`!!`** в on_exit/@cleanup-примерах (return-тип `()`/`Fail`, не Result/Option):
  `03-syntax.md` (3 сайта), prelude `core.nv`/`protocols.nv`/`errors.nv` (4 сайта), aspirational
  `examples/effect_density/http.nv` (2) + `examples/real_world/oxsar_port.nv` (8). Doc-сайты
  (`docs/dev/idioms/*`, `cleanup-cookbook.md`) — по §3.2 переданы в 173 Ф.2 doc-sweep (не тронуты здесь).
  NB: `tx.commit()?` в `okdefer{}`-теле — **D158-exempt** (legal), НЕ мигрированы (это не on_exit).
- **Ф.B cross-carrier диагностики (собственная checker-часть, carrier-независима):** новый
  `check_try_carrier_match` (`types/mod.rs`, вызывается из `f1_expr` Try-арма ТОЛЬКО для `?`, не `!!`):
  enclosing-return-носитель публикуется через `current_fn_return_ty` (RAII `FnReturnTyGuard`, set в
  `f1_check_fn`); операнд-носитель через `infer_expr_type`. Диагностики:
  **`[E_TRY_OPTION_IN_RESULT_FN]`** (Option-`?` в Result-fn → hint `.ok_or(<err>)?`) и
  **`[E_TRY_RESULT_IN_OPTION_FN]`** (Result-`?` в Option-fn → hint `.ok()?`). Консервативно:
  молчит если тип не выводится / несёт generics (gs) / носители совпадают → **не может превратить
  компилирующийся код в падающий** (мисматч уже был бы type-error на `return None`/`Err`).
- **Тесты:** conformance neg `spec_tests/conformance/neg/d85_try_in_fail_fn_neg.nv` (E_TRY_IN_FAIL_FN) →
  conformance `--compile-error` = **53/0**. nova_tests: pos `effects/plan174_2_question_carrier.nv`
  (носители совпали + `.ok_or()?`/`.ok()?` конверсии работают — валидировано изолированным CU,
  PASS), neg `effects/neg/plan174_2_try_{option_in_result,result_in_option}_neg.nv` → **2/0 PASS**.

**ОТЛОЖЕНО (honest, с обоснованием):**
- **E1≠E2 error-type mismatch → hint `.map_err`** (третья диагностика §3.2 Ф.B) — **НЕ реализована**:
  по D85 `E'` обязан быть совместим с `E` (тот же тип ИЛИ supertype через sum-расширение), поэтому
  наивный `E1≠E2` (по base-name) даёт **false-positive на легальном widening** и ломает
  компилирующийся код (регрессия). Корректная форма требует интеграции с sum-extension/assignability-
  движком (зона 172.1). Маркер: `[M-174.2-try-err-type-mismatch-hint]` (новый). Прочие §3.2-пины
  (`a? ?? b` прецеденс, `foo()?.bar()` postfix, `?` в lambda, `?` в main) — поведение уже корректно
  по существующим правилам парсера/D85; отдельная диагностика не требуется.

## Followup-маркер

`[M-174.2-question-return-only]` (бывш. `[M-174.2-question-from]`; авто-From отклонён, Часть B).
Новый отложенный: `[M-174.2-try-err-type-mismatch-hint]` (E1≠E2 `.map_err`-hint, gate = sum-extension
compat в 172.1).
