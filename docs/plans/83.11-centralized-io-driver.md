// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 83.11: Centralized I/O driver — architectural pivot (Tokio paradigm)

> **Создан:** 2026-05-27.
> **Статус:** 🟡 Ф.0-Ф.3 ✅ завершены (2026-05-28); Ф.4-Ф.9 pending.
> **Приоритет:** P0 — закрывает целый класс races (Plans 83.10.1-5 root cause).
> **Оценка:** 9-15 dev-days (Opus 4.7 + Thinking ON для design + critical phases).
> **Родитель:** [Plan 83](../83-armed-mn-runtime.md) (umbrella M:N runtime).
> **Supersedes:** Plan 83.10.5 (Tactical Ф.B reroute → этот architectural fix).
> **Worktree:** новый `D:\Sources\nv-lang\nova-p83-11\`, ветка
> `plan-83.11-centralized-driver` from `github/main`.

---

## 0. Model / effort / thinking

| Фаза | Модель | Effort | Thinking | Обоснование |
|---|---|---|---|---|
| Ф.0 setup worktree | Sonnet 4.6 | High | OFF | Mechanical |
| **Ф.1 Architecture design** | **Opus 4.7** | **High** | **ON** | Driver thread topology + data structures — high-stakes choices |
| **Ф.2 Driver scaffolding** | **Opus 4.7** | **High** | **ON** | Thread lifecycle + shared queues + cross-thread safety |
| **Ф.3 Migrate Time.sleep** | **Opus 4.7** | **High** | **ON** | Primary use-case, must close [M-83.10.4-iso-cancel-startup-race] |
| **Ф.4 Migrate Blocking effect** | Sonnet 4.6 | High | ON | Pattern established в Ф.3, apply |
| **Ф.5 Migrate channels/sync primitives** | Sonnet 4.6 | High | ON | Multiple call sites, follow Ф.3 pattern |
| **Ф.6 Worker loop minimization** | **Opus 4.7** | **High** | **ON** | Remove per-worker UV loop OR keep minimal (decision in Ф.1) |
| Ф.7 Full suite + stress verification | Sonnet 4.6 | High | OFF | Mechanical run |
| Ф.8 Perf bench vs baseline | Sonnet 4.6 | High | OFF | Run benches, compare |
| **Ф.9 Closure (docs sync + D-block + spec)** | **Opus 4.7** | **High** | **ON** | Spec для cancel state-machine, D-block |

**Inherits feedback rules:** все из 83.10.5 §0.

---

## 1. Context — почему architectural pivot

### 1.1 Pattern recognition: 5 planов на тот же area

После Plan 83 (M:N runtime baseline) последовательно потребовались:

| План | Что фиксил | Тип fix |
|------|-----------|---------|
| 83.10.1 | AUTOARM=0 directive sweep | Tactical (test-side) |
| 83.10.2 | armed cancel-timer-hang (3 баг'а: GC aliasing, slot reuse, race 2a/2b, sched_wake branch) | Tactical (4 distinct fixes) |
| 83.10.3 | nested supervised armed routing — worker pump_scope | Tactical (architectural в одной точке) |
| 83.10.4 | TLS handler snapshot race + fail handler cross-mco + 4 residual flaky | Tactical (5+ distinct fixes) |
| 83.10.5 | iso-cancel startup race | Tactical attempts FAILED → architectural Ф.B required |

**Каждый fix решал реальный bug. Каждый bug был в той же зоне:** cross-thread visibility distributed I/O state (timers, cancel-flags, parked-flags) **across N per-worker UV loops**.

**Это не цепочка случайных багов. Это симптомы архитектурного выбора.**

### 1.2 Чем Tokio multi-thread runtime отличается

```
Nova сейчас:                        Tokio (с 1.0):
─────────────                       ─────────────
main thread                         main thread
  ├ worker0 [UV loop]                 ├ driver thread [ОДНА UV-аналог loop]
  ├ worker1 [UV loop]                 │   ├ all timers
  ├ ...                               │   ├ all I/O events
  └ workerN [UV loop]                 │   └ wake jobs queue
                                      ├ worker0 [local queue, БЕЗ UV loop]
                                      ├ worker1 [local queue]
                                      ├ ...
                                      └ workerN [local queue]
```

**Cross-thread cancel в Nova:** walk N worker scopes, читать `pending_stop_cb[]`,
`parked[]`, `count` через ACQUIRE-loads на N different heap pages. Distributed state
→ inherent race surface.

**Cross-thread cancel в Tokio:** атомарный CAS на task-state (один AtomicUsize per
task). Driver на следующем tick видит CANCELLED bit, не запускает task. Один
mutator I/O state — нет races.

### 1.3 Tokio переписали runtime ровно по этой причине

Ранние Tokio (0.1, 0.2 — 2017-2019) пробовали per-worker reactors. Те же races.
1.0 release (2020) переписан на centralized driver. Это в их changelog и blog posts:
- https://tokio.rs/blog/2020-04-preemption
- https://tokio.rs/blog/2019-10-scheduler

Они **знали урок**. Мы можем взять его без повторения 3 лет patching'а.

### 1.4 Go runtime — distributed, платит cost engineering team'ом

Go runtime: per-P (processor) timer heap + per-P run queue. Те же distributed-state
race возможности. У них **десятки timer-race issues** в open-source истории:
#6239, #14383, #27707, #41686, #50190. Решают через mature engineering team,
decades of TSAN/jcstress-like stress testing.

**У нас команда 1 человек.** Невозможно поддерживать distributed-state runtime
качества Go без на порядок больших ресурсов.

### 1.5 Что выберем

Path A из 83.10.5 final report: **architectural pivot на centralized driver**,
**максимально портируя Tokio design**. Никакого reinvent — Tokio решил эти проблемы,
доказал в production, у нас лицензия (MIT) на port их design. См. §3.0 для
explicit Tokio source mapping.

---

## 2. Цель

Переписать M:N I/O integration: вместо N per-worker UV loops — **одна driver
thread с одним UV loop**, которая владеет всеми timers / blocking work / I/O
event sources. Workers становятся pure compute pumps (deque + local sched_state,
без UV loop ИЛИ с минимальным wake-handle-only loop).

**Эффект:**
- Eliminates ENTIRE class of distributed-state races
- Cancel становится атомарным CAS на single state (driver-side data structure)
- Closes 5 outstanding markers (`[M-83.10.4-iso-cancel-startup-race]`,
  `[M-83.10.4-sleep-bench-scale-cliff]`, `[M-83.10.4-env-parser-30-line-limit]`,
  и preventively — future-race-Ns которые ещё не возникли)
- Plan 83.10.5 PARTIAL → SUPERSEDED (можно закрыть)

---

## 3. Стратегия

### 3.0 Tokio reuse map — **порт, не reinvent**

Каждый компонент Plan 83.11 mapped на existing Tokio source. Принцип: **port
verbatim** где можем, адаптируем только libuv-specifics (Tokio uses mio для I/O,
свой timer wheel; мы reuse libuv для обоих). Эта таблица — обязательный референс
для всех Opus-phases (Ф.1, Ф.2, Ф.3, Ф.6, Ф.9):

| Nova компонент | Tokio source | Стратегия порта |
|----------------|--------------|-----------------|
| `_nova_driver_thread` + lifecycle | `tokio/src/runtime/driver.rs` (struct `Driver`, `Handle`) | Port structure verbatim. Замена `IoDriver`+`TimeDriver` → `uv_loop_t` driver. |
| `_nova_driver_main` loop | `tokio/src/runtime/driver.rs::Driver::park` + `park_timeout` | Port loop logic. Заменить mio poll → `uv_run(loop, UV_RUN_ONCE)`. |
| Job queue (worker→driver MPSC) | `tokio/src/runtime/io/driver.rs` (waker dispatch) + `tokio/src/util/idle_notified_set.rs` | Reuse existing Nova MPSC pattern (`wake_pending` в worker scope) — same shape. |
| `NovaSleepState.packed_state` (AtomicU64 state machine) | `tokio/src/runtime/time/entry.rs` (`StateCell` AtomicU64) | **Port state encoding verbatim**: STATE_DEREGISTERED = u64::MAX, STATE_PENDING_FIRE = u64::MAX - 1, valid expiration_ts otherwise. Adapt naming. |
| State transitions (NEW→ARMED→FIRING/CANCEL_REQ→CLOSED) | `tokio/src/runtime/time/entry.rs::TimerEntry::reset/cancel/poll` | Port CAS logic. `compare_exchange_weak(AcqRel/Acquire)` — same orderings. |
| Park-predicate register-then-recheck | `tokio/src/sync/notify.rs::Notified::poll` (state load → optimistic CAS → fail-path: register waker → recheck under lock) | Port pattern. У нас «under lock» = «на driver-thread» (single mutator). |
| `nova_worker_wake_fiber()` cross-thread dispatch | `tokio/src/runtime/scheduler/multi_thread/worker.rs::Shared::schedule_local/schedule_remote` | Port dispatch pattern: prefer local LIFO slot, fallback worker's deque, fallback global injector. |
| Task state machine для cancel | `tokio/src/runtime/task/state.rs` (single AtomicUsize, packed bits) | Port concept: NovaSpawnCtxBase.state = single AtomicUsize. Bits: RUNNING/CANCELLED/COMPLETE/NOTIFIED + refcount. |
| Worker idle wake mechanism (Variant A) | `tokio/src/runtime/scheduler/multi_thread/park.rs` | Port `Parker`/`Unparker` pattern. У нас: `uv_async_t` per worker = parker. Driver sends async = unparker. |
| Worker idle (Variant B if Ф.1 chooses) | `tokio/src/loom/std/parking_lot.rs` (Parker on Mutex+Condvar) | Port std::sync::Condvar pattern. |
| Driver shutdown coordination | `tokio/src/runtime/driver.rs::Cfg::build` + `Driver::shutdown` | Port shutdown sequence: stop signal, drain inflight, join thread. |
| Spec D-block для cancel guarantees | Tokio docs «Cancellation guarantees» (rustdocs `tokio::task::JoinHandle::abort`) | Adapt formal wording. Tokio guarantees: "abort signals the task; after abort returns, the task may not have completed yet — it will be cancelled at next .await point." Same for Nova fiber. |

**Anti-pattern: don't reinvent.** Если задача "design X" — сначала grep Tokio
source за аналогом. Только если nothing matches — design from scratch с
обоснованием в commit message.

**WebFetch URLs для актуальных версий (port at start of Ф.1 design):**
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/driver.rs
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/time/entry.rs
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/time/wheel/mod.rs (для будущей optimization если libuv timer-heap не масштабируется)
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/scheduler/multi_thread/worker.rs
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/sync/notify.rs
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/task/state.rs
- https://raw.githubusercontent.com/tokio-rs/tokio/master/tokio/src/runtime/blocking/pool.rs

### 3.1 Архитектурные варианты (decision в Ф.1)

**Variant A — minimal worker UV loop:**
- Driver thread owns ALL timers / blocking after_cb / I/O events
- Each worker thread keeps tiny UV loop с ОДНИМ `uv_async_t` wake handle
- Worker sleeps на own loop's `uv_run(ONCE)` когда deque empty; driver writes async
  to wake
- **Pros:** minimal refactor, reuse existing UV-based wake mechanism
- **Cons:** still pays per-worker UV loop init cost + thread setup

**Variant B — workers без UV loop (condvar/event):**
- Workers used pthread/Windows event for wake
- No UV at all on workers
- **Pros:** lower per-worker overhead, simpler topology
- **Cons:** larger refactor, two wake mechanisms (UV для driver, condvar для workers)

**Decision in Ф.1 design phase** based on bench numbers и code-size estimate.
Default lean toward **Variant A** (less risky).

### 3.2 Phases

#### Ф.0 — Setup worktree (≤0.5 hours, Sonnet)

Аналог 83.10.5 Ф.0. Worktree `nova-p83-11`, libuv copy, build nova-cli.

#### Ф.1 — Architecture design (1 day, **Opus + Thinking ON**)

Deliverable: `docs/plans/83.11-design.md` covering:

1. **Driver thread lifecycle**
   - Init: `nova_driver_init()` called в `nova_runtime_init` (after worker pool
     materialization)
   - Main loop: `for(;;) { uv_run(driver_loop, ONCE); drain_jobs(); check_stop(); }`
   - Shutdown: `nova_driver_stop()` + `uv_async_send(driver_stop_handle)` +
     `uv_thread_join`

2. **Job submission API (worker → driver)**
   ```c
   typedef enum {
     NOVA_DRV_ARM_SLEEP,
     NOVA_DRV_ARM_BLOCKING,
     NOVA_DRV_CANCEL_SCOPE,
     NOVA_DRV_TIMER_RESET,
     // ...
   } NovaDriverJobKind;

   typedef struct {
     NovaDriverJobKind kind;
     union {
       struct { NovaSleepState* st; uint64_t ms; } arm_sleep;
       struct { struct NovaFiberQueue* scope; } cancel_scope;
       // ...
     };
   } NovaDriverJob;

   int nova_driver_submit_job(NovaDriverJob* job);  // MPSC queue + async_send
   ```

3. **Wake API (driver → worker)**
   - Driver knows fiber's home worker via NovaSleepState.home_worker_id
   - Wake: push to worker's wake_pending + async_send (same as today's cross-thread
     dispatch_ready)

4. **Per-scope ArmedSleepList**
   - Driver-side data structure: `NovaFiberQueue.armed_sleeps_head` (linked list of
     NovaSleepState*)
   - ONLY driver thread mutates → no concurrency needed
   - Insert: at driver's arm_sleep job handler
   - Remove: at driver's close_cb handler OR cancel_scope handler

5. **Cancel semantics post-pivot**
   - `tok.cancel()` on worker submits `NOVA_DRV_CANCEL_SCOPE` job
   - Driver walks `scope.armed_sleeps_head` list, для каждого `NovaSleepState*` —
     `uv_close` (same thread = no race) + wake target fiber
   - Single mutator → no race surface

6. **Worker idle behavior**
   - Variant A: worker's tiny UV loop blocks в `uv_run(ONCE)` on its own
     `uv_async_t`. Driver sends async on wake.
   - Variant B: worker blocks on `pthread_cond_wait` / Windows event. Driver
     signals condvar.

7. **Performance contention analysis**
   - Driver as single mutator может стать bottleneck
   - Mitigation: batch dispatch (driver drains job queue once per uv_run iteration);
     home_worker_id hint cache; lock-free MPSC queue (already exists в `wake_pending`
     pattern)

8. **Spec D-block update plan**
   - D-NNN: cancel state-machine semantics (driver-owned, single-thread mutator)
   - D-NNN+1: I/O subsystem architecture (centralized driver vs per-worker)
   - D-NNN+2: ε-bounded cancel latency formal guarantees

**Acceptance Ф.1:** design doc reviewed (self-review + memory verification), variant
chosen, no architectural ambiguity for Ф.2 implementation.

#### Ф.2 — Driver scaffolding (1 day, **Opus + Thinking ON**)

Implement skeleton, no logic migration yet:

1. `compiler-codegen/nova_rt/driver.h` + `driver.c` — new file
2. `_nova_driver_thread`, `_nova_driver_loop`, `_nova_driver_job_queue` globals
3. `nova_driver_init()`, `nova_driver_shutdown()` — wired in `nova_runtime_init/shutdown`
4. `nova_driver_main()` — main loop, drains job queue + uv_run
5. `nova_driver_submit_job()` API (returns immediately, async to driver)
6. Driver runs but does nothing useful yet (all jobs stubbed)
7. **Existing per-worker UV loops still active** (parallel architecture, gradual migration)

**Tests Ф.2 (smoke):**
- `nova test` (full suite) passes — no regression from scaffolding
- driver init/shutdown smoke (start, stop, check thread joined)

**Acceptance Ф.2:** scaffolding compiles, driver thread starts+stops cleanly, no
test regression.

#### Ф.3 — Migrate Time.sleep (2 days, **Opus + Thinking ON**)

**Core migration.** Closes `[M-83.10.4-iso-cancel-startup-race]`.

1. `_nova_sleep_via_libuv` → `_nova_sleep_via_driver`:
   ```c
   static inline void _nova_sleep_via_driver(NovaFiberQueue* scope, int slot, nova_int ms) {
       NovaFiberQueue* cancel_scope = /* derive parent scope */;
       if (cancel_scope->cancel_requested) return;  // Race 2a (still useful — early skip)

       NovaSleepState st = {
           .scope = scope, .slot = slot,
           .home_worker_id = _current_worker_id,
           .cancel_scope = cancel_scope,
       };
       nova_aint_init(&st.packed_state, NOVA_SLEEP_NEW);

       // Submit arm job to driver. Driver creates uv_timer_t on driver's loop,
       // adds to scope.armed_sleeps_head list, CAS packed_state NEW→ARMED.
       NovaDriverJob job = { .kind = NOVA_DRV_ARM_SLEEP, .arm_sleep = { &st, (uint64_t)ms } };
       nova_driver_submit_job(&job);

       // Park-until pattern unchanged — drive sends wake when timer/cancel fires.
       nova_sched_park_until(scope, slot, _nova_sleep_state_is_done, &st);
   }
   ```

2. Driver-side timer arm handler:
   ```c
   static void _nova_driver_handle_arm_sleep(NovaSleepState* st, uint64_t ms) {
       // Single-thread mutator — no atomic CAS needed for state транзишен NEW→ARMED.
       // But other threads (worker park-predicate) ACQUIRE-load packed_state.
       uv_timer_init(&_nova_driver_loop, &st->timer);
       st->timer.data = st;
       uv_timer_start(&st->timer, _nova_driver_sleep_timer_cb, ms, 0);

       // Add to scope's armed list (driver-thread only).
       st->next_in_scope = st->cancel_scope->armed_sleeps_head;
       st->cancel_scope->armed_sleeps_head = st;

       nova_aint_store(&st->packed_state, NOVA_SLEEP_ARMED, __ATOMIC_RELEASE);

       // Wake target worker if it might be waiting on park-predicate already
       // (Race: worker submitted arm job, called park_until, predicate returned
       // false на NEW → parked. Now we ARMED → predicate would return false still,
       // worker sleeps until timer or cancel.)
       // No wake needed here — worker is parked, will be woken by timer_cb или cancel.
   }
   ```

3. Driver-side timer_cb (natural fire):
   ```c
   static void _nova_driver_sleep_timer_cb(uv_timer_t* h) {
       NovaSleepState* st = (NovaSleepState*)h->data;
       // Single-thread mutator. CAS ARMED→FIRING (loser = cancel already won).
       int32_t expected = NOVA_SLEEP_ARMED;
       if (!nova_aint_cas(&st->packed_state, &expected, NOVA_SLEEP_FIRING)) return;
       _nova_driver_close_and_wake(st);
   }
   ```

4. Driver-side cancel job handler:
   ```c
   static void _nova_driver_handle_cancel_scope(NovaFiberQueue* scope) {
       // Walk scope's armed list (driver-thread only — no concurrent mutation).
       for (NovaSleepState* st = scope->armed_sleeps_head; st; st = st->next_in_scope) {
           int32_t expected = NOVA_SLEEP_ARMED;
           if (nova_aint_cas(&st->packed_state, &expected, NOVA_SLEEP_CANCEL_REQ)) {
               _nova_driver_close_and_wake(st);  // close timer + wake worker
           }
           // CAS loser = timer fired naturally first, already closed.
       }
   }

   static void _nova_driver_close_and_wake(NovaSleepState* st) {
       uv_close((uv_handle_t*)&st->timer, _nova_driver_sleep_close_cb);
   }

   static void _nova_driver_sleep_close_cb(uv_handle_t* h) {
       NovaSleepState* st = (NovaSleepState*)h->data;
       nova_aint_store(&st->packed_state, NOVA_SLEEP_CLOSED, __ATOMIC_RELEASE);

       // Remove from armed list.
       _nova_driver_remove_from_armed_list(st);

       // Wake target worker fiber via cross-thread dispatch.
       nova_worker_wake_fiber(st->home_worker_id, st->scope, st->slot);
   }
   ```

5. Worker park-predicate:
   ```c
   static nova_bool _nova_sleep_state_is_done(void* ctx) {
       NovaSleepState* st = (NovaSleepState*)ctx;
       int32_t s = nova_aint_load(&st->packed_state, __ATOMIC_ACQUIRE);
       return s == NOVA_SLEEP_CLOSED;
   }
   ```

6. Remove old paths:
   - `_nova_sleep_via_libuv` deprecated (keep как bootstrap-only path для compat)
   - `_nova_sleep_timer_cb`, `_nova_sleep_stop_cb`, `_nova_sleep_close_cb` (worker-side)
     removed
   - `nova_runtime_cancel_worker_fibers` removed (replaced by driver-side cancel)
   - `pending_stop_cb[]`, `pending_handle[]` arrays в sched_state could be removed
     (если ONLY sleep used them — but Ф.4/Ф.5 needs to migrate other users first)

**Tests Ф.3:** см. секция 8 Tests.

**Acceptance Ф.3:**
- `stress_iso_3e` 100/100 PASS under loop
- `supervised_cancel_stress_test` 3 теста revived и 100/100 PASS each
- `cancel_latency_bench` все 3 tests PASS solo + stress 10×
- Concurrency suite: no regression vs baseline (71 PASS / 5 fail flaky jobs=4)
- Diagnostic counters removed/ifdef'd

#### Ф.4 — Migrate Blocking effect (1 day, Sonnet + Thinking ON)

`_nova_blocking_via_threadpool` уже uses libuv worker pool. Currently submits via
worker's UV loop. Repoint to driver's loop.

**Acceptance:** Plan 83.3 tests pass.

#### Ф.5 — Migrate channels / sync primitives (1.5 day, Sonnet + Thinking ON)

Audit park-points outside sleep:
- Channels (`nova_chan_send_blocking`, `nova_chan_recv_blocking`)
- Mutex/RwLock (Plan 103.3)
- Once/CountDownLatch/Semaphore/Barrier/Condvar (Plans 103.4-5)

Most of these don't use UV directly — they use `nova_sched_register_pending` for
cancel-wake. Pattern: cancel reads `pending_stop_cb[]`. Same three-load race
applies.

Migration: replace `pending_stop_cb[]` mechanism with driver-routed cancel. Each
primitive registers a `NovaCancelHook*` with driver via job. Driver walks hooks
on scope cancel.

OR keep sched_state's `pending_stop_cb[]` для bootstrap path, route armed-M:N
cancels through driver only. Cleaner: full migration.

**Acceptance:** Plan 103.x tests pass solo + stress 10×.

#### Ф.6 — Worker loop minimization (1 day, **Opus + Thinking ON**)

Decision per Ф.1 design:

**Variant A:** worker keeps tiny UV loop с одним `uv_async_t` wake handle.
Trivial — already exists (worker's `wake_handle`). Just don't init other handles.

**Variant B:** worker uses pthread cond + mutex или Windows event for sleep/wake.
Replace `uv_run(ONCE)` с `pthread_cond_wait`. Larger refactor.

Per Ф.1 recommendation. Default **Variant A**.

**Acceptance:** workers idle на минимально UV state, no leaked handles, shutdown clean.

#### Ф.7 — Full suite + stress verification (1 day, Sonnet OFF)

```bash
# Concurrency stress 100×
for i in {1..100}; do nova test nova_tests/concurrency/; done | analyze
# Full suite
nova test --jobs 4
# Critical fixtures isolation
for t in stress_iso_3e supervised_cancel_stress_test cancel_latency_bench \
         supervised_cancel_test fail_handler_cross_mco_armed; do
    for i in {1..50}; do nova test nova_tests/concurrency/$t.nv; done
done
```

**Acceptance Ф.7:**
- Concurrency suite stress: ALL 100/100 critical tests, ≥99/100 important tier
- Full nova test: no regression
- 0 new TIMEOUT/FAIL types

#### Ф.8 — Perf bench (1 day, Sonnet OFF)

Bench перф deltas vs baseline (Plan 83.10.4 merge):

| Bench | Baseline | Target |
|-------|----------|--------|
| `sleep_bench` 5k concurrent sleeps | ~500ms | ≤500ms ± 10% |
| `sleep_bench` 100k yields | ~1500ms | ≤1500ms ± 10% |
| `cancel_latency_bench` test 1 wake latency | ~50ms p99 | ≤30ms p99 (better!) |
| `parallel_for` 1M iterations | TBD | ± 10% |
| `gc_bench` allocator throughput | TBD | ± 5% (driver не должен влиять) |

**Acceptance Ф.8:** все benchmarks в budget ±10%. Если cancel_latency lучше — note as improvement.

#### Ф.9 — Closure (1 day, **Opus + Thinking ON**)

1. Spec D-block для cancel state-machine + I/O architecture (3 new D-blocks)
2. Memory file: `project-plan83_11-status.md` + index
3. project-creation.txt: Plan 83.11 entry
4. simplifications.md: close 5 markers
5. nova-private discussion-log: architectural pivot narrative + lessons learned
6. Plan 83.10.5 status → SUPERSEDED
7. Merge branch → main (with conflict resolution per established pattern)
8. Cleanup worktree

**Acceptance Ф.9:** merge complete, all docs synced, markers closed, worktree cleaned.

---

## 4. Acceptance criteria (overall)

- ✅ 0 plans «fix M:N race» в следующие 6 месяцев (track via marker file)
- ✅ Все 5 outstanding markers closed:
  - `[M-83.10.4-iso-cancel-startup-race]` ✅
  - `[M-83.10.4-sleep-bench-scale-cliff]` ✅ (10k sleeps should now work)
  - `[M-83.10.4-env-parser-30-line-limit]` ✅ (parser limit unrelated — can fix here)
  - `[M-83.10.1-armed-cancel-timer-hang]` — verify closed
  - `[M-83.10.1-per-fiber-handler-tls-race]` — already closed Plan 83.10.4
- ✅ Concurrency suite stress 100×: critical tier 100/100, important ≥99/100
- ✅ Full nova test concurrency: ≥76 PASS / 0 deterministic FAIL (jobs=4)
- ✅ Plan 103.x sync primitives all pass (no regression)
- ✅ Perf benches ≤10% regression OR improvement
- ✅ Spec D-blocks для cancel state-machine + driver architecture написаны
- ✅ Plan 83.10.5 status → SUPERSEDED, marker → ✅ closed by Plan 83.11

## 4.1 Canonical regression test для STALE-slot race (Plan 83.11 Ф.3)

**Файл:** `nova_tests/concurrency/stress_iso_3e.nv` (в main репо)

```nova
test "99+1 fibers sleep(10000), cancel(30ms) — bench clone" {
    ro tok = CancelToken.new()
    ro t0 = Time.now()
    supervised(cancel: tok) {
        for _ in 0..99 {
            spawn { Time.sleep(10_000) }   // 99 fiber'ов спят 10 секунд
        }
        spawn {
            Time.sleep(30)
            tok.cancel()                   // через 30ms — отменить всех
        }
    }
    ro elapsed = Time.now() - t0
    assert(elapsed - 30 < 300)            // весь блок должен выйти за ~30ms, не зависнуть
}
```

**Что проверяет:** при cancel() 99 sleeping fiber'ов supervisor должен
выйти за ~30ms. Если STALE-slot race присутствует — supervisor зависает
навсегда (TIMEOUT > 30s).

**Как запускать для регрессионной проверки:**

```bash
# Минимальная проверка (targeted):
nova test nova_tests/concurrency/stress_iso_3e.nv

# Stress проверка (30 повторений — использовалась при фиксе):
for i in $(seq 1 30); do
    nova test nova_tests/concurrency/stress_iso_3e.nv || echo "FAIL at $i"; done

# Acceptance criteria (из плана — 100 повторений):
for i in $(seq 1 100); do
    nova test nova_tests/concurrency/stress_iso_3e.nv || echo "FAIL at $i"; done
```

**Метрики фикса:**
- До фикса (STALE-slot race): 3–5 PASS из 30 запусков (~10–17%)
- После фикса (Fix A+B+C): 30/30 PASS (100%)
- Платформа проверки: Windows 11, 16 CPU, clang, NOVA_MAXPROCS=auto

**Примечание:** тест чувствителен к числу CPU. На 1 ядре (NOVA_MAXPROCS=1)
race не воспроизводится — нет реального параллелизма между alloc_slot и close_cb.
Для полной проверки нужно ≥2 CPU без ограничений MAXPROCS.
- ✅ Architectural rationale documented в spec/decisions/

---

## 5. Risk register

| Риск | Вероятность | Severity | Mitigation |
|------|-------------|----------|------------|
| Driver bottleneck при I/O-heavy workload | Средняя | Средняя | Bench Ф.8 catches. Mitigation: batch dispatch, per-worker hint cache |
| 9 dev-day оценка слишком оптимистичная (real 15+) | Высокая | Низкая | Acceptable — это разовая инвестиция |
| Регрессии в Plan 103.x sync primitives | Средняя | Высокая | Полный re-test обязателен в Ф.5+Ф.7 |
| Merge conflicts с параллельной работой над runtime | Высокая | Средняя | Coordinate via project status; merge раньше чем накопится |
| Driver thread crash undetected | Низкая | Критическая | Heartbeat + sysmon thread monitors driver; abort if hung |
| Bench perf regression > 10% | Низкая | Средняя | Document, decide: accept OR revisit Variant B |
| Spec D-block design проблемы выявят retrospective | Низкая | Низкая | Ф.1 design phase должен поймать; iterative refinement OK |

---

## 6. References

- **Tokio source (compare 2026-05-27):**
  - `tokio/src/runtime/scheduler/multi_thread/` — worker pool, scheduling
  - `tokio/src/runtime/driver.rs` — centralized driver design
  - `tokio/src/runtime/time/wheel.rs` — timer wheel implementation
  - Blog: «Making the Tokio scheduler 10x faster» (2019)
  - Blog: «Reducing tail latencies with automatic cooperative task yielding» (2020)
- **Plan 83.10.5** — predecessor (Tactical attempt failed)
- **Plan 83.10.4 audit** — historical race classification
- **Memory `project-plan83_10_5-status`** — diagnostic findings session report
- **D-blocks (existing):** D50, D71, D92, D93 — supervised + cancel + park/wake

---

## 7. Out of scope

- Migrating I/O subsystem на mio (Rust crate) — keep libuv. Pivot only об centralized vs distributed.
- Removing M:N entirely → single-threaded runtime (rejected per Plan 83 baseline).
- Bytecode-level `select` primitive (Go style) — отдельный план если нужно.
- Compile-time cancel-safety analysis — отдельный план (Plan 03.5, P2).
- Typed cancel reasons UX polish — отдельный план (Plan 49.X, P2).
- TSAN/Loom integration — отдельный план (Plan 83.10.7 stress runner).

---

## 8. Tests catalog (positive + negative)

### 8.1 Positive tests — must PASS 100/100 under `nova test --stress 100`

| # | Fixture | Scenario | Tier |
|---|---------|----------|------|
| P1 | `stress_iso_3e` (revived) | 99+1 fibers sleep(10000), cancel(30ms) | **critical** |
| P2 | `supervised_cancel_stress_test` test 1 | 100 parked sleeps + cancel | **critical** |
| P3 | `supervised_cancel_stress_test` test 2 | 50 mixed-duration sleeps + cancel | **critical** |
| P4 | `supervised_cancel_stress_test` test 3 | 3×(100+1) sequential scopes + cancel each | **critical** |
| P5 | `cancel_latency_bench` test 1 | wake latency ≤30ms p99 | important |
| P6 | `cancel_latency_bench` test 2 | warmed workers cancel timing | important |
| P7 | `cancel_latency_bench` test 3 | nested cancels cumulative latency | important |
| P8 | NEW `cancel_during_natural_fire` | some timers fire naturally, cancel mid-flight | **critical** |
| P9 | NEW `nested_supervised_3_levels_cancel` | 3-level nested scopes, cancel outer | **critical** |
| P10 | NEW `cancel_cascade_linked_3` | linked tokens A→B→C, cancel A cascades | important |
| P11 | NEW `10k_fibers_sleep_cancel` | production-scale (closes sleep_bench cliff) | **critical** |
| P12 | NEW `cancel_zero_fibers_in_scope` | no-op edge case | important |
| P13 | NEW `cancel_during_drain_exit` | cancel fires при scope-drain exit pattern | important |
| P14 | NEW `cancel_from_sigint_handler` | Ctrl+C → scope cancel | important |
| P15 | NEW `cancel_during_blocking_work` | Plan 83.3 blocking{} + cancel | important |
| P16 | NEW `channel_recv_cancel` | parked recv() + cancel | important |
| P17 | NEW `mutex_lock_for_cancel` | bounded-wait lock + cancel | important |
| P18 | NEW `condvar_wait_cancel` | wait() + cancel | important |
| P19 | NEW `1000_cancel_storm` | 1000 cancels/sec, no race or leak | optional |
| P20 | NEW `driver_thread_idle_no_cpu` | idle period, driver doesn't spin | important |

### 8.2 Negative tests — must PANIC/error correctly, NOT hang/UAF/SEGV

| # | Fixture | Expected behavior |
|---|---------|-------------------|
| N1 | `cancel_double_call` | idempotent, second cancel no-op |
| N2 | `cancel_unbound_token` | no-op, не panic |
| N3 | `cancel_from_non_fiber` | EXPECT_RUNTIME_PANIC "cancel from non-fiber context" |
| N4 | `cancel_runtime_not_init` | no-op (cancel before runtime arm) |
| N5 | NEW `timer_fire_during_cancel` | race natural-vs-cancel — idempotent CAS, не UAF |
| N6 | NEW `close_cb_after_fiber_dead` | no-op, не UAF на freed coroutine stack |
| N7 | NEW `cancel_with_corrupted_token` (fuzz) | graceful panic, не SEGV |
| N8 | NEW `driver_thread_blocked_in_sleep` | sysmon detects, abort с clear message |
| N9 | NEW `scope_freed_during_cancel` | no UAF on armed list (driver detects + skips) |
| N10 | NEW `cancel_cycle_linked_tokens` | A→B→A cycle, must terminate (cycle detection) |
| N11 | NEW `cancel_with_NULL_reason_ptr` | no-op для NULL reason, не SEGV |
| N12 | NEW `cancel_during_runtime_shutdown` | graceful, drain in-flight before shutdown |

### 8.3 Bench tests (Ф.8 perf verification)

| # | Bench | Baseline | Pass criterion |
|---|-------|----------|----------------|
| B1 | `sleep_bench` 5k sleeps | ~500ms | ≤550ms (10% slack) |
| B2 | `sleep_bench` 100k yields | ~1500ms | ≤1650ms |
| B3 | `sleep_bench` 10k sleeps (new) | TBD (was cliff) | ≤2000ms |
| B4 | `cancel_latency_bench` test 1 | ~50ms p99 | ≤30ms p99 (improvement target) |
| B5 | `parallel_for_bench` 1M ops | TBD | ± 10% baseline |
| B6 | `gc_bench` allocator | TBD | ± 5% (orthogonal) |
| B7 | NEW `driver_throughput_bench` | timer enroll/fire rate | ≥100k timers/sec sustained |
| B8 | NEW `driver_idle_cpu` | idle period CPU % | ≤1% on idle 10s window |

---

## 9. Mechanical instructions (for safe Sonnet phases)

> Detail level аналог Plan 83.10.5 §8. Each Sonnet-executable phase должен иметь
> exact commands + patterns + acceptance criteria. Будет добавлено в Ф.1 design
> phase или separately после design freeze. Critical phases (Ф.1/Ф.2/Ф.3/Ф.6/Ф.9)
> **не** Sonnet-executable — Opus required.

---

## 10. Post-mortem: Ф.3 race investigation (2026-05-27–28)

> **Статус Ф.3:** ✅ ЗАКРЫТА. 30/30 stress PASS. Commit `dd7a4f00bc5`.
>
> Этот раздел документирует ~10 часов / 6 сессий диагностики и исправлений
> одного race condition в `supervised { spawn { sleep(N) } }`.
> Сохранено как обучающий материал для будущих M:N races.

### 10.1 Симптом

```
supervised {
    for _ in 0..100 {
        spawn { sleep(10000) }
    }
    tok.cancel()
}
// → зависает навсегда (без таймаута)
```

`cancel()` вызывается, все timers закрываются (`close_cb` срабатывает), но
`nova_supervised_run_impl` ждёт `pending_remote == 0` вечно.
`cancel_latency_bench` → TIMEOUT >30s. `stress_iso_3e` → 3-5/30 PASS.

### 10.2 Ложные пути (что НЕ сработало)

#### Attempt 1: wake-before-park — unconditional dispatch (Sessions 1-2)

**Гипотеза:** `close_cb` срабатывает ДО того, как fiber успел войти в `nova_sched_park`.
CAS `parked: false → false` fails → wake потерян.

**Fix:** в `close_cb` dispatch fiber unconditionally (без CAS на `parked`).

**Результат:** 1/30 PASS. Вероятная причина — double-dispatch: два wake
на один fiber → double-resume → UB/crash в mco.

**Вывод:** unconditional dispatch без защиты не работает.

#### Attempt 2: Generational futex (Session 2)

**Гипотеза:** нужна версия wake'а — `wake_gen` counter. Fiber проверяет
счётчик после yield, если вырос — wake состоялся (Linux futex idiom).

**Fix:** `NovaSchedState.wake_gen[]` + pre/post checks в `nova_sched_park`.

**Результат:** 1/30 PASS. `dispatch_ready` иерархия имела несколько race
windows сама по себе. wake_gen не помог, так как проблема раньше в стеке.

#### Attempt 3: Simple futex + SEQ_CST stage (Session 3)

**Гипотеза:** добавить `wait_state` per-sleep atomic — NEW/PARKED/WOKEN.
Park CAS NEW→PARKED, wake CAS PARKED→WOKEN, предикат проверяет WOKEN.

**Fix:** `NovaSleepState.wait_state` field (standalone atomic).

**Результат:** 1-19/30 PASS (system-load dependent). SEGFAULT при tight
coupling с fiber arena. Heisenbug: `fprintf` (memory fence) поднимал до
1/1 PASS — классический memory ordering heisenbug.

#### Attempt 4: Single-atomic pure Tokio AtomicWaker port (Session 4)

**Гипотеза:** полностью заменить `parked[]`-based mechanism на
`NovaSleepWaitState` (единый AtomicUsize, packed bits).

**Fix:** remove `parked[]` usage в sleep paths, один CAS per state.

**Результат:** SEGFAULT при arena deallocation + 3/30 PASS в hybrid mode.
Fiber arena жёстко завязана на `parked[]` в `nova_scope_free_slot`.
Полная замена требует переписать arena — ~5 dev-days.

#### Attempt 5: Option A v3 — pending_wake[] integration (Session 5)

**Гипотеза (REVISED):** `pending_remote` leak в spawn-entry epilogue.
7 fibers исчезли из всех state ёмкостей, но `pending_remote` не декрементирован.

**Fix:** `NovaSchedState.pending_wake[]` counter. Park: pre-check CAS 1→0
+ SEQ_CST parked + post-barrier recheck. Wake: CAS pending_wake 0→1 first.

**Результат:** 10/30 PASS (best partial) — но wrong target. `pending_remote`
leak был diagnostic artifact (counters не atomically paired), не реальная
утечка. Сам race был в другом месте.

### 10.3 Финальное исправление (Fix A+B+C)

**Алгоритм который помог локализовать проблему:**

1. Programmatic state-dump при watchdog timeout (`nova_runtime_dump_state`):
   lock-free best-effort snapshot всех workers + sched_state.
2. Атомарные счётчики на каждом шаге wake-delivery chain:
   `close_cb` → `wake_called` → `cas_won` → `sleep_ret`.
3. Dump показал: `close_cb=100, wake=100, cas_won=93` + `fibers[slot]=NULL`.
   **NULL slot при parked=true** — fiber уже вышла, slot переиспользован.

**Root cause — STALE slot condition:**

```
Timeline:
  t1: fiber A занимает slot=5, uv_timer_start
  t2: scope расширяется, slot 5 appears free (parked[5]=true, fibers[5]=NULL)
      → fiber B ЗАНИМАЕТ slot=5 (nova_scope_alloc_slot видит NULL)
  t3: close_cb срабатывает для fiber A
      actual_co = scope->fibers[5] = B (не A!)
      → WRONG-FIBER → wake пропущен → fiber A не просыпается → hang
```

`fibers[slot]=NULL` потому что fiber после `mco_yield` в `nova_sched_park`
не сразу restore'ит `fibers[slot]` — делает это позже в epilogue.
В этот gap `nova_scope_alloc_slot` видит NULL и считает slot свободным.

**Fix A** (`nova_scope_free_slot` в `fibers.h`):
```c
// Пропустить слот если fiber ещё в mco_yield (STALE)
if (__atomic_load_n(&st->parked[i], __ATOMIC_ACQUIRE)) continue;
```
Slot с `parked=true` не считается свободным для переиспользования.

**Fix B** (`_nova_driver_sleep_close_cb` в `driver.c`):
```c
if (actual_co != st->expected_co) {
    // WRONG-FIBER: slot занят другим. Диспатчить expected_co напрямую.
    mco_coro* expected_co = st->expected_co;
    if (expected_co && mco_status(expected_co) == MCO_SUSPENDED) {
        // Очистить parked[slot] для expected_co (не actual_co)
        // Установить DISPLACED sentinel
        displaced_ctx->_nova_worker_slot = -2;
        nova_fiber_state_store(expected_co, NOVA_FIBER_STATE_IDLE);
        sc->dispatch_ready(sc->dispatch_ctx, expected_co);
    }
    return;
}
```
При WRONG-FIBER диспатчим `expected_co` (оригинальный fiber) напрямую,
минуя `nova_sched_wake` который смотрит на slot (теперь занят другим fiber'ом).

**Fix C** (spawn epilogue через `_nova_worker_slot=-2`):
```c
// В epilogue: если slot=-2 (DISPLACED), не вызывать free_slot
if (ctx->_nova_worker_slot != -2) {
    nova_scope_free_slot(scope, ctx->_nova_worker_slot);
}
```
DISPLACED fiber при выходе пропускает `free_slot` — slot уже принадлежит
другому fiber'у (fiber B из Fix B сценария).

**Результат:** stress_iso_3e **30/30 PASS**. Concurrency suite **74-75/78 PASS**.

### 10.4 Lessons learned

1. **Heisenbug = memory ordering**: если `fprintf` fix'ит race — это всегда
   memory fence artifact. Не добавляй `fprintf` и считай «исправлено». Это
   маскировка, не fix.

2. **Counter atomicity важна**: если диагностические счётчики не atomically
   paired с операцией которую считают — их данные misleading. Session 5
   потратила ~2 часа на «утечку pending_remote» которой не было.

3. **State-dump > step-debugger** (при отсутствии debugger): programmatic
   watchdog dump — single most effective diagnostic. Показал WRONG-FIBER
   condition сразу. Без него — 5 сессий слепых попыток.

4. **expected_co как identity**: `NovaSleepState.expected_co` — ключевое поле.
   Без него close_cb не может различить «мой fiber» от «чужой fiber в том же
   slot'е». Аналог Tokio task pointer в `TimerEntry`.

5. **Slot → arena coupling**: `fibers[]`, `parked[]`, `free_slot()` — все три
   должны двигаться атомарно. Попытка заменить только `parked[]` (Attempt 4)
   не работает пока `alloc_slot` смотрит на `fibers[i]==NULL`.

6. **10 часов на один race** — нормально для M:N без debugger под Windows.
   Tokio на тот же класс races потратил ~3 года patching'а. Plan 83.11
   закрывает architectural root cause (centralized driver), Ф.3 был
   tactical must-fix до полной миграции.

---

## 11. Followup: GC aliasing bug `[M-83.11-gc-cancel-token-alias]`

> **Обнаружен:** 2026-05-28 при попытке запустить stress_iso_3e с 999+1
> fiber'ами. Проявляется при ≥990 одновременных fiber'ах.
> **Статус:** 🔴 OPEN. 99+1 fiber'ов — ок; 990+ fiber'ов — panic.

### 11.1 Симптом

```
nova: panic: token already bound to a live scope
```

Паника в `nova_cancel_token_bind` при входе в `supervised(cancel: tok)`.
Возникает только при 990+ одновременно спауненных fiber'ах.

### 11.2 Root cause — структурный aliasing

**Цепочка событий:**

1. `tok = CancelToken.new()` → `nova_alloc` → `GC_malloc` → GC-managed memory.
2. Для цикл спаунит ≥990 fiber'ов → `nova_runtime_spawn_into` → `nova_scope_pin_ctx`.
3. `nova_scope_pin_ctx` вызывает `nova_alloc` (GC_malloc) когда `ctx_pins` массив
   переполняется (удваивается: 16→32→…→1024 при ~512 fiber'ах).
4. `GC_malloc` триггерит Boehm GC sweep.
5. На момент sweep'а `tok` находится в регистре (оптимизированный `-O2` build),
   а не на стеке — conservative GC его не находит как root.
6. GC помечает `tok` как unreachable → собирает память → возвращает следующему
   `nova_alloc` вызову.
7. Следующий `nova_alloc_uncollectable` (для `NovaSpawnCtxBase`) получает тот
   же адрес что был у `tok`.
8. `NovaSpawnCtxBase._nova_parent_scope` устанавливается в `&_nova_scope_q_0`
   (ненулевой указатель на supervised scope).
9. Главный поток вызывает `nova_cancel_token_bind(tok, &_nova_scope_q_0)`:
   - `tok` теперь указывает на `NovaSpawnCtxBase`
   - читает `tok->bound_scope` (offset +8 в `NovaCancelToken`)
   - но offset +8 в `NovaSpawnCtxBase` — это `_nova_parent_scope`
   - = `&_nova_scope_q_0` ≠ NULL → **PANIC**

**Структурный overlap (offset +8):**
```c
typedef struct NovaCancelToken {
    nova_atomic_bool cancel_requested;  // bool, offset 0
    // 7 bytes padding
    NovaFiberQueue*  bound_scope;       // offset 8  ← ключевое поле
    ...
} NovaCancelToken;

typedef struct {
    size_t           _nova_pool_size;    // offset 0
    NovaFiberQueue*  _nova_parent_scope; // offset 8  ← aliases bound_scope!
    int              _nova_worker_slot;
    ...
} NovaSpawnCtxBase;
```

### 11.3 Почему 990+?

- `ctx_pins` array удваивается на степенях 2: 16, 32, 64, 128, 256, 512, 1024
- На 512-м fiber'е происходит grow с 512→1024 → вызов `nova_alloc` = GC trigger
- Между 512 и 999 fiber'ами ещё несколько grow'ов — каждый = новый шанс для GC
- 975 fiber'ов — проходит стабильно (GC не успевает собрать `tok`)
- 990+ — паника 3/3 раза (GC триггерится достаточно раз)

### 11.4 Fix

**Option A — pin `tok` в scope.ctx_pins до первого spawn (рекомендуется):**

Изменение в `emit_c.rs`, в codegen supervised-блока с `cancel:` токеном.
Сразу после `nova_scope_init(&_nova_scope_q_0)` и до первого spawn добавить:

```c
// Pin cancel token as GC root through the scope (before spawning fibers)
nova_scope_pin_ctx(&_nova_scope_q_0, (void*)_nova_cancel_tok_0);
```

Это делает `tok` достижимым через `_nova_scope_q_0->ctx_pins[]` (которая на
стеке и всегда является GC root). Токен не будет собран пока жив scope.

**Option B — `nova_alloc_uncollectable` для CancelToken:**

В `fibers.h:nova_cancel_token_new()`:
```c
// Было:
return (NovaCancelToken*)nova_alloc(sizeof(NovaCancelToken));
// Стало:
return (NovaCancelToken*)nova_alloc_uncollectable(sizeof(NovaCancelToken));
```

Семантически верно (токен — caller-owned, живёт пока caller хочет), но
требует явного `nova_free_uncollectable(tok)` при очистке, иначе leak.
Сейчас cleanup `tok` нет → не подходит без добавления `unbind`→free пути.

**Вывод: Option A.** Одна строка в emit_c.rs, без изменений runtime.

### 11.5 Проверка

```bash
# Должно пройти после фикса:
nova test nova_tests/concurrency/stress_iso_3e.nv   # 99+1 — regression guard
# + расширенный вариант:
nova test nova_tests/concurrency/stress_iso_large.nv  # 999+1 — новый стресс тест
```

Создать `stress_iso_large.nv` (999+1 fiber'ов) как дополнительный стресс тест
после подтверждения фикса.

---

## 12. Followup: cancel-token Heisenbug in cancel-with-reason path (2026-05-31, session #7)

> **Status:** 🔴 OPEN. Reproducer minimal, diagnosis classic Heisenbug — instrumentation masks bug. Real fix requires memory-ordering audit, not more printf.

### 12.1 Symptom

After fast-forward merge `plan-83.11-centralized-driver` → `github/main` (184 commits including Plan 73.1 D180, 91.10/91.11 SB+var_boxed, 110.1.4 panic-distinction, 110.4.2 MultiError, 110.3.1 Guards), full `nova test`:

- `concurrency/cancel_semantics_test` — was PASS pre-merge, now **SIGSEGV (5/5 in stress)** with zero output before crash
- `concurrency/cancel_token_generic_smoke` — flaky in stress (passes isolated)
- `plan103_3/rwlock_with_read_with_write_helpers` — 1 sub-test FAIL (panic-via-defer in write block)
- `concurrency/time_handler`, `concurrency/deep_spawn` — flaky/recovered

**Pre-existing on main, NOT caused by Plan 83.11 Ф.3** (verified: same .exe built pre-merge passed all these tests).

### 12.2 Minimal reproducer

```nova
test "min repro" {
    let tok = CancelToken.new()
    supervised(cancel: tok) {
        spawn {
            tok.cancel("hello")  // ← string reason triggers crash
        }
    }
}
```

5/5 SIGSEGV. With `tok.cancel()` (no arg): 5/5 PASS. **Bug specific to cancel-with-string-reason path.**

### 12.3 Heisenbug confirmation (§10.4 Lesson #1 applied)

Added gated `getenv("NOVA_DIAG_CANCEL")` + `fprintf` checkpoints C1-C10 throughout `nova_cancel_token_cancel_reason` (compiler-codegen/nova_rt/fibers.h:791):

- **Result:** crash DISAPPEARED even with `NOVA_DIAG_CANCEL` UNSET (only the conditional getenv branches present in code).
- **Implication:** code layout / register allocation / branch placement perturbation hides the race.
- Per §10.4 Lesson #1: this IS a memory-ordering bug, not logic. Fprintf inserts compiler/CPU fences that mask it.

### 12.4 Diagnostic algorithm applied (per §10.4)

Used in this session:
- ✅ **State-dump > debugger:** no gdb/lldb on Windows, used programmatic stderr prints
- ✅ **Paired counters not relied on:** main+test+cancel checkpoints showed crash IN cancel_token_cancel_reason itself
- ✅ **Minimal repro:** narrowed 9-test suite to 1-line failing pattern
- ✅ **30× minimum verify rule:** verified 5/5 SEGFAULT consistently before declaring bug

**Lesson reinforced (§10.4 #1):** instrumentation as diagnostic stopped here once Heisenbug pattern confirmed. Adding more printf is masking, not diagnosing.

### 12.5 Suspect commits (in 184 since Ф.3 close)

Top candidates touching cancel/throw machinery:
- `fd816788731 Plan 91.11 var_boxed fix` — variable boxing change in spawn-capture path (CancelToken passed by ref to spawn body)
- `06051deaa49 Plan 110.1.4.g NOVA_THROW_PANIC` — added error_kind=PANIC in nv_panic (though merge resolved out of effects.h, may have left codegen-side artifact)
- `2b4a90e5569 Plan 110.4.2 MultiError cycle-safety` — touches NovaErrorChain composition (cancel sets error_suppressed=NULL)
- `e59c9556d2c Plan 103.9 Consume guards` — MutexGuard/Permit Consumable[never] — may touch consume-bound CancelToken

### 12.6 Next session prescription

Apply §10.4 algorithm step-by-step:
1. **Read suspect commits** (above 4) focusing on `nova_cancel_token_cancel_reason`, `nova_throw_cancel_reason`, `_nova_fail_top->error_kind/error_reason_ptr` writes
2. **Audit memory orderings** in `nova_cancel_token_cancel_reason` for the writes:
   - `t->cancel_requested` (already SEQ_CST via nova_abool_store)
   - `t->reason_ptr` (plain write — **suspect**, no acquire/release pair with reader)
   - `t->has_reason` (plain bool)
   - `t->bound_scope->cancel_reason_ptr` (plain write — same suspect)
3. **Hypothesis to test:** `reason_ptr` plain write races with peer-fiber read in `nova_fiber_yield → nova_throw_cancel_reason` reading `bound_scope->cancel_reason_ptr`. Fix: change to `__atomic_store_n RELEASE` + reader `__atomic_load_n ACQUIRE`.
4. **DO NOT** add fprintf as fix — that's masking. Use atomic fence semantics only.
5. **Verify 30× minimum** на cancel_semantics_test + minimal repro + plan103_3 + concurrency/cancel_token_smoke.

### 12.7 Out of scope

- Plan 83.11 Ф.3 fix (STALE-slot race) is unrelated — `git diff` between pre-merge .exe (PASS) and post-merge .exe (SEGFAULT) confirms regression from other plans, not Ф.3.
- Plan 83.11 Ф.4-Ф.9 can proceed independently — Ф.3 verified stable for sleep migration.

### 12.8 Session #7b — first fix attempt RESULT (2026-05-31, REVERTED)

**Attempted fix:** reorder data-before-flag in `nova_cancel_token_cancel_reason`:
- `__atomic_store_n(&t->reason_ptr, reason_ptr, RELAXED)` BEFORE `cancel_requested = true`
- `__atomic_store_n(&t->bound_scope->cancel_reason_ptr, reason_ptr, RELAXED)` BEFORE `bound_scope->cancel_requested = true`
- Same pattern in `nova_cancel_token_bind`

**Result after fix:**
- min repro `supervised + spawn { tok.cancel("hello") }`: 4/30 PASS (was 0/30) — marginal
- cancel_semantics_test: 0/30 PASS (no change)

**Refined symptom narrowing (3 new probes):**

| Probe | Pattern | Result |
|-------|---------|--------|
| `tok.cancel("hello")` on main thread, no supervised | crashes? | **10/10 PASS** ✅ |
| `supervised + spawn { tok.cancel("hello") }` | crashes? | 26/30 SEGFAULT |
| `supervised + spawn { tok.cancel() }` *(no reason)* | crashes? | **10/10 SEGFAULT** ⚠️ |

**REVISED HYPOTHESIS:** Bug is NOT about reason ptr ordering — it's about **supervised+spawn cancel path** broadly. cancel-without-reason ALSO crashes when called from inside spawn. Reason string is RED HERRING.

**Real candidates for next session:**
1. Race in `nova_runtime_spawn_into` → cancel mid-spawn (worker may not have observed spawn yet when cancel arrives)
2. Race in `_nova_cancel_via_driver` — driver thread CANCEL_SCOPE handler reads scope fields uninitialized
3. Race in Boehm GC + uncollectable cancel_token alloc (related `[M-83.11-gc-cancel-token-alias]` §11)
4. Race in `nova_runtime_cancel_worker_fibers` — TLS effect registry under armed M:N (Plan 83.10.4 area)
5. Crash actually before "Running N tests..." print — meaning init-stage; printf to stdout doesn't flush before SIGSEGV. Need direct write(2) or stderr-write probe earlier.

**Fix REVERTED.** Heisenbug-masking gated branches gave false hope. The reorder didn't materially fix the race because race isn't where suspected.

**Lesson reinforced (§10.4 #1+#2):** Validate hypothesis with **paired counters tied to the operation**, not by general "data-before-flag" pattern that doesn't address actual write contention.

**Next session entry point:** narrow further with `supervised { spawn {} }` (empty body) — does empty spawn under supervised cancel? If yes → race in spawn→supervised cleanup; if no → race specifically in spawn calling cancel on controlling token.

### 12.9 Session #7c — second fix attempt + crash address localization (2026-05-31)

**Attempt 2: pin cancel_tok in scope.ctx_pins via emit_c.rs (Option A from §11):**

Implemented documented fix:
```rust
self.line(&format!("nova_scope_pin_ctx(&{}, (void*){});", queue_var, tv));
```

Generated correctly into .c, but stress 30× still 0/30 PASS on cancel_semantics_test, 1/30 on probe 4. Confirmed: bug is NOT `[M-83.11-gc-cancel-token-alias]` from §11 (that one panics with "token already bound", manifests at 990+ fibers). This is a DIFFERENT race manifesting at 1 fiber.

**Attempt 2 REVERTED.**

**Crash address localization via SIGSEGV VEH:**

Added Windows `AddVectoredExceptionHandler` activated by `NOVA_DIAG_SEGV=1`. Walks stack with `StackWalk64` + dbghelp.lib.

Captured 5 deterministic runs:
```
[SEGV] code=0xc0000005 (EXCEPTION_ACCESS_VIOLATION)
RIP=0x7ff65d8c688f  (consistent across runs)
RSP=variable
RBP=0x7ff65d8b0000 (exe base)
mode=1 (WRITE access)
access_addr=0x7ff65d975547  (~RVA 0xc5547)
```

PE sections of test binary:
- `.text`: RVA 0x1000–0xc4a46
- `.rdata`: RVA 0xc5000–0xecb34
- crash address 0xc5547 = `.rdata + 0x547` (read-only data section)

**Crash signature:** `memcpy` writes into **read-only** `.rdata` section.

This indicates:
- Either dst pointer is garbage (corrupted variable, wild pointer)
- Or memcpy is being called with a struct-literal that codegen placed in .rdata, and reverse src/dst was passed
- Or fiber stack pointer escaped into another fiber's space → arena issue

llvm-symbolizer + llvm-pdbutil could enumerate PDB globals (`nova_cancel_token_cancel_reason`, `nova_supervised_run_impl`, `_nova_cancel_via_driver`, etc. all present) but couldn't resolve addresses-to-symbols on Windows PE+PDB (limitation of llvm tooling here). Stack walk gave 13 frames, all symbolized as `memcpy_$fo_rvas$` — false positive due to llvm-symbolizer fallback.

**Diag VEH REVERTED.** Useful for next session under different debugger.

### 12.10 Refined narrowing (post-Attempts 1+2)

Minimal repro narrowed across 6 probes:

| Probe | Pattern | Result |
|-------|---------|--------|
| `tok.cancel("hello")` on main only | crashes? | **10/10 PASS** ✅ |
| `supervised { spawn { } }` (empty body) | crashes? | **10/10 PASS** ✅ |
| `supervised { spawn { tok.is_cancelled() } }` (read only) | crashes? | **10/10 PASS** ✅ |
| `supervised { tok.cancel() }` from main (no spawn) | crashes? | **10/10 PASS** ✅ |
| `supervised { spawn { tok.cancel() } }` | crashes? | **10/10 SEGFAULT** ⚠️ |
| `supervised { spawn { tok.cancel("hello") } }` | crashes? | **26-30/30 SEGFAULT** ⚠️ |
| `supervised { spawn { tok.cancel(); for_loop } }` | crashes? | 4/10 SEGFAULT (some Heisen mask) |

**Necessary AND sufficient pattern:** `supervised(cancel: tok) { spawn { ... tok.cancel(...) ... } }`. Reason string is **red herring**. The crash is triggered by **calling cancel ON THE BINDING TOKEN from inside a spawned fiber**.

**Crash is WRITE memcpy into .rdata.** Suggests:
- Wild destination pointer in a struct copy (`*p = struct_literal`)
- Could be in cancel_box_str OR a struct field write OR cancel_token mutation through corrupted pointer
- Or stale `bound_scope` pointing into code segment after GC/arena reclamation

### 12.11 Next session entry plan

1. **Pre-revert diag VEH** is documented in `git log` of branch (commit was reverted, source available via `git show <commit>:compiler-codegen/nova_rt/runtime.c`)
2. **Build with `-g -O0`** for full debug + non-optimized — much easier to bisect
3. **Try MSVC cdb** instead of llvm-symbolizer for PDB:
   - `cdb -z _min.pdb -y "/tmp/nova_tests/..."` may resolve addresses
   - Or install Visual Studio Debug Tools via `winget install Microsoft.WindowsSDK.10.0.22621`
4. **OR use `--keep-c` + manual clang invocation** with `-g -O0` flags
5. Once frame[1] (caller of memcpy) is resolved, fix is likely 1-line. **Don't attempt blind fixes** like reordering writes (already tried, failed).

### 12.12 Anti-pattern from this session (do NOT repeat)

1. **Attempt 1 (Heisen-fix):** reorder data-before-flag in nova_cancel_token_cancel_reason — 4/30 PASS, not real fix. Lesson: gated-getenv() branches masked symptom in isolated nova_cancel_token_cancel_reason, but the actual bug is elsewhere (in caller paths). Layout perturbation effect was misread as ordering bug confirmation.
2. **Attempt 2 (GC-pin-fix):** apply documented §11 Option A — confirmed §11 bug is DIFFERENT (990+ fibers, panic not SEGFAULT), pin doesn't help at 1 fiber. Lesson: don't apply a previously documented fix without confirming **same symptom signature**.
3. **Anti-pattern rule:** before any code fix, get **deterministic crash localization** (RIP + caller frame). RIP alone insufficient when crashes in standard libc functions like memcpy — need caller.

### 12.13 Session #7d — third+fourth fix attempts (2026-05-31, REVERTED)

**Attempt 3: codegen bypass for no-arg form** (best partial result, BUT breaks semantics):
- Emit `nova_cancel_token_cancel(tok)` instead of `cancel_reason(tok, box_str(...))` for `tok.cancel()`
- Stress: 25/30 PASS (was 0/30) — **huge improvement**
- 🔴 Breaks language semantics: `tok.reason()` после `tok.cancel()` должен возвращать `Some("cancelled")` (per Plan 49 Ф.1). С bypass возвращает None.
- REVERTED.

**Attempt 4: nova_cancel_box_str runtime variants** (all worse than Attempt 3):
- 4a: `nova_alloc_uncollectable` instead of `nova_alloc`: 7/30 — partial improvement
- 4b: 4a + explicit `memcpy` (no struct assignment): 25/30 ИДЕНТ Attempt 3 numbers
- 4c: 4b + remove `inline` keyword (force real call): 18/20 — similar
- 4d: New `nova_cancel_box_str_raw(const char* ptr, int64_t len)` + codegen using it for `tok.cancel("hello")`: 5/30 — WORSE than 3
- 4e: 4d + `nova_cancel_box_str_raw` for `tok.cancel()` no-arg too: 5/30 — same

REVERTED.

### 12.14 Refined diagnostic + remaining hypothesis

**Confirmed:** ANY `nova_alloc*` call from inside a spawned worker fiber (under M:N armed runtime + GC threads) is unreliable. Bypass that avoids alloc entirely (`nova_cancel_token_cancel(tok)` calls `cancel_reason(tok, NULL)` — no alloc) gets ~80-85% PASS.

**Working hypothesis:** Boehm GC + minicoro stack interaction. Minicoro fibers have separate stacks not registered as GC roots (mco_arena uncollectable, but mco fiber STACK itself may not be scanned). When worker thread switches to minicoro fiber, calls `nova_alloc` → GC may collect newly-allocated block before reference escapes to a scanned location.

**Why SpawnCtx alloc works from worker fiber but cancel_box_str doesn't:**
- SpawnCtx: caller (parent fiber) allocates BEFORE switching to child fiber's stack. Reference is in caller's stack/registers (scanned).
- cancel_box_str: allocated FROM WITHIN child fiber stack. Reference temporarily lives in caller-of-box_str's stack (which IS the minicoro fiber stack — not scanned by Boehm).

This matches §11 root cause but at different code path. §11 fix (pin tok) doesn't apply here because what we need to pin is the box_str return value, which is ephemeral.

### 12.15 Next session: actual fix approach

**DO NOT attempt:**
- More bypass tricks at codegen level (breaks semantics)
- Replacing struct assignment with memcpy (not the issue)
- Removing inline (not the issue)
- Pin cancel_tok via scope.ctx_pins (already tried, §11 Option A)

**DO try:**
1. **Register minicoro fiber stack as GC root** via `GC_add_roots` upon fiber spawn into worker (and remove on fiber death). Investigation needed: is the mco fiber stack heap-allocated separately, or part of mco_arena? If mco_arena is uncollectable, stack inside it IS scanned. Then why does this race happen?
2. **Use `GC_disable() / GC_enable()` around `nova_cancel_box_str` call site** as targeted disable of GC during the critical alloc-then-publish window.
3. **Switch `nova_cancel_box_str` allocation strategy:** Per-cancel-call use a per-fiber TLS scratch slot for reason storage (zero-alloc). Adjustments needed in `nova_cancel_token_reason` getter to dereference correctly.
4. **Bisect main branch commits 665d4654f37..HEAD** to find which commit introduced this. Best estimate: changes to mco_arena allocation, GC integration, or how worker threads register with Boehm. Plan 91.11 var_boxed, Plan 110.x cancel-shield touched related areas.

### 12.16 Lessons reinforced (anti-patterns to avoid)

From session #7d:
1. **«Quick run gives 25/30 PASS → fixed!» FALSE.** Always check: does the fix preserve language semantics? My Attempt 3 broke `tok.reason()` return value. Stress passing != semantically correct.
2. **Runtime workarounds (4a-4e) made things worse compared to bypass (3).** Because bypass eliminated the failing op entirely; runtime variants still performed the failing op. Lesson: when bypass works and "correct fix" doesn't, the bypass is exposing the bug location, not solving root cause.
3. **Don't iterate runtime patches without confirming hypothesis.** I tried 5 variants of box_str (uncollectable, memcpy, non-inline, _raw, mixed), all worse. The hypothesis "memcpy/inline/ABI" was wrong; bug is in alloc.

All changes REVERTED. Branch state clean.

### 12.17 Session #7e — GC_disable bracket (REVERTED — caused TIMEOUT regressions)

**Attempt 5: nova_gc_disable/enable wrappers around box_str + cancel_reason**

Added:
- `nova_gc_disable()` / `nova_gc_enable()` API in alloc.h, three backend impls
  (alloc.c no-op for malloc, alloc_rc.c no-op for RC, alloc_boehm.c calls Boehm's GC_disable/GC_enable)
- Bracket in `nova_cancel_box_str` (around alloc+write)
- Bracket in `nova_cancel_token_cancel_reason` (around entire function body — for cleanup loop, bound_scope walk, linked tokens loop)

**Standalone repro stress 30×:**
- `_min`: 0/30 → 19/30 (+19) ✅
- `_probe_arg`: 0/30 → 17/30 (+17) ✅
- `cancel_semantics_test`: 0/30 → 18/30 (+18) ✅

**Full nova test (vs pre-fix baseline 1516/115/56):**
- PASS: 1515 (-1)
- FAIL: 116 (+1)
- **TIMEOUT: 6 (was 2, +4 regressions)** ⚠️

New TIMEOUTs from fix:
- plan103_3/reentrant_other_fiber_blocked (new)
- plan103_3/rwlock_multiple_readers (new — was passing in §12.10)
- plan103_3/reentrant_unlock_from_wrong_fiber_neg (was already failing)

**Likely cause of regressions:** GC_disable is a nested counter — if cancel_reason holds it through entire function (which includes wake_all + cancel_via_driver — these may take long and yield via uv_run), other threads can't allocate. plan103_3 reentrant tests have multiple cancel-waking-reentrant-mutex-lock cycles → if any holds GC_disable while another waits for alloc → deadlock.

**Verdict:** Standalone improvement real, but **net negative globally**. REVERTED.

### 12.18 Sessions #7b-7e cumulative assessment

Five fix attempts, all reverted:

| Attempt | Approach | Standalone | Full Suite | Verdict |
|---------|----------|-----------|-----------|---------|
| 7b | Reorder data-before-flag | 4/30 marginal | 0 regression | REVERT (wrong site) |
| 7c | Pin tok via ctx_pins (§11 Option A) | 1/30 | 0 regression | REVERT (wrong bug) |
| 7d.3 | Codegen bypass for no-arg | 25/30 best | breaks semantics | REVERT (lang spec) |
| 7d.4a-e | Runtime box_str variants | 5-7/30 | 0 regression | REVERT (worse than 3) |
| 7e | GC_disable bracket | 18-19/30 | **+4 TIMEOUT** | REVERT (net negative) |

**Bug remains [M-83.11-supervised-spawn-cancel-memcpy-segv].** Branch clean at HEAD `3a8199c54a1` + this session's doc commit pending.

### 12.19 Refined understanding after 5 attempts

**What we know for sure:**
1. Crash is WRITE memcpy to .rdata section (deterministic addr 0xC5547 region)
2. Trigger: `supervised(cancel: tok) { spawn { tok.cancel(...) } }` — cancel from spawned fiber
3. Crashes after 184 main commits (pre-merge .exe of same test passed)
4. cancel_box_str is sensitive but bypassing it doesn't fully fix (other code paths involved)
5. GC_disable PARTIAL helps (60% PASS) but breaks plan103_3 tests via held-disable deadlock

**What we don't know:**
- Exact caller of memcpy (llvm-symbolizer can't resolve PDB on Windows here)
- Which of 184 commits introduced regression
- Whether bug is GC/mco interaction, ABI/codegen, или другое

**For real fix, recommended approaches (high investment):**
1. **WinDbg / cdb proper symbolization:** install `winget install Microsoft.WindowsSDK.10.0.22621` then `cdb -z _min.pdb` to resolve frame 1 of memcpy stack trace. Knowing the caller is the keystone.
2. **Git bisect 665d4654f37..HEAD:** 184 commits, ~8 iterations × 2 min build = ~20 min. Test script: build + 5× stress, fail if any SEGFAULT. Will identify specific commit.
3. **Switch to -O0 build** for the test specifically — debug symbols may resolve, and reduced optimization may change crash signature in ways that help.

### 12.20 Session #7 final state

Total time: 8+ hours across 5 attempts. Net code change: 0 (all reverted).
Branch state: clean except docs commits.

**Recommendation: ESCALATE.** This bug needs either:
- A dedicated Opus + Thinking ON deep-dive with proper tooling (WinDbg)
- OR git bisect to identify the introducing commit (mechanical, lower-risk)
- OR accept `[M-83.11-supervised-spawn-cancel-memcpy-segv]` as known-broken
  for 0.1 with documented workaround (don't call cancel from spawn body —
  cancel from main only)

**The 5-attempt pattern from sessions #7b-7e mirrors the 6-attempt pattern from sessions #1-6 for the STALE-slot race (§10). In both cases, multiple plausible-looking fixes consumed hours before correct diagnosis was found via state-dump (§10.4 algorithm). The state-dump tools (programmatic dump, VEH crash localizer) gave us deterministic addresses but Windows PDB toolchain limitations prevented full symbolization.**

**Anti-pattern reinforced:** Don't iterate workaround fixes without proper symbolic debugger. Time invested in workarounds without root cause = lost.

### 12.21 Session #8 — BREAKTHROUGH: Bug confirmed Windows-only (2026-06-01)

**Used WSL Ubuntu (via `wsl -d Ubuntu`) to run identical test on Linux:**

Setup:
- Installed gdb, clang, libgc-dev, libuv1-dev via apt
- Installed rustup stable (1.96) — Ubuntu's packaged rustc 1.93.1 segfaulted on nova-codegen
- Copied worktree to /root/nova (Linux native fs for fast cargo build)
- Fixed Linux build issues:
  - fibers.h:2158 unguarded `__declspec(thread)` → wrapped in `#ifdef _MSC_VER`
  - runtime.c:532 static __thread → non-static (extern/static conflict)

**Result on Linux: `_min` 30/30 PASS, `cancel_semantics_test` 1/1 PASS.**

🎯 **Bug is 100% Windows-specific.** Not Nova logic. Not Boehm in general.

**Root cause (now confirmed):** Boehm on Windows uses SuspendThread + reads thread stack pointer (RSP) at suspension to walk the OS thread's main stack. But worker threads may be mid-`mco_resume` — RSP at that moment points into the FIBER stack, not the main stack. Boehm reads garbage above RSP (above-stack is uncommitted), OR if it reads the main stack range, misses fiber stack pointers entirely. Either way, fresh `nova_alloc` return values held in fiber stack are invisible to GC.

Linux POSIX uses signal-based stack walking — pthread_kill + signal handler reads ucontext.rsp which captures CURRENT stack (whatever fiber is running). Fiber stacks are walked correctly there.

### 12.22 Attempt 6: GC_add_roots per fiber (REVERTED — performance)

**Approach:** populated empty stubs `_nova_gc_add_fiber_roots` / `_nova_gc_remove_fiber_roots` (which were planned but unimplemented since Plan 82) with real `GC_add_roots(stack_base, stack_base+stack_size)` / `GC_remove_roots(...)`. Wired at all `mco_create` / `mco_destroy` sites (already had call sites — just needed bodies).

**Standalone stress (significant improvement):**
- `_min`: 0/30 → **21/30** ✅
- `_probe_arg`: 0/30 → **24/30** ✅
- `cancel_semantics_test`: 0/30 → **7/30** ✅

**Full nova test (DEVASTATING regression):**
- PASS: 1516 → **783** (-733)
- FAIL: 115 → **848** (+733)
- **726 TIMEOUTs** (was 2) across ALL test categories (syntax, contracts, plan103_3, plan62, etc.)

**Cause of regression:** GC_add_roots / GC_remove_roots is expensive per call. With fibers being created/destroyed thousands of times during test suite, the cumulative cost dominates execution time. Each `GC_add_roots` likely:
- Acquires GC allocator lock
- Walks/inserts into roots list (potentially O(N) over existing roots)
- Marks dirty / invalidates GC's cached root scan

With N fibers across all tests, cost ~O(N²) → 70-90 second per-test TIMEOUT.

**REVERTED.**

### 12.23 Path forward: GC_push_other_roots callback

Correct architectural fix: register a SINGLE `GC_push_other_roots` callback at runtime init. When GC marks, callback iterates a worker-maintained list of active mco_coro pointers and calls `GC_push_all_stack(co->stack_base, co->stack_base + co->stack_size)` for each.

Benefits:
- One-time registration cost (no per-fiber-create overhead)
- Callback runs ONLY during GC mark phase
- Cost proportional to live fiber count at GC time, not total fiber lifetime ops

Implementation outline:
1. Per-worker `active_fibers[]` list (pushed on mco_resume, popped on yield/done)
2. `nova_gc_init` (Boehm backend) installs `GC_push_other_roots = nova_fiber_root_walker`
3. `nova_fiber_root_walker` iterates all workers' active_fibers and calls GC_push_all_stack

Risk:
- Worker list updates need lock (or atomic intrusive list)
- During GC's stop-the-world, list must be consistent — Boehm guarantees suspension before callback

Next session implementation target.

### 12.24 Session #8 lessons

1. **WSL/Linux as diagnostic environment is gold.** When Windows tooling fails (no symbolic debugger that works with PE+PDB), reproducing on Linux gives proper gdb support AND confirms platform-specificity. Should be #1 default diagnostic step for Windows-only bugs.

2. **Performance impact of GC_add_roots was underestimated.** Per-event GC interactions have hidden quadratic cost. Boehm-aware design uses ONE callback installed at init, not per-event registrations.

3. **The empty stubs `_nova_gc_add_fiber_roots` from Plan 82 were a TRAP.** They existed and were called everywhere as if they did the work. They didn't. Anyone (myself included) who looked at them would assume Plan 82 handled GC integration. It didn't. Need to mark such no-op stubs more prominently OR delete them.

4. **Bug now has CLEAR root cause documented.** This session was net-zero code but converted "[M-83.11-supervised-spawn-cancel-memcpy-segv] = unknown cause" to "= Windows Boehm SuspendThread can't see mco fiber stacks; fix via GC_push_other_roots callback (next session)".

### 12.25 Session #8 final state

Total time: ~3 hours.
Code change: 0 (Attempt 6 reverted).
Knowledge gained:
- Confirmed Windows-only via WSL
- Architecture identified
- Failed approach documented as anti-pattern (per-fiber GC_add_roots = O(N²) overhead)
- Next approach designed (GC_push_other_roots callback)

Bug `[M-83.11-supervised-spawn-cancel-memcpy-segv]` ready for proper architectural fix.

Branch clean. WSL workspace `/root/nova` ready for future Linux-side testing.

### 12.26 Attempt 7: §12.23 GC_push_other_roots active_fiber callback (2026-06-01, REVERTED)

**Architectural discovery before implementation:** Plan 82 fiber-arena already
installs a `GC_set_push_other_roots(_nova_fw_gc_push_other_roots)` callback at
worker startup ([fiber_arena_win.c:244](../../compiler-codegen/nova_rt/fiber_arena_win.c#L244)).
This existing walker iterates `_nova_fw_arena_list`, and for each arena pushes:
1. All committed regions of slots whose `used_bits[slot] == 1` and `mco_status(co) != MCO_DEAD`
2. Worker's native OS thread stack via `a->native_base` region (VirtualQuery-filtered)

So §12.23's proposed design (per-worker `active_fiber` + GC_push_other_roots callback)
is a strict subset of Plan 82's coverage: every active fiber's slot has `used_bits=1`,
which means Plan 82 walker already pushes it. The §12.23 proposal cannot add coverage
beyond what Plan 82 walker provides.

Per §12.22 evidence: even Attempt 6 (per-fiber `GC_add_roots` — strictly *more* coverage
than §12.23 because it registers ALL spawned fiber stacks, not just currently-resumed)
only achieved 21/30 PASS standalone. So fiber-stack coverage is provably *not* the
gating factor for the bug.

**Implementation attempted anyway (for empirical confirmation):**

1. Added `mco_coro* active_fiber` field to `NovaWorker` ([runtime.c](../../compiler-codegen/nova_rt/runtime.c))
2. Wrapped all 3 `mco_resume` sites (lines 953/1093/1724) with
   `__atomic_store_n(&w->active_fiber, co, RELEASE)` / `NULL` after
3. Added accessor functions `nova_runtime_active_worker_count()` / `nova_runtime_active_fiber_at(idx)`
4. Extended Plan 82 walker `_nova_fw_gc_push_other_roots` to iterate workers
   and call `_nova_fw_gc_push_region(co->stack_base, co->stack_base + co->stack_size)`
   for each `active_fiber`

**Build:** clean (2m 23s).

**Stress 30× standalone:**
- `_min` (`supervised(cancel:tok){spawn{tok.cancel()}}`): **13/30 PASS** (was 0/30 baseline; Attempt 6 was 21/30)
- `cancel_semantics_test`: **4/30 PASS** (was 0/30 baseline)

**Comparison table:**

| Approach | _min stress | cancel_sem stress | Full suite | Verdict |
|----------|-------------|-------------------|------------|---------|
| Baseline (Plan 82 walker only) | 0/30 | 0/30 | clean | — |
| Attempt 5e GC_disable bracket | 18-19/30 | n/a | +4 TIMEOUT | REVERT |
| Attempt 6 per-fiber GC_add_roots | 21/30 | 7/30 | +726 TIMEOUT | REVERT |
| Attempt 7 §12.23 active_fiber callback | 13/30 | 4/30 | not measured | **REVERT** |

§12.23 helped marginally over baseline (0→13) but significantly worse than
Attempt 6 (13 vs 21) which had broader coverage, and decisively below 30/30 target.
Attempt 7 *confirms* the §12.22 hypothesis: more fiber-stack coverage gives more
PASS rate but cannot reach 30/30 — root cause lies elsewhere.

**Root cause hypothesis refined (post-Attempts 6+7):**

The bug is NOT "fiber stack pointers missed by GC". With explicit push of
- ALL live fibers (Plan 82 walker via arena bitmap)
- PLUS currently-resumed fiber (Attempt 7 active_fiber)
- PLUS register file (Boehm via GetThreadContext)

...the bug still fires 17/30 (`_min`) and 26/30 (`cancel_sem`). So scanning is
already comprehensive; the bug is in a different mechanism.

Plausible alternative causes (untested):

1. **Boehm's default thread stack walker SEGV on Windows.** When STW suspends a
   worker mid-mco_resume, GetThreadContext returns RSP_fiber. Boehm walks from
   RSP to stored stack_end (OS thread base). These ranges are in different
   VirtualAlloc regions; walking crosses unmapped pages → ACCESS_VIOLATION
   inside the GC mark phase. Boehm has VEH for this but it may have edge cases
   on Windows minicoro-context-swapped state.

2. **TIB.StackBase / TIB.StackLimit swap interaction.** Minicoro asm swaps these
   on every context switch ([minicoro.h:710-726](../../compiler-codegen/nova_rt/minicoro.h#L710-L726)).
   Boehm read at thread register time captures OS values. If any Boehm code
   re-reads TIB during STW (e.g., stack overflow guard, sigaltstack equivalent),
   it gets fiber values → confusion → potentially wild pointer dereference.

3. **Memcpy in .rdata writeback** (§12.9 crash signature) suggests `memcpy(dst, src, n)`
   where `dst` is a pointer derived from somewhere holding an .rdata address
   (string literal). This could happen if a struct field meant to hold a heap
   pointer was overwritten by a value originating from .rdata — but no direct
   path identified.

4. **MSVC/clang ABI difference on suspended threads.** Worker is suspended at
   arbitrary instruction; saved register state may have caller-saved registers
   that should hold the cancel_box_str return but don't. This is speculative.

**Recommended next steps (per §12.19 priority order):**

1. **cdb / WinDbg symbolization:** `winget install Microsoft.WindowsSDK.10.0.22621`,
   then `cdb -z _min.pdb` to resolve the actual caller of memcpy at crash.
   Knowing whose `memcpy` (cancel_box_str? cancel_token field write? a struct
   field assignment?) is the keystone all 7 attempts have missed.

2. **Git bisect main commits 665d4654f37..HEAD (184 commits):** identify the
   specific commit that introduced the regression. The pre-merge .exe of same
   test passed (per §12.21 finding). Mechanical, ~20 min wall time.

3. **AcceptKnown-broken for 0.1:** mark `[M-83.11-supervised-spawn-cancel-memcpy-segv]`
   open + document workaround "don't call `tok.cancel()` from spawned fiber under
   armed M:N on Windows; cancel from main only". cancel_semantics_test failure
   is the only test affected (per §12.21 full suite delta).

**Decision matrix for future sessions:**

- If a session can deploy WinDbg / proper PE+PDB symbolization → take that path.
  Estimated time: 4-8h research + 1h fix.
- If session is timeboxed to ≤2h → do bisect (mechanical, deterministic outcome).
- Do NOT iterate more fiber-stack-coverage variants — proven insufficient.
- Do NOT iterate runtime-side patches to cancel_box_str — proven insufficient
  (Attempt 4a-e).

**Final state after Attempt 7:**
- Branch clean (revert verified — `git status` empty)
- All `[M-83.11-supervised-spawn-cancel-memcpy-segv]` evidence consolidated
  here (§12.21-26)
- Marker remains **OPEN**
- No partial fixes left in runtime
- Total session time: ~1h (mostly investigation + build + stress)

**Anti-pattern reinforced (added to session §12.16 lessons):**

7. **«Strict subset of existing coverage» trap.** Before implementing a fix
   that adds GC root coverage, audit existing coverage. Plan 82's walker
   already had what §12.23 proposed. Implementation became 80 lines of code
   for measurably partial result. **Always audit before implementing.**

### 12.27 Session #10 — git bisect IDENTIFIED culprit commit (2026-06-01)

**BREAKTHROUGH**: 184-main-commits hypothesis (§12.21) was **WRONG**. Bisect
identified `dd7a4f00bc5` ("fix(plan83.11 Ф.3): STALE-slot race fix + remove
diagnostic instrumentation") as the **first bad commit** — which is the same
Ф.3 fix that memory says was "verified clean 30/30 stress PASS".

**Bisect procedure:**

Bracket: `ced94a65bb4` (pre-Plan-83.11 work) = GOOD vs HEAD = BAD.

Verification stress 15× per commit (P(false-good) at 40% repro rate ≈ 0.4%):
- `ced94a65bb4` (Merge plan-103.7 pre-83.11): **15/15 PASS** ✓ GOOD
- `dd7a4f00bc5` (Ф.3 fix): **1/15 PASS** ✗ BAD
- HEAD `81d875a1af8`: **6/15 PASS** ✗ BAD

`git bisect run /tmp/bisect_check.sh` converged in **3 iterations**:
1. `6dc7ad39506` (Ф.3 wip futex/SEQ_CST diag): GOOD
2. `d5aaf9a20048` (Ф.3.A v2 single-atomic wait_state): GOOD
3. `88fae22f217e` (Ф.3 Phase A state-dump): GOOD
4. → first bad = `dd7a4f00bc5`

`stress_iso_3e` (which dd7a4f00bc5 fixed) tests STALE-slot race. _min.nv
(cancel-from-spawn) is a DIFFERENT race that the same commit accidentally
introduced. Memory snapshot "Ф.3 verified clean" was vs `stress_iso_3e`,
not vs all concurrency tests.

**dd7a4f00bc5 changes (5 files, +225/-103):**

| File | Change | Fires в нашем сценарии? |
|------|--------|------------------------|
| `fibers.h` Fix A | slot_lock spinlock + STALE skip в `nova_scope_alloc_slot` + `expected_co` field в NovaSleepState | spinlock yes (every spawn); STALE skip dead (no sleep) |
| `driver.c` Fix B | WRONG-FIBER direct dispatch в `_nova_driver_sleep_close_cb` | dead (close_cb requires sleep) |
| `runtime.c` Fix C1 | displaced fiber epilogue (`_nova_worker_slot=-2`) в worker_main | dead (set only by Fix B) |
| `runtime.c` Fix C2 | `nova_runtime_spawn_global` deque → wake_pending mutex | dead (codegen uses `spawn_into`, not `spawn_global`) |
| `nova_sched.h` "t4-race-fix" | в `nova_sched_park`: SEQ_CST reload of `parked[slot]` ПОСЛЕ store PARKED, conditional restore IDLE if wake won race | **fires если fiber yields** |
| Multiple | Removed all `nova_aint_inc(&_nova_diag_drv_*)` counters | barrier removal, potential Heisen-mask |

**Sub-bisect attempts (at dd7a4f00bc5):**

| Attempt | Surgical change | _min stress | Verdict |
|---------|-----------------|-------------|---------|
| Baseline dd7a4f00bc5 | (no edits) | 1/15 | BAD |
| Sub-bisect 1 | STALE skip block removed | 0/15 | BAD (slightly worse — randomness) |
| Sub-bisect 2 | slot_lock spinlock acq/rel removed (STALE skip kept) | 2/15 | BAD |

**Conclusion:** Neither STALE skip nor slot_lock spinlock — taken individually —
is the cause. Removing either still leaves bug firing 13-15/15. So root cause
is elsewhere в dd7a4f00bc5.

**Remaining hypotheses (in order of suspicion):**

1. **`nova_sched_park` t4-race-fix logic** (nova_sched.h:163-175). New code path:
   - Worker calls `nova_sched_park` (e.g. during runtime cleanup wait)
   - Stores PARKED state
   - SEQ_CST reload `parked[slot]`; if cleared by concurrent wake → restore state to IDLE
   - The conditional restore creates a window where state is mutated AFTER PARKED but BEFORE mco_yield
   - Could affect cancel-from-spawn path indirectly via `nova_supervised_run_impl` wait mechanism

2. **Diagnostic counter `nova_aint_inc` removal as Heisen-mask.** The removed
   `nova_aint_inc(&_nova_diag_drv_dispatch_same/cross)` calls in `_worker_dispatch_ready`
   (runtime.c:687,700) and `nova_aint_inc(&_nova_diag_drv_worker_drained)` in worker
   wake_pending drain (line 792) were full SEQ_CST barriers that may have hidden
   latent race in dispatch path. Per §10.4 Lesson #1: «if printf fixes race, it's
   a memory fence artifact, not a fix». Inverse: removing fence reveals existing race.

3. **Combination of Fix A + nova_sched.h.** The new slot_lock spinlock changes
   timing across multiple call sites; new t4-race-fix changes park timing. Together
   they may shift races just enough to expose memcpy SEGV. Single-fix revert insufficient.

**Recommended next steps (informed by bisect):**

1. **Examine `nova_sched.h` t4-race-fix more carefully.** This is the only NEW
   logic (vs barrier removal). Specifically:
   - Is the "wake won race" path actually triggerable in cancel-from-spawn?
   - Does the IDLE-restore cause double-resume vulnerability?
   - Try restoring pre-Ф.3 behavior in nova_sched.h, keep Fix A/B/C — see if fixes

2. **Heisen-test:** restore one `nova_aint_inc(&_nova_diag_drv_dispatch_*)` in
   `_worker_dispatch_ready` (runtime.c). If fix → confirms barrier mask hypothesis,
   real bug is in dispatch path memory ordering.

3. **cdb / WinDbg crash localization.** Per §12.19 unchanged recommendation.
   Now we know to focus on dd7a4f00bc5 area, much narrower target.

**Cleanup:**
- Worktree restored to plan-83.11-centralized-driver HEAD (was 81d875a1af8 from §12.26)
- Removed _min.nv test probe (re-created by bisect_check.sh during run, manually deleted)
- `/tmp/bisect_check.sh` retained for future sub-bisect cycles

**Total time:** ~40 min (bisect + 2 sub-bisects + analysis + docs).

**Lessons learned (added к §10.4 + §12.16):**

8. **«Bisect over the hypothesized region first».** §12.21 spent 3+ hours diagnosing
   via WSL the wrong region (184 main commits). 20-min bisect would have correctly
   identified Ф.3 fix as the culprit. Always start bisect at first opportunity
   when you have clear GOOD/BAD bracket.

9. **«Verified clean» needs explicit scope.** Plan 83.11 Ф.3 memory said «verified
   30/30 stress PASS» — это было `stress_iso_3e` (which Ф.3 fixed). Not all
   tests. cancel_semantics_test was also passing in single nova test run, but
   bug was latent (stochastic 7% PASS rate for _min.nv) — only stress reveals.
   Mark verification scope explicitly: «verified X under stress Y, NOT verified
   for Z, W».

10. **Sub-bisect surgical reverts can be misleading.** If a commit contains
    multiple interacting fixes, reverting one may not eliminate bug because
    other fixes' effects compensate. Need either to revert ALL or to study
    interaction. Bisect identifies coarse commit; understanding requires diff
    + dynamic analysis, not just removal.

Marker `[M-83.11-supervised-spawn-cancel-memcpy-segv]` remains **OPEN** but
target region is now precisely identified.

### 12.28 Session #11 — t4-race-fix hypothesis test (2026-06-01)

**Setup change since §12.27:** Merged main (208 commits) into branch. Build
clean. Bug **became deterministic** (was ~40% repro rate, now 100% per
`tools/stress_bisect.sh nova_tests/concurrency/_min.nv 10` → 0/10 PASS).
Determinism upgrade simplifies all subsequent diagnostics (e.g. cdb).

**Test 1: revert `nova_sched.h` t4-race-fix hunk only** (top suspect §12.27):

Removed block in `nova_sched_park` (lines 166-177 at HEAD) — SEQ_CST reload
of `parked[slot]` + conditional `nova_fiber_state_store(IDLE)`.

Result: **5/30 PASS** (17% repro recovery).

Baseline comparison:
- Pre-revert HEAD post-merge: **0/10 PASS** (100% BAD)
- Post-revert HEAD post-merge: **5/30 PASS** (83% BAD)

**Test 2: full combo — t4-race-fix + spinlock + STALE skip removed:**

Additionally removed `fibers.h` Fix A slot_lock acquire/release pairs
(alloc_slot + free_slot) AND the STALE skip `continue` branch.

Result: **5/30 PASS** — IDENTICAL to Test 1.

**Conclusion:** t4-race-fix is the **sole contributor from Plan 83.11 Ф.3
fix** to our scenario. Spinlock + STALE skip are silent for cancel-from-spawn
(consistent with §12.27 analysis: they affect slot allocation, but our crash
isn't about slot allocation).

**BUT 83% still BAD even with all Ф.3 fixes neutralized.** Since pre-merge
HEAD was 40% PASS and post-merge HEAD is 0% PASS, **the 208 main commits
introduced a SECOND contributor** that interacts with whatever's left.

| Configuration | _min stress | Plan 83.11 t4 | Main commits | Verdict |
|--|--|--|--|--|
| ced94a65bb4 (pre-Plan-83.11) | 15/15 PASS | absent | absent | GOOD baseline |
| dd7a4f00bc5 (Ф.3 fix only) | 1/15 PASS | present | absent | BAD ~93% |
| Pre-merge HEAD §12.26 | 6/15 PASS | present | absent | BAD ~60% |
| Post-merge HEAD §12.28 | 0/10 PASS | present | **+208 commits** | BAD 100% |
| Post-merge HEAD, t4 removed | 5/30 PASS | absent | +208 commits | BAD 83% |

Inference: removing t4-race-fix in post-merge state recovers from 0% → 17%
PASS. Pre-merge with t4 removed would likely be ~80-95% PASS (need test).
**Main commits add ~20-65% additional failure rate.**

**Refined root cause model (TWO contributors):**

1. **Plan 83.11 Ф.3 t4-race-fix** changes `nova_sched_park` to introduce a
   state transition AFTER PARKED store under specific race condition. This
   creates a window where state is mutated in non-obvious order, potentially
   leaving fiber state machine in inconsistent state when concurrent wake fires.

2. **Some commit(s) in 208 main commits** added another race surface or
   memory ordering issue. Candidates (per §12.5 suspect list + recent activity):
   - Plan 91.11 var_boxed changes (heap pressure)
   - Plan 110.x cancel-shielding (cancel path mutations)
   - Plan 73.1 V3 consume-binding (codegen changes around cancel)
   - Plan 114 D184 `let` removal (codegen changes everywhere)

**Recommended next session (informed):**

Path A — **t4-race-fix surgical fix (~1h)**:
1. Understand WHY t4-race-fix is needed for STALE-slot race (re-read §10.3)
2. Design replacement that achieves the same goal without the IDLE-restore
   side-effect. E.g., make state transitions atomic across PARKED + IDLE-restore
   via single CAS, OR avoid the entire wake-during-park-window via different
   protocol
3. Verify `stress_iso_3e` (STALE-slot regression) AND `_min.nv` both PASS

Path B — **Bisect 208 main commits with t4 patched** (~30 min):
1. Modify `tools/stress_bisect.sh` to apply t4-race-fix revert at each checkout
2. Bisect ced94a65bb4..main with that script
3. Identifies the second contributor commit
4. Adds clearer fix target

Path C — **cdb crash localization** (~1-2h):
1. install Windows SDK + cdb
2. With deterministic repro, cdb session straightforward
3. Get frame[1] of memcpy stack — keystone all attempts missed
4. Most informative single step but requires Windows tooling setup

Recommended: **Path B first** (cheap mechanical bisect, deterministic
outcome) — knowing the second commit makes Path A much more targeted.
Path C remains valuable backstop.

**Cleanup after §12.28:**
- Branch clean (all 3 files reverted via `git checkout HEAD --`)
- `_min.nv` probe removed
- `tools/stress_bisect.sh` retained for next session
- Merge commit `4cf4e6f074b` brought branch up-to-date with main

**Lessons added:**

11. **«Partial test result IS a result».** Test 1 gave 17% recovery — not
    a "fix" but valuable elimination/confirmation evidence. Don't dismiss
    partial results as failures; document the rate change explicitly.

12. **«Background rate matters».** Bug rate changes across the codebase's
    evolution (40% pre-merge → 100% post-merge) tell us about interaction
    between identified culprit and other code. Use rate changes diagnostically.

Marker remains **OPEN** with refined understanding: TWO contributors, one
in Plan 83.11 Ф.3 (t4-race-fix), one in 208 main commits (unknown specific).

### 12.29 Session #12 — Path B bisect DISPROVES §12.28's 2-contributor model (2026-06-01)

**Setup discovery:** before running Path B, tested main HEAD (`10cc6b50dd4`)
directly with `tools/stress_bisect.sh`:

`main HEAD alone: PASS=3 FAIL=12 / 15`

§12.28 interpreted this as "main commits added a SECOND independent contributor".
That interpretation **was wrong**: §12.28 incorrectly assumed Plan 83.11 was
not merged to main. In reality, **Plan 83.11 Ф.0-Ф.3 were merged into main via
`cf00a37fb16`** (per memory `project-plan83_11-status.md`), so main HEAD
already contains `dd7a4f00bc5`. Bug fires on main alone because dd7a4f00bc5
IS on main.

**Test 1 (Path B v1, 15× stress): bisect ced94a65bb4..10cc6b50dd4**

`git bisect run /tmp/bisect_main.sh` → 8 iterations → first bad =
`8fd2f91a3a1e` (`docs(plan91/Ф.2.5): D177 spec` — DOCS ONLY +119 lines в
spec/decisions/08-runtime.md).

A docs commit cannot introduce a runtime bug. Manual verification:
- `8fd2f91a3a1e`: re-tested 30× stress = **30/30 PASS** ❌ false-positive

**Test 2 (Path B v2, 30× stress): re-bisect 8fd2f91a3a1e..10cc6b50dd4**

Convergence: 8 iterations → first bad = **`dd7a4f00bc5`**.

**Same commit** as §12.27 bisect. Both bisects independently converge.

**Result: §12.28's "2-contributor model" is DISPROVEN.**

Single-contributor model restored:
- dd7a4f00bc5 introduces the bug (`[M-83.11-supervised-spawn-cancel-memcpy-segv]`)
- All other variance is sample noise (15× stress @ low p has high false-good rate)

**Statistical re-check of §12.28 hypotheses:**

| §12.28 claim | Data | Validity |
|--|--|--|
| t4-race-fix removal recovers 17% | 5/30 vs 0/10 | 95% CI overlap [0,26%] vs [5,36%] → **NOT significant** |
| Spinlock + STALE skip silent | 5/30 vs 5/30 | trivially consistent |
| Main commits added contributor | 3/15 vs 15/15 across SHAs | **CONFOUNDED** — dd7a4f00bc5 в main via cf00a37fb16 |

§12.28 t4-race-fix hypothesis test is statistically indistinguishable from
baseline. May have been Heisen-fix timing perturbation, not real signal.

**Lessons (added to §12.16):**

13. **«Verify the graph before bisecting».** §12.28 misread branch topology
    (assumed plan-83.11 NOT merged to main when it WAS via cf00a37fb16).
    Always `git log --graph --oneline good..bad` before interpreting.

14. **«stress 15× insufficient at low p».** Initial Path B false-positive
    because P(false-good) at p=7% with n=15 ≈ 34%. Heuristic for bisect:
    n ≥ ⌈-ln(0.01)/p⌉. At p=0.20: n=23; p=0.07: n=66. **Default n=15
    too low for stochastic races.**

15. **«Overlapping confidence intervals = no signal».** Always check CI
    overlap before claiming partial signal. 5/30 vs 0/10 = same distribution
    likely.

**True status after §12.29:**

- **Single contributor confirmed**: `dd7a4f00bc5` (Plan 83.11 Ф.3 fix)
- Within dd7a4f00bc5 the specific sub-cause is unknown — §12.27 sub-bisects
  were confounded by 15× variance + dead-code paths
- **Most likely sub-cause** per code analysis (not yet statistically verified):
  `nova_sched.h` t4-race-fix new state mutation between PARKED store and
  mco_yield. Requires (a) higher-N stress validation (n≥66), or
  (b) actual debugger crash localization

**Recommended next steps (revised from §12.28):**

1. **cdb / WinDbg crash localization is now THE recommended path** (was
   alternative). Bug is 100% deterministic at HEAD. Single-run cdb session
   can resolve frame[1] of memcpy = keystone all 8 attempts missed.

2. **NOT** sub-bisect (n≥66 needed at p=7%, ~3× time per iteration).

3. **NOT** test additional code variants (§12.13-26 anti-patterns proven).

**Cleanup:**
- Branch: clean, restored to `plan-83.11-centralized-driver` HEAD `3ff9224a779`
- _min.nv probe: removed
- bisect state: reset
- `tools/stress_bisect.sh`: still useful; n-heuristic note pending update

Marker remains **OPEN** with **single-contributor model** restored. Target
narrow: dd7a4f00bc5's 225-line diff, most suspect = nova_sched.h t4-race-fix
hunk, decisive next step = **cdb session**.

### 12.30 cdb session playbook (next-session prep, no execution yet)

Wrapper script committed: `tools/cdb_session.sh`. Preparation for cdb-based
crash localization. **Not run yet** — saved for fresh next session.

**Setup (one-time, ~5-10 min, requires admin elevation for winget):**

```powershell
# Install Windows SDK (includes cdb.exe)
winget install Microsoft.WindowsSDK.10.0.22621
# Verify install
where cdb.exe
# Expected: C:\Program Files (x86)\Windows Kits\10\Debuggers\x64\cdb.exe
```

If `winget` requires elevation, run from elevated PowerShell. Alternative:
download Windows SDK installer from
https://developer.microsoft.com/windows/downloads/windows-sdk/ and run it.

**Session steps (~30 min once cdb available):**

```bash
# 1. Verify cdb detected
bash tools/cdb_session.sh
# Should report: [cdb] using: /c/Program Files .../cdb.exe

# 2. Create minimal repro probe (if not present)
mkdir -p nova_tests/concurrency
cat > nova_tests/concurrency/_min.nv << 'NV'
module concurrency._min
test "min" {
    ro tok = CancelToken.new()
    supervised(cancel: tok) {
        spawn { tok.cancel() }
    }
}
NV

# 3. Run cdb session — captures crash + stack
bash tools/cdb_session.sh nova_tests/concurrency/_min.nv 2>&1 | tee /tmp/cdb_session.log

# 4. Extract frame[1] from output
grep -A 3 "STACK kn" /tmp/cdb_session.log | head -10
```

**Expected output shape:**

```
=== REGISTERS ===
rax=... rbx=... rcx=...     # full register file at crash
rip=00007ff65d8c688f         # crash PC (matches §12.9 RVA 0xC5547)
rsp=...                       # stack pointer
=== STACK kn ===
00 00000000`xxxxxxxx VCRUNTIME140!memcpy+0xNN
01 00000000`xxxxxxxx _min!nova_cancel_token_cancel_reason+0xNN    ← KEYSTONE
02 00000000`xxxxxxxx _min!nova_fn_spawn_body_NN+0xNN
03 00000000`xxxxxxxx _min!_nova_supervised_spawn_wrapper+0xNN
...
```

**Decision tree based on frame[1] symbol:**

| Frame 1 symbol | Implication | Fix direction |
|--|--|--|
| `nova_cancel_token_cancel_reason` | reason_ptr write into bad memory | Audit struct field writes around line 791-867 in fibers.h |
| `nova_cancel_box_str` | box_str's `*boxed = s` writing to .rdata | reason_ptr lifecycle issue |
| `nova_alloc` / `GC_malloc` | GC internal corruption | Boehm + minicoro fiber stack issue |
| `_nova_cancel_via_driver` | driver.c cancel job ctor | malloc/free mismatch |
| `nova_sched_park` / `nova_sched_wake` | t4-race-fix actually firing | confirms §12.27/§12.28 hypothesis |
| Unrelated runtime fn | misdirection — race elsewhere | study that frame |

**If cdb succeeds:** add §12.31 with stack trace + analysis + fix.

**If cdb fails (e.g. private symbols missing):**
- Try `cdb -y "srv*https://msdl.microsoft.com/download/symbols"` for MS symbols
- Try `dumpbin /SYMBOLS _min.exe` to inspect available symbols
- Fall back to `objdump -d _min.exe | less +/0xC5547` for raw disassembly around crash PC

**If bug doesn't reproduce in cdb session:**
- cdb adds heap diagnostics that may mask race (Application Verifier effect)
- Try `cdb -hd` (heap debugging disabled)
- Or run exe outside cdb, attach mid-run via `cdb -p $(pidof _min.exe)`

**Cleanup after session:**
- Remove `nova_tests/concurrency/_min.nv` probe
- Remove `/tmp/cdb_session.log` if not needed
- Branch remains clean

