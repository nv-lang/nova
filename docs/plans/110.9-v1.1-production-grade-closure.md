// SPDX-License-Identifier: MIT OR Apache-2.0
# Plan 110.9 — V1.1 Production-Grade Closure

> **Создан 2026-06-01.** Sub-plan Plan 110 закрывающий 5 V1.1 followup'ов
> которые остались как documented simplifications после V1 umbrella close
> (merge 874f5766ca5).
>
> **Status:** 📋 PLANNED — deferred until trigger condition.
> **Trigger:** любое из:
>   - W_FFI_CANCEL_UNSAFE lint absence ловит реальный production bug.
>   - User explicitly requests V1.1 closure.
>   - WithExitTimeout protocol нужен в реальном stdlib типе (например,
>     `TcpListener.exit_timeout_ms = 30_000`).
>   - finalizer LIFO replay понадобится для metrics flush на shutdown.
>   - CleanupTimeoutError catch-arm pattern matching нужно в user code.
>
> **Estimate:** ~3-4 dev-day (5 sub-tasks).
> **Model preference:** Opus 4.7 + Thinking ON (parser variance в #5
>   тонкий вопрос).

## Контекст: что landed в V1, что НЕ landed

Plan 110 V1 (merge 874f5766ca5) закрыл 8 sub-plans + 12 D-blocks
ACTIVE + 36/36 fixtures PASS. End-to-end working production-grade
runtime для consume scope cleanup.

**НО** header плана обещал «финал Plan 110 = ВСЁ без упрощений».
5 markers нарушают это обещание — задокументированы honestly в
discussion-log + simplifications.md как V1.1 followups. Plan 110.9
закрывает эти 5.

## 5 sub-tasks (atomic-merge каждый)

### 110.9.1 — Typed `CleanupTimeoutError` throw codegen (~2-3ч)

**Что:** Заменить string-fallback throw в `nv_shield_check_deadline` на
typed `Nova_CleanupTimeoutError*` allocation + `nova_throw_typed`.

**Files:**
- `compiler-codegen/src/codegen/emit_c.rs`:
  - В preamble emit'нуть `static void _nova_throw_cleanup_timeout_impl(int duration_ms)`
    в user TU. Function allocate'ит `Nova_CleanupTimeoutError* e = nova_alloc(...)`,
    set `e->duration_ms = duration_ms`, calls `nova_throw_typed(msg, e, NOVA_TID_USER_Nova_CleanupTimeoutError)`.
  - В startup-code (после `nova_runtime_init()`): emit
    `_nova_throw_cleanup_timeout_fn = &_nova_throw_cleanup_timeout_impl;`.
- Conditional: только если `CleanupTimeoutError` в `type_id_registry`
  (всегда true когда prelude loaded, который default).

**Tests:**
- `nova_tests/plan110/cleanup_timeout_typed_throw.nv` (NEW): fixture
  с очень коротким timeout (через `WithExitTimeout` impl from 110.9.2 OR
  test-only override), body suspend'ит → throw fires, outer
  `catch e: CleanupTimeoutError { e.duration_ms }` reads typed field.

**Acceptance:**
- A110.9.1.a: codegen emit'ит impl + pointer assignment.
- A110.9.1.b: catch arm читает typed `duration_ms` field.

**Depends:** 110.9.2 (для concrete fire-test) OR test-only override
API.

---

### 110.9.2 — Level 1 `WithExitTimeout` per-type protocol (~½ дня)

**Что:** Добавить Level 1 в 3-level timeout resolution (D192). Сейчас
codegen skip'ает Level 1.

**Files:**
- `compiler-codegen/src/codegen/emit_c.rs` ConsumeScope arm:
  ```rust
  // Level 1: per-type WithExitTimeout impl lookup
  let level_1_sym = format!("Nova_{}_method_exit_timeout_ms", type_name);
  if self.method_overloads.contains_key(&level_1_sym) {
      self.line(&format!("{} = (int){}({});", timeout_var, level_1_sym, c_binding));
  } else if self.effect_schemas.contains_key("Application") {
      // Level 2 (как сейчас)
  } else {
      // Level 3 (как сейчас)
  }
  ```
- Type-check `types/mod.rs`: при `impl WithExitTimeout for T` —
  register `exit_timeout_ms` method в method_overloads.

**Tests:**
- `nova_tests/plan110/timeout_level1_with_exit_timeout_t3_7.nv` (NEW):
  type implements `WithExitTimeout` returning 100ms; verify Level 1
  wins over Application 5000ms + Level 3 5000ms.

**Acceptance:**
- A110.9.2.a: per-type override beats Application + Level 3.
- A110.9.2.b: type without impl falls through to Level 2/3.

---

### 110.9.3 — Application `register_finalizer` LIFO runtime (~½ дня)

**Что:** Application handler maintains stack of registered finalizers.
On `with Application = ... { body }` exit — fire stack in reverse order.

**Files:**
- `compiler-codegen/src/codegen/emit_c.rs`: per-`with Application` block:
  - Emit local `NovaFinalizerStack _finalizers;` (initialized empty).
  - `Application.register_finalizer(f)` dispatch — push to `_finalizers`.
  - At with-block exit (both normal + throw paths): iterate
    `_finalizers` в reverse, invoke each.
- `compiler-codegen/nova_rt/effects.h`: `NovaFinalizerStack` struct
  + push/pop/iterate helpers (small fixed-size array, grow if needed).

**Tests:**
- `nova_tests/plan110/application_finalizer_lifo_t8_5.nv` (NEW):
  register A, B, C → verify execution order C, B, A.
- `nova_tests/plan110/application_finalizer_throw_path_t8_6.nv` (NEW):
  throw inside with-block → finalizers still fire.

**Acceptance:**
- A110.9.3.a: LIFO order verified.
- A110.9.3.b: finalizers fire on throw path.
- A110.9.3.c: D195 R8 abort/SIGKILL documented как OS limitation.

---

### 110.9.4 — `W_FFI_CANCEL_UNSAFE` lint enforcement (~2-3ч)

**Что:** Parser сохраняет `cancel_safe_attr` ✅. Lint check который fires
warning на call к non-cancel-safe FFI из on_exit body — NOT done.

**Files:**
- `compiler-codegen/src/lints.rs`: new lint pass `lint_ffi_cancel_unsafe`:
  - AST visitor с stack tracking «inside on_exit body?» — push on
    `Stmt::ImplMethod { name: "on_exit", ... }` (Consumable impl).
  - On `ExprKind::Call` to a fn that is:
    - `external fn` (from `external_fn_decls` registry)
    - AND NOT marked `cancel_safe_attr=true`
    → emit `W_FFI_CANCEL_UNSAFE` с suggestion message.

**Tests:**
- `nova_tests/plan110/neg_ffi_cancel_unsafe.nv` (NEW): negative fixture
  where FFI fn без `#cancel_safe` called from on_exit — expected warning
  W_FFI_CANCEL_UNSAFE.
- `nova_tests/plan110/cancel_safe_no_warning.nv` (NEW): FFI с
  `#cancel_safe` — no warning.

**Acceptance:**
- A110.9.4.a: W_FFI_CANCEL_UNSAFE fires.
- A110.9.4.b: `#cancel_safe`-marked FFI silent.
- A110.9.4.c: FFI call вне on_exit — silent (no warning).

---

### 110.9.5 — `on_exit` strict signature check (~½ дня, рискованный)

**Что:** Currently `validate_on_exit_signature` проверяет только param
count. Spec D188 требует точную signature `(self, outcome ScopeOutcome) Fail[E] -> ()`.

**Под-задачи:**

**110.9.5.a — Fix `-> ()` parser variance (foundational):**

В AST `-> ()` представляется как `Tuple([])` ИЛИ `Named("unit")` в
зависимости от context (8 fixture'ов failed раньше из-за этого). Need
canonical form в parser.

- `compiler-codegen/src/parser/mod.rs`: при parsing return type, если
  `()` → emit canonical `Named("unit")`. Update other parse sites
  consistently. Migration audit на existing fixtures.

**110.9.5.b — Strict signature check (after .a):**

В `compiler-codegen/src/types/mod.rs validate_on_exit_signature`:
- Param 0: must be self-receiver (already checked).
- Param 1: must be `ScopeOutcome` (already checked).
- Return type: must be `()` или `Fail[E] -> ()` (NEW check).
- Effects: must contain at most `Fail[E]` (NEW check).
- Generic params: forbidden (NEW check).

**Tests:**
- `nova_tests/plan110/neg_on_exit_wrong_return.nv` (NEW).
- `nova_tests/plan110/neg_on_exit_extra_effects.nv` (NEW).
- Existing 36 fixtures NOT regress (verify post-fix).

**Acceptance:**
- A110.9.5.a: parser canonical `-> ()` representation.
- A110.9.5.b: strict signature check passes valid + rejects invalid.
- A110.9.5.c: zero regression на existing fixtures.

**Risks:**
- Parser variance fix может вызвать cascade failures на широком
  fixtures-base. Recommend running `nova test nova_tests/` (full)
  после 110.9.5.a + comparing failures vs baseline.

---

## Финализация

### 110.9.6 — Plan 110.9 close + spec amend

- Update spec D188 / D192 / D195 / D201 sections: «✅ V1.1 production-grade
  closure complete, no remaining followups».
- Update `docs/plans/110-artifacts/plan110_8_8_umbrella_close.md`
  marking 5 V1.1 followups as CLOSED.
- Memory `project-plan110-status.md` flip to «V1.1 closed».
- Discussion-log entry: «Plan 110.9 V1.1 closure shipped».
- simplifications.md: 5 markers → CLOSED.

## Тесты — суммарно

5 new fixtures (1 per sub-task) + 4 negative (110.9.4 + 110.9.5 family).
Total ~9 new fixtures. Regression baseline: plan110/ 36/36 (existing)
+ syntax/ 53/1 (preserved).

## Workflow rules (per Plan 110 header)

* Каждое sub-task — atomic-merge с working tests + acceptance verified.
* Commit per task (5+ commits в branch plan-110-v1.1).
* No silent simplifications — если что-то extracted, explicit followup
  marker.
* Spec D-blocks updated в той же atomic commit что и impl.
* Production-grade per Plan 110 header: ВСЁ без упрощений.

## Зависимости

* Plan 110 V1 landed (merge 874f5766ca5) ✅.
* nova_tests infrastructure stable ✅.
* No external blockers.

## Trigger conditions (recap)

Plan 110.9 stays 📋 PLANNED until:
1. W_FFI_CANCEL_UNSAFE lint absence catches a real production bug.
2. User explicitly requests V1.1 closure.
3. WithExitTimeout protocol needed в реальном stdlib type.
4. finalizer LIFO replay needed для production scenario.
5. CleanupTimeoutError typed catch needed в user code.

Когда trigger fires — spawn worktree `D:/Sources/nv-lang/nova-p110-v1.1`
branch `plan-110-v1.1`, исполнить 5 sub-tasks sequentially.

## Cross-references

- Plan 110 umbrella: [110-scoped-resources-radical-simplification.md](110-scoped-resources-radical-simplification.md).
- V1 close: [110-artifacts/plan110_8_8_umbrella_close.md](110-artifacts/plan110_8_8_umbrella_close.md).
- Spec: D188, D192, D195, D201 (после V1.1 → fully spec-compliant).
- Discussion-log: nova-private/discussion-log.md (Plan 110 closure entry).
