# Plan 169.1.2 — Консолидация positive-тестов (по плану / по группе сути)

> **Под-план [Plan 169.1](169.1-test-suite-profiling-and-speedup.md)** (ось «скорость»).
> **Создан:** 2026-06-19. **Статус:** 📋 proposed.
> **Приоритет:** P1 — лучший ROI по CU/час (механика, низкий рантайм-риск).
> **Выигрыш (базовый):** −239 CU (39 pos-only мульти-папок → folder-module).
> **Выигрыш (тематический):** потенциально больше (см. Уровень 2).

## Контекст

Перепись 169.1: 72 мульти-module папки. Категория (A) — 39 pos-only,
не cross-resolve → консолидируемы. Метод базовый — ordinal-suffix rename
(как Cat-B, `scripts/catb_convert.py`).

## Обновление 2026-06-19 — Plan 170 merged: `priv(file)` доступен

[Plan 170](170-file-private-visibility.md) ✅ CLOSED+MERGED (main merge `1298c057`)
→ **`priv(file)` — основной механизм разрешения коллизий** для Уровня 1 И Уровня 2
(вместо ordinal-suffix rename): top-level `fn`/`const` и `type` без методов
помечаются `priv(file)` → ноль rename, имена читаемы, происхождение видно по файлу.

**Ограничения (граница 170 — учитывать при слиянии):**
1. **`priv(file)` типы С методами** НЕ дискриминируются по файлу (коллизия
   метод-символа `Nova_<T>_static_<m>`) → для одноимённых типов-с-методами в
   разных файлах: ordinal-rename типа ИЛИ не сливать. `fn`/`const`/тип-без-методов — `priv(file)`.
2. **`fn main()`** не уживаются в одном модуле (один entry на модуль) → папки из
   main-based тестов (напр. `plan67`, 14× `main`) сначала конвертировать
   `main`→`test "…"`, либо не сливать. Прямо сливаются только `test`-блок-файлы.
3. **negative** (`EXPECT_*`) и **cross-resolve** в слияние НЕ включать (как было).

**Подход:** пилот на одной папке (валидировать `priv(file)`-слияние end-to-end) →
затем масштабировать. Первый пилот: `plan144_0` (10 CU→1, коллизия `drive` ×2).

## Стратегия — два уровня

### Уровень 1 (базовый): папка плана → один folder-module

Каждая `plan_NN/` с несколькими positive-файлами (каждый свой `module`) →
объединить в `module nova_tests.plan_NN`. Конфликты имён разрешать
ordinal-suffix rename. Крупнейшие: `plan91_12` (21), `plan67` (14),
`plan103_2`/`plan91_15` (11), `plan144_0` (10).
- **−239 CU**, механически, готовым скриптом.

### Уровень 2 (агрессивный): группа сути → один модуль

**Замысел (запрос 2026-06-19):** не всегда есть смысл держать модуль на каждый
номер плана. Если несколько planNN относятся к ОДНОЙ тематической группе
(синтаксис, файберы/concurrency, протоколы, контракты, …) — их можно слить в
ОДИН модуль по сути теста, а не по номеру плана:
- `plan55`/`plan56`/syntax-planNN → `module nova_tests.syntax`
- concurrency-planNN (plan83*/plan103*) → `module nova_tests.concurrency`
- protocol-planNN → `module nova_tests.protocols`

→ десятки plan-папок одной темы схлопываются в единицы модулей. Выигрыш по CU
существенно больше Уровня 1.

**Сохранение связи «тест ↔ план» через имя файла (решение 2026-06-19).**
folder-module объединяет файлы по одинаковой `module`-директиве, а **имена
файлов произвольны**. Поэтому при слиянии в `module nova_tests.<theme>` файлы
именуются с **префиксом плана**: `plan55_<name>.nv`, `plan56_<name>.nv`. Тогда:
- 1 CU на тему (выигрыш),
- происхождение видно в имени файла (`nova_tests/syntax/plan55_closure.nv`),
  раннер показывает путь при падении → навигация цела.

Механика: завести папку темы (`nova_tests/syntax/`), перенести positive-тесты
из `plan55/`, `plan56/`… с префиксными именами, всем — `module nova_tests.syntax`.

**Бонус:** префикс плана применять и к top-level символам (`plan55_helper`
вместо `helper`) → одновременно разрешает конфликты имён И кодирует
происхождение в имени символа.

**Остаточная цена Уровня 2:**
- **Конфликты top-level имён** между планами в одном namespace. Два варианта:
  (а) **`priv(file)`** ([Plan 170](170-file-private-visibility.md)) на helper'ах
  — ПРЕДПОЧТИТЕЛЬНО: ноль rename, имена читаемы; делать Plan 170 ДО консолидации.
  (б) префикс плана у символов (`plan55_helper`) — если 170 ещё не готов.
- **cross-resolve и negative** в группу НЕ включать (категории B/C/D из 169.1).
- Нужна **таксономия тем** (какие planNN в какую группу) — отдельное design-
  решение. Уровень 1 от неё не зависит → делать Уровень 1 первым.

## План работ

1. **Ф.1 (Уровень 1):** прогнать `scripts/catb_convert.py` (или расширенный) по
   39 pos-only папкам категории (A). Skip: cross-resolve (B), negative-heavy (C),
   partial (D — отдельно в [169.1.3](169.1.3-consolidate-partial-d.md)).
   Регресс затронутых папок (0 новых FAIL). **−239 CU.**
2. **Ф.2 (Уровень 2, опц.):** составить таксономию тем; слить plan-папки одной
   темы в `module nova_tests.<theme>`. Замерить дополнительный −CU vs цену
   навигации. Решение go/no-go по данным.

## Замечания

- Уровень 1 — чистая выгода, низкий риск, рекомендуется безусловно.
- Уровень 2 — экспериментальный; делать только если −CU существенно перевешивает
  потерю co-location «тест ↔ план». Возможно частично (только крупные группы:
  syntax, concurrency).
- Не трогать: 674 compile-error (жёсткий пол), cross-resolve группы (B).

## Followup-маркеры

`[M-169.1.2-consolidate-l1]` (Уровень 1, −239 CU),
`[M-169.1.2-consolidate-l2-theme]` (Уровень 2, таксономия + слияние по теме).
