<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Plan 172.4 — Единый value-ABI + автоматический placement (by-ref / heap↔stack)

**Статус:** ✅ CLOSED 2026-07-04 (correctness-core Ф.1-Ф.3 = owner-acceptance выполнен: fluent `mut @ -> @` + структурное `==` + единый receiver-ABI VR+NT; §0-консолидация предиката f8d6bfaa). P3-perf Ф.4-Ф.6 ВЫНЕСЕНЫ → [172.14](172.14-value-abi-perf-placement.md) (решение владельца «отложить P3, закрыть 172-умбреллу»).
**Umbrella:** [Plan 172 — Переработка компилятора: типовая система](172-compiler-rework.md).
**Источник:** [`spec/open-questions.ru.md` Q-value-abi-auto-placement](../../spec/open-questions.ru.md) +
[Q29 RESOLVED](../../spec/open-questions.ru.md) (explicit `ref T` отвергнут → авто).
**Deps:** **172.1** (единый носитель `ResolvedType`/[D315](../../spec/decisions/02-types.md#d315-resolvedtype--единый-канонический-носитель-типа-plan-1721-2026-06-21)
должен стать authoritative — U.4/U.5/U.6). **Старт ПОСЛЕ MVP-консолидации 172.1** (172.1 byte-identical;
172.4 — behavior-changes поверх единого носителя).
**Приоритет:** P1 (корректность value-records + перф — §2 «быстрый сгенерированный код»).

---

## 1. Цель

Компилятор обращается со всеми value-типами **единообразно** (один путь representation +
приёмник + передача) и **автоматически** выбирает передачу/размещение (by-ref vs by-value,
heap vs stack) — без явных аннотаций ([Q29](../../spec/open-questions.ru.md): нет `ref T`). ABI/placement
**выводятся** из `ResolvedType` + escape/recursion-анализа (D315: «ABI выводится, не хранится»),
а не разбросаны по веткам codegen.

## 2. Acceptance-критерий (от владельца, 2026-06-21)

ЭТО должно компилироваться, запускаться и проходить:

```nova
type Point value { x int, y int }
fn Point mut @inc(a int) -> @ { @x += a; @y += a; @ }

mut p Point = { x: 0, y: 0 }
ro p2 = p.inc(10).inc(20)
assert(p == p2 && p.x == 30)
```

**Сейчас НЕ работает** (диагностика 2026-06-21, release nova на ветке plan-172):
1. **Fluent `mut @ -> @` на value-record** → CC-FAIL: codegen путает `NovaValue_Point` (by value)
   и `NovaValue_Point*` (by pointer) в цепочке (`initializing 'NovaValue_Point *' with
   'NovaValue_Point'`). Heap-record (`type Point {}`) тот же паттерн **проходит** → пробел
   value-record-специфичен.
2. **`==` на value-record** → CC-FAIL: `==` на C-структуре. Heap-record `==` компилится, но это
   **reference-eq** (два разных `{1,2}` → не равны). Sum-типы имеют авто-структурное `==`
   (tag+payload, [02-types.md](../../spec/decisions/02-types.md) ~:6797) — value-records НЕ имеют.

## 2.1. Подход (важно — §0/§10): НЕ патчить старый codegen, чинить ПОСТРОЕНИЕМ

Формулировка владельца (2026-06-21): **«поломано в СТАРОМ компиляторе; главное — чтобы работало
в НОВОМ»**. Эти CC-FAIL'ы — артефакт **фрагментированного** value-ABI (раздельные пути `NovaValue`
/ `NovaTuple` / heap-record — источник постоянных багов). **Запрещено** чинить их band-aid'ом в
старом codegen-пути ([`compiler-conventions.md`](../dev/compiler-conventions.md) §10-анти-паттерн
«углублять фрагментацию»; §0 «сокращать»). Правильно: единый носитель `ResolvedType` (172.1/D315)
даёт **ОДИН** value-ABI-лоуэринг, и acceptance-кейс проходит **ПО ПОСТРОЕНИЮ** — потому что
раздельности нет. То есть **Ф.3 (единый value-ABI) — первичная работа**; fluent `mut @` (Ф.1) и
структурное `==` (Ф.2) — её **следствия/acceptance**, а не отдельные заплатки. Зависимость на 172.1
(единый носитель authoritative) — жёсткая: без него «починка» свелась бы к патчу старого пути.

## 3. Фазы

- **Ф.1 — value-record fluent `mut @ -> @` работает (acceptance).** Рассинхрон `NovaValue_X` ↔
  `NovaValue_X*` в цепочке — следствие фрагментации; устраняется **единым value-ABI-лоуэрингом
  (Ф.3)**, а НЕ точечным патчем старого `NovaValue`-пути (§2.1). Регресс-гард = acceptance-кейс.
- **Ф.2 — value-record структурное `==` (+ дизайн ref-vs-structural).** Авто-вывод структурного
  `==` для value-records (tag/поля, как sum-типы), а не `==` на C-структуре. Дизайн-вопрос (D-блок):
  value-record `==` = **структурное** (они значения); heap-record `==` = текущее reference — оставить
  или тоже структурить? → решить spec-first.
- **Ф.3 — единый value-ABI representation.** Свести `value-record` / `named tuple` / `Tuple(int,str)`
  к ОДНОМУ пути representation + приёмника. Убрать раздельность (named-tuple `ro @` by-value vs
  value-record by-pointer — [Plan 128](128-mut-receiver-abi.md)). Вектор — 172.1 U.5/U.6 (свернуть
  tuple/value-record/sum-schema дубли); 172.4 завершает на ABI-уровне.
- **Ф.4 — авто-by-ref передачи параметров.** Большие value передавать указателем автоматически,
  когда копия не наблюдаема (закрывает ЗА-аргумент Q29 без `ref T`). Foundation: `escape_analyze.rs`
  (Plan 127) уже авто-промоутит value-records — обобщить на by-ref-передачу.
- **Ф.5 — авто heap↔stack placement, в т.ч. нерекурсивный sum→стек.** Sum по умолчанию heap
  (`Nova_X*`) — правильно для **рекурсивных** (деревья/связи); **нерекурсивные/перечисления** →
  авто-свёртка в **стек-value (tagged-union)**, как билтин Option (`NovaOpt` stack) vs Result (heap),
  [Plan 62.A.bis](62.A.bis-sum-schema-registry.md). Детектор рекурсии есть (size/GC,
  [08-runtime.md](../../spec/decisions/08-runtime.md) ~:8211). Консервативно: рекурсия/неопределённость
  → heap (Plan 127 over-promote stance).
- **Ф.6 — copy-elision: RVO/NRVO + move-on-last-use для value-типов** (перф; добавлено 2026-06-26
  по итогам обсуждения с владельцем). Свежие/уникальные value-значения (результат `return`, литералы,
  временные), у которых escape-анализ (Plan 127) доказал **отсутствие второго владельца**, **строятся
  прямо в слоте-назначении** (RVO/NRVO через скрытый `sret`-указатель) либо **перемещаются на
  последнем использовании** (move-on-last-use) — вместо `memcpy`. **Прозрачно** (для `value`-типов
  копия ≡ move наблюдательно: нет идентичности; heap — moot, by-ref). **БЕЗ нового синтаксиса** (вывод,
  как Rust/Swift; НЕ `T&&`/`tmp`-маркер): едет на ОБЫЧНОМ by-value параметре, на `consume` (там даже
  move-`memcpy` не нужен), и на `return`. **Ортогонально `consume`:** `consume` = контракт владения
  (affine); это — оптимизация копий БЕЗ контракта (владелец 2026-06-26 развёл оси «владение» vs «не
  копировать лишнее»). **Обобщает D326 R7/R8** (`-> @` value-record decay = частный случай). GC проще
  C++: нет деструкторов → move = чистый перенос байтов, без moved-from-состояния (нет move-ctor).
  **Инвариант** (узкий D-блок, если понадобится): компилятор ВПРАВЕ move'ить доказанно-уникальные
  value-временные; программа НЕ зависит от идентичности копии (для value-record уже истинно).
  Маркер `[M-172.4-rvo-move-on-last-use]`. **Приоритет P3** (перф; важно для крупных value-record,
  мелкие/heap ≈ ноль); делать ПОСЛЕ Ф.3-Ф.5 (единый ABI), мерить blast-radius. **Корректность не
  затрагивает** — чистая оптимизация.

## 3.5. ГРАНИЦА ФАЗ: correctness-core ✅ vs P3-perf-tranche (2026-07-04)

**Ф.1-Ф.3 ЗАВЕРШЕНЫ** — это owner-acceptance-критерий (2026-06-21: fluent `mut @ -> @` +
структурное `==`), плюс полная унификация receiver-ABI (VR+NT single pointer-carrier).
Корректность value-типов достигнута: fluent-цепочки, структурное равенство, консистентный
receiver — всё работает, гейты зелёные.

**Ф.4-Ф.5-Ф.6 — отдельный P3-perf-tranche** (все три помечены P3 в плане; «корректность НЕ
затрагивают»). Blast-radius замер (§7, 2026-07-04):
- value-record деклы: **117**; value-типы как параметры (by-value кандидаты): **~180**;
  named-tuple: **4**.
- **Ф.4** (авто by-ref) меняет ABI ~180 param-сайтов: сигнатура + call-site (взятие адреса) +
  тело (deref) + escape-доказательство «копия не наблюдаема» на КАЖДЫЙ. Масштаб = keystone-класс
  A6, но ×180 вместо receiver. Foundation escape_analyze.rs (Plan 127) промоутит value-records
  на heap при escape — обобщение на by-ref-передачу требует нового анализа «второго владельца».
- **Ф.5** (нерекурсивный sum→стек) — поведенческий, трогает sum-representation широко;
  детектор рекурсии есть, но свёртка heap→stack-value для каждого нерекурсивного sum = широкий радиус.
- **Ф.6** (RVO/move-on-last-use) — чистая оптимизация, P3, «после Ф.3-Ф.5».

**Рекомендация (§2 soundness, §7 «не рашить крупный ABI в хвосте»):** P3-tranche — измеренный, но
самостоятельный focused-эффорт с полным blast-radius + detect-режимом, НЕ автопродолжение в хвосте
марафона. 172.4 correctness-core закрывает owner-acceptance; P3-perf может идти отдельным заходом.

## 4. Сквозные критерии (как 172.1)

1. Соответствие [`compiler-conventions.md`](../dev/compiler-conventions.md) §0/§1/§2 + §6 (ошибки понятны)
   + §2 (быстрый сгенерированный код — это и есть мотив Ф.4/Ф.5).
2. Без упрощений (как для прода). Behavior-change → детект-режим/замер blast-radius ДО фатала (§7).
3. Pos+neg через релизный `nova` (C-codegen). Acceptance-кейс — регресс-гард.
4. 0 регрессий против чистого бинаря; spec-first для семантики (`==`, placement) — D-блок.

## 4.5. DESIGN-LOCK (2026-06-28, старт 172.4 — greenlight владельца, пивот с gated-172.1.2)

**Dep-статус (verified):** U.5 ✅ (`ResolvedType` — единственный носитель чекера, `Ty`/`TyCat`/`cat_of`
снесены); U.6 lowering-консолидация ✅ в основном (`resolved_type_to_c` ПОЛНЫЙ паритет U.4.6b `a6f645a3`;
3 type→C таблицы → 1; финальный `type_ref_to_c`-full-retire = endgame U.6.1, OUT of MVP но НЕ блокирует
value-ABI). ⇒ **единый ABI-лоуэринг (`resolved_type_to_c`) в месте** — proper-fix Ф.3 feasible, НЕ band-aid.

**Acceptance CC-FAIL'ы ВОСПРОИЗВЕДЕНЫ** (standalone `nova build`, `fn main`, обход test-harness/assert):
- **Ф.1 fluent** (`va.c:1778`): `NovaValue_Point* _nv_tmp = (*nova_self);` — `-> @` decay путает
  by-value (`NovaValue_Point`) ↔ by-pointer (`NovaValue_Point*`). Сайт: `-> @`/`=> @` mut-receiver
  return decay, **D181/Plan 91.7** (`emit_c.rs:~25292` «mut method returns @ (receiver pointer) for
  fluent chain» + value-used-as-value `:21973`). Корень — фрагментация receiver-ABI (Plan 128
  named-tuple by-value vs value-record by-pointer): цепочка `.inc(10).inc(20)` смешивает формы.
- **Ф.2 ==** (`va.c:1801`): `nova_bool eq = (p == p2);` — сырой C-`==` на `NovaValue_Point` struct.
  Корень: `emit_field_eq` (`emit_c.rs:12904`, §0 ЕДИНЫЙ structural-eq dispatch-by-C-type) **НЕ имеет
  `NovaValue_`-арма** → value-record падает в fallback `(l==r)`; И top-level `==`-лоуэринг value-record'а
  не роутит в структурный eq (в отличие от sum/record/Vec/HashMap — eq-консолидация simplifications.md).
- (StringBuilder-interp `consume_into_str→int` в репро — ОТДЕЛЬНЫЙ extern-return баг, НЕ value-ABI.)

**Подход (§2.1 — fix by construction):**
- **Ф.2 (первый срез, §0-clean, tractable):** РАСШИРИТЬ единый `emit_field_eq` арм'ом `NovaValue_<Name>`
  → структурный field-by-field eq (как record, но value-placement: поля из `record_schemas`/
  `value_record_names`), + роутить top-level `==` value-record'а в тот же диспетчер (НЕ raw C-`==`).
  Это РАСШИРЕНИЕ единого источника (§0), НЕ band-aid отдельным value-record-eq-путём. **Spec-first
  (D-блок):** value-record `==` = СТРУКТУРНОЕ (они значения — Q-value-abi, естественный ответ); heap-record
  `==` остаётся reference (отдельная ось — решить в D-блоке: оставить ref или тоже структурить).
- **Ф.1/Ф.3 (основная работа, после Ф.2):** единый value-ABI representation — свести
  value-record/named-tuple/`Tuple` к ОДНОМУ пути representation+receiver через `resolved_type_to_c`
  (он уже паритетен). `-> @` decay перестаёт путать by-value/by-pointer ПО ПОСТРОЕНИЮ (нет раздельности).
  Это behavior-change поверх единого носителя; де-риск §7 (detect → blast-radius → 0 регрессий vs чистый).
- **Acceptance-кейс → регресс-гард** (`nova_tests/`, после фикса D78-module-path: `module parent.target`).

**Первый implementation-срез:** Ф.2 (== structural для value-record) — bounded, §0-clean, spec-D-блок +
`emit_field_eq` NovaValue_-арм + top-level-`==`-routing. **✅ ЗАЛЭНДЖЕН 2026-06-28 (`4c375169`, D328):**
0-NEW-FAIL + **починил 13 pre-existing** (вся net_v2/net-семья plan91_12/15/16 — это были value-record
`SocketAddr` ==/!= CC-FAIL'ы, §1210). PASS 477→490.

### Ф.1/Ф.3 (fluent value-record `mut @ -> @`) — ДВА interacting блокера (разведано 2026-06-28)
Acceptance `mut p Point = {0,0}; ro p2 = p.inc(10).inc(20); assert(p == p2 && p.x == 30)` упирается в
ДВА **независимых** codegen-блокера (оба — value-record-vs-heap ABI-раздельность, §2.1 fix-by-construction):

1. **Self-return CC-FAIL (`inc` тело):** `NovaValue_Point* _nv_tmp = (*nova_self)` — return-тип `-> @`
   (`receiver_c_type` → `NovaValue_X*` ВСЕГДА, независимо от mut, emit_c.rs:11939) = pointer, но
   SelfAccess-decay (emit_c.rs:22061) для value-record даёт `(*nova_self)` (value) → рассинхрон
   infer(pointer)/emit(value). **ФИКС (разведан, рабочий, НЕ закоммичен):** return-site-scoped флаг
   `in_recv_ptr_return_position` (set в `emit_return_expr` когда `ret_c == var_types["nova_self"]` —
   оба `NovaValue_X*`), в SelfAccess → `nova_self` (pointer). Применять в ОБОИХ body-emit путях:
   `emit_block_stmts` (emit_c.rs:18291, non-generic emit_fn) + 3 mono/method-копии (`emit_monomorphized_fn`/
   `_method`/`emit_generic_method_erased`). **Устраняет CC-FAIL** (проверено: компилируется).

2. **Chain-materialization КОПИЯ (chain_norm):** `p.inc(10).inc(20)` нормализуется (`chain_norm.rs:542`,
   `_chain_root_N_<field> = root`) в `{ let _chain_root = p; _chain_root.inc(10); _chain_root.inc(20);
   _chain_root }`. Для value-record `p` (value) `_chain_root = p` = **КОПИЯ** → обе мутации на копии,
   оригинал `p` нетронут → runtime `p≠p2 && p.x≠30`. chain_norm doc ПРЯМО признаёт «Value-type fields
   not handled — V2 followup» (chain_norm.rs:44); рассчитан на reference-roots (`@F`-pointer-copy =
   тот же heap-объект). **ФИКС (TODO):** value-record chain-root = **reference** (codegen эмитит
   `NovaValue_X* _chain_root = &p` + method-calls передают pointer напрямую + trailing decay-в-value
   при binding), ЛИБО chain_norm не нормализует value-record + codegen нитит return-pointer (но тогда
   prepare_method_recv не должен re-address уже-pointer return — double-ptr hazard). Интрикат.

**⚠️ ПОРЯДОК (§1 soundness):** блокер-1-фикс БЕЗ блокер-2-фикса превращает CC-FAIL (громкий) в
**silent-wrong-behavior** (мутация копии) — РЕГРЕССИЯ звучности. Self-return-фикс разведан/откатан (не
оставлять partial).

**🔑 §2.1 ВЕРДИКТ (проверено по конвенции — определяет ПОДХОД):** targeted-фиксы (self-return-флаг +
chain-root-pointer + trailing-deref + binding-decay) = **россыпь name-keyed special-cases на `_chain_root`**
= ровно **band-aid фрагментированного value-ABI, который §2.1 ЗАПРЕЩАЕТ** («НЕ патчить старый codegen;
единый носитель → acceptance ПО ПОСТРОЕНИЮ»). Корень обоих блокеров — ОДИН: value-record представлен
**НЕКОНСИСТЕНТНО** (receiver=`NovaValue_X*` ptr, local/value=`NovaValue_X` struct, chain_root=value-copy,
return=ptr, param=?) — это §10 «параллельные пути под одно». **ПРАВИЛЬНЫЙ Ф.1/Ф.3 = ЕДИНОЕ представление
value-record:** консистентный **pointer-carrier** во ВСЕХ внутренних контекстах (receiver/return/chain/
binding-как-alias) + **value-decay-коэрция** РОВНО в точках explicit value-position (binding к value-local,
передача в by-value param, по-значению поле). Тогда self-return/chain/binding работают ПО ПОСТРОЕНИЮ (нет
раздельности → нет рассинхрона). Это **существенный careful refactor** (value-record carrier model +
decay-точки), вектор — 172.1 U.5/U.6 единый носитель → 172.4 завершает на ABI-уровне (§3 плана Ф.3).
**НЕ targeted-патчи** (они band-aid, переделываются при унификации = §0 «целевая форма vs интерим»).
Гейт: detect172 + 0-CC-FAIL + 0-NEW-FAIL + acceptance runtime `p.x==30`; blast-radius §7 detect-mode
(ВСЕ value-record `-> @`/chains/bindings). Multi-session careful — НЕ рашить в хвосте сессии (§2 soundness).

**Ф.3 ПОПЫТКА (worktree-агент) 2026-06-28 — РЕВЕРТНУТА (regress-gated).** Multi-agent-агент реализовал
3 type-driven фикса (self-return-ptr флаг `in_recv_ptr_return_position`/`emit_return_expr`; chain_norm
skip value-record lvalue-корней; value-decay `value_record_let_needs_decay` ptr→value при binding) +
4-й (== уже в Ф.2). **Standalone acceptance прошёл (`eqx30`), но ПОЛНЫЙ регресс = 16 NEW fails**
(re-broke всю net-семью plan91_12/15/16, что Ф.2 ПОЧИНИЛ + plan144/148/96). **Корень:** value-decay
гейтнут на `infer_expr_c_type(chain) == NovaValue_X*`, но infer возврата `-> @` **context-fragile** —
в standalone-`fn main` резолвится (ptr → decay срабатывает), в **folder-module/net-контексте НЕ
резолвится** (value → decay НЕ срабатывает → `NovaValue_P p2 = inc(...ptr...)` CC-FAIL). Это ТОТ ЖЕ
receiver-inference-depth-гейт (172.1.2). **Урок:** (1) sample-валидация агента ПРОПУСТИЛА blast-radius
— **гейт = ПОЛНЫЙ регресс, не сэмпл** (§7.6); (2) **D-conformance-тест (`spec_tests/conformance/`)
поймал регрессию, которую сэмпл пропустил** — подтверждает методологию (§5.1 umbrella). **Re-attempt
требует:** decay на **robust emit-based сигнале** (флаг «RHS заэмитил value-record-ptr», ставится в
emit метода `-> @`), НЕ на context-fragile `infer`; + полный регресс + spec-тест `d326_value_record_
fluent.nv` (добавить ВМЕСТЕ с landing). Diff попытки: `/tmp/f3_final_diff.patch` (против main b12007b3).

**RE-ATTEMPT РАЗБЛОКИРОВАН (2026-07-04, после завершения 172.1):** корень реверта —
context-fragile `infer_expr_c_type` возврата `-> @` (в folder-module не резолвился) —
**УСТРАНЁН**: fluent-return теперь ДЕТЕРМИНИРОВАННО в канале
(`resolve_instance_method_return_arity`: `returns_receiver → Some(peeled)`, коммит
2026-07-03) + Channel 6j (value-form if) + канал строится post-normalize на
эмитируемом дереве. Точки реализации (сверено с кодом 2026-07-04):
- блокер-1: Stmt::Return эмиссия `emit_c.rs:19464` (+trailing-точка fn-body) —
  Cell-флаг `in_recv_ptr_return_position` при `ret_ty == var_types[nova_self] ==
  NovaValue_*\*`; SelfAccess-арм `:22598` при флаге → `nova_self` (ptr);
- блокер-2: chain-root — по §2.1-вердикту pointer-carrier (`NovaValue_X* root = &p`)
  либо skip-normalize value-record lvalue-корней + прямая цепочка (ptr нитится,
  prepare_method_recv БЕЗ re-address);
- гейт: ПОЛНЫЙ регресс + conformance + acceptance runtime (`p.x==30`) + spec-тест
  `d326_value_record_fluent.nv` ВМЕСТЕ с лэндингом; §1-порядок: блокеры ТОЛЬКО
  связкой (partial = silent-wrong).

**Ф.4/Ф.5/Ф.6** (авто by-ref / heap↔stack / RVO) — после Ф.3 unified representation, как в плане.

## 4.7. Ф.3 ПРОГРЕСС АТОМОВ (2026-07-04)

- **A1 ✅** `return self` зеркало SelfAccess return-позиции (ptr при `-> @`).
- **A2** — свёрнут в keystone A6 (Self-арм канала: mut-NT покрывается A6).
- **A3 ✅** user `@equal` для value-record — per-type by-value обёртка `nova_vr_ueq_<T>` (двухфазная эмиссия: прото в opt-typedef зону, тело в NOVAOPT_EQ_FNS). Fixture `d237_value_record_user_equal.nv`.
- **A4 ✅** `NovaTuple_`-арм в `emit_field_eq` — структурный `==` для named-tuple (user @equal + field-by-field). Fixture `d215_named_tuple_eq.nv`.
- **A5** — свёрнут в keystone A6 (binding-decay NT недостаточен без согласованного pointer-carrier; SelfAccess-deref одной точки ломает method-body). Fixture `d215_named_tuple_fluent.nv` → inprogress до A6.
- **A6 ✅** keystone (2026-07-04): NT receiver always-pointer (D226-модель). Координированный атом по карте §4.6 (ultracode-разведка 6 агентов): receiver_c_type NT ro→ptr + prepare_method_recv (recv_mutable-гейт снят) + SelfAccess emit/infer decay-когерентность + fluent_self_ret/binding-decay для NT + unit-матрица (2 теста ro→pointer). ИНВАРИАНТ сохранён: NT как ЗНАЧЕНИЕ (поле/параметр/return-не-Self/Vec-элемент) остаётся by-value (type_ref_to_c неизменён — D215). Fixture d215_named_tuple_fluent (mut+ro chain). Ф.3-раздельность (receiver-ABI) для VR+NT УСТРАНЕНА — обе категории на едином pointer-carrier receiver + value-decay в value-позициях. Остаток Ф.3: anon-tuple Self недостижим (N/A); mono-Self by-value (карта строка 6b) — отдельный follow-up при появлении фикстуры.

## 4.6. Ф.3 КАРТА РАЗДЕЛЬНОСТИ (ultracode-разведка 2026-07-04, 6 агентов, код перепроверен)

## Ф.3 — Единая карта раздельности value-ABI (verify-свод, код перепроверен в nova-p172)

Легенда: **VR** = value-record `NovaValue_X`, **NT** = named tuple `NovaTuple_X`, **AT** = анонимный tuple `_NovaTuple_N_…`, **HR** = heap-record `Nova_X*`. Все строки ниже перепроверены чтением emit_c.rs (коммит-состояние worktree nova-p172); строки без пометки — подтверждены.

### 1. Сводная карта

| # | Контекст | VR | NT | AT | HR | Сайты |
|---|----------|----|----|----|----|-------|
| 1 | Receiver-сигнатура, ro | `NovaValue_X*` всегда (D226) | `NovaTuple_X` **by-value** | недостижимо (ветки нет → fallback `Nova_<name>*`) | `Nova_X*` | 12061-12196; mono 12099-12100 |
| 2 | Receiver-сигнатура, mut | `NovaValue_X*` (mut-инвариант) | `NovaTuple_X*` (D215 amend) | — | `Nova_X*` (mut-инвариант) | 12183-12191, 12194; тесты 44871-44957 |
| 3 | Call-site адаптер, lvalue | `&(obj)` | mut: `&(obj)`; ro: pass-through copy | pass-through (`_Nova…` не матчит префиксы) | pass-through ptr | 36846-36887, 36819-36828 |
| 4 | Call-site адаптер, rvalue | hoist `tmp; &tmp` | mut: hoist; ro: copy | pass-through | pass-through | 36863-36883 |
| 5 | Promoted-binding как receiver | pass-through (`ends_with('*')` guard) — **прецедент pointer-carrier** | promotion не распространяется | — | — | 36857/36869 guard; 31962-31996 |
| 6 | C-return `-> Self`/`-> @` | non-generic: `NovaValue_X*`; **mono: `NovaValue_<short>` by-value** | mut: `NovaTuple_X*` (4045) / **`NovaTuple_X` через resolved_type_to_c (2264 хардкод `false`)**; ro: value | N/A | `Nova_X*` | 4042-4046, 2263-2264, 2341-2342, 35246-35253 |
| 7 | Return `-> X` явный | `NovaValue_X` by-value | by-value | by-value (+stash 13126) | `Nova_X*` | 4050-4053, 18632, 19535 |
| 8 | `@` в return-позиции | ptr при гейте `fluent_self_ret` (sv==ret_ty), иначе `(*nova_self)` | bare `nova_self` (гейт только NovaValue_) | N/A | bare ptr | 22626-22649, 19503-19507, 18603-18616 |
| 9 | `self` (Ident) в return-позиции | **всегда `(*nova_self)` — гейт НЕ проверяется** | bare | N/A | bare ptr | 20448-20459 |
| 10 | Let-binding fluent-результата | Ch1 (fn_ret_by_span) → ptr-АЛИАС; аннотация/Ch2-3 → value + deref-decay по `fn_ret_*` | **decay-зеркала нет** (гейт 18806 только NovaValue_) | destructure by-value | ptr | 18806-18816, 37260-37268, 37297-37351 |
| 11 | Escape-promotion (`&v`-sinks) | третья форма: `NovaValue_X*` на heap | не промоутится | не промоутится | N/A | escape_analyze.rs 347-599; 18746-18786, 31962-31996, 21379-21394 |
| 12 | Chain-рецепция | ptr течёт сквозь без decay/re-take | mut &/hoist; ro copy | pass | pass | 36846-36887 |
| 13 | unit-domination if/match | значение дропается до decay-точки | так же | так же | так же | 29494-29539, 43426, 40280 |
| 14 | Обычный параметр | by-value copy | by-value | by-value | ptr | 12290-12316, type_ref_to_c |
| 15 | Поле (HR/VR/NT контейнер) | inline by-value — единый канал | inline | inline | ptr-поле | 10936-11070 |
| 16 | Sum-payload | inline by-value (сам sum всегда heap); late-emission | inline; late | inline; **early** (другой буфер) | ptr | 11072-11220, 36260-36280 |
| 17 | Vec-элемент, legacy NovaArray | **box → ptr-в-nova_int** (26024-26030) | **push НЕ боксит** (else 26035, struct-в-nova_int); box только на literal-пути 32537-32551 | box на literal-пути | ptr-в-nova_int cast | 26024-26035, 32537-32551, 10218 |
| 18 | Vec-элемент, typed D239 | inline natural repr | inline | inline | ptr-буфер | 2103-2131, 32601-32618 |
| 19 | `==`/`!=` → emit_field_eq | арм есть (13227-13268); **user-@equal 13235 = `c_name(l,r)` без `&`** при receiver-ABI `NovaValue_X*` | **арма НЕТ → memcmp 13570** (unsound f64/padding/вложенные) | 2 арма (mono/legacy) | полный арм 13301-13524 | 20975-21050, 13227-13570 |
| 20 | `.clone()` | только derive/user (identity-арм 26134 гейтит Nova_+`*`) | как VR | пути нет | user либо identity-арм | 26114-26148 |

### 2. Противоречия и уточнения (перепроверено)

1. **Единственная фактическая неточность агентов** — строка «Vec-элемент legacy»: NT якобы боксится при push. По коду box в push-пути (26024) есть только для `nova_str` и `NovaValue_`; `NovaTuple_X`-аргумент падает в else (26035) → `nova_array_push_nova_int(arr, <struct>)` — не box, а несовместимый вызов (gap, не поведение). Box для NT существует только на array-literal-пути (32537-32551, тесты 45172+).
2. **Upgrade PLAUSIBLE→CONFIRMED (статически)**: `emit_field_eq` NovaValue-арм, ветка user-@equal (13235) эмитит `sig.c_name(l, r)` by-value, тогда как любой instance-метод VR (включая synthesized `equal`) имеет receiver `NovaValue_X*` (12189-12191) — C-type mismatch без `&`-адаптации/hoist. Runtime-repro не гонялся (worktree read-only) — нужен фикстурный гейт.
3. **Мнимое противоречие** строк «fluent Self» и «`@` в return» про mut-NT: не противоречие, а два разных канала. Через fwd-decl (4045) mut-NT fluent = `NovaTuple_X*` и bare `nova_self` консистентен; через `resolved_type_to_c` Self-арм (2264, хардкод `receiver_c_type(recv, false)`) тот же метод числится `NovaTuple_X` — рассинхрон каналов, страдает только NT (VR/HR mut-инвариантны).
4. Хардкод 2264 и mono-value-return (2341-2342, 35246-35253) подтверждены дословно.

### 3. (а) Контексты, УЖЕ соответствующие модели «pointer-carrier внутри + value-decay в value-позициях»

- **Receiver-ABI VR** (D226 always-pointer, mut-инвариант) + **generic value mono receiver** (12099) — carrier.
- **prepare_method_recv** — обе decay/re-take точки работают: `&(lvalue)` / hoist rvalue (36857-36868).
- **ValueHeapPromoted** — полный прецедент: биндинг живёт как `NovaValue_X*` весь lifetime, в receiver-позиции pass-through без адаптации (36857 guard).
- **Chain-рецепция** — ptr течёт сквозь цепочку без деградации.
- **`@` в return-позиции** (блокер-1): гейт `sv==ret_ty` корректно отличает `-> @` (ptr) от `return Some(@)` (decay в payload).
- **Binding-decay аннотированного let** (блокер-2): deref по emit-сигналу `fn_ret_*`.
- **Value-позиции без утечки ptr**: обычный параметр, поля всех трёх контейнеров, sum-payload, явный `-> X` return — канонический by-value, decay «уже сделан».
- **Typed Vec-канал D239** — inline natural repr, целевая форма для элементов.

### 4. (б) Атомы унификации, от наименее рискованного (гейты: unit = cargo test emit_c; fixture = targeted `nova test-build/test`; поведенческий гейт = detect172 pos+neg; baseline = nova_tests byte-identical при kill-switch на том же бинаре)

| № | Атом | Изменение | Риск | Гейт |
|---|------|-----------|------|------|
| A1 | Зеркало `return self` | В Ident("self")-арм (20455) добавить проверку `in_recv_ptr_return_position` — дословное зеркало SelfAccess 22640-22646 | Минимальный (одна ветка, reserved-имя) | unit на оба зеркала; fixture fluent-VR `return self`; detect172 |
| A2 | Self-арм канала типов | 2264: `receiver_c_type(recv, false)` → реальный mut-флаг текущего метода (или снять NT-ветвление после A6) | Низкий: затрагивает только mut-NT (VR/HR mut-инвариантны) | unit-матрица receiver_c_type; fixture mut-NT fluent c let-binding; baseline |
| A3 | `&`-адаптация user-@equal для VR | 13235: hoist l/r в temp + передать `&` (receiver-ABI `NovaValue_X*`) — тот же шаблон prepare_method_recv | Низкий (одна ветка, сейчас CC-fail территория) | fixture VR c user `@equal` в `==` и во вложенной позиции (поле tuple, Option-payload); CC-pass |
| A4 | NT-арм в emit_field_eq | Добавить `NovaTuple_`-арм зеркально NovaValue-арму (user @equal by-value — для NT ro-ABI корректно; else рекурсия по record_schemas, схема уже зарегистрирована 11061) | Низкий; убирает memcmp-unsoundness | fixture NT `==` c f64 NaN/-0.0 полем + Option[NT]; существующие eq-тесты |
| A5 | Binding-decay для NT | Расширить гейт 18806 на `NovaTuple_` (mut-fluent возвращает `NovaTuple_X*`) | Низкий-средний (симметричен блокеру-2) | fixture mut-NT fluent let c аннотацией; baseline |
| A6 | **Keystone: NT receiver → always-pointer** | Убрать mut-ветвление NT (12183-12187 → всегда `*`), prepare_method_recv NT-ветка теряет `recv_mutable`-гейт, ro-decay в точках value-позиций — NT встаёт на модель D226; A2/A5 при этом схлопываются | Средний: единственная mut-чувствительная ABI в системе, много тестовых фиксаций (44871-44957) | полная unit-матрица; detect172 pos+neg; baseline на не-NT коде |
| A7 | Канал-унификация fluent-binding | Ch1 `fn_ret_by_span` для `NovaValue_X*`-возврата: выровнять с Ch2/3 (strip-`*` + deref-decay), убрать семантическую вилку ptr-алиас vs copy | Средний-высокий (меняет наблюдаемую семантику неаннотированных биндингов) | fixture: мутация через биндинг ДО/ПОСЛЕ; detect172; решение зафиксировать D-блоком |
| A8 | Mono fluent Self → ptr-carrier | register_mono Self-return (35247-35253) и 2341-2342: value-generic fluent сейчас by-value — выровнять с non-generic ptr-ABI либо D-блоком зафиксировать value-decay на mono-границе | Высокий (все mono call-sites) | fixture generic value-record fluent chain; полный detect172 |
| A9 | Legacy NovaArray → typed Vec (V2.1) | Устранить межканальное расхождение элементов (box-в-nova_int vs inline): миграция литералов/push на D239-канал; закрывает и NT-push-gap (26035) | Высокий, отдельный трек | typed-vec паритет-фикстуры; baseline поэтапно |

Порядок A1→A5 — независимые корректностные фиксы (можно параллельно, каждый со своим fixture-гейтом); A6 — keystone, после него A2/A5 ревизуются на упрощение; A7/A8 требуют D-блока с фиксацией семантики; A9 — отдельный V2.1-трек.

Checkpoint-файл верификации: `C:\Users\B7E3~1\AppData\Local\Temp\claude\d--Sources-nv-lang-nova\a48a9f3a-0403-4a44-a6e3-8894781d4b88\scratchpad\f3map_verify.md`

## 5. Cross-refs

- Q-value-abi-auto-placement / Q29 (open-questions) — источник.
- D315 (ABI выводится); D228/D277/D290 (value-record stack); Plan 127 (escape→promote);
  Plan 128 (receiver ABI — раздельность под унификацию); Plan 62.A.bis (NovaOpt stack/Result heap);
  Plan 105 (explicit-base enum); 172.1 U.5/U.6 (единый носитель/representation).
- **D326** (`ref`-режим / `@`/`-> @`) — Ф.6 RVO **обобщает** его R7/R8 (`-> @` value-record decay).
  `consume` (D131/D133) — Ф.6 ортогональна (контракт владения ≠ copy-elision).
