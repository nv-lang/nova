// PROBE H13: `import` with NO path at all. Not the @push_if door -- the path is
// a `while` loop over Ident/Dot/DotDot/Slash that is allowed ZERO iterations,
// which is the same silent-accept shape reached by a different mechanism.
// site  : novac/src/parse/decls.nv:105-107
// novac : check rc=0, ZERO diagnostics; emit rc=0.
// oracle: rc=1, "error: expected identifier, got newline" at 9:7
module import_no_path

import

fn main() {
    println("ok")
}
