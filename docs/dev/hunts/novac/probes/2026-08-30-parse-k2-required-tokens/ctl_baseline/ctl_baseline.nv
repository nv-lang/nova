// CONTROL C1: the same shape written correctly. Both implementations accept.
// novac : check rc=0, ZERO diagnostics
// oracle: rc=0, "ok: ... PASS: 1  FAIL: 0"
// This is what makes rc=0 on the H-probes a FINDING and not a harness that
// always says yes.
module ctl_baseline

fn main() {
    println("ok")
}
