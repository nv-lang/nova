// PROBE H6: a `type` declaration with no name.
// spec/syntax.md:675 -- the table is "After `type Name` comes ...": the name is
// part of the form, the record brace is what comes AFTER it.
// site  : novac/src/parse/parse.nv:302 -- @push_if(kids, TokenKind.Ident)
// novac : check rc=0, ZERO diagnostics; emit rc=0.
// oracle: rc=1, "error: expected identifier, got `{`" at 9:6
module type_no_name

type { n int }

fn main() {
    println("ok")
}
