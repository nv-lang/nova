// PROBE H2: the NAME of a `mut` binding is missing.
// site  : novac/src/parse/parse.nv:896 -- @push_if(kids, TokenKind.Ident)
// novac : check rc=0, ZERO diagnostics. `novac emit` then writes
//             nova_int = = ((nova_int)1LL);
//         because emit_c.nv:796 reads the name as `leaf_text(kids[1])` and
//         slot 1 now holds the `=` leaf. clang -fsyntax-only on that line:
//         rc=1, "expected identifier or '('".
// oracle: rc=1, "error: expected pattern, got `=`" at 12:9
module bind_no_name

fn main() {
    mut = 1
    println("ok")
}
