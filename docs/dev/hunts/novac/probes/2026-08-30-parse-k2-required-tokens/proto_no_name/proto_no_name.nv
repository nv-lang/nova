// PROBE H11: a protocol method signature with no name after `@`.
// site  : novac/src/parse/parse.nv:540 -- @push_if(kids, TokenKind.Ident)
// novac : check rc=0, ZERO diagnostics; emit rc=0.
// oracle: rc=1, "error: expected identifier, got `(`" at 11:24
//
// The neighbouring position -- a METHOD name after `fn Type.` at parse.nv:666,
// same door, same token -- IS caught ("a fn declaration needs a name"), so the
// gap is per-call-site, not per-token.
module proto_no_name

type Sized protocol { @() -> int }

fn main() {
    println("ok")
}
