// PROBE H10: a sum-type variant introduced by `|` with no name.
// spec/syntax.md:742 -- "Sum-type variants ... After the variant name": a
// variant HAS a name; the three forms differ only in what follows it.
// site  : novac/src/parse/parse.nv:438 -- @push_if(vk, TokenKind.Ident)
// novac : check rc=0, ZERO diagnostics; emit rc=0.
// oracle: rc=1, "error: expected identifier, got `|`" at 9:17
module variant_no_name

type Color enum | Red | | Blue

fn main() {
    println("ok")
}
