// SPDX-License-Identifier: MIT OR Apache-2.0
// A method called with TOO MANY arguments, dotted form: which cause is named?
module basics.p_arity_dot

type Box { k int }

fn Box @take(a int) -> int => @k + a

fn main() {
    ro x = Box { k: 1 }
    println("n = ${x.take(1, 2)}")
}
