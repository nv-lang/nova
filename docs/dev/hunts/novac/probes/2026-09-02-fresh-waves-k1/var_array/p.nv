module p

type Kind enum Dot | Dash

fn label(k Kind) -> int {
    match k {
        Dot => 1
        Dash => 2
    }
}

fn main() {
    ro ks = []Kind.of(Dot, Dash)
    for k in ks {
        println(label(k))
    }
}
