module c8bind.m

// `??` inside a binding initializer, under a `+`.
fn pick(f bool) -> Option[int] {
    if f {
        return Some(3)
    }
    None
}

fn main() {
    ro k = (pick(false) ?? -1) + 1
    println(k)
}
