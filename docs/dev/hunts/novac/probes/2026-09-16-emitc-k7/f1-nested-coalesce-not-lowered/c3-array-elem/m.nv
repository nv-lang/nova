module c3arrayelem.m

// `??` as an element of an array literal.
fn pick(f bool) -> Option[int] {
    if f {
        return Some(3)
    }
    None
}

fn main() {
    ro xs = [pick(false) ?? -1]
    println(xs.len())
}
