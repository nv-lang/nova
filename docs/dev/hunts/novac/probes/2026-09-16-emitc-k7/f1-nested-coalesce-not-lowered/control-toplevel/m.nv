module controltoplevel.m

// CONTROL: the shape the corpus carries -- `??` IS the whole argument.
fn pick(f bool) -> Option[int] {
    if f {
        return Some(3)
    }
    None
}

fn main() {
    println(pick(false) ?? -1)
}
