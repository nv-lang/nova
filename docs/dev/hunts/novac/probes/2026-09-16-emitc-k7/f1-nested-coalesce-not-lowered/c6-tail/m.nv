module c6tail.m

// `??` inside a block tail.
fn pick(f bool) -> Option[int] {
    if f {
        return Some(3)
    }
    None
}

fn go() -> int {
    (pick(false) ?? -1) + 1
}

fn main() {
    println(go())
}
