module p

type Inner {
    m int
}

type Pt {
    a int
    b int
}

fn tick(n int) -> int {
    println("t", n)
    n
}

fn take(q Inner) -> int {
    q.m
}

fn main() {
    ro p = Pt { a: 1, b: take(Inner { m: tick(2) }) }
    println(p.b)
}
