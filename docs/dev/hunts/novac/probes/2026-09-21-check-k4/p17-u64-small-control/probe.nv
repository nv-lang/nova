module p
// КОНТРОЛЬ к Н1: та же сигнатура, маленький литерал. Доказывает, что `u64` в
// аргументе подмножеству ИЗВЕСТЕН и отказ в p1 — про величину, а не про тип.
fn take(x u64) -> () { println(1) }
fn main() { take(7) }
