module p
// ТО ЖЕ ТЕЛО плюс МЁРТВАЯ привязка после него. Дописанная строка ничего не
// делает — и файл становится законным.
fn f(n int) -> int {
    if n > 0 { return 1 } else { return 2 }
    ro dead = 0
}
fn main() { println(f(3)) }
