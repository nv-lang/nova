module p
// Второй контроль к Н3: привязка в хвосте БЕЗ терминатора отвергается —
// значит в p8 молчит именно дверь `stmt_terminates`, а не «дверь спит».
fn f(n int) -> int {
    ro dead = 0
}
fn main() { println(f(3)) }
