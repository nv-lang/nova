module p
// КОНТРОЛЬ к Н6: тот же вызов с `mut`-привязкой принимается — значит число
// аргументов ни при чём.
fn bump(mut x int) -> () { x += 1 }
fn main() {
    mut x = 1
    bump(x)
    println(x)
}
