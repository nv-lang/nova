fn g() -> int {
    with Random = effect Random {
        u64() -> u64 => 41
        bytes(n int) -> []u8 => "ab".bytes()
    } {
        println(Random.u64() + 1)
    }
    0
}

fn main() {
    println(g())
}
