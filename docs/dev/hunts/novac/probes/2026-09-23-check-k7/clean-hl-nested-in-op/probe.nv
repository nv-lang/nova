fn g() -> int {
    with Random = effect Random {
        u64() -> u64 {
            ro inner = effect Random {
                u64() -> u64 => 4
                nope() -> int => 1
                bytes(n int) -> []u8 => "ab".bytes()
            }
            return 7
        }
        bytes(n int) -> []u8 => "ab".bytes()
    } {
        println(Random.u64())
    }
    0
}

fn main() {
    println(g())
}
