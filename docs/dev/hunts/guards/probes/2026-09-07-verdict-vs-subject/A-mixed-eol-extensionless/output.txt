=== ДЕФЕКТ: смешанные окончания в scripts/githooks/pre-commit (без расширения) ===
0000000   #   !   /   b   i   n   /   s   h  \r  \n   e   c   h   o    
0000020   o   n   e  \r  \n   e   c   h   o       T   W   O   -   w   i
0000040   t   h   -   b   a   r   e   -   L   F  \n   e   c   h   o    
0000060   t   h   r   e   e  \r  \n
check-mixed-eol ok: смешанных окончаний строк в рабочем дереве нет
rc=0

=== КОНТРОЛЬ 1: ТОТ ЖЕ файл, переименован в pre-commit.sh ===
check-mixed-eol: FAIL — файлов со смешанными окончаниями строк: 1
scripts/githooks/pre-commit.sh  CRLF=3  bare-LF=1  first-anomaly-line=3
    Один одиночный LF в CRLF-файле разводит построчные и побайтные
    счётчики: diff показывает правку там, где её нет, а два инструмента
    дают разные позиции для одного места. В редакторе всё выглядит цело.
    Лечится НЕ коммитом — в git файл уже однороден, а перевыкладкой:
      rm <файл> && git -C "/tmp/probeA.3017093" checkout -- <файл>
rc=1

=== КОНТРОЛЬ 2: pre-commit.sh с ОДНОРОДНЫМИ CRLF (исправный) ===
check-mixed-eol ok: смешанных окончаний строк в рабочем дереве нет
rc=0

=== КОНТРОЛЬ 3: тот же дефект в .claude/commands/x.md (SKIP_DIRS) ===
check-mixed-eol ok: смешанных окончаний строк в рабочем дереве нет
rc=0

=== КОНТРОЛЬ 4: тот же дефект в docs/x.md (судимый каталог) ===
check-mixed-eol: FAIL — файлов со смешанными окончаниями строк: 1
docs/x.md  CRLF=3  bare-LF=1  first-anomaly-line=3
    Один одиночный LF в CRLF-файле разводит построчные и побайтные
    счётчики: diff показывает правку там, где её нет, а два инструмента
    дают разные позиции для одного места. В редакторе всё выглядит цело.
    Лечится НЕ коммитом — в git файл уже однороден, а перевыкладкой:
      rm <файл> && git -C "/tmp/probeA.3017093" checkout -- <файл>
rc=1
