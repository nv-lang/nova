=== строка-носитель в ЖИВОМ дереве (только чтение) ===
# Путь к хуку выводится от расположения теста — литеральный путь к машине автора
# в отслеживаемом скрипте это класс №698 (см. check-no-machine-paths).
import json
import os
import subprocess
import sys

H = os.path.join(os.path.dirname(os.path.abspath(__file__)), os.pardir, "guard-git.py")
R = "/d/Sources/nv-lang/nova"  # дерево без слияния в процессе

=== как отвечает страж на ЖИВОМ дереве (чистый замер, без правок) ===
check-no-machine-paths ok: скриптов проверено 483, путей к машине вне комментариев 0 (№698)
rc=0

--- КОПИЯ носителя как есть -> ожидается зелёный (это и есть дефект) ---
check-no-machine-paths ok: скриптов проверено 1, путей к машине вне комментариев 0 (№698)
rc=0

в КОПИИ снят ТОЛЬКО хвостовой комментарий, путь не тронут
--- КОНТРОЛЬ: у копии снят ТОЛЬКО хвостовой комментарий -> ожидается КРАСНЫЙ ---
check-no-machine-paths: FAIL — абсолютный путь к машине в отслеживаемом скрипте (№698):
  scripts/claude-hooks/selftest/test-guard-git-commit-scope.py:
    17:R = "/d/Sources/nv-lang/nova"
    Расположение выводится от $ROOT / git-common-dir / dirname $0, а не пишется.
rc=1
