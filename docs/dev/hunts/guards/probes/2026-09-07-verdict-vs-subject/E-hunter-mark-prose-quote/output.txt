--- КОНТРОЛЬ 0: реестр без единой метки -> ожидается зелёный ---
check-hunter-mark ok: novac: отчётов 0, находок 0+0 свёрнутых, меток 0; oracle: отчётов 0, находок 0+0 свёрнутых, меток 0; guards: отчётов 0, находок 0+0 свёрнутых, меток 0; мера считается грепом по строкам, пробы в дереве непусты и различны
rc=0

строка прозы, добавленная в реестр (ровно форма из шапки стража):

Пояснение к форме: метка пишется как `НАЙДЕНО ОХОТНИКОМ <дата> (<трек>)` и в бэктиках не считается.
--- ДЕФЕКТ: проза с ПРЕДПИСАННОЙ шапкой цитатой-шаблоном в бэктиках ---
check-hunter-mark: FAIL — строка реестра с меткой без даты (YYYY-MM-DD) или без трека «(novac)»/«(oracle)»/«(guards)» после даты:
    7:Пояснение к форме: метка пишется как `НАЙДЕНО ОХОТНИКОМ <дата> (<трек>)` и в бэ�
rc=1

--- КОНТРОЛЬ 1: та же проза, но в бэктиках стоит НАСТОЯЩАЯ дата+трек -> зелёный (значит краснит именно шаблон, а не проза как таковая) ---
check-hunter-mark ok: novac: отчётов 0, находок 0+0 свёрнутых, меток 0; oracle: отчётов 0, находок 0+0 свёрнутых, меток 0; guards: отчётов 0, находок 0+0 свёрнутых, меток 0; мера считается грепом по строкам, пробы в дереве непусты и различны
rc=0

--- КОНТРОЛЬ 2: настоящая СТРОКА-ЗАПИСЬ с испорченной меткой -> КРАСНЫЙ, страж делает своё дело ---
check-hunter-mark: FAIL — строка реестра с меткой без даты (YYYY-MM-DD) или без трека «(novac)»/«(oracle)»/«(guards)» после даты:
    6:| 2 | P2 | K3 | НАЙДЕНО ОХОТНИКОМ вчера |
rc=1

