КЛЕТКА | страж, чей ВЕРДИКТ не совпадает с тем, что он на самом деле проверил.

Трек: guards. Дата: 2026-09-07. Пробы: каталог рядом с этим файлом, `probes/`.
Каждая проба самодостаточна: `sh cmd.sh [КОРЕНЬ-РЕПОЗИТОРИЯ]`, абсолютных путей
внутри нет, корень ищется вверх по дереву от каталога пробы. Все шесть проб
прогнаны; дословный вывод — `output.txt` в том же каталоге.
Рабочее дерево не правилось ни разу: пробы строят мини-деревья в TMPDIR,
живой носитель (находка B) копируется, а не изменяется.

Чистые замеры на живом дереве (все зелёные, без единой правки):
  check-mixed-eol           ok  rc=0
  check-no-machine-paths    ok: скриптов проверено 483, путей к машине вне комментариев 0 (№698)  rc=0
  check-gate-steps-assert   ok  rc=0
  check-commit-language     (не гонял на живом дереве: пишет метку в .git; проба — на своём репозитории)
  check-hunter-mark         ok: novac 9/58+57/31, oracle 5/54+0/27, guards 2/26+0/14  rc=0
  check-license-hygiene     ok  rc=0

================================================================================
НАХОДКИ
================================================================================

НАХОДКА | check-mixed-eol.sh | «Страж: в рабочем дереве нет файлов со СМЕШАННЫМИ окончаниями строк» / вердикт «ok: смешанных окончаний строк в рабочем дереве нет» | A-mixed-eol-extensionless | судит только файлы с расширением из списка EXTS и не заходит в `.claude/`: `mixed-eol-scan.py:22` EXTS без пустого расширения, `:26` SKIP_DIRS содержит `.claude`, `:44` `if not fn.endswith(EXTS): continue`. Пять исполняемых shell-скриптов БЕЗ расширения — `scripts/githooks/{pre-commit,commit-msg,pre-push,post-merge,pre-merge-commit}` — не судятся вовсе, хотя git запускает их на каждом коммите. Проба: один и тот же байтовый мусор (CRLF + один голый LF) в `scripts/githooks/pre-commit` даёт «ok: ... нет» rc=0; тот же файл, переименованный в `pre-commit.sh`, даёт «FAIL — файлов со смешанными окончаниями строк: 1». Контроли: однородный CRLF-файл `.sh` зелёный (не ложнит), тот же дефект в `.claude/commands/x.md` зелёный, тот же дефект в `docs/x.md` красный. Самотест `test-check-mixed-eol.sh` работает только с `.nv` — ни случая без расширения, ни случая под `.claude/`. Класс, о котором речь, назван опаснейшим в шапке соседа `check-script-eol-pinned.py`: «шелл-скрипты БЕЗ РАСШИРЕНИЯ, исполняемые git'ом на каждом коммите: правило ключевалось по `.sh` и их не видело. Их поломка выглядела бы как «git сломался»».

НАХОДКА | check-no-machine-paths.sh | «ЧТО СЧИТАЕТСЯ ПУТЁМ К МАШИНЕ: ... в строке, которая НЕ комментарий» / вердикт «ok: скриптов проверено N, путей к машине вне комментариев 0 (№698)» | B-machine-path-inline-comment | второй фильтр конвейера — `grep -vE 'Проверялся|проверялся|# |Prichina:|machine-path-fixture|^[^:]*:[0-9]+:step '` — выбрасывает ЛЮБУЮ строку, где ГДЕ УГОДНО стоит решётка с пробелом либо слово «проверялся». Строка с ХВОСТОВЫМ комментарием комментарием не является: настоящие комментарии уже сняты предыдущим фильтром `^[^:]*:[0-9]+:[[:space:]]*#`. Свойство «исполняемая строка несёт абсолютный путь к машине» выразимо минимум четырьмя синтаксисами, ловится один: (1) голая строка — КРАСНЫЙ; (2) та же строка + `# GC lives here` — зелёный; (3) та же строка + слово «проверялся» — зелёный; (4) та же строка в файле без расширения (`scripts/githooks/pre-commit`) — зелёный, и пути даже не входят в пересчёт («скриптов проверено 1»). Контроль на прицельность: тот же хвост БЕЗ пробела после решётки (`#GCliveshere`) — КРАСНЫЙ, то есть спусковой крючок ровно «решётка+пробел». **ЖИВОЙ НОСИТЕЛЬ** (проба `live-carrier.sh` в том же каталоге, файл только читается и копируется): `scripts/claude-hooks/selftest/test-guard-git-commit-scope.py:17` — `R = "/d/Sources/nv-lang/nova"  # дерево без слияния в процессе`. Именованной отметки `machine-path-fixture` на строке НЕТ, в отличие от трёх соседних файлов (`test-guard-git-powershell.py:41,43`, `test-guard-shell-nonascii.py:48`), где она стоит. Шапка самого файла-носителя, строки 9-10: «Путь к хуку выводится от расположения теста — литеральный путь к машине автора в отслеживаемом скрипте это класс №698 (см. check-no-machine-paths)». В копии снят ТОЛЬКО хвостовой комментарий, путь не тронут — страж краснеет. Самотест `test-check-no-machine-paths.sh` проверяет путь в полной строке-комментарии, в тексте `step "` и в фикстуре с отметкой; хвостового комментария, слова «проверялся» внутри исполняемой строки и файла без расширения там нет.

НАХОДКА | check-gate-steps-assert.sh | «Шаг гейта обязан предъявить свою строку» / «2. каждый страж зовётся из гейта через обёртку `guard`» / «4. обёртка стоит в позиции аргумента чужой команды» / вердикт «ok: каждый шаг гейта требует от стража его собственную строку» | C-gate-steps-assert-one-gate | `GATE="$ROOT/scripts/gate.sh"`, и свойства 2 и 4 грепают ТОЛЬКО этот один файл. Гейтов в дереве два, и CI запускает оба: `.github/workflows/nova-gate.yml:197` — `bash scripts/gate-novac.sh "$(pwd)"`, `:266` и `:334` — `bash scripts/gate.sh`. Свойство 1 (литеральная пара «слэш+n») перечисляет `scripts/*.sh` целиком и второй гейт видит; свойства 2 и 4 — нет. Проба: голый `bash "$ROOT/scripts/guards/check-demo.sh"` в `gate.sh` — КРАСНЫЙ («стражи зовутся мимо обёртки guard — 1»); ТА ЖЕ строка, перенесённая в `gate-novac.sh`, — «ok: каждый шаг гейта требует от стража его собственную строку» rc=0. То же для формы 4: `timeout 60 \` + `guard ...` в `gate.sh` красный, в `gate-novac.sh` зелёный. Контроль 3: если удалить `gate.sh`, оставив `gate-novac.sh`, страж отказывает «нет .../scripts/gate.sh» — про второй гейт он не знает вовсе. Ни один другой страж этих двух свойств у `gate-novac.sh` не судит: его читают `check-gate-budget.py` (секунды), `check-gate-timeout-word.py` (пределы), `check-novac-seams-listed.py` (SEAMS), `check-novac-guard-registry.py` (имена), `check-merge-discipline.sh`, `check-novac-fuzz-zero-panic.sh`. В самотесте `test-check-gate-steps-assert.sh` слово `gate-novac` не встречается ни разу (замер: 0).

НАХОДКА | check-commit-language.sh | «ЧТО СЧИТАЕТСЯ НАРУШЕНИЕМ. Кириллица в теме или теле коммита. Исключение — merge-коммиты, чью тему пишет git» / вердикт «ok: кириллицы в сообщениях после точки перехода нет» | D-commit-language-merge | `git log --no-merges ... "$SCAN_FROM..HEAD"` выбрасывает merge-коммиты ЦЕЛИКОМ — и тему, и тело. Довод исключения («тему пишет git») в этом дереве не держит: тему и тело слияния пишет рука, и это порядок, а не случай — `/commit-push` требует нести в коммите слияния `# index-verified: <причина>`. Дословно из `git log --merges`: `0ece2222d merge p274-novac (4): the live line dated where the guard actually looks, and a budget rule that now needs three conditions`; `a938da625 merge main into p283-float-parse: two guards were red only because the branch was BEHIND, and the proof was running them in both trees`. Проба на своём временном репозитории: ОДНО И ТО ЖЕ рукописное русское сообщение — как merge-коммит (родителей 2) даёт «ok: кириллицы в сообщениях после точки перехода нет» rc=0, как обычный коммит (родитель 1) даёт «FAIL — кириллица в сообщениях коммитов» rc=1. Контроль 2: снять обычный коммит, слияние оставить — снова зелёный. Собственный самотест стража печатает «selftest: OK (5 проверок)», и ни одна из пяти не про слияние. Ни один другой страж язык merge-сообщений не судит: `check-commit-hygiene.sh` смотрит Co-Authored-By, маркеры конфликта и авторство, `check-merge-message-hashes.sh` — хеши.

НАХОДКА | check-hunter-mark.sh | «ПОЧЕМУ МЕТКИ СЧИТАЮТСЯ ПО СТРОКАМ-ЗАПИСЯМ РЕЕСТРА, А НЕ ГРЕПОМ ПО ФАЙЛУ ... Считаем СТРОКИ-ЗАПИСИ» + «ЦИТАТА МЕТКИ ПИШЕТСЯ В БЭКТИКАХ — `НАЙДЕНО ОХОТНИКОМ <дата> (<трек>)` — и тогда не считается» / вердикт «ok: ... мера считается грепом по строкам» | E-hunter-mark-prose-quote | правило 1 (форма метки) — единственная проверка стража, которая НЕ считает по строкам-записям и НЕ снимает бэктики: `BADMARK=$(grep -nE "НАЙДЕНО ОХОТНИКОМ" "$REG" | grep -vE "НАЙДЕНО ОХОТНИКОМ 20[0-9]{2}-..." | head -3)` — греп ПО ФАЙЛУ. Следствие: ровно та форма цитаты, которую шапка ПРЕДПИСЫВАЕТ (шаблон со словом `<дата>` вместо даты), краснит стража, а сообщение при этом называет прозу «строкой реестра с меткой без даты». Проба: строка прозы «метка пишется как `НАЙДЕНО ОХОТНИКОМ <дата> (<трек>)`» в мини-реестре даёт rc=1. Контроль 1: та же проза, но в бэктиках настоящая дата и трек — зелёный (значит краснит шаблон, а не проза как таковая). Контроль 2: настоящая строка-запись с испорченной меткой — КРАСНЫЙ, страж делает своё дело. Самотест: случай 12 (`test-check-hunter-mark.sh:146-154`) цитирует метку в бэктиках, но с НАСТОЯЩЕЙ датой и внутри строки-записи — то есть меряет только одну из двух форм цитаты, а итоговая строка самотеста заявляет шире: «цитата-в-прозе не ложнит». Носителя в живом реестре сегодня нет (замер: 72 строки с меткой, 0 мимо фильтра, 0 не-записей) — это заряженное ружьё, а не текущий отказ. Побочно, в том же выводе: `cut -c1-140` под `LC_ALL=C` режет сообщение об отказе ПО БАЙТАМ и обрывает кириллицу посередине символа — хвост строки в `output.txt` невалидный UTF-8 (оставлен дословно).

НАХОДКА | check-license-hygiene.sh | «ЧТО ПРОВЕРЯЕТСЯ: 1. У каждого Cargo.toml ... есть поле `license`. 2. У каждого nova.toml ... есть поле `license`. 3. Рядом с каждым таким манифестом лежат ОБА файла лицензии. 4. Каждый подмодуль из `.gitmodules` назван в `THIRD_PARTY/README.md`» | F-license-hygiene-promise3 | пункта 3 в коде нет ни строкой: в теле два блока — цикл по манифестам (пункты 1 и 2) и блок `.gitmodules` (пункт 4). Замер по коду: слово `LICENSE` встречается в страже 0 раз и в его самотесте 0 раз. Проба: манифест с полем `license = "MIT OR Apache-2.0"` и БЕЗ единого файла лицензии рядом — «ok: манифесты объявляют лицензию, подмодули названы» rc=0; тот же манифест только с `LICENSE-MIT` (второго нет) — тоже зелёный, хотя пункт 3 требует ОБА. Контроль: убрать поле `license` — «НАРУШЕНИЕ — манифест без поля license» rc=1, то есть пункты 1-2 механизм держит. Вердикт при этом сформулирован по факту работы и потому сам по себе не лжёт — расхождение видно только тому, кто откроет код: читатель шапки уверен, что отсутствие `LICENSE-MIT`/`LICENSE-APACHE` рядом с пакетом краснит гейт. Поводом к заведению стража была ровно эта половина: «вендоренный Brotli, MIT, не имел файла уведомлений вовсе» (его же шапка).

================================================================================
ЧТО ОБОШЁЛ И ПОЧЕМУ
================================================================================

- Класс «зелёный ноль на пустом корне» у семьи `check-novac-*` — держится
  мета-стражем `check-guard-empty-root.py` с храповиком `guard-empty-root.baseline`.
  Переоткрывать не стал.
- `check-guard-wiring.sh` судит только `check-*.sh`, python-стражи под его суд не
  попадают — известная ширина, названа в брифе, в находки не пишу.
- `check-registry-entry-shape` (невидимость строк с `TBD`) и `check-commit-refs`
  (достижимость по `rev-list --all`), `gate-bg.sh -c`, `check-novac-plan-liveline`,
  `guard-git.py`/`merge\b`, `check-script-eol-pinned` — уже найдены окном, в моём
  отчёте их нет.
- Стражей без шва инъекции входа НЕТ: замер по всем 191 файлам
  `scripts/guards/check-*.{sh,py}` — у каждого есть либо `$1`/`argv`, либо
  env-override (`scan4.py` рядом с отчётом). То есть строки «этого стража нельзя
  проверить, не правя дерево» в этом прогоне не возникло.
- `check-registry-row-closed.sh`: селектор `^\| [0-9]+ \|` требует РОВНО одного
  пробела и числового номера, а вердикт говорит «все $TOTAL строк реестра закрыты
  трубой». Строка с номером `TBD` или с двумя пробелами невидима, и при нулевом
  совпадении вердикт был бы «все 0 строк закрыты». Пробу не строил: носителя в
  реестре сегодня нет (985 строк вида `| N |`, 0 строк `| TBD`), а вторая половина
  класса уже названа окном на `check-registry-entry-shape`. Зазор назван, носителя
  нет.
- `check-no-manual-status-table.sh`: при 1-2 подозрительных строках печатает
  «ok: рукописных статус-таблиц нет ($hits подозрительных строк, порог 3)» — слово
  «нет» при hits>0. Порог объявлен в шапке и в самой строке вердикта, поэтому
  считаю это названной ценой, а не расхождением; в находки не пишу.
- `check-guard-honesty.py` объявляет «ПРОВЕРЯЕТ scripts/**/*.sh» — то есть те же
  `scripts/githooks/*` без расширения вне суда. Формы не пробовал: обещание шапки
  здесь названо расширением явно, а не свойством, и это добросовестная граница,
  а не расхождение.
- `check-crate-tests.sh` печатает «ok: cargo недоступен — судить нечем» — зелёный
  по среде. Оговорка стоит в самой строке вердикта, поэтому под клетку не подвёл.
- Мега-CU и `nova test` не гонял; компилятор не собирал — ни одна из шести проб
  его не требует.

================================================================================
ПРОТИВОРЕЧИЯ
================================================================================

1. Форма цитаты метки охотника: шапка стража предписывает одно, его же код
   краснит на предписанном.

   Место A — `scripts/guards/check-hunter-mark.sh`, шапка:
     «ЦИТАТА МЕТКИ ПИШЕТСЯ В БЭКТИКАХ — `НАЙДЕНО ОХОТНИКОМ <дата> (<трек>)` — и
      тогда не считается. Иначе различить «своя метка» и «пересказ соседней»
      нельзя ничем».

   Место B — тот же файл, тело:
     BADMARK=$(grep -nE "НАЙДЕНО ОХОТНИКОМ" "$REG" 2>/dev/null \
         | grep -vE "НАЙДЕНО ОХОТНИКОМ 20[0-9]{2}-[0-9]{2}-[0-9]{2} \((novac|oracle|guards)\)" \
         | head -3)

   Вопрос, на который два места отвечают по-разному: «законна ли в реестре
   строка, цитирующая форму метки в бэктиках вместе со словом `<дата>`?»
   Шапка: да, и так и надо. Код: нет, это «строка реестра с меткой без даты».
   Выбирать между ними не моё дело.

2. Оговорка про merge-коммиты и порядок работы с слияниями.

   Место A — `scripts/guards/check-commit-language.sh`, шапка:
     «Исключение — merge-коммиты, чью тему пишет git».

   Место B — `.claude/commands/explain.md`, раздел «Коммит, пуш, синхронизация»:
     «Синхронизация с `main` — слияние `origin/main` в свою ветку тем же
      порядком (коммит слияния несёт `# index-verified: <причина>`)».

   Вопрос, на который два места отвечают по-разному: «кто пишет сообщение
   merge-коммита в этом дереве?» Страж: git. Регламент: автор, и обязан вписать
   туда своё обоснование. Из первого ответа следует исключение, из второго — что
   исключение снимает с надзора класс сообщений, которые пишет человек.

3. Заявленный и выполняемый перечень проверок лицензий.

   Место A — `scripts/guards/check-license-hygiene.sh:22-26`:
     «ЧТО ПРОВЕРЯЕТСЯ: ... 3. Рядом с каждым таким манифестом лежат ОБА файла
      лицензии.»

   Место B — тот же файл, тело (строки 45-68): цикл по манифестам проверяет
   только `^license[[:space:]]*=`, блок `.gitmodules` — только имена подмодулей;
   слово `LICENSE` в файле не встречается ни разу.

   Вопрос: «краснеет ли гейт, если рядом с пакетом нет файлов лицензии?»
   Шапка: да. Код: нет.
