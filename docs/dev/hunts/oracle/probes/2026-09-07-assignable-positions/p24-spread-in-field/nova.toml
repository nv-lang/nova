[package]
name = "probe"
version = "0.1.0"
# license added for check-license-hygiene (No.556): the probe measures the
# assignable-door defect, not packaging -- this line changes neither.
license = "MIT OR Apache-2.0"
nova-version = "0.5"

[lib]
src = "."
