# Передача окна 274 (novac) — состояние на 2026-09-09

> Файл переписывается ЦЕЛИКОМ при каждой передаче. Всё, что тут написано, проверено запуском в
> этой же сессии; где не проверено — сказано прямо. **Читай раздел «Что я сам отменил» раньше
> очереди:** за смену я дважды опроверг собственный вывод, и устаревший снимок повёл бы тебя по
> отменённому пути.

## Кто ты и где работаешь

Окно **274**: `novac` — компилятор Nova, написанный на Nova (план
[274](../../plans/274-novac-self-hosted-compiler.md), бренд **Carina**, релиз-цель Nova Carina 1.0).
Текущий подплан — [274.8](../../plans/274.8-novac-mir.md), «MIR для Карины».

Дерево — **только** `d:/Sources/nv-lang/nova-p274`, ветка `p274-novac`. `main` принадлежит
интегратору: синк = слияние `origin/main` В СВОЮ ветку, никогда наоборот.

**Соседи на 2026-09-09:** интегратор — сессия **`nova-be`**, контролёр — **`nova-b8`**. Имена
меняются при перезапуске: адрес для ответа берётся из входящего сообщения дословно, поле `from=`.

**Решение владельца от 17:00 2026-09-09:** релиз оракула на ПАУЗЕ, основная задача интегратора —
Карина и обслуживание моих запросов. Слот машины по умолчанию мой; просить его надо ПИСЬМОМ с
условием («скажи, когда кончится ярус»), а не ловить зазоры — минутные окна между ярусами циклом
не поймать, проверено.

## Состояние дерева

`HEAD` = `685f48b26`, дерево ЧИСТОЕ. Девять коммитов волны ВЛИТЫ интегратором (`bf61b8e36`) и идут в ярусе `push` с 20:42:41; всё, что после них, — документное, компилятор при заморозке не запускался.
`099776b4a`, `3a7758e25` (снимок передачи), `1d6f00bdf` (слияние `origin/main`, 22 коммита),
`80769df1f` (строка реестра №1060), `cd0a359ee` (расцепление `DeclAt`), `1c1ead064` (этот
снимок), `a70b25ebd` (нормализатор получил один дом), `d43cb58b7` (пять выдуманных имён на
индекс). Волна M3 наполовину
слита в `main` (`fe7e69bb4`).

**Строка реестра №1060 ЗАВЕДЕНА** (`80769df1f`), база поднята 1015 -> 1016, `max=1060`. Раньше
здесь стояло «готова и НЕ заведена»: дыра в нумерации была настоящей, но закрылась слиянием —
№1057–1059 интегратора пришли в `1d6f00bdf`, и строка встала сразу после.

**НАХОДКА, КОТОРАЯ НЕ ЖИВЁТ БОЛЬШЕ НИГДЕ, а перезапуск переживает только этот файл:** в базе
`scripts/guards/registry-rows.baseline` строка `rows=` стояла ДВАЖДЕ. Я поднял ПОСЛЕДНЮЮ — жест
естественный, правишь то, что видно в конце файла, — и вердикт не изменился, потому что
`parse_key` (`scripts/guards/check-registry-rows-intact.py:143`) берёт ПЕРВОЕ совпадение
`re.search`. Правка была бесшумно недействительной. Дубль убран; интегратор подтвердил, что
заводил его ТРИЖДЫ своими разовыми скриптами (летопись со значением плюс строка значения), и
завёл общий `bumpbase.py`, который после записи ПЕРЕЧИТЫВАЕТ файл. **Правило, которое из этого
следует: значение храповика обязано быть РОВНО ОДНОЙ строкой `key=`, и проверяет это тот, кто
пишет, сразу после записи.** И второе, шире: правку базы нельзя проверять тем же грепом, каким
её делал, — он воспроизводит ту же ошибку и подтверждает неверное.

## Очередь — порядок ВЫВЕДЕН, а не назначен

Каждая стрелка — зависимость, найденная чтением, а не предпочтение:

перевод ПЯТИ выдуманных имён на индекс → `SwitchTerm.uniq` от билдера → **переезд понижения
из `emit_c/` в `lower/`** → **M3b** (операции и вызовы) → **M3d** (прицепы) → **M3e** (литерал
образца и уборка).

**Предусловие СНЯТО 18:23** — маркер `[M-localkind-answers-two-questions]` закрыт (`cd0a359ee`,
раздел 2ж плана): точку объявления несёт своё поле `DeclAt enum | FirstAssign | Zeroed`, а
`LocalKind` отвечает только за происхождение имени. Доказано двумя замерами — расцепление
`0 file(s) differ of 96 real emissions (raw: 0)`, перевод скрутинии `raw: 12` с живым
объявлением `nova_int _novac_l2 = n;`; модульные 26/26; дифференциал 36/36, храповик корпуса
`contract 66, behavior 58 (== база)`.

**ГОЛОВА ОЧЕРЕДИ ЗАКРЫТА 19:05 (`d43cb58b7`):** пять выдуманных имён переехали на индекс,
`0 файлов из 96 (raw 14)`, модульные 26/26, дифференциал 36/36 при неподвижном храповике.
Условие на будущее записано СВОЙСТВОМ, а не числом: `grep -rn "@ir.named(.*_novac" novac/src`
обязан давать 0 — семь оставшихся вызовов несут имя АВТОРА.

**НОМЕР `switch` ОТ БИЛДЕРА — ЗАКРЫТО 19:25 (`0a15dc682`), тремя частями с отдельными замерами** (план,
раздел 2к). Часть 1: два мёртвых параметра `uniq` сняты; замер опроверг МОЙ ЖЕ критерий — я
обещал `0 raw 0` «по построению», а вышло 2 файла (raw 2), потому что вместе с параметром
снимался его АРГУМЕНТ `@fresh_uniq()`, а тот не значение, а взвод счётчика. После нормализации
`0 из 96 (raw 2)`. Часть 2: счётчик `switches` на `FnBuilder`, `0 из 96 (raw 12)`, глазами
`_novac_matched_t1` -> `_novac_matched_t0`. Часть 3: три прозаических утверждения, ставшие
ложными за день (заметка `SwitchTerm`, дока `@lower_match`, `shell.nv` про «единственный
источник имён»). Модульные 26/26.

**КЛАСС, НАЗВАННЫЙ ЭТИМ ШАГОМ — «мёртвый признак с живым побочным эффектом».** Обратен прочим
носителям дня (признак есть, действия нет): здесь чистое на вид удаление ненужного НЕСЁТ
действие. Правило приёмки: «пусто в СЫРОМ виде» законно требовать лишь когда правка не трогает
ни одного вызова с побочным эффектом (счётчики, `fresh_*`, аллокаторы, логгеры — все таковы);
иначе критерий — «пусто после нормализации при НЕПУСТОМ raw», и непустой `raw` называется
ЗАРАНЕЕ.

**СЛЕДУЮЩЕЕ — ПЕРЕЕЗД ПОНИЖЕНИЯ, И ЭТО СМЕНА ПОЛУЧАТЕЛЯ, А НЕ ПЕРЕНОС ФАЙЛОВ** (план, разделы 2л
и 2м). Все девятнадцать функций понижения суть МЕТОДЫ `Emitter`, а `Emitter` объявлен в
`emit_c/shell.nv`: метод состояния эмиттера не может жить в модуле НИЖЕ эмиттера — это обращение
ребра, запрещённое таблицей архитектуры. Замер полей (`scratch/measure_receiver_fields.py`,
тела БЕЗ комментариев) дал форму получателя точно: девятнадцать трогают РОВНО ТРИ поля — `@ir`
(19 из 19), `@out` (7), `@ctx` (5) — и НИ ОДНОГО поля бэкенда (ни `@body`, ни `@pool`, ни
`@lit_tmp`, ни `@next_tmp`, ни `@fres`, ни `@reach`).

**ФОРМА ПОЛУЧАТЕЛЯ ВЫБРАНА ВТОРЫМ ЗАМЕРОМ, и первая версия ЭТОГО ЖЕ АБЗАЦА называла другую —
знать это важнее, чем знать выбранную.** Здесь стояло `type Lowerer { ctx Ctx, out CheckOut,
mut ir FnBuilder }` — получатель, владеющий всеми тремя. Замер обращений по телам (`scratch/
measure_move_radius.py`) эту форму отверг: `@ctx` спрашивают 121 раз ПРИНТЕРЫ против 7 у
понижения, `@out` — 37 против 11, и только `@ir` наоборот, 65 против 23. То есть `ctx`/`out`
суть общие данные ЧТЕНИЯ, которыми живёт печать (`ctx` — C-именование типов, знание бэкенда),
а собственное изменяемое состояние понижения — только билдер. Форма «все три» была бы неверна
по существу, а не только дорога: 181 правка у принтеров против 23.

**ВЫБРАНО: `type Lowerer { mut ir FnBuilder }` — билдер, и только он.** `ctx`/`out` остаются
у `Emitter`; восемнадцать обращений к ним изнутри понижения получают их параметрами либо
read-only полями — что именно, решает КОМПИЛЯТОР, а не память. Двадцать три обращения
принтеров к `@ir` переходят на чтение через получателя: `emit_place.nv` 10, `emit_c.nv` 5,
`emit_flow.nv` 5, `emit_match.nv` 3. Это одна волна, а не месяц.

Проба формы приведена к выбранной и ждёт машину: `scratch/probe_lowerer_shape.py` (есть
`--revert`; мёртвый тип в дереве — долг, поэтому проба не коммитится как есть). Она спрашивает
ровно два оставшихся вопроса: может ли тип здесь владеть `mut ir FnBuilder` и звать его двери
из `mut`-метода, и что компилятор скажет о таком типе рядом с `consume`-`Emitter`.

**И закономерность, которую стоит знать про ЭТОТ файл:** он устаревает не от времени, а от
каждого замера. За вечер он трижды нёс конструкцию, которую я сам же отменил, — и каждый раз
это была не опечатка, а то, по чему следующее окно пошло бы работать. Правило: сделал замер,
меняющий конструкцию, — правь снимок ТЕМ ЖЕ ходом, а не «потом».

**КРИТЕРИЙ ЭТОГО ШАГА Я ПЕРЕПИСАЛ, И ПРЕЖНЮЮ ВЕРСИЮ НАДО ЗНАТЬ, ЧТОБЫ НЕ ВЕРНУТЬ ЕЁ.** Здесь
стояло: «закрыт, когда `--split` перестанет печатать `19 method(s) still write statements`». Он
числовой, машинный, не подгоняется задним числом — и всё равно неверен дважды. НЕ ДОСТАТОЧЕН:
`emit_block_stmts_fn` есть позвоночник рекурсии понижения, в билдер сам не пишет, мерой не
виден и в девятнадцати его нет — а оставшись в `emit_c/`, он даёт вызов `lower/` -> `emit_c/`. И
описывал СЛЕДСТВИЕ, а не работу: «19 -> 0» случится, когда получатель сменён, но исполнителю
не говорит, ЧТО делать. Окно, взявшее его буквально, получило бы зелёное число и сломанную
архитектуру.

**Критерий теперь ДВА условия, и второе — свойство, потому что числом его не поймать:**
(1) `--split` печатает ноль пишущих в `emit_c/` (кроме названной тройки жизненного цикла);
(2) **в `lower/` ноль вызовов к `emit_c/`** — греп импортов и вызовов.

Порядок: (а) получатель + семь чистых функций + позвоночник — одним переездом, порознь они
дадут обращение ребра; (б) `lower_eval` отдельно (`PRINT_FN` из `builtins` — единственное
обращение к запрещённому ребру, вопрос M3d); (в) остаток по одному, каждый со своим диффом.
Ещё не спрошено у компилятора: как получатель живёт внутри `consume`-типа `Emitter`.

**Прежняя формулировка, для сверки:** `python
scripts/tools/novac-tree-edges.py --split` печатает `19 method(s) still write statements` при 12
READ-only. Переезд закрыт, когда пишущих в `emit_c/` не останется (кроме тройки жизненного
цикла). Затем M3b, M3d, M3e.

**МЕРА К1 НЕ СДВИНУТА НИ ОДНИМ ИЗ СЕГОДНЯШНИХ ШАГОВ, и это не упущение:** половина A — 15
мест в `lower/ir.nv`, половина B — 68 древочитающих имён в 8 файлах, как и утром. Сегодня
закрывались ПРЕДУСЛОВИЯ (точка объявления, имена, номер), а не сам мост дерева. Не проверив
это, легко решить, что волна близка к закрытию. разбита
на ТРИ части с разной ценой доказательства.** Правки написаны и проверены `--check`, ждут
машины (`scratch/uniq_part1_dead_params.py`, `..._part2_builder_mints.py`,
`..._part3_stale_prose.py`):

1. **два МЁРТВЫХ параметра `uniq`** — у `@emit_if_value` (`emit_c.nv:469..492`) и у
   `@lower_arm` (`emit_match.nv:174..244`); в обоих слово встречается ТОЛЬКО в подписи
   (проверено чтением тел, не грепом по файлу). Первый держит живым `@fresh_uniq()` на
   `emit_place.nv:392`, то есть понижение ВЫГЛЯДИТ держащим печатное состояние, не держа его.
   Доказывается компилятором: приёмка — сборка плюс дифф `0 raw 0`;
2. **номер выдаёт билдер** — счётчик `switches` на `FnBuilder`, `@begin_switch(scr)` без
   аргумента, `@lower_match` без параметра, три вызова перестают звать `@fresh_uniq()`. Поле
   `uniq` ОСТАЁТСЯ полем: довод при `SwitchTerm` не изменился (обход иначе восстанавливал бы
   номер разбором имени). Приёмка: `0` после нормализации при НЕПУСТОМ `raw`;
3. **две докстроки, устаревшие от правки 18:12** — заметка при `SwitchTerm` говорит, что из
   номера пишется и скрутиния (`_novac_scr_t${uniq}`), а она с 18:12 `_novac_l<N>`; дока
   `@lower_match` говорит «каллер объявил `_novac_match_t<uniq>`», а результат теперь временная.

После этого: переезд понижения `emit_c/` -> `lower/`, затем M3b, M3d, M3e.

**ГРАНИЦА, которую легко прочитать неверно:** выдуманные имена есть и ВНЕ канала — принтер
пишет их прямо в `@body` (`emit_c.nv:657`, `:746`, три в `emit_interp.nv`, `_novac_matched_t`
в четырёх местах). Их дом — M3b/M3d, НЕ голова очереди. Мера шага (вызовы `@ir.named`) уже
своего предмета («выдуманные имена»), и «пять» не значит «все».

**Прежний замер двенадцати вызовов `@ir.named` — в плане, раздел про порядок.**
Вызовов `@ir.named` ДВЕНАДЦАТЬ, не шесть. Выдуманных имён шесть, из них скрутиния переведена —
остаётся ПЯТЬ: `emit_c.nv:511` (хойст `??`), `emit_destructure.nv:39` (`bind_pair_temp`),
`emit_flow.nv:178` (Option у `if let`), `emit_flow.nv:301` и `:305` (вектор и индекс `for each`).
Остальные семь несут имя АВТОРА (`vname`, `bname`, `leaf_text(...)`) и не трогаются вовсе.

**Точку объявления решать не придётся ни на одном из пяти:** `declare_zeroed` не зовётся ни на
одном (все его вызовы — `emit_c.nv:476`, `:516`, `emit_match.nv:68`), а обе двери создают локаль
как `FirstAssign`. Перевод сохраняет точку по КОНСТРУКЦИИ, меняется только имя
(`_novac_tmp_t<uniq>` -> `_novac_l<N>`). Приёмка та же, что у скрутинии: дифф корпуса пуст после
нормализации при НЕПУСТОМ `raw` — непустой `raw` обязателен, он и доказывает, что правка
подействовала.

**СНЯТО МОЁ ЖЕ УТВЕРЖДЕНИЕ, и это важнее снятого предусловия.** Здесь стояло: «единственное
настоящее решение там — отступ: ветвь `Temp` печатает восемь пробелов, `Binding` четыре...
решается замером». Решения не существовало. Посылка держалась на том, что отступ коррелирует с
ВИДОМ локали; чтение всех трёх сайтов `@ir.temp` показало, что КАЖДАЯ существующая `Temp`
объявляется нулём, то есть отступ коррелирует с ТОЧКОЙ ОБЪЯВЛЕНИЯ и после расцепления следует за
новым полем сам. Ни одной печати не изменилось. **Утверждение опаснее устаревшего факта:** факт
следующее окно перепроверит, а «вот единственное решение здесь» примет на веру и потратит на него
прогон. Я выводил его из ДВУХ ВЕТВЕЙ ПРИНТЕРА и не спросил, кто в каждую ветвь реально попадает.

**Меры волны в дереве, обе с отказом на пустом входе (`rc=2`), а не с нулём:**
`python scripts/tools/novac-tree-edges.py` — половина A (IR выдаёт узлы дерева: 15 мест) и
половина B (принтеры читают дерево: 68 имён); `--split` — кто ПИШЕТ в билдер: 19 методов против
12 читающих плюс 1 «только жизненный цикл» (`emit_callable`, названное исключение по НАБОРУ
ДВЕРЕЙ, не по имени функции).

## Что сделано этой сессией

* **M2c** — имя временной даёт ИНДЕКС локали (`_novac_l<i>`), а не вызывающий. Вскрыло два
  дефекта дороже самого среза: `@emit_coalesce` возвращала имя, которое сама же собрала (два
  конца совпадали по совпадению), и `LocalKind` отвечает на два вопроса разом.
* **Шаг 3б** — печать тела ОДНИМ проходом по сеченным блокам; форму C решает теперь
  ПРИНАДЛЕЖНОСТЬ БЛОКУ, а не миг печати.
* **M3a** — последовательность `println` понижается (её чередование с эффектами наблюдаемо);
  `??` уходит через одну дверь в любой позиции значения. Тип литерала спрашивается У КАНАЛА и
  падает в дверь формы только там, где канал молчит — а молчит он ровно об одной форме,
  строковой. Обходной путь УМРЁТ САМ, когда тип опубликуют.
* **M3c** — чтение места стало оператором канала (`Projection` + `PlaceRead`); закрыло четыре
  носителя из пяти. Пятый — цепочечный `if let`, писавший временную не в тот БЛОК.
* **Починка возврата** у функции без результата: печатает обход, по типу возвратного места, и
  условие называет ОБА способа не иметь значения (тип единичный ИЛИ типа нет — второе есть точка
  входа, `ret_id = -1`).
* Инструменты: область биекции нормализатора привязана к ФУНКЦИИ C (была к файлу), заведена
  мерка `--split`.

**Числа приёмки:** дифф C после нормализации — все 94 прежние эмиссии идентичны; дифференциал
`EXIT=0`, фикстуры 36 из 36 байт-в-байт, `allow: 0`, храповик `contract 66, behavior 58` равен
базе; модульные тесты 26 из 26 стражем, который перечисляет файлы ЯВНО.

## Что я сам отменил — читай до очереди

1. **«Голый `return` — живой дефект»: НЕВЕРНО.** Чекер отвергает форму по имени: `outside the
   subset: return must carry a value`. Я прочёл ЭМИТТЕР и заключил о ДОСТИЖИМОСТИ, не спросив
   чекер. Печать в `lower_return` была недостижима; границу подмножества держит чекер — там ей и
   место. Верной осталась ВТОРАЯ форма: unit-функция, чьё тело просто кончается, возврата не
   получала вовсе.
2. **Проба сама ошиблась и сказала `NONE` там, где возврат БЫЛ.** Она искала функцию грепом по
   имени и брала ПОСЛЕДНЕЕ совпадение — а это место ВЫЗОВА `main` в оболочке, не её определение.
   Якорь обязан быть `^static .*<имя>`.
3. **Мера переезда: не 15 и не 32, а 19 из 20.** Счёт по приставке `@lower_*` недосчитывает
   (понижают и `emit_coalesce`, `emit_if_value`, `emit_bind`, `bind_pair_temp`,
   `emit_destructure_bind`); счёт по «трогает `@ir`» перебирает (принтеры законно СПРАШИВАЮТ
   запись). Верное правило: «зовёт ИЗМЕНЯЮЩУЮ дверь билдера», а набор дверей читается из `ir.nv`.

## Уроки смены — не наступай снова

* **Проверка бывает похожа на предмет, не будучи им. ШЕСТЬ носителей за один день.** Ратчет цены
  мерил ЛУЧШИЙ МОМЕНТ машины (минимум из трёх) вместо загрузки; страж свежести оракула сравнивает
  mtime, а не содержимое, и перевернулся с красного на зелёный без единой сборки; таблица хойста
  была локалью на бедняцкий лад; утверждение теста проверяло НАЛИЧИЕ имени вместо потока
  значения; **пять комментариев указывали на поле `zero_init`, снятое той же правкой** — компилятор
  поймал КАЖДОГО читателя в коде и НИ ОДНОГО в прозе; **сторож судил живость прогона по имени
  рабочего бинаря** и выдал ложную смерть через четыре минуты, потому что дифференциал зовёт
  `novac` по одной фикстуре за раз.

  **ДВА вопроса-различителя, и они ловят РАЗНОЕ.** Мой: «что проверка измерит, если предмет
  останется прежним, а изменится всё вокруг?» — он про ЧУВСТВИТЕЛЬНОСТЬ. Интегратора (18:33,
  короче и злее): **«совпадает ли ОБЛАСТЬ проверки с её ПРЕДМЕТОМ?»** — он про ОХВАТ, и именно он
  ловит последние два носителя: у компилятора область — код, а предмет правки был и в прозе; у
  сторожа область — имена процессов, а предмет — идёт ли работа. Спрашивать надо оба.

  И отдельно про сторожа: **ложная смерть дороже ложной жизни.** По ложной жизни окно ждёт лишние
  минуты; по ложной смерти — объявляет вердикт и правит дерево, которое ещё судят. Живость
  прогона анкерится PID ЛАУНЧЕРА, взятым в момент запуска (`ps | awk '$1==<pid>'`), а не именем
  рабочего бинаря.
* **Правило есть, ШАГА нет.** Трижды за смену: нарушил «ноль запусков при чужом гейте» через час
  после того, как сам записал его; ставил штампы времени из чужих сообщений вместо `date`; ставил
  номера строк в план, хотя в этом же плане записал «ссылка даётся ФУНКЦИЕЙ». Текст правила
  проверяется вниманием, а внимание мы везде заменяем механизмом.
* **«Я это починил» и «этим судят» — разные утверждения.** Утром я сузил область биекции
  нормализатора с файла до функции C и положил починку в `scripts/tools/novac-normalise-temps.py`,
  а цикл приёмки `scratch/m2b2_window.sh` звал `scratch/normalise_names.py` — копию БЕЗ починки.
  Приёмка перевода имён покраснела на четырёх файлах, и все различия были `T2` против `T1`, то
  есть сдвигом нумерации, который биекция обязана снимать. **В докстроке починенного
  инструмента этот отказ описан заранее, с числом замера (16 файлов из 94): проверка знала свою
  дыру и не была вызвана.** Починка проверяется не существованием правки, а ПУТЁМ, который
  зовёт цикл: греп зовущих сразу после починки, и судья живёт в дереве, а не в `scratch/`.
* **Дерево под читающим агентом СТОИТ.** Откатил волну, пока агент читал `novac/src`; он честно
  записал расхождение как «артефакт инструмента поиска», а артефакт был мой.
* **Фильтр измеряет меньше предмета.** Приёмку M2c закрыл `--filter lower` и увидел `PASS: 1`;
  упало 16 файлов из 26, и узнал я это только из яруса. Раннер, которому дали каталог, открывает
  не всё: знаменатель обязан печататься.
* **Ноль носителей делает зелёное бессмысленным.** Оба сегодняшних дефекта имели НОЛЬ файлов
  своей формы в корпусе, поэтому все зелёные замеры о них молчали. Прежде чем доверять зелёному —
  «где файл ЭТОЙ формы?».
* **Чёрная сторона берётся ВОЗВРАТОМ правки**, а не подсовыванием негодного рядом: тогда обе
  стороны видны на одном дереве и одном бинаре.
* **Дневник черновика в дереве не нужен ни на каком языке.** Обход приехал с 125 строками
  русских комментариев; половина была записями о том, КАК черновик писался («ЧЕРНОВИК — НЕ В
  ДЕРЕВЕ», стоя в дереве) — они удалены, а не переведены.

## Отношения с интегратором

Слияния, пуш и теги — его; ветка и волна — мои. Он гоняет ярус на несколько коммитов разом
(сорок минут на прогон), поэтому просит паузу в коммитах до «запушено» — соглашаться дёшево, если
дерево чистое. Его находки за смену, которые касаются меня: ратчет цены починен (медиана из девяти
вместо минимума из трёх), бюджет `diff-corpus` перемерен 230→560 на тихой машине, заведён подплан
221.3 «дефекты оракула, блокирующие релиз Карины», доказан №691 на живом C Карины.

**Его вердикт по цене НЕ принимать за замер, если машина была занята** — и это правило
двустороннее: «уложился» под нагрузкой значит так же мало, как «не уложился».

## Что НЕ твоё

Оракул (`compiler-codegen`, `nova-cli`) — правки только по слову владельца через интегратора;
реестр 221.1 — номера даёт интегратор, окно пишет `№TBD`; `main` — его; теги — владельца.
Мега-CU и полный `nova test` — тоже его: мои прогоны точечные (своя фикстура, свой корпус, свой
дифференциал).
