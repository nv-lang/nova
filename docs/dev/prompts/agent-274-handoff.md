# Агент 274 — передача состояния

Ветка `p274-novac` в ворктри `d:/Sources/nv-lang/nova-p274`. Обновлено 2026-08-29
(предыдущая версия была от 2026-08-23 и протухла целиком). Ниже ровно то, что нужно,
чтобы продолжить без чтения истории.

## Кто я и что делаю

Окно 274: **план [274](../../plans/274-novac-self-hosted-compiler.md)** — `novac`,
компилятор Nova на Nova. Работаю ТОЛЬКО в своём ворктри; `main` принадлежит
интегратору, синк — слиянием `origin/main` в свою ветку по `/commit-push`.
Режим: `/flow` (сделал → доложил → сделал), `/delegate` (механика — агентам на
haiku/sonnet, чтение доков — агенту `spec-reader`; себе — решения, слияния, гейты).
Доклад по шагу несёт строку «Модели агентов: …».

## Где мы (2026-08-29, ночь)

* **Подплан [274.4](../../plans/274.4-one-door-per-root.md) «одна дверь на корень» —
  ЗАКРЫТ ЦЕЛИКОМ и запушен.** Разрешение имён — модуль `resolve` (`FnIndex`/`FieldIndex`
  живут только в `Checker`, поля `priv`); отказ и вход программы пишутся в канал;
  правило E стража `channel-one-writer` + колонка «дом двери» в §3а карты держат запрет
  машинно. CI зелёный на этом состоянии (run на `1829df492`).
* **Подплан [274.5](../../plans/274.5-read-own-source.md) «читать свой исходник» — ИДЁТ.**
  Мера — самодистанция: `novac check` по своим 52 файлам. Волны В1 (тип поля через общую
  дверь), В2 (модульный `const`, подстановкой), В3 (`priv` у типа), В4 (хвостовой вызов
  — значение возврата) сделаны; статусы и доказательства — в разделах 2а–2е подплана.
  Корпус на В4 намерен 17/10 (contract/behavior), причина уже вписана в baseline.
* **Последний коммит:** `23593ca92` (В3). **Волна В4 — в дереве, НЕ закоммичена**;
  порядок закрытия — раздел 2ж подплана 274.5 (заголовок `operators.nv` поправлен,
  ярус — прогнать, сообщение готово в `target/msg-b4.txt`, файлы перечислены там же).
* **Синк с main сделан** по `88d5e13f7` (правка D200, план 276 закрыт, spec-queue=29).
  После передачи координатора 2026-08-29 ~04:47 появился новый хвост main
  (`e788d1f96`+) — синкнуться после коммита В4, слиянием, не наоборот.

## Решения координатора, уже НЕ требующие действий

Ёмкость D239: v0.1 выходит с оговоркой в release notes, novac уже точен, оракул до тега
не трогается (№783). Дом голой формы `const` — **D184**, не D200 (цитату в D200 поправил
координатор). Битовые имена остаются `@bitand/@bitor/@bitxor/@bitnot`. План 276 закрыт;
его стражи (`check-plan-wave-boundaries` и др.) уже судят мои планы. Номера реестра сам
не беру — пишу №TBD.

## Очередь после В4 (по переписи, а не по памяти)

В5 — `match` в позиции инструкции/возврата (5 файлов). В6 — `#realtime nogc` читать и
игнорировать (6 файлов). В7 — hex-литералы (`0x0A`, находка В2, носитель `lex/lex.nv`).
Новый лидер переписи — «no such method in the declarations handed» (4 файла, вырос после
В4) — сперва замер, каких деклараций не хватает. Файлы `*_test.nv` — названная отсрочка
(раннер тестов). Перепись повторяется одной командой из §1 подплана 274.5; сравнение —
по паре (файл, СТРОКА), не по тексту сообщения (раздел 2в, находка о методе замера).

## Грабли, уже оплаченные (не наступать второй раз)

* Обратная проба снимается ОБРАТНОЙ ПРАВКОЙ; `git checkout --` откатывает и
  незакоммиченную волну (раздел 2а, полчаса потеряно).
* Кириллица в командной строке запрещена хуком — правки только Write-скриптом.
* Один `git commit` на один вызов Bash; сообщение файлом; `--only -- <файлы>`.
* Проверка одного варианта закрытого множества в цикле — инверсия + `continue`
  (раздел 2г: `else` красит один из двух стражей формы).
* Слово «донор» рядом со словом «оракул» красит `check-novac-plan-donor` даже в
  отрицании — формулировать без пары этих слов.
* `Guarded by:` в шапке модуля называет стража С РАСШИРЕНИЕМ (`.py`/`.sh`).
* Newtype в руке суммы объявляется в `callables.nv` ([LEGACY-#761], три случая).

## Команды одного взгляда

Ярус: `NOVAC_TIER=push bash scripts/gate-novac.sh` (бюджет 400с; лог в `target/`).
Пуш: `bash scripts/tools/push-after-gate.sh` (оба яруса, потом push). Смоук:
`sh scripts/tools/novac-e1-smoke.sh examples/basics/<f>.nv`. Перепись самодистанции и
корпус: `sh scripts/tools/novac-diff-corpus.sh`. Статус: `/status`; сборка novac:
`nova-cli/target/release/nova build novac/src/main.nv -o novac/target/novac.exe`.
