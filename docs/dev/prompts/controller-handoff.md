<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Снимок окна-контролёра — 2026-09-08, 15:31Z (18:31 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в самом задании цикла (`CronCreate`,
> в этой сессии — `00f4f1aa`), потому что задание приезжает в каждый цикл гарантированно, а файл
> цикл может не прочитать (решение владельца 2026-09-08: «все правила должны быть в команде»).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> ЗАДАНИЕ, а не сюда.

## Опознание сессии

- id `ef28f97e-e5fa-482b-8c44-c8e039d7d396`, имя для соседей `nova-c6` (живёт до рестарта;
  новое — из `ListAgents`, первая строка выдачи).
- Модель **Opus** (переключено владельцем 14:26Z). Механика вынесена в скрипты; дорогая модель
  тратится на суждение «встал или ждёт законно».
- Расписание: задание `00f4f1aa`, шаг 7 минут. **Живёт только в сессии и гаснет через 7 дней.**
  При рестарте: `CronList` → если пусто, завести заново (текст задания — в истории сессии).
- Инструменты (вне репозитория, чтобы пережить рестарт):
  `D:\Sources\nv-lang\controller-peers-scan.py [N]`,
  `D:\Sources\nv-lang\controller-peers-deep.py <id-prefix> [K] [tail_mb]`.
- Файл держать НЕОТСЛЕЖИВАЕМЫМ: интегратор возьмёт его по имени в свой пакет после пуша
  (его слово, 15:04Z: «прочитал целиком, беру в пакет, дока принимает»).

## Соседи — снимок 15:23Z (18:23 мест.)

| имя | ref | id истории | где / что | вердикт |
|---|---|---|---|---|
| nova-55 | 94b91d | `e601e366…` | `claude-limits`, T2.1 пакет `nova-duckdb`; после двух толчков 3 коммита, 3 модуля зелёные; остаток по её списку: `Statement`, `Appender`, `sqltag.nv`, тесты §6, скрипты сборки, 2 README | работает |
| nova-03 | 217ca3 | `5383c33a…` | окно 283 (`../nova-p283`): правки `>>=` в `float_parse` не закоммичены, №1045 выдан, но заводится ПОСЛЕ пуша (иначе дырявая нумерация: 1033–1038 не в `origin`) | стоит 55 мин, законно по всем 3 признакам |
| nova-a9 | 28b06c | `3b203470…` | **интегратор**, `main`: novac-ярус зелёный (2074 с при бюджете 3000, `head=514b41c5d`), основной идёт, на 15:23Z стоял на мега-CU; непушенных 65 на всех трёх зеркалах | ждёт свой ярус |
| nova-04 | a7dca7 | `bf14b5f5…` | окно 274 (`../nova-p274`, novac): шаг 274.8 3б подготовлен, №1039 ждёт пуша; в заморозке нашло 3-ю за смену свою слепую регулярку | стоит 12 мин, законно |

**Узкое место смены:** оба стоящих окна ждут ДВУХ слов интегратора — «запушено» (снимает
заморозку коммитов и позволяет завести строки реестра) и «берите» (отдаёт машину). Он держит
заморозку сознательно.

## Что сделано за смену (2026-09-08, ~14:15Z → 15:31Z)

- **Толчков 3.** nova-55 дважды (14:17Z, 14:31Z): после первого сработала — два коммита, три
  модуля зелёные. nova-03 (14:19Z): ответила обоснованным отказом заводить `№TBD` и была права
  (страж формы реестра даёт `FAIL` на такую строку).
- **Писем интегратору 4:** мой файл в его дереве; сводка очереди (по ней он выдал №1045, не
  дожидаясь гейта); снятый отказ `check-accepted-branch-merged`; прогресс основного яруса.
- **Циклов пустых 5** (ноль сообщений соседям).
- **Агентов 1:** haiku, карта 19 рабочих копий. Дала интегратору второй носитель класса №1029 —
  страж несведённых веток видит локально 10, на CI 0, и там же советует опустить базу долга.
- **Своих ошибок 2:** вывод о готовности дерева (14:38Z, поправлен интегратором в 14:43Z, из
  чего родилось правило предмета) и кириллица в командной строке bash.

## Находки, лежащие для интегратора (после его пуша)

- **15 брошенных worktree**, отставание до 2954 коммитов; `nova-pcdoors` впереди `main` на
  коммит и запушена. Территория интегратора, не контролёра.
  **Поправка 15:43Z, моя же:** формулировка «у 12 незакоммиченные ПРАВКИ» была сильнее факта.
  Проверил содержимое: в девяти из них грязный файл ровно один и это МУСОР рабочего каталога
  (`?? TASK.md`, `?? scratch_p493/`, `?? scratch_pdiag/`), а не код. Настоящий кандидат один —
  `nova-p259` с 39 файлами. И база деревьев в порядке: `worktree-count.baseline` = 20, фактически
  20. То есть «двенадцать грязных» звучало как потерянная работа, а под этим черновики.
- **`/tmp/gate_full.done` протухает молча:** несёт `RC=1 SEC=524 HASH=25a4a495c`, тогда как
  единственная причина того отказа снята слиянием (проверено прогоном стража 14:38Z).

## Открытые вопросы к владельцу

- (пусто)
