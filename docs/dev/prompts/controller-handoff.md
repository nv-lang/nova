<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Снимок окна-контролёра — 2026-09-08, 16:33Z (19:33 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в команде
> [`.claude/commands/controller.md`](../../../.claude/commands/controller.md) (заведена
> 2026-09-08 по слову владельца: «сделай команду для запуска со всеми правилами, чтобы ничего
> не потерялось после перезапуска»). Задание `CronCreate` (в этой сессии — `b0b843e5`) её
> ВЫЗЫВАЕТ, а не повторяет (в этой сессии — `6e897643`; перечисление шагов из его тела убрано
> 17:10Z как вторая копия — строка реестра №1042).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> ЗАДАНИЕ, а не сюда.

## Опознание сессии

- id `ef28f97e-e5fa-482b-8c44-c8e039d7d396`, имя для соседей `nova-c6` (живёт до рестарта;
  новое — из `ListAgents`, первая строка выдачи).
- Модель **Opus** (переключено владельцем 14:26Z). Механика вынесена в скрипты; дорогая модель
  тратится на суждение «встал или ждёт законно».
- **После рестарта:** сказать `/controller` — команда несёт всё, включая стыковку. Затем
  завести расписание (`CronCreate`, шаг 7 минут, тело — вызов `/controller`): оно живёт только
  в сессии и гаснет через 7 дней, проверять `CronList`.
- **Стыковка обязательна первым шагом:** имена соседей (`nova-XX`) и id их сессий после
  рестарта ДРУГИЕ. `python /d/Sources/nv-lang/controller-dock.py 2` опознаёт их по
  свидетельствам — дерево по частоте упоминаний в истории, имя из их собственного `ListAgents`
  (последнее вхождение, по всему файлу), интегратор по упоминаниям гейта и пуша.
- **Инструменты — в репозитории, `scripts/tools/`** (перенесены 17:27Z по слову владельца;
  раньше лежали рядом с деревом и потому не версионировались). Запускать из корня главного
  дерева: `python scripts/tools/controller-dock.py 2` (стыковка),
  `controller-peers-scan.py [N]`, `controller-peers-deep.py <id-prefix> [K] [tail_mb]`,
  `controller-machine-watch.py` (дежурство за очередью машины). У каждого — строка в
  `scripts/tools/README.md`, как требует правило того каталога.
- Файл держать НЕОТСЛЕЖИВАЕМЫМ: интегратор возьмёт его по имени в свой пакет после пуша
  (его слово, 15:04Z: «прочитал целиком, беру в пакет, дока принимает»).

## Соседи — снимок 00:53Z (03:53 мест.) 2026-09-09

**ИМЕНА СМЕНИЛИСЬ У ВСЕХ БЕЗ ПЕРЕЗАПУСКА СЕССИЙ** (замер 00:45Z): файлы историй те же, размеры
прежние, последнее имя в них СТАРОЕ. Я был `nova-c6`, стал `nova-b8`. Карта ниже собрана
ОПРОСОМ; опознавать по дереву, имя проверять вторым признаком.

| имя | дерево / роль | id истории | состояние на 00:53Z |
|---|---|---|---|
| `nova-be` | **интегратор**, главная копия, план 221 + реестр 221.1 | `3b203470…` | ярус с 03:48:29 мест., ~40 мин (слияние тронуло `std/src/runtime/float_parse/*.nv`, короткий ярус не годится) |
| `nova-9c` | `claude-limits` + пакет `nova-duckdb`, план 01, шаг T2.1 | `e601e366…` | пересборка DuckDB с 00:48Z, 156/374; прерываемо, приоритет отдан ярусу |
| `nova-09` | `../nova-p283`, план 283 | `5383c33a…` | план ЗАКРЫТ, очередь пуста, 1 коммит ждёт слияния; своя передача — `docs/dev/prompts/window-283-handoff.md` |
| `nova-7f` | `../nova-p274` (по остатку), план 274.8 | `bf14b5f5…` | 4 коммита ждут слияния; своей строки опознания не прислал |
| `nova-57` | главная копия, **плана нет** | нет файла | стартовало 00:50Z, задачи от владельца не получало, очередь пуста |

**Состояние машины и дерева (00:52Z):** машину держат ДВА ЯРУСА `gate.sh` интегратора плюс
пересборка DuckDB — столкновение доложено ему в 00:50Z. Зеркала на `84568506c`, локальный
`main` — `63bb30607`, непушенных **10**. Осталось влить: `p283` — 1 коммит, `p274` — 4.
В дереве висят два МОИХ незакоммиченных файла (команда и дежурство) — их видит чужой греп,
и `nova-57` это заметило первым.
## Итог смены по пушу (слово интегратора 21:27 мест., сверено мной 18:29Z)

Пуш ушёл в 21:22: три зеркала на `a6ac4229faed`, **0 непушенных** (мой замер и его совпали).
**Пуш останавливали ЧЕТЫРЕ раза подряд, и ни один отказ не был про код:** незакоммиченное
дерево дважды (мои три `M`, потом мои же 19 строк), слепота моего дежурства к имени бинаря без
`.exe` на Linux, и бюджет яруса, оказавшийся жребием (`cal-factor.sh` три раза на свободной
машине дал **1, 1, 2**; под этим прикрытием ярус `loop` подорожал вдвое незамеченным: база 240 с
снята при 59 шагах, сегодня 82 шага и 405 с, разогнавшегося шага нет — цена размазана). Его
слова про смену целиком: пятнадцать строк реестра (1040–1044, 1046–1054), одна из них блокер
тега (№1040), починено пять инструментов, три его собственных, **и к тегу ближе не стало — вся
смена ушла в слой инструментов**.

Моё в дереве на 18:33Z: четыре инструмента влиты и запушены; `controller-selftest.py` и правка
команды положены мной по его слову (условие «сперва посмотри, не идёт ли ярус» проверено:
процессов ноль). Четыре стража зелёные, включая `check-guard-honesty` (352 `.sh`, 133 `.py`,
слепых к имени бинаря 0).

## Журнал (последние двое суток)

- **18:43Z (21:43 мест.), цикл пустой.** Все четыре окна активны, ярусов нет, дерево чистое,
  непушенных 2. Окно 283 после разблокировки сделало синк (`687839fe0`, отставание от
  `origin/main` = 0) и правит реестр под №1045; окно 274 пишет скрипт шага 3б; окно 55 держит
  машину сборкой DuckDB до 22:06 (гейт не гоняет); интегратор коммитит, следующий ярус не
  раньше 22:25 по его слову. Толчков ноль.
- **01:00Z (04:00 мест.), цикл с двумя разворотами очереди за три минуты.** Интегратор уступил
  машину `claude-limits` и снял свой ярус (его довод сильнее вежливости: вердикт под чужой
  компиляцией непригоден — сегодня один страж дал 702 с против 313 с), а пересборка кончилась
  в 00:55Z, ДО того как слово дошло. Итог: машина свободна (проверено командой 00:58:35Z,
  BUSY 0), ярус снят зря. Передал обоим. Окно 274 стоит законно (шаг 3б готов, нужен слот),
  283 — законно (план закрыт, границу двигает владелец). Толчков ноль, писем три.
- **Правило, купленное этим циклом:** обстановка живёт минуты — слово соседа о занятости машины
  ПЕРЕСВЕРЯТЬ командой перед передачей, иначе разносишь протухший факт как свежий.
- **Замер 18:40Z: тяжёлый обзор историй впервые не уложился в 180 с** — истории по 200 МБ, а
  диск занят сборкой. Быстрая замена, которой хватает для вердикта: возраст файла истории
  (`stat -c %Y`) плюс `git status`/`rev-list` по деревьям.

## Что сделано за смену (2026-09-08, ~14:15Z → 15:31Z)

- **Толчков 3.** nova-55 дважды (14:17Z, 14:31Z): после первого сработала — два коммита, три
  модуля зелёные. nova-03 (14:19Z): ответила обоснованным отказом заводить `№TBD` и была права
  (страж формы реестра даёт `FAIL` на такую строку).
- **Писем интегратору 4:** мой файл в его дереве; сводка очереди (по ней он выдал №1045, не
  дожидаясь гейта); снятый отказ `check-accepted-branch-merged`; прогресс основного яруса.
- **Циклов пустых 5** (ноль сообщений соседям).
- **Агентов 1:** haiku, карта 19 рабочих копий. Дала интегратору второй носитель класса №1029 —
  страж несведённых веток видит локально 10, на CI 0, и там же советует опустить базу долга.
- **Своих ошибок 2:** вывод о готовности дерева (14:38Z, поправлен интегратором в 14:43Z, из
  чего родилось правило предмета) и кириллица в командной строке bash.

## Находки, лежащие для интегратора (после его пуша)

- **15 брошенных worktree**, отставание до 2954 коммитов; `nova-pcdoors` впереди `main` на
  коммит и запушена. Территория интегратора, не контролёра.
  **Поправка 15:43Z, моя же:** формулировка «у 12 незакоммиченные ПРАВКИ» была сильнее факта.
  Проверил содержимое: в девяти из них грязный файл ровно один и это МУСОР рабочего каталога
  (`?? TASK.md`, `?? scratch_p493/`, `?? scratch_pdiag/`), а не код. Настоящий кандидат один —
  `nova-p259` с 39 файлами. И база деревьев в порядке: `worktree-count.baseline` = 20, фактически
  20. То есть «двенадцать грязных» звучало как потерянная работа, а под этим черновики.
- **`/tmp/gate_full.done` протухает молча:** несёт `RC=1 SEC=524 HASH=25a4a495c`, тогда как
  единственная причина того отказа снята слиянием (проверено прогоном стража 14:38Z).

## Открытые вопросы к владельцу

- (пусто)
