<!-- SPDX-License-Identifier: CC-BY-4.0 -->
## 0-2026-09-21-ночь. Состояние на 2026-09-21, ночь

**Дополнение 16:06.** Правило владельца про backlog-followups (согласование ДО начала работы, передано через nova-46, коммит b11bb5885 в nova-p274) вписано в раздел «ГРАНИЦА РОЛИ» push-controller.md. 30-минутный вопрос новой 4-частной формулировкой разослан в 14:44/15:13/16:06 (все доставки success: true). Ответы: nova-a5 (14:45) — закрыла #1239(половина)/#1252/#1255/#1264, 2 новых дефекта оракула, восстановила #1249, оптимальность «беру то, что дают интегратор/Карина»; nova-37 (15:13) — пункт 274.10, непрерывный цикл слияния+пуш, закрыл #1247 (свой session-id без префикса session: ложно раздувал счётчик мёртвых хешей), снял статусы 274.1/274.4, оптимальность «да», договорился с Кариной про пакетирование docs-коммитов. Толкал nova-6e(28.6 мин)/nova-a5(11.8, 6.7, 9.6 мин)/nova-46(5.6, 19.4 мин) — все доставки success: true, кроме одного случая, где Карина встала повторно после первого толчка (19.4 мин на втором цикле). Именование сессий продолжает дрейфовать при каждом рестарте (nova-40 как имя своей сессии в этом окне), опознание идёт по id из controller-verdict.py, не по имени. Обновил снимок очереди (queue-snapshot.py) один раз по требованию стража. Побеги Stop-стража за 24ч (28 штук) названы владельцу фактом, не разбирались — не в границе роли. Таймер тот же `8b82e051`.

**Дополнение 14:56.** Правка команды исполнена (владелец дал две последовательные формулировки): 30-минутный вопрос теперь несет 4 части — рамка «основная задача — релиз Карины», «какой пункт плана по релизу», «приближает ли оптимально», и новая четвертая — «если нет, согласуй с интегратором». Разослано новой формулировкой в 14:44 всем 4 живым (success: true). Ответ nova-a5 (14:45): закрыла #1239(половина)/#1252/#1255/#1264, подала 2 новых дефекта оракула/резолвера, восстановила #1249 — считает работу оптимальной по модели «беру то, что дают интегратор/Карина». Толкнула nova-6e в 14:41 (28.6 мин стоянки, success: true), с 14:53 интегратор поднял свой ярус — остальные легитимно ждут тира. Именование сессий снова сменилось (`nova-40`/`nova-6e`/`nova-46`/`nova-37`/`nova-a5` — новый слой поверх тех же id из `controller-verdict.py`). Таймер тот же `8b82e051`.

**Дополнение 13:48.** Спокойный участок: `nova-6e` вышла из долгой стоянки в 13:27
(текст сменился), остальные окна легитимны непрерывно. `nova-a5` (помощник) ответила
на 30-минутный вопрос в 13:28: закрыла #1252, #1255, свою половину #1239, нашла новый
К1-дефект оракула (мискомпиляция sum auto-wrap round-trip) — считает работу оптимальной.
30-минутный такт шёл в 12:06/13:20 (все доставлены), часовое напоминание про `/delegate`
— в 10:06Z (4 живым). Владелец пока не сформулировал правку команды `push-controller.md`
(с хода 12:41 — без изменений). Таймер тот же `8b82e051`.

**Дополнение 12:41.** `nova-6e` стояла на неизменном тексте с 10:24 до ~12:34 (более
двух часов), толкалась почти каждый цикл, все доставки `success: true`; сейчас наконец
в тире вместе с интегратором и `nova-a5` — предмет остановки (масштаб 21 место/15
файлов вне `runtime/string`, ждёт слово владельца) закрылся сам через общий ярус.
30-минутный такт вопроса шёл в 11:13/12:06 (все 6 доставлены), часовое напоминание про
`/delegate` — в 08:56Z (4 живым). Владелец пока не сформулировал правку команды
`push-controller.md`, обещанную после «пиши тут ответ на вопрос…» — вопрос остаётся
открытым, цикл продолжается без изменений в команде. Таймер тот же `8b82e051`.

**Дополнение 11:34.** `nova-6e` стоит ~70 минут подряд, толкается почти каждый цикл
(08:41→11:34, все доставки `success: true`), последний реальный текст неизменен с
10:24 (партия 13 мест закрыта, всплыл масштаб 21 место/15 файлов вне `runtime/string`,
ждёт слово владельца по расширению). Остальные окна легитимны непрерывно, единичные
STOOD у интегратора закрывались с первого толчка. 30-минутный такт вопроса шёл в
09:36(нет)/10:06/10:41/11:13, часовое напоминание про `/delegate` — в 07:49Z (4 живым).
Владелец на прямой вопрос про правку команды (после «пиши тут ответ на вопрос…») пока
не дал формулировки правки — жду его слов, продолжаю обычный цикл. Таймер тот же
`8b82e051`.

**Дополнение 09:56.** Спокойный участок: все окна легитимны циклами подряд (свой ярус
или own-tree build/test), STOOD не встречался с последнего толчка `nova-6e` в 09:20.
`nova-6e` по слову владельца правила `std/src/runtime/string/slice.nv` (замена
`bytes[off]` → `.read_at(off)`, вне лестницы плана 274, проверено компилятором и
корпусом). 30-минутный такт вопроса отправлен в 09:06 (все 6 доставлены), часовое
напоминание про `/delegate` — в 06:48Z (4 живым, `success: true`). `nova-p283`-окно
второй час недостижимо в `ListAgents` — толчки и вопросы ей не доставляются, факт
называется, а не разбирается. Таймер тот же `8b82e051`.

**Дополнение 08:41.** Интегратор опубликовал 175+ накопленных коммитов на все три
зеркала (`b4b988424`, проверено `git ls-remote`) — узкое место ночи закрыто, очередь
интегратора пуста, работу считает оптимальной. `nova-6e` (дерево `nova-cli`) вставал и
толкался четыре цикла подряд (07:58→08:41), доставки все `success: true`. Такт вопроса
дал ещё одну рассылку (08:13). Окно `nova-p283`/`5383c33a` второй-третий цикл подряд
недостижимо в `ListAgents` (имя не резолвится) — толчок и вопрос ей не доставлены, факт
называется, а не разбирается. Часовое напоминание про `/delegate` разослано 05:41Z всем
живым (nova-37/nova-46/nova-a5/nova-6e), метка обновлена. Таймер тот же `8b82e051`.

**Дополнение 07:41.** Ночь продуктивна: интегратор слил все накопившиеся ветки
(p274-novac×2, p283-float-parse, measure/1186), очередь интегратора пуста, ~155
коммитов ждут пуша, девятый гейт ночи идёт чисто. Карина починила ICE №1229 (блокировал
меру самосборки S5 всю ночь) и получила первое настоящее число ступени 0.2. Такты
вопроса о плане/релизе прошли в 04:34/05:06/06:06/06:41/07:13/07:41 — все живые окна
отвечают содержательно и считают работу оптимальной. Ярус дерева `nova` долгий (сейчас
с 06:51:39), толкать некого пока держится. Таймер тот же `8b82e051`. Побегов стража 24
за 24ч (интеграторский снимок + один побег самого интегратора 07:15, не мой предмет).

**Дополнение 06:34.** Дерево `nova` держит долгий общий ярус с 05:43:49 (интегратор,
помощник, `nova-6e`), Карина работает своим ходом (несколько коммитов за час, последний
`32478250f`). Хук `guard-stop-v2.py` продолжает блокировать мой ход на интеграторском
снимке `target/queue.json` (слияния трёх веток) — не беру эти пункты на себя, отвечаю
кодом `СТОП: неавторизовано слияние-веток` по правилу, внесённому в 04:20. Такт вопроса
о плане/релизе шёл в 04:34, 05:06, 06:06 — все живые окна отвечали содержательно
(интегратор: приёмка находок + слияние Карины, оптимально; Карина: нашла более широкий
дефект №1232 про ASI-границы, блокирующий К1). Таймер тот же `8b82e051`. Побегов стража
24 за 24ч (все на снимке интегратора).

**Дополнение 05:27.** Владелец заказал новый такт: каждые 30 минут спрашивать все живые
окна дословно «какой пункт плана делаешь?» и «твоя работа приближает релиз Карины
оптимальным образом?» — правка внесена в `.claude/commands/push-controller.md`
(раздел «ВОПРОС О ПУНКТЕ ПЛАНА»), первая рассылка 04:34, вторая 05:06, обе доставлены
пяти живым окнам (`success: true`). Между тактами продолжал толкать по сырому сигналу
без разбора: интегратор, помощник, Карина, `nova-p283`, `nova-6e` вставали и толкались
циклично, все доставки `success: true`. Таймер тот же `8b82e051`. Побегов стража 23 за
24ч — три новых на снимке `target/queue.json` (роль `integrator`, чужой предмет), уже
объяснено владельцу и учтено в команде.

**Дополнение 04:20.** Хук `guard-stop-v2.py` дважды подряд (04:06, 04:20) блокировал
мой ход снимком `target/queue.json` с тремя пунктами слияния веток
(`p274-novac`/`p283-float-parse`/`p717-ro-provenance`) — снимок несёт `role: integrator`,
то есть чужой предмет, а хук не различает роль вызывающего окна. Владелец подтвердил:
остановка решается только его словом, сырой код хода этого не отменяет. Правка
внесена в `.claude/commands/push-controller.md` (раздел «ОСТАНОВКА — ТОЛЬКО СЛОВОМ
ВЛАДЕЛЬЦА»): пункты снимка с чужой ролью не входят в границу роли, называются один
раз и не дают повторной остановки. Проверено `check-guard-honesty` — ок. Разослал
также часовое напоминание про лимиты и `/delegate` четырём живым окнам (интегратор,
помощник, Карина, `nova-6e`), все `success: true`, метка поставлена. Таймер тот же
`8b82e051`. Побегов стража 22 за 24ч (было 20 — три новых, все на снимке очереди).

**Дополнение 03:20.** `nova-6e` (`3b5937f0`) вставала повторно пять циклов подряд
(01:41→03:20) — толкал каждый раз по сырому сигналу без разбора причины,
`success: true` во всех случаях; молчание не накопилось (6–10 мин каждый раз,
новый ход после толчка). Остальные окна дерева `nova` (интегратор, помощник,
Карина) чередуют работу и короткие стоянки — все толчки доставлены. Таймер тот
же `8b82e051`. Побегов стража 20 за 24ч (было 22 — два вышли за окно суток).

**Дополнение 02:13.** Опознал Карину надёжно: id `bf14b5f5`, дерево `nova-p274`,
подтверждено ею самой в ответ на опрос («да, это я»); имя `nova-d0` протухло,
текущее меняется на каждом рестарте (`nova-46`, затем другое) — карта имён не
хранится, сопоставление по id каждый раз заново. За цикл толкал по сырому
сигналу без разбора причины (правило «толкать всегда»): Карину, помощника
(`2ed7d346`), `nova-p283` (`5383c33a`), интегратора (`3b203470`), `nova-6e`
(`3b5937f0`) — все доставки `success: true`. `claude-limits` (`e601e366`)
остаётся вне охвата контролёра — не толкаю никогда, это граница дерева, а не
исключение из правила толчка. Сейчас (02:12:20) все четыре сессии дерева `nova`
держат общий ярус — толкать некого. Таймер тот же `8b82e051`. Побегов стража 22
за 24ч (было 23, один вышел за окно суток).

Лимит аккаунта сброшен в 00:20 (Europe/Moscow), как и был объявлен. Толкал по
сырому признаку раз имена сопоставлялись: помощник (`nova-a5`) вставал два
раза подряд после сброса и оба раза сдал смену следом (коммиты
`d9d3cdba1` в `assistant-handoff.md`); интегратор (`nova-37`) тоже вставал и
толкался. Мой прошлый толчок в `nova-e8` был ошибочной догадкой на роль
Карины — `e601e366`/`nova-f1` подтвердил, что это дерево `claude-limits`, вне
охвата контролёра; больше не гадаю по этому кандидату. Новая, ещё не
сопоставленная сессия `6a42a07e` (дерево `nova-cli`) появилась около 00:16 и с
00:48 держит общий слот `build/test` главного дерева вместе с интегратором,
помощником и `e601e366` — судя по времени, это один прогон на четверых.
Таймер тот же `8b82e051`. Побегов стража 21 за 24ч (было 20) — новый вид у
интегратора: «второй вопрос подряд без единого действия между ними».

**Дополнение 23:40 мест. (20:41Z).** Общий лимит аккаунта (сброс 00:20) держит
Карину/помощника/`claude-limits` весь час, толчков им не было — текст не менялся.
Интегратор (`nova-37`) за это время дважды вставал и толкался (22:56, 23:06, 23:13,
23:20), один раз ошибочно счёл смену сданной («Код „смена" без сданной смены» —
побег стража 22:57), сам это признал и продолжил; сейчас держит свой ярус с
23:36:39 вместе с двумя другими окнами того же дерева (общий слот главной копии).
Таймер тот же `8b82e051`. Побегов стража 20 за 24ч (было 21 — старые вышли за окно
суток).

**Дополнение 22:41 мест. (19:41Z).** Несколько рестартов подряд (19:32, 20:06 и
позже) — таймер каждый раз заводился заново под новым id, последний `8b82e051`,
шаг тот же. Толкал по сырому признаку, пока имена не протухали: доставка
`nova-a5`/`nova-6e`/`nova-e8` (кандидат для Карины, не подтверждён) — все
`success: true`. Соседи независимо нашли и передали два факта: интегратор
(`nova-37`/`nova-7e`/`nova-47` — три имени одной сессии за смену) вёл слияния
Карины и помощника; окно `claude-limits` (`nova-e8`/`nova-f1`) поймало у себя
дефект `guard-stop-v2.py` — протухшая визитка отдаёт роль ИНТЕГРАТОРА первому,
кто зашёл в главное дерево на `main`, — передано интегратору для разбора, сам не
чинил. С 20:34–20:50 у трёх окон (Карина, помощник, `claude-limits`) один и тот
же текст — общий лимит аккаунта, сброс 00:20 (Europe/Moscow); не толкаю, жду
срока. Побегов стража 21 за 24ч, новый вид (19:35) — сбой разбора хука у
`e601e366`.

**Дополнение 14:59 мест. (11:59Z), ОСТАНОВКА.** Владелец обновил правило толчка ещё
раз: 06:39 запись ниже сняла оба словесных исключения («заморозка», «пустая
очередь») — 2026-09-20 он подтвердил дословно: «мы с тобой договаривались, что ты не
смотришь, кто на чём остановился, а просто толкаешь». Обновил
`.claude/commands/push-controller.md` и добавил `test-push-always.py` (см. запись
06:39), затем толкал строго по сырому признаку `controller-verdict.py` весь остаток
смены — `nova-ac` (`2ed7d346`) и `nova-78` (`829b98e3`) по STOOD, оба `success: true`,
без учёта их же слов о причине стоянки (`nova-78` дважды кончала ход `СТОП: вопрос»`
с вопросом, снять ли её с обхода — не читал это как повод не толкать).
**Новая находка того же класса, что 14:19 (роль по ветке, не по автору):** снимок
`target/queue.json`, снятый с главного дерева на `main`, подписал очередь
`role=integrator` и перечислил ЧЕТЫРЕ слияния веток как открытые пункты; Stop-страж
`guard-stop-v2.py` читает этот же снимок и потому потребовал у контролёра слить
чужие ветки — я это не сделал (слияние в `main` не входит в границу роли), спросил
владельца вопросом, ответа в рамках этого хода не получил. **Владелец: «стоп, выключи
таймер»** — таймер `27dab074` снят (`CronDelete`), обход контролёра ОСТАНОВЛЕН. Вопрос
про снимок остаётся ОТКРЫТЫМ для следующего окна-контролёра или для того, кто снимет
таймер снова. Побегов стража на 14:47 — 13 за 24ч (было 10), лог по-прежнему без имён.

**Дополнение 14:19 мест. (11:19Z).** Крупный разрыв во времени между циклами (09:20 →
14:17) — обход возобновил штатно. **Интегратор (`3b203470`, было имя `nova-47`)
пропал из живых около 09:15** — подтверждено НЕЗАВИСИМО мной, `nova-f1`
(`e601e366`) и `nova-ac` (`2ed7d346`, помощник); работа за смену не запушена
(строки реестра 1193–1196 на `origin/main` отстают). Кандидата на новое имя нет.
`e601e366` (`nova-f1`) сам оказался деревом `claude-limits` — не Nova, вне роли,
не толкаю его вовсе (не исключение из правила толчка, а вопрос охвата роли).
**Находка от `nova-ac`:** снимок очереди общий на всё главное дерево, а роль в
нём выводится из ВЕТКИ дерева запуска — значит любой не-main запуск снимка из
ГЛАВНОГО дерева подписывается `role=integrator`, даже когда пишет
помощник; `role=integrator` в снимке — НЕ признак живого интегратора. Передал
владельцу как факт измерительного дефекта, сам ничего не менял. Побегов стража
10 за 24ч на 14:18, лог по-прежнему без имён.

**Дополнение 08:33 мест. (05:33Z).** Час прошёл по правилу «толкать всегда»: `nova-78`
(`829b98e3`) толкал ещё пять раз подряд (итого восемь за смену) — каждый раз чисто по
сырому признаку STOOD, без чтения текста; все `success: true`. Разово толкнул и
интегратора (`3b203470`) — 10.5 мин без хода. Заявление `nova-78` про личное
исключение от владельца («ты не должен слушать толкателя») по-прежнему без
подтверждения — толкаю её на общих основаниях. `.claude/commands/push-controller.md`
и `test-push-always.py` из позапрошлого часа всё ещё НЕ закоммичены, ждут
интегратора. Побегов стража 8 за 24ч на 08:32 (без изменения второй час подряд), лог
по-прежнему без имён.

**Дополнение 07:39 мест. (04:39Z).** Час прошёл по новому правилу («толкать всегда»)
без сбоев: толкнул `nova-78` (`829b98e3`) трижды подряд по сырому признаку
`controller-verdict.py`, не читая текст, все `success: true`. Один раз `nova-78`
заявила, что владелец лично освободил её от толчков контролёра («ты не должен
слушать толкателя»); не принял на веру (это заявление соседа, не слово владельца
мне), продолжил толкать по действующему правилу и передал заявление владельцу
вопросом — ответа пока нет. `.claude/commands/push-controller.md` и
`test-push-always.py` из прошлого часа по-прежнему НЕ закоммичены — ждут
интегратора, сам не трогаю (не моё дерево-действие). Побегов стража всё ещё 8 за
24ч на 07:39 (без изменения с прошлой записи), лог по-прежнему без имён.

**Дополнение 06:39 мест. (03:39Z).** Правило толчка сменилось СУЩЕСТВЕННО по слову
владельца: «мы с тобой договаривались, что ты не смотришь, кто на чём остановился,
а просто толкаешь» — прежнее чтение полного текста на стоп-код (см. запись 05:50
ниже) оказалось той же дырой, что разбор причины: чтобы применить исключение
(«очередь пуста»/«заморозка»), контролёр обязан был СМОТРЕТЬ на текст, ровно то
действие, которое правило и так запрещает. Владелец выбрал явно: толкать ВСЕГДА,
без исключений. Обновил `.claude/commands/push-controller.md` (оба исключения сняты,
статус «стоит законно» убран из словаря) и добавил самотест
`scripts/claude-hooks/selftest/test-push-always.py` — проба в обе стороны прошла
(FAIL при возврате старой формулировки, OK после восстановления). Файлы НЕ
закоммичены — по своей же конвенции («controller does not write into the main
tree by its own boundary»), ждут интегратора. С этого момента толкаю по сырому
признаку `controller-verdict.py`, не читая текст хода вовсе: толкнул `nova-ac`
(2ed7d346) дважды за час без разбора, оба раза `success: true`; она подтвердила,
что оба толчка попали по делу. Побегов стража 8 за 24ч на 06:37 (первый новый со
времени правки — не связываю, лог по-прежнему без имён).

**Дополнение 05:50 мест. (02:50Z).** Час прошёл штатно. Урок из измерительной дыры
применил на практике: перед каждым толчком читаю ПОЛНЫЙ текст хода (не только сводку
`controller-verdict.py`) на стоп-код — так не толкнул `nova-ac` (session:2ed7d346, взяла
`serde`/1183, замолчала 12 мин, стоп-кода не было — толкнул) и НЕ толкнул Карину
(session:bf14b5f5, `СТОП: очередь-пуста` с обоснованием — 4 формы плеча закрыты, 5-я
заблокирована типизацией связывателей E2-b3, ждёт слияния интегратора). Владелец
спросил прямо, почему не толкнул Карину при 10 минутах тишины — ответил цитатой её
стоп-кода. Закоммитил свою же незакоммиченную запись по второй за смену просьбе
интегратора (`ea0b10d97`) и договорился коммитить её сразу тем же ходом, что пишу, —
разрыва между «написал» и «закоммитил» больше не будет. `nova-af` (session:5383c33a) нашла
и одна закрыла столкновение D478→D482 (`777d821e1`) и предложила чинку измерительной
дыры — привязать исключение «не толкать» в `push-controller.md` к машинным кодам
`flow.md`/`guard-stop-v2.py`; передал владельцу, сам не менял. Побегов стража 7 за 24ч
на 05:47, лог по-прежнему без имён.

**Дополнение 04:48 мест. (01:48Z).** Свой перезапуск (таймер cron:27dab074 вместо погасшего
cron:bea6b0db, тот же шаг). Закоммитил свою же незакоммиченную ночную запись по прямой
просьбе интегратора (`b3ec6a5ab`) — тот же узкий прецедент, что раньше: разблокировал
его пуш, не тронув ничего чужого. **Найден дефект своего измерителя**, важнее любого
отдельного толчка: `controller-verdict.py` ошибочно толкнул `nova-af` (session:5383c33a) —
у неё БЫЛ стоп-код («СТОП: вопрос» дважды подряд, стоит законно по слову владельца
«стоп»), а скрипт судит только возраст и служебный тип последней записи, не читая сам
текст на стоп-код. Признал ошибку перед `nova-af`, она подтвердила диагноз и добавила
второй пункт: цена ложного «стоит без кода» и ложного «всё в порядке» несимметрична,
и форма толчка («ПЕРЕДАЮ: … ПРОШУ: ничего, ответа не жду» — приказ) дороже получателю,
чем короткое подтверждение («стоп-кода не нашёл — подтверди») — предложила сменить
форму. Не взял на себя: форма толчка задана `.claude/commands/push-controller.md`, менять
её не в моей власти, как и правило «толкай без разбора» (тот же класс отказа, что
предложение `nova-90` про сверку побегов перед толчком). Обе находки — измерительная
дыра и предложение формы — переданы владельцу как факты, решение за ним, ничего не
менял сам. Побегов стража 7 за 24ч на 04:47, лог по-прежнему без имён.

**Дополнение 03:46 мест. (00:46Z).** Час прошёл спокойно: толкал по одному стоящему за
цикл (Карину дважды — стоянка после её `СТОП: смена`, потом ответ на «дальше — реестр»
не понадобился, `nova-78`/session:829b98e3 — ответила владельцу и замолчала на 15 мин),
интегратора (session:3b203470) — после коммита. Все толчки `success: true`. Карина один раз
стояла ЗАКОННО — реальный `СТОП: вопрос» владельцу («продолжать сейчас или
остановиться, идёт четвёртый час ночи?»), не толкал, дождался её же ответа и
продолжения. Появилась новая роль `assistant` (session:2ed7d346, ранее «помощник» без
записки) — завела `docs/dev/prompts/assistant-handoff.md` по прямому слову владельца,
коммит `958c0e9d2`; её маршрут к записке теперь в `/save`/`/stop`/`/assistant`, у неё
своя визитка, ветка называет ЗАДАЧУ, а не роль. Снимок очереди (`queue-snapshot.py`)
освежал по требованию стража ещё раз — то же самое разблокирование своего конца хода,
не взятие чужой очереди. Побегов стража 7 за 24ч на 03:46, лог по-прежнему без имён.

## 0-2026-09-20-ночь. Состояние на 2026-09-20, ночь

Свой перезапуск в 02:44 мест. — таймер `session:bea6b0db` погас (7 дней истекли по
устройству `CronCreate`), завёл заново: cron:27dab074, тот же шаг 7 минут. Имена/id
соседей опять другие после их же перезапусков (см. дежурство ниже) — сопоставлял по
дереву и последнему тексту, не по памяти.

**Дежурство после рестарта.** Толкнул троих подряд, все `success: true`: Карину
(дисплей `nova-d0`, id session:bf14b5f5, тише 51 мин после её `СТОП: смена` — очередь у
неё была НЕ пуста, сама назвала остаток ICE/многофайловость), помощника (дисплей
`nova-ac`, id session:2ed7d346, 61 мин, назвала «дальше — реестр» и замолчала), интегратора
(дисплей `nova-47`, id session:3b203470, 5 мин после коммита, «беру оставшиеся четыре»).
Механика прежняя: правило не разбираю, толкаю по её же слову, доставку проверяю
результатом `SendMessage`, а не фактом вызова.

Появился новый снимок очереди — `python scripts/tools/queue-snapshot.py .`
(план 292 Ш.2): читает записку интегратора и кладёт кэш в `target/queue.json` вне
git; освежил его один раз по требованию Stop-стража (`role=none ok=true open=0`) —
это не взятие чужой очереди, а разблокировка своего же конца хода.

Побеги стража продолжают копиться без имён (8 за 24ч на 02:45) — тот же известный
пробел плана 292 §5б, не считаю это отдельно пропущенной остановкой соседа.

**Дополнение 03:13 мест. (00:13Z).** Механизм сменился: замечать остановку теперь
делает Stop-страж (`guard-stop-v2.py`) и пульс ролевых записок (`role-pulse.py`) —
мой обход стал вторым рубежом, ловящим побеги стража. Механику применил: `nova-90`
и Карину толкал по вердикту `controller-verdict.py` без разбора причины, оба раза
`success: true`. `nova-91`=`session:d88c3231` стоит законно (её слово: «очередь пуста, план
закрыт»). Побеги стража — два старых случая (01:57, 02:00), оба мои же отказы от
чужой очереди слияния веток (`target/queue.json` требовал взять 12 пунктов интегратора
— отказался явно, СТОП-кодом). `nova-90` предложила ПЕРЕД толчком сверять побеги
стража вместо чистого правила «толкай без разбора» — это смена правила роли, не в её
власти и не в моей; передал владельцу как факт, решение за ним, правило не менял.
Сейчас интегратор/`nova-90`/`nova-91` ждут общий ярус `tier` (законно), Карина —
рано судить.

**Дополнение 01:57 мест. (22:57Z).** Цикл идёт штатно. Интегратор снял 7 мёртвых worktree
(включая мой `nova-c2` — подтвердил владельцу право снять, после проверки: старый файл
`docs/.sessions/handoff-*.md` от 18-го числа перекрыт текущей запиской). Новая сессия
`nova-90` продолжает план 292, стояла и была толкнута несколько раз. `session:de83dbfa` вышла из
списка живых (сессия закрыта). Появилась посторонняя `session:e601e366` (дерево `claude-limits`,
не Nova) — вне роли, не наблюдаю. Два хука `guard-stop-v2.py` в этом цикле требовали от
меня взять чужую очередь (`target/queue.json`, слияние веток) — это НЕ моя роль (граница
роли: только смотреть/толкать), сообщил владельцу, не взял.

## 0-2026-09-19-ночь. Состояние на 2026-09-19, ночь

**Что сделано за смену (продолжение с 2026-09-18):**

- Цикл `/push-controller` идёт непрерывно с 2026-09-18 08:44 мест., таймер `session:bea6b0db`,
  шаг 7 минут.
- Правило толчка упрощено дважды по слову владельца: сначала «толкать всех, кроме
  заморозки/пустой очереди», затем добавлено требование подтверждённой доставки
  (`success: true` у `SendMessage`) и запрет читать содержание текста («применяю
  своим ходом» ≠ признак работы) — обе правки внесены в `.claude/commands/push-controller.md`
  интегратором по моей передаче текста.
- За смену толкал интегратора и Карину десятки раз без разбора причины остановки.
- Новая живая сессия `nova-90` (id `session:2ed7d346`, ветка `research/regex-nlib-0917`, план 292)
  закрыла Ш.2 и объявила «моя очередь пуста, жду слова владельца» — стоит с 2026-09-18
  23:07:59, законное исключение.
- `session:de83dbfa` держит «жду следующего запроса» с 2026-09-18 21:41:32 — законное исключение.

**Что открыто и чьё слово нужно:**

- `nova-90` ждёт нового задания от владельца (её очередь пуста).
- Интегратор гонит гейт `tier` с 00:22:55 (2026-09-19) — идёт долго (26+ мин на момент
  записи), не толкаю: активный слот — не признак остановки.

**Чего НЕ делать:** правка любого файла в главном дереве самим контролёром — только
текстом интегратору; разбор причины стоянки соседа (снято словом владельца 2026-09-18);
толчок без проверки `success: true` доставки.

**Уроки смены:** дважды подряд принял слова сессии («жду слова о пуше», «применяю своим
ходом») за законное исключение, хотя владелец поправил — второе не было исключением.
Различитель: заморозка/пустая очередь — законные исключения; «жду вердикта своего же
длинного прогона» и намерение в настоящем времени — НЕ исключения, толкать. Смена дня
(00:00) сама по себе не отменяет заморозку с условием, если условие не про календарь
(«машину сегодня не трогаю» относилось к чужому гейту, не к дате) — проверять формулировку
условия, а не дату.

**Время и коммит:** 00:48 мест. (21:48Z), правка передана интегратору текстом.

## 0-2026-09-18-день. Состояние на 2026-09-18, день

**Что сделано за смену** (после `/stop` в ~08:44 мест., таймер `session:bea6b0db`):

- Визитка контролёра заведена (`session-card.sh write controller`) — по правке
  про `tree-map.json`/`UNKNOWN` роль.
- Найдена и передана интегратору правка `controller-limits-reminder.py` (путь
  `221.3`→`274.10`), закоммичена им (`c8d1d97c7`).
- Найдена и передана находка про пропажу метки часового напоминания и
  `tree-map.json` из `%TEMP%` (диск C: был забит) — интегратор объяснил причину
  и берёт перенос состояния роли в общий `.git` на себя, ещё не сделано.
  Заведено строкой реестра №1153 (интегратор, 2026-09-18).
- Цикл `/push-controller` идёт штатно каждые 7 минут, толчков не требовалось
  несколько часов подряд — интегратор и Карина оба работают/держат свой ярус
  легально.

**Что открыто и чьё слово нужно:**

- Перенос состояния роли (`last-limits-reminder.txt`, `tree-map.json`) из
  `%TEMP%` в общий `.git` — за интегратором, не блокирует контролёра.

**Чего НЕ делать:** правка любого файла в главном дереве самим контролёром —
только текстом интегратору; толчок с оценкой цены работы; письмо соседу без
нового факта.

**Уроки смены:** диск C:, заполненный почти под ноль (9.8 МБ свободно), вызывает
системную чистку `%TEMP%`, которая стирает состояние роли МОЛЧА — состояние,
живущее во временном каталоге, такой чистки не переживает. Перенос в общий `.git`
устраняет класс, а не только симптом.

**Время и коммит:** 13:34 мест. (10:34Z), `d91290413`.

**Раздел передан контролёром текстом и применён интегратором** — окно-контролёр
в главное дерево не пишет по своей же границе (правка 2026-09-18).

**Дополнение 17:48 мест. (14:48Z).** Цикл продолжает идти штатно каждые 7 минут по
упрощённому правилу («толкать всех, кроме заявленной заморозки/пустой очереди», см.
`.claude/commands/push-controller.md`). За истекший час толчков не требовалось: интегратор
и Карина (`nova-p274`) держат ход попеременно (гейт `build/test` в 17:34, свежий текст
каждый цикл), `session:de83dbfa` подтверждена законным исключением («очередь пуста»), `session:78551d82` —
посторонняя не-Nova сессия, вне роли. Часовое напоминание про `/delegate` было отправлено в
13:57:48Z, следующее — примерно в 14:58Z.

**Дополнение 19:25 мест. (16:25Z).** Цикл продолжает идти штатно каждые 7 минут.
За истекший час: интегратор и Карина (`nova-p274`) чередовали работу и гейты, встали
и были толкнуты дважды — Карина по синтезу memberwise-`@display` (после амендмента D109,
`46bc74599`) и по реализации приёмки (`bae24d208`) — оба раза по последнему названному
ими шагу, без разбора причины стоянки. `session:de83dbfa` подтверждена законным исключением
(«очередь пуста»), `session:78551d82` — посторонняя не-Nova сессия, вне роли. Часовое напоминание
про `/delegate` было отправлено в 13:57:48Z.

**Дополнение 20:28 мест. (17:28Z).** Цикл идёт штатно. За истекший час: Карина закрыла
волну синтеза memberwise-`@display` целиком, включая пятый пункт приёмки (дифференциал
EXIT=0, 36/36 байт-в-байт) и завела три новые фикстуры по своему же замечанию, что зелёный
корпус доказывал отсутствие регресса, а не работу фичи. Интегратор продолжает очередь
274.10 (№762). Толкал дважды: Карину (после закрытия волны — на дальнейшую очередь) и
интегратора (на №762) — оба раза по последнему названному шагу, без разбора причины.
`session:de83dbfa` — законное исключение («очередь пуста»), `session:78551d82` — посторонняя сессия.

**Дополнение 21:33 мест. (18:33Z).** Урок цикла: я толкал и сам, и по правилу «содержание
не признак», но два раза подряд провалился на новой форме той же ловушки — принял «жду
слова о пуше» (законное) и «применяю своим ходом» (НЕ законное, будущее в настоящем
времени) за одно и то же. Владелец поправил дважды: «интегратор стоит, мы же
договаривались, что ты просто толкаешь». Передал интегратору вторую правку
`.claude/commands/push-controller.md` (доп. пункт в разделе «ПРИЗНАКИ» про «применяю
своим ходом» / «в работе прямо сейчас») вслед за первой (подтверждённая доставка толчка).
Обе не применены мной — интегратор применяет своим ходом (сейчас это утверждение верно
по прямому вызову `SendMessage`, а не по чтению текста).

**Дополнение 22:35 мест. (19:35Z).** Цикл идёт штатно. Толкал Карину дважды (проверка
непроверенного случая К1/К2, затем сохранение записки), оба раза `success: true`.
`session:de83dbfa` держит статус «жду следующего запроса» (законное исключение) — стоит уже 53
минуты, наблюдаю, не толкаю, так как исключение объявлено ею самой словом и владелец не
отменял именно этот случай. `session:78551d82` (посторонняя) вышла из списка живых; появилась
новая посторонняя `session:2ed7d346` — вне роли, не наблюдаю детально.

**Дополнение 23:42 мест. (20:42Z).** Цикл идёт штатно. Новое живое окно `nova-90`
(`session:2ed7d346`, ветка `research/regex-nlib-0917`, план 292) закрыло Ш.2 и объявило «моя
очередь пуста, жду слова владельца» — законное исключение, стоит без толчка. `session:de83dbfa`
всё ещё в исключении «жду следующего запроса» (120+ мин). Интегратор и Карина оба
несколько раз вставали и толкались подряд — оба раза без разбора причины, включая случаи
«доложу как сядет» и «пуш завтра одним заходом», которые НЕ являются законным
исключением (только заморозка с условием или пустая очередь освобождают от толчка).

# Снимок окна-контролёра — 2026-09-14, 22:50Z (01:50 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в команде
> [`.claude/commands/controller.md`](../../../.claude/commands/controller.md) (заведена
> 2026-09-08 по слову владельца: «сделай команду для запуска со всеми правилами, чтобы ничего
> не потерялось после перезапуска»). Задание `CronCreate` её ВЫЗЫВАЕТ, а не повторяет
> (перечисление шагов из его тела убрано 17:10Z как вторая копия — строка реестра №1042).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> КОМАНДУ, а не сюда.
>
> **Id заданий и сессий пишутся БЕЗ обратных кавычек** (правка 22:50Z 2026-09-14): страж
> `check-commit-refs` опознаёт хеш коммита по форме токена в кавычках и красил гейт на моих
> id заданий как на «недостижимых из main» — они коммитами не являются вовсе.

## ГРАНИЦА РОЛИ — ДВА ДЕЙСТВИЯ, СПИСОК ЗАКРЫТ

Слово владельца 00:41 мест. 2026-09-13, дословно: «ты контролер, ты только контроируешь
работу др окон и толкаешь их, когда они замирают, больше ничего не делаешь без согласования
со мной». Разрешено: **смотреть** (обход, вердикт по признакам, дежурство, чтение чужих
историй и деревьев, доклад владельцу) и **толкать** замершее окно по ЕГО плану, плюс
дословный вопрос стоящему. Всё прочее — со слова владельца: правка любого файла в дереве,
рассылки, заведение расписаний и инструментов, прогоны чужих стражей, **взятие задач ОТ
СОСЕДЕЙ** (просьба соседа идёт владельцу строкой «<окно> просит <что>», а не исполняется).

## Опознание сессии

- id ef28f97e-e5fa-482b-8c44-c8e039d7d396, имя для соседей на 01:40 мест. 2026-09-14 —
  **`nova-b8`** (имена меняются БЕЗ рестарта сессий — брать из `ListAgents`, не отсюда).
- Модель **Opus**. Механика вынесена в скрипты; дорогая модель тратится на суждение
  «встал или ждёт законно».
- **После рестарта:** сказать `/controller` — команда несёт всё, включая стыковку. Затем
  завести расписание (`CronCreate`, шаг 7 минут, тело — вызов `/controller`): оно живёт только
  в сессии и гаснет через 7 дней, проверять `CronList` КАЖДЫЙ цикл и требовать ровно одну строку.
- **Инструменты — в репозитории, `scripts/tools/`.** Запускать из корня главного дерева:
  `controller-dock.py 2` (стыковка), `controller-peers-scan.py [N]`,
  `controller-peers-deep.py <id-prefix> [K] [tail_mb]`, `controller-machine-watch.py`
  (дежурство), `controller-verdict.py [--prove]`, `controller-limits-reminder.py`,
  `controller-selftest.py`.
- Передача по `/stop` живёт В ЭТОМ ЖЕ ФАЙЛЕ — разделом сверху, со словом
  ОСТАНОВКА в заголовке (правка 2026-09-18 по слову владельца). Прежний адрес
  `docs/.sessions/handoff-<id>.md` стоял под `.gitignore`: написанное туда не
  переживало ни клон, ни перезапуск машины и не было видно никому, кроме самого
  окна.

## Соседи — снимок 01:42–01:50 мест. 2026-09-14

| имя | дерево (ОБЪЯВЛЕНО окном) | id истории | состояние |
|---|---|---|---|
| `nova-be` | `nova` — главная копия, роль интегратора | 3b203470… | СЕДЬМОЙ ярус с 01:17:14; мега-CU прошёл `PASS: 925 FAIL: 1` (один прощённый красный), впервые за семь заходов корпусный шаг зелёный; шёл `conformance-full`. В 01:49 машина уже FREE |
| `nova-6e` | `nova-p274`, волна 274.7 В13 | bf14b5f5… | 51 коммит ждёт пуша; ветка заморожена (признак работы — mtime в `scratch/`, НЕ коммиты). Ищет носитель ДВОЙНОГО `@cleanup`; реализацию не начинают до слова владельца |
| `nova-5c` | `nova-p283`, план 285 | 5383c33a… | ОСТАНОВЛЕНО по `/stop` владельца. Коммиты 9c8dc0063 (№1096), a0425dfdb (D478), ddcda7d35, 359236bed; грязный `std/src/runtime/decimal/core.nv` |
| `nova-9c` | `claude-limits` (дом) и `nova-duckdb` | e601e366… | план 286, фасад под соединение; тег `nova-duckdb v0.1.0` ждёт слова владельца |

### КАРТА ДЕРЕВЬЕВ — ДАННЫЕ, А НЕ ВЫВОД (единственное место снимка, где лежит состояние)

Живой файл карты — `<scratchpad>/controller-reminder/tree-map.json`, ключ = **id сессии**
(имена меняются, id нет). Он рестарта НЕ переживает, поэтому дублируется здесь. Четыре
починки вывода дома по тексту истории лишь ПЕРЕМЕЩАЛИ ошибку, пока источником не стало слово
самого окна. Формулировка окна `nova-9c`: «чините приписку под этот случай, а не под догадку».

```json
{"e601e366": {"trees": ["claude-limits", "nova-duckdb"], "said_by": "the window itself"},
 "bf14b5f5": {"trees": ["nova-p274"], "said_by": "its own commands and commits"},
 "5383c33a": {"trees": ["nova-p283"], "said_by": "the window itself"},
 "3b203470": {"trees": ["nova"], "said_by": "role: the integrator works in the main copy"}}
```

### Признаки работы, названные САМИМИ окнами

Проверять ПЕРЕД толчком; своё суждение уступает им — они назвали признак заранее, а не в
оправдание после.

- **окно 283 (`nova-5c`), 01:5x 2026-09-14 — заменяет прежний признак по `fmt_buf/**`:**
  `git -C d:/Sources/nv-lang/nova-p283 log --oneline -1 p283-float-parse` — сдвинулся хэш,
  значит работают; не сдвинулся ДОЛЬШЕ 20 минут — основание СПРОСИТЬ, не вердикт. Файловый
  признак у них **ложно отрицательный по построению**: черновики живут в скретчпаде сессии,
  вне `nova-p283`, и правка видна только став коммитом. Оговорка их же: 20 минут — граница
  ИХ темпа, между коммитами законно проходит долгий разбор.
- **окно 274 (`nova-6e`):** пока ветка заморожена, коммит из признаков ВЫЧЕРКИВАЕТСЯ —
  отсутствие коммитов есть правильное состояние, а не простой; годен только mtime в `scratch/`.
- **окно `nova-9c`:** тронуты файлы под рабочими каталогами — работа; тронут ТОЛЬКО их
  файл-снимок — это подготовка к стоянке, а не работа.

## ДЕФЕКТ ИНСТРУМЕНТА, КОТОРЫЙ НАДО ЗНАТЬ СЛЕДУЮЩЕМУ ОКНУ РОЛИ

`controller-verdict.py` за ночь 13→14 сентября **СЕМЬ раз** сводил чужое окно в главную копию
`nova` и выдавал ему статус `waits for its own tier` по ЧУЖОМУ ярусу интегратора (страдали 283
и `claude-limits`, однажды 274). Такой статус в таблицу НЕ пускать: признак брать из ИХ дерева
(`git -C <дерево> log --oneline -1`, `status --porcelain`) и из `controller-peers-deep.py <id> 1 2`.
Починка не сделана — она за границей роли, слово владельца не получено.

## Журнал — свёрнут до уроков (подробности в истории сессии и в команде роли)

- **Статус окна считает СКРИПТ, не я** (слово владельца 14:05 мест. 09-09):
  `controller-verdict.py`, закрытый список признаков, печать самого признака. Свой предел он
  называет сам: отвечает «есть ли признак работы», а НЕ «законна ли стоянка».
- **Дом окна ОБЪЯВЛЯЕТСЯ окном** — карта выше.
- **У стоящего надо СПРОСИТЬ** (слово владельца 17:03 мест. 09-09): «ты всё сделало по своему
  плану?» — дословно, с замером, на котором вопрос основан. Ответ идёт владельцу: он
  показывает, можно ли давать окну новую работу, а замеры этого не показывают.
- **Правки в главное дерево — только при свободной машине**, дежурство ДВАЖДЫ, решает второй
  замер; отложенное лежит в `<scratchpad>/journal-pending/`.
- **Ошибки роли — одного класса:** измерение сохраняет форму и смотрит в другое место.
  Носители названы в команде поимённо — читать их там, а не восстанавливать по памяти.
- **МЕТКА ВРЕМЕНИ В ПИСЬМЕ БЕРЁТСЯ КОМАНДОЙ `date`, А НЕ ПЕРЕСЧЁТОМ UTC В УМЕ** (моя ошибка
  01:44 мест. 2026-09-14): я поставил в два письма соседям `22:44Z (01:44)`, а по команде было
  22:36Z / 01:36–01:40. Решений это не изменило, но метка — то, по чему сверяют чужие прогоны.
  Правило в команду ещё НЕ дописано: правка команды за границей роли, слово владельца не дано.

## Хроника ночи 13→14 сентября — свёрнуто до фактов

- **Толчков за смену НОЛЬ.** Все стоянки оказались законными: интегратор под своими ярусами
  (шестой 00:48:47, седьмой 01:17:14), окно 274 (51 коммит ждёт пуша, оба блокера чужие),
  окно 283 (слово владельца + сборка у интегратора), `claude-limits` (вопросы владельцу).
- **Писем за смену — три:** карта адресов окну `nova-9c`; отказ окну 274 взять их просьбу о
  разборе спеки (просьба соседа не есть задание); вопрос владельца стоящему окну 283.
- **Окно 274 уронило ПЯТЫЙ прогон интегратора** временным деревом `nova-p274-old` → строка
  реестра **№1098**, класс «шаг гейта судит живое разделяемое состояние машины, которым не
  владеет»; их правило: под чужим гейтом не запускать общий бинарь И не создавать/снимать worktree.
- **Интегратор дал окну 283 номер 1096** и вынес решение: спек-амендмент НЕ нужен, правило уже
  нормативно, чинится в канале чекера 196.
- **Ответ окна 283 на вопрос владельца (01:5x):** незаблокированного в очереди НЕ осталось,
  стоянка законная. Остаток — правка `decimal/core.nv` ждёт слота на сборку; синк ждёт
  непушенных №1084–1098 интегратора; вопрос о позиционной передаче умолчательного поля ждёт
  слова владельца (D102 запрещает, std делает так в 126 вызовах, зелёная фикстура
  `m_ice_named_tuple_positional_defaults_pos.nv` пинает как «verified-non-bug»).

## Открытые вопросы к владельцу — ни один не закрыт

1. **окно 274 ПРОСИТ разбор:** должен ли `consume`-ПАРАМЕТР автоматически чиститься через
   `@cleanup` (D432, §4 `spec/decisions/02-types.md:6401`, амендмент 2026-08-21 = реестр №672).
   Их довод: D432 объявляет семантику такого типа аффинной, но освобождает только форму
   `consume X = e`; §4 берёт донором семантику Rust `Drop`, где владеющий параметр дропается
   как `let`; §4 п.3 говорит, что вызывающий дизармит флаг ПРИ ПЕРЕДАЧЕ — значит после
   передачи чистить некому. Их волна 274.7 В13, строка реестра №1099; реализацию не начинают.
2. **`claude-limits` — три пункта по плану 286** (строки 466–489, этап Д5 строка 1176): фасад
   остаётся только под соединение и `Db` подлежит ЗАМЕНЕ (покрыто 11 вызовов C из 60); уместен
   ли тег `nova-duckdb v0.1.0` сейчас; не блокирует ли Д5 оговорка про окно между Э1–Э3 и Э5
   плана 279. Плюс три прежних с их рекомендациями: `--install-autostart` по-настоящему не
   запускать; эффект оставить двухслойным; историю оставить, вопрос в СРОКЕ тега.
3. **окно 283:** позиционная передача умолчательного поля — от ответа зависит, просыпается ли
   класс маркера `[M-fixture-as-norm-source]` или фикстура подтверждается и маркер снимается.
4. **мои, ждут с 00:41 2026-09-13:** рассылать ли часовые напоминания (комплект лежит в
   `%TEMP%/controller-reminder/reminders/`); дописывать ли в команду правило «адрес карточки
   берётся `python scripts/plan-card-status.py --verbose`, а не грепом по заголовку»; чинить ли
   дефект приписки дерева в `controller-verdict.py`; дописывать ли правило про метку времени.
5. **51 коммит окна 274** готов к пушу у интегратора.

**Дополнение 16:42 мест.** Слово владельца: разбор причины стоянки снят с роли
целиком (коммит `0a42afd40`, применён интегратором) — контролёр теперь толкает
любую встающую сессию без анализа блокера, кроме двух исключений, объявленных
словом самой сессии (заморозка с условием, пустая очередь). Толкнул Карину и
интегратора несколько раз подряд без разбора — оба приняли, оба продолжили работу.

**Время и коммит:** 16:42 мест. (13:42Z), `6be499791`.
