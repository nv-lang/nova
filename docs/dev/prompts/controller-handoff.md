<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Снимок окна-контролёра — 2026-09-08, 16:33Z (19:33 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в команде
> [`.claude/commands/controller.md`](../../../.claude/commands/controller.md) (заведена
> 2026-09-08 по слову владельца: «сделай команду для запуска со всеми правилами, чтобы ничего
> не потерялось после перезапуска»). Задание `CronCreate` (в этой сессии — `b0b843e5`) её
> ВЫЗЫВАЕТ, а не повторяет (в этой сессии — `6e897643`; перечисление шагов из его тела убрано
> 17:10Z как вторая копия — строка реестра №1042).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> ЗАДАНИЕ, а не сюда.

## Опознание сессии

- id `ef28f97e-e5fa-482b-8c44-c8e039d7d396`, имя для соседей на 12:45Z 2026-09-09 — **`nova-b8`**
  (было `nova-c6`; сменилось БЕЗ рестарта сессии, файл истории тот же — брать имя из `ListAgents`,
  первая строка выдачи, а не отсюда).
- Модель **Opus** (переключено владельцем 14:26Z). Механика вынесена в скрипты; дорогая модель
  тратится на суждение «встал или ждёт законно».
- **После рестарта:** сказать `/controller` — команда несёт всё, включая стыковку. Затем
  завести расписание (`CronCreate`, шаг 7 минут, тело — вызов `/controller`): оно живёт только
  в сессии и гаснет через 7 дней, проверять `CronList`.
- **Стыковка обязательна первым шагом:** имена соседей (`nova-XX`) и id их сессий после
  рестарта ДРУГИЕ. Инструмент стыковки опознаёт их по свидетельствам — дерево по частоте
  упоминаний в истории, имя из их собственного `ListAgents` (последнее вхождение, по всему
  файлу), интегратор по упоминаниям гейта и пуша. **Путь к нему — только в пункте ниже**: здесь
  жила вторая копия (`/d/Sources/nv-lang/controller-dock.py`, редакция до переезда 17:27Z), и
  это ровно та ошибка, которая в самой команде стоила двух вызовов в «No such file».
- **Инструменты — в репозитории, `scripts/tools/`** (перенесены 17:27Z по слову владельца;
  раньше лежали рядом с деревом и потому не версионировались). Запускать из корня главного
  дерева: `python scripts/tools/controller-dock.py 2` (стыковка),
  `controller-peers-scan.py [N]`, `controller-peers-deep.py <id-prefix> [K] [tail_mb]`,
  `controller-machine-watch.py` (дежурство за очередью машины). У каждого — строка в
  `scripts/tools/README.md`, как требует правило того каталога.
- Файл держать НЕОТСЛЕЖИВАЕМЫМ: интегратор возьмёт его по имени в свой пакет после пуша
  (его слово, 15:04Z: «прочитал целиком, беру в пакет, дока принимает»).

## Соседи — снимок 09:37Z (12:37 мест.) 2026-09-09

**ИМЕНА МЕНЯЮТСЯ БЕЗ ПЕРЕЗАПУСКА СЕССИЙ** (замер 00:45Z): файлы историй те же, размеры прежние,
последнее имя в них СТАРОЕ. Я был `nova-c6`, стал `nova-b8`. Опознавать по ДЕРЕВУ, имя проверять
вторым признаком. Пятое окно `nova-57` (главная копия, без плана) исчезло из `ListAgents` между
01:02Z и 01:08Z.

| имя | дерево / роль | id истории | состояние на 04:59Z |
|---|---|---|---|
| `nova-be` | **интегратор**, главная копия, план 221 + реестр 221.1 | `3b203470…` | ЗАПУШЕНО 12:14 (`RC=0`), зеркала на `4623ae8af`; ярус снят, заморозка снята |
| `nova-9c` | `claude-limits` + пакет `nova-duckdb`, план 01 | `e601e366…` | нашли пропуск пункта 1 чек-листа модулей: в `nova-duckdb` НОЛЬ эффектов, мокабельности нет; план 01.4 §4.2 дописан, тег держат |
| `nova-7f` | `../nova-p274`, план 274.8 | `bf14b5f5…` | M3a в работе (первый прогон 79 из 150, ICE `no type recorded`), конструкции M3b и M3c записаны до кода, порядок M3b↔M3c свапнут |
| `nova-09` | `../nova-p283`, план 283 | `5383c33a…` | «стоп» владельца 06:00Z: план ЗАКРЫТ, ветка влита, ждут слова. Передача: `docs/dev/prompts/window-283-handoff.md` |

**ЗАПУШЕНО 12:14 (09:14Z), ЗАМОРОЗКА СНЯТА — четвёртый пуш смены.** `/tmp/gate_full.done` несёт
`RC=0 at 12:14`, `origin/main` и локальный `main` совпали на `4623ae8af` (проверено мной 12:37
командой `git ls-remote`), и головной коммит — моя правка приписки держателя слота. Прежние пуши
смены: `8123e10563e4` в 07:53 (6 коммитов, `dev-override` активен — прогон НЕ доказывает сборку
на чистом checkout'е), `59ad7c70f` в 09:29, `7fbf5cd9f46f` в 06:14.

**ЛЕЖИТ ДЛЯ ВЛАДЕЛЬЦА (передано 04:51):** окно `claude-limits` не может завести репозитории
`nova-duckdb` на gitverse и sourcecraft — `gh` есть только для GitHub. Проверил сам:
`scripts/tools/integrator-push.sh` знает все три зеркала (строки 46 и 57), но это ПУШ в
существующие репы; скрипта их СОЗДАНИЯ в дереве нет. Ремоуты прописаны, после создания пуш
пойдёт одной командой; пока пакет на одном зеркале из трёх.
## Журнал (последние двое суток)

- **11:05–11:53Z (14:05–14:53 мест.), ПРАВИЛО СТАЛО ПРОГРАММОЙ по слову владельца** («я тебе не
  верю, наши скрипт на питоне согласно всех правил и им проверяй и толкай»). Причина: за смену я
  ТРИЖДЫ поставил работающий статус стоящему окну — по возрасту записи, по служебному типу
  записи и по СОДЕРЖАНИЮ доклада, — и каждый раз правило в команде уже стояло. Заведён
  `scripts/tools/controller-verdict.py`: закрытый список признаков (процесс своего дерева держит
  слот, `tool_use` в последней записи, коммит или незакоммиченный файл новее последнего текста),
  печатает признак, на котором стоит статус, и строку `PUSH:`. Проба в обе стороны 8/8, свой
  предел называет сам: отвечает «есть ли признак работы», а НЕ «законна ли стоянка».
- **И этот инструмент нашёл в себе ТРИ дефекта за четыре цикла, все — мои же классы.** (1) Секундный
  `nova build` объявлялся ярусом → разделены `gate.sh`/`gate-novac` и прочие держатели. (2) Признаком
  работы интегратора стал МОЙ файл `scripts/tools/README.md`, изменённый двумя минутами раньше —
  запрет 11 через файловую систему; закрыто списком `MINE_PATTERNS`, и после починки ответ стал
  обратным (`STOOD, silent 6.5 min`). (3) «Дерево по частоте» отдало интегратору ЧУЖОЕ дерево:
  окно 274 упоминает `nova-p274` **290** раз, `claude-limits` — 183, а интегратор — **4** раза,
  потому что это я пишу ему про их ярус; закрыто порогом `MIN_TREE_HITS = 20`.
- **11:43Z, четвёртый дефект нашло РАСХОЖДЕНИЕ ДВУХ МОИХ ИЗМЕРИТЕЛЕЙ.** Дежурство назвало
  держателя `nova` (подтверждено `readlink /proc/<pid>/cwd`), вердикт в ту же минуту — `unknown`.
  Починено переносом `cwd_owner()` на уровень модуля: ответ на «чьё это дерево» теперь в ОДНОЙ
  функции, которую зовут оба. Правило в команде: расхождение двух моих инструментов по одному
  предмету — дефект, а не шум.
- **11:44Z, что сработало:** пять моих файлов интегратор взял ДО старта своего яруса (правки
  14:37:20 и 14:37:37 против старта 14:39:55, дерево чистое) — вердикт того прогона судил мою
  редакцию целиком. Сам прогон закрылся красным: `RC=1 at 14:46`, он его разбирает.

- **08:23Z (11:23 мест.), ДЕФЕКТ В МОЁМ ДЕЖУРСТВЕ: держателем слота названо МОЁ ОКНО.** Инструмент
  выдал `machine BUSY by nova-b8 x2`, а ярус я не запускал: он искал метку дерева ПО ВСЕЙ строке
  процесса и нашёл моё имя внутри текста сообщения коммита интегратора («Donor: window nova-b8.
  Committed by name to still the tree for the tier»). Класс тот же, что вся смена — инструмент
  ответил на вопрос «какая строка содержит имя», объявив это ответом на «кто держит слот», —
  и это десятый мой носитель за сутки, второй в дежурстве. Починено 08:47Z: имя берётся только
  между разделителями пути, текст после `-c`/`-m`/`-F` считается данными; проба в обе стороны
  **5/5** (на прозе старая регулярка даёт `nova-b8`, новая `?`). Запушено в 12:14, `4623ae8af`.
- **08:47Z, ЗАМЕР ДО ПРАВКИ ОТВЕТИЛ НЕ НА ТОТ ВОПРОС — и правило было исполнено буквально.**
  Дежурство в 11:45:42 дало `FREE`, ярус поднялся в 11:45:55 (через 13 секунд), файл записался в
  11:46:53. Доложил интегратору сам, с обоими временами. Механика в команде: дежурство ДВАЖДЫ —
  до правки и сразу после записи, решает второй замер.
- **08:52Z, у починки нашлась вторая сторона.** Сужение приписки закрыло ложное обвинение и молча
  открыло обратное: ярус, запущенный относительным путём, стал `BUSY by ?` — «занято неизвестно
  кем» из моего же запрета 16. Фолбэк: `cwd` процесса через msys `readlink /proc/<pid>/cwd`
  (`os.readlink` бесполезен — Windows-питон `/proc` не видит вовсе, `WinError 3`); проба 2/2,
  предел назван в коде (только msys-pid; у нативного `nova.exe` путь берётся из аргументов).
- **09:16Z, толчок 274 по адресу «конструкция M3b до кода» дал две ложные границы.** Они
  собирались записать по памяти «проекций индекса в подмножестве нет» — на деле `NodeKind.Index`
  разбирают три места чекера; и нашли, что `@emit_store` печатает цель ДВАЖДЫ, то есть
  `xs[i] += 1` печатает индексное чтение дважды (что выходит — ice или двойное вычисление —
  требует запуска, проба у них определена). Их вывод: словарь `Place` снимает класс по построению.
- **09:25Z, МОЯ ОШИБКА: два грепа по номеру вместо чтения абзаца.** Я передал 274, что реестр
  221.1 «не ограничен оракулом», сославшись на строки 590 и 599 их зонтичного плана. Прочитав
  абзацы целиком (`sed -n '586,600p'`), убедился: первая — про запись корзины расхождений, вторая
  — про ЧТЕНИЕ реестра; обе об оракуле. Вывод снял письмом. Тот же класс, за которым слежу
  у других, только инструментом был греп.
- **Восьмой раз наступил на кириллицу в командной строке bash** (09:39Z, `grep -n` с русским
  образцом). Хук срезал команду целиком; поиск по русскому тексту — только инструментом `Grep`.

- **08:09Z (11:09 мест.), утро: смена перешла из «ночь чинит гейт» в «три окна ведут свои волны».**
  Толчков за утро **семь**, все сдвинули работу. Пуш смены второй — `59ad7c70f` в 09:29. Ярус
  интегратора под D471 закрылся КРАСНЫМ (`RC=1 at 11:03`), непушенных 6, вливания `cxx` ещё нет.
- **D471 (`cxx = true`) — работа интегратора по слову владельца 09:26.** Готовы разбор ключа,
  поле в `ResolvedFfiConfig`, драйвер `clang++`/`g++`, громкий отказ при отсутствии драйвера,
  D-блок в `07-modules.md`. Номер сперва был назван D466 по памяти и оказался занят (15 вхождений,
  проверено мной) — верный **D471**. `claude-limits` даёт живое доказательство прогоном СВОЕГО
  пакета на его ветке (Windows, Linux), фикстуру в `nova` не пишет: приёмка слияния его.
- **МОЯ ОШИБКА УТРА, найденная окном `claude-limits`:** свежесть работы читал по mtime и дважды
  толкнул по ЗАНЯТОМУ адресу. Правило в команде: сперва `git log`, потом `status`, и лишь затем
  mtime — «иначе всякая закрытая работа выглядит брошенной ровно до следующего касания файла».
  **Две ошибки интегратора того же рода, названные им самим:** номер D-блока и штампы времени
  брались по памяти — «называл величину по памяти там, где её надо снимать командой».
- **Окно 274 закрыло M2c обеими половинами критерия** и написало критерий M3 ДО кода. Их строка:
  дифференциал дал `EXIT=1` из-за ратчета цены, и вместо ослабления гейта они сняли другой
  действительный замер — «проверка, которая мешает именно тогда, когда ты уверен в своей
  правоте, — единственная, которая вообще чего-то стоит».
- **Требование владельца 06:30Z «экономь лимиты» положено в команду разделом:** глубокое чтение
  историй только под решение о толчке, доклад по пустому циклу — таблица и одна строка. Цикл
  сжался с шести вызовов до трёх.

- **02:00–03:09Z, час под ЗАМОРОЗКОЙ: шесть ярусов, ноль пушей.** Объявлена в 05:05: четвёртый
  заход упал от трёх коммитов, легших ПОД сорокаминутным прогоном. Его слова: «гоню проверку
  сорок минут по дереву, которое за эти сорок минут меняется — гонка, которую повторными
  заходами не выиграть», и сразу же «ни одно окно ничего не нарушило, плохой здесь порядок, а не
  люди». Толчков за час три, все сработали: заморозка про главное дерево, а окна в своих.
- **Моя слепая полоса 109 секунд** — `freeze-now` брала старт только по `gate.sh`, а слот держат
  ещё `cargo`, `clang`, `nova`; в 02:23Z проявилось вживую. Проба 5/5 — и нашла дефект в самой
  себе: мерила не тот набор, который проверяет.
- **Три поправки к моим выводам за час, все приняты:** «влито нацело» без указания, ЧЕЙ `main`
  (у 283 было 0 против локального и 9 против `origin/main`); в споре стража и сводки я выбрал
  сторону вместо третьего замера, а соврал СТРАЖ. Отсюда его правило: **когда два инструмента
  расходятся, неверным может быть ЛЮБОЙ, включая тот, что выглядит первоисточником.**
- **Класс ночи — восемь носителей, и половина в слое инструментов, а не в коде:** мёртвый
  снимок `ps`, часы вместо моментов, слепая полоса, `os error 32` у гейта, кэш-ключ без флагов,
  сводка против стража, выдуманные ссылки охотника, тест «отказ принят», зеленевший на разборе
  SQL. Форма одна: **инструмент отвечает не на тот вопрос, который ему задан, и молчит об этом.**
- **01:00–01:11Z (04:00–04:11 мест.), первый час ночи, свёрнуто.** Очередь за машиной развернулась дважды за три минуты (ярус снят зря); в расписании нашлась ВТОРАЯ КОПИЯ правил — старое задание с полным текстом внутри, удалено, `CronList` внесён в цикл; в дежурстве найден свой дефект (снимок `ps` умирал на кодировке, а вердикт подписывался как построенный на двух); первый ярус закрылся отказом `check-accepted-branch-merged`.
- **01:16–01:51Z, свёрнуто — четыре урока, каждый прожил ночь.** Третий ярус упал НЕ на коде
  (промежуточный `.c` рядом с исходником, два шага на одном пути, `os error 32`; починено и
  доказано прогоном 07:51). `freeze-now` получила МЕТКУ ПРЕДМЕТА — мы с интегратором два цикла
  отвечали «успел ли страж прочитать правку» вместо «а этот страж про этот файл?»; она же
  поймала МЕНЯ на живом случае: механизм не удерживает от нарушения, он делает его видимым в тот
  же цикл. Находка `claude-limits` («`nova check` молча принимает `throw` без `Fail`») роздана
  как работа: `novac` этого НЕ повторяет (`E7301`/D405), значит чинить оракул.
- **Поправка владельца 01:36Z:** «274 молчит с 04:28, а ты говоришь работает». Дыра была в
  мериле: я брал ВОЗРАСТ записи, не глядя на ТИП. `assistant:text` есть конец хода, то есть
  остановка в момент рождения. Правило в команде переписано.
- **Правило того же часа:** обстановка живёт минуты — слово соседа о занятости машины
  ПЕРЕСВЕРЯТЬ командой перед передачей, иначе разносишь протухший факт как свежий.
- **03:37–04:30Z, седьмой ярус и вторая заморозка:** упал на ветке 274 — третий раз за ночь по
  одной схеме, за сорок минут прогона туда легли два коммита. **Мой восьмой носитель класса, про
  метод:** я написал «правки `.c` в дереве нет», а она была закоммичена в 06:46 — смотрел
  `git status`, откуда закоммиченное уходит по определению. «Нет в дереве» доказывается ЛОГОМ.
- **04:45–04:51Z, десятый и одиннадцатый толчки, свёрнуто.** Оба взяли адрес из плана самого
  окна и оба закрылись; уроки, легшие в мою роль: «узнать, жива ли находка, можно только
  запустив стража на подсунутом входе» (их триаж по словам ошибся в трёх случаях из четырёх) и
  «выдуманный вход даёт настоящий отказ». Правила того же часа в команде: дом пути к моим
  инструментам стал ОДИН (жили две редакции, старая раньше — два вызова в «No such file»), и
  полный `find` по дереву окна в таймаут не лезет — спрашивать точечно у названного каталога.
- **Обобщение окна 274, принятое интегратором как сильнейшее за ночь:** «у меня протух не
  каталог, а СНИМОК, но читается одинаково — шаг отработал, число получено, а относилось оно
  не к тому дереву». Форма класса: **артефакт пережил своё дерево, а измеритель этого не
  заметил.** Строку реестра он заведёт от их формулировки.
## Находки, лежащие для интегратора (после его пуша)

- **15 брошенных worktree**, отставание до 2954 коммитов; настоящий кандидат на разбор один —
  `nova-p259` с 39 файлами, у прочих грязь это мусор рабочего каталога (`?? TASK.md`,
  `?? scratch_*/`). База деревьев в порядке: 20 при базе 20. Территория интегратора.
- **`/tmp/gate_full.done` протухает молча** — читать по СОДЕРЖИМОМУ вместе со временем файла;
  за смену это дважды давало ложный вывод (мой и его).

## Открытые вопросы к владельцу

- **`claude-limits` (передано 12:09):** форма доступа к тексту ошибки в `nova-duckdb` (склоняются
  ко варианту с приватным `message_of` и `@last_error()` на каждый тип-ручку, C не трогая) и —
  **можно ли им брать саму правку пакета**: эффект-семейство лежит формально за границей плана 01.
  Тег `nova-duckdb` они держат, пока пункт 1 чек-листа модулей не выполнен.
- **283 (`nova-09`):** стоят с 06:00Z по вашему «стоп»; план закрыт, ветка влита, расширение
  границы — ваше слово.
