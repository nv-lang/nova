<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Снимок окна-контролёра — 2026-09-08, 16:33Z (19:33 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в команде
> [`.claude/commands/controller.md`](../../../.claude/commands/controller.md) (заведена
> 2026-09-08 по слову владельца: «сделай команду для запуска со всеми правилами, чтобы ничего
> не потерялось после перезапуска»). Задание `CronCreate` (в этой сессии — `b0b843e5`) её
> ВЫЗЫВАЕТ, а не повторяет (в этой сессии — `6e897643`; перечисление шагов из его тела убрано
> 17:10Z как вторая копия — строка реестра №1042).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> ЗАДАНИЕ, а не сюда.

## Опознание сессии

- id `ef28f97e-e5fa-482b-8c44-c8e039d7d396`, имя для соседей `nova-c6` (живёт до рестарта;
  новое — из `ListAgents`, первая строка выдачи).
- Модель **Opus** (переключено владельцем 14:26Z). Механика вынесена в скрипты; дорогая модель
  тратится на суждение «встал или ждёт законно».
- **После рестарта:** сказать `/controller` — команда несёт всё, включая стыковку. Затем
  завести расписание (`CronCreate`, шаг 7 минут, тело — вызов `/controller`): оно живёт только
  в сессии и гаснет через 7 дней, проверять `CronList`.
- **Стыковка обязательна первым шагом:** имена соседей (`nova-XX`) и id их сессий после
  рестарта ДРУГИЕ. `python /d/Sources/nv-lang/controller-dock.py 2` опознаёт их по
  свидетельствам — дерево по частоте упоминаний в истории, имя из их собственного `ListAgents`
  (последнее вхождение, по всему файлу), интегратор по упоминаниям гейта и пуша.
- **Инструменты — в репозитории, `scripts/tools/`** (перенесены 17:27Z по слову владельца;
  раньше лежали рядом с деревом и потому не версионировались). Запускать из корня главного
  дерева: `python scripts/tools/controller-dock.py 2` (стыковка),
  `controller-peers-scan.py [N]`, `controller-peers-deep.py <id-prefix> [K] [tail_mb]`,
  `controller-machine-watch.py` (дежурство за очередью машины). У каждого — строка в
  `scripts/tools/README.md`, как требует правило того каталога.
- Файл держать НЕОТСЛЕЖИВАЕМЫМ: интегратор возьмёт его по имени в свой пакет после пуша
  (его слово, 15:04Z: «прочитал целиком, беру в пакет, дока принимает»).

## Соседи — снимок 01:52Z (04:52 мест.) 2026-09-09

**ИМЕНА МЕНЯЮТСЯ БЕЗ ПЕРЕЗАПУСКА СЕССИЙ** (замер 00:45Z): файлы историй те же, размеры прежние,
последнее имя в них СТАРОЕ. Я был `nova-c6`, стал `nova-b8`. Опознавать по ДЕРЕВУ, имя проверять
вторым признаком. Пятое окно `nova-57` (главная копия, без плана) исчезло из `ListAgents` между
01:02Z и 01:08Z.

| имя | дерево / роль | id истории | состояние на 01:52Z |
|---|---|---|---|
| `nova-be` | **интегратор**, главная копия, план 221 + реестр 221.1 | `3b203470…` | ЧЕТВЁРТЫЙ заход яруса; три предыдущих: снят зря, невлитая ветка 274 (слил, 8 коммитов), os error 32. Непушенных 23 |
| `nova-9c` | `claude-limits` + пакет `nova-duckdb`, план 01 | `e601e366…` | пакет ОПУБЛИКОВАН на github (19 коммитов, 24 файла, тесты 21/21), CI идёт с 01:48Z; тег только по зелёному CI |
| `nova-7f` | `../nova-p274`, план 274.8 | `bf14b5f5…` | 10 коммитов; закрыл 41 адрес и вопрос про `syntax`; ждёт слот на прогон 3б |
| `nova-09` | `../nova-p283`, план 283 | `5383c33a…` | план ЗАКРЫТ, очередь пуста с 00:47Z; расширение границы — слово владельца. Передача: `docs/dev/prompts/window-283-handoff.md` |

**ЛЕЖИТ ДЛЯ ВЛАДЕЛЬЦА (передано 04:51):** окно `claude-limits` не может завести репозитории
`nova-duckdb` на gitverse и sourcecraft — `gh` есть только для GitHub. Проверил сам:
`scripts/tools/integrator-push.sh` знает все три зеркала (строки 46 и 57), но это ПУШ в
существующие репы; скрипта их СОЗДАНИЯ в дереве нет. Ремоуты прописаны, после создания пуш
пойдёт одной командой; пока пакет на одном зеркале из трёх.
## Журнал (последние двое суток)

- **01:00Z (04:00 мест.), цикл с двумя разворотами очереди за три минуты.** Интегратор уступил
  машину `claude-limits` и снял свой ярус (его довод сильнее вежливости: вердикт под чужой
  компиляцией непригоден — сегодня один страж дал 702 с против 313 с), а пересборка кончилась
  в 00:55Z, ДО того как слово дошло. Итог: машина свободна (проверено командой 00:58:35Z,
  BUSY 0), ярус снят зря. Передал обоим. Окно 274 стоит законно (шаг 3б готов, нужен слот),
  283 — законно (план закрыт, границу двигает владелец). Толчков ноль, писем три.
- **01:51Z (04:51 мест.), четвёртый ярус; третий упал НЕ на коде.** `RC=1 at 04:47`, отказ один:
  `conformance-full` дал две `CODEGEN-FAIL` с «файл занят другим процессом» (os error 32) на
  `a_q3_println_debug_record.c`. Диагноз интегратора: компилятор пишет промежуточный `.c` РЯДОМ
  С ИСХОДНИКОМ, и два шага яруса столкнулись на одном пути; мега-CU зелёный 925/0, обе фикстуры
  поодиночке `PASS: 2 FAIL: 0`. То есть отказ — артефакт собственного инструмента, тот же класс,
  что мой мёртвый снимок `ps` и кэш-ключ без флагов у `claude-limits`. Починку (писать `.c` во
  временный каталог) он отложил до дня: она требует пересборки и четвёртого прогона поверх.
- **01:42Z (04:42 мест.), `freeze-now` получила МЕТКУ ПРЕДМЕТА** — заказ интегратора после того,
  как мы оба два цикла отвечали не на тот вопрос («успел ли страж прочитать правку» вместо «а
  этот страж про этот файл?»). `WARN` стал двух сортов: `[no known subject]` — в общем порядке,
  `[script-eol/mixed-eol]`/`[context-layer]` — немедленно. Первый прогон нашёл у меня второй
  сорт и я доложил сам; его второе число — стражи EOL шли на 115-й секунде, правка легла на
  1147-й, прочитана старая редакция. Проба 4/4, включая нечитаемый список → `UNREADABLE`,
  а не `no known subject`.
- **01:30Z (04:30 мест.), `freeze-now` сработала на живом случае и поймала МЕНЯ** (правка на
  257-й секунде яруса). Механизм не удержал от нарушения — он и не должен; он сделал его
  видимым в тот же цикл, без чужого участия и без моей памяти.
- **01:39Z (04:39 мест.), находка `claude-limits` роздана как работа.** `nova check` молча
  принимает `throw` без `Fail` и голый `int` под `Result` (`PASS: 1 FAIL: 0`). Вопрос «а
  `novac` так же?» отдал окну 274 — его канал, слота не требует. Ответ 04:42: **`novac` НЕ
  повторяет**, отказывает по делу с `E7301`/D405, значит чинить оракул и правило не изобретать.
  Их честность по второй половине: `throw` вне подмножества `novac`, потому «не проверено», а
  не «ловит».
- **01:36Z, поправка владельца:** «274 молчит с 04:28, а ты говоришь работает». Дыра была в
  мериле: я брал ВОЗРАСТ записи, не глядя на ТИП. `assistant:text` есть конец хода, то есть
  остановка в момент рождения. Правило в команде переписано.
- **01:23Z, толчок окну 274 адресом из его же плана** (41 адрес на функции) — закрыт двумя
  коммитами; **01:16Z, толчок `claude-limits`** — первый случай, где остановку показала
  механика: самый свежий файл дерева оказался СТАРШЕ его собственного доклада.
- **01:11Z (04:11 мест.), ярус кончился отказом; отложенное перенесено.** `/tmp/gate_full.done`
  несёт `RC=1 at 04:09`, отказ РОВНО ОДИН и не про код: `check-accepted-branch-merged: FAIL` —
  «работа принята в реестре, но её ветка не влита» (строка 239–240 лога, `head=3fb7fea3a`).
  Это тот самый страж класса №1029. Машина свободна с 04:10, очередь: интегратор → 274.
- **01:09Z (04:09 мест.), толчок 00:59Z сработал — мера в коммитах.** Окно 274 после письма
  «очередь пуста только когда пуст ВЕСЬ план, а не его незаблокированная часть» взяло два
  пункта, не требующих машины, и закрыло их ЧТЕНИЕМ: вопрос про `syntax` снят (`109f593d5`),
  долг по адресам пересчитан 18 → 29 (`165404daf`), причём половина находки в самом
  измерителе — он печатал «нет такого файла», потому что искал не в том каталоге (их шестой
  за смену случай того же класса). Проверено деревом в 01:08Z: 8 коммитов, чисто.
- **01:06Z (04:06 мест.), цикл пустой; первое применение правила о заморозке.** Строка журнала
  легла не в дерево, а в отложенную очередь — дежурство перед правкой показало ярус.
- **01:05Z (04:05 мест.), нашлась вторая копия правил — в расписании.** `CronList` дал ДВА
  задания на одном шаге: живое `6e897643` (вызывает команду) и старое `71cd3f57` с ПОЛНЫМ
  текстом правил внутри и мёртвым именем `nova-c6`. Оно подавало устаревшую копию в каждый
  второй цикл и удваивало частоту; нашлось случайно — цикл пришёл через минуту. Удалено,
  осталось одно. `CronList` внесён в цикл как обязательный шаг, самотест получил проверку
  `cronlist-cycle` (17/17, краснеет на пробе).
- **01:03Z (04:03 мест.), цикл пустой.** Ярус интегратора идёт (BUSY 3, оба снимка `ps` целы —
  починка кодировки держит), окна 274 и `claude-limits` работают, 283 стоит законно. Толчков
  ноль, писем ноль. Эта строка тоже легла под идущим ярусом — файл уже в списке доложенного
  интегратору, новой сущности не добавляет, но правило одно и для журнала.
- **01:02Z (04:02 мест.), дефект в СВОЁМ инструменте.** Дежурство выдало вердикт, потеряв один
  из двух снимков `ps` на `UnicodeDecodeError`: напечатало `ps failed` и всё равно подписалось
  «union of two samples». Четвёртый носитель класса, названного интегратором, — теперь у меня.
  Починено: декодирую байты сам, подпись называет число ВЫЖИВШИХ снимков, одиночный помечен
  `UNPROVEN`, оба мёртвых → `VERDICT: UNKNOWN`. Проба в обе стороны 2/2. Правка легла через
  минуту после старта его яруса — доложил ему сам, откат по его слову.
- **Правило, купленное этим циклом:** обстановка живёт минуты — слово соседа о занятости машины
  ПЕРЕСВЕРЯТЬ командой перед передачей, иначе разносишь протухший факт как свежий.
## Находки, лежащие для интегратора (после его пуша)

- **15 брошенных worktree**, отставание до 2954 коммитов; `nova-pcdoors` впереди `main` на
  коммит и запушена. Территория интегратора, не контролёра.
  **Поправка 15:43Z, моя же:** формулировка «у 12 незакоммиченные ПРАВКИ» была сильнее факта.
  Проверил содержимое: в девяти из них грязный файл ровно один и это МУСОР рабочего каталога
  (`?? TASK.md`, `?? scratch_p493/`, `?? scratch_pdiag/`), а не код. Настоящий кандидат один —
  `nova-p259` с 39 файлами. И база деревьев в порядке: `worktree-count.baseline` = 20, фактически
  20. То есть «двенадцать грязных» звучало как потерянная работа, а под этим черновики.
- **`/tmp/gate_full.done` протухает молча:** несёт `RC=1 SEC=524 HASH=25a4a495c`, тогда как
  единственная причина того отказа снята слиянием (проверено прогоном стража 14:38Z).

## Открытые вопросы к владельцу

- (пусто)
