<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Снимок окна-контролёра — 2026-09-08, 16:33Z (19:33 мест.)

> **Это НЕ свод правил.** Правила, запреты и формы живут в команде
> [`.claude/commands/controller.md`](../../../.claude/commands/controller.md) (заведена
> 2026-09-08 по слову владельца: «сделай команду для запуска со всеми правилами, чтобы ничего
> не потерялось после перезапуска»). Задание `CronCreate` (в этой сессии — `b0b843e5`) её
> ВЫЗЫВАЕТ, а не повторяет (в этой сессии — `6e897643`; перечисление шагов из его тела убрано
> 17:10Z как вторая копия — строка реестра №1042).
> Здесь только СОСТОЯНИЕ: кто где стоит, чем занят интегратор, что уже сделано. Предел ~150
> строк, хроника — двое суток, старше вырезать. Правило, найденное циклом, дописывается В
> ЗАДАНИЕ, а не сюда.

## Опознание сессии

- id `ef28f97e-e5fa-482b-8c44-c8e039d7d396`, имя для соседей `nova-c6` (живёт до рестарта;
  новое — из `ListAgents`, первая строка выдачи).
- Модель **Opus** (переключено владельцем 14:26Z). Механика вынесена в скрипты; дорогая модель
  тратится на суждение «встал или ждёт законно».
- **После рестарта:** сказать `/controller` — команда несёт всё, включая стыковку. Затем
  завести расписание (`CronCreate`, шаг 7 минут, тело — вызов `/controller`): оно живёт только
  в сессии и гаснет через 7 дней, проверять `CronList`.
- **Стыковка обязательна первым шагом:** имена соседей (`nova-XX`) и id их сессий после
  рестарта ДРУГИЕ. `python /d/Sources/nv-lang/controller-dock.py 2` опознаёт их по
  свидетельствам — дерево по частоте упоминаний в истории, имя из их собственного `ListAgents`
  (последнее вхождение, по всему файлу), интегратор по упоминаниям гейта и пуша.
- **Инструменты — в репозитории, `scripts/tools/`** (перенесены 17:27Z по слову владельца;
  раньше лежали рядом с деревом и потому не версионировались). Запускать из корня главного
  дерева: `python scripts/tools/controller-dock.py 2` (стыковка),
  `controller-peers-scan.py [N]`, `controller-peers-deep.py <id-prefix> [K] [tail_mb]`,
  `controller-machine-watch.py` (дежурство за очередью машины). У каждого — строка в
  `scripts/tools/README.md`, как требует правило того каталога.
- Файл держать НЕОТСЛЕЖИВАЕМЫМ: интегратор возьмёт его по имени в свой пакет после пуша
  (его слово, 15:04Z: «прочитал целиком, беру в пакет, дока принимает»).

## Соседи — снимок 04:59Z (07:59 мест.) 2026-09-09

**ИМЕНА МЕНЯЮТСЯ БЕЗ ПЕРЕЗАПУСКА СЕССИЙ** (замер 00:45Z): файлы историй те же, размеры прежние,
последнее имя в них СТАРОЕ. Я был `nova-c6`, стал `nova-b8`. Опознавать по ДЕРЕВУ, имя проверять
вторым признаком. Пятое окно `nova-57` (главная копия, без плана) исчезло из `ListAgents` между
01:02Z и 01:08Z.

| имя | дерево / роль | id истории | состояние на 04:59Z |
|---|---|---|---|
| `nova-be` | **интегратор**, главная копия, план 221 + реестр 221.1 | `3b203470…` | ЗАПУШЕНО 07:53, пишет отчёт владельцу; очередь пуста |
| `nova-9c` | `claude-limits` + пакет `nova-duckdb`, план 01 | `e601e366…` | три охоты из четырёх закрыты (34 находки, стражи 13/13, 32 коммита); четвёртая 13 из 15, толкнут 04:51Z |
| `nova-7f` | `../nova-p274`, план 274.8 | `bf14b5f5…` | шаг 3б: 91 из 94 эмитируемых, 12 расхождений; переезд инструмента приёмки сделан, коммитить уже можно |
| `nova-09` | `../nova-p283`, план 283 | `5383c33a…` | план ЗАКРЫТ, очередь пуста с 00:47Z; расширение границы — слово владельца. Передача: `docs/dev/prompts/window-283-handoff.md` |

**ЗАПУШЕНО 07:53 (04:53Z), ЗАМОРОЗКА СНЯТА, вторая за смену.** Три зеркала на `8123e10563e4`,
**6 коммитов**, `GATE OK (final)`, `RC=0`, отказов ноль, восьмой заход. Вердикт несёт свою
оговорку: `dev-override` активен (`examples/nova.override.toml` подменяет tls, http, polaris,
compress) — прогон НЕ доказывает сборку на чистом checkout'е. **Итог ночи интегратора: восемь
заходов, два пуша, 39 коммитов**; лишним был ровно один заход, первый. Прежний пуш смены —
`7fbf5cd9f46f` в 06:14, 33 коммита.

**Правка `.c` доказана ПРОГОНОМ, а не компиляцией:** ярус прошёл мега-CU и `conformance-full`
целиком (оба шага пишут промежуточный `.c` по всему корпусу), после них рядом с исходниками
**ноль** файлов `.c` — раньше оставалось по четыре, и на них умер третий заход ночи. Жалоба
владельца от 7 сентября закрыта проверяемо.

**ЛЕЖИТ ДЛЯ ВЛАДЕЛЬЦА (передано 04:51):** окно `claude-limits` не может завести репозитории
`nova-duckdb` на gitverse и sourcecraft — `gh` есть только для GitHub. Проверил сам:
`scripts/tools/integrator-push.sh` знает все три зеркала (строки 46 и 57), но это ПУШ в
существующие репы; скрипта их СОЗДАНИЯ в дереве нет. Ремоуты прописаны, после создания пуш
пойдёт одной командой; пока пакет на одном зеркале из трёх.
## Журнал (последние двое суток)

- **02:00–03:09Z (05:00–06:09 мест.), час под ЗАМОРОЗКОЙ: шесть ярусов, ноль пушей.** Интегратор
  объявил её в 05:05 первой просьбой за ночь — до его «запушено» не коммитить в ветки и не
  править главное дерево. Причина цифрами: четвёртый заход упал потому, что окно 274 дописало
  три коммита ПОД сорокаминутным прогоном. Его слова: «гоню проверку сорок минут по дереву,
  которое за эти сорок минут меняется — гонка, которую повторными заходами не выиграть», и
  сразу же «ни одно окно ничего не нарушило, плохой здесь порядок, а не люди». Передал всем
  троим. Пятый упал на `crate-tests`, шестой идёт с 05:43. Числа пересчитаны 05:51: непушенных
  **33**, зеркала на `84568506c` (вчерашний), у ветки 274 впереди 11, у 283 — 9.
- **Толчков за час — три, все сработали.** `claude-limits` дважды (05:38 и 06:02): первый дал
  заход с четырьмя находками и сносом бесполезной проверки, второй — по перечню, который окно
  назвало само. Довод обоих писем: заморозка про главное дерево, а они в своём каталоге, и
  вопрос владельцу про тег блокирует ТЕГ, а не четыре других пункта.
- **Моя слепая полоса 109 секунд, найденная сопоставлением с чужим замером.** `freeze-now`
  брала старт только по `gate.sh`, а слот держат ещё `cargo`, `clang`, `nova`; в 02:23Z
  проявилось ВЖИВУЮ. Правка положена 03:19Z, проба 5/5, самотест 18/18 — и проба нашла дефект
  в самой себе: мерила не тот набор, который проверяет.
- **Три поправки к моим выводам за час, все приняты.** (1) «Ветки влиты нацело» без указания,
  ЧЕЙ `main`: у 283 было 0 против локального и 9 против `origin/main`. (2) В споре стража и
  сводки я выбрал сторону вместо третьего замера — соврал СТРАЖ (шаблон `^error(\|:)` ловил
  `error: test failed`). (3) Отсюда его правило: **когда два инструмента расходятся, неверным
  может быть ЛЮБОЙ, включая тот, что выглядит первоисточником.**
- **Класс ночи — восемь носителей, и половина в слое инструментов, а не в коде:** мёртвый
  снимок `ps`, часы вместо моментов, слепая полоса, `os error 32` у гейта, кэш-ключ без флагов,
  сводка против стража, выдуманные ссылки охотника, тест «отказ принят», зеленевший на разборе
  SQL. Форма одна: **инструмент отвечает не на тот вопрос, который ему задан, и молчит об этом.**
- **01:00–01:11Z (04:00–04:11 мест.), первый час ночи, свёрнуто.** Очередь за машиной развернулась дважды за три минуты (ярус снят зря); в расписании нашлась ВТОРАЯ КОПИЯ правил — старое задание с полным текстом внутри, удалено, `CronList` внесён в цикл; в дежурстве найден свой дефект (снимок `ps` умирал на кодировке, а вердикт подписывался как построенный на двух); первый ярус закрылся отказом `check-accepted-branch-merged`.
- **01:16–01:51Z (04:16–04:51 мест.), свёрнуто — четыре урока, каждый прожил ночь.** (1) Третий
  ярус упал НЕ на коде: `conformance-full` дал две `CODEGEN-FAIL` с os error 32 — компилятор
  писал промежуточный `.c` рядом с исходником, и два шага столкнулись на одном пути (починено,
  доказано прогоном 07:51). (2) `freeze-now` получила МЕТКУ ПРЕДМЕТА, потому что мы с
  интегратором два цикла отвечали не на тот вопрос: «успел ли страж прочитать правку» вместо
  «а этот страж про этот файл?». (3) Она же поймала МЕНЯ на живом случае (правка на 257-й
  секунде яруса) — механизм не удерживает от нарушения, он делает его видимым в тот же цикл.
  (4) Находка `claude-limits` роздана как работа: `nova check` молча принимает `throw` без
  `Fail`; ответ окна 274 — `novac` НЕ повторяет (`E7301`/D405), значит чинить оракул.
- **Поправка владельца 01:36Z:** «274 молчит с 04:28, а ты говоришь работает». Дыра была в
  мериле: я брал ВОЗРАСТ записи, не глядя на ТИП. `assistant:text` есть конец хода, то есть
  остановка в момент рождения. Правило в команде переписано.
- **Правило того же часа:** обстановка живёт минуты — слово соседа о занятости машины
  ПЕРЕСВЕРЯТЬ командой перед передачей, иначе разносишь протухший факт как свежий.
- **03:37–04:30Z (06:37–07:30 мест.), седьмой ярус и ВТОРАЯ ЗАМОРОЗКА.** Седьмой упал на ветке
  274 — третий раз за ночь по одной схеме: за сорок минут прогона туда легли два коммита.
  Его оговорка прежняя: «плохой здесь порядок, а не люди». В 04:30Z впервые за смену были
  активны ВСЕ ЧЕТЫРЕ окна.
- **МОЙ ВОСЬМОЙ НОСИТЕЛЬ КЛАССА, и он про метод.** В толчке 06:58 я написал «правки `.c` в
  дереве нет» — а она была закоммичена в 06:46 (`7c2edab57`). Я смотрел `git status`, то есть
  ИЗМЕНЁННЫЕ файлы, откуда закоммиченное уходит по определению. **Правило: «нет в дереве»
  доказывается ЛОГОМ и содержимым, а не списком незакоммиченного.**
- **04:45Z, десятый толчок — окну 274, и он закрыл адрес целиком за две минуты.** Оно встало
  сразу после доклада, объявив «дальше беру то, что не пишет в ветку»; замер — ноль файлов
  новее его текста. Адрес взял из его же плана §5: переезд инструмента приёмки. Сделано
  больше названного — три файла под новыми именами, **девять внутренних ссылок переписаны**,
  раздел 3.1 в `novac-bootstrap.md`. Их собственная находка: перенос без правки ссылок дал бы
  инструмент, который ломается МОЛЧА в день уборки `scratch/`.
- **04:51Z, одиннадцатый толчок — `claude-limits`,** по их же строке «осталось: 13 из 15»; три
  вопроса владельцу ту охоту не держат. Их урок, ложащийся в мою роль: «мой триаж по ключевым
  словам ошибся в трёх случаях из четырёх, где я его проверил — узнать, жива ли находка, можно
  только запустив стража на подсунутом входе». И второй: их проба подставила подпись, которой
  в макетах нет, — **выдуманный вход даёт настоящий отказ**.
- **Правила, купленные циклами 04:37–04:45Z, положены в команду.** (1) В самой команде жили ДВЕ
  редакции путей к моим инструментам, и старая стояла раньше — два вызова ушли в «No such
  file»; дом пути теперь один. (2) Полный `find` по дереву рабочего окна не укладывается в
  таймаут (обход `nova-p274` — две минуты) — свежесть спрашивать точечно у названного окном
  каталога.
- **Обобщение окна 274, принятое интегратором как сильнейшее за ночь:** «у меня протух не
  каталог, а СНИМОК, но читается одинаково — шаг отработал, число получено, а относилось оно
  не к тому дереву». Форма класса: **артефакт пережил своё дерево, а измеритель этого не
  заметил.** Строку реестра он заведёт от их формулировки.
## Находки, лежащие для интегратора (после его пуша)

- **15 брошенных worktree**, отставание до 2954 коммитов; `nova-pcdoors` впереди `main` на
  коммит и запушена. Территория интегратора, не контролёра.
  **Поправка 15:43Z, моя же:** формулировка «у 12 незакоммиченные ПРАВКИ» была сильнее факта.
  Проверил содержимое: в девяти из них грязный файл ровно один и это МУСОР рабочего каталога
  (`?? TASK.md`, `?? scratch_p493/`, `?? scratch_pdiag/`), а не код. Настоящий кандидат один —
  `nova-p259` с 39 файлами. И база деревьев в порядке: `worktree-count.baseline` = 20, фактически
  20. То есть «двенадцать грязных» звучало как потерянная работа, а под этим черновики.
- **`/tmp/gate_full.done` протухает молча:** несёт `RC=1 SEC=524 HASH=25a4a495c`, тогда как
  единственная причина того отказа снята слиянием (проверено прогоном стража 14:38Z).

## Открытые вопросы к владельцу

- (пусто)
