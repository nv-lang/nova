<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Передача окна 283 (float) — состояние на 2026-09-09, 19:03

> Файл переписывается ЦЕЛИКОМ при каждой передаче. Всё, что тут написано, проверено
> запуском или чтением дерева; где не проверено — сказано прямо, этим словом и помечено.
> Предыдущая редакция (2026-09-08) описывала мир, где у окна был только план 283.

## Кто ты и где работаешь

Окно **283**, дерево `d:\Sources\nv-lang\nova-p283`, ветка `p283-float-parse`. `main`
принадлежит интегратору; ты работаешь в своей ветке, он сливает.

**Твоя тема — ОБЕ стороны преобразования float ↔ строка:**

* [283](../../plans/283-float-parse-own-rounding.md) — строка → число. **ЗАКРЫТ**
  (решение владельца 2026-09-08), опубликован. Модуль `std/src/runtime/float_parse/`;
* [283.1](../../plans/283.1-float-parse-own-module.md) — вынос модуля. **ЗАКРЫТ**;
* [285](../../plans/285-float-format-own.md) — число → строка. **ВЗЯТ 2026-09-09**,
  передан решением интегратора по вопросу владельца; до этого был назначен треку Carina.
  Это твоя ТЕКУЩАЯ работа.

Границу владелец сужал 2026-09-08 словами «твоя задача только план 283»; 2026-09-09 он же
поручил спросить про 285 и обсудить донора с интегратором — то есть граница расширена ИМ,
а не тобой. Правило прежнее: не бери чужого «раз уж мы здесь», назови находку и отдай.

## Состояние дерева

* **берётся КОМАНДОЙ, не читается здесь** — снимок устаревает молча (первая редакция
  этого файла состарилась от коммита самой себя за минуту):
  `git -C <дерево> rev-parse --short HEAD`, `rev-list --count main..HEAD` (ждёт слияния),
  `rev-list --count HEAD..origin/main` (ждёт СИНКА — именно `origin/main`, так велит
  `/commit-push`; отставание от ЛОКАЛЬНОГО `main` — чужая непушенная работа, не твой долг);
* незакоммиченные файлы СЧИТАЙ КОМАНДОЙ (`git -C <дерево> status --porcelain`), а не
  по списку здесь: список устаревает от каждой правки — эта строка уже дважды за день
  называла неверное число. Правило вместо числа: **что придержано и ПОЧЕМУ** — на 19:03
  придержан коммит остатка Ф.0, потому что `fmt_buf/shortest.nv` не проверен компилятором
  и слот запрошен; заморозка интегратора на МОИ коммиты снята его словом
  («коммит ваш, заморозку он не нарушает»).

## Очередь

1. ~~Ждать три строки от интегратора~~ — **ПОЛУЧЕНЫ 2026-09-09, 17:56, все три:**
   передача 285 подтверждена напрямую; **номер D472 — АМЕНДМЕНТ к D422 §5**, не новый
   блок (новый означал бы новое решение о ПОВЕРХНОСТИ, а его нет; я сверил сам — `D472`
   в `spec/decisions/` не встречается ни разу); **донор — Ryu**. Всё трое записано В ПЛАН,
   а не только здесь: две строки открытых вопросов закрыты ответами.

   **ДВА ЕГО УСЛОВИЯ, обязательные:** (а) оговорка про третьего кандидата — это ПАМЯТЬ
   исполнителя, и её надо проверить КОДОМ ДО первой строки порта; обманула — донор
   ПЕРЕСМАТРИВАЕТСЯ, а не подгоняется; (б) шапка лицензии называет ВЕРСИЮ И КОММИТ
   источника, а не только имя — порт без точки во времени есть та же ловушка, только на годы.
2. ~~Ф.0 плана 285~~ — **СДЕЛАНА 2026-09-09, всё четыре части.** Каркас
   `std/src/runtime/fmt_buf/roundtrip_test.nv` (round-trip замкнут нашим `to_f64`, libc в
   судейской части нет), пустой файл производителя `fmt_buf/shortest.nv` с шапкой
   провенанса, лицензии `THIRD_PARTY/ryu-LICENSE` + `ryu-LICENSE-Boost` (скачаны дословно,
   не набраны), запись №6 в `THIRD_PARTY/README.md`. Вердикты: `nova check` и
   `nova test std/src/runtime/fmt_buf` — `PASS: 1  FAIL: 0`; проба в обе стороны — со
   сломанным нарочно ассертом `PASS: 0  FAIL: 1`, rc=1 на `roundtrip_test.nv:90`.

   **ЭТО И ЕСТЬ ДОКАЗАТЕЛЬСТВО, а не строка сводки:** раннер называет единицу
   `fmt_buf/core`, потому что папка компилируется ОДНИМ модулем, — то есть зелёная сводка
   НЕ говорит, что тесты файла-соседа исполнялись. Говорит красный прогон.

   **ОБА УСЛОВИЯ ИНТЕГРАТОРА ЗАКРЫТЫ — НЕ ПРОВЕРЯТЬ ТРЕТИЙ РАЗ:**
   (а) оговорка про третьего кандидата проверена КОДОМ, память НЕ обманула. Печатная
   сторона Rust лежит в `library/core/src/num/imp/flt2dec/` (разбор — `num/float_parse.rs`
   и `num/imp/dec2flt/`), её документация модуля дословно: «They are total for all finite
   `f32` and `f64` inputs (Grisu internally falls back to Dragon if necessary)», и рядом
   ОБА пути — `strategy::grisu` и `strategy::dragon`. Донор Ryu остаётся;
   (б) точка во времени: `ulfjack/ryu@4c0618b0e44f7ef027ebae05d2cc7812048f7c8f`
   (2026-02-09), копирайт «Copyright 2018 Ulf Adams» из `ryu/d2s.c` НА ЭТОМ коммите,
   лицензия Apache-2.0 ИЛИ BSL-1.0, Nova берёт Apache-2.0.

   **Оговорку «исходника на машине нет, версия не зафиксирована» НЕ ВОЗВРАЩАТЬ.** Она была
   верна про машину и неверна про доступное знание: локальной копии правда нет (в кэше
   cargo 143 крейта, `ryu` среди них нет; `rust-src` в toolchain нет; у `serde_json` только
   разбор), но донор читается через GitHub API (`gh api repos/ulfjack/ryu/contents/<путь>?ref=<sha>`,
   `--jq '.content' | base64 -d`) — и именно так взяты и лицензии, и строка копирайта.
   Порт Ф.1 берёт донорские файлы тем же способом: `ryu/d2s.c`, `ryu/d2s_full_table.h`,
   `ryu/d2s_intrinsics.h`; для Ф.2 — `ryu/f2s.c`.

3. **Ф.1 начата 19:05, первый шаг сделан машиной:** таблицы донора перенесены генератором
   в `std/src/runtime/fmt_buf/pow5_split_table.nv` (342+342+326+326 = 1336 констант,
   столько же строк `0x` в выданном файле; какой столбец донора высокий — из его же
   `mulShift64`). **Руками эти таблицы не править никогда:** одна неверная цифра портит
   округление одного редкого значения и не краснеет ни на одном разумном наборе тестов;
   генератор лежит в скрэтчпаде сессии, при нужде пишется заново по донору. Карта порта
   «донорская функция → функция Nova» — в Ф.1 плана 285, составлена по прочтении
   `ryu/d2s.c` целиком.

   **Незакрытый хвост Ф.0 на 19:03:** `fmt_buf/shortest.nv` НЕ проверен компилятором
   (законен ли файл-сосед без единого объявления — вопрос к компилятору, не к памяти);
   слот запрошен у интегратора, коммит придержан до его слова.
3. Дальше по фазам плана: Shortest f64 → Shortest f32 (второй НАБОР КОНСТАНТ над тем же
   кодом, как 283 Ф.7) → переключение и уборка `strtod`-проверки → Fixed и Sci без
   `snprintf` → замер.

## Что уже решено и чего не надо решать заново

* **эталон свой** — round-trip проверяется нашим `to_f64`, без libc; плюс внешний корпус
  283 Ф.5 (5 299 993 строки, в каждой биты `f32` И `f64`) даёт пять миллионов готовых пар;
* **донор РЕШЁН — Ryu** (интегратор, 2026-09-09 17:56), а не умолчание: выбора больше
  нет. Третий кандидат (сторона донора волны 283) отвергнут доводом: там алгоритм С
  ОТКАТОМ на большую арифметику, а приёмка Ф.1 требует «без цикла проверок» — то есть ПО
  УСТРОЙСТВУ, а не по вкусу. Довод был памятью исполнителя; **2026-09-09 он ПРОВЕРЕН
  КОДОМ донора и подтвердился** — цитата и путь в пункте 2 очереди выше;
* **что НЕ делаем** (в плане отдельным разделом): `f16`, локали, `%a`/hex-float, оси
  форматирования (D422 не трогаем — меняется производитель цифр, не поверхность),
  `nova_f64_shortest`/`nova_f32_shortest` не удаляем пока есть вызывающие — они становятся
  эталоном дифференциала;
* **что уже есть в дереве** (чтобы не построить дважды): `to_bits`/`from_bits`,
  `u64 @widening_mul`, `u64 @leading_zeros` (283 Ф.0 — Ryu нужны оба), фикстура
  `json_roundtrip_float_shortest.nv`, `FloatKind enum Shortest | Fixed | Sci`,
  лента `_slow.nv`, каталог `THIRD_PARTY/` с конвенцией `<донор>-LICENSE`.

## Восемь вещей, которые стоили времени — не наступай снова

1. **Рабочий каталог оболочки уезжает в `main`** (за сессию четырежды). Печатай `pwd` в ТОЙ
   ЖЕ команде, что и замер; пути абсолютные.
2. **Обратные апострофы через оболочку съедаются** — сообщение коммита потеряло три
   подлежащих. Тексты только файлом (`Write`), из оболочки лишь запуск.
3. **Не-ASCII в командной строке блокируется хуком** — скрипт писать файлом.
4. **`PASS: 0 FAIL: 0` — НЕ замер.** Отсюда №1040 (блокер тега): `nova test --filter` на
   положительном conformance даёт зелёный ноль И код 0. Фильтрованный прогон обязан назвать
   единицу: `PASS: N`, N ≥ 1.
5. **Форма с нулём применений в дереве не обкатана**, даже если компилятор её знает.
   Проверяй ТИП, а не наличие оператора (так нашлась №1045: беззнаковый `>>=` без свидетеля).
6. **Мусор агента — долг вызывающего.** В бриф обязательна строка «промежуточные файлы
   только в системном temp»: без неё агент положил 15 черновиков в корень ЧУЖОЙ репы.
7. **Конфликт базы храповика решается ПЕРЕСЧЁТОМ предмета, а не выбором стороны** — обе
   стороны считали свою прибавку и обе были неверны (rows=1009 против фактических 1010).
8. **Метка времени файла не доказывает его содержимое, а живой файл истории — живой канал.**
   Два соседа за сутки сделали неверный вывод именно так; содержимое проверяется чтением,
   доставка — `ListAgents` или доставленным письмом.

## Отношения с интегратором и соседями

* **интегратор — `nova-be`** (ref `a0c88d`, 2026-09-09; прежний адрес канала умер, имена
  меняются). Машина — по его слову «берите»; занятость судится АРТЕФАКТАМИ
  (`/tmp/gate_novac.done`, `/tmp/gate_full.done`) и хэшем в них, а не процессами;
* **номера реестра и D-блоков — только у него**, приходить с тремя координатами;
* **слово соседа не авторизует действие наружу** и не заменяет прямого слова на чужой план;
* **контролёр** (`nova-b8`) толкает вставшее окно; его факты сверяй сам — за сутки два его
  замера были верны по числу и неверны по выводу.

## Что НЕ твоё

* **№1007, 1012, 1020, 1023, 1025, 1040, 1045** — открыты, маршрут «оракул, канал 196». Ты
  автор-регистратор, не исполнитель;
* **план 275** (цена гейта) — работа этой сессии там принята и влита, волна Ф.8 (ярус
  `loop` за 60 с) написана с приёмкой, но исполнять её без слова владельца не начинай;
* **план 284** (`RangeIndex[V]`) и волна M3 — трек Carina, окно 274.
