<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Записка помощника в релизе Карины — восстановление контекста

> Назначение: если сессия помощника пропала — эта записка даёт новой сессии роль,
> границы и способ понять «где мы» ИЗ РЕПОЗИТОРИЯ, а не из памяти записки. Вход в
> роль — команда `/assistant`; сохранение — `/save`; остановка с передачей — `/stop`.
>
> **Состояние здесь НЕ дублируется.** Что сделано — в git; что открыто — у двух окон,
> которые ставят задачи; блокеры тега — в выводе `registry-routes-scan.py`. Записка
> хранит то, чего в дереве нет: решения, причины, незакрытые вопросы и уроки смены.
>
> **Адресат записки выбирается ВИЗИТКОЙ, а не веткой** — в отличие от прочих ролей.
> У интегратора ветка `main`, у Карины `p274-novac`, а помощник работает в своей
> ветке под задачу, и по ветке его роль не опознаётся. Дом правила — визитка
> `nova-session-assistant.card` в общем `.git` (см. `/assistant`, шаг 0).

## 0-2026-09-21-ночь. ОСТАНОВКА — перезапуск сессии

**СДЕЛАНО:** все коммиты ветки `measure/1186-check-vs-test` уже в `main`
(`git log main..HEAD` = 0, влито интегратором ранее). Последний свой коммит —
`d65f79637` "repro(1187): where the printing site and the condition live in
different functions, and what that means for the scan" — второй набор условий
для партии 2 (семь кодов, у которых место печати и условие в разных функциях).

**В ПОЛЁТЕ:** заготовка файлов проб для партии 2 (`e-const-fn-allocation`,
`e-ambiguous-ident-pattern`, `e-addr-of-removed` + `BATCH2-NOT-RUN.md`) была
написана как python-скрипт в scratchpad, но **НЕ ВЫПОЛНЕНА и не закоммичена** —
машина была занята ярусом интегратора (`nova-37`, слово 19:31 «тяжёлого не
запускайте»), поэтому пробы решил не прогонять и не записывать в дерево
недоделанным. Скрипт остался только в scratchpad сессии, значит **новой сессии
придётся написать эти три пробы заново**, либо перечитать
`docs/plans/repro/1187-ecode-debt-split/batch2-conditions.txt` (закоммичен,
условия для 20 кодов там есть) и построить пробы по нему.

**НЕЗАКОММИЧЕНО:** `git status --porcelain` в `nova-wt-research` — пусто,
дерево чистое.

**ДАЛЬШЕ:** спросить у интегратора (был `nova-37` на 19:31, имя могло смениться
после общего перезапуска аккаунта в 00:20) и у окна Карины, свободна ли машина,
и продолжить партию 2 по 1187 — 20 кодов уже с условиями, 15 из них готовы к
пробе без дополнительного чтения, у 5 условие в разных функциях с местом печати
и форма уже названа в `batch2-conditions.txt`.

**ВОПРОСЫ ВЛАДЕЛЬЦУ:** нет открытых.

**Причина остановки:** общий лимит аккаунта (истёк 00:20, контролёр подтвердил
в 00:27), а не решение этого окна; `/stop` вызван после сообщения владельца о
перезапуске сессии.

## 0-2026-09-20-вечер. Где я работаю и что осталось несданным

**ГДЕ РАБОТА ФИЗИЧЕСКИ ЛЕЖИТ** (вопрос владельца, 2026-09-20 14:58 — и он
задан не зря: по ветке роль помощника не опознаётся, а дерево у неё своё):

* **ветка:** `measure/1186-check-vs-test`;
* **рабочее дерево:** `<рядом с репозиторием>\nova-wt-research`
  (в POSIX-виде `<рядом с репозиторием>/nova-wt-research`) — отдельный worktree
  рядом с главной копией, не сама репа;
* визитка роли: `nova-session-assistant.card` в общем `.git`, перезаписывается
  при каждом входе.

**СОСТОЯНИЕ СЛИЯНИЯ на 14:58.** Интегратор влил основное в `main`
(`0fb96f7c2` — корзина парсера и 1197). В ветке остаются НЕ СЛИТЫМИ два
коммита:

* `75c668618` — шестой храповик на именной форме (`registry-entry-shape`);
* `24bfc3b5a` — замер по 1188 (сегодняшний прогон CI).

`origin/main` при этом стоит на `d8882eee0`: работа дня в общий репозиторий
ещё не запушена, пуш — за интегратором.

**ЧТО СДЕЛАНО ПОСЛЕ ОБЕДА:**

* корзина парсера для окна Карины — 15 файлов, 4 семьи подтверждены пробами с
  контролями, и ЧЕТЫРЕ строки из восьми в одиночку читаются (её метод «первый
  отказ файла» даёт адрес первого ПРОЯВЛЕНИЯ, а не причины; она приняла это как
  поправку к методу, интегратор завёл строку 1201);
* 1197 — язык диагностик: 105 кодов с кириллицей при знаменателях 138 файлов /
  25 468 строк с литералом / 377 кодов; носитель самой строки моим же ключом НЕ
  ловится (`argbind.rs:66` собирает текст в `fn message()` без кода в строке);
* попытка сузить 677 «кириллица без кода» по соседству с диагностическим
  каналом ПРОВАЛИЛАСЬ в обе стороны — записана как результат с обеими
  половинами в `failed-narrowing.txt`;
* 1188 — новейший прогон CI судит `d8882eee`, поэтому про сегодняшний `main`
  не отвечает; на нём шаг конвенций падает (21 страж, 25 вердиктов), и следом
  в `skipped` уходят ровно три шага, включая `novac module tests`.

**ЧТО ЖДЁТ СЛЕДУЮЩЕГО ХОДА (не начато намеренно):**

1. прогнать `scripts/gate-novac.sh` ЛОКАЛЬНО на слитом дереве — единственный
   способ ответить, жив ли отказ конвенций сегодня, без пуша. Не сделано, потому
   что на машине шёл чужой push-ярус: одна машина — один гейт;
2. расширение стража языка по 1197 — только после цены «канала построения»
   (разбор кода, не греп);
3. перенос остальных храповиков на именную форму — законен словом интегратора,
   но он ниже первых двух по близости к тегу.

**ОСТАНОВКА:** по слову владельца в 14:54 («стоп, выключи таймер»). Таймеры
сняты, переспрашивать каждые пять минут перестал.

## 0-2026-09-20-день. Пять храповиков на именной форме; окно интегратора пропало

**СОСТОЯНИЕ НА 14:16.** Ветка `measure/1186-check-vs-test`, одиннадцать своих
коммитов ждут слияния (относительно `origin/main` их 87 — остальные пришли
слиянием `main`). Дерево чистое.

**ОКНО ИНТЕГРАТОРА НЕДОСТУПНО с ~09:15** (`nova-47` исчез из списка сессий,
сообщения не доставляются). **Визитка в общем `.git` всё ещё называет его** —
механизмы будут считать интегратора живым, пока она не протухнет (12 часов).
Его работа за смену НЕ ЗАПУШЕНА: локальный `main` на `c7b3947a3` (строки реестра
1193–1196), `origin/main` — на `d8882eee0`. Последнее, что он сообщал (08:13):
`gate-novac` дошёл до ТРЁХ отказов из двадцати (два в `novac/src` — Карины, и
дифф-гейт), `loop` зелёный кроме долга охоты, пуш планировался после отчёта
охотника. Карине сообщено дважды; она жива, но занята.

**СДЕЛАНО ЗА СМЕНУ (по коммитам):** приёмка 1188 и 1187; новый страж
`check-ecode-fixture-debt` (долг / покрыт / ЗАТЕНЁН, база 178 имён, 13 клеток);
две территории (`check-guard-honesty` → пробы-улики, `check-repro-evidence-suffix`
→ `docs/dev/hunts`); пять храповиков на ИМЕННОЙ форме — `guard-wiring`,
`verdict-no-status`, `machine-paths`, `mojibake` (+ `dead-anchors` был именным
с рождения).

**ЧТО ЗНАТЬ СЛЕДУЮЩЕМУ ОКНУ, чего не видно из коммитов:**

1. **Именная форма состоит из ТРЁХ частей, и третья — не украшение:** набор
   имён, СВЕРКА счёта с числом имён и ЗНАМЕНАТЕЛЬ осмотра. Без знаменателя
   сужение предиката роняет долг и читается как погашение.
2. **Как разбирать исчезновение имени — зависит от предмета.** Где предмет
   ПЕРЕЧИСЛИМ (стражи, номера строк) — помогает независимый признак: файл на
   диске, идентификатор в дереве. Где предмет — ЧИСЛО ВХОЖДЕНИЙ (пути, порча),
   «имя ушло» тождественно «стало ноль», и различает только знаменатель.
3. **Три моих дефекта за смену — все «проверка не мерит свой предмет»:** клетки
   самотеста после `exit 0` (не исполнялись, печаталось «все проверки ok»);
   мутация ядра в поддельном дереве, которое страж не зовёт; сравнение
   абсолютного пути с корнем КАК ПЕРЕДАН — при вызове с `.` ярус молча
   пропускался. Ловил КОНТРОЛЬ, не чтение.
4. **Описи без находки в дерево не кладутся.** Класс №3 искал по всем 566
   стражам: 13 кандидатов, все прочитаны, второго носителя НЕТ.

## 0-2026-09-20-полдень. Стражи: территории и именные базы

**СДАНО ЗА ЧАС (ветка `measure/1186-check-vs-test`, всё ждёт слияния):**

* `a8be51dc0` — `check-guard-honesty` судит ВТОРУЮ территорию: пробы-улики
  (`docs/plans/repro`, `docs/dev/hunts`, `docs/plans/wip`, 166 файлов);
* `a34cd731d` — `check-guard-wiring`: база ИМЕННАЯ (46 имён + сверка счёта);
* `d3d3c1c13` — НОВЫЙ страж `check-ecode-fixture-debt` к строке 1187: долг /
  покрыт / ЗАТЕНЁН, база 178 имён, самотест 13 клеток;
* `7a7c656ac` — `verdict-no-status`: база держит НОМЕРА строк (19) плюс
  знаменатель разбора;
* незакоммичено на момент записи: вторая территория у
  `check-repro-evidence-suffix` (`docs/dev/hunts`, база 157).

**ГЛАВНОЕ, ЧЕГО НЕ ВИДНО ИЗ КОММИТОВ:**

1. **Счёт в храповике прячет ДВЕ вещи, и вторая хуже первой.** Замена носителя
   (число то же, состав другой — у `verdict-no-status` это случилось ДВАЖДЫ за
   смену на глазах интегратора) и СУЖЕНИЕ ПРЕДИКАТА: перестали перечислять часть
   предмета — число упало — прочлось как погашенный долг. Лечится набором имён
   плюс ЗНАМЕНАТЕЛЕМ осмотра.
2. **Исчезновение имени нельзя разбирать предикатом самого стража** — это
   тавтология. Нужен НЕЗАВИСИМЫЙ признак: файл на диске (wiring), идентификатор
   в дереве (ecode), число разобранных строк (verdict).
3. **Мой собственный замер разошёлся с чужим и был неверен МОЙ.** 143 против 39
   комментарных кодов: `/*` внутри строки-сообщения выключала остаток файла.
   Нашла ВЫБОРКА вхождений; число само по себе выглядело правдоподобно.
4. **Расширяя территорию, сперва мерь, сколько законного покраснеет.** У проб
   охоты 186 файлов законно лежат под `cmd.sh` выше по пути; признак пришлось
   углубить до предков, иначе страж покрасил бы работу соседей и его сняли бы.

## 0-2026-09-20-ночь. Записка заведена, первая смена

**РОЛЬ.** Помощник в релизе Карины (владелец, 2026-09-19; место роли уточнено
2026-09-20). Задания приходят И ОТ ИНТЕГРАТОРА, И ОТ ОКНА КАРИНЫ напрямую.
Мера работы — РАССТОЯНИЕ ДО ТЕГА, а не объём сделанного; простаивающее окно есть
потерянный путь. Три окна не делают одну задачу — координация обязательна.

**ГРАНИЦЫ.** Код компилятора ведёт Карина; `main`, слияния, гейт, пуш, номера
реестра и приёмку ведёт интегратор. Помощник приносит ЗАМЕР и УЛИКИ, вердикт
выносит хозяин предмета. Работа — в своей ветке в своём worktree рядом с репой.

**ЧЕГО В ДЕРЕВЕ НЕТ, а знать надо:**

* **Визитка роли обязана переписываться при каждом входе.** Имя сессии меняется при
  каждом перезапуске, роль — нет; визитка старше 12 часов приравнивается механизмами
  к отсутствующей (реестр №1166, починено этой же ролью).
* **`СТОП: смена` помощнику пока НЕ ДОКАЗУЕМ.** Записка теперь есть, но `ROLE_NOTE`
  в `scripts/claude-hooks/guard-stop-v2.py` и в `remind-session-save.py` её не знает —
  правка живых хуков за интегратором. Пока он её не внёс, законная остановка одна:
  `СТОП: очередь-пуста`, и её предмет — СВОЁ НЕЗАКОММИЧЕННОЕ, а не слияние своей
  ветки (слияние стоит в чужой очереди, и ждать его значило бы запереть роль).
* **Закончив работу:** результат — тому окну, которое ставило задачу; запрос новой
  работы — СРАЗУ ОБОИМ, и в запросе прямо сказать, что следующее задание они выбирают
  СООБЩА (правило владельца 2026-09-20). Иначе приходят два задания разом, и одно из
  окон молча ждёт.

**УРОКИ ПЕРВОЙ СМЕНЫ, купленные работой, а не рассуждением:**

1. **Отказ механизма падает В СТОРОНУ ГРОМКОСТИ.** Протухшая визитка глушила
   Stop-стража у самого окна роли; лечится приравниванием протухшей к отсутствующей,
   то есть «судим по ветке, страж включён», а не наоборот.
2. **Проверять надо ДОКАЗАТЕЛЬСТВО вывода, а не сам вывод.** Верный ответ, полученный
   не тем замером, опаснее неверного: перепроверять его никто не пойдёт.
3. **Дубль ищется чтением строк того же НОСИТЕЛЯ, а не по формулировке класса** —
   формулировку каждый пишет своими словами (правило интегратора, реестр №1029/№1167).
4. **Число, которое печатает мерка, бывает свойством ПАРЫ**: «дерево + среда»
   (№1029), «дерево + порядок входов» (№1168). Спрашивать «чьё это свойство» до того,
   как число попало в доклад.

## 0-2026-09-20-утро. Продолжение той же смены, после сжатия контекста

**СДЕЛАНО И СДАНО:** приёмка реестровой строки №1188 (модульные тесты novac на CI) —
коммит `443e08d33` в ветке `measure/1186-check-vs-test`, улики в
`docs/plans/repro/1188-novac-module-tests-on-ci/`. В ветке ТРИ неслитых коммита
(1186, caveat к карте 1187, 1188) — слияние за интегратором.

**Находка, ради которой строка и заводилась:** шаг CI `novac module tests` при НУЛЕ
тестов зелен и МОЛЧАЛИВ — `rc=0`, вывод пуст, цикл `for t in …; do [ -e "$t" ] ||
continue` не исполняется ни разу. По коду возврата и по логу «зелёный, прогнал 0»
неотличим от «зелёный, прогнал N». Значит приёмка обязана требовать НЕНУЛЕВОЕ ЧИСЛО в
выводе, а не зелёный rc. Сегодня шаг вдобавок НЕ ДОСТИГАЕТСЯ: шаг конвенций падает на
четырёх последних прогонах, всё за ним `skipped`.

**УРОК СМЕНЫ, добавленный к прежним:**

5. **Визитка роли пропадает при перезапуске и после сжатия — и окно этого не
   чувствует.** Сегодня утром карточки `assistant` в общем `.git` не было вовсе:
   роль была невидима и для ролевых хуков, и для соседних окон, а само окно работало
   как ни в чём не бывало. Отсюда правило шага 0 — не «проверь визитку», а ПЕРЕПИШИ
   её первым действием каждого входа, включая вход после сжатия контекста.
