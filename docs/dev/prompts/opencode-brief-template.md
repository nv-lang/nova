<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Шаблон задания для opencode (механический исполнитель)

**Требование владельца 2026-08-08:** «используй opencode для задач агентам там, где
это возможно; для механического исполнителя нужны очень точные команды, точный путь
внутри проекта, явный список запретов и образец строки результата».

Этот файл — **шаблон, а не памятка**: копируй его целиком и заполняй, вместо того
чтобы каждый раз сочинять бриф заново. Два первых задания провалились именно
потому, что бриф сочинялся на ходу.

## Что opencode тянет, а что нет

**ТЯНЕТ:** перечисления, сверки, сборка таблицы фактов из кода, механические
замены по точному списку, отчёты «где что лежит». Проверено: читает файлы,
пишет файл, выходит с нулём.

**НЕ ТЯНЕТ (не давать):** классификацию с поиском эквивалента, суждение
«баг это или норма», проектирование, приёмку. Это к sonnet/opus.

## Ограничения среды — знать до написания брифа

1. **Запись ЗА пределы каталога проекта БЛОКИРУЕТСЯ** его системой разрешений
   (`permission requested: external_directory` → отказ). Путь результата ОБЯЗАН
   быть внутри репозитория. Первое задание провалилось ровно на этом: я дал путь
   в `%TEMP%`.
2. **Свои временные файлы он тоже пишет вовне** — если задача требует
   промежуточного файла, задание провалится. Формулируй так, чтобы промежуточных
   файлов не требовалось («держи в памяти», «пиши сразу в итоговый файл»).
3. **Сбрасывает рабочий каталог** в корень проекта (`Shell cwd was reset to …`).
4. Вызов: `opencode run "<текст>"`; есть `-m provider/model`, `--agent`,
   `-c/--continue`. Исполняемый файл — `opencode.exe` в npm-каталоге
   (обёртка в PATH ломалась о кириллицу в пути).
5. **Модель по умолчанию — `kimi-code/kimi-for-coding`** (слово владельца,
   разбор и текущая граница доверия — `docs/dev/opencode-runbook.md`, не
   повторяется здесь, чтобы не разойтись).

## Скелет задания (копировать целиком)

```
ЗАДАЧА ТОЛЬКО НА ЧТЕНИЕ репозитория плюс запись ОДНОГО файла-отчёта.
Ничего не менять, не коммитить, git не трогать вовсе.

Рабочий каталог: d:/Sources/nv-lang/nova (ты уже в нём).

КОНТЕКСТ. <одним абзацем: зачем это нужно и что уже известно>

ЧТО НЕЛЬЗЯ ДЕЛАТЬ:
1. НЕ выполнять никаких команд git.
2. НЕ изменять и НЕ удалять ни один файл репозитория.
3. НЕ создавать файлов, кроме ОДНОГО указанного ниже.
4. НЕ писать за пределы каталога d:/Sources/nv-lang/nova.
5. НЕ запускать сборку: ни cargo, ни nova, ни clang.
6. НЕ делать выводов и НЕ классифицировать — только факты из кода.

ШАГ 1. Выполни ровно эту команду:
    <дословная команда с LC_ALL=C>
Результат держи в памяти (промежуточных файлов НЕ создавай).

ШАГ 2. Для КАЖДОЙ найденной записи собери ровно эти поля:
    ПОЛЕ 1 "<имя>" — <как получить, дословно>
    ПОЛЕ 2 …
Читай реальные строки файла, НЕ угадывай по имени.

ШАГ 3. Запиши результат в ОДИН файл по абсолютному пути:
    d:/Sources/nv-lang/nova/<путь внутри репы>.md
Первая строка файла:
<!-- SPDX-License-Identifier: CC-BY-4.0 -->
Затем заголовок: # <заголовок>
Затем абзац: <дисклеймер «только факты, классификации здесь нет»>
Затем таблица со строкой заголовка ровно такой:
| # | <кол1> | <кол2> |
|---|---|---|

ОБРАЗЕЦ ЗАПОЛНЕННОЙ СТРОКИ (формат копируй, содержимое подставляй своё):
| 1 | <реальный пример> | <реальный пример> |

ШАГ 4. В конце файла — заголовок «## Сводка» и счётчики <перечислить>.

ЕСЛИ ЧТО-ТО НЕ УДАЁТСЯ разобрать — всё равно впиши строку, а в непонятных
полях поставь текст: не установлено. Не пропускай молча и не останавливайся
из-за одной трудной записи.

Записывай файл ПОСТЕПЕННО: после первых 20 записей уже создай файл с тем, что
есть, и далее дополняй (устойчивость к обрыву).

В КОНЦЕ ответь тремя строками: сколько записей найдено, сколько попало в
таблицу, полный путь созданного файла.
```

## Задание на ВЕТКУ (исполнитель вместо окна) — форма сдачи

Скелет выше — для чтения. Когда модель делает работу в своей ветке (как пачка
Kim Code 2026-09-23 по `novac/**`), в бриф дословно добавляется блок сдачи ниже.
Почему именно он и что пошло не так без него — замер в
[opencode-runbook.md](../opencode-runbook.md), абзац «ЗАМЕР 2026-09-23»; здесь
не повторяется.

```
СДАЧА — последним сообщением, пять пунктов, ни один не пропускается:

1. ВЕТКА И ВЕРШИНА: <ветка> <полный хэш>, дерево <путь>;
   вывод `git -C <дерево> status --porcelain` — пустой, иначе перечисли.
2. КРАСНОЕ ДЕРЕВА — вывод этой команды ЦЕЛИКОМ, без пересказа:
       bash scripts/tools/merge-precheck.sh --report <дерево>
   Она гоняет ярус loop обоих гейтов только на твоём дереве и печатает строки
   отказов дословно. В главное дерево не заходи и гейты push/full не гоняй.
3. ЧТО ЗАПУСКАЛОСЬ — по строке на каждый прогон:
       <команда дословно> · код выхода · итоговая строка вывода дословно
   Слово PASS, «зелёный», «проверено» без команды рядом не пишется.
4. КРАСНОЕ, КОТОРОЕ ОСТАВЛЕНО: каждое — строка отказа дословно и почему не
   чинилось (чужое / было на main / нужен чей-то ответ).
5. ЛЕДЖЕР: для коммитов в novac/** — строка docs/dev/novac-time-ledger.md за
   каждую дату этих коммитов.
```

**Приёмка не меняется:** окно, отдавшее задачу, гоняет
`bash scripts/tools/merge-precheck.sh --branch <дерево>` само. Пункт 2 сдачи
говорит, что видел исполнитель, а приёмка — чьё это красное.

## Приёмка результата opencode

Как у любого окна: **интегратор проверяет сам**. Отчёт-перечисление — сырьё,
не вердикт. Классификацию по нему делает человек. Если в файле появились
выводы или рекомендации — это признак, что бриф был сформулирован нечётко.

## Готовый рабочий каталог — `scratch-opencode/`

Не выдумывай путь для логов и не проси `/tmp`: в корне проекта уже есть
`scratch-opencode/` (содержимое под `.gitignore`, сам каталог существует). Все
промежуточные файлы направляй туда:

```
cd d:/Sources/nv-lang/nova && <команда> > scratch-opencode/step1.log 2>&1
echo "RC=$?"
```

Это и есть ответ на `permission requested: external_directory` — не ослаблять
защиту opencode, а не давать бриф, которому внешний путь нужен.

### Два наблюдения о самом запуске (2026-08-09, проверено)

* **Прогон не завершается сам вовремя.** Проба записала файл за секунды, но
  процесс не вышел и за пять минут. Запускай opencode с таймаутом и проверяй
  результат **по артефактам в `scratch-opencode/`**, а не по коду возврата
  команды и не по ожиданию её выхода.
* **Однажды обёртка упала так:** путь до `opencode.exe` потерял кириллическое
  имя пользователя и превратился в `C:\Users\` + `/node_modules/...`. Гипотеза
  про `LC_ALL=C` **проверена и не подтвердилась** (`HOME` под ним разрешается
  верно), причина не установлена. Записано как наблюдение, не как диагноз: если
  повторится — смотреть на обёртку `npm`, а не на локаль.
