<!-- SPDX-License-Identifier: CC-BY-4.0 -->

# spec/nova.ungrammar против novac/src/parse -- сырые факты (58 правил)

Только факты, собранные механически. Классификации и выводов
здесь нет -- совпадает ли грамматика с кодом, решает человек по каждой
строке отдельно.

| # | правило | грамматика | файл:строка | код | не найдено |
|---|---|---|---|---|---|
| 1 | File | ModuleDecl? Item* | novac/src/parse/parse.nv:315 | if p.i < p.toks.len() { ⏎ items.push(p.take()) ⏎ } ⏎ p.node(NodeKind.File, items) | нет |
| 2 | ModuleDecl | 'module' 'ident' ('.' 'ident')* | novac/src/parse/parse.nv:333 | if @peek() == TokenKind.Ident { ⏎ kids.push(@take()) ⏎ while @peek() == TokenKind.Dot { ⏎ kids.push(@take()) ⏎ if @peek() == TokenKind.Ident { kids.push(@take()) } else { break } ⏎ } ⏎ @node(NodeKind.ModuleDecl, kids) | нет |
| 3 | ImportDecl | ('import' \| 'use') ('ident' \| '.' \| '..' \| '/')* Err? BalancedRun? | novac/src/parse/decls.nv:152 | while @peek() == TokenKind.Ident \|\| @peek() == TokenKind.Dot \|\| ⏎ @peek() == TokenKind.DotDot \|\| @peek() == TokenKind.Slash { ⏎ kids.push(@take()) ⏎ } ⏎ if @peek() == TokenKind.LBrace { ... } ⏎ @node(NodeKind.ImportDecl, kids) | нет |
| 4 | ExportDecl | 'export' (FnDecl \| TypeDecl \| SumDecl \| ImportDecl \| ConstDecl \| Err) | novac/src/parse/decls.nv:68 | } else if k == TokenKind.KwUnsafe && @peek(skip: 1) == TokenKind.KwFn { ⏎ kids.push(@fn_decl([]Node.of(@take()))) ⏎ } else { ⏎ kids.push(@error_node(TokenKind.ErrorTok)) ⏎ } ⏎ @node(NodeKind.ExportDecl, kids) | нет |
| 5 | AttrDecl | '#' 'ident'* BalancedRun? | novac/src/parse/decls.nv:194 | mut kids = []Node.of(@take()) // '#' ⏎ while @peek() == TokenKind.Ident { ⏎ kids.push(@take()) ⏎ } ⏎ if @peek() == TokenKind.LParen { ... } ⏎ @node(NodeKind.AttrDecl, kids) | нет |
| 6 | ConstDecl | 'const' 'ident' TypeRefLeaves? '=' Expr | novac/src/parse/decls.nv:235 | if @peek() != TokenKind.Assign { ⏎ kids.append(@type_ref()) ⏎ } ⏎ @push_expected(kids, TokenKind.Assign) ⏎ kids.push(@expr()) ⏎ @node(NodeKind.ConstDecl, kids) | нет |
| 7 | LetDecl | 'ro' 'ident' TypeRefLeaves? '=' Expr | novac/src/parse/decls.nv:270 | if @peek() != TokenKind.Assign { ⏎ kids.append(@type_ref()) ⏎ } ⏎ @push_expected(kids, TokenKind.Assign) ⏎ kids.push(@expr()) ⏎ @node(NodeKind.LetDecl, kids) | нет |
| 8 | TestDecl | 'test' 'string' (Block \| Err) | novac/src/parse/decls.nv:284 | if @peek() == TokenKind.LBrace { ⏎ kids.push(@block()) ⏎ } else { ⏎ kids.push(@error_node(TokenKind.ErrorTok)) ⏎ } ⏎ @node(NodeKind.TestDecl, kids) | нет |
| 9 | FnDecl | 'extern'? 'string'? 'unsafe'? 'fn' Recv? Generics? TypeRefLeaves '(' Param* ')' RetType? Requires* (Block \| FnExprBody)? | novac/src/parse/parse.nv:548; novac/src/parse/parse.nv:557 | if @peek() == TokenKind.FatArrow { ⏎ mut ek = []Node.of(@take()) ⏎ ek.push(@expr()) ⏎ kids.push(@node(NodeKind.FnExprBody, ek)) ⏎ return @node(NodeKind.FnDecl, kids) ⏎ } ⏎ if @peek() == TokenKind.LBrace { kids.push(@block()) } ⏎ @node(NodeKind.FnDecl, kids) | нет |
| 10 | Recv | Generics? TypeRefLeaves mode:'ident'? ('@' \| '.') | novac/src/parse/parse.nv:418 | ro is_recv_mark = @peek() == TokenKind.At \|\| @peek() == TokenKind.Dot ⏎ if is_recv_mark { ⏎ head.push(@take()) // '@' or '.' ⏎ kids.push(@node(NodeKind.Recv, head)) ⏎ @push_expected(kids, TokenKind.Ident) | нет |
| 11 | Param | '...'? mode:'ident'? 'ident' TypeRefLeaves ('=' Expr)? ','? | novac/src/parse/parse.nv:480; novac/src/parse/type_decl.nv:376; novac/src/parse/type_decl.nv:410 | pk.append(@type_ref()) ⏎ if @peek() == TokenKind.Assign { pk.push(@take()) pk.push(@expr()) } ⏎ @push_if(pk, TokenKind.Comma) ⏎ kids.push(@node(NodeKind.Param, pk)) // а в type_decl.nv:376: while @peek() == TokenKind.Ident { ⏎ mut pk = []Node.of(@take()) // parameter name ⏎ pk.append(@type_ref()) ⏎ kids.push(@node(NodeKind.Param, pk)) | нет |
| 12 | RetType | '->' TypeRefLeaves | novac/src/parse/parse.nv:497; novac/src/parse/type_decl.nv:386; novac/src/parse/type_decl.nv:420 | if @peek() == TokenKind.Arrow { ⏎ mut rt = []Node.of(@take()) ⏎ rt.append(@type_ref()) ⏎ kids.push(@node(NodeKind.RetType, rt)) ⏎ } // те же 4 строки в type_decl.nv:386 и :420 | нет |
| 13 | EffectItem | TypeRefLeaves | novac/src/parse/parse.nv:492 | while @peek() == TokenKind.Ident { ⏎ kids.push(@node(NodeKind.EffectItem, @type_ref())) ⏎ } | нет |
| 14 | Requires | 'requires' Expr (',' 'string')? | novac/src/parse/parse.nv:519 | while @peek_ident("requires") { ⏎ mut rq = []Node.of(@take()) ⏎ rq.push(@expr()) ⏎ if @peek() == TokenKind.Comma { rq.push(@take()) @push_expected(rq, TokenKind.StrLit) } ⏎ kids.push(@node(NodeKind.Requires, rq)) | нет |
| 15 | FnExprBody | '=>' Expr | novac/src/parse/parse.nv:547 | if @peek() == TokenKind.FatArrow { ⏎ mut ek = []Node.of(@take()) ⏎ ek.push(@expr()) ⏎ kids.push(@node(NodeKind.FnExprBody, ek)) ⏎ return @node(NodeKind.FnDecl, kids) | нет |
| 16 | Block | '{' Stmt* '}' | novac/src/parse/parse.nv:603 | while !@at_eof() && @peek() != TokenKind.RBrace { ⏎ ro before = @i ⏎ kids.push(@stmt()) ⏎ if @i == before { break } ⏎ } ⏎ @push_expected(kids, TokenKind.RBrace) ⏎ @node(NodeKind.Block, kids) | нет |
| 17 | TypeDecl | 'type' 'ident' Generics? 'value'? 'consume'? ('priv' BalancedRun?)? ( 'protocol' ('{' ProtoMethod* '}')? \| '{' Field* '}' \| '(' Field* ')' \| 'alias'? TypeRefLeaves )? | novac/src/parse/type_decl.nv:85; novac/src/parse/type_decl.nv:113; novac/src/parse/type_decl.nv:357 | while @peek() == TokenKind.At { kids.push(@proto_method()) } ⏎ @push_expected(kids, TokenKind.RBrace) ⏎ } ⏎ return @node(NodeKind.TypeDecl, kids) // то же в :113; а :357: kids.append(@type_ref()) ⏎ } ⏎ @node(NodeKind.TypeDecl, kids) | нет |
| 18 | SumDecl | 'type' 'ident' Generics? 'value'? 'consume'? ('priv' BalancedRun?)? ('enum' Variant* \| Variant+) | novac/src/parse/type_decl.nv:248; novac/src/parse/type_decl.nv:251 | kids.push(@node(NodeKind.Variant, vk)) ⏎ } ⏎ return @node(NodeKind.SumDecl, kids) // и: ⏎ if is_sum_marker { return @node(NodeKind.SumDecl, kids) } | нет |
| 19 | Variant | '\|'? 'ident' BalancedRun? ('(' 'ident' (',' 'ident')* ')')? | novac/src/parse/type_decl.nv:207; novac/src/parse/type_decl.nv:246 | if @peek() == TokenKind.LParen { ⏎ vk.push(@take()) ⏎ while @peek() == TokenKind.Ident { vk.push(@take()) @push_if(vk, TokenKind.Comma) } ⏎ @push_expected(vk, TokenKind.RParen) ⏎ } ⏎ kids.push(@node(NodeKind.Variant, vk)) // та же пуш-строка в :246 | нет |
| 20 | Field | mode:'ident'? 'ident' TypeRefLeaves ('=' Expr)? | novac/src/parse/type_decl.nv:281; novac/src/parse/type_decl.nv:325 | fk.append(@type_ref()) ⏎ kids.push(@node(NodeKind.Field, fk)) ⏎ @push_if(kids, TokenKind.Comma) // и :325: if @peek() == TokenKind.Assign { ⏎ fk.push(@take()) ⏎ fk.push(@expr()) ⏎ } ⏎ kids.push(@node(NodeKind.Field, fk)) | нет |
| 21 | ProtoMethod | '@' 'ident' ('(' Param* ')')? RetType? | novac/src/parse/type_decl.nv:391 | if @peek() == TokenKind.Arrow { ⏎ mut rt = []Node.of(@take()) ⏎ rt.append(@type_ref()) ⏎ kids.push(@node(NodeKind.RetType, rt)) ⏎ } ⏎ @node(NodeKind.ProtoMethod, kids) | нет |
| 22 | EffectOp | 'fn' 'ident' ('(' Param* ')')? RetType? | novac/src/parse/type_decl.nv:425 | if @peek() == TokenKind.Arrow { ⏎ mut rt = []Node.of(@take()) ⏎ rt.append(@type_ref()) ⏎ kids.push(@node(NodeKind.RetType, rt)) ⏎ } ⏎ @node(NodeKind.EffectOp, kids) | нет |
| 23 | Generics | '[' BalancedRun ']' | novac/src/parse/type_ref.nv:167; novac/src/parse/expr.nv:228 | ro k = @peek() ⏎ ro delta = bracket_delta(k) ⏎ depth += delta ⏎ kids.push(@take()) ⏎ } ⏎ @node(NodeKind.Generics, kids) // и expr.nv: @push_expected(gk, TokenKind.RBracket) ⏎ @node(NodeKind.Generics, gk) | нет |
| 24 | IfStmt | 'if' cond:Expr (Block \| Err) ('else' (Block \| IfStmt \| Err))? | novac/src/parse/parse.nv:713 | if @peek() == TokenKind.KwIf { ⏎ mut kids = []Node.of(@take()) ⏎ kids.push(@head_expr()) ⏎ if @peek() == TokenKind.LBrace { kids.push(@block()) } ⏎ if @peek() == TokenKind.KwElse { kids.push(@take()) ⏎ if @peek() == TokenKind.KwIf { kids.push(@stmt()) } ⏎ return @node(NodeKind.IfStmt, kids) | нет |
| 25 | WhileStmt | 'while' cond:Expr (Block \| Err) | novac/src/parse/parse.nv:818 | if @peek() == TokenKind.KwWhile { ⏎ mut kids = []Node.of(@take()) ⏎ kids.push(@head_expr()) ⏎ if @peek() == TokenKind.LBrace { kids.push(@block()) } else { kids.push(@error_node(TokenKind.ErrorTok)) } ⏎ return @node(NodeKind.WhileStmt, kids) | нет |
| 26 | ForStmt | 'for' binder:'ident' 'in' iter:Expr (Block \| Err) | novac/src/parse/parse.nv:832 | if @peek() == TokenKind.KwFor { ⏎ mut kids = []Node.of(@take()) ⏎ @push_expected(kids, TokenKind.Ident) ⏎ @push_expected(kids, TokenKind.KwIn) ⏎ kids.push(@head_expr()) ⏎ ... return @node(NodeKind.ForStmt, kids) | нет |
| 27 | RetStmt | 'return' Expr? | novac/src/parse/parse.nv:734 | if @peek() == TokenKind.KwReturn { ⏎ mut kids = []Node.of(@take()) ⏎ if @peek() != TokenKind.RBrace && @peek_same_line() { ⏎ kids.push(@expr()) ⏎ } ⏎ return @node(NodeKind.RetStmt, kids) | нет |
| 28 | BindStmt | ('mut' \| 'ro' \| 'consume') 'ident' TypeRefLeaves? '=' Expr | novac/src/parse/parse.nv:806 | @push_expected(kids, TokenKind.Ident) ⏎ if @peek() != TokenKind.Assign { kids.append(@type_ref()) } ⏎ @push_expected(kids, TokenKind.Assign) ⏎ kids.push(@expr()) ⏎ return @node(NodeKind.BindStmt, kids) | нет |
| 29 | DestructureBind | ('mut' \| 'ro' \| 'consume') ( '(' Name (',' Name)* ')' \| '{' Name (',' (Name \| '..'))* '}' ) '=' Expr | novac/src/parse/parse.nv:763; novac/src/parse/parse.nv:792 | if @peek() == TokenKind.LParen { ⏎ kids.push(@take()) ⏎ @push_binder(kids) ⏎ ... @push_expected(kids, TokenKind.Assign) ⏎ kids.push(@expr()) ⏎ return @node(NodeKind.DestructureBind, kids) // та же return-строка в :792 (LBrace-ветка) | нет |
| 30 | AssignStmt | lhs:Expr op:'assign_op' rhs:Expr | novac/src/parse/parse.nv:838 | ro e = @expr() ⏎ if is_assign_op(@peek()) { ⏎ mut kids = []Node.of(e, @take()) ⏎ kids.push(@expr()) ⏎ return @node(NodeKind.AssignStmt, kids) | нет |
| 31 | Bin | lhs:Expr op:'bin_op' rhs:Expr | novac/src/parse/expr.nv:149 | ro right = @expr_bp(bp + 1) ⏎ left = @node(NodeKind.Bin, []Node.of(left, op, right)) ⏎ if bp == 2 { return left } | нет |
| 32 | Unary | op:'un_op' Expr | novac/src/parse/expr.nv:266 | if is_unop(@peek()) && !@minus_before_literal() { ⏎ mut kids = []Node.of(@take()) ⏎ kids.push(@primary()) ⏎ return @node(NodeKind.Unary, kids) | нет |
| 33 | Cast | Expr 'as' TypeRefLeaves | novac/src/parse/expr.nv:138 | while @peek() == TokenKind.KwAs { ⏎ mut ck = []Node.of(left, @take()) ⏎ ck.append(@type_ref()) ⏎ left = @node(NodeKind.Cast, ck) ⏎ } | нет |
| 34 | Coalesce | lhs:Expr '??' (rhs:Expr \| 'return' Expr?) | novac/src/parse/expr.nv:349 | mut qk = []Node.of(e, @take()) // lhs, '??' ⏎ if @peek() == TokenKind.KwReturn { ⏎ qk.push(@take()) ⏎ if @peek() != TokenKind.RBrace && @peek() != TokenKind.Eof { qk.push(@primary()) } ⏎ } else { qk.push(@primary()) } ⏎ e = @node(NodeKind.Coalesce, qk) | нет |
| 35 | Try | Expr '?' | novac/src/parse/expr.nv:327 | if @peek() == TokenKind.Question { ⏎ e = @node(NodeKind.Try, []Node.of(e, @take())) ⏎ continue ⏎ } | нет |
| 36 | Paren | '(' Expr ')' | novac/src/parse/expr.nv:685 | if @peek() == TokenKind.RParen { kids.push(@take()) } else { kids.push(@missing_of(TokenKind.ErrorTok)) } ⏎ @no_ctor = saved ⏎ if is_tuple { ⏎ return @node(NodeKind.TupleExpr, kids) ⏎ } ⏎ return @node(NodeKind.Paren, kids) | нет |
| 37 | TupleExpr | '(' Expr (',' Expr)+ ')' | novac/src/parse/expr.nv:681 | mut is_tuple = false ⏎ while @peek() == TokenKind.Comma { ⏎ is_tuple = true ⏎ kids.push(@take()) ⏎ kids.push(@expr()) ⏎ } ⏎ ... if is_tuple { ⏎ return @node(NodeKind.TupleExpr, kids) | нет |
| 38 | MatchExpr | 'match' scrutinee:Expr '{' MatchArm* '}' | novac/src/parse/expr.nv:587 | @push_if(ak, TokenKind.Comma) ⏎ kids.push(@node(NodeKind.MatchArm, ak)) ⏎ if @i == before { break } ⏎ } ⏎ @push_expected(kids, TokenKind.RBrace) ⏎ @node(NodeKind.MatchExpr, kids) | нет |
| 39 | MatchArm | Pattern Guard? '=>' (Block \| Expr) ','? | novac/src/parse/expr.nv:583 | if @peek() == TokenKind.LBrace && !@brace_opens_ctor() { ⏎ ak.push(@block()) ⏎ } else if @peek() == TokenKind.KwReturn { ⏎ ak.push(@stmt()) ⏎ } else { ak.push(@expr()) } ⏎ @push_if(ak, TokenKind.Comma) ⏎ kids.push(@node(NodeKind.MatchArm, ak)) | нет |
| 40 | Guard | 'if' Expr | novac/src/parse/expr.nv:543 | if @peek() == TokenKind.KwIf { ⏎ mut gk = []Node.of(@take()) ⏎ gk.push(@expr()) ⏎ ak.push(@node(NodeKind.Guard, gk)) ⏎ } | нет |
| 41 | Call | 'ident' Generics? '(' Arg* ')' | novac/src/parse/expr.nv:901; novac/src/parse/expr.nv:911 | mut kids = []Node.of(name, @turbofish()) ⏎ if @peek() == TokenKind.LParen { ⏎ kids.push(@take()) ⏎ @call_args(kids) ⏎ } ⏎ return @node(NodeKind.Call, kids) // и :911: mut kids = []Node.of(name, @take()) ⏎ @call_args(kids) ⏎ return @node(NodeKind.Call, kids) | нет |
| 42 | MethodCall | ( recv:Expr '.' 'ident' '(' Arg* ')' \| SelfExpr '@' 'ident' '(' Arg* ')' ) | novac/src/parse/expr.nv:308; novac/src/parse/expr.nv:747 | @push_expected(kids, TokenKind.Ident) ⏎ if @peek() == TokenKind.LParen { ⏎ kids.push(@take()) ⏎ @call_args(kids) ⏎ e = @node(NodeKind.MethodCall, kids) // и :747: ro self_leaf = @missing_of(TokenKind.At) ⏎ mut kids = []Node.of(@node(NodeKind.SelfExpr, []Node.of(self_leaf)), @take()) ⏎ kids.push(@take()) ⏎ kids.push(@take()) ⏎ @call_args(kids) ⏎ return @node(NodeKind.MethodCall, kids) | нет |
| 43 | NamedArg | 'ident' ':' Expr | novac/src/parse/parse.nv:577 | if @peek() == TokenKind.Ident && @peek(skip: 1) == TokenKind.Colon { ⏎ mut ak = []Node.of(@take(), @take()) // name, ':' ⏎ ak.push(@expr()) ⏎ kids.push(@node(NodeKind.NamedArg, ak)) | нет |
| 44 | FieldAccess | recv:Expr '.' ('ident' \| 'int') | novac/src/parse/expr.nv:301; novac/src/parse/expr.nv:310 | if @peek() == TokenKind.IntLit { ⏎ kids.push(@take()) ⏎ e = @node(NodeKind.FieldAccess, kids) ⏎ continue ⏎ } // и :310: } else { ⏎ e = @node(NodeKind.FieldAccess, kids) ⏎ } | нет |
| 45 | Index | ( recv:Expr '[' Expr ']' \| Name '[' Expr (',' Expr)* ']' ) | novac/src/parse/expr.nv:291; novac/src/parse/expr.nv:878 | mut ik = []Node.of(e, @take()) // recv, '[' ⏎ ik.push(@expr()) ⏎ @push_expected(ik, TokenKind.RBracket) ⏎ e = @node(NodeKind.Index, ik) // и :878: mut ik = []Node.of(@node(NodeKind.Name, []Node.of(name)), @take()) // recv, '[' ⏎ ik.push(@expr()) ⏎ while @peek() == TokenKind.Comma { ik.push(@take()) ⏎ ik.push(@expr()) } ⏎ return @node(NodeKind.Index, ik) | нет |
| 46 | SelfExpr | '@' | novac/src/parse/expr.nv:743; novac/src/parse/expr.nv:758 | ro self_leaf = @missing_of(TokenKind.At) ⏎ mut kids = []Node.of(@node(NodeKind.SelfExpr, []Node.of(self_leaf)), @take()) // и :758: if @peek() == TokenKind.At { ⏎ return @node(NodeKind.SelfExpr, []Node.of(@take())) | нет |
| 47 | SelfField | '@' 'ident' | novac/src/parse/expr.nv:753 | if @peek() == TokenKind.At && @peek(skip: 1) == TokenKind.Ident { ⏎ mut kids = []Node.of(@take()) ⏎ kids.push(@take()) ⏎ return @node(NodeKind.SelfField, kids) | нет |
| 48 | Name | 'ident' | novac/src/parse/expect.nv:114; novac/src/parse/expr.nv:860; novac/src/parse/expr.nv:914; novac/src/parse/expr.nv:918 | if @peek() == TokenKind.Ident { ⏎ kids.push(@node(NodeKind.Name, []Node.of(@take()))) ⏎ } else { ⏎ @push_expected(kids, TokenKind.Ident) ⏎ } // и expr.nv: mut ik = []Node.of(@node(NodeKind.Name, []Node.of(name)), @take()) ⏎ ... ⏎ return @node(NodeKind.Name, []Node.of(name)) | нет |
| 49 | Lit | '-'? 'literal' | novac/src/parse/expr.nv:638; novac/src/parse/expr.nv:646 | if @peek() == TokenKind.IntLit \|\| @peek() == TokenKind.FloatLit \|\| @peek() == TokenKind.StrLit \|\| ⏎ @peek() == TokenKind.CharLit { ⏎ return @node(NodeKind.Lit, []Node.of(@take())) // и :646: kids.push(@take()) ⏎ return @node(NodeKind.Lit, kids) | нет |
| 50 | InterpStr | 'str_frag' ('{' Expr '}' 'str_frag')* | novac/src/parse/expr.nv:789 | if @peek() == TokenKind.StrFrag { ⏎ mut kids = []Node.of(@take()) ⏎ while @peek() == TokenKind.InterpOpen { ⏎ kids.push(@take()) ⏎ kids.push(@expr()) ⏎ @push_expected(kids, TokenKind.InterpClose) ⏎ @push_expected(kids, TokenKind.StrFrag) ⏎ } ⏎ return @node(NodeKind.InterpStr, kids) | нет |
| 51 | ArrayLit | ( '[]' ('ident' '.' 'ident' ('(' Arg* ')')?)? \| '[' (Expr \| ArraySpread)* ']' ) | novac/src/parse/expr.nv:814; novac/src/parse/expr.nv:833 | if @peek() == TokenKind.EmptyBrackets { ⏎ mut kids = []Node.of(@take()) ⏎ if @peek() == TokenKind.Ident && @peek(skip: 1) == TokenKind.Dot { ... } ⏎ return @node(NodeKind.ArrayLit, kids) // и :833: @push_expected(kids, TokenKind.RBracket) ⏎ return @node(NodeKind.ArrayLit, kids) | нет |
| 52 | ArraySpread | '...' Expr | novac/src/parse/expr.nv:825 | if @peek() == TokenKind.Ellipsis { ⏎ mut sk = []Node.of(@take()) ⏎ sk.push(@expr()) ⏎ kids.push(@node(NodeKind.ArraySpread, sk)) | нет |
| 53 | RecordCtor | Name? '{' (CtorField \| CtorSpread \| Err)* '}' | novac/src/parse/expr.nv:842; novac/src/parse/expr.nv:916 | mut kids = []Node.of(@take()) ⏎ @ctor_fields(kids) ⏎ return @node(NodeKind.RecordCtor, kids) // и :916: mut kids = []Node.of(@node(NodeKind.Name, []Node.of(name)), @take()) ⏎ @ctor_fields(kids) ⏎ return @node(NodeKind.RecordCtor, kids) | нет |
| 54 | CtorField | 'ident' (':' Expr)? | novac/src/parse/expr.nv:609 | } else if @peek() == TokenKind.Ident { ⏎ mut fk = []Node.of(@take()) ⏎ if @peek() == TokenKind.Colon { ⏎ fk.push(@take()) ⏎ fk.push(@expr()) ⏎ } ⏎ kids.push(@node(NodeKind.CtorField, fk)) | нет |
| 55 | CtorSpread | '...' Expr | novac/src/parse/expr.nv:598 | if @peek() == TokenKind.Ellipsis { ⏎ mut sk = []Node.of(@take()) ⏎ sk.push(@expr()) ⏎ kids.push(@node(NodeKind.CtorSpread, sk)) | нет |
| 56 | Unsafe | 'unsafe' Block | novac/src/parse/expr.nv:634 | if @peek() == TokenKind.KwUnsafe && @peek(skip: 1) == TokenKind.LBrace { ⏎ mut uk = []Node.of(@take()) // 'unsafe' ⏎ uk.push(@block()) ⏎ return @node(NodeKind.Unsafe, uk) | нет |
| 57 | Err | Any* | novac/src/parse/parse.nv:194; novac/src/parse/decls.nv:98; novac/src/parse/expr.nv:928 | @node(NodeKind.Err, []Node.of(@missing_of(k))) // expect.nv-дверь :194; ⏎ decls.nv:98: lead.push(@error_node(TokenKind.ErrorTok)) ⏎ return @node(NodeKind.Err, lead) ⏎ expr.nv:648: return @node(NodeKind.Err, kids) ⏎ expr.nv:928: @node(NodeKind.Err, []Node.of(@take())) // ещё: parse.nv:289, 301, 532; expr.nv:450, 537, 614, 648 | нет |
| 58 | DepthErr | Any* | novac/src/parse/expr.nv:120 | if ek.len() == 0 { ⏎ ek.push(@missing_of(TokenKind.ErrorTok)) ⏎ } ⏎ @depth -= 1 ⏎ return @node(NodeKind.DepthErr, ek) | нет |

## Сводка

- Обработано правил из 58: 58
- Нашли место в парсере: 58
- Не нашли (поле 5 = да): 0


