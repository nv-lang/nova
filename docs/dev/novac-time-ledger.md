<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
# Леджер времени 274 — доля «274 против 221» считается отсюда

Контракт (план [274](../plans/274-novac-self-hosted-compiler.md) §1.4, поправка
2026-08-14): решение дня 30 требует долю времени «274 против 221», и без носителя
она была бы восстановлена по памяти — ровно тот «загрязнённый замер», от которого
§1.4 защищается. Одна строка в конце каждой сессии, коснувшейся `novac/**` или
фикс-окна оракула. Классы: `274` (код/архитектура novac) · `221-блокер` (фиксы
блокеров текущего компилятора) · `оракул-фикс` (фикс-окна §1.5) · `ревью`
(ревью-волны novac). Доля — десятичная доля рабочего дня, честная оценка:
доли всех строк одной даты в сумме дают не больше 1.0, потому что делится
ОДИН день, а не длительность сессии. Страж `check-novac-time-ledger.py`: дата
коммита в `novac/**` без строки здесь — красный; сумма долей за дату больше
1.0 — тоже красный. Файл живой (не wip): день 30 читает его целиком.

**Нормализация 2026-09-01 (страж; дважды: 1.01 при 10 строках, затем 1.12 при
11, затем строки 12-15; после слияния с main строка интегратора 0.01
довела день до 1.01 — пересчитан ещё раз).** День был волновым конвейером; доли пересчитаны пропорционально к
сумме 1.00 (шаг 0.05, минимум 0.05, строки не терялись). Доля — часть одного
рабочего дня, не «сколько сделано»: содержание строк не менялось.

**Нормализация 2026-08-15 (ревью 274.3, находка F4).** Доли за 14–15 августа
пересчитаны один раз. Исходные писались по ощущению «сколько сделано» и в сумме
давали 5.55 «доли рабочего дня» за 2026-08-14 и 2.60 за 2026-08-15 — метрика
§1.4 («доля времени 274 против 221», решение дня 30) от таких чисел
арифметически недействительна: делить не на что. Строки масштабированы
пропорционально до суммы 1.00 на дату, шаг 0.05, минимум 0.05 на строку —
ни одна строка не потеряна, пропорции сохранены с точностью шага (на 13 строках
одного дня шаг съедает разницу между 0.4 и 0.15: обе стали 0.05). Ведущая
тильда, как и раньше, помечает оценку и на арифметику не влияет.


**ПЕРЕСЧЁТ 2026-08-24.** Девять строк за 2026-08-23 давали в сумме 2.05 — я писал
долю как «сколько сделано», а она часть ОДНОГО рабочего дня (274 §1.4). Строки
пересчитаны пропорционально до 0.90 шагом 0.05, ни одна не потеряна; страж
`check-novac-time-ledger` поймал это на последнем прогоне последнего яруса — то
есть механизм сработал там, где я не заметил, и это ровно его работа.

| дата | класс | доля | что |
|---|---|---|---|
| 2026-08-14 | 221-блокер | 0.05 | №656 серия 6: фикс двух рук Деккера, 150 прогонов WSL, влитие и пуш |
| 2026-08-14 | ревью | 0.05 | ревью Э1 (8 пунктов) + исполнение правок в код, дверь ice(), П12 |
| 2026-08-14 | 274 | 0.05 | риск-ревью плана: §1.5, шесть поправок, линза процесса, стражи волн |
| 2026-08-14 | 274/Э1 | ~0.20 | ядро: шов эмиттера доказан и реализован — hello через novac идентичен оракулу; шаблон-оболочка извлечена фикс-пойнтом; smoke-скрипт; серде/пайплайн/ice-правки по ревью влиты merge'ем |
| 2026-08-14 | 274/Э1 | ~0.20 | ЯДРО ВЗЯТО: arithmetic+hello через novac идентичны оракулу; грамматика/эмиттер подмножества; №659-маркеры выстрелили и сняты; [LEGACY-#TBD] char-литералы |
| 2026-08-14 | 274/Э1 | ~0.05 | ноль-паник приёмка: фаззер мутаций (детерминированный) нашёл 3 класса багов — вечный цикл на повторном module, срез кодпоинта в ErrorTok, Eof-лист мимо двери позиций; все закрыты той же волной, 1011 кейсов зелёные; фикстурные каталоги переименованы под D78 rev-3 (гейт #649 оракула) |
| 2026-08-14 | 274/Э1 | ~0.05 | обвязка: корпусный дифф-раннер (60 файлов, DANGER 0 PANIC 0, цена замерена), реестр novac-divergences.md заведён, выборка №221.1, оракул-пин → cc7f3fab2 |
| 2026-08-14 | оракул-фикс | 0.05 | фикс-окно №659+№660: dummy-span дерайва, фикс+гард+фикстура, маркеры сняты |
| 2026-08-14 | 274/Э2 | ~0.05 | механизм Э2 до кода: раннер с корзинами §10.4 (вне-точки/заблокировано/самосборка) + поведенческое число смоуком; храповик novac-corpus.baseline (8/2) в check-novac-differential; предсказания Э2-а в реестр |
| 2026-08-14 | 274/Э2 | ~0.05 | Э2-а strings ВЗЯТ байт-в-байт: зонтичный probe + машинная регенерация оболочки (5900 строк из эмиссии оракула); ro/while/метод-цепочки; таблица подмножества одна в check (ребро emit_c→check в архитектуру); мини-решётка типов + диспетчер печати; граница подмножества падает в check (поле/неизвестный метод); храповик 9/3 |
| 2026-08-14 | 274/Э2 | ~0.05 | Э2-а records ВЗЯТ байт-в-байт с первого прогона: FloatLit/Ellipsis/Colon/type в лексере; TypeDecl/конструкторы (именной+анонимный spread, punning)/FieldAccess в парсере с Go-правилом «в голове if/while/for конструктора нет»; tmp-схема оракула в эмиттере; ctor только init-позицией (граница в check); probe += print_f64; храповик 10/4 |
| 2026-08-14 | 274/Э2 | ~0.10 | Э2-а match_demo ВЗЯТ: суммы/мейкеры/match с гардами/`=>`-функции/унарный минус; три правки владельца той же волной — единый реестр типов, сводная таблица деклараций (rustc Res-модель), дверь names.NameTable + запрет тихих int-дефолтов и линейных сканов (страж resolve-discipline, №652-класс); два новых бага оракула обойдены с LEGACY-маркерами (var-index-method, never-in-tail); храповик 11/5 |
| 2026-08-15 | 274/Э2 | ~0.20 | ревью владельца исполнено волной: строки реестров — записи (AoS, rustc IndexVec) value-типами (без кучи на строку), kind — суммами (DefKind/TypeKind); конвенция П13 (каждый тип/поле с комментарием, ///-док экспортным) + страж type-field-docs; семантический слой вынесен в модуль sem (rustc_middle-модель — бекендам одна семантика; П9-разрез emit_c 1008→669) |
| 2026-08-15 | 274/Э2 | ~0.15 | второе ревью sem исполнено: пары-спутники срослись в записи (DefTable/FieldTable/Scope — таблица имён + строки одним типом с методами); Prims = rustc CommonTypes (хардкод-индексы примитивов сняты, seed возвращает фактические id); kind-match вместо магических 0..3/>=4; Def.id → Def.payload (id-vs-idx честно); /// на полях нелегален в языке — кандидат в D-блок, конвенция уточнена |
| 2026-08-15 | 274/Э2 | ~0.20 | Э2-а demo ВЗЯТ байт-в-байт с первого прогона — basics ЦЕЛИКОМ 6/6: [ ] в лексере, ArrayLit/ArraySpread/массив-паттерны, std-инстансы []int/Option[int] в реестре (TkStd + c_name), Some/None через единую таблицу, tmp-схема массива; П13 расширен на функции (60 ///-доков, страж покрывает fn), []T-канон vs Vec-компиляторная форма; храповик 12/6 |
| 2026-08-15 | 274/Э2 | ~0.25 | смоук в секунды: профиль показал ~20с ВНЕ компилятора (dep-lock пакета 12с, vcvars 7с/вызов) — кэш эталонного бинаря по хэшу + прямой clang с argv оракула через дверь NOVA_CLANG (40с/файл → 5–8с); вердикт по builtins: C_*-константы/std-инстансы/Some-None — К7 и П1-класс, откат demo из подмножества (храповик 12/6→11/5, записан), дверь мэнглинга sem/mangle + страж mangle-fixed-point (самотест красный на сломанном правиле), П5 дополнен вердиктом с эталонами; страж no-name-hardcode заведён (модуль builtins — лист); 20/20 стражей |
| 2026-08-14 | оракул-фикс | 0.05 | фикс-окно №658: канал resolved_variant_ctors, реверт обхода std, фикстура |
| 2026-08-15 | 221-блокер | 0.10 | №662 вскрытие (SIGPIPE, gdb 10/10) + фикс класса №664 в driver_init |
| 2026-08-15 | 274/Э2 | ~0.10 | окно разблокировано (#669 интегратора): merge main, novac собирается; пин → 3883310ba; 56 полей → /// (D104 rev-2, обе формы: trailing коротким, над полем длинным), страж field-docs в строгом режиме по пину; батч-CLI `novac check f1 f2…` проверен (5 файлов 333 мс, rc=худший); фаззер батчем 65 с → 16.5 с; смоуки 5/5, стражи 20/20, линт 0 |
| 2026-08-16 | 274/Э2 | 1.00 | волна 1 плана 274.3 (класс К-A) закрыта: гейт строит novac и краснеет без бинаря, фаззер в гейте, одно определение паники (+молчаливая дверь), spec-queue считается, леджер судится арифметикой, маркеры #TBD видимы и стареют от первого появления, свежесть shell.tpl.c, мета-страж реестра, 7 стражей подключены и внесены в аудит, 25 самотестов; все пять реализаций потока вернулись PARTIAL от адверсарной проверки — дыры закрыты, калибровка машины в храповике цены |
| 2026-08-16 | 274/Э2 | 0.00 | (та же дата, см. строку выше — доля дня уже учтена) хардкод имён std снят по-настоящему: novac ЧИТАЕТ сигнатуры (`--std`), таблица builtin_method_ret удалена; попутно вскрылось, что ридер стоял на сломанном восстановлении — сделан фикс A2/F15/F16/F17 (терминаторы, take без assert, missing-токен нулевой ширины, защита от непродвижения в шести циклах): `g(1 + )` 4 диагностики → 1, фаззер 192/0 |
| 2026-08-17 | 274/Э2 | 0.30 | ревью владельца по коду: `ro` не держится через возврат — `fn f() -> []T => ro_binding` принимается оракулом И мутация через отмытый алиас РАСТИТ сам `ro` (репро в реестре расхождений); три двери novac переведены на `-> ro []T` (`builtin_types`, `branch_children`, `subst_of`); слияние main 5de8f26bf — исчерпаемость match стала ошибкой языка, novac под ней чист; в план записаны №705 (ключ таблицы типов несёт модуль) и №702 (порядок в generic-списке не проверять); профилирование сборки: чек 1.36с, кодоген ~3.9с, clang 1.5-2.5с, PCH в `nova build` не используется |
| 2026-08-18 | 274/Э2 | 0.30 | режимы параметра разобраны НАБОРОМ (`mut`/`ro`/`consume`/`uninit`), а не одним словом `consume`; двери `param_name_at`/`param_type_at` заведены и проведены через typing.nv; измерена тихая половина #676 |
| 2026-08-19 | 274/Э2 | 0.40 | ревью владельца по мэнглингу: имя вызываемого выводится ЕДИНООБРАЗНО для любой функции (плана 273 методов нет — ресивер это параметр ноль), параметры и тип возврата вошли в имя. Найдена живая мискомпиляция: две законные перегрузки давали два C-определения с одним именем. Заодно: `is_entry` убран из двери, `foreign` стал полем строки, вызов и «метод» резолвятся через одну дверь реестра |
| 2026-08-20 | 274/Э2 | 0.60 | разбор владельца снял `foreign` целиком: внешняя функция в Nova — это `extern "C" fn`/`extern "nova" fn`, а `Nova_<Тип>_method_<имя>` — обычный мангл обычного метода. Имя стало одним и нашим, `type_tag` — подстановочным; отсюда два отказа по имени (тела нет; имя перегружено — замер: `starts_with("hello")` печатал имя char-перегрузки), база корпуса 11/5 → 10/4 объявленным откатом. Попутно, предсуществующее: каскад от файлового счётчика диагностик (отказ в одной функции глушил привязки в следующих — «unknown name» о привязанном имени), у стража храповика не было механизма законного отката, доказыватель мутацией не судил 44 python-стража. Форма `extern` теперь ЧИТАЕТСЯ, отказ по имени (§9.4): было «this is not a form of the language» о форме языка |
| 2026-08-21 | 274/Э2 | 0.15 | newtype ЧИТАЕТСЯ: поправка интегратора сняла половину раздела 9.1г — форма есть в языке и в оракуле (проверено запуском: `type Row int` и `type Row alias int` собираются, различие типов энфорсится E7301), не читал её novac. Оба названных блокера закрыты: `@type_decl` не съедал хвост (одна ветка «хвост = ссылка на тип» закрыла newtype и alias разом), а `Row(7)` давал ICE на ЗАКОННОЙ форме — теперь чек судит конструкцию сам и пишет её в канал (`DefKind.DefType` + id типа), эмиттер пишет аргумент и только его. Модель: `TkNewtype` — седьмой вид типа, представление в args терма; две двери — `type_tag` держит различие в C-имени, `carrier_of` отвечает про значение (печать newtype печатает содержимое, как оракул). Фикстуры на обе формы объявления и на конструктор, смоук байт-в-байт; форма в реестре. Третий носитель О-1в по дороге: `Row("s")` и `Row(1, 2)` оракул принимает на чеке (первое падает в clang) |
| 2026-08-21 | 274/Э2 | 0.15 | ось РЕЖИМА в подборе (Р14) ВЗЯТА на свободных вызовах, раньше тел методов: `fn tag(c Cel)` рядом с `fn tag(mut c Cel)` дают две C-функции, `tag(r)` и `tag(m)` печатают 1 и 2 байт-в-байт с оракулом; `consume` выбирается для owned-ВРЕМЕННОГО (`pick(mk())` → `__c_Cel`), тоже байт-в-байт. Устройство: `ParamDef` (тип+режим одной строкой), `ArgSpec` (тип+класс), `mode_admits` — само правило, выбор САМОГО СПЕЦИФИЧНОГО вместо отказа «перегружено», режим вошёл в мангл (`m_`/`c_`; `ro` без метки, поэтому ни одно прежнее имя не поехало), `Scope` держит режим привязки. Три дефекта той же волной: эмиттер брал строку по ИМЕНИ (две перегрузки → одно C-имя, clang отказывал юниту); форма `consume x = …` парсером не читалась ВОВСЕ, и отказ врал «unknown name» об использовании; `uninit` числился четвёртым режимом — замер по вопросу владельца показал, что это квалификатор ТИПА (`*mut uninit T`, №358), и ложная запись 2026-08-17 в моём же коде исправлена. Пересчёт долей дня: четыре прежние строки масштабированы (0.10/0.20/0.30/0.40 → 0.05/0.15/0.25/0.30) — прецедент нормализации 2026-08-15, сумма дня ровно 1.00 |
| 2026-08-21 | 274/Э2 | 0.05 | вопрос владельца «учитывает ли `fits` режимы `ro`/`mut`/`consume`»: ответ — нет и не может (строка реестра несёт только id типов), но замер по вопросу вскрыл дефект хуже вопроса. Оба коллектора читали тип параметра фиксированным индексом `pk[1]`, а при `mut v int` там ИМЯ: файловая функция отвергалась ложью «novac не знает тип int», а в пути деклараций молча выбрасывалась ВСЯ сигнатура — то есть все `consume`-методы std, включая обе finalize-пары D429, были невидимы. Оба коллектора переведены на дверь `param_type_at`; форма `param_mode` внесена в реестр с парой фикстур, pos — смоук байт-в-байт. Попутно: `sem.nv` разрезан по смыслу (`harvest.nv` — чтение сигнатур из деклараций) и убран дубль блока, который я сам породил неудачной нарезкой |
| 2026-08-21 | 274/Э2 | 0.15 | Э2-б3, первый шаг: замер премисы вскрыл живую мискомпиляцию вместо записанного пробела — ключ вызова строился из `UNKNOWN_TYPE`, и `show(c)` с записью там, где объявлен `f64`, принимался, а эмиссия писала указатель на структуру в позиции double (программа, которую оракул отвергает). Аргументы теперь типизируются ДО подбора, отказ называет причину из трёх (имя / арность / тип с позицией), фикстура `free_call_form/neg_2.nv` отвергается обоими. Пары `#coerce` (D429) читаются из деклараций в `Ctx.coerces` — ни одной пары в коде компилятора, слово атрибута в builtins по П5; когда пара покрывает позицию, отказ говорит «вызов законен, novac не эмитит тело метода». Попутный дефект той же двери: парсер знал ровно `mut` как квалификатор ресивера, поэтому ВСЕ `consume`-методы std (включая обе finalize-пары D429) были невидимы — набор режимов сведён в одну дверь вместо трёх копий. Заодно закрыта щель стража строк реестра: он смотрел на список файлов, и перенос `CoercePair` в со-равный файл вывел поля из-под суда без правки правила |
| 2026-08-21 | 274/Э2 | 0.15 | Э2-б2 ЗАКРЫТ приёмкой: проводка инстансов сделана целиком — `pipeline.instances_of` обходит множество mono, типизирует каждое тело через `check_instance` и отдаёт эмиттеру `Instantiation` с КОНКРЕТНЫМИ типами (переписывает mono, не бэкенд); эмиттер пишет по функции на инстанс одной дверью плюс прототипы. Замер: `dbl[int]`/`dbl[f64]` — две C-функции из одного тела, смоук байт-в-байт с оракулом, фикстура `generic_call` + форма в реестре (форм 6, фикстур 7). Попутно: решётка типов получила канал (иначе дженерик-вызов отвечал `TkParam` и ломал выбор принтера), снялись два дубля правила (подстановка, «где имя декларации»), отказ на вложенном дженерике перестал врать про опечатку. Вскрыто у оракула: `check` не проверяет содержимое turbofish — лишний type-аргумент принимается и компилируется, несуществующий тип принимается и падает в clang (класс О-1в, два носителя) |
| 2026-08-21 | 274/Э2 | 0.15 | ключ mono стал `(строка, подстановка)`: тип `Instance`, в `Reach` два множества (флаг на строку для обычного вызываемого, элемент на подстановку для дженерика), дверь канала `callee_subst_of`. Заодно снята ложь доков: план и записка числили Э2-б2 «ждущим архитектурного решения», а решение было принято сутками раньше в `aae74cd24` (канал на инстанс) — переписано замером, и названо то, что реально осталось: `check_instance` зовут только из теста, настоящий конвейер — ни разу. Гигиена той же волны: восстановлен ///-док, отъеденный у соседа при вставке функции; два пустых `else` (П31); экспорт без вызывающего убран вместо поднятия базы поверхности под него |
| 2026-08-21 | 274/Э2 | 0.10 | сужение D55 из main (`03061dd9f`) окупилось в тот же час: правило назвало ЧЕТЫРЕ места в исходнике novac, где голый `int` тёк в параметр `FnRow`, и все четыре были одним скрещиванием — полиморфный `CalleeRef.target_row` (строка fn для `DefFn`, строка ВАРИАНТА для `DefVariant`) уходил прямо в дверь мэнглинга, которая хочет строку ВЫЗЫВАЕМОГО. Закрыто дверью с предусловием `CalleeRef.callable_row()` (`requires kind == DefFn`), а не слепой обёрткой; `FnRow` проведён через mono/typing/emit_c. Заодно завёрнуто то, что я малодушно отложил — три вопроса владельца подряд, и все три по делу: `decl_fn_row` и `DefTable.fn_row` отвечают `Option[FnRow]`, `Ctx.fn_decl_rows` держит `[]FnRow` с `no_row()` вместо `-1`, а `?? UNKNOWN_TYPE` со сравнением сентинела стало `if Some(id) = …` в двух местах. Пятый носитель О-1в по дороге: неописанное имя `NO_ROW` чек оракула ПРИНЯЛ, а кодоген дал `[INTERNAL-PANIC]` с обвинением компилятора. Четвёртый вопрос владельца («почему не `target_row FnRow`?») вскрыл, что и запись в плане была неверна: я записал «завернуть НЕЛЬЗЯ», а верно — «обёртка тут не форма ответа». `FnRow` был бы ЛОЖЬЮ на двух ветках из трёх, и `c_maker` получал бы `FnRow` со строкой варианта — ровно та путаница, ради запрета которой обёртка вводится. Нагрузка уехала ВНУТРЬ дискриминанта: `CalleeTarget enum | CalleeFn(FnRow) | CalleeVariant(int) | CalleeCtor(int)`, пара `kind`+`target_row` стала одним полем `target`, и `DefKind` ушёл из двух импортов — сумма несёт ответ сама. Тот же довод этот файл уже выигрывал: `callees`/`substs` срослись в `Resolution`, и обход `assert_disjoint` не усилился, а удалился. Пятый вопрос владельца («есть ещё кандидаты для newtype?») замерен, и два крупнейших кандидата оказались вообще не newtype: `Def{kind,payload}` — тот же полиморфный int по соседнему тегу (id типа / строка варианта / строка вызываемого), стал `Def{target DefTarget}` — имена плеч не менялись, а сама сумма `DefKind` исчезла целиком вместе с тремя МЁРТВЫМИ импортами, которых никто не замечал; `CoercePair.via_row` был строкой `Ctx.fns` всегда — живое скрещивание, стал `FnRow`. Инвентарь остальных внесён в план таблицей с размерами (`TyId` 193 упоминания, строка варианта 15, `NodeId` 27, строка `defs` 10, строка поля 17, слот tparam 26) и с названными НЕ-кандидатами (смещения пулов — половины диапазона, не личности; `TyRow.head_id` — хеш-ключ внутри интернера, ответ — именованные двери чтения). Шестой носитель О-1в по дороге, изолирован минимальным репро: разбор записи по НЕСУЩЕСТВУЮЩИМ полям проходит `check` и падает в clang — то есть форма, которая от переименования поля страдает первой, ломается последней и не на языке пользователя. Доли дня пересчитаны (строка mono 0.25 → 0.20 → 0.15, эта 0.05 → 0.10), сумма ровно 1.00 |
| 2026-08-21 | 274/Э2 | 0.10 | волна В1 подплана 9.1г.1 (по слову владельца «запиши в подплан и сделай»): пространство id типа завёрнуто целиком (193 упоминания, 20 файлов): `TyId` плюс двери `raw_ty`/`no_ty`/`is_ty`, `sem.UNKNOWN_TYPE` снят как `-1` под именем в модуле, который пространством не владеет. По правилу честного нуля главное число вперёд: скрещиваний «голый int в позицию типа» правило поймало **НОЛЬ** — пространство типов единственное большое, путались СТРОКИ друг с другом. Зато волна нашла четыре вещи, которые без заворачивания не видны: (1) пространств ДВА — то, что течёт, есть индекс строки `Ctx.types`, а `TypeDef.ty_id` — строка ИНТЕРНЕРА, и на `TkParam` они расходятся НАМЕРЕННО (мой первый заход поставил ассерт на равенство — он бы падал на законном реестре; ассерт снят, оба пространства названы, решение «кому обёртка» — владельцу, а не тому, кто первым набрал тип поля); (2) `candidates_from` — экспортирована, импортирована тремя файлами, НЕ вызывалась ни разу и при этом с off-by-one (в набор ложился СЛЕДУЮЩИЙ за кандидатом, а на последнем — −1) — удалена, а не типизирована; (3) эмиттер брал C-имя структуры от ИНДЕКСА, опираясь на то самое совпадение пространств — теперь от `td.ty_id`; (4) дефект оракула на трёх строках: `fn pass_int(v int) -> int => v` компилируется, `fn pass_row(r Row) -> Row => r` над `type Row int` — E_READONLY_COERCE: ro-launder-анализ не снимает newtype до носителя, хотя печать и раскладка снимают, — то есть заворачивание ЛЮБОГО примитивного пространства делает `return t` незаконным, и это цена КАЖДОЙ из семи волн подплана (обход — дверь `same_ty` под [LEGACY-#TBD-newtype-ro-launder], шесть вызовов). Рёбра `emit_c`/`mono`/`pipeline` → `types` внесены в таблицу §3 с контрактом «течёт имя и три двери». Гейт push зелёный 195с, модульные 8/8. Доли дня пересчитаны (две строки 0.20 → 0.15), сумма ровно 1.00 |
| 2026-08-22 | 274/Э2 | 0.25 | волна В8 подплана 9.1г.1 — та, что владелец поставил первой после разбора доноров: `TyId` переехал на терм ИНТЕРНЕРА, строка таблицы деклараций получила своё имя `DeclId` (модуль `names`, слой 3 карты), и тип теперь НЕСЁТ декларацию, а не наоборот — как у всех пяти доноров (rustc `Ty` vs `DefId`, Zig `InternPool.Index` vs `Decl`, Go `types.Type` vs `TypeName`, Swift `CanType` vs `NominalTypeDecl`, clang canonical `QualType` vs `RecordDecl`). **Здесь правило поймало не ноль, а живой дефект, который ТЕРЯЕТ ТИПЫ**, и найден он чтением, а не пробой: `type Count alias int` перед `type Cel { v f64 }` заставлял novac отвечать «unknown field» про существующее поле — оракул тот же файл принимает, а без строки алиаса принимал и novac. Причина: алиас потребляет id, но строки не имеет по определению (своя строка сделала бы его отдельным типом), поэтому у всех типов ниже id расходился с индексом строки, страж `id != types.len()` их пропускал, и тип молча исчезал из реестра. Починка — не лучший страж: проход 1 теперь СТРОИТ строку, «id есть индекс» держится по построению, а вид объявления читает новая дверь `shape_of_decl` по ДЕРЕВУ (прежняя спрашивала реестр, и потому ссылка вперёд читалась как «запись»). Фикстура `newtype/pos_3.nv`. Попутно снято ровно то, что предсказывало решение §9.1г.2: поддельные строки `TkParam` с именем `"#0"` для того, чего никто не объявлял (`tys.param(slot)` ЕСТЬ тип); вид типа больше не спрашивают у таблицы ДЕКЛАРАЦИЙ (`c_type`/`c_struct`/`type_tag`/`subst_type`/`carrier_of` читают `tys.kind_of`, и первый же прогон это назвал: «a PRIMITIVE names no declaration»); `representation_of` перестала ходить в таблицу за фактом, который держит терм. Ещё два дефекта нашёл ГЕЙТ, не я: (а) чекер стал ТИШЕ — три границы в `check` сторожили `raw_ty(t) >= ctx.types.len()`, то есть сравнивали id с длиной чужой таблицы; как только id стал термом интернера, граница перестала значить что-либо, и правила (исчерпаемость match, конструкция newtype, каст) молча перестали судить — корпус назвал это сам, десять файлов переехали из «отставания» в «оба принимают»; урок: когда индекс меняет пространство, проверки его ГРАНИЦЫ переезжают вместе с ним, иначе проверка превращается в пропуск; (б) МЁРТВАЯ ПАЧКА читалась как «все приняли» — снятый инвариант `types[i].ty_id == i` убил пачечный прогон на третьем файле из 61 с кодом 2, а раннер смотрел `> 2` и слово `panic`, которого в тексте ICE нет: 58 неотчитанных файлов пошли в «принято», и это ложно-зелёное в моей же обвязке (признаков смерти теперь три, включая маркер ICE; после починки пачка отвечает о 57 файлах вместо 3). Гейт push зелёный 216с, модульные 8/8. День открыт — доля не итоговая |
| 2026-08-22 | 274/Э2 | 0.25 | волна В2: строка ВАРИАНТА стала пространством (`VariantRow` + `raw_vrow`/`no_vrow`/`is_vrow`), и ни одно плечо `DefTarget`/`CalleeTarget` больше не несёт голого `int` — цель волны, а не побочный эффект. Скрещиваний поймано НОЛЬ (правило честного нуля), но вскрыт дефект оракула, бьющий по любому newtype в сумме: ЭМИССИЯ ЗАВИСИТ ОТ ПОРЯДКА ОБЪЯВЛЕНИЙ — newtype, использованный суммой, объявленной выше него, эмитится боксированным `struct Nova_X*` и сталкивается со своим же `typedef nova_int Nova_X`; изолировано на 12 строках, в эмиссии видно как три плеча одной суммы, три newtype над `int`, и среднее указателем. Внутри модуля правило идёт по ФАЙЛАМ, поэтому объявление поднято в первый файл под маркером `[LEGACY-#TBD-newtype-order-in-sum-arm]`; в реестре расхождений, канал 196. Плюс своя ошибка, снятая вопросом владельца в тот же час: `same_vrow` и `names.same_decl` завёл «по симметрии» с `same_ty` (у той 11 вызывающих) — у этих двух НОЛЬ, обе удалены, базы опущены. Урок: обёртка приносит набор дверей, и каждую надо заводить по нужде, иначе волна заворачивания сама производит мёртвый код. И ЕЩЁ ОДИН вопрос владельца в тот же час — «`raw_row(x) == raw_row(y)` почему не `x == y`?» — вскрыл класс, который производит сама волна заворачивания: `==` на newtype работает НАПРЯМУЮ (замер на трёх строках), а распаковка ради сравнения сравнивает два `int` и пропустила бы сравнение `TyId` с `DeclId` — то есть открывает ровно ту дыру, ради закрытия которой обёртка вводится. Плюс ломает контракт самих дверей («`raw_*` в выражении значит „я индексирую“»). Носителей нашлось 34 в 9 файлах, ВСЕ написаны мной за четыре волны — сняты машинно. Правило переведено в механизм: страж `check-novac-no-unwrap-compare.py` (самотест 13 случаев в обе стороны, включая «сравнение с ДЛИНОЙ зелёное» и «упорядочивание зелёное»), стражей 62 -> 63. Гейт push зелёный 215с, модульные 8/8. День открыт |
| 2026-08-22 | 274/Э2 | 0.15 | волна В3: `NodeId` — ключ каждой side-таблицы канала чекера (`tree`: `NodeId` + `raw_node`/`no_node`/`is_node`; дом там, где id КУЮТ — парсер, как `HirId` у rustc). Скрещиваний ноль — третий ноль из четырёх волн, и это сам по себе результат: пространства novac путались НЕ друг с другом, а СТРОКИ друг с другом, ровно та пара, которую §9.1г назвал в начале. По существу изменилось три вещи: `-1` для ЛИСТА стал `no_node()` и вопрос «есть ли у узла личность» перестал быть сравнением с числом; предусловия каналов стали читаемыми (`raw_node` стоит ровно там, где индексирование, как обещает контракт двери); и одна арифметика получила имя — `raw_node(file.id_of()) + 1` есть ЧИСЛО УЗЛОВ, потому что узел файла куётся последним, а до обёртки это стояло в трёх файлах без объяснения. Гейт push зелёный 295с, модульные 8/8. День открыт |
| 2026-08-22 | 274/Э2 | 0.10 | волна В4 ОТМЕНИЛА свой же план, и это её главный результат: «завернуть строку `defs`» не потребовалось — замер показал, что индекс не переживает того `rows[d]`, которое кормит (все пять читателей брали `find(name)` и индексировали на следующей строке, ни один не носил индекс дальше). Индекс, не переживающий своего индексирования, — это ШАГ, а не личность, и типизировать шаг значит завести церемонию: обёртку, три двери, строку базы — ради числа на полторы строки. Пространство УДАЛЕНО: дверь `DefTable.def_of(name) -> Option[Def]` отдаёт строку, с индексом ушли `-1`, пять проверок `d >= 0` и экспорт `find` (поверхность не выросла: одно имя ушло, одно пришло). Правило для остальных волн: прежде чем заворачивать, спроси, переживает ли значение хоть строку после использования — обёртка защищает ИДЕНТИЧНОСТЬ, и там, где её нет, защищает шаг от самого себя. Плюс третья форма раннего возврата, названная владельцем: `?? return` язык отвергает (D86), верно `?` там, где у функции есть обёртка для проброса, и явный `match` там, где её нет. Гейт push зелёный 296с, модульные 8/8. День открыт |
| 2026-08-22 | 274/Э2 | 0.15 | волна В5, первая, где НОВЫЙ СТРАЖ решал, состоится ли волна: критерий В4 («личность ХРАНИТСЯ») прогнан на `FieldRow` — да, `FieldDef.next_row` его держит, цепочка одноимённых полей ходит по пространству, значит обёртка законна. Скрещиваний ноль (четвёртый ноль из пяти волн), но вскрыт класс, невидимый до неё: `TypeDef.row_off/row_cnt` — ПОЛИМОРФНЫЙ диапазон (поля для записи, варианты для суммы, решал сосед `kind`) — тот же силуэт, что `Def{kind,payload}` и `CalleeRef{kind,target_row}`. И вскрылся он не пробой, а ОТКАЗОМ СИСТЕМЫ ТИПОВ отвечать: как только оба ряда стали типами, диапазон перестал типизироваться ни как один. Лечение то же — нагрузка внутрь дискриминанта (`TypeRows`), примитив и newtype отвечают `NoRows` вместо нулевой пары, читатели спрашивают `field_range_of`/`variant_range_of`. Третий раз за сутки сработал дефект порядка объявлений оракула: `TypeRows` называет `FieldRow` ниже себя — оба пространства строк уехали в первый файл модуля под общий маркер. Гейт push зелёный 275с, модульные 8/8. День открыт |
| 2026-08-22 | 274/Э2 | 0.05 | волна В6 ОТМЕНЕНА критерием ДО работы — первый раз, когда правило сэкономило волну, а не наоборот (В4 стоила целой волны, чтобы это правило вывести; в этом и разница между правилом и механизмом). Замер: слот type-параметра нигде не ХРАНИТСЯ — производится `tparam_slot` и потребляется на следующей строке `param_rows[slot]`; единственное «лежит» — полиморфное `TyRow.head_id`, которое по плану и не подлежит заворачиванию. Зато тот же шаг был выписан ДВАЖДЫ дословно, и это отсутствующая дверь (П18): вопрос получил имя `ty_of_type_name`. Плюс третья за сессию дверь без вызывающего — `FnTable.tparam_index` (до неё `candidates_from` с off-by-one и `same_vrow`/`same_decl`); три за сессию — привычка, а не случайность, и правило записано словами: дверь оправдывает ВЫЗЫВАЮЩИЙ. Гейт push зелёный 237с, поверхность не сдвинулась (одно имя ушло, одно пришло). День открыт |
| 2026-08-22 | 274/Э2 | 0.05 | волна В7 и ЗАКРЫТИЕ ПОДПЛАНА: голова терма полиморфна по виду, но внутри интернера это ХЕШ-КЛЮЧ — суммой ей стать нельзя, лечится ЧТЕНИЕ: `prim_ordinal_of` и `param_slot_of` с предусловием на вид, `head_of` стала приватной. Двери ровно для тех видов, о которых спрашивают: у vid вывода читателя нет — двери нет. ИТОГ восьми волн вписан в §9.1г.4: скрещиваний поймано ЧЕТЫРЕ (все в В8, все одним скрещиванием), остальные волны — ноль и так и записано; зато шесть дефектов, которые иначе не видны, и три класса, которые волны породили САМИ и которые теперь держат стражи. Два раза верным ответом была НЕ обёртка (В4, В6), один — названные чтения (В7). Гейт push зелёный 322с, модульные 8/8. Доля В8 0.30 -> 0.25, сумма дня 1.00 |
| 2026-08-23 | 274/Э2 | 0.15 | день начался словами владельца «перечитай команды» и кончился тем, что перечитывание оказалось ЗАПУСКОМ: из четырёх команд хендоффа врала одна (`loop` записан «~20с» при факте 38с), и первый прогон в тот день вышел КРАСНЫМ по бюджету — 46с при пределе 40. Причина моя: страж счётчиков реестра, поставленный сутками раньше в ярус loop, запускает ВСЕ девять самотестов, 27с из 38; перенесён туда, где живёт его напарник по реестру (ярус push), ярус вернулся к 15с. Поймал это не я и не ревью, а `check-gate-budget` — довод в пользу стражей, судящих РЕЗУЛЬТАТ, а не имя: правила «замерь стража перед loop» у меня не было и не понадобилось. Дальше запуск вывел на CI, и там оказалось главное: `nova-gate` на этой ветке красный КАЖДЫМ прогоном с 08-21, семь подряд, а мои доклады говорили «гейт зелёный» о локальном ярусе. Корень — бинарь ОРАКУЛА отстал на трое суток (собран 08-21 21:16, main влит 08-22 23:57, три `.rs` новее), CI строит свой, поэтому судил ДРУГИМ компилятором; пересборка 190с воспроизвела отказы CI локально с первого прогона. Закрыто два класса, оба с механизмом и самотестом: (К2) самотест обязан звать стража ЧЕРЕЗ интерпретатор — прямой `"$G"` на Linux даёт rc=126, самотест печатает НОЛЬ строк `ok`, и мой же страж счётчиков доложил «обещает 12, печатает 0», то есть красный пришёл не оттуда, где ошибка; два места из 147, оба мои, страж `check-novac-selftest-interpreter.py` (14 случаев, несущий — форма `OUT=$("$G" ...)`, в которой дефект и жил); (К4) бинарь оракула обязан быть новее своих исходников, потому что оракулом судится ВСЁ за рубежом F1 — страж `check-novac-oracle-fresh.sh` (12 случаев, несущий — `.rs` внутри `target/` не судится, иначе краснело бы после каждой успешной сборки), и стоит он ПЕРЕД рубежом. Два блокера названы классом и выписаны в §9.1д: (К1) оракул сменил форму суммы без полезной нагрузки на ЗНАЧЕНИЕ — это план 172.14 F.2 атом A4, `efbe73ff5`, kill-switch `NOVA_KILL_A4`; novac всё ещё отвечает `Nova_<X>*` и САМ определяет эти типы через `nova_alloc`, то есть две формы одного std-типа — ровно тот ABI-конфликт, ради которого фикс-пойнт-страж и заведён; волна расписана в §9.1д-К1 по правилу донора дословно, вместе с честным открытым вопросом (пункт «не за забором» требует учёта УПОТРЕБЛЕНИЯ, которого у novac нет); (К3) перехват clang-argv в смоуке сделан Windows-only (`.cmd` + `cygpath`, 7 мест в 4 файлах) — на Linux падает на каждой фикстуре и тянет за собой дифф-раннер. Снимок оболочки пересобран свежим оракулом (6045 -> 6022 строк). Стражей 70 -> 72. Локальный гейт: один отказ остался — К1. День открыт, доля не итоговая |
| 2026-08-23 | 274/Э2 | 0.10 | Э2-б3, волна В1 подплана §9.1е (сам подплан написан этой же волной: порядок В1..В5 нигде не был назван, а волна без порядка превращается в «сделаем всё сразу»): аргумент-направленная дедукция type-аргументов, донор Go 1.18. Премиса проверена запуском ДО работы: `dbl(4)` отвергался названно, `dbl[int](4)` принимался. Несущее решение — дедукция и turbofish наполняют ОДНУ переменную и пишут в ОДИН канал (`node_substs`): второй источник подстановки заставил бы мономорфизацию спрашивать два места, какую инстанцию значит вызов. Приёмка числами: эмиссия `dbl(4)` байт-в-байт равна `dbl[int](4)` (6033 строки; сверять пришлось на ОДИНАКОВОМ имени модуля — мэнглинг несёт длину имени, и первая проба сравнивала несравнимое), 12 фикстур из 12 байт-в-байт, корпус без сдвига, модульные 8/8, ярус push зелёный 248с. Волна поймала ОДНО скрещивание, и оно у оракула: `fn make[T]() -> T` (параметр только в возврате) проходит его `check` и падает в кодогене — шестой носитель класса О-1в, в реестре расхождений; фикстурой не закреплено сознательно (исход расходится, `neg_*` утверждает согласие), держит модульный тест. Гейт назвал две мои недоделки в ту же минуту: неполное ветвление (П31) и храповик веток — восемь из девяти локальных веток получили копию на origin (сделал не я), база опущена 9 -> 1 тем же диффом; осталась `p274-mangle-subst` с 217 невлитыми коммитами. Плюс поправка интегратора принята: прирост мёртвых хешей — мои же коммиты в моих же документах, на main 829==829, базу не двигаем; мой union-фикс (`main` ∪ `origin/main` ∪ `HEAD`) снимает этот ложный класс ДО слияния ветки. День открыт |
| 2026-08-23 | 274/Э2 | 0.05 | бюджет яруса перестал судить ПЕРЕСБОРКУ как проверку: ярус loop шёл 15с на свежем бинаре и 26с сразу после слияния, пересобравшего оракула, — и второй прогон краснел на здоровом дереве. Цена сборки вычитается и называется вслух в вердикте; доказано в обе стороны. |
| 2026-08-23 | 274/Э2 | 0.15 | волна В4 §9.1е: умолчания и ИМЕНОВАННАЯ передача (D102). Связывание стало решением чекера и уехало в канал; правила — три бесаллокационных примитива, дверь поиска осталась дверью. Свой же регресс (`type_id >= 0` отвечало ИСТИНОЙ для `no_ty()`) починен по КЛАССУ: страж `tyid-door` нашёл ещё двух носителей. |
| 2026-08-23 | 274/Э2 | 0.10 | волна В5 §9.1е: объявленная коэрсия (D429) — вторая ступень подбора, `E_COERCE_AMBIGUOUS`, вставка зовёт НАЗВАННУЮ дверь. Доказано в обе стороны: без обёртки отказывает clang, без обхода достижимости — линкер. Подплан §9.1е закрыт целиком. |
| 2026-08-23 | 274/Э2 | 0.10 | вопросы владельца по диффу: `consume` — настоящее ключевое слово (novac ПРИНИМАЛ `fn consume()`, который оракул отвергает), и `@peek_ident()` — четыре копии одного вопроса. Обёртка сразу показала, что `enum` написан в парсере, а не взят из builtins. |
| 2026-08-23 | 274/Э2 | 0.05 | ICE `leaf_kind on a branch node` (нашла умирающая пачка корпуса) починен по классу: одна дверь `mode_of_leaf` со защитой внутри. Пачка ожила, прогон корпуса подешевел 16.3с → 1.5с. |
| 2026-08-23 | 274/Э2 | 0.10 | §9.1д: страж уникальности НАШИХ имён (приёмка вписана ДО кода), затем путь модуля вошёл в имена. Порядок нарочный: механизм раньше, имена потом. |
| 2026-08-23 | 274/Э2 | 0.10 | рост подмножества по КОРПУСУ: `export` прочитан (пять обходов детей файла свелись к двери со стражем), форма `import` — корзина из 755 диагностик оказалась двумя строками парсера, размещение value-записи легло с фикстурой на семантику копии. |
| 2026-08-24 | 274/Э2 | 0.20 | перепроверка: статус-строка плана правлена, main влит (CI молчал из-за конфликтующего PR), вторая одноразовая цена бюджета названа маркером, три инварианта на честном слове получили держателя. |
| 2026-08-24 | 274/Э2 | 0.20 | типы ПЕРЕДАННЫХ объявлений входят в реестр, и своё объявление побеждает чужое одноимённое. Замер снял мою же цепочку (ненаблюдаема) и вернул границу (правило «дубликат» принимало своё за чужое: файл компилировался против `NovaValue_Pair`). |
| 2026-08-26 | 274/Э2 | 0.09 | маркер `[M-novac-gate-pch-not-subtracted]` переехал из плавающих в план 274: CI потребовал ему номер в реестре 221.1, а номер был бы неправдой — маркер про инструмент плана, не про дефект поставляемого компилятора. Правка маршрута, а не номера. |
| 2026-08-26 | 274/Э2 | 0.18 | объявления std разбираются ОДИН раз на процесс. Замер спрашивал про цену полного интерфейса std и нашёл дефект: тексты парсились заново на каждый файл пачки. Корпусная пачка 1308 → 740мс, с полным std 17548 → 2620мс — только после этого расширение интерфейса стало оплатным. |
| 2026-08-26 | 274/Э2 | 0.09 | номера обходов 760–764 получены от интегратора и проставлены: девять площадок маркеров в семи файлах, три строки реестра получили номера, заведены строки 760/761. Попутно снят дубль №753 — след вчерашнего разрешения конфликта «сохранить обе стороны». |
| 2026-08-26 | 274/Э2 | 0.09 | шаг А границы интерфейса std: novac читает ВЕСЬ `std/src` рекурсивно (минус тесты), generic-объявления пропущены сборщиком — иначе голый `Vec` в подписи стал бы РАЗРЕШЁННЫМ (вседозволенное расхождение, найдено до кода и вошло в критерии). `Metadata` в подписи CLEAN, пачка 3504мс, дифференциал 16/16, свои файлы 0 → 0 по «unknown type». |
| 2026-08-26 | 274/Э2 | 0.09 | шаг Б1: generic-объявления регистрируются ГОЛОВАМИ (`TypeDef.tparam_cnt`, §10.3в), голое `Vec` отвергает резолвер — и текст отказа говорит правду («напиши аргументы типа»), а не «unknown type». Оракул голую голову ПРИНИМАЕТ (замер) — строгость нарочная и записана. sem.nv разрезан: реестр имён → defs.nv. |
| 2026-08-26 | 274/Э2 | 0.14 | шаг Б2: `Vec[int]` и `[]int` в позиции типа — терм применения через одну дверь `ty_of_type_ref` (D239-сахар, `Self`, арность головы как крышка Б1). Гистограмма «type form» по своим файлам 159 → 16. Ворота биндера «тип — простой идентификатор» сняты: тип решает СТРОКА. slots.nv разрезан → typeref.nv. |
| 2026-08-26 | 274/Э2 | 0.09 | шаг Б3: C-имя ИНСТАНСА — написание оракула (`Nova_Vec____nova_int*`), интероп назван в двери с условием конца; юнит-тест доказан в обе стороны. Два стража П24 судили шире правила (запись разделителя, тест-спецификация) — сужены замером, самотест 8 → 12. |

| 2026-08-26 | 274/Э2 | 0.09 | подплан Л, сантехника: набор символов оболочки — ВХОД компиляции (`Ctx.interop`, снят с встроенной оболочки один раз на процесс, §10.3б), двери имён инстанса (`static_new`, `_method_<name>`) с юнит-тестом; emit_c.nv перешагнул тысячу — оболочка уехала в `shell.nv`. |
| 2026-08-26 | 274/Э2 | 0.04 | Л1: литерал массива в двух написаниях — тип через терм `Vec[elem]`, отказ по имени там, где оболочка не несёт инстанса, эмиссия `static_new`+`push`; сквозной смоук байт-в-байт; каскад `[]int.of` снят в парсере; typing.nv разрезан → binds.nv. |
| 2026-08-26 | 274/Э2 | 0.10 | Л2+Л3: методы инстанса — структурное сопоставление типов (`type_admits`/`slot_binding_in`, донор Go `unify`), `-> @` впервые читается парсером и харвестом, `type_of` читает канал, mono не инстанцирует чужие строки, эмиссия — имя оболочки; фикстура `vec_len.nv`, корпус 11/5; граница №676 пробой. |
| 2026-08-27 | 274/Э2 | 0.05 | синк с `main` (13 коммитов, два конфликта разрешены руками: реестр — объединение по номеру, `window-status.sh` — редакция main с внешним гейтом №770); оракул пересобран, оболочка перегенерирована без изменений; `spec-queue` 24 → 26 по машинному замеру после двух новых решений в спеке. Кода novac не менялось. |
| 2026-08-28 | 274/Э2 | 0.01 | интегратор: `spec-queue` 26 → 28 по машинному замеру — D239-амендмент (ёмкость литерала) и vec-sweep-амендмент легли в спеку. Кода novac не менялось. |
| 2026-08-29 | 274/Э2 | 0.01 | интегратор: `spec-queue` 28 → 29 — уточнение D200 (тип у module-const необязателен, дом правила D184) легло в спеку; для парсера novac это подтверждение, а не новая работа (голая `const X = 7` — его собственная основная форма). Кода novac не менялось. |
| 2026-08-27 | 274/Э2 | 0.05 | правка владельца: вместимость литерала массива — точная (`_static_new(2)` вместо оракульского `0`), расхождение формы записано группой В; оба смоука байт-в-байт зелёные, модульные 10/10. |
| 2026-08-27 | 274/Э2 | 0.05 | вместимость литерала по амендменту D239: доказано над НАБЛЮДАЕМЫМ (`[7, 8].cap()` — у нас 2, у оракула 8), геттер `cap()` введён в пробную программу и поверхность оболочки, фикстура корпуса + строка allow + история; исправлено моё ложное «вместимость ненаблюдаема»; смоук научился показывать ОБЕ стороны расхождения. |
| 2026-08-28 | 274/Э2 | 0.10 | 274.4 шаг 1: решение об операторе переехало в канал (разрешений вызываемого вне `check/` — ноль), неоднозначный `equal` отвергается чекером по имени; найдено и закрыто, что `==` на строках никогда не линковалось — дверь `c_shell_method`, имя по владельцу тела; фикстура `str_eq.nv`, корпус 13/6. |
| 2026-08-28 | 274/Э2 | 0.30 | 274.4 шаг 2: модуль resolve — FnIndex у чекера, отказ пишет тип, вход через канал; вторая дверь закрыта значением |
| 2026-08-28 | 274/Э2 | 0.15 | 274.4 шаг 3: поле решает обход, FieldIndex в резольвере, priv на обоих индексах; приватность доказана отказом пробы |
| 2026-08-29 | 274/Э2 | 0.08 | 274.4 шаг 4: правило E стража channel-one-writer -- дверь подбора вне check/ и resolve/ красит гейт; самотест обеими сторонами |
| 2026-08-29 | 274/Э2 | 0.20 | 274.5 волны В2+В3: модульный const (подстановкой, через канал) и маркер priv со спектром квалификаторов; корпус 13/6 -> 16/9 |
| 2026-08-29 | 274/Э2 | 0.12 | 274.5 волна В4: хвостовой вызов — значение возврата (два зеркальных исключения сняты, контекст одним полем); корпус 16/9 -> 17/10 |
| 2026-08-29 | 274/Э2 | 0.12 | 274.4 шаг 5: колонка «дом двери» в §3а, машина в arch-class-proofs (21 дверь), самотест 5->12; подплан закрыт |
| 2026-08-29 | 274/Э2 | 0.15 | 274.5 волна В1: тип ПОЛЯ читается той же дверью, что тип параметра (парсер + чекер + sem); корпус 13/6 -> 14/7 |
| 2026-08-29 | 274/Э2 | 0.14 | 274.5 волна В6: `#realtime` читается и не проверяется (по замеру оракула); САМОДИСТАНЦИЯ 53/53 -> 41/53, корпус 18/11 -> 19/12 |
| 2026-08-29 | 274/Э2 | 0.18 | 274.5 волна В5: `match` инструкцией и хвостом тела; форма исчезла из переписи 5 -> 0, корпус 17/10 -> 18/11 |
| 2026-08-30 | 274/Э2 | 0.08 | 274.5 В8-А: индексация получила УЗЕЛ и честный отказ (35 из 56 меток «литерала» были её); разводка с турбофишем — по токену за скобками |
| 2026-08-30 | 274/Э2 | 0.16 | 274.5 В8-Б: литерал массива аргументом и полем конструктора (hoisting во временную); emit_match.nv отрезан от emit_c.nv; корпус 19/12 -> 20/13 |
| 2026-08-30 | 274/Э2 | 0.12 | ложь замера 41/53 вскрыта: пачка умирала на ice (метка Bin до операндов); раннер откатывается при ICE; честная самодистанция 54/54 |
| 2026-08-30 | 274/Э2 | 0.16 | 274.5 В9-А: `{` после `=>` — блок, а не анонимный конструктор (29 лгущих меток); цепочка чекер-решётка-эмиттер; корпус 20/13 -> 21/14 |
| 2026-08-30 | 274/E2 | 0.19 | hunter defects 810 (int literal magnitude: silent miscompile) and 807/808 (parser depth counter, one named refusal, no stack death); class tests for both |
| 2026-08-30 | 274/Э2 | 0.01 | интегратор: `spec-queue` 29 → 31 по машинному замеру — два коммита D467 (сама норма строковых литералов и амендмент про ХВОСТ блочного backtick-литерала, симметричный голове). Для novac это РАБОТА, а не подтверждение: его лексер backtick’ов не знает вовсе, а 23 его тестовых payload’а держатся на сыром переводе строки внутри `"..."`, который D467 §4 снимает (миграция — Ф.2 плана 277, сознательно ПОСЛЕ Ф.5). Кода novac не менялось. |
| 2026-08-30 | 274/E2 | 0.13 | hunter defects 809 (a closing bracket was optional in all SIXTEEN places; the pair is a cause only when the file ended AND the construct is whole -- four earlier formulations rejected by the run) and 812 (record construction judged against its declaration: unknown name, missing field, name twice); expect.nv cut out of parse.nv; guard + two class tests, both proved reversible |
| 2026-08-30 | 274/E2 | 0.15 | #815 class fix: ONE door for every required token (18 of 20 sites judged one by one), the gap rule moved into the checker where order does not exist, the two doors merged when their bodies became identical, the loop-without-a-minimum shape of the same class, guard renamed and widened; hunt battery 13 divergences -> 0 |
| 2026-08-31 | 274/E2 | 0.04 | resolve: the winner of an overload lookup is kept in the pass that finds it -- the second walk of the chain and the invariant it needed (`ice`: "a row of this specificity will be found again") both go. Owner's question; the doc's justification did not hold, because a caller sees only ONE of the two rows. resolve.nv drops from two ices to one |
| 2026-08-31 | 274/E2 | 0.08 | resolve: a protocol signature is built with StringBuilder instead of interpolating the accumulator -- convention P36 plus the guard that holds the class (9 self-test cases, three of them the boundary: builder, other variable, _test.nv). Owner's instruction. The guard is red on the HEAD snapshot at all four sites and green on the tree. Side payoff: the guard-wiring ratchet had crept 52 -> 57 unseen, and TWO of the five did name an address -- the reader knew three spellings, not the fourth. Reader fixed, three of my own guards addressed, base lowered to 50 |
| 2026-08-31 | 274/E2 | 0.10 | the gap names the token it wanted (#850): a required token's door kept the kind it wanted instead of dropping it (Roslyn EatTokenAsKind), and the checker says it -- "expected a name", "expected `=`", measured through the CLI. Safe because the leaf sits inside an Err BRANCH and every consumer filters direct children by kind (measured, not assumed); recovery keeps the old message on purpose, since a resynchronised gap knows a form broke but not what would mend it. Spelling table is a full match over 74 kinds so the compiler forbids a kind with no spelling |
| 2026-08-31 | 274/E2 | 0.14 | five ICEs on novac's own source go to zero (two roots: a reader that did not exist for `Index`, and a question asked after a refusal -- `nosuch(i)[0]`), and wave B12 compiles a call in statement position instead of refusing everything but `println`. Self-distance moves for the first time, 57/57 -> 56/57, though the file that came clean is a `_test.nv` the plan defers. The first acceptance assertion was green WITHOUT the fix and the reverse probe caught it |
| 2026-08-31 | 274/E2 | 0.12 | wave B13: a record constructor stands in argument position, hoisted into a temporary the way an array literal has been since B8. The map missed a fourth place and a probe found it: `is_expr_kind` never named RecordCtor, so the argument was not COUNTED. The four steps that judge a construction became one door called from both positions instead of a copy. Oracle prints 7 on the same program; the reverse probe reddens exactly this test with a named ice |
| 2026-08-31 | 274/E2 | 0.10 | wave B14: a tail `if` is the returned value. Two doors, one of which the project did not have -- emit a block and assign its TAIL to the result temporary, because an `if` branch is a block where a match arm is an expression. Branches must agree on a type or the form is refused by name. `IfStmt` joins the tail allow-list and NOT `is_expr_kind`, applying B13's lesson without paying it again. Oracle prints 1 on the same program; the first reverse probe was invalid and that is recorded |
| 2026-08-31 | 274/E2 | 0.06 | B14b: an `if` initializer binds through the B14 doors -- three routing arms, no new mechanism. The probe caught the fact of the wave before the commit: value-position `if` is its OWN node kind (IfExpr, same child layout), so the first arm matched IfStmt and never fired. Ten carriers in novac itself (haiku's list, verified). Oracle prints 10 on the same program |
| 2026-08-31 | 274/E2 | 0.08 | hunt emit_c x K1 accepted (hunter: opus; 11 findings in 5 classes, 20+ controls, 56 probes). Two classes are TODAY'S waves: the hoist runs argument effects ahead of its neighbours, and a construction nested in a ctor FIELD is emitted twice (the hoist walk descends into the subtree AND emit_record_ctor hoists the same field again). Verified by my own runs and by reading the C |
| 2026-08-31 | 274/Э2 | 0.01 | интегратор: перегенерация `novac/src/emit_c/shell.tpl.c` — фикс №799 в оракуле (локали фазы 2 callnorm получили имена `__nova_bind_*`) сдвинул ЭМИССИЮ, а шаблон — её снимок, то есть он протух в ту же секунду и `check-novac-shell-freshness` покраснел на CI. Дифф — 41 строка, все переименование локалей. Проверено прогонами: свежесть шаблона ok, novac собирается текущим оракулом (9.1с), `novac-e1-smoke` байт-в-байт, модульные PASS 10 FAIL 0, мэнгл-страж ok. **Кода novac не менялось.** Урок: правка оракула, меняющая ГЕНЕРИРУЕМЫЙ C, требует ОБОИХ гейтов — снимки эмиссии живут в дереве novac. |
| 2026-09-01 | 274/E2 | 0.05 | H3 of the hunt: int negation was raw C in the file where addition goes through the checked helper -- the oracle panics on INT_MIN, novac printed it back in silence. Negation now emits nova_int_checked_neg, form chosen by the CARRIER read from the channel (the H5 door); floats and `!`/`~` keep the raw form. Both probes green byte-for-byte, reverse probe reddens the carrier, corpus ratchet 25/18 |
| 2026-09-01 | 274/E2 | 0.05 | H4 of the hunt closed by a NAMED refusal, not the ABI: the fixed point lowers mut-value parameters to V* (plan 184 P10) and novac passed the record by value -- the callee wrote to a copy, the oracle prints 2, novac printed 1 in silence. The receiver case had the refusal already; the parameter case now shares it, judged where type and mode meet. Zero carriers in novac itself (grepped over all 23 value types), so the subset loses nothing; the full ABI is its own wave on ground the oracle still calls open |
| 2026-09-01 | 274/E2 | 0.05 | B15 step 3: the tuple TERM lives in the interner as its own kind TkTuple (head = arity, components in args) -- no synthetic DeclId, so no door lies about nominality. The full-match mechanism listed all 15 dispatch sites itself and each answered by context: structural doors walk components with arity for a head, emission arms ice with 'arrives with step 4', wave bounds ice by the neighbouring pattern. Reverse probe is named: a fresh id for a head fails exactly the new test on p1 == p2 |
| 2026-09-01 | 274/E2 | 0.10 | B15 step 4a: a tuple VALUE compiles -- `t.0` parses (IntLit after the dot; the lexer needs a digit BEFORE a dot for a float, so no conflict), the pair types through the step-3 term with an explicit bind route (TupleExpr is not in is_expr_kind, B13's lesson), the projection is judged against the ARITY before the record door, and the emission lays a D123 mono struct with on-demand guarded typedefs ordered by term id. Byte-for-byte against the oracle on three probes; the reverse probe needed a pair the SHELL does not carry -- (int,int) stayed green with typedefs off, which is why the mixed pair became its own corpus carrier |
| 2026-09-01 | 274/E2 | 0.05 | the tail probe of the same wave found a hole 4a itself opened: a bare tuple STATEMENT passed check in silence (the tail_rules arm trusted a refusal 4a had lifted) and ICEd the emitter. The oracle accepts and discards the form; novac now types it by a named arm and builds the dropped temporary. The gate then held three shapes honest: the file split (emit_tuple.nv, P27 header), two P31 branches, and the kind comparison moved into the is_tuple door (P21) with the surface chronicle |
| 2026-09-01 | 274/E2 | 0.10 | the owner's question opened the same-name class and the NORM closed it: D35 refuses a primitive's name forever (the whole universe table, not the subset's four), D125 lets a user type shadow a handed-over one, and D381's module-qualified C name makes the shadow emittable -- own_shadow_prefix reuses the same module_prefix door functions have carried since 9.1d. The first cut refused every shadow and the diff corpus caught the false width within minutes: a hunt carrier shadows std's Inner and had always worked. Reverse probe named (prefix off fails exactly the holder on Nova_p_Widget); the oracle's own two defects on the same probes go to the registry as TBD |
| 2026-09-01 | 274/E2 | 0.10 | B15 step 4b: a tuple RETURN type compiles byte-for-byte -- the reference door reads the paren run by depth into the step-3 term (one TyId for signature and literal), LParen leaves the return-form refusal the way []T left with B2, type_tag spells the pair as the D123 mono name (a return type is an overloading axis, D84), and the emitter's tail chain returns the built temporary. The reverse probe renames the tail arm and the carrier falls off the function's end. A tuple PARAMETER stays out: the form rule refuses it today and the bind judge holds the boundary for the day the form narrows |
| 2026-09-01 | 274/E2 | 0.05 | B15 step 5 closes the tuple wave: `ro (a, b) = two()` byte-for-byte -- the pattern is its OWN kind (TupleBind), the judges refuse a non-tuple initializer and an arity mismatch by name (the arity text carries D123's class), and the emission is one temporary plus a local per name from .fN. The 4a lesson paid once, not twice: the checker's lattice answer is not a record, so the initializer's type is recorded explicitly. Acceptance (v) of the wave lands: the tuple refusals are GONE from the core files -- check.nv now stops on a subset method, pipeline.nv on the for head (the open COLLECT question), main.nv on a syntax form |
| 2026-09-01 | 274/E2 | 0.05 | wave B16, both halves in one day: a for head that is a CALL, NAME or FIELD walks its vector -- the head runs once into a temporary, the loop guards on _method_len and reads _method_get(v,i).value, where .value is the shell's Option ABI (interop, read off the template) and never a language Option. The loop variable binds to the element type through the same TkApp doors index typing walks; the head's type is recorded because the Call arm does not cache. Three probes byte-for-byte including the head-effect-once contract; the scalar head refuses by name. pipeline.nv moves to its next layer (the two-call println limit) |
| 2026-09-01 | 274/E2 | 0.10 | the sibling-module interface hands in through NOVAC_SELF_PATH -- an env root beside std, the exact NOVA_STD_PATH shape, unset = old behaviour. The dominant self-distance class collapses 22 -> 3 files; the diff runner self-build leg sets the env and its metric moves to 60/61 refused with ONE file reading clean end to end. All four machine acceptance points from the plan entry, including the reverse probe (env off -> 22 again) and the untouched corpus ratchet. The peers-as-foreign lie is named and tolerated until the multi-file pipeline |
| 2026-09-01 | 274/E2 | 0.05 | the if-let wave: `if Some(v) = xs.get(1)` byte-for-byte -- the Option instance takes its FAMILY spelling (NovaOpt_<elem>, by value, the mangle door; other sums keep the named ice), the instance-method judge lifts the family refusal exactly for Option, the parser reads the five-token pattern shape as its own kind, typing records the initializer and judges non-Option and non-Some by name, and the co-equal emission guards on the shell's tag constant and reads .value. str.find turned out NOT to be a carrier (std bodies are refused by the 2026-08-20 decision) -- Vec.get is; the shell probe grew s.find for the day std methods travel. The Some-CONSTRUCTOR outside match ices and is its own inventory row |
| 2026-09-01 | 274/E2 | 0.05 | the pattern-disjunction wave: `A | B =>` and `A(_) | B(_, _) =>` compile -- a large slice of the 16 parse refusals (22 -> 19 files, each file peels to its NEXT form). Alternatives lie flat in the Pattern node; the head-slot door dodges the #676 variable-index trap with the binding hop; every head counts for exhaustiveness; a named binder refuses (alternatives would have to agree in name and payload type); the emitter ORs the tag tests and skips the unpack. The reverse-probe lesson is chronicled: the first probe only drove the FIRST heads, and a starved OR-chain stayed green until a value went through the second head |
| 2026-09-01 | 274/E2 | 0.05 | the requires wave: `fn half(n int) -> int requires n > 0, "msg"` compiles -- contract clauses stand on mangle.nv and channel.nv. A contextual word before the panic bucket; the condition types in the parameters' scope; the body prologue calls the shell's own nova_contract_violation (interop, gone when novac emits the prelude). The display door joins the node's leaves and matches the oracle byte-for-byte; file/line stay empty -- the named boundary, visible only on violation (carriers never violate), inventoried for a source-location channel |
| 2026-09-01 | 274/E2 | 0.05 | the hex wave: `0x0A` is one IntLit (carriers: lex.nv's B_* consts and byte table, strings.nv's BACKSLASH). The lexer's hex branch runs before the touching-ident refusal; a broken tail stays one ErrorTok; the float branch takes no dot after a hex head. #810 reads hex by its own textual bound (16 digits, leader under 8). Emission unchanged -- C reads the same spelling, proven on the oracle probe |
| 2026-09-01 | 274/E2 | 0.05 | the owner's same-day catches, two small waves: the requires message is OPTIONAL (the shell's own contracts carry none -- boundary lifted, comma optional, NULL spelling already in place) and the contextual-word check goes through the @peek_ident door (my line was the fifth copy of the form the door was built against); plus the gap dedup -- ten copies of the Err-wrap fold into @error_node(k), @missing() dissolves into @missing_of(k), and the kind stays ErrorTok on any gap outside an Err branch |
| 2026-09-01 | 274/Э2 | 0.01 | интегратор: `spec-queue` 36 → 37 по машинному замеру — лёг амендмент D86 (`X ?? return R` законен, где возврат функции НЕ Option/Result). Для novac это РАБОТА, и именно его код её вызвал: 22 обхода в трёх формах (11 ручных `match … => return`, 9 через фиктивное `?? no_ty()` со сравнением, 2 через фиктивный кортеж), все в функциях `-> bool`/`TyId`/`FnRow`/`()`. Окно 274 уточнило своим замером: в его лексере токена `??` НЕТ ВООБЩЕ, то есть волна на шаг длиннее (лексема → `??`-выражение → продукция `?? return`); срочности нет — обходы остаются валидным кодом. **Кода novac не менялось.** |
| 2026-09-02 | 274/E2 | 0.10 | the variant-value wave: bare `Dot` and `Kind.Dot` outside a call compile -- the checker writes the same CalleeVariant decision a `C(7)` call does (a local shadows a variant; a type-named head is judged before the operand walk), the emitter calls the maker the sum's declaration already emits. A payload-carrying variant as a value refuses by name. Both probes byte-for-byte; the Option family (bare `Some`/`None`) stays refused -- family sums live outside the file's defs, inventoried |
| 2026-09-02 | 274/E2 | 0.10 | the hunt lands and its sharpest class is repaid the same day: the hunter-debt guard (278 F.5) reddened the push at 1539/1500 added lines, the hunter ran the fresh-surface x K1 cell (64 probes, six classes, three contradictions -- registry #869-#878), and #869 is closed: an integer literal's text went to C verbatim, so C read `0123` as octal and a `010 =>` arm matched the other branch; one spelling door (int_literal_c_text), both emitters through it, the hunt probe green by the same smoke |
| 2026-09-02 | 274/E2 | 0.05 | #872 (hunt N4): the contract display is escaped into its C literal (its own quotes used to close the string -- clang refused a program both sides accepted), and the message slot goes through the ONE dollar door -- unescape_dollar moves from the shell's private scope to a sem export with two readers, which closes the hunt's P1 contradiction by construction; the remaining divergence on the violating probes is pure #874 (:0: vs file:line) |
| 2026-09-02 | 274/E2 | 0.05 | #870 (hunt N2): a branch is never punctuation -- an argument form outside is_expr_kind used to fall out of BOTH println walks silently (`ab` for `a20b`); the checker now refuses it by name on the argument path and the emitter holds an ice for the inexpressible remainder, closing the hunt's P2 contradiction the way it asked. Paid twice in the holder: the oracle's mono-cache collides same-TEXT indexings across functions (d[0] took rules.nv's []Node type -- reported with a repro), and the poor test decls add their own refusal, so the holder judges by message text |
| 2026-09-02 | 274/E2 | 0.10 | the contract channel closes #871 and #874, and #873 refuses honestly: the checker -- the one holder of the source -- records each clause's exact source slice and real line (Contract arm in Resolution + ContractNote pool; nine matches extended by the compiler itself), the emitter reads the decision, the file name travels from main's base_name; the leaf-join display_of dies with its only reader. Ten of eleven violating hunt probes go byte-for-byte; the eleventh exposes the ORACLE shipping an invisible 0x01 before an escaped dollar in its own panic -- reported |
| 2026-09-02 | 274/Э2 | 0.01 | интегратор: `spec-queue` 37 → 38 по машинному замеру — лёг амендмент к D84 (`10-overloading.md`, реестр №857): набор перегрузок, различающихся только режимом параметров, разрешается ДОМИНИРОВАНИЕМ, а при ничьей — отказ `[E_OVERLOAD_AMBIGUOUS_MODE]` с перечнем кандидатов (раньше оракул молча выбирал одну). Сам амендмент текст D84 НЕ меняет — он фиксирует дату исполнения и имя диагностики, то есть для novac это РАБОТА только если его диспатч уже умеет ось режимов; проверка — за окном 274. **Кода novac не менялось.** |
| 2026-09-02 | 274/E2 | 0.05 | #875 (hunt var_shadow): a binding that NAMES a visible payload-less variant refuses as refutable (D411) -- novac used to shadow the variant in silence where the oracle refuses; judged in type_bind before the initializer, a payload-carrying variant's bare name stays a binder, the legal cascade after the refusal is named and the holder judges by text |
| 2026-09-02 | 274/E2 | 0.10 | wave I1: string interpolation compiles for bare local names of type str|int -- the oracle's own builder chain read off its build cache (static_new with the oracle's cap formula, per-fragment append, per-slot display_spec, consume_into_str); slots judged by the scope, slot types travel through record_subst, the const substitution point is fenced by name; chains/calls/operators refuse toward I2. The interpolation map (209 occurrences, 60% bare names) was drawn by a sonnet agent |
| 2026-09-02 | 274/E2 | 0.05 | the self wave: bare `@` is the receiver's value -- `match @` in a sum method compiles byte-for-byte (diag.nv:37 is the real carrier, its self-reading moves to its next form); SelfExpr runs the whole pipe (parse atom, is_expr_kind, scope-typed with the outside-a-method refusal, channel-read type, RECV_C_NAME emission) |
| 2026-09-02 | 274/Э2 | 0.05 | интегратор: ПЯТЬ ПРОПУЩЕННЫХ ИМПОРТОВ в `novac/src` — `RECV_BINDING` (sem/harvest.nv), `OPTION_FAMILY_C` и `OPTION_SUM` (sem/mangle.nv), `OPTION_SUM` (check/type_of.nv), `OPTION_SOME_VARIANT` (check/typing.nv). Имена ИСПОЛЬЗОВАЛИСЬ, но не стояли в `import ../builtins.{...}` своего файла; оракул молчал, потому что пропускал ЛЮБОЕ имя с заглавной буквы (реестр №882). После сужения пропуска компилятор их назвал, и `nova build novac/src/main.nv` краснел на CI. Правка — только дописанные имена в СУЩЕСТВУЮЩИЕ строки импорта, логика не тронута; сборка novac зелёная (7.6с), `gate-novac.sh` — все содержательные проверки прошли (единственный отказ — бюджет валл-клока на занятой машине). Окно 274 предупреждено списком файлов до коммита. |
| 2026-09-03 | 274/Э2 | 0.01 | интегратор: `spec-queue` 38 → 45 по машинному замеру — СЕМЬ D-коммитов разом, из них четыре мои по D468 (место вызова, доступное коду). Счётчик отставал настолько, что внешний гейт покраснел и по правилу pre-push ОСТАНОВИЛ следующий пуш во все три зеркала — то есть строка бухгалтерии держала выкладку чужой работы (реестр №846, седьмое срабатывание класса). Тем же днём заведён механизм: правило 4 в общем pre-commit хуке ловит расхождение НА КОММИТЕ, за секунду, вместо CI через час; проба обеих сторон снята (при 47 против нужных 48 — блок с точным текстом, после правки — проход). **Кода novac не менялось.** |
| 2026-09-03 | 274/Э2 | 0.01 | интегратор: `spec-queue` 45 → 47 — легли D468 (переписан под явную форму: место вызова приезжает ОБЫЧНЫМ параметром с умолчанием `loc CallerLoc = caller_loc()`, атрибут `#track_caller` и скрытый параметр ОТОЗВАНЫ владельцем) и его уточнения (`throw` берёт параметр наравне с `panic`; `ensures` — сознательное исключение). **Для novac это работа НЕ срочная и дешевле, чем кажется:** ABI не меняется, тип функции не меняется, эмиссия функции без такого параметра прежняя — правило для подмножества Carina то же, что применено к `#realtime` (имя признаётся, поведение — своим чередом). **Кода novac не менялось.** |
| 2026-09-03 | 274/Э2 | 0.01 | интегратор: регенерация `novac/src/emit_c/shell.tpl.c` после Э1 плана 280 — дельта РОВНО восемь строк, typedef типа прелюдии `Nova_CallerLoc`. Снимок протух не от правки кода novac, а оттого, что тип прелюдии попадает в КАЖДУЮ эмиссию оракула; план 280 утверждал обратное, и гейт это опроверг (пункт переписан). Команда — `scripts/tools/novac-regen-shell.sh`, страж свежести зелёный (6082 строки, 301171 байт). **Кода novac не менялось**, но окну 274 стоит пересобрать novac у себя: снимок под ним сдвинулся. |
