# Conventions governance — изменения и отклонения ТОЛЬКО по согласованию

Этот файл управляет **всеми** конвенционными документами проекта Nova. Он нормативен и
сам подчиняется правилу ниже (правка этого файла — тоже по согласованию).

## Конвенционные документы (нормативные)

Обязательны для любого контрибьютора, включая AI-агентов:

- [dev-workflow.md](dev-workflow.md) — процесс разработки + **жёсткие операционные правила** (worktree-модель, никакого `git stash`, `git add` по именам файлов, коммит на задачу). Онбординг-вход для агентов — `AGENTS.md`; в порядок чтения добавлен в [promts/read-project.md](promts/read-project.md).
- [compiler-conventions.md](compiler-conventions.md) — разработка компилятора (front-end + codegen).
- [test-conventions.md](test-conventions.md) — написание и запуск тестов.
- [nv-coding-style.md](nv-coding-style.md) — стиль кода на Nova (`.nv`).
- [perf-conventions.md](perf-conventions.md) — производительность компилятора.
- [bench-conventions.md](bench-conventions.md) — бенчмарки.
- [lsp-conventions.md](lsp-conventions.md) — языковой сервер (наследует compiler-conventions).
- [project-philosophy.md](project-philosophy.md) — принятие дизайн-решений языка.
- `spec/decisions/` (D-блоки) — нормативная семантика языка (spec-first).

## Главное правило

> **Конвенции НОРМАТИВНЫ. Любое ИЗМЕНЕНИЕ конвенционного документа и любое ОТКЛОНЕНИЕ от
> конвенции — ТОЛЬКО по явному согласованию с владельцем проекта. Не править и не
> отклоняться самовольно.**

Конкретно:

- **Изменение текста конвенции** (правка/добавление/смягчение/ужесточение правила) — сначала
  согласовать с владельцем, потом править. Коммит правки конвенции упоминает согласование.
- **Отклонение в коде/тестах** от конвенции — только как **согласованное исключение**,
  задокументированное на месте (комментарий + маркер `[M-*]`, где уместно) со ссылкой на причину.
- **Нашёл ошибку / противоречие / устаревание** в конвенции — НЕ чинить молча. Вынести владельцу
  с конкретным предложением (что не так + варианты + рекомендация), получить решение, затем править.
- **Конвенция vs реализация инструмента** (напр. `test-conventions.md` ↔ `test_runner.rs`): перед
  правкой конвенции проверить, что она не противоречит фактическому поведению инструмента;
  расхождение — повод согласовать, какую сторону менять.
- **Спека (`spec/decisions/`)** — spec-first: поведение решается в D-блоке по согласованию, потом
  кодируется (compiler-conventions §5).

## Почему

Конвенции — единый источник правды процесса. Самовольная правка/отклонение их форкает: разные
агенты следуют разным версиям, фикстуры/код дрейфуют, теряется доверие к документу. Согласование
держит конвенции консистентными и авторитетными.

## Лог согласованных изменений конвенций

Существенные согласованные правки конвенций — здесь, с датой и сутью (для аудита).

- **2026-06-20 · test-conventions.md** — устранены два внутренних противоречия (правило↔пример),
  смягчением (согласовано с владельцем; проверена совместимость с `test_runner.rs` — классификация
  по маркеру `EXPECT_*`, группировка TU по равенству `module`-деклараций; раннер НЕ читает суффикс
  `_neg` и имя папки `neg/`):
  - `test`-блок в `EXPECT_COMPILE_ERROR`-файле **допустим** (файл не компилируется → блок не
    исполняется; раннер проверяет ошибку на этапе codegen независимо от блока).
  - Суффикс `_neg` **необязателен внутри `neg/`** (путь `neg/` + `module neg.<name>` уже сигналят);
    **рекомендуется/обязателен для neg-файлов ВНЕ `neg/`** (где иного сигнала нет).
- **2026-06-21 · compiler-conventions.md §7.5** — устранено противоречие с жёстким правилом
  «никакого `git stash`» (`dev-workflow.md` §6): §7.5 советовал `git stash` для baseline (опасно при
  общем `.git` нескольких worktree → коллизия/потеря чужих изменений). Согласовано с владельцем:
  заменено на безопасный baseline — **temp-worktree** / **commit+reset** / **patch+checkout**, со
  ссылкой на `dev-workflow.md §6`. Заодно: убрана ставшая ненужной приписка в `dev-workflow.md §6`
  («compiler-conventions ещё упоминает stash») и вычищены 3 stale `stash+rebuild`-ссылки в плане 172.1
  (+ исправлен §-номер §6→§7). Нормативного поведения тестов не меняет — приведение к уже принятому правилу.
- **2026-06-21 · discoverability `dev-workflow.md`** — устранён разрыв обнаружения: `dev-workflow.md`
  (создан 2026-06-21, держит жёсткие операционные правила) был доступен только через `AGENTS.md`, но
  **не** через собственный session-start промпт `promts/read-project.md` и **не** в нормативном списке
  выше → агент, идущий по «прочитай проект», к нему не попадал (ровно это дало жизнь stash-противоречию).
  Согласовано с владельцем: (1) добавлен в этот нормативный список; (2) добавлен «СНАЧАЛА»-callout в
  `promts/read-project.md` (шаг до nova.toml, с указателем на `AGENTS.md`).
- **2026-06-21 · compiler-conventions.md §2** — расширена область §2 со «скорость КОМПИЛЯЦИИ» на ДВЕ
  оси производительности: (1) быстрый компилятор и (2) быстрый сгенерированный код — обе приоритет
  первого класса. Согласовано с владельцем. Добавлено: примат безопасности явный («при конфликте
  скорости и звучности выигрывает звучность — инвариант»); «Почему»-пункт про рантайм как обещание
  системной ниши (эффективный C: моно вместо боксинга, прямой вызов вместо vtable при статически
  известном приёмнике); «Правило»-пункт «эффективный код наравне с эффективным компилятором»;
  «Граница» распространена на рантайм — soundness-гарантии (bounds/overflow/nil/narrowing) элидируются
  ТОЛЬКО по пруфу (`index_site_elided`/`overflow_site_elided`), никогда ради скорости; §11-резюме
  обновлено. Не ослабляет ни одного существующего правила — только добавляет рантайм-ось с тем же
  safety-инвариантом, что уже был у компиляторной оси.
- **2026-06-21 · compiler-conventions.md §6** — добавлен ведущий принцип «каждая ошибка ПОНЯТНА
  пользователю»: никаких утёкших непонятных ошибок (`CC-FAIL`, сырые codegen-строки, паники,
  `unknown type name Nova_…`) — это баг фронтенда, их место — чистая диагностика чекера (код +
  сообщение + span + как-починить). Согласовано с владельцем. Усиливает §1 (C — не первичный чекер)
  без ослабления правил; даёт нормативное основание задаче переноса «уродливых» CC-FAIL в чекер
  (напр. Plan 172.1 U.3.3-instance: несовпадение типов методов-экземпляров → чистый
  `E_NO_MATCHING_OVERLOAD` вместо CC-FAIL).
