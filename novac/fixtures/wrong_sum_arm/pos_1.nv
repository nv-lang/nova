module wrong_sum_arm.pos_1

// The arms name variants of the matched sum, a disjunction included.
type Color enum Red | Green | Blue

fn warm(c Color) -> bool {
    match c {
        Red | Green => true
        Blue => false
    }
}

fn main() {
    println(warm(Color.Red))
    println(warm(Color.Blue))
}
