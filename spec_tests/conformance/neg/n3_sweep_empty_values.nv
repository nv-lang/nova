// Plan 57.B.3: bench-sweep с пустым values list → compile error.
// EXPECT_COMPILE_ERROR empty
module neg.n3_sweep_empty_values

bench "empty-sweep" (n in []) {
    measure {
        bench.opaque(n)
    }
}

test "stub" { assert(true) }
