// Plan 84 Ф.5 NEGATIVE — относительный импорт за границу пакета.
// `../../../` из spec_tests/conformance/neg/ поднимается выше корня пакета
// `spec_tests/` (Plan 198 миграция: было `../../` из nova_tests/plan84/,
// глубина увеличилась на 1 уровень при переносе в spec_tests/conformance/neg/).
// EXPECT_COMPILE_ERROR границу пакета
module neg.neg_escape_test
import ../../../outside.{nope}

test "unreachable" {
    assert(true)
}
