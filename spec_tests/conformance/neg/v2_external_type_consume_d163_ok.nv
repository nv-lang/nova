// EXPECT_COMPILE_ERROR E_EXTERNAL_TYPE_RETRACTED
//
// D163 (`external type X consume`) fully retracted.
//
// `external type X consume` was Plan 100.5 (D163) FFI opaque consume-type mechanism.
// Now retracted: use value-record pattern instead:
//   type FileResource value { priv handle int }
//   fn FileResource consume @close() -> () { ... }
//
// Acceptance: E_EXTERNAL_TYPE_RETRACTED fires for the consume form.

module neg.v2_external_type_consume_d163_ok

external type FileResource consume

fn dummy() -> int => 42

test "D163 external type consume is now retracted" {
    assert(dummy() == 42)
}
