// Plan 115 Ф.1 NEG-T1.5b — *() order comparison banned (only Eq/Neq allowed).

// EXPECT_COMPILE_ERROR E_PTR_ARITHMETIC_BANNED

module neg.t1_ptr_lt_neg

test "*() < *() is banned" {
    ro p *() = (0 as *())
    ro q *() = (0 as *())
    ro result = p < q
    assert(!result)
}
