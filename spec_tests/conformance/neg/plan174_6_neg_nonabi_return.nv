// Plan 174.6 M1 (D282 rule 2) NEG — the RETURN type of an `extern "C" fn`
// is validated too: a `Vec[T]` return is not C-ABI-compatible.
//
// EXPECT_COMPILE_ERROR E_FFI_NON_C_ABI_TYPE

module neg.plan174_6_neg_nonabi_return
extern "C" fn c_return_vec(n int) -> Vec[int]

test "rejects non-C-ABI return over C ABI" {
    assert(1 == 1)
}
