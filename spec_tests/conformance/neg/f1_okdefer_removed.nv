// EXPECT_COMPILE_ERROR D189-removed-okdefer
//
// Plan 173 Ф.1 (#4, [M-172-errdefer-okdefer-dead-surface]): `okdefer`
// ретрактнут (D189 hard cutover). AST-вариант `Stmt::OkDefer` удалён;
// парсер отвергает форму на месте tombstone-хинтом `[D189-removed-okdefer]`.
// Замена: `consume X = e { body }` с `match outcome { Success => ... }` (D188).

module neg.f1_okdefer_removed

fn cleanup_on_ok() -> () {
    okdefer { () }                               // ❌ [D189-removed-okdefer]
}
