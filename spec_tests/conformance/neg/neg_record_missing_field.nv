// EXPECT_COMPILE_ERROR E_MISSING_FIELD_IN_LITERAL
// SPDX-License-Identifier: MIT OR Apache-2.0
//
// Реестр 221.1 №1142. Конструктор записи БЕЗ объявленного поля собирался без
// единой диагностики и печатал неинициализированную память, РАЗНУЮ на каждом
// запуске (замер трижды: 1865325339888, 2905450931440, 2752327181552).
// Спека дословно (02-types.md:152): «Construction всегда требует все
// обязательные поля».
//
// ПОЧЕМУ ЭТО БЛОКЕР СТУПЕНИ 0.2, а не просто дефект: этим компилятором
// собирают Карину. Носитель в `novac/src` отравил бы сам бутстрап, а
// недетерминированный мусор читался бы как плавающая ошибка Карины.
//
// ЧЕГО ЭТА ФИКСТУРА НЕ ПОКРЫВАЕТ (названо, а не подразумевается): литерал со
// `spread`, тип с `embed`, приватное поле вне области типа. Все три обязаны
// проверку ПЕРЕЖИТЬ, и у каждой свой положительный сосед.
module neg.neg_record_missing_field

type Point value { x int, y int }

fn main() {
    ro p = Point { x: 1 }   // nova:expect E_MISSING_FIELD_IN_LITERAL
    println("${p.x}")
}
