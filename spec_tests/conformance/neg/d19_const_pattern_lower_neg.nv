// EXPECT_COMPILE_ERROR E_MATCH_CONST_PATTERN
//
// Реестр 221.1 №1157 (тихая половина №762), решение владельца 2026-09-18.
//
// ЭТА КЛЕТКА ВАЖНЕЕ СВОЕЙ ГРОМКОЙ СОСЕДКИ, и вот чем. Имя СТРОЧНЫМИ парсер
// разбирает как `Pattern::Ident`, то есть как СВЯЗЫВАНИЕ: до запрета эта
// программа СОБИРАЛАСЬ без единой диагностики и печатала `mut=1 other=1` —
// первая ветка матчила ВСЕГДА, `_` была мертва, функция возвращала 1 на любом
// входе. Громкий отказ нельзя не заметить; этот заметить было нельзя.
//
// Автор в обоих случаях писал СРАВНЕНИЕ, а получал разное в зависимости от
// того, как назвал константу. Канон — guard: `w if w == mut_w => 1`.
module neg.d19_const_pattern_lower_neg

const mut_w = "mut"

fn classify(w str) -> int => match w {
    // nova:expect E_MATCH_CONST_PATTERN -- до запрета связывало молча и матчило всегда
    mut_w => 1
    _ => 0
}

fn main() -> () {
    ro _ = classify("mut")
}
