// EXPECT_COMPILE_ERROR E_SHARE_INVALID_KIND
//
// Plan 173.3 (D415 §1): `#share` применим только к типам с собственной
// instance-идентичностью (record / named tuple / newtype / opaque / sum).
// На protocol — нет storage, нечего ручаться → E_SHARE_INVALID_KIND.

module neg.share_invalid_kind

#share
type NotShareable protocol {
    @poke() -> int
}
