module spec_tests.conformance
test "primitive operators unaffected" {
    assert(1 < 2)
    assert(3 > 2)
    assert(2 <= 2)
    assert(2 >= 2)
    assert(1 == 1)
    assert(1 != 2)
    assert(true == true)
    assert(false != true)
}
