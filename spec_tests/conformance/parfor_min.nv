module nova_tests.err173_1

test "parallel for range int" {
    ro xs = parallel for i in 0..5 {
        i * 2
    }
    assert(xs.len() == 5)
    mut sum = 0
    for v in xs { sum += v }
    assert(sum == 20)
}
