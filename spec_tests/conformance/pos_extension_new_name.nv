// Plan 154: extension-метод с НОВЫМ именем на str — разрешён (нет orphan rule).
module spec_tests.conformance
import std.unicode

fn str @shout() -> str => @to_upper()

fn main() -> () {
    assert("abc".shout() == "ABC")
}
