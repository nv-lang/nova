//
// Plan 153.1 — негатив: @resize(n,v) с n < 0 → контракт `n >= 0` паника.
module conformance.resize_negative_neg
test "resize negative neg" panics "requires failed" {
    mut v = Vec[int].of(1, 2, 3)
    v.resize(-1, 0)
}
