// Plan 67 Ф.2 f2 — println(str.from(bool)) → "true"/"false" строкой.

module plan67.f2_static_method_str_from_bool

// EXPECT_STDOUT true
// EXPECT_STDOUT false

fn main() Io -> () {
    println(str.from(true))
    println(str.from(false))
}
