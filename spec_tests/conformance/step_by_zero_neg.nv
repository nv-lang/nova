//
// Zero-cost StepByIter: step_by(0) violates `requires n > 0` — runtime panic.

module conformance.step_by_zero_neg
import std.collections.vec_iter

test "step by zero neg" panics "requires" {
    ro v = Vec[int].of(1, 2, 3)
    ro _ = v.iter().step_by(0).collect()
}
