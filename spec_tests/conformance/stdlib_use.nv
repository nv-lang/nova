module spec_tests.conformance
// Plan 172.3 Ф.4 — use stdlib SignedInt (from prelude) WITHOUT local declaration.
// EXPECT_RUN_OK
priv(file) fn[T SignedInt] pick(x T) -> T => x
test "stdlib_use smoke" { ro _ = pick(5) }
