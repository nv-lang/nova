// Plan 67 Ф.2 f2 — println(bool.to_str()) → "true"/"false" строкой.
// Plan 174.2: мигрировано со `str.from(bool)` (ретрактирован) на `.to_str()`.

module standalone.f2_static_method_str_from_bool
// EXPECT_STDOUT true
// EXPECT_STDOUT false

fn main() Io -> () {
    println(true.to_str())
    println(false.to_str())
}
