// Plan 67 Ф.2 f7 — println(char_literal.to_str()) via the char CharLit path.
// CharLit → nova_char_to_str (bare-T `@to_str()` blanket, interp primitive
// dispatch) → nova_print_str dispatch. Plan 174.2: мигрировано со
// `str.from(char)` (ретрактирован) на `.to_str()`.

module standalone.f7_char_var
// EXPECT_STDOUT b

fn main() Io -> () {
    println('b'.to_str())
}
