// Plan 67 Ф.2 f7 — println(str.from(char literal)) via str.from CharLit path.
// CharLit → nova_char_to_str in str.from codegen → nova_print_str dispatch.

module spec_tests.conformance
// EXPECT_STDOUT b

fn main() Io -> () {
    println(str.from('b'))
}
