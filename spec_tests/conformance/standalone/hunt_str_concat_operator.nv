// Plan 159 Ф.1 over-prune hunt (ревизия 2026-07-21, E_STR_CONCAT_PLUS): оператор
// `+` для str УБРАН из языка (D46-амендмент) — исходная предпосылка hunt'а
// («`+` desugars to concat, селектор не виден синтаксически») MOOT. Сохранена
// суть DCE-охоты: явные chained `.concat()`-вызовы обязаны держать `str.concat`
// достижимым (reachability closure не должен prune'ить его).
// EXPECT_STDOUT a+b+c

module standalone.hunt_str_concat_operator
fn main() {
    ro s = "a".concat("+").concat("b").concat("+").concat("c")
    println(s)
}
