// Plan 67 Ф.2 f8 — nested conversion: println((x + y).to_str()).
// x + y returns nova_int → int.to_str() → nova_print_str dispatch.
// Plan 174.2: мигрировано со `str.from(x + y)` (ретрактирован) на `.to_str()`.

module standalone.f8_nested_str_from
// EXPECT_STDOUT 15

fn main() Io -> () {
    ro x = 7
    ro y = 8
    println((x + y).to_str())
}
