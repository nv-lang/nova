// Plan 84 Ф.5 — facade: re-export относительного модуля `./inner`.
// Аудит Ф.4: `export import ./X` — относительная резолюция при re-export.
module reexport.facade

export import ./inner.{compute}

test "facade self-check" {
    assert(compute() == 42)
}
