// Plan 84 Ф.5 POSITIVE — импорт реэкспортнутого имени через `./facade`.
module reexport.use_reexport_test

import ./facade.{compute}

test "relative ./ — re-exported name through facade" {
    assert(compute() == 42)
}
