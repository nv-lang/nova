module spec_tests.conformance
test "simple fail handler with interrupt" {
    mut caught = ""
    with Fail = effect Fail {
        fail(msg) -> never {
            caught = msg
            interrupt ()
        }
    } {
        throw "boom"
    }
    assert(caught == "boom")
}
