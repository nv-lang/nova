//
// Plan 90 followup — негатив: append_zero(n) с n < 0 → panic.
// D141 amend (2026-06-01): отрицательный n — bug, panic с diagnostic.

module spec_tests.conformance
test "append zero negative n neg" panics "requires failed" {
    mut arr []u8 = [1, 2, 3]
    arr.append_zero(-1)
}
