module fixed_array.index_rw

test "basic [N]u8 index read/write" {
    mut buf [5]u8 = [0, 1, 2, 3, 4]
    buf[2] = 42
    ro x = buf[2]
    assert(x == 42)
    assert(buf[0] == 0)
    assert(buf[4] == 4)
}
