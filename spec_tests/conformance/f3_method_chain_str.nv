// Plan 67 Ф.2 f3 — println(str method chain) через infer_expr_c_type.
// s.to_ascii_upper() returns nova_str — must dispatch to nova_print_str.

module spec_tests.conformance
// EXPECT_STDOUT HELLO

fn main() Io -> () {
    ro s = "hello"
    println(s.to_ascii_upper())
}
