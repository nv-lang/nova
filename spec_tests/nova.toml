# spec_tests — clean spec/D-block conformance suite (Plan 172 verification methodology).
#
# SEPARATE from `nova_tests/` (the legacy corpus, half-broken + duplicated) so the corpus
# can eventually be deleted. Each touched D-block gets a per-D test file inside a per-topic
# folder-module. See docs/test-conventions.md §"spec/D-conformance suite".
[package]
name = "spec_tests"
version = "0.1.0"
nova-version = "0.5"
license = "MIT OR Apache-2.0"
description = "Spec/D-block conformance tests (clean soundness signal; replaces nova_tests)"
[lib]
src = "."
[dependencies]
