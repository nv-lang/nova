module p270.f2_clone_bare_ctor
import std.io
type Colour enum Red | Green | Blue(int)
fn Colour @clone() -> Self => Colour.Green
fn main() Io -> () {
    ro c = Blue(7)
    ro d = c.clone()
    match d {
        Green => print("OVERRIDE")
        _ => print("BUILTIN-OR-OTHER")
    }
}
