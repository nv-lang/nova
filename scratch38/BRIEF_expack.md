# БРИФ p-expack (принят интегратором 2026-08-02; очередь: дорожка А после S1b)
# Источник: параллельная сессия. Исполнение — компиляторное окно, ОДНО за раз.
# П.2 = №275, П.3 = №276 (заведены при приёмке брифа). П.4 = №244 (свободен на момент приёмки).
[полный текст брифа — как передан, исполнять дословно с поправкой №№]

П.1 — №34 [M-concrete-instance-arity-overload-mangle]: инстанс-перегрузки по арности/bound-generic на конкретном типе → один C-символ. Прецеденты place-фикса: №140, «5b»-схема [M-138.2-generic-method-overload-mono] → «1b» (Plan 200 П5); инстанс-случай — ТОТ ЖЕ канал + per-overload суффикс, не новый резолвер. Репро-минимум + pos/neg фикстуры. Приёмка: polaris-проба Router @get[R IntoResponse] + хендлер Result[Json[T], HttpError] с ?. Date @at — НЕ делать, отметить. [M-static-multi-overload-chain-call-type] — одним механизмом при общем корне.
П.2 — №275: same-path multi-method регистрация, два замыкания порознь захватывают внешний mut items → краш НА РЕГИСТРАЦИИ (03-json-api:136-147; цепная форма работает). Репро вне polaris (два замыкания с захватом одной mut-локали в разные слоты) → диагноз → чекер-фикс → фикстура.
П.3 — №276: локаль ws затеняется МОДУЛЕМ polaris.ws — .recv()/.send() на локали → E7401 «no function in module» (08-websocket:27-33). Ожидание: локальный биндинг затеняет модуль в выражении (Rust/Go). Репро → фикс резолва → фикстура pos + обратная (module-path без локали жив). После: вернуть ws вместо w в примере 08.
П.4 — №244: тело effect-op не видит модульную свободную fn (репро 11-json-logs, encode 5×). Проверить незанятость → репро → диагноз (DCE/collect_used_names?/mono-канал op-body) → фикс → фикстура → снять обход в 11-json-logs.
Гейты по пунктам: cargo чистый + фикстура standalone-техникой + polaris-пробы; финал: check std канон, ratchet не вырос. Порядок = приоритет; не успел пункт — честный СТОП, следующий БЕЗ обходов.
Правила стандартные + (интегратор): фоны ЖДАТЬ СИНХРОННО; ход «жду уведомление» запрещён; ≤2 nova-процессов; осиротевшие nova.exe убивать.
