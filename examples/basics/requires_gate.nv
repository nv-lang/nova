module basics.requires_gate

fn half(n int) -> int
    requires n > 0, "half wants a positive"
{
    n / 2
}

fn third(n int) -> int
    requires n > 0
{
    n / 3
}

fn main() {
    println(half(8))
    println(third(9))
}
