// Plan 114.4 Ф.2 NEG: scope-local const requires constexpr RHS.

// EXPECT_COMPILE_ERROR E_CONST_NOT_CONSTEXPR

module neg.scope_const_non_constexpr_neg

fn make() -> int => 42

test "scope-local const with fn-call" {
    const BAD = make()
    assert(BAD == 42)
}
