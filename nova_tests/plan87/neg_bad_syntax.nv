// EXPECT_COMPILE_ERROR expected type
//
// Plan 87 Ф.4.6 — НЕГАТИВ: мусор между loop-pattern и `in`.
//
// После loop-pattern компилятор ожидает либо `in`, либо TypeRef
// (правило «name type»). Числовой литерал `123` — не тип → parser
// останавливается с понятной ошибкой «expected type».

module plan87.neg_bad_syntax


test "garbage between loop pattern and in" {
    for x 123 in [1, 2, 3] {
        ro _ = x
    }
}
