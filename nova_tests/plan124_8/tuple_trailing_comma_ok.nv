// Plan 124.8 — positive: single-line tuple + trailing comma.
module plan124_8.tuple_trailing_comma_ok

type Vec3(x f64, y f64, z f64,)

test "single-line + trailing comma" {
    ro v = Vec3(x: 1.0, y: 2.0, z: 3.0)
    assert(v.x == 1.0)
}
