// Plan 124.8 — positive: multi-line tuple + trailing comma.
module nova_tests.plan124_8

type Vec33(
    x f64,
    y f64,
    z f64,
)

test "multi-line + trailing comma" {
    ro v = Vec33(x: 1.0, y: 2.0, z: 3.0)
    assert(v.y == 2.0)
}
