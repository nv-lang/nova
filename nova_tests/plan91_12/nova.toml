# nova.toml — Plan 91.12 remaining tests (extern-fn / generic-newtype cases).
#
# Plan 182 Ф.1: the std/net "V2" test batch (net_v2_*.nv) was removed — old
# std/net was deleted and net2 was promoted/renamed to std/net (no more
# V1/V2 codegen conflict, so the net_v2_guard.h FFI shim this manifest used
# to force-include is gone too).

[package]
name = "plan91_12"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Plan 91.12 extern-fn / generic-newtype tests"

[lib]
src = "."

[dependencies]
