// SPDX-License-Identifier: MIT OR Apache-2.0
//
// Plan 110.4.5 T8.1 (D195): Application effect basic operations.
// Verifies that declarations type-check; runtime integration с
// 3-level resolution lands в Plan 110.4.6.

module plan110.app_effect_basic_t8_1

// Type-check verification: Application effect operations declared.
// Бойлер consumer fn that references both register_finalizer +
// default_exit_timeout_ms.
fn app_basic_consumer() Application -> int {
    Application.register_finalizer(|| ())
    Application.default_exit_timeout_ms()
}

test "Plan 110.4.5 T8.1: Application effect basic declarations" {
    // Compile + link verification only. Runtime handler binding
    // (with Application = handler { ... }) requires ApplicationHandler
    // type that lands в Plan 110.4.6 codegen integration.
    assert(true)
}
