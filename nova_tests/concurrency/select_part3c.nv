module nova_tests.concurrency.select_part3c

test "match with None empty body" {
    let ch = Channel.new(1)
    ch.tx.send(77)
    let opt = ch.rx.recv()
    let mut got = 0
    match opt {
        Some(v) => { got = v }
        None    => {}
    }
    assert(got == 77)
}
