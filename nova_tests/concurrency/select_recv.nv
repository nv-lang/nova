module nova_tests.concurrency.select_recv

test "select single recv" {
    let ch = Channel.new(1)
    let tx = ch.tx
    let rx = ch.rx
    tx.send(42)
    let mut got = 0
    select {
        Some(v) = rx => { got = v }
    }
    assert(got == 42)
}
