module nova_tests.concurrency.select_part3b

test "select send arm minimal" {
    let ch = Channel.new(1)
    let tx = ch.tx
    let mut sent = 0
    select {
        tx.send(77) => { sent = 1 }
        _           => { sent = -1 }
    }
    assert(sent == 1)
}
