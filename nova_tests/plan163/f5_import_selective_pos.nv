// Plan 163 Ф.2 (D282): positive — `import m.{name}` is allowed (selective import).

module plan163.f5_import_selective_pos

import nova_tests.plan163.plan163_lib.{greet}

test "selective import compiles and bare name is callable" {
    assert(greet() == 42)
}
