// EXPECT_RUNTIME_PANIC out of bounds
//
// Plan 139.2 Ф.1 — негатив: @byte_at с отрицательным index панику́ет.
//
// Guard в @byte_at — `i < 0 || i >= @len()`. Отрицательный индекс ловится
// первой дизъюнкцией (как было в C nova_str_byte_at: `i < 0 || (size_t)i
// >= s.len`). Escape до process-panic через main-flow.

module plan139_2.byte_at_oob_neg

fn main() -> () {
    ro s = "abc"
    ro _ = s.as_bytes()[-1]         // ← OOB: negative index
}
