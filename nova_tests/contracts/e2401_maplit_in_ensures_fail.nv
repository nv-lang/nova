// Ф.9.1 (Plan 33.6): map literal в ensures → E2401.
// EXPECT_COMPILE_ERROR E2401

module contracts.e2401_maplit_in_ensures_fail

fn build_map(x int) -> int
    ensures [x: x + 1] == [x: x + 1]
{
    x
}
