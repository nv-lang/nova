// Plan 33.8 Ф.3.1/Ф.3.3: `assume` вне `#trusted`-функции вводит
// непроверяемое допущение → lint-warning `trust-introduced`.
// EXPECT_COMPILE_WARNING trust-introduced

module contracts.assume_trust_introduced_warn

fn risky(x int) -> int {
    assume x >= 0
    x + 1
}
