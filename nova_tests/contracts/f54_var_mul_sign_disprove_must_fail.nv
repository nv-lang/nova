// EXPECT_COMPILE_ERROR error
//
// Ф.54 (test pack): негативный guard для Ф.33.2/Ф.34.2 var-mul signs.
// `requires a >= 0; requires b >= 0; ensures (a*b) < 0` — невозможно
// (positive × positive = non-negative). Если TrivialBackend ошибочно
// доказывает — bug.

module contracts.f54_var_mul_sign_disprove_must_fail

#verify
fn pos_pos_cant_be_negative(a int, b int) -> int
    requires a >= 0
    requires b >= 0
    ensures result < 0
{
    a * b
}
