// EXPECT_COMPILE_ERROR `old(...)` is not available in `requires`
//
// Plan 33.1 Ф.2 (D24): `old(...)` доступен только в `ensures` и
// post-state позициях `invariant` (в 33.2).

module contracts.typecheck_old_in_requires

fn bad(x int) -> int
    requires old(x) > 0
=> x + 1
