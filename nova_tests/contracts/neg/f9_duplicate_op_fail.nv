// EXPECT_COMPILE_ERROR effect `Db`: duplicate operation `balance`
//
// Plan 33.3 Ф.9 (refactor): unique-name check — два operation с одним
// именем внутри effect → compile error.

module neg.f9_duplicate_op_fail

type Db effect {
    fn balance(id int) -> int
    fn balance(id int) -> int
}
