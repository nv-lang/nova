// EXPECT_COMPILE_ERROR effect `Db`: duplicate operation `balance`
//
// Plan 33.3 (refactor): два operation с одинаковой полной сигнатурой
// (name + param types) → error. Перегрузка по типу разрешена,
// полный дубликат — нет.

module neg.f9_effect_same_sig_fail

type Db effect {
    fn balance(id int) -> int
    fn balance(id int) -> int
}
