// EXPECT_COMPILE_WARNING W2403
//
// Plan 33.9 Ф.6.4: `#fuel(0)` explicit — эквивалентно отсутствию → W2403 noise.

module nova_tests.contracts

#opaque #pure #fuel(0)
fn helper3(x int) -> int
=> x + 1
