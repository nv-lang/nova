// EXPECT_COMPILE_WARNING W2403
//
// Plan 33.9 Ф.6.2: `#opaque` fn никогда не reveal'ится → W2403 (dead opaque).

module nova_tests.contracts

#opaque #pure
fn never_revealed(x int) -> int
=> x * 2

// Caller просто использует fn без reveal — opaque body не доступен SMT.
// Но никто не делает reveal — opaque бесполезно.
#verify
fn use_it2(n int) -> int
    requires n >= 0
    ensures result >= 0
{
    n
}
