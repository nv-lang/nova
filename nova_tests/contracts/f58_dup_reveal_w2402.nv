// EXPECT_COMPILE_WARNING W2402
//
// Plan 33.9 Ф.6.3: duplicate `reveal X; reveal X;` → W2402.

module nova_tests.contracts

#opaque #pure
fn helper2(x int) -> int
=> x + 1

#verify
fn caller6(n int) -> int
    requires n >= 0
    ensures result >= 0
{
    reveal helper2
    reveal helper2   // дубликат
    n
}
