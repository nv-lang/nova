// Ф.7.4 positive (Plan 33.6): модуль без axioms — W2402 не эмитится.
// check_axiom_consistency пропускает effects без axiom block.

module nova_tests.contracts

type Logger1 effect {
    log(msg str)
}

fn simple(x int) -> int
    ensures result == x
{
    x
}

test "no axioms → no W2402" {
    assert(simple(42) == 42)
}
