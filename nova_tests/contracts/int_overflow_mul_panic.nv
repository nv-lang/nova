// Plan 33.8 Ф.1.5: переполнение знакового `int` при умножении → panic.
// EXPECT_RUNTIME_PANIC integer overflow

// SOUNDNESS_REGRESSION — Plan 33.8 Ф.7: стережёт закрытую soundness-дыру.
module contracts.int_overflow_mul_panic

fn double(x int) -> int => x * 2

test "int * overflow panics" {
    let _ = double(int.MAX)
}
