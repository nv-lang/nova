// Ф.25.2 negative (Plan 33.6): `ensures len > 0` не доказуем без bounds.
// Trivial знает только `len >= 0` (non-negative), upper bound — нет.
// REQUIRES_SMT_BACKEND z3
// EXPECT_COMPILE_ERROR contract violation in `data_len_positive`

module contracts.trivial_string_len_fail

type Wrapper {
    data str,
}

#verify
fn data_len_positive(w Wrapper) -> int
    ensures result > 0
{
    w.data.len()
}
