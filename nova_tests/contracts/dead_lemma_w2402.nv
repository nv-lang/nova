// Ф.17.3 (Plan 33.6): dead lemma tracking — lemma определена но не применена → W2402.
// EXPECT_COMPILE_WARNING W2402

module nova_tests.contracts

lemma unused_lemma(x int)
    ensures result == x
=> x

#verify
fn just_use_x(x int) -> int
    ensures result == x
{
    x
}
