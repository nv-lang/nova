// Ф.26.3 (Plan 33.6): axiom без binders + `=> true` → W2402 vacuous.
// EXPECT_COMPILE_WARNING W2402

module nova_tests.contracts

type Trivial effect {
    #pure check() -> bool
    axiom trivial_truth() => true
}

fn use_it1() -> bool
{
    true
}

test "axiom no binders" {
    assert(use_it1() == true)
}
