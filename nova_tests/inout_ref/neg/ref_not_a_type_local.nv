// EXPECT_COMPILE_ERROR E_REF_NOT_A_TYPE
//
// D326 R1: `ref` — режим передачи параметра, НЕ тип (ref-локал запрещён).
module neg.ref_not_a_type_local

test "ref type in local" {
    mut y = 5
    ro x ref int = y
    assert(true)
}
