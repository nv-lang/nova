// EXPECT_COMPILE_ERROR E_REF_ARG_NOT_ADDRESSABLE
//
// D326 R4: `ref` требует адресуемое место, не rvalue (результат вызова).
module neg.arg_not_addressable

fn inc(mut ref a int) {
    a = a + 1
}

fn src() -> int => 5

test "ref of call result" {
    inc(ref src())
    assert(true)
}
