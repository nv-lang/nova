// EXPECT_COMPILE_ERROR E_REF_MARKER_NOT_ALLOWED
//
// D326 R4: маркер `ref` допустим только для `mut ref`-параметра.
module neg.marker_not_allowed_byval

fn h(a int) -> int => a + 1

test "ref on by-value param" {
    mut x = 0
    ro _ = h(ref x)
    assert(true)
}
