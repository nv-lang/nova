// EXPECT_COMPILE_ERROR E_REF_MODE_REQUIRES_RO_OR_MUT
//
// D326 R2: `ref` в позиции параметра требует `ro`/`mut` перед собой.
module neg.ref_bare_no_ro_mut

fn bad(ref a int) {
    a = a + 1
}

test "bare ref param" {
    mut x = 0
    bad(ref x)
    assert(true)
}
