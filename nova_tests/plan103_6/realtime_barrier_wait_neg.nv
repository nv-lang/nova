// Plan 103.6 — Barrier.wait is #parks; forbidden in realtime{}.
//
// EXPECT_COMPILE_ERROR E_REALTIME_SYNC_PARK

module nova_tests.plan103_6.realtime_barrier_wait_neg

test "Barrier.wait banned in realtime block" {
    let b = Barrier.new(1)
    realtime {
        let _ = b.wait()
    }
}
