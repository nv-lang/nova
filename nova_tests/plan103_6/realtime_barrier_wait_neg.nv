// Plan 103.6 — Barrier.wait is #parks; forbidden in #realtime fn.
// Plan 113 migration: realtime { } → #realtime fn.
//
// EXPECT_COMPILE_ERROR E_REALTIME_SYNC_PARK

module plan103_6.realtime_barrier_wait_neg

#realtime fn _rt_check_barrier_wait(b mut Barrier) {
    ro _ = b.wait()
}

test "Barrier.wait banned in #realtime fn" {
    mut b = Barrier.new(1)
    _rt_check_barrier_wait(b)
}
