// Plan 103.6 — Once.call_once is #parks; forbidden in realtime{}.
//
// EXPECT_COMPILE_ERROR E_REALTIME_SYNC_PARK

module nova_tests.plan103_6.realtime_once_call_once_neg

test "Once.call_once banned in realtime block" {
    let o = Once.new()
    realtime {
        o.call_once() { () }
    }
}
