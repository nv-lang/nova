// Plan 103.6 — Condvar.wait is #parks; forbidden in #blocking fn.
// Plan 113 migration: blocking { } → #blocking fn.
//
// EXPECT_COMPILE_ERROR E_BLOCKING_SYNC_PARK

module plan103_6.blocking_condvar_wait_neg

#blocking fn _blk_check_condvar_wait(cv mut Condvar, m mut Mutex) {
    m.lock()
    cv.wait(m)
    m.unlock()
}

test "Condvar.wait banned in #blocking fn" {
    // compile-only test; triggers E_BLOCKING_SYNC_PARK in fn body
}
