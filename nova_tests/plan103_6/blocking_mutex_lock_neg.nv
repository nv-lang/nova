// Plan 103.6 — Mutex.lock is #parks; forbidden in #blocking fn.
// Plan 113 migration: blocking { } → #blocking fn.
//
// EXPECT_COMPILE_ERROR E_BLOCKING_SYNC_PARK

module nova_tests.plan103_6.blocking_mutex_lock_neg

#blocking fn _blk_check_mutex_lock(m mut Mutex) {
    m.lock()
    m.unlock()
}

test "Mutex.lock banned in #blocking fn" {
    // compile-only test; triggers E_BLOCKING_SYNC_PARK in fn body
}
