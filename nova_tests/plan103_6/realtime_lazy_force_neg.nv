// Plan 103.6 — Lazy.force is #parks; forbidden in realtime{}.
//
// EXPECT_COMPILE_ERROR E_REALTIME_SYNC_PARK

module nova_tests.plan103_6.realtime_lazy_force_neg

test "Lazy.force banned in realtime block" {
    let lz = Lazy[int].new() { 99 }
    realtime {
        let _ = lz.force()
    }
}
