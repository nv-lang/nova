// EXPECT_COMPILE_ERROR E_PROTO_IMPL_MUT_FOR_RO
// Plan 108.4 Ф.2: protocol declares `@hash()` (ro), impl uses mut receiver.

#no_prelude
module plan108_4.neg_mut_for_ro

type Hashable protocol {
    @hash() -> u64
}

#impl(Hashable)
type MyId { id u64 }

// mut receiver — mismatch: protocol requires ro
fn MyId mut @hash() -> u64 => @id
