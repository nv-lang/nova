// EXPECT_COMPILE_ERROR E_PROTO_IMPL_RO_FOR_MUT
// Plan 108.4 Ф.2: protocol declares `mut @next()`, impl uses ro receiver.

#no_prelude
module plan108_4.neg_ro_for_mut

type Iterable protocol {
    mut @next() -> int
}

#impl(Iterable)
type Counter { val int }

// ro receiver — mismatch: protocol requires mut
fn Counter @next() -> int => @val
