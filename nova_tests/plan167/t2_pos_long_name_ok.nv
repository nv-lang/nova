// Plan 167 Ф.4 POS T2 — multi-char type name is accepted.
//
// type MyNode { x int } — name length 6 → no E_TYPE_NAME_TOO_SHORT.

module nova_tests.plan167

type MyNode {
    x int
}

test "long record name — OK" {
    ro n = MyNode { x: 7 }
    assert(n.x == 7)
}
