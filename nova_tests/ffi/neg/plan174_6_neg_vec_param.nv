// Plan 174.6 M1 (D282 rule 2) NEG — `Vec[T]` (GC type) as an `extern "C" fn`
// parameter is not C-ABI-compatible.
//
// EXPECT_COMPILE_ERROR E_FFI_NON_C_ABI_TYPE

module neg.plan174_6_neg_vec_param

extern "C" fn c_take_vec(v Vec[int]) -> int

test "rejects Vec param over C ABI" {
    assert(1 == 1)
}
