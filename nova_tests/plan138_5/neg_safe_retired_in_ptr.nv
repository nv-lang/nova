// Plan 138.5 NEG — `safe` retired even as the (former) propagation-stopper
// inside a pointer (`* safe T`).
//
// With prefix `unsafe *` forbidden there is nothing to stop; `safe` in any
// type position is a hard error.
//
// EXPECT_COMPILE_ERROR E_SAFE_RETIRED

module plan138_5.neg_safe_retired_in_ptr

external fn p(x * safe int) -> ()
