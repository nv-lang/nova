// Plan 108.3 (D36 amend): `for x in iter` (default) — x immutable, read OK.

module nova_tests.plan108_3.for_default_readonly_ok

test "for default loop-var — read OK" {
    let arr = [1, 2, 3]
    let mut sum = 0
    for x in arr {
        sum = sum + x
    }
    assert(sum == 6)
}
