// Plan 108.3 (D36 amend): `let mut [...]` — group mut на array pattern запрещён.

// EXPECT_COMPILE_ERROR E_PATTERN_GROUP_MUT

module nova_tests.plan108_3.group_mut_array_pattern_neg

test "let mut [a, b] — group mut → error" {
    let mut [a, b] = [1, 2]
}
