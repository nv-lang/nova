// Plan 114.4.1 Ф.1 parse smoke.

module nova_tests.plan114_4_1._parse_smoke

type Config {
    const VERSION int = 2
    const MAX_PEERS int = 1024
    name str
    timeout int
}

test "parses without error" {
    assert(true)
}
