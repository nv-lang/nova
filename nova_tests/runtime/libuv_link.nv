// libuv_link.nv — Plan 22 Ф.1 + Ф.2 smoke-test.
//
// Validates:
//   - Ф.1: libuv vendored submodule собирается, libuv.lib линкуется
//     в test exe без link errors.
//   - Ф.2: codegen emit_main вызывает nova_evloop_init() в prelude
//     и nova_evloop_close() в эпилоге — без crash'а / abort'а.
//     PASS этого теста подтверждает что lifecycle отрабатывает чисто.
//
// Этот файл должен PASS:
//   - с NOVA_USE_LIBUV=0 (libuv stubs) → init/close = no-op
//   - с NOVA_USE_LIBUV=1 либо libuv.lib present (auto) → uv_default_loop
//     init + uv_loop_close в эпилоге.
// В обоих случаях output идентичен.

module runtime.libuv_link

// EXPECT_STDOUT libuv link smoke ok

fn main() Io -> () {
    println("libuv link smoke ok")
}
