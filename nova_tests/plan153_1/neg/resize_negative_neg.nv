// EXPECT_RUNTIME_PANIC requires failed
//
// Plan 153.1 — негатив: @resize(n,v) с n < 0 → контракт `n >= 0` паника.
module neg.resize_negative_neg
fn main() -> () {
    mut v = Vec[int].of(1, 2, 3)
    v.resize(-1, 0)
}
