// Plan 169 Ф.6: verify fast-path timing — simple compute, no IO.
// This test should have low run_ms (< 500ms) relative to compile_ms.
module nova_tests.plan169

test "fast compute — sum 0..1000" {
    mut sum = 0
    for i in 0..1_000 {
        sum = sum + i
    }
    assert(sum == 499_500)
}
