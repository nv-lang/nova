// EXPECT_COMPILE_ERROR E_DEFER_OUTCOME_ARITY
//
// Plan 173 Ф.2.B1 (D314): `defer(o ScopeOutcome) { … }` принимает РОВНО один
// outcome-биндинг. Лишние токены после типа → `[E_DEFER_OUTCOME_ARITY]`.

module neg.f2_defer_outcome_arity

fn bad_defer_arity() -> () {
    defer(o ScopeOutcome extra) { () }           // ❌ [E_DEFER_OUTCOME_ARITY]
}
