// EXPECT_COMPILE_ERROR nova_runtime_reset
//
// Plan 173 Ф.5 п.6: `nova_runtime_reset()` — runtime-примитив сброса
// thread-local error/handler-состояния между panic-тестами (инфра Ф.6
// panics-клаузулы; вызывается ТОЛЬКО codegen'ом test-runner'а между
// тест-фреймами). Из user-кода НЕ доступен: идентификатора нет в
// Nova-неймспейсе (нет decl в std) — ссылка = ошибка компиляции.

module neg.f5_runtime_reset_unavailable

fn try_reset() -> () {
    nova_runtime_reset()
}
