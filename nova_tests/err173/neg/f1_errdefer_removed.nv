// EXPECT_COMPILE_ERROR D189-removed-errdefer
//
// Plan 173 Ф.1 (#4, [M-172-errdefer-okdefer-dead-surface]): `errdefer`
// ретрактнут (D189 hard cutover). AST-вариант `Stmt::ErrDefer` удалён;
// парсер отвергает форму на месте tombstone-хинтом `[D189-removed-errdefer]`.
// Замена: `defer` (D162 all-path cover) или `consume X = e { body }` (D188).

module neg.f1_errdefer_removed

fn cleanup_on_err() -> () {
    errdefer { () }                              // ❌ [D189-removed-errdefer]
}
