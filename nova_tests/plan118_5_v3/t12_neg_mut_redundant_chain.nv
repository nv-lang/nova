// Plan 118.5 V3 Ф.4 NEG T12 — `mut * mut T` redundant inner mut.
//
// Per D216 V3 §V3.4: outer `mut` already propagates через chain.
//
// EXPECT_COMPILE_ERROR E_REDUNDANT_TYPE_MODIFIER

module plan118_5_v3.t12_neg_mut_redundant_chain

type Acc {
    id i64
}

// Use return type to force type-level Mut(...) shape (param post-name
// `mut` would be eaten as binding modifier per D6 legacy form).
external fn t12_p() -> mut * mut Acc
