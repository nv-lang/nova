// Plan 118.5 V3 T11 → RECODED by Plan 138.5 (2026-06-11).
//
// The §V3.4 redundancy check (`ro * ro` inner ro redundant) is retired —
// the leading prefix `ro` before `*` now fails first with
// E_POINTER_PREFIX_MODIFIER. Migrate → `*ro Acc` (single postfix pointee).
//
// EXPECT_COMPILE_ERROR E_POINTER_PREFIX_MODIFIER

module plan118_5_v3.t11_neg_ro_redundant_chain

type Acc {
    id i64
}

external fn p(x ro * ro Acc) -> ()
