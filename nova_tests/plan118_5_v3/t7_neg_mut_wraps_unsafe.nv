// Plan 118.5 V3 T7 → FLIPPED by Plan 138.5 (2026-06-11).
//
// `mut unsafe T` was E_MODIFIER_ORDER under the retired §V3.2 model; the
// order check is retired (axes commute on a value-T), so it now COMPILES.

module plan118_5_v3.t7_neg_mut_wraps_unsafe

extern "nova" fn t7_p() -> mut unsafe int

test "mut unsafe T value-T order now accepted (§V3.2 retired)" {
    assert(1 == 1)
}
