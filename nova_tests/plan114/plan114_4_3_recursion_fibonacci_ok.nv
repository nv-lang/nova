// Plan 114.4.3 T4.2 positive: Fibonacci с memoization (без memo eval
// был бы O(2^n) — fib(20) timeout. With V1 memo: O(n) — instant).

module nova_tests.plan114

fn fib(const n int) -> const int =>
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }

test "fibonacci with memoization" {
    const A_plan11443recursionfibonacciok = fib(10)
    const B_plan11443recursionfibonacciok = fib(20)
    assert(A_plan11443recursionfibonacciok == 55)
    assert(B_plan11443recursionfibonacciok == 6765)
}
