# nova.toml — Plan 91.15 remaining tests (Debug-impl positives).
#
# Plan 182 Ф.1: the std/net-consumer tests (net_*.nv) were migrated to
# std/net/*_test.nv + std/net/neg/ (net2 was promoted/renamed to std/net; no
# more V1/V2 codegen conflict, so the net_v2_guard.h FFI shim this manifest
# used to force-include is gone too).

[package]
name = "plan91_15"
version = "0.1.0"
license = "MIT OR Apache-2.0"
description = "Plan 91.15 Debug-impl positive tests"

[lib]
src = "."

[dependencies]
