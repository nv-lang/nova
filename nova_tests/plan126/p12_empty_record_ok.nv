// Plan 126 вЂ” POS F12:
// Single-field record + `#impl(Equal)` вЂ” minimal field-count synth.
// AC-126.2 minimal case.
module plan126.p12_empty_record_ok

#impl(Equal)
type Marker {
    id int
}

test "single-field record type-checks" {
    ro m = Marker { id: 1 }
    assert(m.id == 1)
}
