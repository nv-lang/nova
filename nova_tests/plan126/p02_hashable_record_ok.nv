// Plan 126 вЂ” POS F02:
// `#impl(Hash)` РЅР° record в†’ auto-derive @hash().
// AC-126.3.
module nova_tests.plan126

#impl(Hash)
type UserId {
    value int
}

test "UserId type-checks with auto-derived @hash" {
    ro u = UserId { value: 42 }
    assert(u.value == 42)
}
