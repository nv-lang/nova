// Plan 126 вЂ” POS F13:
// NamedTuple (Plan 120 D215) auto-derive Equal.
// AC-126.12.
module nova_tests.plan126

#impl(Equal)
type Pair(left int, right int)

test "named tuple auto-derives Equal" {
    ro p = Pair(1, 2)
    assert(p.left == 1)
}
