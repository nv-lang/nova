// EXPECT_COMPILE_ERROR E_AUTO_DERIVE_FIELD_LACKS_PROTOCOL
//
// Plan 126 (D230) вЂ” NEG F18:
// Clone auto-derive вЂ” field type РЅРµ Clone в†’ error.
// AC-126.9.
module plan126.p18_cloneable_field_lacks_neg

type Plain {
    v int
}

#impl(Clone)
type Wrapper {
    plain Plain
}

test "Clone field lacks protocol fails" {
    ro w = Wrapper { plain: Plain { v: 0 } }
    assert(w.plain.v == 0)
}
