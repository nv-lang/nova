// Plan 126 вЂ” POS F04:
// `#impl(Compare)` РЅР° record в†’ auto-derive @compare() (lexicographic).
// AC-126.5.
module nova_tests.plan126

#impl(Compare)
type Money {
    cents int
}

test "Money type-checks with auto-derived @compare" {
    ro m = Money { cents: 100 }
    assert(m.cents == 100)
}
