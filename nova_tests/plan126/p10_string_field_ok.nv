// Plan 126 вЂ” POS F10:
// `str` field eligible РґР»СЏ auto-derive Equal (primitive).
// AC-126.2 вЂ” primitive str.
module plan126.p10_string_field_ok

#impl(Equal)
type Person {
    name str
    age int
}

test "Person with str field type-checks" {
    ro p = Person { name: "Alice", age: 30 }
    assert(p.age == 30)
}
