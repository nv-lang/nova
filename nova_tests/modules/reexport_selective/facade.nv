// Plan 42.17 Ф.6: facade re-export'ит ОБА item'а (alpha + beta) из
// internal/. Consumer, выбирающий только `alpha`, не должен видеть
// `beta` — re-export проходит через селективный фильтр consumer'а.

module reexport_selective.facade

export import nova_tests.modules.reexport_selective.internal.impl.{alpha, beta}
