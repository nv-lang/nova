// Plan 42.09: facade peer — re-export'ит internal impl.
// External users делают import nova_tests.modules.reexport_facade,
// видят parse() = parse_internal через re-export.

module reexport_facade.public

// Re-export: items из internal.impl становятся видны через эту facade.
export import nova_tests.modules.reexport_facade.internal.impl.{parse_internal}

export fn ping() -> int =>
    7
