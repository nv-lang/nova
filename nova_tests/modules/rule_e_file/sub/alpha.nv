// Plan 42.17 Ф.7: peer nested folder-module `rule_e_file.sub`.
// `rule_e_file/` direct `.nv` не содержит — только `sub/` — поэтому
// `rule_e_file.nv` (single-file) и `rule_e_file/sub/` валидны вместе.

module sub.alpha

export fn from_sub_a() -> int =>
    20
