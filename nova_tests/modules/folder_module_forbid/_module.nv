// Plan 42.10: `_module.nv` — special module-config peer.
// `#forbid` attrs здесь наследуются всеми peers folder-module
// (Net forbid применяется к core.nv и helper.nv).
// Plan 42.16: attrs ПЕРЕД module.

#forbid Net
module folder_module_forbid._module
