// Plan 42.10: peer без своих #forbid. Наследует Net forbid от _module.nv.
// Можно использовать чистые операции (без Net effect).

module folder_module_forbid.core

export fn compute(x int) -> int =>
    x * 2 + 1
