// Plan 42.15: positive test peer A. Объявляет shared declaration
// `alpha_fn`, использует `beta_fn` из peer B (Rule C: peers share
// declarations namespace — это OK).

module modules.peer_isolation_ok

export fn alpha_fn() -> int =>
    beta_fn() + 1
