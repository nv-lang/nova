// EXPECT_COMPILE_ERROR E7320
//
// Plan 79 Ф.3: a call to a method that does not exist on the type.

module nova_tests.plan79.member_no_method

type Counter { n int }

fn Counter @value() -> int => @n

fn run() -> int {
    ro c Counter = { n: 5 }
    c.increment()
}
