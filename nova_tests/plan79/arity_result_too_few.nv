// EXPECT_COMPILE_ERROR E7310
//
// Plan 79 Ф.2: type-arity check. `Result` requires exactly 2 type
// arguments (T, E). `Result[int]` with a single argument used to compile
// silently — a latent miscompilation. Now → E7310.

module plan79.arity_result_too_few

fn classify(n int) -> Result[int] => Ok(n)
