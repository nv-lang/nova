// EXPECT_COMPILE_ERROR E7301
//
// Plan 79 Ф.1: a float literal is NOT assignable to an integer type
// (unlike an integer literal, which is numeric-polymorphic per D44).

module nova_tests.plan79.assign_let_float_to_int

fn run() -> int {
    let n int = 1.5
    n
}
