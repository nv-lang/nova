// Plan 96 Ф.5 D-iter-snap — позитив: for x in arr[a..b] работает.
// View это []T → for-in использует view->len как snapshot (Go-style).

module plan96.slice_iter_for

test "for-in over slice view" {
    mut arr = [10, 20, 30, 40, 50]
    ro view = arr[1..4]
    mut sum = 0
    for x in view {
        sum = sum + x
    }
    assert(sum == 90)
}

test "for-in over open-ended slice" {
    mut arr = [1, 2, 3, 4, 5]
    ro view = arr[2..]
    mut count = 0
    for _ in view {
        count = count + 1
    }
    assert(count == 3)
}
