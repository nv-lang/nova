// EXPECT_RUNTIME_PANIC array: slice
//
// Plan 96 Ф.4 — негатив: arr[0..100] где len==3 → panic.
// D-neg-panic: OOB → детерминированный panic (D13), не clamp.

module nova_tests.plan96.neg_slice_oob_to

fn main() -> () {
    let mut arr = [1, 2, 3]
    let _ = arr[0..100]
}
