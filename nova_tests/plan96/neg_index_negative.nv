// EXPECT_RUNTIME_PANIC array: index -1 out of bounds for length 3
//
// Plan 96 Ф.1 — негатив: arr[-1] → panic, НЕ wrap-around.
// Nova не следует Python-семантике negative-index wrap.

module plan96.neg_index_negative

fn main() -> () {
    mut arr = [10, 20, 30]
    ro _ = arr[-1]
}
