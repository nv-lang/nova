// EXPECT_RUNTIME_PANIC array: slice
//
// Plan 96 Ф.4 D-str-slice — негатив: s[a..b] с OOB → panic
// (НЕ clamp, в отличие от старого s.slice(a, b) метода).

module nova_tests.plan96.neg_str_slice_oob

fn main() -> () {
    let s = "abc"
    let _ = s[0..100]
}
