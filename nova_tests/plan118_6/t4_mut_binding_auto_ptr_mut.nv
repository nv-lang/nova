// Plan 118.6 — mut binding gives *mut T automatically; write through pointer.
// EXPECT_PASS

module plan118_6.t4_mut_binding_auto_ptr_mut

test "mut binding gives mut pointer" {
    mut y = 42
    ro p = &y
    unsafe {
        p.write(100)
        assert(p.read() == 100)
    }
}
