// Plan 118.6 — unsafe { &x } gives raw stack pointer (for FFI).
// EXPECT_PASS

module nova_tests.plan118_6

test "unsafe addr_of gives raw pointer" {
    ro x = 99
    unsafe {
        ro p = &x
        assert(p.read() == 99)
    }
}
