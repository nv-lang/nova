// Plan 108.2 (D36 enforcement): `let` без `mut` → push даёт E_LOCAL_NOT_MUT.

// EXPECT_COMPILE_ERROR E_LOCAL_NOT_MUT

module nova_tests.plan108_2.let_nomut_array_push_neg

test "let no-mut + push → error" {
    let mut arr = [1]
    arr.push(2)
}
