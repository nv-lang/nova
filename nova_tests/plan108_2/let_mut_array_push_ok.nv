// Plan 108.2 (D36 enforcement): `let mut` разрешает mut-методы на arrays.

module plan108_2.let_mut_array_push_ok

test "let mut + push" {
    mut arr = [1, 2]
    arr.push(3)
    arr.push(4)
    assert(arr.len() == 4)
}
