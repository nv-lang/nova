// Plan 108.2 (D36 enforcement): `let` без `mut` → array.truncate даёт E_LOCAL_NOT_MUT.

// EXPECT_COMPILE_ERROR E_LOCAL_NOT_MUT

module nova_tests.plan108_2.let_nomut_truncate_neg

test "let no-mut + truncate → error" {
    let mut arr = [1, 2, 3, 4, 5]
    arr.truncate(2)
}
