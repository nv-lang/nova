// Plan 108.2 (D36 enforcement): `let` без `mut` → array.pop даёт E_LOCAL_NOT_MUT.

// EXPECT_COMPILE_ERROR E_LOCAL_NOT_MUT

module nova_tests.plan108_2.let_nomut_pop_neg

test "let no-mut + pop → error" {
    let mut arr = [1, 2, 3]
    let _ = arr.pop()
}
