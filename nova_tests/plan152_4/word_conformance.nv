// AUTO-GENERATED by `nova-codegen unicode --emit-conformance`. DO NOT EDIT.
// UAX #29 word conformance from WordBreakTest.txt: assert `as_words()`
// yields the expected segment sequence (content-checked).
// Coverage: uniform spread = 1500 of 1826 test lines.
module nova_tests.plan152_4

import std.unicode.{WordsView}

test "UAX#29 WordBreakTest (chunk 0, 250 cases)" {
    {
        mut w = "\u{1}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{a}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{b}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3031}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3031}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
}

test "UAX#29 WordBreakTest (chunk 1, 250 cases)" {
    {
        mut w = "\u{41}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{3a}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2c}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{2e}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{2e}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
}

test "UAX#29 WordBreakTest (chunk 2, 250 cases)" {
    {
        mut w = "\u{5f}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5f}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5f}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{22}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{22}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{27}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
}

test "UAX#29 WordBreakTest (chunk 3, 250 cases)" {
    {
        mut w = "\u{231a}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{231a}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{231a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{ad}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{ad}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{300}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{300}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2060}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
}

test "UAX#29 WordBreakTest (chunk 4, 250 cases)" {
    {
        mut w = "\u{61}\u{2060}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{3a}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{308}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{308}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{308}\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{308}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{27}\u{2060}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{27}\u{2060}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
}

test "UAX#29 WordBreakTest (chunk 5, 250 cases)" {
    {
        mut w = "\u{31}\u{27}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{27}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{27}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2c}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{b}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{b}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{3031}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3031}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{2e}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{308}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{1f1e6}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{22}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{22}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{231a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{231a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{20}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{ad}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}\u{ad}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{300}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}\u{300}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{200d}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{200d}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{61}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{61}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{61}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{61}\u{27}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{61}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}\u{308}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{31}\u{3a}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{31}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{31}\u{2c}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2060}\u{308}\u{31}\u{2e}\u{2060}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{2e}\u{2060}\u{308}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}\u{2060}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{d}\u{a}\u{61}\u{a}\u{308}".as_words()
        match w.next() { Some(s) => assert(s == "\u{d}\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{308}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{308}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{308}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{20}\u{200d}\u{646}".as_words()
        match w.next() { Some(s) => assert(s == "\u{20}\u{200d}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{646}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{671}\u{644}\u{631}\u{64e}\u{651}\u{62d}\u{650}\u{64a}\u{645}\u{650}\u{20}\u{6dd}\u{661}".as_words()
        match w.next() { Some(s) => assert(s == "\u{671}\u{644}\u{631}\u{64e}\u{651}\u{62d}\u{650}\u{64a}\u{645}\u{650}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{6dd}\u{661}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{721}\u{719}\u{721}\u{718}\u{72a}\u{710}\u{20}\u{70f}\u{71d}\u{717}".as_words()
        match w.next() { Some(s) => assert(s == "\u{721}\u{719}\u{721}\u{718}\u{72a}\u{710}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{70f}\u{71d}\u{717}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{72c}\u{70f}\u{72b}\u{712}\u{718}".as_words()
        match w.next() { Some(s) => assert(s == "\u{72c}\u{70f}\u{72b}\u{712}\u{718}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{41}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{41}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{3a}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{3a}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{27}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{27}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{5d0}\u{22}\u{5d0}".as_words()
        match w.next() { Some(s) => assert(s == "\u{5d0}\u{22}\u{5d0}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{30}\u{30}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{30}\u{30}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{2c}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}\u{2c}\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{30}\u{2c}\u{2c}\u{30}".as_words()
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{30}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{5f}\u{30}\u{5f}\u{3031}\u{5f}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{5f}\u{30}\u{5f}\u{3031}\u{5f}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{41}\u{5f}\u{5f}\u{41}".as_words()
        match w.next() { Some(s) => assert(s == "\u{41}\u{5f}\u{5f}\u{41}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f1e6}\u{1f1e7}\u{1f1e8}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{1f1e7}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e8}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{1f1e6}\u{1f1e7}\u{1f1e8}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{1f1e7}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e8}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{1f1e6}\u{200d}\u{1f1e7}\u{1f1e8}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{200d}\u{1f1e7}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e8}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{1f1e6}\u{1f1e7}\u{1f1e8}\u{1f1e9}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e6}\u{1f1e7}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f1e8}\u{1f1e9}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f476}\u{1f3ff}\u{1f476}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f476}\u{1f3ff}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f476}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f6d1}\u{200d}\u{1f6d1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f6d1}\u{200d}\u{1f6d1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{200d}\u{1f6d1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{200d}\u{1f6d1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{200d}\u{2701}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{200d}\u{2701}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f476}\u{1f3ff}\u{308}\u{200d}\u{1f476}\u{1f3ff}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f476}\u{1f3ff}\u{308}\u{200d}\u{1f476}\u{1f3ff}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f6d1}\u{1f3ff}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f6d1}\u{1f3ff}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{1f6d1}\u{1f3ff}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{1f6d1}\u{1f3ff}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{200d}\u{1f6d1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{200d}\u{1f6d1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{1f6d1}\u{1f6d1}".as_words()
        match w.next() { Some(s) => assert(s == "\u{1f6d1}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{1f6d1}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{308}\u{200d}\u{308}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{308}\u{200d}\u{308}\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{20}\u{20}\u{62}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{20}\u{20}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{62}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{3a}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{3a}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{3a}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2e}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2e}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2e}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2e}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2e}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2c}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2c}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{2c}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2c}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{31}\u{2c}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{31}\u{5f}\u{61}\u{2c}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{31}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{3a}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{3a}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{3a}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{3a}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{3a}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2e}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2e}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2e}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2e}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2e}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2e}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2e}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2e}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2e}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2e}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2c}\u{3a}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2c}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2c}\u{3a}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{3a}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2c}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2c}\u{2e}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2c}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2c}\u{2e}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2e}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2c}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{61}\u{2c}\u{2c}\u{31}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{31}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{2c}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
    {
        mut w = "\u{61}\u{5f}\u{31}\u{2c}\u{2c}\u{61}".as_words()
        match w.next() { Some(s) => assert(s == "\u{61}\u{5f}\u{31}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{2c}"), None => assert(false) }
        match w.next() { Some(s) => assert(s == "\u{61}"), None => assert(false) }
        assert(w.next() == None)
    }
}

