// C6 — duplicate generic name `fn[T, T]` → parse error.
//
// EXPECT_COMPILE_ERROR

module neg.plan101_1_duplicate_prefix_name_neg

fn[T, T] T @bad() -> T => @

test "should error" {
    assert(1 == 1)
}
