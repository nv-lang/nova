// Plan 118 Ф.1 NEG-T1.14 — `*` без trailing type.
//
// EXPECT_COMPILE_ERROR expected type

module plan118.t1_neg_pointer_incomplete

// `*` без type after — parser fails recursive parse_type call.
external fn p118_bad(p *) -> i64

test "rejects" { assert(1 == 1) }
