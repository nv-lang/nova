// Plan 118.1.5 T1 POS — `export external unsafe fn` parses successfully.
// D2 amend: unsafe keyword extended к extern "nova" fn declarations.

module nova_tests.plan118_1_unsafe_extern_fn

export external unsafe fn foo(x int) -> ()

test "unsafe extern fn declaration parses" {
    // Declaration only — no call. Verifies parser accepts keyword.
    assert(1 == 1)
}
