// Plan 118.1.5 T1 POS — `#unsafe export external fn` parses successfully.
// D2 amend: #unsafe attribute extended к external fn declarations.

module plan118_1_unsafe_extern_fn.t1_pos_unsafe_extern_fn_parses_ok

export #unsafe external fn foo(x int) -> ()

test "unsafe extern fn declaration parses" {
    // Declaration only — no call. Verifies parser accepts attribute.
    assert(1 == 1)
}
