// Plan 118.1.5 T2 NEG — call к #unsafe extern fn без unsafe {} wrap rejected.
// D2 amend / D216 §9 enforcement: same call-site rules as #unsafe Nova fn.
//
// EXPECT_COMPILE_ERROR E_UNSAFE_CALL_REQUIRES_WRAP

module plan118_1_unsafe_extern_fn.t2_neg_unsafe_extern_fn_call_outside_unsafe

export #unsafe external fn risky_extern(x int) -> ()

test "calling #unsafe extern fn without unsafe wrap rejected" {
    risky_extern(42)
}
