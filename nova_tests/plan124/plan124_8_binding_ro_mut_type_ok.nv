// Plan 124.8 — positive: `ro x mut T = ...` form (binding ro, content mut)
// — D176 amend Ф.2 closes parser gap.
module nova_tests.plan124

test "ro binding + mut type — parses + works" {
    mut arr []int = [1, 2, 3]
    ro view mut []int = arr      // valid distinct case
    assert(view[0] == 1)
}
