// Plan 03.1 Ф.3 NEGATIVE — межпакетный цикл зависимостей A→B→A:
// `amod` (cyc_a_pkg) → `bmod` (cyc_b_pkg) → `amod` (cyc_a_pkg).
// Cycle detection — по declared module name, не по пути, поэтому
// межпакетный цикл ловится тем же механизмом, что и внутрипакетный.
// EXPECT_COMPILE_ERROR import cycle
module cyc_consumer.cycle_test

import cyc_a_pkg.amod.{a_val}

test "unreachable" {
    assert(true)
}
