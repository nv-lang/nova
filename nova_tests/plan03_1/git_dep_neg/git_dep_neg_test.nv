// Plan 03.1 Ф.3 NEGATIVE — импорт из git-зависимости. Git fetch+кэш —
// Plan 03.1 Ф.2; пока git-зависимости не резолвятся, ошибка — честная.
// EXPECT_COMPILE_ERROR git-зависимость
module git_dep_pkg.git_dep_neg_test

import remotelib.calc.{add}

test "unreachable" {
    assert(true)
}
