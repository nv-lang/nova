// Plan 03.1 Ф.3 NEGATIVE — попытка импортировать `internal/`-модуль
// зависимости напрямую. Rule H: `internal/` доступен только изнутри
// своего пакета — межпакетный импорт запрещён.
// EXPECT_COMPILE_ERROR internal module
module consumer_int.leak_internal_test

import deplib_int.internal.secret.{secret_base}

test "unreachable" {
    assert(true)
}
