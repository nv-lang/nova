// Plan 100.6 (D164): re-export consume через `export import` (Plan 42.09).
// Marker сохраняется через chain A → B → C.

module nova_tests.plan100_6

// "Package A":
export type Transaction_crosspkgconsumereexportok consume { id int }
export fn Transaction_crosspkgconsumereexportok consume @commit() -> () { () }
export fn begin_crosspkgconsumereexportok() -> Transaction_crosspkgconsumereexportok => { id: 1 }

// "Package B" — re-exports от A:
// export import a.types.{Transaction_crosspkgconsumereexportok, begin_crosspkgconsumereexportok}     // в реальном коде

// "Package C" — imports Transaction_crosspkgconsumereexportok через chain:
// import b.facade.Transaction_crosspkgconsumereexportok

test "re-export preserves consume-marker через chain" {
    consume tx = begin_crosspkgconsumereexportok()
    tx.commit()
}
