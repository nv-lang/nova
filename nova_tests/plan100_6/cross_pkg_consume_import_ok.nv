// Plan 100.6 (D164): consume-маркер propagates через границы пакетов.
// Package A exports Transaction_crosspkgconsumeimportok consume; Package B imports + consumes.

module nova_tests.plan100_6

// Simulated: in real test, would be 2 packages.
// Here single-package mock с export + import.

// "Package A":
export type Transaction_crosspkgconsumeimportok consume {
    id int,
}

export fn Transaction_crosspkgconsumeimportok consume @commit() -> () { () }
export fn Transaction_crosspkgconsumeimportok consume @rollback() -> () { () }
export fn begin_crosspkgconsumeimportok() -> Transaction_crosspkgconsumeimportok => { id: 42 }

// "Package B" — would import Transaction_crosspkgconsumeimportok.
test "cross-package consume — import работает; binding-form OK" {
    consume tx = begin_crosspkgconsumeimportok()                        // consume-marker visible from A
    tx.commit()
}
