// EXPECT_RUNTIME_PANIC requires
//
// Plan 153.4 — negative: windows(n) with n < 0 violates the `n > 0` contract
// → runtime panic.
module plan153_4.windows_neg

import std.collections.vec_lazy as vec_lazy

fn main() -> () {
    ro v = Vec[int].of(1, 2, 3)
    ro _ = v.windows(-1).collect()
}
