// EXPECT_RUNTIME_PANIC requires
//
// Plan 153.4 — negative: rchunks(n) with n == 0 violates `n > 0` → panic.
module neg.rchunks_zero_neg

import std.collections.vec_lazy

fn main() -> () {
    ro v = Vec[int].of(1, 2, 3)
    ro _ = v.rchunks(0).collect()
}
