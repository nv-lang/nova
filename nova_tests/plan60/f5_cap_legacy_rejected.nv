module nova_tests.plan60.f5_cap_legacy_rejected

// Plan 60 / D112: legacy `.cap` без скобок (теперь `.capacity()`).
// Diagnostic suggests rename, не просто append parens.
// EXPECT_COMPILE_ERROR E_SIZE_ACCESSOR_FIELD

fn main() -> () {
    ro a []int = [1, 2, 3]
    ro c int = a.cap   // ← should error: rename to .capacity()
    println(c)
}
