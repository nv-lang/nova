module plan60.f3_str_field_rejected

// Plan 60 / D112: str.len без скобок → error.
// EXPECT_COMPILE_ERROR E_SIZE_ACCESSOR_FIELD

fn main() -> () {
    ro s str = "hello"
    ro n int = s.len   // ← should error
    println(n)
}
