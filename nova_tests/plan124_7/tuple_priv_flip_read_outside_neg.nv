// EXPECT_COMPILE_ERROR E_PRIV_FIELD_READ
//
// Plan 124.7 — negative: type-level priv flip — external read of
// type-default-priv field blocked.
module plan124_7.tuple_priv_flip_read_outside_neg

type Secret priv (key str, salt str)

fn Secret.new(k str, s str) -> Secret =>
    Secret(key: k, salt: s)

// External fn — `key` priv (inherited from type-level flip) → blocked.
fn external_get_key(s Secret) -> str =>
    s.key
