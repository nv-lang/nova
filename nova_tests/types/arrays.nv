// 10_arrays.nv — массивы (D27), spread (D60), методы

module types.arrays

test "array literal and len" {
    mut a = [1, 2, 3]
    assert(a.len() == 3)
}

test "empty array" {
    mut a []int = []
    assert(a.len() == 0)
}

test "indexing" {
    mut a = [10, 20, 30]
    assert(a[0] == 10)
    assert(a[1] == 20)
    assert(a[2] == 30)
}

test "spread concat (D60)" {
    mut a = [1, 2, 3]
    mut b = [4, 5]
    mut c = [...a, ...b]
    assert(c.len() == 5)
    assert(c[0] == 1)
    assert(c[4] == 5)
}

test "spread with literals around" {
    ro mid = [2, 3]
    ro full = [1, ...mid, 4, 5]
    assert(full.len() == 5)
    assert(full[0] == 1)
    assert(full[1] == 2)
    assert(full[3] == 4)
}

test "multiple spreads" {
    mut a = [1]
    mut b = [2]
    mut c = [3]
    ro combined = [...a, ...b, ...c]
    assert(combined.len() == 3)
    assert(combined[1] == 2)
}

test "spread copy is independent" {
    mut a = [1, 2, 3]
    mut b = [...a]
    assert(b.len() == 3)
    assert(b[0] == 1)
}

test "push and len" {
    mut a = [1, 2]
    a.push(3)
    a.push(4)
    assert(a.len() == 4)
    assert(a[3] == 4)
}

test "pop returns Some/None" {
    mut a = [10, 20, 30]
    ro last = a.pop()
    assert(last == Some(30))
    assert(a.len() == 2)
}

test "pop empty returns None" {
    mut a []int = []
    ro r = a.pop()
    assert(r == None)
}

test "get with valid index" {
    mut a = [10, 20, 30]
    ro v = a.get(1)
    assert(v == Some(20))
}

test "get with out-of-range index" {
    mut a = [10]
    ro v = a.get(99)
    assert(v == None)
}

test "for-in over array" {
    ro xs = [1, 2, 3, 4, 5]
    mut sum = 0
    for x in xs {
        sum += x
    }
    assert(sum == 15)
}

test "index assignment" {
    mut a = [1, 2, 3]
    a[0] = 100
    a[2] = 300
    assert(a[0] == 100)
    assert(a[1] == 2)
    assert(a[2] == 300)
}

test "compound index assignment" {
    mut a = [10, 20]
    a[0] += 5
    assert(a[0] == 15)
}

test "tuple of arrays" {
    ro pair = ([1, 2], [3, 4])
    ro left = pair.0
    ro right = pair.1
    assert(left[0] == 1)
    assert(right[1] == 4)
}
