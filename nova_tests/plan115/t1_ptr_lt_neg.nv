// Plan 115 Ф.1 NEG-T1.5b — ptr order comparison banned (only Eq/Neq allowed).

// EXPECT_COMPILE_ERROR E_PTR_ARITHMETIC_BANNED

module plan115.t1_ptr_lt_neg

test "ptr < ptr is banned" {
    ro p ptr = (0 as ptr)
    ro q ptr = (0 as ptr)
    ro result = p < q
    assert(!result)
}
