// Plan 90.1 — позитив: insert_from(len, src) ≡ extend_from(src).
// D141 amend: i == dst.len is valid (append-at-end case).

module nova_tests.plan90_1.insert_from_at_end

test "insert_from at end (i == len) — equivalent to extend_from" {
    let mut dst []int = [1, 2, 3]
    let src []int = [4, 5, 6]
    dst.insert_from(3, src)
    assert(dst.len() == 6)
    assert(dst[0] == 1)
    assert(dst[3] == 4)
    assert(dst[5] == 6)
}

test "insert_from at end of empty array" {
    let mut dst []int = []
    let src []int = [10, 20]
    dst.insert_from(0, src)
    assert(dst.len() == 2)
    assert(dst[0] == 10)
    assert(dst[1] == 20)
}

test "insert_from at end triggers growth" {
    let mut dst []int = [1, 2, 3, 4, 5, 6, 7, 8]  // fills default cap=8
    let src []int = [9, 10]
    dst.insert_from(8, src)
    assert(dst.len() == 10)
    assert(dst[8] == 9)
    assert(dst[9] == 10)
}
