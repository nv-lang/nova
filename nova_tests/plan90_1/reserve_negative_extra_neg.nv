// EXPECT_RUNTIME_PANIC reserve: extra must be >= 0
//
// Plan 90.1 — негатив: reserve — negative extra → panic.
// D141 amend: extra < 0 is a bug; panic with diagnostic.

module plan90_1.reserve_negative_extra_neg

fn main() -> () {
    mut arr []int = [1, 2, 3]
    arr.reserve(-1)
}
