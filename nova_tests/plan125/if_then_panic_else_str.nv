// Plan 125 Ф.2: trailing-panic divergent в then.

module nova_tests.plan125

fn pick8(c bool) -> str {
    if c {
        panic("nope")
    } else {
        "ok"
    }
}

test "panic in then, str in else" {
    ro s = pick8(false)
    assert(s == "ok")
}
