// Plan 125 Ф.4: if-let — divergent в then.

module nova_tests.plan125

fn pick4(opt Option[int]) Fail -> str {
    if let Some(_x) = opt {
        throw "bad"
    } else {
        "none-ok"
    }
}

test "if-let then-throw, else-str" {
    with Fail = effect Fail {
        fail(_) { interrupt () }
    } {
        ro s = pick4(None)
        assert(s == "none-ok")
    }
}
