// Plan 125 Ф.2: trailing-exit divergent в then.

module nova_tests.plan125

fn pick6(c bool) -> str {
    if c {
        exit(2, "nope")
    } else {
        "fine"
    }
}

test "exit in then, str in else" {
    ro s = pick6(false)
    assert(s == "fine")
}
