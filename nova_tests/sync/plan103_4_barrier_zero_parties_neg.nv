// Plan 103.4 — Barrier: Barrier.new(0) → RUNTIME_PANIC.
//
// Unconditional invariant check (fires in Dev AND Release — §Discovery from 103.5).
// parties must be >= 1.
//

module nova_tests.sync

test "Barrier.new(0) panics" panics "Barrier.new(): parties must be >= 1" {
    ro _b = Barrier.new(0)
}
