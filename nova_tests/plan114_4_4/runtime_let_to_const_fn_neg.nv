// Plan 114.4.4 Ф.2 NEG: `ro f = const_fn` runtime binding rejected.

// EXPECT_COMPILE_ERROR E_CONST_FN_FIRST_CLASS

module plan114_4_4.runtime_let_to_const_fn_neg

fn double(const x int) -> const int => x * 2

test "runtime let to const fn" {
    ro f = double
    assert(f(5) == 10)
}
