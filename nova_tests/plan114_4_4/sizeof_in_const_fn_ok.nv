// Plan 114.4.4 Ф.5 positive: sizeof[T]() в const fn body.

module plan114_4_4.sizeof_in_const_fn_ok

fn buf_size(const count int) -> const int =>
    count * sizeof[int]()

test "sizeof в const fn body" {
    const TOTAL = buf_size(10)
    // 10 * 8 = 80
    assert(TOTAL == 80)
}
