// EXPECT_RUNTIME_PANIC out of bounds
//
// Plan 145 — негатив: индексация байтового слоя за границей → panic.
// NovaArray-путь (emit_bchk_array_access) через nova_idx_chk.

module neg.neg_bytes_index_oob

fn main() -> () {
    ro s = "AB"
    ro _ = s.as_bytes()[5]
}
