// Plan 73.1 (D180): regular `let` для не-consume RHS работает без consume keyword.

module nova_tests.plan73_1.binding_let_primitive

test "let для int — без consume keyword" {
    let n = 42
    let s = "hello"
    let b = true
    assert(n == 42)
    assert(s == "hello")
    assert(b == true)
}
