// Plan 133 NEG — `usize` type is removed; use `int` instead.
//
// EXPECT_COMPILE_ERROR usize` is removed

module plan133.t1_usize_removed_neg

fn main() -> () {
    ro x usize = 5
    println(x)
}
