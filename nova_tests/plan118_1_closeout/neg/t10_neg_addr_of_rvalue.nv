// Plan 118.6 T10 NEG — addr_of() removed → E_ADDR_OF_REMOVED.
// EXPECT_COMPILE_ERROR E_ADDR_OF_REMOVED
module neg.t10_neg_addr_of_rvalue
fn produce() -> int { 42 }
test "addr_of removed — rvalue case also gives E_ADDR_OF_REMOVED" {
    unsafe {
        ro p = addr_of(produce())
    }
}
