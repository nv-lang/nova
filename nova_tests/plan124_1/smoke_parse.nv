// Plan 124.1 Ф.1 — smoke test: priv modifier parses без ошибок.
// Type-level priv flip + per-field priv/pub markers.
module nova_tests.plan124_1

// Per-field priv modifier (D220).
export type Account_smokeparse {
    priv mut money f64
    ro name str
    priv id u64
}

// Type-level priv {} flip (D220 §3.3.1).
export type Secret_smokeparse priv {
    pub ro tag str
    mut salt u64
}

fn main() -> () {
    ro _ = "smoke"
}
