// Plan 137 (D237) — POS P10:
// `#impl(Hash)` на record → auto-derive @hash.
module nova_tests.strings

#impl(Hash)
type UserId {
    value u64
}

test "Hash auto-derive works" {
    ro id = UserId { value: 42 }
    assert(id.value == 42)
}
