module nova_tests.strings

test "sentences is_empty on empty string" {
    assert("".sentences().is_empty())
}

test "sentences is_empty after partial iteration" {
    mut it = "Hello. World.".sentences()
    ro _ = it.next()
    assert(!it.is_empty())
    ro _ = it.next()
    assert(it.is_empty())
}
