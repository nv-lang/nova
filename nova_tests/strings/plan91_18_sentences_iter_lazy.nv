module nova_tests.strings

test "as_sentences is O(1)" {
    ro it = "Hello. World.".sentences()
    assert(!it.is_empty())
}

test "sentences basic" {
    mut cnt = 0
    mut it = "Hello. World. Foo.".sentences()
    while it.next() != None { cnt += 1 }
    assert(cnt >= 2)
}

test "sentences is_empty after drain" {
    mut it = "Hi.".sentences()
    while it.next() != None {}
    assert(it.is_empty())
}

test "sentences empty string" {
    assert("".sentences().is_empty())
}
