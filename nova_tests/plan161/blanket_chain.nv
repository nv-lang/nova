module plan161.blanket_chain

import std.collections.vec.{Vec}
import std.collections.vec_iter_zc

test "blanket chain through zmap" {
    ro v = Vec[int].of(1, 2, 3)
    ro r = v.ziter().zmap(|x| x * 2).zcollect()
    assert(r == Vec[int].of(2, 4, 6))
}
