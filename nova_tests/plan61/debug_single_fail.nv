module nova_tests.plan61

test "simple fail handler with interrupt" {
    mut caught = ""
    with Fail = effect Fail {
        fail(msg) {
            caught = msg
            interrupt ()
        }
    } {
        throw "boom"
    }
    assert(caught == "boom")
}
