module nova_tests.plan61.debug_single_fail

test "simple fail handler with interrupt" {
    let mut caught = ""
    with Fail = handler Fail {
        fail(msg) {
            caught = msg
            interrupt ()
        }
    } {
        throw "boom"
    }
    assert(caught == "boom")
}
