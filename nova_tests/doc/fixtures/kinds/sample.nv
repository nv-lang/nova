//! Plan 45 / D107: fixture РґР»СЏ Sum / Effect / Protocol / Alias items.
//!
//! #stable

module kinds.sample

/// Sum-С‚РёРї СЃ unit-РІР°СЂРёР°РЅС‚Р°РјРё.
///
/// #stable
export type Color | Red | Green | Blue

/// Counter вЂ” РїСЂРѕСЃС‚РѕР№ effect СЃ axiom.
///
/// #stable
export type Counter effect {
    next() -> int
    axiom non_negative(x int) => x >= 0
}

/// Eq вЂ” protocol.
///
/// #stable
export type Eq protocol {
    @equal(other Eq) -> bool
}

/// Alias РЅР°Рґ int вЂ” semantic clarity.
///
/// #stable
export type Pixels alias int

/// Newtype РЅР°Рґ int.
///
/// #stable
export type AccountId int
