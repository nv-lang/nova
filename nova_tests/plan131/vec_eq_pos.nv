// Plan 131 Р¤.4 вЂ” Vec[T] Equal: pairwise element comparison.
// EXPECT_OUTPUT: true
// false

module plan131.vec_eq_pos

import std.collections.vec_owned.{Vec}

fn main() -> () {
    ro a = Vec[int].from([1, 2, 3])
    ro b = Vec[int].from([1, 2, 3])
    ro c = Vec[int].from([1, 2, 4])
    println(a.equal(b))
    println(a.equal(c))
}
