// EXPECT_COMPILE_ERROR D159
//
// Plan 100.4.2 (D159): supervised{} в defer body — error.

module nova_tests.plan100_4_2.defer_err_supervised_in_body

fn process() -> () {
    defer {
        supervised {   // ❌ D159
            ()
        }
    }
}

test "supervised в defer body — D159 error" {
    process()
}
