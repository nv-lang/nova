// EXPECT_COMPILE_ERROR D159
//
// Plan 100.4.2 (D159): spawn в defer body — error.
// D159 amend'нул D90 §5 (suspend allowed) НО spawn / parallel for /
// supervised / detach / blocking — keep banned (leak supervised hierarchy).

module nova_tests.plan100_4_2.defer_err_spawn_in_body

fn process() -> () {
    defer {
        spawn { println("leaked") }   // ❌ D159-spawn-in-defer
    }
}

test "spawn в defer body — D159 error" {
    process()
}
