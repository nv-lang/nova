// Plan 120 D215-amend: negative — supplying an unknown field name.
// EXPECT_COMPILE_ERROR E_TUPLE_UNKNOWN_FIELD

module neg.neg_t5_unknown_field

type Pair(a int = 0, b int = 0)

test "unknown field name is an error" {
    ro p = Pair(a: 1, c: 3)   // c is not a field of Pair
}
