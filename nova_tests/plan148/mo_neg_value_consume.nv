// EXPECT_COMPILE_ERROR E_MODIFIER_ORDER
//
// Plan 148 Ф.1 (D241 amend) — negative: `value consume` is out of canonical order.
//
// D241 revised (Plan 91.12): canonical order is ownership → representation:
//   consume (rank 0) must precede value (rank 1).
// `value consume` inverts them → E_MODIFIER_ORDER, fix-it "reorder to `consume value`".
module plan148.mo_neg_value_consume

type Token value consume {
    id int
}

fn Token consume @use() -> () { () }
