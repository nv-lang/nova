// Plan 57.C.2 smoke: gc.last_pause_ns API.
module bench.micro

bench "gc pause measurement" {
    measure {
        // Force a collect — under malloc no-op, под boehm — real cycle.
        gc.collect()
        ro p = gc.last_pause_ns()
        bench.opaque(p)
    }
}
