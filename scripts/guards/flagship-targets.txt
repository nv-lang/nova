# ЦЕЛИ ФЛАГМАН-СБОРКИ (`--strict-effects`).
#
# ЕДИНСТВЕННЫЙ источник правды. Читают ОБА гейта: `scripts/gate.sh` и
# `.github/workflows/nova-gate.yml`.
#
# ЗАЧЕМ ФАЙЛОМ. До 2026-08-10 список был записан ДВАЖДЫ — отдельно в скрипте,
# отдельно в workflow — и ничто не проверяло, что копии совпадают. Ровно этот
# класс уже стоил нам дважды: №509 (правило игнорирования `.c` велось
# перечислением каталогов и молча пропустило 172 файла) и №524 (изменение
# публичной поверхности `std` грепалось по своему дереву, пакетов не видевшему).
# Перечисление, которое надо помнить и дописывать в двух местах, протекает —
# вопрос только когда.
#
# Формат строки:  <имя> <путь от корня репозитория>
# Строки с `#` и пустые пропускаются.
#
# ПОЧЕМУ ЭТИ ЦЕЛИ. Conformance ловит язык, но НЕ ловит регрессии приложений
# (прецедент 206/splitmix64: conformance 470/0 при упавшем флагмане). Поэтому
# behavior-changing слияние обязано вдобавок собрать флагман-примеры.
#
# ДОБАВЛЯЯ ЦЕЛЬ: одной строкой здесь — и она подхватится обоими гейтами.

aggregator       examples/flagship/aggregator/src/main.nv
http_proxy_chain  examples/flagship/http_proxy_chain/main.nv
echo_client_net  examples/net/echo_client.nv
echo_server_net  examples/net/echo_server.nv
echo_client_tls  examples/tls/echo_client.nv
echo_server_tls  examples/tls/echo_server.nv
