# План 261 Ф.0 — список ПАКЕТНЫХ репозиториев.
#
# ЕДИНСТВЕННЫЙ источник правды. Читают и гейт, и CI: перечисление, которое
# надо помнить и дописывать, протекает (реестр 221.1 №509 — правило игнорирования
# .c велось перечислением каталогов и молча пропустило 172 файла).
#
# Формат строки:  <имя> <путь> <команда тестов>
# Строки с # и пустые пропускаются.
#
# Отбор сделан по признакам, а не по имени: каталог с `.git`-КАТАЛОГОМ (не
# файлом — файл означает временную рабочую копию окна) И с `nova.toml`.
# Инвентаризация 2026-08-09: 101 каталог рядом с проектом, из них 13
# репозиториев и 87 временных копий окон.
#
# НЕ входят и почему:
#   nova                          — само дерево, гейт и так его проверяет
#   nova-bigint-trial-deepseek    — эксперимент по выбору реализации bigint,
#   nova-bigint-trial-laguna        не публикуется
#   nova-private                  — журнал обсуждений, кода нет
#   tree-sitter-nova              — грамматика для редакторов, своя сборка
#   www, www.nv-lang.ru           — сайт, компилятором не собирается
#
# У nova-http нет `nova.sh` — вызывается напрямую бинарём. Это расхождение
# между пакетами само по себе долг: команда запуска должна быть одинаковой.

nova-bignum    d:/Sources/nv-lang/nova-bignum    ./nova.sh test src
nova-compress  d:/Sources/nv-lang/nova-compress  ./nova.sh test src
nova-http      d:/Sources/nv-lang/nova-http      NOVA test src
nova-polaris   d:/Sources/nv-lang/nova-polaris   ./nova.sh test src
nova-socks     d:/Sources/nv-lang/nova-socks     ./nova.sh test src
nova-tls       d:/Sources/nv-lang/nova-tls       ./nova.sh test src
