# 2026-08-19  550 -> 551, и это НЕ отступление от правила.
#   Многофайловая neg-фикстура `neg/d65_exhaustive_crossmod_collision/`
#   (№705, окно W6): раннер читает EXPECT_COMPILE_ERROR с ВХОДНОГО
#   файла, а сама ошибка возникает в СОСЕДНЕМ модуле.
#   Построчный пин стоит там, где ошибка ЕСТЬ (`ka/kinds.nv`);
#   во входном файле ему взяться НЕОТКУДА, а пин ради числа
#   был бы ложью — ровно тот класс, против которого храповик.
#   Соседние многофайловые (`file_priv_leak`, `privfile_free_fn_leak`)
#   стоят в базе по той же причине.
# База храповика — scripts/guards/check-nova-expect-ratchet.sh
#
# Считаются `.nv` в spec_tests/conformance с файловым `EXPECT_COMPILE_ERROR`
# маркером и БЕЗ хотя бы одной построчной `// nova:expect E_CODE -- причина`
# пометки (twin `nova:allow`, план 262 Часть Б). Рост запрещён; снижение
# свободно — база опускается той же правкой, когда волна размечает старую
# фикстуру попутно.
#
# ЛЕТОПИСЬ
# 2026-08-10 — база заведена значением 550 (окно p262-b-expect, план 262
#   Часть Б). Первый замер: `bash scripts/guards/check-nova-expect-ratchet.sh`
#   сразу после появления самого механизма `nova:expect` — ни одна
#   существующая фикстура ещё не мигрирована (нулевая точка отсчёта).
#   ДОЛГ: 550 негативных фикстур в spec_tests/conformance несут только
#   файловый маркер. Снижать по мере разбора; каждая волна, трогающая
#   старую негативную фикстуру, обязана довести её до построчной пометки.
unpinned_neg_fixtures=551
