# База 2026-08-11 (реестр 221.1 №591): ИМЕНА тестов std, которые падают сегодня.
# Список, а не счётчик: счётчик прячет подмену — в первый же прогон локально
# было семь отказов против восьми на CI, но пересечение неполное.
#
# КАЖДОЕ ИМЯ НЕСЁТ НОМЕР ЗАПИСИ. Причина названа в №599: №559 был заведён сутки
# назад и прямо говорил «ни один наш прогон не собирал CU, импортирующий uuid»,
# а те же два теста я вписал сюда как «известный фон», не сверившись с реестром.
# Имя в базе без номера — это не фон, а отложенный дефект без следа.
#
# Имя уходит отсюда ТОЛЬКО вместе с фикстурой, доказывающей исчезновение
# причины, а не потому, что кто-то счёл его шумом.
#
# 2026-08-12: два имени uuid УШЛИ — №559 закрыт (const-агрегат в указательный
# слот уводится на lazy-путь с GC-корнем, `nova test std/src/identifiers`
# даёт PASS: 4 FAIL: 0). База опущена, а не подтёрта: причина названа.
#
# 2026-08-12: cron_test, civil_arith_test и json_test УШЛИ.
#
# cron_test — №599 закрыт (`?`-проброс `map_err`-результата через
# бесконтрольный closure-body резолвил F не по owning-типу, а по спеллингу
# варианта; чекер теперь резолвит F через тот же variant→owner поиск, что и
# остальной чекер). `nova test std/src/time` PASS:5 FAIL:1 (только №601,
# который в тот момент был ещё открыт).
#
# civil_arith_test — №601 закрыт (digits_at, std/src/time/civil/parse.nv:
# closure-литерал внутри while-цикла ломал мутацию mut-переменной условия —
# компиляторный дефект [M-TBD-closure-while-mut-capture]; фикс — match вместо
# .map_err(closure)).
#
# json_test — №602 закрыт (IndexMap[K,V].@keys(),
# std/src/collections/index_map/core.nv: теперь материализованный []K вместо
# ленивого итератора — alias-метод-резолв, компиляторный дефект
# [M-TBD-alias-method-shadow-cross-module], делал JsonObject-фасад
# недостижимым; №505 сверен отдельно — уже закрыт раньше, не пересекается).
#
# `nova test std/src/time/civil std/src/encoding` — PASS без этих трёх имён,
# три прогона подряд. Фикстуры:
# spec_tests/conformance/standalone/n601_civil_pattern_digits_at_while_closure.nv,
# n602_json_object_keys_len.nv.
#
# decode_errors_test — причина №599 (генерик `[]T.deserialize` TurboFish-вызов
# терял элемент-тип, `NovaOpt_Vec[str]` получал `Vec[int]`) ЗАКРЫТА и
# подтверждена фикстурой: сгенерированный C для ВСЕХ вызовов
# `Nova_NovaArray_nova_str_static_deserialize...` теперь несёт правильный
# `nova_str`, CC-FAIL ушёл. Имя ОСТАЁТСЯ в базе — компиляция теперь проходит,
# но `nova test std/src/encoding/serde` вскрыл RUN-FAIL, ранее НЕВИДИМЫЙ за
# тем же CC-FAIL (весь модуль не собирался ни разу): 4 теста в
# `sum_autoderive_test.nv` (той же CU) падают в рантайме (`<typed err>` /
# `wrong kind`) — сериализация/десериализация sum-варианта с record-,
# Option- и Vec-payload, независимо от типизации кодогена. Новый дефект,
# №624, не тот же класс — не чинился в этом окне (см. отчёт p599).

std/src/encoding/serde/decode_errors_test   # №624 (было №599/CC-FAIL, ЗАКРЫТО окном p599; теперь RUN-FAIL в sum_autoderive_test.nv, скрытый прежде тем же CC-FAIL)
std/src/net/addr                            # №591 (только CI: wakes a directly-parked accept)
std/src/os/d324_os_env_args_cwd_test        # №591 (только CI: d453 supervised timeout reaps)
