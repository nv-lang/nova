<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# scripts/selftest/ — регресс-тесты механизмов принуждения

## Зачем

Скрипты в `scripts/` (гейт, ratchet'ы, хуки) — это код, который может тихо
перестать ловить то, для чего создан: ландшафт меняется, появляются новые
пути в командах, кто-то рефакторит хук. **Механизм принуждения без
собственного регресс-теста — это доверие на слово.** Хуже того: страж,
который молча разучился что-то ловить, ХУЖЕ отсутствия стража вовсе — он
создаёт ложное ощущение защиты, пока конвенция уже нарушается никем не
замеченной.

Это план [231](../../docs/plans/231-bug-cycle-exit.md) трек Ж (§4в):
«скрипты и автоматизированные функции проверки должны сами быть
подтверждены регресс-тестами, что ничего не пропускают» (вопрос владельца
2026-07-26).

## Что обязан проверять каждый самотест

Ровно ДВА свойства (без обоих — тест неполный):

1. **ЛОВИТ нарушение.** Синтетический случай, воспроизводящий то, против
   чего страж создан (испорченный лог, копия рантайма, запрещённая
   git-команда, ...) → страж обязан упасть/заблокировать.
2. **НЕ ловит законный случай.** Синтетический ЧИСТЫЙ случай (или сама
   реальная репа nova, если применимо) → страж обязан пропустить его без
   ложного срабатывания. Ложняк дорог: если страж ложно блокирует legit-
   случаи, из него быстро начинают выкручиваться обходами, и он теряет
   доверие даже там, где прав.

Образец формы — [`test-check-no-runtime-copy.sh`](test-check-no-runtime-copy.sh):
временный каталог (`mktemp -d` + `trap rm -rf ... EXIT`), несколько
`check()`-кейсов (имя, ожидаемый код, фактический код), сводный
счётчик `fails`, итоговый `exit 1`, если хоть один кейс не совпал.

## Как добавить новый самотест

Один файл `test-<имя-стража>.sh` (или `.py`, если страж на Python) на
каждый механизм. **Правка `gate.sh` НЕ нужна** — `gate.sh` обходит этот
каталог циклом:

```sh
for st in "$ROOT"/scripts/selftest/test-*.sh; do
    [ -e "$st" ] || continue
    bash "$st" || fail "самотест стража: $(basename "$st")"
done
```

т.е. новый файл, названный по маске `test-*.sh`, подхватывается
автоматически при следующем прогоне `gate.sh`.

## Таблица покрытия (план 231 §4в — источник истины, актуализировать там)

Полная таблица «механизм → тест» ведётся в
[`docs/plans/231-bug-cycle-exit.md`](../../docs/plans/231-bug-cycle-exit.md)
§4в «Трек Ж — Самотесты механизмов принуждения» — здесь только копия
статуса на момент написания этого README (2026-07-27),
**за актуальным состоянием — в план, не сюда**, чтобы не дублировать и не
рассинхронизироваться (тот же принцип, что статус планов — только
пофайлово).

| Механизм | Самотест есть? |
|---|---|
| `check-no-runtime-copy.sh` | ✅ `test-check-no-runtime-copy.sh` (5 свойств: ловит копию в пакетной репе, не ловит главную репу/чистую репу, ловит одну грязную среди чистых, не ломается на настоящей nova) |
| `gate.sh` | ❌ нет (план требует: синтетический красный кейс на каждый отдельный ассерт гейта + синтетический зелёный) |
| `arch-ratchet.sh` | ❌ нет (план требует: baseline+1 строка в `emit_c.rs` через временный fixture-файл → красный; baseline как есть → зелёный) |
| `scripts/claude-hooks/guard-git.py` | ❌ нет (план требует таблицу команд: `git config user.email x` — блок; `git config user.email` без записи — пропуск; `git add -A`/`.` — блок; heredoc с текстом «git add -A» внутри — НЕ блок, это уже был найденный руками ложняк-регресс 2026-07-26) |
| `scripts/claude-hooks/guard-memory.py` | ❌ нет (план требует: `Write` без `enforcement:` — блок; с полем — пропуск) |
| `scripts/githooks/pre-commit` (5 реп) | ❌ нет (план требует: staged-diff с `<<<<<<<` — блок; авторство = Claude — блок; чистый коммит — пропуск) |
| Уникальность D-номеров (шаг внутри `gate.sh`, не отдельный скрипт) | ❌ нет (план требует: дублирующий `## D999.` в двух файлах → красный, кроме allow-листа) |

Мерило плана 231 §4в: **0 механизмов без самотеста.** На 2026-07-27 закрыт
только первый пункт таблицы — остальные шесть в очереди.
