<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# scripts/tools/ — разовые инструменты волн

В отличие от [`scripts/guards/`](../guards/README.md) (постоянные механизмы
принуждения, обязаны иметь самотест и быть подключёнными к `gate.sh`),
файлы здесь выполнили (или выполняют по требованию) миграцию/конвертацию
для КОНКРЕТНОГО плана и не обязаны иметь самотест или быть подключены
куда-либо — их можно запускать вручную, когда план того требует, и они
могут устареть, если корпус, который они мигрировали, уже весь мигрирован.
Не удалять не глядя — часть из них (`gen-plan-status.sh`,
`stress_bisect.sh`, `cdb_session.sh`) активно используется и сейчас.

| Файл | Что мигрировал / для чего | Волна |
|---|---|---|
| [`gen-plan-status.sh`](gen-plan-status.sh) | НЕ миграция — генератор `docs/plans/STATUS.md` из пофайловых `**Статус:**`-строк. Единственный не-разовый файл здесь (используется постоянно, но это генератор, не страж — поэтому не в `guards/`). | план 231 §0а / `docs/dev/conventions-governance.md` |
| [`window-status.sh`](window-status.sh) | НЕ миграция — факты о состоянии окна для команды `/status`: ветка и расхождение с `origin/main`, живые пункты плана и какие из них блокеры тега, последние закрытия, вердикт ВНЕШНЕГО гейта (авторитетного) и лишь затем локального, строки реестра без номера. Читает, не правит. Первая редакция — окно 274 (`c3d179ecf`); здесь добавлен раздел внешнего гейта: 2026-08-26 три пуша подряд ушли в main после локального «GATE OK» яруса `loop`, который корпус не судит вовсе (реестр 221.1 №770). | команда `/status` / реестр 221.1 №770 |
| [`controller-selftest.py`](controller-selftest.py) | НЕ миграция — **самотест роли контролёра**: читает `.claude/commands/controller.md` и проверяет, что требования в нём стоят (шапка, роль до процедуры, стыковка, время в обеих шкалах, форма доклада таблицей, форма толчка с третьей частью, три признака законной стоянки, «обещание не работа», «ответ владельцу не ход», заморозка дерева, отставание от `origin/main`, один дом правил, запрет русских слов в командной строке, наличие ВСЕХ инструментов (число выводится из списка, а не вписано: литерал «4» простоял час при шести существующих), строка «Модели агентов»), плюс что все они на месте и разбираются питоном. Отчёт — ТАБЛИЦА и итоговая строка; **судить по коду возврата запрещено в его же тексте** (блокер тега №1040: `nova test --filter` даёт `rc=0`, не выполнив ни теста). `--prove` строит для каждой проверки образец с удалённым свидетельством и требует, чтобы она ПОКРАСНЕЛА: замер 2026-09-08 — 16/16 прошли, 15/15 содержательных краснеют, бесполезных ноль. | роль контролёра, 2026-09-08 |
| [`controller-dock.py`](controller-dock.py) | НЕ миграция — **стыковка окна-контролёра** (`/controller`) после перезапуска: имена соседей `nova-XX` и id их сессий меняются, поэтому личность восстанавливается по свидетельствам, а не по памяти — дерево по частоте упоминаний в истории сессии, имя из её собственного `ListAgents` (последнее вхождение, поиск по ВСЕМУ файлу: в хвосте его может не быть), интегратор — по упоминаниям гейта и пуша, потому что своего дерева у него нет. Читает истории сессий, ничего не правит. | роль контролёра, 2026-09-08 |
| [`controller-peers-scan.py`](controller-peers-scan.py) | НЕ миграция — обзор N свежих историй соседних сессий для контролёра: тип последней записи, возраст, последний ввод владельца, последний текст ассистента. Отвечает на вопрос «кто встал», но НЕ судит: вердикт «встал или ждёт законно» принадлежит окну-контролёру. | роль контролёра, 2026-09-08 |
| [`controller-peers-deep.py`](controller-peers-deep.py) | НЕ миграция — разбор ОДНОЙ истории сессии глубоко (последние K вводов владельца с разметкой источника: владелец / сообщение соседа / прерывание / уведомление задачи, плюс последний текст). Нужен, когда обзор показал остановку и надо прочитать, чем окно её объяснило. | роль контролёра, 2026-09-08 |
| [`controller-machine-watch.py`](controller-machine-watch.py) | НЕ миграция — **дежурство за очередью машины** (просьба интегратора 2026-09-08): делит процессы на держателей слота (`gate.sh`, `nova test`, `nova.exe`, `novac.exe`, `cargo`, `clang`) и просто нагрузку (зависшие `tail -f`, осиротевший `cmd.exe` с `vcvars64`), приписывает процесс дереву по пути и по родителю. Три ловушки уже зашиты: наблюдатель исключает СВОИ команды (иначе доносит на невиновное окно), подкаталог главной копии (`nova/nova-cli/...`) не считается отдельным деревом, и снимок берётся ДВАЖДЫ с паузой — одиночный `ps` попадает в промежуток между потомками гейта и объявляет занятую машину свободной. | роль контролёра, 2026-09-08 |
| [`controller-verdict.py`](controller-verdict.py) | НЕ миграция — **вердикт по каждому соседнему окну ПО ПРИЗНАКАМ, а не по смыслу его текста** (слово владельца 2026-09-09, 14:05 мест.: «я тебе не верю, наши скрипт на питоне согласно всех правил и им проверяй и толкай»). За смену контролёр трижды поставил работающий статус стоящему окну — сперва по возрасту записи, потом по служебному типу записи, потом по СОДЕРЖАНИЮ доклада («беру их по порядку» прочитано как работа), и каждый раз правило уже было написано. Здесь оно стало программой: закрытый список признаков — процесс СВОЕГО дерева держит слот (ярус отличается от сборки, иначе секундный `nova build` объявляется гейтом), `tool_use` в ПОСЛЕДНЕЙ записи, коммит дерева новее последнего текста, незакоммиченный файл новее его; ничего больше признаком не является. Печатает признак, на котором стоит статус, и строку `PUSH:`. **Свой предел называет сам:** отвечает «есть ли признак работы», а НЕ «законна ли стоянка» — окно, стоящее по слову владельца или под чужим слотом, выходит как `STOOD`, и толкать его нельзя. `--prove` — проба в обе стороны на восьми синтетических случаях, включая «обещание в тексте не признак» и «сборка не ярус». | роль контролёра, 2026-09-09 |
| [`prove-owner-forms.py`](prove-owner-forms.py) | НЕ миграция — **проба приписки процесса дереву на ШЕСТИ формах записи одного пути** (роль контролёра, 2026-09-09). Заведена после того, как дежурство дважды за день назвало держателем слота дерево `nova-cli`, которого нет ни в одном чекауте: путь был ОТНОСИТЕЛЬНЫЙ (`./nova-cli/target/release/nova`), а снятие подкаталогов главной копии требовало абсолютной формы. Прежняя проба держалась 4/4, потому что все четыре её случая были абсолютными. Случаи A–C — что сужение обязано поймать (главная копия в обоих написаниях и относительно), D–F — чего оно касаться НЕ должно (настоящий worktree, worktree с тем же подкаталогом внутри, worktree, чьё имя не начинается на «nova»). Случай F вскрыл второй дефект: имена деревьев ПЕРЕЧИСЛЯЛИСЬ шаблоном, и `claude-limits` был шаблону невидим — теперь список выводится с диска. Случай B ожидает `?`: у относительного пути имени дерева нет вовсе, оно приходит из `cwd` процесса, и неизвестность обязана называться неизвестностью. | роль контролёра, 2026-09-09 |
| [`gen-pow5-table.py`](gen-pow5-table.py) | НЕ миграция — генератор и пересчётчик `std/src/runtime/float_parse/pow5_table.nv` (651 строка × два слова u64, степени пяти для Eisel-Lemire): `--write` пишет файл, `--check` пересчитывает точными целыми и сверяет побитно (его зовёт страж `guards/check-pow5-table.sh`), `--against-rust` сверяет сам генератор с донорской `table.rs`. Таблицей владеет скрипт, руками её не правят. | план 283 Ф.1 |
| [`fetch-parse-float-corpus.sh`](fetch-parse-float-corpus.sh) | НЕ миграция — загрузка внешнего корпуса разбора float (`github.com/nigeltao/parse-number-fxx-test-data`, ~5,3 млн строк) по зашитому хешу коммита в gitignored-кэш `nova_tests/.cache/parse-number-fxx/` (или `$NOVA_PARSE_FLOAT_CORPUS`); идемпотентен. Читает его медленный тест `std/src/runtime/parse_float_corpus_slow.nv`. Корпус в репозиторий не кладём — в дереве скрипт и хеш. | план 283 Ф.5 |
| [`catb_convert.py`](catb_convert.py) | Конвертация `nova_tests/` на folder-module модель: переименование конфликтующих top-level имён внутри файла, `module X.stem` → `module nova_tests.X`, перенос `EXPECT_COMPILE_ERROR`-файлов в `neg/`. | Cat-B (Plan 169.1 — folder-module для 75 директорий с конфликтующими именами) |
| [`d78_audit_migrate.py`](d78_audit_migrate.py) | Аудит + миграция module-деклараций на форму rev-3 (`module = parent.target`, `internal/`-спецкейс `owner.internal.target`). | «D78 rev-3» (module-declaration rule, см. spec/decisions/07-modules.md) |
| [`migrate_modules_rev3.ps1`](migrate_modules_rev3.ps1) | PowerShell-миграция module-деклараций rev-1 → rev-3 для workspace-members `std`/`nova_tests` (более ранняя волна той же темы, что `d78_audit_migrate.py`; пути хардкожены — bootstrap-скрипт). | Plan 42 Sub-plan 42.6 (ЗАКРЫТ 2026-05-13) |
| [`demojibake.py`](demojibake.py) | Разовый/переисполняемый чинитель двойного mojibake (UTF-8 прочитан как cp1251) в русскоязычных комментариях/строках компилятора. | Повод — GitHub issue #1 (см. `docs/project-creation.txt`, `docs/dev/simplifications.md`) |
| [`plan114_rewrite.py`](plan114_rewrite.py) | Массовый regex-рерайт `.nv`-корпуса, правила R1–R12 (`let`→`ro`/`mut`, `if let`/`while let` → без `let`, `readonly`→`ro`). | Plan 114 (keyword refresh ro/mut/no-let) |
| [`plan114_apply_md.py`](plan114_apply_md.py) | Применяет `plan114_rewrite.py` к `docs/**/*.md` + `spec/**/*.md`, исключая `history/`. | Plan 114 |
| [`plan114_rust_nova_body.py`](plan114_rust_nova_body.py) | Переписывает встроенные Nova-строки внутри `nova_body: Some("...")` в Rust-исходниках на новый синтаксис ключевых слов. | Plan 114 Ф.2 / D184 |
| [`plan114_4_stmt_const_arms.py`](plan114_4_stmt_const_arms.py) | Добавляет соседние `Stmt::Const(_)` match-руки рядом с `Stmt::Let(...)` в `compiler-codegen/src/types/mod.rs` по указанным номерам строк. | Plan 114.4 Ф.2 |
| [`cdb_session.sh`](cdb_session.sh) | Сессия Windows kernel debugger (`cdb.exe`) для локализации кадра-виновника стохастического crash в M:N-рантайме. | Plan 83.11 §12.30, `[M-83.11-supervised-spawn-cancel-memcpy-segv]` |
| [`stress_bisect.sh`](stress_bisect.sh) | `git bisect run`-совместимый stress-harness для стохастических SEGV/hang concurrency-тестов; переиспользуемый (не только для бага, на котором родился). | Plan 83.11 §12.27, ссылается `docs/dev/debugging-races.md` |
| [`setup_worktree_p118.sh`](setup_worktree_p118.sh) | Копирует уже инициализированный `libuv`-submodule + prebuilt `libuv.lib` из главной репы в `nova-pNN`-worktree той же репы (экономит ~30с submodule-init на каждый worktree) + печатает `NOVA_GC_*`-env-переменные. | Plan 118 (typed pointers / unsafe) |

## Замечено при переносе (2026-07-27), не исправлялось (логику не менять)

- `cdb_session.sh` и `stress_bisect.sh` в собственных докстрингах называют
  себя `tools/cdb_session.sh` / `tools/stress_bisect.sh` — уже расходилось
  с фактическим путём ДО этой волны (файлы всегда лежали в `scripts/`, не
  в `tools/` на верхнем уровне репы); теперь, после переноса, путь
  `scripts/tools/...` почти совпал с докстрингом случайно. Не правилось —
  не в мандате этой волны (менять логику/докстринги скриптов запрещено
  правилами задания, кроме шапок стражей).
- `docs/plans/114.4-const-narrow-generalize-fn.md:295` ссылается на
  `scripts/plan114_4_const_rewrite.py` — файла с таким именем НЕТ ни здесь,
  ни раньше не было в репе (ближайший — `plan114_4_stmt_const_arms.py`).
  Похоже на разошедшееся упоминание из старой редакции плана, не связанное
  с этим переносом — оставлено как есть, не в мандате этой волны.

## Пути после переезда (2026-07-27)

Все файлы раньше лежали в `scripts/`. Скрипты, вычислявшие свой корень репы
от собственного расположения, обновлены на `.../../..` вместо `.../..`
(`gen-plan-status.sh`, `setup_worktree_p118.sh`, `migrate_modules_rev3.ps1`,
`catb_convert.py`). `plan114_apply_md.py` жёстко звал соседний
`plan114_rewrite.py` по относительному пути `scripts/plan114_rewrite.py` —
обновлено на `scripts/tools/plan114_rewrite.py`. Остальные файлы не
вычисляют собственное расположение (принимают пути аргументами или
рассчитывают на CWD = корень репы при запуске) — переезд их не затронул
функционально, только docstring-примеры использования.
