# Решения по дизайну Nova

Журнал принятых решений по дизайну языка с обоснованиями.

> **Структура.** Раньше всё лежало в одном файле `decisions.md` (~6000
> строк, 48 D-решений по хронологии). Теперь разбито по темам — каждый
> файл описывает одну область, решения внутри расположены логически
> (от общего к частному), а не хронологически.
>
> История эволюции (что менялось, что отменялось) — в `history/`.

## Тематические разделы

| # | Файл | Что внутри | D-решения |
|---|---|---|---|
| 01 | [01-philosophy.md](01-philosophy.md) | Цели, парадигма, AI-first | D1, D9, D10 |
| 02 | [02-types.md](02-types.md) | Record, sum-type, protocol, generic, поля, bounds, ghost state, hybrid dispatch, tuple mono, named tuples, symmetry decl↔literal, signed indexing, debug formatting protocol (D229), Clone + auto-derive (D230) + value-record (D228), RawMem allocator API (D231), Vec[T] (D232) | D15, D17, D32, D36, D39, D42, D52, D53, D55, D66, D72, D110, D119, D122, D123, D142, D215, D216 typed-pointers (V2 + V3 amend), D354 (anon-tuple mono, ex-D216), D226, D228, D229, D230, D231, D232, D246 (three-axis mutability: L1 binding / L2 view / L3 pointee; restores D216 §V2.6 `*T≡*ro T`; flip-scan-draft RETRACTED) |
| 03 | [03-syntax.md](03-syntax.md) | Объявления, литералы, операторы, методы, парсинг, defer/defer(o) (errdefer ретрактнут — D189/D314), атрибуты `#name`, default generics, select, named params, map-literal, doc-comments, size accessors, static-dot в protocol, numeric literal inference | D16, D19, D20, D22, D23, D27, D30, D33, D34, D35, D37, D38, D40, D43, D44, D45, D46, D48, D49, D54, D58, D59, D60, D69, D82, D83, D88, D90, D94, D96, D102, D104, D108, D117, D143, D227 |
| 04 | [04-effects.md](04-effects.md) | Fail, Io, Db, effect-литерал, with-блоки, interrupt, forbid, realtime, ?, `Effect[E, IRT]`, contracts (#pure/axiom/trusted), axiom binder, Fail payload, std/net effects (TcpNet/UdpNet/AddrNet/DnsNet, split, polish), `#default_handler` | D2, D3, D4, D11, D12, D18, D25, D28, D31, D61, D62, D63, D64, D65, D67, D68, D85, D86, D87, D115, D118, D120, D282, D291, D295, D298, D301, D302, D431 |
| 05 | [05-memory.md](05-memory.md) | Managed GC, escape analysis, regions | D6, D21 (cancelled) |
| 06 | [06-concurrency.md](06-concurrency.md) | Fiber runtime, structured concurrency, spawn, detach, supervised(cancel:), channels (Channel revision capability-split), select, handler scoping, park/wake API, implicit main-scope, fiber stack allocation, work-stealing scheduler, preemption | D14, D50, D71, D75, D79, D80, D91, D92, D93, D97, D98, D103 |
| 07 | [07-modules.md](07-modules.md) | Модули, импорты (включая селективный `import X.{A, B}` и `export import` re-export), видимость, package tooling, `_module.nv`, межпакетные зависимости, version-диапазоны, effect-aware deps, prelude attrs, partial-prelude декомпозиция (core↔unicode) | D5, D29, D47, D78, D100, D138, D139, D140, D174, D308 |
| 08 | [08-runtime.md](08-runtime.md) | Panic, capability, deployment, prelude, From/Into+TryFrom/TryInto (⛔ ретрактированы 2026-07-06 — остались имена-конвенции .from/.try_from и consume @into_*), math, Mem, assert, primitive built-ins (hash/eq/ord), примитивы доступа к памяти, field caching, LICM, pure-call cache, IPA | D7, D13, D26, D41, D70 (replaced → D73), D73, D74, D76, D77, D81, D109, D141, D217, D218, D219, D223 |
| 09 | [09-tooling.md](09-tooling.md) | Тесты, контракты, форматирование, CLI, EXPECT-маркеры, conditional compilation, `#doc`, doc-attrs, doc-tests, JSON schema, contracts verifier (assume/quantifiers/cache/Z3), bench DSL | D24, D89, D95, D99, D101, D105, D106, D107, D111, D112, D113, D114, D116, D121 |
| 10 | [10-overloading.md](10-overloading.md) | Перегрузка функций и методов: четыре оси, резолв | D84 |

### Свежие D-решения (по нумерации)

| D# | Файл | Что |
|---|---|---|
| D85 | 04-effects.md | Операторы `?` и `!!` — унифицированное поведение для `Result` и `Option`; **AMEND 2026-07-07: метод-близнец `@unwrap()` ретрактирован** (`.unwrap()` → `!!`, D9) |
| D86 | 04-effects.md | `??` coalesce-оператор — fallback для `Result`/`Option` без `Fail`; **AMEND 2026-07-07: близнецы `@unwrap_or`/`@unwrap_or_else` ретрактированы** (`?? v` ленивый, покрывает оба) |
| D87 | 04-effects.md | `Handler[E, IRT]` — параметризация handler типом interrupt'а |
| D88 | 03-syntax.md | Default-значения generic-параметров (`Handler[E]` ≡ `Handler[E, never]`) |
| D89 | 09-tooling.md | Test-tooling конвенции — `EXPECT_*` маркеры для negative-тестов |
| D90 | 03-syntax.md | `defer` — scope-level cleanup statement (`errdefer` ретрактнут D189; замена — `defer(o ScopeOutcome)`, D314) |
| D91 | 06-concurrency.md | Channel revision — capability-split на `ChanWriter` / `ChanReader` |
| D92 | 06-concurrency.md | Top-level `main` как implicit supervised scope |
| D93 | 06-concurrency.md | Park/wake — нормативный runtime primitive для блокирующих операций |
| D94 | 03-syntax.md | `select { ... }` — multiplexed channel operations |
| D95 | 09-tooling.md | CLI path конвенции — `nova check <path>` / `nova test <path>` |
| D96 | 03-syntax.md | Синтаксис атрибутов — `#name` без квадратных скобок (`#realtime`, `#pure`) |
| D97 | 06-concurrency.md | Fiber stack allocation — per-thread mmap arena (Linux/macOS) / calloc (Windows) |
| D98 | 06-concurrency.md | Work-stealing scheduler — per-worker runqueue, global queue fallback |
| D99 | 09-tooling.md | Conditional compilation — `#if platform(...)` / `#if feature(...)` атрибуты |
| D100 | 07-modules.md | `_module.nv` — entry-point файл для folder-module |
| D101 | 09-tooling.md | `#doc` атрибут — structured documentation comments |
| D102 | 03-syntax.md | Named parameters — `f(x: 1, y: 2)` синтаксис вызова |
| D103 | 06-concurrency.md | Preemption — cooperative yield-points в fiber tight loops |
| D104 | 03-syntax.md | Синтаксис doc-comment'ов — `///` outer, `//!` inner |
| D105 | 09-tooling.md | Doc-атрибуты — `#doc(section = "...", example = "...")` |
| D106 | 09-tooling.md | Семантика doc-test'ов — `nova doc --test` |
| D107 | 09-tooling.md | JSON output schema v1 — `nova doc --json` |
| D108 | 03-syntax.md | Map-literal `[k: v]` синтаксис + map-coercion (D55 rev) |
| D109 | 08-runtime.md | Встроенные методы примитивных типов — hash, eq, ord |
| D110 | 02-types.md | Ghost state — spec-only bindings для SMT-верификации |
| D111 | 09-tooling.md | `assume` / `assert_static` / `#trusted` external |
| D112 | 09-tooling.md | Bounded quantifiers (`forall`/`exists` по коллекции) |
| D113 | 09-tooling.md | `#must_verify_module` — strict mode на модуле |
| D114 | 09-tooling.md | SMT cache + parallel verification |
| D115 | 04-effects.md | Axiom binder — `BinderType` enum вместо `Option<TypeRef>` |
| D116 | 09-tooling.md | Z3 backend через собственные FFI-биндинги |
| D117 | 03-syntax.md | Size-like accessors require call syntax (`.len()` vs `.len`) |
| D118 | 04-effects.md | Typed `Fail[E]` codegen — payload preservation via fail-frame |
| D119 | 02-types.md | Method-level type parameters в generic methods |
| D120 | 04-effects.md | `#pure` views + axioms + `#verify`/`#trusted` handlers |
| D121 | 09-tooling.md | Benchmark DSL — `bench "..." { measure { ... } }` + `bench.*` namespace |
| D122 | 02-types.md | Hybrid dispatch для bound-K methods (vtable infra) |
| D123 | 02-types.md | Tuple monomorphization — per-concrete-combo structs |
| D124 | 06-concurrency.md | Monotonic vs Timestamp — раздельные типы для wall-clock и монотонных часов |
| D369 | 07-modules.md | Межпакетный импорт — только через объявленную `[dependencies]`-зависимость (Plan 03.1) |
| D139 | 07-modules.md | Version-диапазоны git-зависимостей — резолв по тегам репозитория (Plan 03.2) |
| D140 | 07-modules.md | Effect-aware зависимости — effect-surface, effect-diff, `forbid` на границе (Plan 03.4) |
| D141 | 08-runtime.md | Примитивы доступа к памяти — `str.byte_at`, bulk slice-операции `[]T`, `compare` (Plan 90); append/insert API `append`/`insert`/`reserve` (renamed Plan 91 2026-05-30; ранее `extend_from`/`insert_from`), `copy_from` hardening + W_VIEW_EXTEND_DETACH lint (Plan 90.1 amend 2026-05-27); `fill` memset fast-path для single-byte T + `append_zero(n)` polymorphic zero-extend (Plan 90 followup 2026-06-01) |
| D142 | 02-types.md | Симметрия effect/protocol declaration ↔ literal — keyword `handler`→`effect`, `Handler[E,IRT]`→`Effect[E,IRT]`, анонимный protocol-литерал, capture-rules (Plan 97) |
| D143 | 03-syntax.md | Static-method префикс `.method()` в `type X protocol { ... }` теле — симметрия с D35 instance-методом `fn T.method()` (Plan 97) |
| D144 | 02-types.md | Sub-slice views `arr[a..b]` / `str[a..b]` — `cap == len` model, 5 форм Range (RangeBounds parity), bounds-check для raw `arr[i]` (Plan 96) |
| D133 | 02-types.md | type-level `consume` — обязательная consume-семантика (Plan 100.1, proposed Ред. 2 2026-05-24); расширяет D131 affine с противоположной стороны (must-be-consumed). Унифицированная модель «view-default, `consume`-explicit». Foundation Plan 100 family |
| D156 | 02-types.md | Generic `[T consume]` bound + collection-aware iteration с 3 mode'ами (`for tx` view / `for mut tx` mut-view / `for consume tx` consume) — Plan 100.2 proposed Ред. 2 |
| D157 | 05-memory.md | Implicit view default + closure capture analysis + `match consume` syntax (Plan 100.3, proposed Ред. 2 2026-05-24). `view T` keyword отвергнут — view это no-qualifier default везде. Automatic closure-mode detection (view / mut-view / consume) |
| D158 | 03-syntax.md | Failable cleanup body (Plan 100.4.1, ✅ закрыт 2026-05-26) — amend D90 §4; defer/errdefer body может Fail; Plan 49 multi-error composition; MultiError prelude type; runtime helpers nv_compose_suppressed / nova_rethrow_with_suppressed |
| D159 | 03-syntax.md | Async/suspend в cleanup body (Plan 100.4.2, ✅ закрыт 2026-05-26) — amend D90 §5; checker сняла suspend-call ban; spawn/parallel for/supervised/detach/blocking keep banned (E D159-spawn-in-defer); cancel-shielding runtime — [M-100.4.2-cancel-shielding] followup |
| D160 | 03-syntax.md | `okdefer` + reason-aware `defer \|result\|` (Plan 100.4.3, proposed); complement к errdefer; mixed LIFO defer family |
| D161 | 03-syntax.md | Multi-defer LIFO error accumulation + panic-in-defer composition (Plan 100.4.4, ✅ закрыт 2026-05-26) — amend D90 §«panic»; codegen per-defer NovaFailFrame wrap; LIFO continues после partial failure; no Rust double-panic-abort; closes [M-100.4.1-emit-defer-wrap] |
| D162 | 03-syntax.md | Consume-integration final (Plan 100.4.5, ✅ закрыт 2026-05-26 bootstrap MVP) — check_consume + d162_mark_defer_cover распознаёт defer/errdefer/okdefer cover; leverage Plan 100.8 D166; D90 §7 amend (interrupt → errdefer) — P2 BREAKING [M-100.4.5-d90-§7-interrupt-errdefer] followup |
| D163 | 02-types.md | 🔴 **RETRACTED** (Plan 91.10, 2026-05-30) — `needs <Cap>` capability syntax удалён, redundant с effect system. FFI consume integration — type-driven, без `external consume fn` keyword (Plan 100.5, proposed Ред. 2 2026-05-24). Унифицировано с regular fn: return-type carries consume-ness; `consume` keyword только на params/receivers (D131 semantic) |
| D164 | 02-types.md | Cross-module consume — visibility, mangling-bit extension Plan 81 D134, package contracts (Plan 100.6, proposed) |
| D165 | 09-tooling.md | Consume-types migration policy — `nova consume-migrate` CLI + edition versioning (Plan 100.7, proposed) |
| D166 | 09-tooling.md | Consume-types developer experience — perf budget <5%, LSP quick fixes (12 error codes), hover info, `nova doc` integration, structured diagnostic format (Plan 100.8, proposed) |
| D371 | 07-modules.md | Prelude control attributes (ex-D174, renumber 2026-07-03; Plan 107, 2026-05-27) — `#no_prelude` / `#prelude(names)` / `#allow(shadow)` перед `module`; amends D78 §«Prelude opt-out» (Plan 62.F/62.F.bis inline-клаузы удалены); extends D100 `_module.nv` prelude inheritance + pre-scan fix |
| D180 | 05-memory.md | `consume` binding syntax (Plan 73.1, 2026-05-28) — V2 extension D131 на let-binding site. 3 правила: Rule 1 `consume X = expr` обязателен на consume-RHS (E_CONSUME_KEYWORD_MISSING); Rule 2 `let Y = consume_var` запрещён в теле (E_VIEW_BINDING_FORBIDDEN; views только как function-params); Rule 3 `consume Y = consume_var` = MOVE. Restriction-based safety без lifetime annotations |
| D354 (ex-D216, renumber 2026-07-03) | 02-types.md | Generic anonymous tuple monomorphization (Plan 59.1, 2026-06-01) — `fn[T] f() -> (A[T], B[T])` мономорфизируется per instantiation через mono'd schema `_NovaTuple_<arity>_<L1>_<T1>_..._<LN>_<TN>` (length-prefixed mangling). Closes gap в Plan 59 Ф.7.5 (Result mono landed; general anonymous tuple оставался под V1 erasure fallback). Edge cases: multi-instantiation / multi-param / nested tuple / tuple-in-Option ✅; `[]MonoTuple` / `pair.OOB` / Channel.new ad-hoc cleanup — followups. D91 signature now буквально implementable |
| D216 V2 amend | 02-types.md | Universal right-binding rule + first-class `unsafe T` wrapper (Plan 118.5 V2, 2026-06-04) |
| D216 V3 amend | 02-types.md | 4 modifier composition rules + `safe` keyword + binding-context relaxation (Plan 118.5 V3 Ф.0-Ф.6, 2026-06-04/06-05) |
| D220 | 02-types.md | Per-field visibility `priv` keyword + type-level default flip (Plan 124.1, 2026-06-02) — per-field `priv mut money f64` modifier + type-level `type X priv {}` flip syntax с `pub` field override. Default public (D47 unchanged, validated kubernetes 92% public в API surface). 4 error codes E_PRIV_FIELD_{READ,WRITE,INIT,PATTERN} (V1 parser/AST landed; enforcement = followup `[M-124.1-checker-enforcement]`). E_PRIV_PUB_CONFLICT bidirectional. Replaces deprecated `_prefix` convention (D47 amend 2026-06-02). No reflection backdoor — compile-time hard guarantee |
| D217 | 08-runtime.md | Method-local receiver field caching V1 (Plan 123.1, 2026-06-02) — AST-pass `field_cache::cache_module` injects `ro _at_<F> = @<F>` prefix-let'ы для ro-fields (unconditional cache across entire body) и mut-fields (first-region cache до first write/call boundary) accessed ≥ threshold (default N=2). Устраняет redundant `self->X` derefs в `.c` output (15-30% reduction на hot-path methods в `-O0` builds). Pure AST→AST трансформация: semantic equivalence guaranteed (5 verification methods). Closure capture / protocol-receiver / consume-field / embed-field — все skipped (conservative aliasing safety). Escape hatch: `NOVA_FIELD_CACHE=0`. Tunable: `NOVA_FIELD_CACHE_THRESHOLD/_MAX`. V2-V7 (LICM / `#pure` cache / chain / LSP / telemetry / IPA) — sub-plans 123.2-123.7 (extension D218-D219 + amends). 18/18 plan123_1 fixtures PASS identically под ON и OFF (semantic equivalence differential) |
| D218 | 08-runtime.md | LICM для receiver fields V2 (Plan 123.2, 2026-06-02) — `licm_fn`/`licm_block` phase в field_cache.rs runs BEFORE D217 per-fn caching; hoist'ит invariant `@<F>` reads из loop bodies (for/while/loop/while_let) в `ro _at_<F>_loop = @<F>` immediately before the loop в enclosing block. Eligibility: ≥ licm_threshold reads + no @F write + no closure-capture + no spawn/parallel-for в body; для mut fields — no Call в body (V2 conservative). Composition с D217 — LICM first, D217 second; no double-cache (LICM replaces reads inside body). Escape hatches: `NOVA_FIELD_CACHE_LICM=0` (LICM-only), `NOVA_FIELD_CACHE=0` (both). 14/14 plan123_2 fixtures PASS identically под всеми 3 configurations (default / LICM=0 / FIELD_CACHE=0). ParallelFor / Supervised / Spawn / Detach / Blocking bodies skipped (concurrent aliasing safety) |
| D219 | 08-runtime.md | Pure-call result caching V3 (Plan 123.3, 2026-06-02) — Nova-edge feature leveraging effect system. `pure_cache_fn` phase в field_cache.rs caches `@<pure_method>()` results when method has `Purity::Pure` (D24 annotation/inference) + args-less + receiver Instance + receiver type не Protocol. Composition order: D218 LICM → D219 pure-cache → D217 per-fn (three distinct AST patterns, no overlap). Conservative invalidation V3: ANY `@F = ...` write anywhere в body → skip all pure-cache (V3.1 followup `[M-123.3-frame-based-invalidation]` refines с D24 f.reads frame). Naming `_at_<method>_call` (distinct от D217 `_at_<F>` и D218 `_at_<F>_loop`). Escape hatch `NOVA_FIELD_CACHE_PURE=0`. 12/12 plan123_3 fixtures PASS identically под default и PURE=0 (semantic equivalence). V3.1 enhancements deferred: args-with-literals + frame-based invalidation |
| D217 amend V4 | 08-runtime.md | Chain caching `@a.b.c` (Plan 123.4, 2026-06-02) — extends D217 baseline для nested chain access patterns. `chain_cache_fn` phase caches multi-segment paths (depth 2-4) accessed ≥ chain_threshold times. Composition: D218 LICM → V4 chain → D219 pure → D217 V1; four distinct cache naming patterns (`_at_<F>` / `_at_<F>_loop` / `_at_<M>_call` / `_at_<a>_<b>[_<c>[_<d>]]_chain`), no overlap. Critical detection rule: method-dispatch `@a.b.method()` ≠ chain (recurse into receiver `obj` of Call.func Member, не into func itself). Conservative invalidation: any @F write skips chain caching. Escape hatch `NOVA_FIELD_CACHE_CHAIN=0` + tunable `_CHAIN_THRESHOLD` / `_CHAIN_DEPTH`. 10/10 plan123_4 fixtures PASS identically под default и CHAIN=0 (semantic equivalence). V4.1 followups: per-segment invalidation via f.reads + chain prefix sharing |
| D217 amend V5 | 08-runtime.md | Diagnostic mode + LSP integration (Plan 123.5, 2026-06-02) — `analyze_module(&Module, &Config) -> ExplainReport` API runs cache analysis на cloned AST без mutation, returns per-fn ExplainReport (ro/mut/licm/pure/chain breakdown). CLI flag `nova check <files> --explain-cache` emits human-readable diagnostic per fn (uses nova-cli's resolve_imports + type-check pipeline). User-facing doc `docs/guide/field-cache-optimization.md` explains 4 layers + escape hatches + perf expectations + semantic equivalence guarantee. LSP code-lens + hover provider deferred ([M-123.5-lsp-codelens] + [M-123.5-lsp-hover]) — infrastructure ready, integration straightforward |
| D217 amend V6 | 08-runtime.md | Telemetry + production rollout (Plan 123.6, 2026-06-02) — `nova check <files> --telemetry-cache` (+ `--telemetry-json`) aggregates corpus-wide metrics: methods_total / methods_affected / pct / median+p99 caches/fn / per-layer D217+D218+D219+chain breakdown. JSON output для CI integration. Production migration guide `docs/migration/123-field-cache.md` covers baseline differential strategy + regression detection workflow + CLI flags reference + FAQ. Env vars cover все config; full `--field-cache-*` CLI flag set deferred V6.1 ([M-123.6-cli-flags-full]); CI perf regression gates с Plan 57 bench — followup ([M-123.6-ci-perf-gates]) |
| D217 amend V6.1 | 08-runtime.md | Sugar CLI flags + CI perf gate (Plan 123.6.1, 2026-06-02) — 12 global CLI flags translate к NOVA_FIELD_CACHE_* env vars (--no-field-cache + 4 layer-specific + --field-cache-threshold/-licm-threshold/-pure-threshold/-chain-threshold + --field-cache-max/-licm-max/-chain-depth). CI perf gate via `--telemetry-baseline=FILE` compares current telemetry с baseline JSON; exit 1 if methods_affected_pct drops > 5pp OR caches_total drops > 10%. parse_json_number minimal extractor (no serde_json dep). Workflow: capture baseline in main → compare in PRs. V6.2 followup: Plan 57 nova bench CPU integration |
| D217 amend V5.1 | 08-runtime.md | LSP code-lens + hover (Plan 123.5.1, 2026-06-02) — `nova-lsp::server::Backend` extended с `code_lens_provider` + `hover_provider`. `compute_field_cache_lenses(src) -> Option<Vec<CodeLens>>` returns per-method lens с title "N cache(s): ro=X mut=Y licm=Z pure=W chain=V". `compute_field_cache_hover(src, pos)` returns markdown с cache origin info для `@<field>` token at hover position. Both use V5 `analyze_module` API. 3/3 integration tests PASS (nova-lsp/tests/field_cache_lens.rs). V5.2 semantic tokens + V5.3 quickfix `#pure` followups deferred |
| D219 amend V3.1 | 08-runtime.md | Pure-call literal args extension (Plan 123.3.1, 2026-06-02) — расширяет V3 args-less к args-with-literal-arguments (IntLit/FloatLit/StrLit/BoolLit/CharLit/UnitLit/NullPtrLit + Unary-Neg на literals). PureCallKey {method, args_key} canonical encoding (e.g. `_at_scaled_2i_call` для `@scaled(2)`); non-literal args fallback к V3 (not cached). capture_sample_args_in_body saves original args для prefix-let reconstruction. rewrite_pure_calls_in_*_v31 matches by canonical key. Orthogonal к V3.1 frame-based invalidation (delivered как part of V7.1). 4/4 plan123_3_1 fixtures PASS (literal int/bool + different args + non-literal skip negative) |
| D221 | 02-types.md | Pattern destructure + literal spread edge cases (Plan 124.2, 2026-06-02) — extends D220 §4 на дополнительные pattern sites (Match arms / IfLet / WhileLet / For / ParallelFor + nested Pattern::Record recursive descent), formalizes `..` rest pattern non-binding semantics, и вводит E_PRIV_FIELD_INIT_SPREAD для `Type { ...other }` outside type-method scope (spread copies all fields including priv). Helper `check_priv_pattern_recursive`. 14/14 plan124_2 fixtures PASS |
| D222 | 02-types.md | Named tuple priv + protocol impl boundary (Plan 124.4, 2026-06-02) — extends D220 на named-tuple form (D215): per-field `priv` modifier; `Vec3(priv x f64, ...)` syntax с `E_PRIV_PUB_CONFLICT` mutual exclusion; READ через f3_check_member NamedTuple arm; INIT через f5_check_tuple_construct named-args check; PATTERN через unified `check_priv_pattern_recursive` (Record+NamedTuple). §3 protocol impl boundary: type-method gets priv (recv = T), external free-fn blocked. 10/10 plan124_4 PASS, Plan 120 backward-compat 8/8 unchanged |
| D223 | 08-runtime.md | IPA — Inter-Procedural Analysis V7 (Plan 123.7, 2026-06-02) — `module_write_sets(&Module) -> HashMap<(type, method), HashSet<field>>` public API computes per-method field-write-set via direct write detection + iterative call closure (≤10 iterations transitive). V7 ships infrastructure + public API; V1 mut barrier integration (replacing block_contains_call с IPA-aware check) deferred V7.1 ([M-123.7-full-integration]). Conservative fallback: unknown methods treated как writing all fields (no regression). Cross-module/link-time IPA deferred indefinitely (per-module covers practical hot paths). 1 regression fixture; further cross-module testing requires V7.1 integration. Closes umbrella V7 milestone — Plan 123 family complete. **Renumbered от D220 → D223 после merge (Plan 124 owns D220-D222)** |
| D223 amend V7.1 | 08-runtime.md | IPA full integration (Plan 123.7.1, 2026-06-02) — расширяет V7 infrastructure across all 4 cache layers. IpaCtx struct threads write_sets + recv_type + read_sets. Layer integrations: V1 mut path (count_mut_prefix_reads_with_ipa + IPA-aware barrier helpers); V2 LICM (collect_loop_eligible_fields uses IPA per-field); V3.1 pure-cache frame-based (build_read_set_registry parallel infrastructure; per-method check body_writes ∩ M.read_set); V4.1 chain per-segment (chain path invalidated only if ANY segment ∈ body writes). Thread-local IPA contexts (LICM_WRITE_SETS / PURE_IPA_CTX / CHAIN_IPA_CTX) set by *_with_ipa wrappers — avoids ~20 fn signature refactors. Escape hatch `NOVA_FIELD_CACHE_IPA=0` returns V1-V6 conservative. 10/10 plan123_7_1 fixtures PASS identically под default и IPA=0. Acceptance A7.1.1-A7.1.10 met |
| D224 | 02-types.md | Priv-escape hatches `#test_access` + `#visible_to` (Plan 124.6, 2026-06-02; **renumbered from D223 → D224 after collision с Plan 123 IPA**) — fn-level `#test_access(TypeX, TypeY...)` grants priv access body fn ко всем listed types (test escape hatch); field-level `#visible_to(OtherType)` grants methods OtherType access ко marked priv field (per-field friend declaration). Combined predicate `priv_field_access_allowed(tname, &visible_to)` — type-method-scope OR test_access OR visible_to. RAII `PrivTestAccessGuard` tracks current_fn_test_access. 7/7 plan124_6 PASS (4 positive + 3 negative). Reuses Plan 124.1-124.4 error codes; hints mention escape hatches |
| D225 | 02-types.md | Type-level priv flip для named tuples (Plan 124.7, 2026-06-02; **renumbered from D224 → D225 after collision с Plan 123 IPA**) — `type Secret priv (key str, salt str)` symmetric extension D220 §3.3.1 на named tuple form (D215). Existing `TypeDecl.default_field_priv` re-used; new parser shim `parse_named_tuple_fields_with_default(default_priv)` propagates type-level default к per-field resolution. Explicit `pub` overrides; `priv pub`/`pub priv` → E_PRIV_PUB_CONFLICT. **RETRACT 2026-06-02 (Plan 124.8 Ф.1):** type-level priv flip для tuples retracted — tuples всегда all-public. Records preserve D220 §3.3.1. |
| D226 | 02-types.md | Signed indexing convention — `int` для `len`/`capacity`/index (2026-06-03; amend 2026-06-03 §5+§7 pointer-aware; **RETIRED (Plan 133, 2026-06-09): `usize`/`isize` удалены, `int` = address-sized signed integer**) — Все publish stdlib API сайзов/позиций используют signed `int` (= `intptr_t` = i64 на 64-bit). Capacity-API получают `requires n >= 0`. `usize`/`isize` больше не валидные Nova-типы — E_TYPE_UNKNOWN с hint «use `int`». |
| D227 | 03-syntax.md | Numeric literal inference — default `int`, context coercion, hard range-check (2026-06-03; amend 2026-06-03 Rule 7 pointer-aware) — amend D44 §«default-типы». 4 numeric rules + 1 pointer rule: (1) default без context → `int` (= i64 per D129); (2) context coerce без cast; (3) compile-time `E_LIT_OUT_OF_RANGE` на overflow (`ro a i32 = 3_000_000_000` → error); (4) negative literal в unsigned context = hard error, не wrap; **(7) Pointer-typed positions: литерал НЕ coerce'ится в pointer type — требуется `0 as ptr` / `0x1000 as *T`; `null ptr` retracted (D214 amend, Plan 118 A23) → `(0 as ptr)`; offset в `ptr + 1` остаётся `int` per Rule 1, scaled by sizeof(T) per D216 §6.** Rejects Rust-style narrow-fallback. Inline amend D44 line «`int` платформенно» → «`int` = `i64` per D129». Followups: `[M-D227-lit-range-error-code]`, `[M-D227-literal-range-tests]`. |
| D228 | 02-types.md | Value-record allocation contract (Plan 124.8 Ф.2/Ф.4, 2026-06-02; **renumbered from D226 → D228 (2026-06-03) after collision с main D226 «signed indexing» + D227 «numeric literal inference»**) — `type X value { ... }` stack-allocated value type с copy-on-pass semantics. `value` — contextual keyword (Ident-based match, backward compat для variables/fields named 'value'). Composable с consume/priv в любом order (canonical: value consume priv). **V2 production-grade landed 2026-06-03:** real stack codegen — `NovaValue_X` typedef + method receiver = `NovaValue_X*` (pointer to stack slot) + `prepare_method_recv` helper; closes [M-124.8-value-codegen-stack]. Plan 124.8 amends 6 D-blocks (D33/D52/D175/D176/D215/D222) + retract D225 + D228 NEW. **27/27 plan124_8 fixtures PASS** (V1 16 positive + 7 negative + V2 4 verification). Industry edge: первый язык с unified `type X { ... }` syntax + explicit `value` modifier — ahead-of-curve vs Kotlin value class (single-field constraint) и Java Valhalla (incoming). |
| D229 | 02-types.md | Debug protocol + `${expr:?}` format spec — sibling к D183 Display; diagnostic vs user-facing display; 3 error codes; foundation для format-DSL extensions (Plan 91.14, 2026-06-05) |
| D230 | 02-types.md | Clone protocol — single-method `clone() -> Self` для deep recursive copy; auto-derive через `#impl(Clone)` + memberwise synthesis (record literal с `.clone()` per field, primitive copy без recursion); cycle detection (visited set) + field eligibility (4 error codes); resolved Q «hash/eq/clone для пользовательских типов» из D109. Plan 126 family + D109 amend covers все 5 built-in protocols (Equal/Hash/Clone/Compare/Display) (Plan 126, 2026-06-05) |
| D231 | 02-types.md | RawMem allocator API — `RawMem.alloc` / `RawMem.alloc_uncollectable` / `RawMem.free_uncollectable`; GC-tracked vs uncollectable allocation; #unsafe gating (E_UNSAFE_CALL_REQUIRES_WRAP); C mapping to nova_rt/alloc.h; foundation for Nova-native data structures (Plan 131, 2026-06-08) |
| D232 | 02-types.md | Vec[T] — Nova-native generic growable array; layout `data *mut T + len int + cap int`; ×2 growth (initial 8); typed storage for ALL T including Option[T] / value-record / named tuple; 20 methods; VecIter[T]; protocols Iterable/Equal/Clone/Display/Debug; closes [M-91.1-value-struct-array-elem] (Plan 131, 2026-06-08) |
| ~~flip-scan-draft~~ | 02-types.md | **RETRACTED (Plan 147 Ф.1, 2026-06-12)** — «running-current flip-scan» pointer model. ОТКЛОНЁН adversarial-критикой: `*T` контекстно-зависим (наследует binding-current) → тип не самодостаточен (4 BLOCKER). **Заменён D246 (три оси мутабельности).** Восстановлен `*T ≡ *ro T` универсально. `E_REDUNDANT_POINTER_MODIFIER` (flip-scan-only) никогда не реализовывался; под D246 заменён на `E_REDUNDANT_POINTER_RO` (`*ro T` → fix-it `*T`). |
| D259 | 02-types.md | **Конструктор-конвенция `Vec[T]`** (Plan 153.1, формализована 2026-06-14) — `Vec[T].of(a,b,c)` для **литерала элементов** (1 аллокация, аналог Rust `vec![]`); ~~`Vec[T].from(coll)` ТОЛЬКО для **конверсии существующей коллекции**~~ (`clone`-подобная копия, аналог `Vec::from(iter)`). `from([литерал])` — **анти-паттерн**: под D239 литерал `[1,2,3]` уже `Vec[int]`, поэтому `from` копирует его во второй буфер (2 аллокации) ради результата, который `of(1,2,3)` даёт за 1. `from([])` → `Vec[int].new()`. Когда тип выводится — просто `[a,b,c]`. doc-comment `from` (core.nv) направляет на `of`; миграция `[M-153.1-of-vs-from-sweep]`. **AMEND (2026-07-06, of-guard):** пустой `Vec[T].of()` (0 аргументов) ЗАПРЕЩЁН контрактом `requires args.len() > 0` — пустой вектор ТОЛЬКО `Vec[T].new()`; негативная фикстура `std/collections/vec/neg/vec_of_empty_panic.nv`. **AMEND (2026-07-20, Plan 200 П16, retract):** `Vec[T].from` ПОЛНОСТЬЮ РЕТРАКТИРОВАН — он был буквально `items.clone()`; same-T конверсия теперь `existing.clone()` (Clone, D230), width-конверсия — явный поэлементный цикл. `of` без изменений. |
| D246 | 02-types.md | **Три оси мутабельности** (Plan 147 Ф.1, 2026-06-12; supersedes flip-scan-draft; restores D216 §V2.6 `*T≡*ro T`). L1 binding (`ro`/`mut` перед именем = reassignability + корень прав записи; никогда в типе). L2 view (`ro`/`mut` перед типом value/record = транзитивный freeze owned-графа, access-time, D175 §V2 = это; **СТЕНА на каждом `*`**). L3 pointee-capability (постфикс: `*T`=ro дефолт / `*mut T`=mut единственный опт-ин; реально В ТИПЕ, позиционно-независимо). `*T ≡ *ro T` ВЕЗДЕ — самодостаточность типа. НЕТ наследования pointee-mut от binding (flip-scan убран). Ошибки: `*ro T` → **E_REDUNDANT_POINTER_RO** (fix-it `*T`); prefix `mut */ro *` → E_POINTER_PREFIX_MODIFIER; `ro x ro T`/`*ro ro T` → E_REDUNDANT_TYPE_MODIFIER; write через `*T`/`*ro` pointee → **E_POINTER_RO_ASSIGN**; ro-content→mut-content coercion → E_READONLY_COERCE. Оракул A-E (value/heap/pointer/return/generic). Cross-ref D33/D36/D175§V2(KEEP=L2)/D176/D184/D216§V2.6(restored). Гейтит Plan 139 [M-139-f0-lang-item-decl] (str `ptr *u8`). |
| D262 | 03-syntax.md | **Slice-op surface на `[]T`-view модели — без нового типа** (Plan 153.4, ✅ IMPLEMENTED ЦЕЛИКОМ; A: eager-views 2026-06-14, B: lazy chunks/windows 2026-06-15). Весь slice-операционный API строится на принятой `[]T ≡ Vec[T]`-view модели (D238/D239 + Plan 96 D-single-type/D-cap-len) и НЕ вводит `Slice[T]`. **Eager view-заголовки** (`std/collections/vec/views.nv`): `@split_at(i) -> (Self,Self)` (контракт `0<=i<=len`, OOB→panic, инвариант `len(l)+len(r)==len`), `@split_first()`/`@split_last() -> Option[(T,Self)]` (пусто→None), `@first_n(n)`/`@last_n(n) -> Self` (CLAMP «до N»), `@as_slice() -> Self` ro + `mut @as_slice() -> mut Self` (write-through, receiver-mut overload, НЕ `as_mut_slice`). **Ленивые** (`std/collections/vec_lazy.nv`, поверх 153.2): `@chunks(n)`/`@chunks_exact(n)`/`@rchunks(n)`/`@windows(n)` `-> BoxIter[Self]` (yield zero-copy `[]T`-view'ы, БЕЗ внешнего `[][]T`-Vec; `collect()` материализует по требованию), контракт `n > 0`. Detach-on-resize (Go-модель GC-safe): view `cap==len` → первая realloc детачит. `[M-153.4-chunks-windows-lazy]` ✅ CLOSED. |
| D264 | 02-types.md | **Vec-протоколы: `Hash` + FromIterator / collect-target** (Plan 153.6, ✅ IMPLEMENTED — Hash 2026-06-13, FromIterator/collect-target 2026-06-14). **`Vec[T Hash] @hash() -> u64`** (`protocols.nv`) — order/length-sensitive FNV-1a (64-bit), consistency с `@equal` → `Vec[T:Hash]` сам `Hash` (элемент `HashSet`). **FromIterator** — НЕ enforced single-method-протокол (Nova типизирует итераторы структурно, D58), а **набор конструкторов/терминаторов** (Rust collect/FromIterator/extend parity): (1) default collect-target `BoxIter[T] @collect() -> Vec[T]` (ленивый слой D260); (2) `BoxIter[T Hash] @collect_set() -> Set[T]` (dedup, Rust `collect::<HashSet>()`, `vec_lazy.nv`); (3) прочие таргеты композицией над собранным `Vec` (`Set.from_iter(it.collect())`, `HashMap.from(pairs.collect())` — под D239 собранный `Vec` = `[]T`-аргумент); (4) FromIterator из любого `Iter`-источника `Vec[T].new().extend(src)` (`@extend[S Iter[T]]`). Gated (compiler-gaps, НЕ упрощение): `[M-153.6-collect-static-generic]` (static `Vec[T].from_iter[S Iter[T]]` с for-in по `S` не компилируется; instance `@extend` — обход), `[M-153.6-collect-map-tuple-receiver]` (`BoxIter[(K,V)] @collect_map()` не парсится). `[M-153.6-fromiterator-gated]` ✅ RESOLVED. |
| D260 | 02-types.md | Ленивый итератор `Vec[T]` — **boxed-fluent** адаптеры (Plan 153.2 Phase A, 2026-06-14). `type BoxIter[T] { step fn()->Option[T] }` — единый erased курсор; `v.lazy().map(f).filter(p).collect()` без промежуточных Vec-аллокаций; адаптеры (map/filter/filter_map/enumerate/take/skip) оборачивают upstream-step; терминаторы (collect/fold/reduce/count/sum/any/all/find/for_each/min/max/last) драйвят цепочку с short-circuit (`nth` ретрактирован 2026-07-06 — см. AMEND в 02-types.md). Sibling FILE-модуль `collections.vec_lazy` (opt-in import, closure-leak/D145). **Amended by D277.** |
| D277 | 02-types.md | **By-value мономорфизация generic value-records + generic-over-source zero-cost адаптеры** (Plan 153.2 Ф.2, 2026-06-14/15; amends D228 + D260, depends D226). **Stage 1:** `type X[T] value {…}` лоуэрится BY VALUE для каждого mono-инстанса (inline `NovaValue_<short>`, passed/returned/copied by value, 0 `nova_alloc` обёртки — зеркало str-пути); `BoxIter[T]` помечен `value` → 6→0 wrapper-allocs. Codegen-контракт: receiver_c_type → `NovaValue_<short>*` (D226 always-ptr), dispatch через prepare_method_recv, fn-field accessor по value-ness, return-inference strip `NovaValue_` перед `Nova_`. **Stage 2:** новый sibling `collections.vec_iter_zc` — Rust-style `MapIter[I,T,U]`/`FilterIter[I,T]` generic-over-source `value`-рекорды (источник inline полем `src I`, `@next()` статически диспетчит `(@src).next()`); цепочка → один вложенный mono-тип; source-box 9→0, step-индирекция убрана. Sibling, НЕ замена (boxed `vec_lazy` сохранён). Остаток: callback `f`/`pred` ещё boxed `[M-153.2-closure-as-mono-type]` (P3); take/skip/enumerate на boxed. `[M-153.2-generic-over-source-zerocost]` → 🟡 PARTIAL. |
| D285 | 10-overloading.md | **Receiver-compatibility rule — blanket dispatch priority** (Plan 164 Ф.3, 2026-06-16). Blanket-метод на совместимом receiver всегда приоритетнее concrete-метода на несовпадающем типе с тем же именем. Алгоритм: извлечь base_type из obj_ty → проверить `type_impl_protocols[base_type]` против bounds blanket-FnDecl → dispatch через blanket если все bounds OK. Пополняется `#impl(P[U])` на `@next()`-методах адаптеров. Закрывает [M-153.2-drop-z-prefix] + [M-codegen-blanket-generic-param-order]. |
| D355 (ex-D282, renumber 2026-07-03) | 02-types.md | **Blanket protocol-receiver methods** (Plan 161) — `fn[I Proto[T]] I @name(...)`: метод виден на каждом реализаторе bound-протокола; конкретный метод приоритетнее; ≤1 impl протокола на тип. Перенумерован из D282 (коллизия с extern-ABI D282, Plan 91.12). |
| D363 (ex-D184, renumber 2026-07-03) | 03-syntax.md | **Operator dispatch via protocols** (Plan 91.8b) — `==`/`!=`/`<`/`<=`/`>`/`>=` через `Equal.@equal` / `Compare.@compare`; удалены magic `@eq`/`@lt`/… Перенумерован из D184 (коллизия с keyword-refresh D184, Plan 114). |
| D364 (ex-D292, renumber 2026-07-03) | 02-types.md | **Net C FFI pattern: opaque handle + value-record wrapping** (Plan 91.12) — `CX(*())` newtype → priv `extern "C"` ffi → public value-record `{priv handle}`. Перенумерован из D292 (коллизия с ModuleSigTable-D292, Plan 162.1). |
| D365 (ex-D291, renumber 2026-07-03) | 04-effects.md | **std/net effect family → консолидируется в единый `Net`** (Plan 91.12; [M-net-merge-to-single-effect]) — заголовок на main отсутствовал (branch plan-91.12); рождается с net-sweep как единый `Net` (канон D62). Перенумерован из D291 (коллизия с module-resolution-D291, Plan 162.2). |
| D366 (ex-D124, renumber 2026-07-03) | 08-runtime.md | **Edition-versioned prelude resolver** (Plan 62.F.bis) — `[package].edition` pin → `std/prelude/<sanitized>.nv`. Перенумерован из D124 (коллизия с Monotonic-vs-Timestamp-D124, Plan 65). |
| D369 (ex-D138, renumber 2026-07-03) | 07-modules.md | **Межпакетный импорт — только через объявленную `[dependencies]`** (Plan 03.1). Перенумерован из D138 (коллизия с Default-on-M:N-runtime-D138, Plan 83.4.5.6). |
| D377 (ex-D298, renumber 2026-07-03) | 04-effects.md | **UDP Socket Split — `UdpSendHalf`/`UdpRecvHalf`** (Plan 166). Перенумерован из D298 (коллизия с Test-suite-time-budget-D298, Plan 169.1). |
| D367 (ex-D125, renumber 2026-07-03) | 02-types.md | **Удаление `byte` — каноническое `u8`** (Plan 69). Перенумерован из D125 (коллизия с Prelude-shadow-lint-D125, Plan 62.F.bis). |
| D368 (ex-D126, renumber 2026-07-03) | 02-types.md | **Strict type propagation — no silent `nova_int` fallback** (Plan 70). Перенумерован из D126 (коллизия с `external type`-D126, Plan 62.D.bis). |
| D372 (ex-D180, renumber 2026-07-03) | 02-types.md | **Canonical `.new()` constructors (convention)** (Plan 91.7). Перенумерован из D180 (коллизия с `consume`-binding-syntax-D180, Plan 73.1). **AMEND (vec-sweep, 2026-07-06):** `with_capacity` удалён везде (`Vec[T]`/`[]T`, `HashMap`, `Set`, `StringBuilder`, `WriteBuffer`, `Queue`) — ёмкость теперь D117-свойство `cap()`/`mut cap(n) -> @`; конструкция `X.new().cap(n)`. `Vec[T].from_raw_parts` переименован в перегрузку `Vec[T].new(ptr, len, cap)` (арность-диспетчинг 0-арг vs 3-арг). Обнаруженные компиляторные гэпы задокументированы на месте амендмента (`[M-vec-spelling-array-value-position-cap-collision]`, `[M-vec-spelling-consume-chain-cap-collision]`, `[M-vec-spelling-maplit-desugar-cap-ice]`). |
| D374 (ex-D258, renumber 2026-07-03) | 02-types.md | **Write-sink протокол — декаплинг Display/Debug от StringBuilder** (Plan 152.7.1). Перенумерован из D258 (коллизия с format-spec-интерполяции-D258, Plan 152.7). |
| D370 (ex-D173, renumber 2026-07-03) | 06-concurrency.md | **AI-first guidance — sync-primitive decision tree** (Plan 103.7). Перенумерован из D173 (коллизия с std/net-stdlib-D173, Plan 83.12). |
| D375 (ex-D277, renumber 2026-07-03) | 08-runtime.md | **Per-type GC pointer-offset bitmaps** (Plan 144.1). Перенумерован из D277 (тройная коллизия; by-value-mono сохраняет D277, Plan 153.2). |
| D376 (ex-D277, renumber 2026-07-03) | 09-tooling.md | **Test-discovery skip/route-конвенции — `fixtures/`, OS-суффикс, `_slow.nv`** (Plan 156). Перенумерован из D277 (тройная коллизия). |
| D283 | 09-tooling.md | **Reachability-based codegen DCE — эмиссия только достижимого** (Plan 159, ✅ IMPLEMENTED 2026-06-15; вариант A, Zig-модель). Codegen эмитит в C ТОЛЬКО объявления (free fns + module-level `const` + `ro` lazy-static globals + методы), достижимые от `main`. Executable: `export`/`pub`-fns НЕ roots (у Nova нет C-ABI-экспорта), режутся если недостижимы; FFI-entry = только `is_external`. **Library / no-main / `EXPECT_CC_ERROR`-фикстура ⇒ DCE OFF (держим весь API).** `const`/`ro`-таблицы (гигантские `*_DATA`) держатся только если их читает достижимая fn/const. Method-level DCE: метод `T.m` эмитится ⇔ тип `T` достижим **И** селектор `m` вызван по имени (coarse-by-name → name-collision OVER-KEEP). Дженерики mono-on-use. Непрямые ссылки (protocol-dispatch/closures/`@`-методы/for-iter/операторы/string-interp desugar) засеяны как достижимые-по-имени. Kill-switch `NOVA_REACH_DCE` (unset/`!=0` ⇒ ON default; `0` ⇒ байт-идентичное старое поведение). G0 консервативная корректность: НИКОГДА не отрезать достижимое (over-keep ok, over-prune = release-blocker). Замер: `import std.unicode.{is_alphabetic}` + вызов только `is_alphabetic` → C 10606→2494 строк (~4.25×↓), collate/normalize/GC_DATA 37/9/2→0/0/0, нужная ALPHA_DATA сохранена. Вариант B (прекомпил std + `cc --gc-sections`) отложен под скорость сборки. Ф.4 (Option A) разблокировал no-import char-методы → `[M-152.3b-char-methods-no-import]` ✅ CLOSED. Остаток: `[M-159-method-pruning]` (P3 per-kind desugar-аудит), `[M-159-lazy-module-resolution]` (P3). |
| D282 | 04-effects.md | **`extern "C" fn` literal-name FFI** (Plan 91.12, 2026-06-16) — std/net C FFI layer; literal C-symbol entry points (без mangling) для net runtime (`nova_rt/net.{h,c}`); foundation под TcpNet/UdpNet/AddrNet effect handlers. |
| D290 | 02-types.md | **Value-record iterator types** (Plan 165, 2026-06-16). |
| D291 | 07-modules.md | **Module resolution — collect-signatures-first, lazy bodies; cross-module cycles allowed** (Plan 162.2, 2026-06-16). ⚠ ex-net-effect-family (91.12) под этим номером renumber → D365 (2026-07-03). |
| D292 | 07-modules.md | **ModuleSigTable — two-pass resolver** (collect_all_signatures + inline-merge; Plan 162.1, 2026-06-16). ⚠ ex-Net-C-FFI-pattern (91.12) под этим номером renumber → D364 (2026-07-03). |
| D295 | 04-effects.md | **DnsNet V2 — async DNS resolution `[]SocketAddr`** (Plan 91.13, 2026-06-16) — vtable erasure fix (`is_generic_stub_c` + `!name.contains("____")`); `resolve()` возвращает `[]SocketAddr`; 21/0 PASS. |
| D377 | 04-effects.md | **UDP Socket Split (ex-D298, renumber 2026-07-03) — `UdpSendHalf` + `UdpRecvHalf`** (Plan 166, 2026-06-17) — `UdpSocket.split()` на send/recv половины с независимыми C-side park-слотами (TOCTOU fix); образец для TCP split D301. |
| D301 | 04-effects.md | **TCP Stream Split — `TcpReadHalf` + `TcpWriteHalf`** (Plan 91.16, 2026-06-17) — `TcpStream.split() -> (TcpReadHalf, TcpWriteHalf)`; полнодуплексный read+write через раздельные `read_scope`/`write_scope` park-слоты; atomic `split_refcount` для owning/close; `write_all` семантика. V1 limit: tuple-consume binding не отслеживается `[M-91.16-tuple-consume-binding]`. |
| D302 | 04-effects.md | **std/net API polish — `NetError.Eof`, `@to_str()`, `SocketAddr @ip()`, `write_all`** (Plan 91.15, 2026-06-17) — EOF как `Err(NetError.Eof)` (не `Ok("")`); `NetError @to_str()` для всех вариантов; `host_str()` → `ip()` rename; `write_all` C-backed; новые варианты `PermissionDenied`/`ConnectionReset`; завершает удаление `Blocking` effect (D172). **Амендмент (Plan 176 Ф.4, Q3, 2026-07-09):** аддитивная проекция `NetError @to_error_kind() -> io.ErrorKind`/`@to_io_error(op)`(строки `@to_str()` не тронуты); `TcpStream.@read`/`@write` → `Result[_, io.IoError]` (io.Read/io.Write conformance); `HttpError.ErrSource.Net(NetError)` разгейчен. |
| D407 | 04-effects.md | **`std/net` переработка — один слой FFI, байтовый транспорт, zero-copy, M:N-safety** (Plan 183, 2026-07-06) — амендит D173/D282/D301/D302: публичные C-функции `nova_net_*` C-ABI (без `NovaRt_*_method_*`/двойной обёртки); транспорт `[]u8` (не `str`); read/write zero-copy (`alloc_cb`=срез буфера вызывающего, `uv_write` прямо из `[]u8`; malloc/memcpy/nova_alloc данных в hot-path = 0); удалены 6 `__thread`-результатных слотов (M:N-гонка Д2) → коды-возврата + out-параметры; `SocketAddr` = value-запись; split без дублирующего C-API (раздельные `read_scope`/`write_scope` с рождения). C-слой `net2.c` Ф.1, .nv-обвязка Ф.2, потребители мигрированы Ф.3 (старый `std/net`/`net.c` НЕ удалён — DEPRECATED-баннер, гейт `[M-183-old-net-removal-after-182]` на Plan 182), M:N-стресс+loop-affinity-контракт+эхо-замер ~600 MiB/s Ф.4. Ф.0-Ф.4 ✅ SHIPPED 2026-07-06. |
| D409 | 03-syntax.md | **`-> @`: автоматический возврат приёмника** (решение владельца, ✅ IMPLEMENTED 2026-07-06) — амендит D181/D132. Тело `-> @`-метода больше НЕ пишет возврат приёмника: конец тела без хвостового выражения / голый `return` / хвостовое НЕ-`@` выражение (discard) → неявный `return @`. Explicit-формы (`@` в хвосте, `return @`, `=> @`) — **запрещены**, `E_EXPLICIT_SELF_RETURN` (амендмент владельца: изначальная обратная совместимость снята, все ~90 мест std/тестов мигрированы). Вернуть иное значение — ошибка как раньше (D132 rule 2). Чекер: `check_fluent_return` (types/mod.rs); лоуэринг: AST-pass `self_return_lower.rs` (синтезирует `@` на implicit-exit'ах ПОСЛЕ check — codegen без изменений, переиспользует manual-форму эмиссии). Класс «забыл вернуть приёмник» невозможен по построению. |
| D410 | 03-syntax.md | **Ретракция `as_*`/`to_*`-близнецов** (решение владельца 2026-07-06, миграция `[M-d410-as-to-migration]`) — префикс `as_` упразднён: виды/линзы = голые существительные (`bytes`/`chars`/`words`/`sentences`/`slice`/`ptr`); копия — явно `.clone()`/`.collect()` на месте вызова (близнецы `to_bytes`/`to_chars` удалены); `to_*` только трансформации (`to_upper`, `to_str`); `into_*` — потребляющий финализатор (без изменений). Мотив: близнецы не самообеспечивались (to_bytes 124 vs as_bytes 67), []T-вид и random-access и итерируем (один `bytes()` = rust as_bytes+bytes+to_bytes), плата за копию видна в точке вызова. D262-амендмент: `as_slice`→`slice`. |
| D411 | 03-syntax.md | **Record-деструктуризация в биндингах `ro`/`mut`** (решение владельца 2026-07-07, ✅ IMPLEMENTED 2026-07-07) — match-грамматика record-паттернов в irrefutable-позиции: `ro {line, col, ..} = @`, переименование `поле: имя`, `..` обязателен при частичном списке; mut-вариант; суммы/литералы запрещены (`E_REFUTABLE_BINDING`); частичный список без `..` — `E_RECORD_PATTERN_NEEDS_REST`; семантика pattern-native (без AST-десугара — `Pattern::Record` идёт через чекер/кодоген напрямую, источник вычисляется один раз в codegen tmp). Парсер/irrefutability-проверка/codegen-биндинг существовали ДО D411 (Plan 53 `let`-record-destructure, унаследовано через retraction `let`→`ro`/`mut`); D411 добавил код-теги + `..`-правило + conformance-тесты + json-лексер как потребитель. `[M-d411-record-binding-destructuring]` ✅ CLOSED. |
| D417 | 02-types.md | **Closure-литерал против скалярного return-типа — `E_CLOSURE_SCALAR_RETURN`** (найдено 2026-07-10, расследование `[M-toml-repeated-fail-call-run-fail]`) — ни `assignable` (call-arg/annotated-let), ни literal-coercion канал не отвергали closure-литерал (`|| body`/`|x| body`/`fn(...) ...`) в return-позиции (implicit trailing/arrow-body/explicit `return`) против скалярного (`bool`/int-family/float/`char`) объявленного return-типа — codegen лоуэрит closure в fn-pointer, скаляр-коэрсия молча бит-реинтерпретирует указатель (`bool` всегда `true`), без диагностики. Типовой триггер — ведущий `||`/`|x|` на continuation-строке многострочного `&&`/`||`-выражения (парсер намеренно не продолжает OR через ведущий `||` — синтаксис зарезервирован за zero-arg closure-литералом), вторая строка становится СВОИМ closure-statement'ом = implicit-return. Проверка — `check_closure_scalar_return`+walk-семейство (types/mod.rs), зеркалит reachable-позиции `materialize_returns_in_block`/`_in_expr`; closure против fn-типа (легитимный HOF-возврат) не флагуется. Побочный улов той же волной: `std/data/semver.nv` `is_ascii_ident_char` и `std/encoding/csv.nv` `needs_quoting` — тот же баг, обе всегда возвращали `true`; смигрированы на трейлинг-`||` (канон `toml.nv`'s `is_bare_key_char`). `[M-closure-trailing-scalar-coercion-no-typecheck]` ✅ CLOSED. |
| D414 | 06-concurrency.md | **Structured error propagation + `supervised`-value + `parallel for → []T` канальный сбор** (Plan 173 Ф.3 + Plan 173.1, 2026-07-09) — §1 primary-selection precedence PANIC>USER>CANCEL (`nova_throw_kind_precedence`, обе report-точки); §2 `detach` error-policy (LogAndDrop default + `[E_DETACH_REQUIRES_EFFECT]` enforcement); §3 channel closed-vs-value канон (`recv → Option` остаётся; select `None`-арм `want_none`); §4 `supervised { … v }` = value-expression (post-join trailing) + `parallel for → []T` для ЛЮБОГО T/итератора через канал (клон-в-родителе → send → close-на-выходе; drain-fiber; K=min(len,16); **completion order, плотный**; D71-amend — примитив-whitelist/slot-запись/iteration-order УДАЛЕНЫ; `[M-parfor-record-result-miscompile]` закрыт) + runtime-хардненинг канала (`[M-chan-spurious-wake-retry]`, `[M-chan-close-phantom-zero]`). |
| D307 | 02-types.md | **File-private visibility `priv(file)`** (Plan 170, 2026-06-19) — самый узкий уровень лесенки видимости top-level символов: `priv(file) fn`/`type`/`const` виден только в своём файле, не утекает к peer-файлам folder-module. Лесенка `priv(file)` ⊂ module-default (D281) ⊂ `export`. `file` — контекстный Ident (не KW). Два одноимённых `priv(file)` в разных peer-файлах НЕ конфликтуют (disjoint file-scope, dedup в checker) + file-дискриминированный codegen (`nova_fn_<mod>_f<file_id>_<name>` — нет линк-коллизии). `E_FILE_PRIV_LEAK` при ссылке peer-файла на чужой file-private; `E_PRIV_QUALIFIER` для `priv(file)`+`export` / bare top-level `priv` / `priv(<other>)`. Аналог Rust `pub(self)`. Не нарушает D29 (модуль остаётся один — visibility-hint). **Нумерация:** план назвал «D304», но он занят (Test Category Selectors); D305/D306 были временно зарезервированы за 104.10, но 104.10 получил D378-D380 → **D305/D306 СВОБОДНЫ**; присвоен D307. |
| D373 | 02-types.md | **Generic array API: sort/min/max/binary_search + _by variants** (ex-D185, renumber 2026-07-03; Plan 91.8c, 2026-06-17) — `fn[T Compare] []T @sort_of/min_of/max_of/binary_search_of`; `fn[T] []T @sort_by_of/min_by_of/max_by_of`; suффикс `_of` избегает collision с concrete `[]int` методами; insertion sort O(n²) MVP; 13/13 PASS. **§amend-1 (2026-06-17):** SelfAccess arm в `compute_array_elem_type_for_obj` + `emit_monomorphized_method` заполняет `array_element_types["nova_self"]` на входе → прямой `@[i].method()` dispatch в generic array телах. Closes [M-91.8c-direct-index-method]. |
| D315 | 02-types.md | **`ResolvedType` — единый канонический носитель типа** (Plan 172.1, 2026-06-21) — тип имеет ОДНО каноническое представление; проверки/совместимость/конверсии/перевод-в-C выводятся из него; `type_ref_to_c` (резолв+перевод) ретайрится; сахар (алиас/`Self`) нормализуется, исходное написание — отдельный диагностический канал; ABI-факты (мангл/erasure/`NovaValue_`/int64-слоты) выводятся лоуэрингом, не хранятся в типе. Supersedes врезку M2 Plan 172.1. |
| D308 | 07-modules.md | **`prelude.core` развязан от `std.unicode`** (Plan 169.2.1, 2026-06-19) — char-@методы (`'a'.is_alphabetic()` ×10) перенесены обратно в `std/unicode/category.nv`; `core.nv` больше не импортирует `std.unicode` (цикл `core→unicode→collections→core` разорван) → частичный `#prelude(core, …)` не тянет `cps_to_str`'s `consume sb` и не падает D133. Доступность без import — через resolver-инъекцию `std.unicode` на char-method-селектор (`@method:`, восстановлен Plan 159 Ф.4), НЕ facade-реэкспорт (тот сломал бы opt-in-границу `n_char_unicode_opt_in`). `Option/Result @debug` вынесены в `protocols.nv` (восстановлено «ZERO imports» для core). Closes [M-169.2.1-core-unicode-decouple]. |
| D316 | 04-effects.md | **`Time`: плумбинг-эффект + единый источник схемы + `TimerMetrics`-split** (Plan 175 Ф.1, 2026-07-04) — схема `Time` имеет ОДИН источник (`.nv`-decl в `std/prelude/effects.nv`; codegen читает её через RUNTIME_DEFINED_TYPES/`emit_type_decl`, хардкод + закомментированное 5-е зеркало удалены). 5 timer-observability-счётчиков вынесены из `Time` в read-only эффект `TimerMetrics` (direct-C `Nova_TimerMetrics_timer_*`, не suspend, симметрично `Mem`; Q1). int-провод (`now()->int`/`now_monotonic()->int`/`sleep(ms int)`) сохранён без смены поведения. Amends D11/D14/D62/D124. **Typed-surface + infallibility (Q15) + non-serializable Monotonic — amend в Ф.2/Ф.3; overflow → D317; non-regression → D318.** |
| D317 | 04-effects.md | **Duration/instant overflow-policy** (Plan 175 Ф.1c, 2026-07-06) — 3-tier: операторы траппят (debug И release, никогда silent-wrap/build-mode-UB); `checked_*` → `Option`; `saturating_*` → clamp ±(2⁶³−1). `@abs(i64::MIN)`→MAX-saturate; `@div(0)`/`@div(MIN,-1)`/`@neg(MIN)`→trap; f64 NaN/inf/OOR → trap + `try_*`→Option. Timestamp-окно 1677..2262 (Q16). Amends D316. Публичные `Duration.MAX/MIN`-консты отложены (`[M-175-type-const-max-shadows-builtin]`). |
| D318 | 04-effects.md | **Monotonic non-regression + clock-source contract** (Plan 175 Ф.1c, 2026-07-06) — регресс: `@elapsed_since` saturate-to-zero, `@checked_duration_since`→None (без global-lock, Rust-1.60-урок); non-serializable (Q13); per-OS clock-source (Q14, uv_hrtime, suspend-EXCLUDED); infallible-by-contract (Q15). Amends D124. |
| D319 | 04-effects.md | **Civil-time модель: type-ladder Plain/Offset/Zoned + proleptic Gregorian + epoch-day** (Plan 175.1 Ф.0/Ф.1/Ф.3, 2026-07-10) — `Date`/`TimeOfDay`/`DateTime` (value-records, Hinnant epoch-day algo) → `Offset`/`ZonedDateTime`; validate-by-default `Result[T,DateError]`; никакого implicit Plain→Timestamp. |
| D320 | 04-effects.md | **`Period`(y/m/d) ≠ `Duration`(ns) + DateBased/TimeBased-разделение** (Plan 175.1 Ф.2, 2026-07-10) — `Date` принимает только `Period`, `TimeOfDay` только `Duration` (compile-error на смешивание); calendar-арифметика CLAMP biggest-unit-first; trap-on-overflow (D317); wall-vs-elapsed асимметрия Zoned. |
| D321 | 04-effects.md | **DST `Disambiguation`(4-way)+`OffsetConflict`+parse-strictness+структурные ошибки+IANA tz-db** (Plan 175.1 Ф.3/Ф.4/Ф.5, 2026-07-10) — `Compatible`(default)/Earlier/Later/Reject как Result-значение; strict-parse default; `DateError`/`ParseDateTimeError`; `OffsetConflict{RejectMismatch(default),Use,Prefer,Ignore}`; §1а-парсеры `s.to_date()`/`to_datetime()`/`to_timestamp()`/`to_zoned_datetime()`; `:60`→clamp; `s.to_timezone()`+`TimeZone.from_tzif`+`load_timezone` — OS-TZif-parser (`std.fs`) + rule-based embedded-fallback curated-зоны (§tzdb — задокументированное сужение полного snapshot, follow-up `[M-175.1-full-tzdb-embed]`); pattern-DSL `DateTimeFormat`; §impl-отступления с маркерами. |
| D322 | 04-effects.md | **io-core: `io.Read`/`io.Write`/`io.Seek`, `SeekFrom`, структурный `IoError`/`ErrorKind`, `BufReader`/`BufWriter` must-consume (D133), `Io` effect (stdin/stdout/stderr, real/mock)** (Plan 176 Ф.1, 2026-07-04). EOF/partial/EINTR-контракт (Q9); эффект-агностичные протоколы (Q15, D122-amend); per-op error sets — considered/REJECTED (Q14). |
| D323 | 04-effects.md | **fs: byte-backed `Path` (Q1, WTF-8 Win), `Fs` effect (тонкий int-primitive; real_fs libuv `uv_fs_*` / mock_fs MemFs), `File` must-consume (D133), `OpenOptions` (Q13), `Metadata`(→Timestamp)/`DirEntry`/`Permissions` (Q8/Q12), `write_atomic` (5-шаг durable)** (Plan 176 Ф.2, 2026-07-04). |
| D324 | 04-effects.md | **os: `Os` effect (env/args/cwd/dirs/exit-flush; тонкий int/str-primitive + Nova-обёртки; real_os нативные хуки `os_env.h` / mock_os in-memory с recorded-exit); `IoError` reuse (Q3); race-контракт (§3c)** (Plan 176 Ф.3, 2026-07-06). **Амендмент (2026-07-07):** `env_get -> Option[str]` (сентинель-"" ретрактирован); публичная поверхность — перегрузка по арности: `env(k)`/`env(k,v)`, `cwd()`/`cwd(p)`, `env_bytes()` ×2. Subprocess → под-план 176.1 (Q5). |
| D325 | 04-effects.md | **Единый fallible-контракт std — Result-everywhere** (Plan 177, 2026-06-25); amends D77 (4-way→2-way), retracts D178 (bare `parse_int` + `parse_int_opt`). `try_` только для `from`/`try_from` + `into`/`try_into`; Option через `.ok()`; throw через `!!`; `Fail[E]` остаётся в языке. |
| D326 | 02-types.md | **`ref` как режим передачи параметра** (safe in-out/borrow; Plan 172.5, 2026-06-26) — owner переоткрыл Q29 в param-mode (`ref`-ТИП остаётся отвергнут). `ro ref`/`mut ref` (Swift `inout`/C# `in`+`ref`; без лайфтаймов); `@` ≡ `mut/ro ref @`, `-> @` ≡ `ref @` (D181). Узкая эксклюзивность `E_REF_ALIAS_OVERLAP` (не Rust/Swift — GC). Amends Q29/D132/D228; adopt D181/D184; bounds D157/D246-P10. |
| D333 | 04-effects.md | **codec-контракт `std/encoding/compress`: PURE-codec, byte-first `[]u8`, `Result[T,CompressError]`** (Plan 179 Ф.1/Ф.3, 2026-07-04) — plain fallible fns без effect-триады (явное conventions-исключение); OPEN `ErrorKind`; decode=`fn(data,max_output)`, encode=`fn(data,level)`; `CompressLevel` value-record. Приземлён к классу-соседям (D322/D323/D325), НЕ в аспирационный `05-stdlib.md`. |
| D334 | 04-effects.md | **bomb-cap: обязательный `max_output` decode-инвариант** (Plan 179 Ф.1, 2026-07-04) — decode-сигнатура без `max_output` = compile-error; инкрементальная проверка (первый байт сверх cap → `Bomb`) до over-аллокации; anti-flood прогресс-входа; `0`=unlimited escape-hatch; encode БЕЗ cap (выход<входа). §8.0-critical. |
| D335 | 04-effects.md | **streaming incremental coder `feed`/`read`/`finish` + SYNC-FLUSH + Plan 178 BodyReader-мост** (Plan 179 Ф.1/Ф.3, 2026-07-04) — `Inflater`/`ZlibReader`/`GzipReader` + `Deflater`/`ZlibWriter`/`GzipWriter` (plain value, НЕ consume); bounded-per-call `read(max_emit)`; `@flush` SYNC-FLUSH (`00 00 FF FF` decodable-префикс); read→`Result[[]u8]`+`is_done()` (codegen-обход `Option[Vec[u8]]`). |
| D336 | 04-effects.md | **checksum-контракт: CRC-32 (gzip) / Adler-32 (zlib) / ISIZE verify-by-default** (Plan 179 Ф.1, 2026-07-04) — `crc32`/`adler32` + incremental; mismatch → `Checksum{kind,expected,got}`; ISIZE=`(len mod 2^32)`; CRC-32 промоут из `_experimental` (Q15), Adler-32 NEW. |
| D337 | 04-effects.md | **brotli C-FFI-контракт (FORWARD-SPEC, gated `[M-179-brotli-vendor-lib]`)** (Plan 179 Ф.2) — `extern "C"` к `libbrotlidec` (C-ABI без `[]u8`, D355/174.6 M1); `BrotliReader` `consume value` (единственный consume-кодер, D133); bomb-cap-over-FFI (output-cap≠window-cap); НЕ landed (libbrotli отсутствует — честный build-gate). |
| D421 | 09-tooling.md | **Contract execution model — `#debug` dev-only префикс + `--contracts`-уровни (`checked`/`optimized`/`verified`); ретракция `#unchecked` и `debug_assert`** (Plan 194, 2026-07-14, решение владельца) — amends D24 (Plan 140 `#unchecked`+`--contracts=enforce\|off` RETRACTED) и D81 (`debug_assert` RETRACTED → `#debug assert(...)`). `#debug` — единый модификатор перед `requires`/`ensures`/`invariant`/`assert`, dev-only (стирается в `optimized`/release); bounds/overflow остаются always-on safety (не контракт). **Ф.0+Ф.1 этой волной** (спека + парсер, флаг `debug_only` в AST инертен); codegen-эрозия по режиму + физическая ретракция `#unchecked`/`debug_assert` — Ф.2/Ф.4 (см. таблицу статуса в D421). |
| D422 | 02-types.md | **Unified Formatter — единый `@display(mut f Fmt)`/`@debug(mut f Fmt)` REQUIRED-примитив (без to_str-дефолта, цикл структурно невозможен); байтовый `Write` (`@write([]u8)`, инфаллибельный, минимальный); `Fmt` embeds `Write` через `use` (D145 protocol-embed) + оси width/align/fill/sign/alternate/kind/pad; буфер-примитивы (int/bool/char/радикс/pad) переезжают в `.nv`, C остаётся только `fmt_f64_into` (`extern "C" fn`, D282 литеральное имя); энумы `Align`/`Sign`/`FmtKind`/`FloatKind` (D406); `StringBuilder`-аменд (`@reserve`/`@advance`/`@pad_in_place`/`@write_padded`); derived `Display`≠`Debug` формой (`Point(1,2)` vs `Point { x: 1, y: 2 }`)** (Plan 208, 2026-07-15, решение владельца) — keystone, RETRACTS D419 целиком; amends ×2 D374 (sink `Write`→`Fmt`, `@write_str(str)`→`@write([]u8)`), D237 (сигнатуры `@display`/`@debug`), D229 (диспетч `${expr:?}` через `@debug(f)`, радикс через `f.kind()`), D179 (`StringBuilder` байтовый API); amends D55 (str-**литерал**→`[]u8` коэрсия, общее правило любой `[]u8`-позиции, carve-out на литералах); ретрактирует мёртвый `str.from_debug` вместе с default-body Debug-протокола. **Статус: дизайн финализирован 2026-07-15 (owner sign-off Ф.0), Ф.1-4 pending** (см. таблицу статуса в D422). |
| D423 | 04-effects.md | **Арифметическая overflow-политика: примитив `@overflowing_*` + trap-дефолт для ВСЕХ `Ints` + `checked_*`/`saturating_*`/`wrapping_*` .nv-бланкеты** (Plan 206 Ф.0/Ф.1/Ф.1b/Ф.2, 2026-07-15) — `type Ints set` (full-union `SignedInt ∪ UnsignedInt`, amends D310 `E_TYPE_SET_MIXED_SIGNEDNESS` для full-union кейса); `fn[T Ints] T @overflowing_add/_sub/_mul(rhs T) -> (T, bool)` компиляторный интринсик (`__builtin_*_overflow`, D109-класс dispatch, НЕ generic-extern); trap-дефолт `+`/`-`/`*` расширен с безграничного `int` на ВСЕ sized-типы (`nova_<T>_checked_*`, было — сырой C signed-UB/unsigned-wrap). Ф.2: три `.nv`-бланкета поверх интринсика (`std/src/prelude/protocols.nv`) — `@checked_add/_sub/_mul -> Option[T]`, `@wrapping_add/_sub/_mul -> T` (модульно), `@saturating_add/_sub/_mul -> T` (op-специфичная клампинг-формула направления). Overflow-зависимый std-код (xoshiro/splitmix64 PRNG, FNV/bloom-filter double-hashing, MD5/SHA-1/SHA-256 mod-2^32 compression) мигрирован на `wrapping_*`. `unchecked_*` — отложен; Duration-миграция — Ф.3. `div`/`neg`/`mod` вне рамок ([206.1](../../docs/plans/206.1-div-neg-trap.md)). |
| D424 | 02-types.md | **raw-ptr `extern`/`external` fn ⇒ `unsafe fn` по инференсу (без keyword) + carve-out `E_UNSAFE_UNUSED` удаляется** (Plan 174.6 M4, 2026-07-15, решение владельца) — amends D216 §9 (карта unsafe-триггеров, п.10) + D282. extern fn с сырым указателем (`*T`/`*mut T`/`*()`/`CStr`) в параметре ИЛИ возврате классифицируется unsafe-to-call (`unsafe { }` обязателен, `E_UNSAFE_CALL_REQUIRES_WRAP`); scalar/handle-only externs остаются безопасными; `E_UNSAFE_UNUSED` (hard error) честен без исключений — `unsafe { }` без реально-unsafe операции флагается. Вариант A (vs B «снести обёртки»): raw-ptr в C разыменуется без проверки = настоящий unsafe; 0 churn std/net+std/tls. **Статус: спека/дизайн 2026-07-15; checker-enforcement — Plan 174.6 M4, `[M-174.6-rawptr-extern-unsafe-infer]`.** Закрывает `[M-unsafe-cluster-extern-ptr-arg-policy]`. |
| D425 | 06-concurrency.md | **CAS возвращает свидетеля провала: `compare_exchange`/`compare_exchange_weak` `bool` → `Result[(), T]`** (Plan 207, 2026-07-15) — amends D168 §1 (матрица операций). `Ok(())` — успех; `Err(actual)` — провал, `actual` = фактически прочитанное значение (witness), даром из C11 `__atomic_compare_exchange_n` (уже пишет его в `expected` при провале) — убирает лишний повторный `load()` в CAS-retry-цикле. Применяется ко всем 13 CAS-методам (`AtomicI8..I64`/`U8..U64`/`Isize`/`Usize`/`Ptr`/`Bool`/legacy `AtomicInt`). Не меняет memory-ordering, `fetch_*`, `load`/`store`. Retag `[M-cas-return-witnessed-value]`. **Статус: landed** (std/runtime/sync.nv + sync_primitives.h, Plan 207 этой волной). |
| D427 | 04-effects.md | **`div`/`mod`/unary-`neg` always-on trap-guard + `.nv`-бланкеты политик** (Plan 206.1, 2026-07-16, P1 — division-by-zero ранее = сырой C `/` → SIGFPE неконтролируемый крэш) — amends D423 §R6 (явно выносил div/neg за рамки) и D13 (int overflow trap теперь и для div/mod/neg, не только `+`/`-`/`*`). Guard — обычное сравнение (у `__builtin_*_overflow` нет div/neg-варианта), НЕ HW-флаг: `b==0` → паника `"division by zero"` (ВСЕГДА, домен-ошибка, signed+unsigned одинаково); signed `a==T.MIN && b==-1` → паника `"division overflow"` (unsigned структурно не имеет этого случая — MIN=0, всегда представимо); `%` получает ТОТ ЖЕ guard что `/` (x86 `idiv` фолтит на overflow частного независимо от истинного остатка). Unary `-x`: signed `x==T.MIN` → `"negation overflow"`; unsigned НЕ трогается (raw `-x` уже полностью определена C-стандартом, не UB). Лоуэринг — `nova_<T>_checked_div/_rem/_neg` (`nova_rt/effects.h`, зеркало D423 `nova_<T>_checked_add/sub/mul`), 5 сайтов emit_c.rs (Binary/Unary арм + typed-target propagation + `AssignOp::Div` compound-assign — нет `%=` в языке). `.nv`-бланкеты (`protocols.nv`, ЕДИНЫЙ `fn[T Ints]`, не split SignedInt/UnsignedInt): `@checked_div/_rem -> Option[T]`, `@wrapping_div/_rem -> T` (паникуют на `/0`, wrap только MIN/-1 — Rust-прецедент), `@checked_neg/@wrapping_neg` — reformulated через `@overflowing_sub`-флаг (`0-x`), НЕ прямая `x==T.MIN`-ветка (та инвертирована между знаковостями, не может быть единым телом). §R4 документирует 2 эмпирически найденных припятствия (негативный литерал в unsigned-моно — D227 Rule 4; nested `.nv`-blanket-в-blanket call ICE, followup `[M-206.1-nested-blanket-call-p67]`). Z3-элизия механически подключена, СТОРОНА доказательства не реализована (`prove_int_overflow_sites` фильтрует на Add/Sub/Mul, followup `[M-206.1-div-neg-z3-elision]`). Duration НЕ мигрирована (уже D317-корректна, свои message-тексты, followup `[M-206.1-duration-div-neg-dedup]` P3). |
| D429 | 02-types.md | **`#coerce` — декларативные неявные zero-cost конверсии (view + finalize)** (Plan 214, 2026-07-18, прод-спека без V1-упрощений) — ОДИН атрибут, две формы записи (метод-источник / статик-приёмник `.new`), унарность; полосы: view (не-consume → `ro O`) и finalize (`consume`-метод → владеющий O, zero-cost move, ВСЕ позиции вкл. call-arg); one-per-pair, one-level, exact>coercion, охват = все типы вкл. user (extension-форма для orphan); канон call-сайта = голое значение + линт `W_COERCE_EXPLICIT_REDUNDANT` (тот же реестр пар = одно окно); диагностика finalize use-after обязана указывать точку вставки. RETRACT: D55-подсекция «Str-литерал→[]u8» (общее правило покрывает литералы автоматически); name-gated `write`-костыль сносится (закрывает `[M-d55-str-literal-coercion-name-gated]`, нарушение compiler-conventions §3). Коды: E_COERCE_NOT_UNARY / NOT_ZERO_COST / DUPLICATE_PAIR / AMBIGUOUS. **Статус: спека нормативна; реализация — Plan 214 (не начата), до Ф.2 переходно действует write-костыль.** |
| D430 | 04-effects.md | **Проверяемое числовое сужение — `try_to_<T>` чейн-семья + `RangeError`** ([M-numeric-try-narrowing], 2026-07-20, owner-approved backlog item, чистый std-API — компилятор не менялся) — `(300u32).try_to_u8() -> Result[u8, RangeError]`, метод на ИСХОДНОМ значении (владелец: чейн, НЕ Rust-static `u8::try_from`), `try_`+`to_<T>` — тот же принцип именования, что `str.to_i8`/`checked_*`. `RangeError` — новый unit-тип (`std/prelude/errors.nv`, re-exported facade, `PRELUDE_VERSION` 18→19) — НЕ переиспользован `ParseIntError` (str-parse-специфичные варианты `Empty`/`InvalidDigit`/`InvalidRadix` нерелевантны число→число). Реализация — 10 `.nv`-бланкетов (`fn[S Ints] S @try_to_<T>()`, один на целевой тип, `std/prelude/protocols.nv`), НЕ N²=100 ручных методов и НЕ split SignedInts/UnsignedInts на одно имя (нет прецедента) — единый `Ints`-бланкет с sign-agnostic `if @ < 0 {} else {}`-веткой (форма `@saturating_pow`) покрывает оба знака одним телом; каждый бланкет расширяет `@` до full-width `i64`/`u64` ПЕРЕД сравнением с границами цели (soundness — избегает wraparound при касте границы цели ВНИЗ в узкий источник); полная матрица 10×10 (вкл. identity + widening-пары, тривиально `Ok`) — Nova type-sets не имеют width-based исключения, тот же выбор что Rust `TryFrom` numeric impls. D227 Rule 4 не триггерится (негативные константы кастуются только в signed `i64`, никогда в unsigned). Компилятор (`types/mod.rs`/`emit_c.rs`/`lints.rs`) НЕ тронут. Известный НЕ-новый разрыв (не specific для этой волны): `for`-bound receiver для generic type-set-bound бланкета падает в pre-existing `[P67-LEGACY]` — обходится типизированной локалью (тот же класс, что D423/D427 «Известные разрывы»). Тесты — `std/src/math/try_narrowing_test.nv` (не рядом с `protocols.nv` — тот же auto-import-prelude-disabled gap, что `overflow_policy_test.nv`). |
| D431 | 04-effects.md | **`#default_handler(X)` — ambient lazy default-handler factory для эффектов** (Plan 175 Ф.2-v2, 2026-07-21/22) — generic front-end (parser/checker `check_default_handlers`: duplicate/arity/return-type/unknown-effect/cycle) + per-эффект runtime-хук (лениво, once-per-thread, конструирует+ставит `_nova_handler_X` при первом использовании без `with`; явный `with` переопределяет как раньше). `Time` — первый мигрированный эффект (`std/src/time/duration/core.nv` `time_default`, C-хардкод-fallback сохранён как bootstrap-safety-net). Побочный фикс той же волной: `[M-effect-handler-body-record-literal]` CLOSED (handler-literal captures → common closure-capture path, `var_boxed`, без `#define`-макросов) + `[M-spawn-var-boxed-leak]` (var_boxed-изоляция spawn/detach/blocking-тел). Typed-schema retype Time + ambient-retraction (D62 amend) — OPEN, followups `[M-175-time-typed-schema-scalar-bridge]`/`[M-175-time-ambient-retraction]`. |
| D439 | 06-concurrency.md | **`supervised{}` — прямая блокирующая операция в теле без `spawn`** (Plan 221.1 №162, 2026-07-30) — тело `supervised` исполняется inline, на той же coroutine, что вошла в блок; `emit_supervised` больше НЕ переставляет `_nova_active_scope`/`_nova_active_slot` на время исполнения statement'ов тела, поэтому прямой `.accept()`/`sleep` паркуется штатно (раньше — `abort()`: парковка шла на новый scope со старым слотом, `slot < scope->count` при `count == 0`). Отвергнутая альтернатива (self-slot в новом scope) даёт double-resume: внешний водитель coroutine не видит парковку во внутреннем scope — abort сменялся на hang. **Ограничение:** такая блокировка НЕ защищена `timeout:`/`cancel:` этого scope (enforcement живёт в join-цикле, стартующем после тела) — нужна защита, оборачивай в `spawn`. Amends D14/D50/D71/D75. |
| D440 | 03-syntax.md | **FixedArray `@len()` / `@ptr()` аксессоры** (Plan 200 П19, 2026-07-21; **перенумерован из D431 → D440** 2026-07-30, реестр 221.1 №123 — номер D431 занят `#default_handler` в 04-effects.md) — `[N]T` получает зеркало Vec/str-поверхности: `arr.len()` (компайл-тайм `N`) и `arr.ptr()`/`mut`-перегрузка `-> *mut T`. Компилятор-синтез, НЕ `.nv`-декларация (method-level const-generic `N` в языке отсутствует). Amends D27. |

## История

- [history/rejected.md](history/rejected.md) — все отвергнутые альтернативы с причинами.
- [history/evolution.md](history/evolution.md) — как менялись решения по ходу разработки.

## Шаблон D-решения

Каждое D-решение в новых файлах следует единому формату:

```markdown
## DXX. Название

### Что
Одно предложение — суть решения.

### Правило
Подробные правила и примеры current syntax.

### Почему
Обоснование с прецедентами / trade-offs.

### Что отвергнуто
Краткий список альтернатив с причинами отказа.

### Связь
- DXX (зависит / уточняется)
- DYY (родственное)

### Эволюция (если применимо)
Краткая хронология изменений с указанием прежних формулировок.
```

Если решение **отменено** — в начале блока пометка `> ⚠️ ОТМЕНЕНО, см. DZZ`.

## Принципы записи

1. **Только current state** в основном тексте. Ссылки на устаревшие
   формулировки — только через раздел «Эволюция».
2. **Все примеры синтаксически валидны** по текущим правилам. Никаких
   `trait`/`impl`, lowercase эффектов, `:` в типах.
3. **Перекрёстные ссылки внутри `spec/decisions/`** — относительные
   пути (`02-types.md#d17`).
4. **Внешние ссылки** на `syntax.md`, `effects.md` etc. — относительные
   `../syntax.md` (они теперь рядом, в `spec/`).

## Миграция

Старый `decisions.md` в корне репозитория **удалён** после переноса
всех решений в эту директорию (`spec/decisions/`). Все cross-references
в живых документах (`spec/*.md`, `examples/`, `docs/articles/`,
`docs/plans/`, `docs/research/`, `README.md`, `CONTRIBUTING.md`,
`editors/vscode/README.md`) обновлены на новые пути.

См. процесс миграции — [history/evolution.md](history/evolution.md).
