# PROGRESS — план 241, задача «актуализация spec/paradigm.md»

> Предыдущая задача в этом файле (глоссарий spec/GLOSSARY.en.md, ветка
> p241-glossary) закрыта — см. git log PROGRESS.md. Файл переиспользован
> под эскалацию: три захода дешёвого исполнителя по spec/paradigm.md дали
> ноль коммитов. Ветка: `p241-specfix`. Исполнитель: sonnet.

## ГОТОВО

`spec/paradigm.md` актуализирован по разделам, коммит после каждого
раздела (6 содержательных коммитов + 1 фикс структуры fence + 1 шапка):

1. Заголовок документа + «Четыре строительных блока»: `traits + data` →
   `protocols + data`; `trait`/`impl` → `protocol`/структурное
   соответствие без `impl`-блоков (D53, D15).
2. Пример Account: `mut self` → `mut @field`, `throws` → `Fail`,
   `type X = {}` → `type X {}`, бесколонные аннотации (D35, D25/D65,
   D52). Многостроковые тела (`if`-guard'ы) переведены в блок-форму
   `{ }` (D40) — без этого `mut self`→`@`-конверсия оставляла бы
   невалидный старый bare-`=` синтаксис рядом со свежим.
3. «Полиморфизм через protocol»: `trait Printable` → `type Printable
   protocol { }`, `impl Printable for Account` → `fn Account @show()`.
   Блок «Printable для int» и производный от него пример
   `items []Printable` — TODO (см. ниже), не переписаны по догадке.
4. Embed/delegate (AuditedAccount): `use Account` → `use account
   Account` (D39 требует явное alias-имя поля; без имени — ошибка
   компиляции), `self.Account.deposit()` → `@account.deposit()`.
5. Sum-types (Shape) + динамический диспатч: `type Shape = | ...` →
   `type Shape enum | ...` (D406, enum-маркер), `dyn Trait` убран —
   protocol-тип в обычной позиции значения = existential/vtable по
   умолчанию (D53, таблица existential vs universal).
6. Инкапсуляция + таблица «Как в ООП, только…»: `type X = {}` → `type
   X {}`, `trait`→`protocol` по строкам таблицы, снята «номинальная»
   форма (D15: «Нет номинальности»). Строка «Абстрактный класс» — TODO.
7. Фикс структуры: TODO-комментарий для примера int оказался внутри
   ```nova fence (был бы принят за код) — вынесен наружу, блок разбит
   на три отдельных fence'а.
8. Шапка: «⚠️ УСТАРЕЛО» заменена на честную короткую — что сделано, что
   осталось (3 TODO), какие пункты исходного списка расхождений в тексте
   не встречались вовсе.

`spec/overview.md` (Задача 2 из TASK-specfix.md) НЕ трогал — сделано
интегратором заранее (коммит d5199ce7d), подтверждено по git log перед
началом работы.

## TODO-маркеры в файле (3 штуки)

1. `Printable для int` (раздел «Полиморфизм через protocol», строки
   ~99-107) — extension-методы на чужом/примитивном типе запрещены
   (D46, 03-syntax.md#d35 «Receiver» — методы на встроенных типах
   определяются только в stdlib-модулях); `int` для пользовательского
   кода — foreign type, `int @show()` не объявить. Заблокированный код
   оставлен как есть в отдельном fence.
2. `items []Printable` (раздел «Динамический диспатч», строки ~165-169)
   — тот же корень: `42`/`"hello"` должны удовлетворять `Printable`,
   что требует того же запрещённого extension-метода.
3. Строка «Абстрактный класс» в таблице «Как в ООП, только…» (~193-198)
   — протоколы не несут default-методов (D15: «Что отвергнуто:
   Дефолтные методы»), аналога abstract class в языке нет вовсе.

## Пункты исходной шапки, которых не было в теле файла (менять нечего)

- alias через `=` → `type X alias Y` (D52) — конструкции alias в теле
  файла не было вообще.
- `Async`/`Mut`/`Par` как эффекты (D62/D14 REVISED) — не встречались.
- `resume` keyword (D61) — не встречался.
- `to_str(self)`/`ToStr` protocol (D70→D73) — тело уже писало
  `self.to_str()`, что фактически совпадает с ДЕЙСТВУЮЩИМ каноном
  (D410, семейство `to_str()`/`to_*`); упомянутая в шапке замена на
  `From`/`Into` сама успела устареть — протоколы From/Into
  ретрактированы 2026-07-06 (08-runtime.md#d73, шапка ретракции).
  Правка тела не потребовалась.

## Решения вне буквального списка 8 пунктов задания (раскрыты, не скрыты)

Применены только как «неразрывные спутники» уже редактируемых строк —
без них результат содержал бы гибрид старого/нового синтаксиса на одной
и той же строке:
- bare `=` → `=>`/блок-форма `{ }` для тела функции (D23/D40) — только
  там, где строка уже правилась по одному из 8 пунктов.
- match-arm `->` → `=>` (D19) — только в `Shape.area`, тот же блок, что
  и `self`→`@`.
- `dyn Trait` убран (D53, таблица existential/universal) — та же зона
  правки, что и trait/impl.
- `use Account` → `use account Account` — обязательное alias-имя (D39)
  — тот же блок, что и `mut self`→`@`.
- `[AuditEntry]`/`[dyn Printable]` → `[]AuditEntry`/`[]Printable` (D27,
  префиксный `[]T`) — только внутри уже трогаемых строк.
- `let x int = 42` в САМОЙ шапке (устаревший пример из шапки, а не
  предписание менять `let` по всему файлу) → `ro`/`mut` (D184: `let`
  ретрактирован) — только при переписывании самой шапки.

НЕ тронуто (вне решённого набора, вынесено как отдельное наблюдение для
владельца/интегратора, а не как TODO-маркер файла — эти строки не
входят ни в один блок, трогаемый по 8 пунктам задания):
- `pub` (07-modules.md: переименован в `export`) — встречается в разделе
  «Инкапсуляция» и в таблице ООП, изолированно, не тронуто.
- `let` в строке «Singleton» таблицы «Как в ООП, только…» — изолированная
  строка, отдельно от шапки не переписывалась.

## Дисциплина
- Грепа конфликт-маркеров — в каждом коммите одной командой.
- `git add` — только `spec/paradigm.md` поимённо.
- Пуш — сразу после последнего коммита, ветка `p241-specfix`,
  `git push github p241-specfix`. В main не вливать.
