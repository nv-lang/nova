// Plan 175 Ф.1c / D317 — Duration `/ 0` traps (не UB, не silent).
// EXPECT_RUNTIME_PANIC division by zero

module rt.dur_div_zero_traps

import std.time.duration

test "Duration @div(0) → trap" {
    ro d = 10.to_seconds()
    ro _ = d / 0                        // ← trap: division by zero
}
